# 2026-09-16 (session 3) — Build 4 opens: the ability table extended, the representation scope pinned, the mask-cache gate re-read

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **State review** (the user's ask: "the void re-roll skip is next?") — corrected: it landed as an
  ADR-0110 rider (`-searchvoidskip`, default 1, in `AnvilRun`; forkcheck 499/500 on the pin). Both
  repos already pushed (fork `master` = `origin` at `8137d0c41c`; Anvil `main` = `origin`; the
  session-2 "pushes = the user's call" note was stale). Box idle, every ledger run done.
- **Decisions (user):** (1) the order — the table extension → the targets evening on the current
  trunk → the three representation items under ONE re-warm → the allocation head fit on the
  re-warmed trunk → the post-Build-4 read; (2) the representation scope = all three items, no trim;
  (3) run the mask-cache gate re-read in the background. **The finding that reframed (2):** the obs
  already records every stack instance (host, controller, label, targets — `ObsSnapshot`), but the
  encoder consumes exactly one scalar from it, `stack_size` (`encoder/transform.py`) — so
  stack-entry tokens are a STRENGTH item for the network-alone windows (instant speed, the
  opponent's turn; no lookahead there per ADR-0109), not readiness. `sa_emb` is still the
  string-id `nn.Embedding` (`policy/model.py:78`; `state/tokens.py:59` for the schedule tokens);
  Build 3's hash-keyed table is the surfaces' key only. Format-as-features = a Python-side lookup
  from the format id already on every row + a small learned embedding — zero fork delta, inert
  for a single-format run (a one-format external project gains nothing from it; the onboarding
  doc is what helps them, at the documentation pass), rides the same re-warm.
- **The mask-cache gate re-read** (`scripts/build4_maskcache_gate.sh`; launched 14:11 through
  `anvil.runs` as `maskcache-gate`, dir `data/runs/build4-maskcache-gate-w1-g120`, stall 45 min):
  the memo gate's recipe on the pin jar — 120 same-seed games at ONE worker (the 09-14 rule), cache
  OFF vs ON, the e3 build serving, `obs_diff.py`. Verdict rule: the OFF/OFF control at one worker
  is identity, so ANY mask-class first divergence = the ADR-0102 key gap persists → OFF stays.
  Checked before enabling anything: the cache map is a `WeakHashMap` (no retention on search
  copies). The off arm ≈ 95/120 at 14:25 (≈ 8 s/game at one worker).
- **The ability table extended** (ADR-0110's routed step, before any served arm on the merged jar):
  - the re-dump on the pin jar (`data/runs/build4-abilities/abilities-8137d0c41c.jsonl`; run by hand
    from `../forge/forge-gui` — `AnvilRun` needs Forge's `res/` as cwd, the harness's
    `FORGE_GUI_DIR`): 1,701 cards / 5,149 abilities / 0 missing — **the key set identical to the
    riders-tip dump** (the flip tip is harness-side), +16 / −15 vs the pool table as recorded.
  - `scripts/extend_ability_table.py`: the served table `abil-cf2ca6ba-b3s2-qwen3` (15,473 rows)
    copied byte-identical (asserted after the write) + the 16 new keys embedded with the pinned
    Qwen3 (the revision check: 16 base keys re-embedded, cosine min 1.00000) →
    **`data/embeddings/abil-cf2ca6ba-b4-qwen3` (15,489 keys; the base rows keep their row indices,
    so the e3 build's lookups are unchanged)**. The 15 gone keys stay in the table — the banked
    stores of the previous era carry them and the re-warm reads those stores.
  - **`anvil.bridge.server --abilities <stem>`**: overrides the ckpt config's table stem (the
    fleet's `child_argv` forwards it; the served line prints `ability table … (override)`);
    smoked on the e3 build. The reference arm of the targets read serves the b4 table through it →
    no OOV confound between arms.
  - re-pinned: `data/pool/abilities-cf2ca6ba.jsonl` = the pin's dump (the pre-merge dump kept as
    `abilities-cf2ca6ba-pre-merge-23c3d2a85d.jsonl`); the stem defaults in `surface_fit.py`,
    `pay_fit.py`, `build3_label_read.py` → b4. Tests 59 pass (abil / surface / server).
  - Not a quickstart item: the custom-pool path (BC only) has no ability-table step.

- **The mask-cache gate re-read (14:48): FAIL** — 108 / 120 identical at one worker, 12 divergent,
  9 mask-class (`chooseOptionalCosts`, `chooseCardsForConvokeOrImprovise`,
  `chooseSingleStaticAbility`, `chooseSpellAbilityToPlay`), 3 other → the cache stays OFF, the item
  closed for the era (ADR-0110 addendum 14:50).
- **The target surface (fork):** first as the three callback hooks ADR-0109 named
  (`chooseTargetsFor` / `chooseNewTargetsFor` / `chooseTarget`; tip `23b8e36c03`, the enumerate
  test fixed in `9438db1146`) — the 4-game recipe smoke ran clean (178 windows, 126 sub rows, 0
  crashes) but recorded NO target windows: **47 `playTrigger` vs 3 `chooseTargetsFor` — the AI
  targets its triggers inside `doTrigger` (`prepareSingleSa`), never through the callback.** The
  fix (tip `5dd8ab3ede`): `PlayerControllerAi.preparedTrigger(sa)`, a protected no-op hook between
  a trigger's preparation and its play at both sites (`playTrigger`, `orderAndPlaySimultaneousSa`;
  the first fork delta in that file), overridden in `CensusPlayerController`: one TARGET window per
  targeting ability in the prepared chain (`playTriggerTargets`; candidates enumerated with the
  targets cleared as `setupTargets` does, then restored), the heuristic's picks = the natural
  line, a directed / bridged answer re-targets the ability (`Surfaces.retargetPrepared`) — every
  other decision the trigger logic made is kept. The callback hooks stay (copies with new targets,
  `TargetingPlayer`, ChangeTargets). Python: `surf_target` (TASKS 16, the compat pad), the tag
  registered in the featurizer / server; the label launcher gained `ABIL` + `EXTRA_FARGS`;
  `scripts/build4_targets_queue.sh` (smoke → forkcheck) relaunched 14:46 on the retarget jar.

- **ADR-0111 written** (Build 4 opens: the table rule, the scope, the site, the gate switch, the
  three-arm read; two standing rules). The evening's chain armed: the pool `b4-tgtlab` (14:52,
  1,000 games, 24 × 2), `build4-targets-chain` (ingest → b4s table → fit → build `m12-build4-e1` →
  smoke → the read `b4e1` off / on / nogate on `forge-targets-gate.jar`), the forkchecks
  `run-20260916-build4-targets` (running) and `run-20260916-build4-gate` (queued).

- **The representation completions built in the worktree `anvil-wt-b4rep` (branch
  `b4-representation`, `dc6b362`)** while the chain runs on main: format scalars in the globals,
  the additive stack-entry representation (host / target / state tokens through zero-init
  projections — no new tokens, identity-preserving), `cand_ak` + `cand_abil_proj` beside `sa_emb`;
  loader + featurizer + collate emit the fields; the identity contract tested; the worktree's suite
  313 pass. Fork `7343c40d84`: `ak` on recorded stack entries (forkcheck queued third,
  `run-20260916-build4-stackak`, jar `forge-stackak.jar`). The re-warm's recipe is OPEN (plan
  record 15:20: the Build 1 fit re-run vs a continuation fit from tonight's build).

- **The pool landed 17:30** (1,000 games, 2.6 h, 385 g/h; target 3,251 sub rows, spread ≥ 0.02 on
  11.9% — entity-set scale; every directed target answer applied) → `abil-cf2ca6ba-b4s-qwen3`
  (17,431 keys) → the fit chain (17:32). One crash of a NEW class (`StackOverflowError`, game 500)
  — NOT new: the same status in the 09-07 `b2-dzla05arm-s1` arm and three pre-M12 runs (a rare engine recursion class); the one-worker replay played a different game (the micro-batch rule), so no trace; routed to the upstream worklist with the standing class.

- **THE READ (20:04):** on − off +0.00 ± 1.19 (nothing broke; 1,249 target answers, 0 invalid, 0
  crashes), nogate − off −1.19 ± 1.29 → the gate retires on the pre-registered rule (noise floor,
  negative sign; the post-Build-4 read re-measures). The build `m12-build4-e1` serves seven
  surfaces + tuck (pooled target 0.512). **The re-warm recipe decided (user): option 4, the offline
  search distillation** — built next in the representation worktree.

- **The re-warm built and launched 21:00** (`anvil.training.search_distill` in the worktree;
  `build4-rewarm`, ≈ 1 h): the acting rule's distribution as the target (KL on acted windows at
  bar 0.10 / T 0.025; the value term as consistency), init `m12-build4-e1`, ≈ 460K searched windows
  from the dz/dzla arms + the Build 3 pools + the target pool. Baseline on acted windows: KL ≈ 11.

- **`b4-representation` merged into main 21:10** (`8949d58`, 315 tests pass); the re-warm's paired
  read queued (`build4-rewarm-read-queue`: e1 vs rw, 300 per seat, network alone, gate off).

- **The first re-warm COLLAPSED at serve (−46.7 ± 2.1 pp):** a pass-happy policy (pass mass 0.09 →
  0.55) — the policy term shaped only the acted 7% and nothing anchored the rest. Fixed with the
  frozen-teacher KL anchor on un-acted windows (the acting rule's own semantics); relaunched as
  `build4-rewarm2` + the read `b4rw2` (unattended; lands ≈ 01:30). The battery's null-winner bug fixed.

- **The anchored re-warm still loses −18.8 ± 2.1 pp (00:57)** with a sane offline distribution;
  the text path cleared as a cause; hypothesis = the other heads reading the moved trunk. Launched
  01:05: rw2's priority-only read, the frozen-trunk re-warm rw3, its two reads (unattended, ≈ 05:00).

- **rw2's priority-only read −21.4 ± 2.2 (the policy itself; vetoes 27% vs 5.6%) → the ROOT
  CAUSE found in the trainer's join (02:00):** the search row's indices are over the searched
  (non-mana) options and its labels truncated — 12.5% of rows matched (the mana-free slice).
  Fixed to 100%; relaunched as `build4-rewarm4-queue` (rw4 anchored, rw5 frozen trunk, their
  reads; ≈ 07:30 09-17).

- **rw4 (the fixed join) reads −7.3 ± 2.1 (02:39)**; the excess vetoes are `no_shape_fit` ×5 —
  the target decoder's plans on the moved trunk, for the targeted spells the distilled policy now
  prefers. rw5 (trunk frozen) is the test; prediction recorded in the plan. rw4's priority-only read −7.35 ± 2.23 = the full read: the loss is the priority policy + its plans alone.

## Broke / surprised me

- A join that silently matches an eighth of its rows trains a plausible-looking model on a biased
  slice; the trainer now counts and reports the join (the DataLoader workers' counters do not
  reach the main process — that report is a follow-up).

- The first re-warm's held-out numbers were all true and the served policy was broken anyway —
  argmax agreement on acted windows cannot see a distribution collapse on the un-acted ones; the
  eval now carries pass mass + entropy.

- **The fit chain crashed at fold 0** (a CUDA gather assert): `surf_method_emb` lacked the OOV row
  (`n_methods + 1` vs the vocab's OOV id `n_methods`) — the first never-seen surface method name
  (`playTriggerTargets`) tripped it; fixed (+2 rows, compat pad, the evaluate lookup guarded).
- **A Build 3 latent bug found while fixing it:** `surface_fit.load_net` set the net's ability
  table from the ckpt config / module default, not the `--abilities` stem the loaders used → e1–e3
  trained every store-only key on the clamped last row of the 5,148-row table and served the real
  vectors (train / serve mismatch on in-play abilities). Fixed: the net's table = the loaders'.

- `AnvilRun` console modes NPE on the resource bundle from any cwd but Forge's `res/` root — the
  harness always sets it; a by-hand `-abilities` dump must `cd ../forge/forge-gui`. A stray
  `CHRONICLE_BINDER` enum line on stderr = the playable-branch prefs leak (memory), harmless.
- ADR-0109's target-callback premise was wrong for the AI (the census had the counts all along:
  `playTrigger` rows, no `chooseTargetsFor`) — the smoke caught it in four games.
- The first `pytest -k` pass reported a collection error on `test_certify.py` that does not
  reproduce alone (5 pass); not chased.

## Next session picks up (the 09-17 morning)

- **The re-warm line, in one line:** option 4 does not transfer to serve strength in five builds
  (−47 / −19 / −7.3 / −9.0 / −9.2 with the fallback); the mechanism is recorded in ADR-0111's
  04:40 addendum (the search values options under the heuristic's realization; the distilled
  policy over-generalizes "cast instead of pass" into casts the engine's AI itself refuses — 95% of
  the fallback's attempts). Running: rw6 / rw7 (the void options as playability negatives; the
  softer variant) with their reads (`data/runs/build4-rewarm6/queue.log`, ≈ 10:00). **The
  user's call in the morning:** if rw6 / rw7 stay negative, the re-warm is closed as a serve
  candidate (the finding is the deliverable: the with-lookahead advantage partly IS the heuristic's
  planning of the search's picks — a Build 5 risk to design against: the target decoder co-distilled
  on the realized plans, route (b)); the representation completions stay on main at zero-init
  (byte-identical serve) and take their gradient in the loop (option 3).
- The fork tips: `d734937c56` (the veto fallback, flag-gated; forkcheck `run-20260917-build4-vetofb`
  running — if PASS, the pin; the fallback is neutral and stays off) on `7343c40d84`.
- Then: the allocation head's served output; the post-Build-4 read on the served build (e1 or the
  best re-warm if any reads ≥ 0); the documentation pass; the shakedown.

## Next session picks up (as written at 15:00 09-16)

- **Forkcheck PASS 15:50 on the retarget tip `5dd8ab3ede`** (499/500, the standing seed; fidelity = the baseline's).
- **Forkcheck PASS 16:27 on the gate tip `a36975497b` → the fork pin** (499/500, the standing seed).
- **Forkcheck PASS 17:20 on the stack-key tip `7343c40d84` → the fork pin** (499/500; 20260853's fork flip replayed clean = the residual).
- **Pick up:** the re-warm's `data/runs/build4-rewarm/log.json` (the after-eval on acted windows vs the ≈ 11-nat baseline) → the 600-game paired read `m12-build4-rw` vs `m12-build4-e1` (network alone, `build3_surface_read.sh` with two ckpts — needs a two-ckpt arm form) → the merge of `b4-representation` into main (the main tree is idle once the read is not running) → the allocation head's served output → the post-Build-4 read.
- (done) the chain's stages (`run-20260916-build4-{targets,gate,stackak}`) (`data/forkcheck/run-20260916-build4-{targets,gate}/compare.txt` → the pin), the chain's stages (`data/runs/build4-e1/chain.log`: pool-read.md, the fit folds, the smoke, the read `data/runs/build3-surface-read-b4e1/read.json`), then the ADR-0111 addendum with the three-arm verdict (the gate retires or stays), the map's pin + run-ledger rows.
- **The gate's read (DONE 14:48 — FAIL, recorded)** (`data/runs/build4-maskcache-gate-w1-g120/obs-diff.txt`, ≈ 14:50): mask-class
  first divergences > 0 → OFF stays (a line in ADR-0102's record); 0 → the cache may go ON in the
  recipe, a ≥ 3-cell g/h read deciding whether the flag is worth carrying.
- **The targets surface (fork, on `8137d0c41c`):** hooks on `chooseTargetsFor` /
  `chooseNewTargetsFor` / `chooseTarget` in `CensusPlayerController`, a `Surfaces` TARGET kind over
  the engine's legal-target set (min/max from the `TargetRestrictions`), the wire tag
  `mtg.surface.target`, forced on search copies, `SurfaceDirective`; then the forkcheck ∥ the label
  pool (1,000 games, the e3 build serving with `--abilities` b4), the fit on the entity-set decoder
  keyed by `ak` (+ the legal-target count option feature), the three-arm read (served / + targets
  gated / + targets gate OFF; pre-registered in ADR-0109).
- **The re-warm's recipe (decide with the user)**, then merge `b4-representation` into main once the chain's Python stages are done (never mid-run), run the re-warm from the worktree, read it by the Build 1 cells, re-fit the surfaces on the re-warmed trunk. Then the allocation head's served output, the post-Build-4 read, the shakedown.
- (superseded) Then the representation items under one re-warm (+ the Build 1 cells smoke): `ak` on stack
  entries (recording-only) + the encoder's stack tokens; `sa_emb` / `sched_sa_emb` → the b4
  table's vectors through the ability projection; format features from the format id. Then the
  allocation head's served output, the post-Build-4 read, the shakedown.
