# 2026-08-31

## Done

- **The parity witness earned its existence on day one: the first mint
  run is DISCARDED and rerun ([ADR-0088 addendum](../decisions/ADR-0088-grounded-driver.md)).**
  The forensic chain, in order: two lanes tripped the honest-DONE gate
  at ~87–90% rows → the deficit was whole turn-group TAILS clustered by
  game → prefix comparison showed those games diverging at their FIRST
  fork turn, not their last → the full census showed **~97% of ALL
  games diverged** (39/40 lane-2, 37/38 probe1 lane-0), pinned at the
  fork-window turn → the divergence-point census sealed it: **76/76
  divergence points are own-seat cast decisions**
  (`chooseSpellAbilityToPlay` + nested `chooseOptionalCosts`), zero
  opponent decisions, zero other methods.
- **Root cause: serve carry keyed (g, seat) + overlapping store
  ranges.** Probe1's g13 and probe2's g13 are different games sharing a
  carry key; concurrent cross-store replay clobbers carry constantly
  and flips answers exactly where carry is consumed. The solo smoke was
  exact (no collision); generation was always safe (workers take
  disjoint ranges of one store). NOT the GameCopier watch-list class;
  NOT an engine defect; NOT batch-inference jitter (ruled out: the solo
  replay of concurrently-generated games was byte-exact).
- **Fix taken: phased-by-store rerun** (zero code risk — within one
  store lanes partition games disjointly = the generation shape the
  smoke validated). Lanes regenerated 12-per-store, driver runs the
  stores sequentially at concurrency 10; fresh server (the old one's
  carry FIFO held poisoned state); watchd re-registered; session
  monitor re-armed; an early-parity check gates the first hour.
- **Durable fix routed by name (serve hardening): carry keyed by
  connection/channel instead of (g, seat)** — removes the class for
  any future multi-store replay. Standing rule added (never replay
  overlapping-range stores concurrently against one carry-stateful
  server).
- Earlier same-day: two lanes' "dropped turns" accepted (the
  exact-but-truncated parity class coded into the verb) — now
  understood as the SAME defect's milder face (diverged games ending
  early); the acceptance notes were revoked with the run. The class
  stays in the parity verb as a distinct counted outcome.

## Session 2 (user-prompted audit): the similar-issues sweep + the residual hole

The user asked whether the defect suggests siblings — it did, including
one in MY OWN FIX from an hour earlier:

- **Cross-phase stale carry (REAL, fixed):** the phased rerun shared one
  server across phases; SchedServe keys `(g, seat)` and discards stale
  state only when the stored TURN NUMBER differs (`st.turn != turn` →
  fresh) — so a phase-1 probe1-g13 entry whose last turn number matches
  phase-2 probe2-g13's current turn would feed another game's schedule,
  sporadically. Fixed live without touching the running driver: phase-2
  lane scripts (unopened files — safe to edit) got a `serve2.READY`
  sentinel wait, and a detached `phase2-interlock.sh` restarts the
  server at the boundary (release = 12 DONEs OR phase-2 lanes observed
  starting — deadlock-safe when a phase-1 lane fails its gate). The
  durable form is baked into `sched_mint.py`: the driver now owns the
  serve lifecycle, fresh server per phase + serve.pid — which also
  covers the sibling case (lane RERUNS against a warm server).
- **Audited clean:** v1 plan carry INACTIVE this era (ckpt has only
  `assemble.plan_tok`; activation needs `plan_`/`assemble.plan_proj` —
  verified against the predicate); batch-composition logit jitter ruled
  out earlier (solo replay of concurrently-generated games byte-exact).
- **Noted, benign:** `serve-mu.jsonl` mixes both stores' (g,s) rows —
  nothing ingests it; do not ever point training at it.
- **Family observation:** third appearance of the "integer identity
  collides across streams" class (run17 fork-id collision → `-forkns`;
  wire-fork carry exclusion; now serve carry). The routed channel-keyed
  carry hardening closes the family at the server boundary.
- Incident-ledger entry: one more kill-and-relaunch-in-one-call exit
  144 (the heredoc body contained the target name) — rule 3 exists for
  a reason; redone as separate calls.

## Broke / surprised me

- The near-miss chain is worth staring at: without the honest-DONE
  row-count gate (added only after the boot-storm incident), both
  probe2 lanes would have DONE'd silently, `finish` would have minted
  ~900 labels from 97%-poisoned states, the FUND decode leg would have
  re-banked against them, and probe3 would have trained its primary
  decode signal on states the policy never visited — a defect class
  that no downstream gate reads. Guard depth paid for itself twice in
  24 hours.
- The cost of the discard: ~285k rolls (~18 h × 10 lanes of nice-19
  compute). The phased rerun costs the same wall again (~27 h).

## Next session picks up

1. **Early-parity gate** (~1 h into phase 1): run the divergence census
   on completed phase-1 frames — EXACT expected; any divergence within
   a single-store phase is a NEW mechanism and stops the run.
2. Then the standing pipeline: lanes finish (~27 h) → `parity` →
   `finish` → yield read (~900–1,000 labels expected; <750 ⇒ top-up
   path) → seedlab day-zero re-bank → `launch_m10_probe3.sh`.
3. The serve-hardening item (channel-keyed carry) is queued by name —
   schedule at the next scoping session, NOT mid-mint.
4. Watchd: `sched-mint-lanes`/`sched-mint-serve` re-registered with the
   rerun's pids; same spurious-GONE-at-completion caveat as before.
