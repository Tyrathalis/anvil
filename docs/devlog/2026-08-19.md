# 2026-08-19 — M9 design session: the interface round designed and OPENED

## Done

- **State review for the design session:** ADR-0062 routing + the
  M9-seed devlog framing + ADR-0042's priority note + design-doc
  §3a/§3c + the callback census, plus two implementation facts
  confirmed in the fork: `PlayerControllerAnvil` does NOT override
  `payManaCost` (inherits `ComputerUtilMana` auto-payment via the AI
  controller), and the consequential-payment flag doesn't exist
  engine-side (`specifyManaCombo`/`applyManaToCost` never fired in the
  census). `payManaCost` is ~120 calls/game — the flag is what keeps
  the 2.6% bridge tax alive, not just design elegance.
- **Training-signal question resolved (user + assistant convergence):**
  straight RL over `{auto} ∪ payment-classes` with **auto-biased
  init** — the design doc's "auto-payment as the default action"
  taken literally as a distinguished action. Day-zero behavior
  bit-identical to today (BC's safety) with no imitation of the
  heuristic's payment choices (BC's teacher dropped). Key argument:
  the head fires only in consequential windows, which is exactly
  where the auto-payer is least trustworthy — user named the two
  blind-spot families (chained-activation payments ComputerUtilMana
  can't construct; non-mana tap residuals: dork-as-blocker,
  attacker-vs-mana). Known failure mode (collapse to always-auto on
  sparse deviation reward) is why D4 reads drills + deviation
  telemetry, not strength.
- **The trap found at design:** the chained-activation blind spot is
  an INTERFACE problem, not a training problem — if class enumeration
  is built on the auto-payer's machinery, those payments never appear
  as options and no training signal can reach them. Pinned as a build
  requirement: **enumeration must be legality-derived**, and D3 opens
  with an engine capability audit (can the engine *execute* a
  directed chained-activation payment at all; a "no" re-prices the
  boundary before commitment).
- **Boundary decision (user):** ONE boundary event — the §3c fork
  delta rides the queued next-boundary bundle (rebase + multi-format
  + store-namespace fix + forensics), **held until the engine changes
  are ready**; D1/D2 run on the current era. The 2-arm forced-seq
  campaign trim flagged likely MOOT (belongs to the retired act−hold
  campaign) — land or retire explicitly at the boundary.
- **D2 affordability probe funded (user):** frozen-trunk probe first
  (is affordability predictable from `[STATE]`? labels free from
  engine adjudications), aux-head-in-loop second — the minimal
  "payment-aware," mechanism evidence before any Java.
- **[m9-plan.md](../design/m9-plan.md) DRAFTED and OPENED
  (user-approved):** ladder D1 knowability decomposition → D2a/D2b →
  D3 build (three strict rungs; payment drill evalset BEFORE any
  run — dork-as-blocker / wrong-color-tap / chained-activation
  scenarios, mined + ddmin-certified hand-constructed fallback) → D4
  ~5-iter learning-signal probe → D5 full run with three attributions
  (strength / knowable-veto collapse with argmax-vs-sampled split,
  falsification first-class / throughput dividend instrumented at
  identical chunking) → D6 §3a tier-2 plan latent, own read. Gates
  are PIN-AT-DESIGN slots per deliverable. Out: tier-3 search
  (critic-leaf 0.42-vs-0.94, unpark conditions named), **§3b
  learnable stops = named the M10 candidate**, deterrence family,
  pool growth beyond the bundle.
- **Docs swept:** CLAUDE.md M9 OPEN bullet; map (pills — M8 open pill
  was stale from the close, fixed —, start-here, Now card → M9/D1,
  watch-list boundary-bundle re-scope + rising-priorities line
  resolved into M9, new M9 `<details open>` block, footer stamp).
  Also fixed en route: the Now card's stale "M7 kill list pending"
  note (executed + verified 08-17) → replaced with the live M8 ~5.7G
  kill-list pending note.

## Broke / gotchas

- Nothing broke — docs-only session. The one near-miss worth
  recording: the map's header pill row still said "M8 open" after the
  M8 close sweep (the ADR-0062 commit swept everything else) — pill
  rows are easy to miss in the close checklist.

## Next session picks up

1. **D1 — veto knowability decomposition:** instrument design session
   (pin the knowable-fraction gate + the classification method), then
   run it over the run17 + baseline-era stores. No generation, no
   training.
2. User executes the ADR-0062 kill list (~5.7G) if not yet done.
3. On the bench behind D1: D2a probe design (frozen-trunk
   affordability, label extraction from logged adjudications).
