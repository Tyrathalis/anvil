# 2026-08-26 session 2 (the probes session — v2 target probe RESOLVED, M11 probes spec adjudicated, engine deltas BUILT + PROVEN)

## Done

- **The v2 offline target probe: spec drafted, adjudicated, built, run,
  and RESOLVED in one session
  ([ADR-0079](../decisions/ADR-0079-v2-target-probe.md)).**
  - Pre-registration
    ([m10-v2-target-probe-spec.md](../design/m10-v2-target-probe-spec.md))
    adjudicated on four forks (user, all on the recorded leans): E/R
    population = m9-rebaselinearm s0+s1; R gates on the running-ledger
    regression axes (afford-bit structurally unsupported — negatives
    live in the census sidecar / Target F); F gates on realize-AUC
    alone (degrade-slot report-only); ADR-0074 gate numerics verbatim.
    Spec + instrument (`scripts/v2_target_probe.py`) committed
    PRE-DATA at `1a56dee`.
  - Implementation truths recorded pre-data: the MAIN1 fork-consistent
    window rule for all three targets (refines the draft's looser
    wording; off-turn groups drop out by construction); obs sv=2 has
    no mana-pool field ⇒ E floating axis recorded unavailable, never
    fabricated; the schedfile IS the arm record (no arm re-derivation);
    rolls aggregated, never per-roll rows.
  - Dumps clean: 22,224 own-turn groups + 38,948 R slots (1.75
    casts/turn — the census mean exactly; 1 slot dropped), 600/600
    fork windows matched for F, 7,950 (turn, arm) rows (584 hold-alls
    excluded, 189 crash rolls skipped, ~240 arms without outcomes).
  - **Verdicts: E PASS 0.3374→0.5438 (+0.206, 4.1× margin); R PASS
    0.5826→0.6490 (+0.066); F FAIL 0.8073→0.8048 (−0.003 trunk
    increment).** The [PLAN]-slot dividend replicates on resource axes
    (E +0.036, R +0.017 over [STATE] alone).
  - **Birth aux roster = E+R; F's pre-registered fail path invoked**:
    validity predicate = R + the veto-knowability splitter; penalty
    revisit folded into the numerics session; a nonlinear/interaction
    F re-probe is a NAMED future instrument against the banked 96k-row
    label mint, never a silent reroute. The F reading recorded with
    the verdict: cheap afford-arithmetic alone hits 0.807 AUC — h2
    feasibility is largely knowable without the trunk, which
    REASSURES the knowability-gated penalty design.
- **M11-routing probes spec drafted + ADJUDICATED
  ([m11-routing-probes-spec.md](../design/m11-routing-probes-spec.md)),
  grounded by a fresh-era rate census** (census.jsonl sweep, 500
  games): effect=true payManaCost 50.12/g (the plan's ~54/g
  confirmed); tutor class = chooseSingleEntityForEffect 5.87/g +
  chooseSingleCardForZoneChange 5.79/g pre-filter. Four forks (user,
  all on leans): both SELECT_ONE classes for T; decline-legal forcing
  only for P; routing bar PINNED at the ADR-0078 funding-threshold
  scale (≈2.25pp/game gate-scale ⇒ schedule into M11, below ⇒
  re-defer with the number); engine deltas (`-forcechoice` +
  pay/decline override) built in ONE Java session after this probe,
  CPU mining rungs in between. Select/score roll split + mining-rung-
  first discipline pinned for both.

- **Both CPU mining rungs BUILT + RUN** (`scripts/m11_mining.py`
  committed pre-run at `9a1e2de` → `data/runs/m11-mining/`; results in
  the spec's mining-rung section):
  - **Probe T live and well-sized:** tutor_fetch 0.784/seat-game (744
    forceable, 92% optional, fetchland-dominated, candidate tail to
    10+ ⇒ consider name-dedup for the k pin) + dig 0.336/seat-game ≈
    **1.08/seat-game in-family forceable — 3.4× the payment window
    rate**; the routing bar needs ≈+2pp/window, an open question the
    forcing rung answers. The `other` stratum (4.7/seat-game
    targeting/untap noise) correctly excluded by the classifier.
  - **Probe P telemetry-blocked:** knowable decline-legal lower bound
    = 0.36/seat-game vs 25.06 raw; empty-sa (18.6, MAIN1+CLEANUP
    clusters) + `[Phase:]` rows (4.4) unresolvable — prompt always
    null, no isCancellable/source fields. **The engine-delta session
    gains a third item: extend the census payManaCost emission**
    (isCancellable + source + effect class); the true decline-legal
    rate sits anywhere in [0.36, 25.1]/seat-game, and the plan's
    ~54/g raw figure was never the pay-or-suffer rate.

- **The Java session ran SAME-DAY — all three deltas built, proven,
  landed in one fork touch (`07c28fcf8a`), spec's engine-delta section
  carries the full record:**
  - Archaeology first, and it PAID: the decline-legal universe was
    never at `payManaCost` — **`payCostToPreventEffect` is its own
    census-wrapped callback (1.66/seat-game, decline-legal BY
    CONSTRUCTION — the UnlessCost path)**; the cancellability-threading
    question dissolved, and probe P's forcing point moved there. The
    "telemetry-blocked" verdict thereby SHRANK to an attribution gap
    (empty sa strings), closed by the src/api enrichment.
  - `ChoiceDirective` + AnvilRun `-forcechoice` mode (the
    ScheduleDirective/doSchedRollouts idiom verbatim; family regex =
    the mining classifier, so forced universe ≡ mined universe);
    `forcePrevent` bypasses `willPayUnlessCost` via the
    PlayerControllerAi payment path; census `FORCE_OVERRIDES` emission
    added to the generator (regeneration surgical — only the three
    methods changed).
  - **ADR-0025 proof via the banked-smoke trick** (the old jar was
    truncated by the build before it could be copied — the banked
    era-jar mechanical-smoke outputs stood in): mined schedfile
    identical, 72/72 labels rows identical modulo ms, census clean
    modulo the three declared diff classes (src/api kvs, one `d`
    stack-frame bump on the helper-routed method, end-row ms). The
    `d` field = `Thread.getStackTrace().length` — REC_OVERRIDE
    helpers bump it by one; worth remembering for future cross-jar
    census diffs.
  - **`choice_smoke.py` PASSED** (schedule_smoke genre): distinct
    candidates forced by index at the same window, idx_oob
    fired-with-miss, pay/decline forcing, rollSeed pairing, natural
    purity, re-run byte-determinism. Window recurrence post-fork is
    stochastic per roll as designed — `fired:false` rows are the
    coverage instrument.

## Broke / surprised me

- **F failing while E and R pass decisively is the session's real
  finding**: the trunk knows the resource STATE (E) and anticipates
  the running ledger (R) but adds nothing to feasibility-of-a-given-
  schedule beyond capacity-vs-demand arithmetic — either a linear-
  probe interaction blindspot or a true representation gap;
  deliberately not adjudicated, both accounts recorded in ADR-0079.
  The pinned instrument did its job: the head does not ship on an
  unverified premise.
- One sklearn ill-conditioned ridge warning at the smallest alpha (CV
  selects larger) — same recorded noise as R1.

- **The forcing rungs PRE-REGISTERED AND LAUNCHED (same session,
  continuing):**
  - Launch pins adjudicated (user): **EXHAUSTIVE** (both universes are
    small — T 796 / P 1,333 active-player-forkable points, measured
    with the corrected active-player proxy after the first proxy
    marked 100% forkable — both seats hold MAIN1 windows; the fix:
    active = holder of the FIRST MAIN1 cast window) with
    **pause/resume first-class** (user constraint: no 34h unbroken
    quiet-box): rollSeeds are point-keyed so `choice_plan.py resume`
    (drop complete points, rotate outs) reproduces identical rolls.
  - Package committed PRE-DATA at `5c0b307`: `choice_pins.py` (K=8
    split 0–3/4–7, T cap 6, fired-pairing ≥2, h=0, routing bar
    2.2/1.1, rates 1.078/1.656) + `choice_plan.py` (one combined
    exhaustive plan — 1,946 points, 6,718 arms, 69,312 completions;
    T+P share natural rolls on 183 collisions) + `choice_read.py`
    (paired best-vs-natural, game-cluster bootstrap, gate-scale +
    coverage-discounted row, crash/unended asymmetry health flag —
    the quiet-box concern converted into a measured guard).
  - **LAUNCHED ~22:45**: iter-019 argmax server on :50065, lane-0
    smoke verified under the real bridge (arm 1 forced Badlands, arm
    2 Blood Crypt at the same ncand-4 fetch window, paired rollseed,
    game-end rows ~6–15s), then all 12 lanes via the detached driver
    (`run-rest.sh` — waits, auto-runs the read, notifies,
    unregisters). Coverage armed: watchd on driver (90m) + server
    (240m), session monitor (2h progress + death alerts), resume
    path documented. First pace check projects ~13–15h aggregate,
    conservative bound ~34h.

## Next session picks up

- **The probe read lands via notify** when the lanes drain (or after
  pause/resume cycles): `choice-read.json` carries both routing
  verdicts + health; the session that reads it writes the
  routing ADR (T schedules-or-defers, P satisfies ADR-0077's
  re-deferral condition either way) and the design-round closeout
  ADR (done-when 1) alongside.
- **Numerics pinning session** (kill/FUND/unmask + read-protocol pin
  + penalty-revisit confirmation) at the M10 training-probe launch,
  day-zero banked first.
- Watch (carried): mainline draw-clock both-win wart — route the fix
  with the M10 label work.
- **Numerics pinning session** (kill/FUND/unmask thresholds +
  read-protocol pin + penalty-revisit confirmation) at training-probe
  launch, day-zero banked first.
- Design-round closeout ADR after the two ceiling probes resolve
  (done-when 1).
- Watch (carried): mainline draw-clock both-win wart — route the fix
  with the M10 label work.
