# 2026-07-30 (session 3) — security-track cleanup: playable consolidated onto upstream's fixes, fork repo tidied

## Done

- **Upstream presence confirmed for everything the track produced.** All three
  advisory merges are on `Card-Forge/forge` master (`3e01055378`,
  `49e4bca114`, `2df8aaab07`); chat rate limiting
  ([#11457](https://github.com/Card-Forge/forge/pull/11457)) and the
  determinism hooks ([#11285](https://github.com/Card-Forge/forge/pull/11285))
  remain open PRs, both backed by branches on the Tyrathalis fork remote.
- **The playable branch is consolidated and current — `origin/playable` @
  `2900701083`.** Two merges: first `origin/playable` (the public QoL/updater
  stack; conflict-free, shared base `5fbc2ac98d` exactly as recorded), then
  current `upstream/master` — which brings the three merged advisories in
  upstream-side. Conflict resolution followed the ownership map: upstream won
  where an advisory merged (`UpdateLobbyPlayerEvent` unmodifiable getter,
  `Assets.java` where upstream's #11424 restored RGBA8888 card textures
  off-iOS — its comment describes the resolved code); ours won where the
  content is still only ours (chat rate limit + chat length bound for #11457,
  and both **declined patches**: reconnect capability in
  `RemoteClient`/`FServerManager`, host-side validation — plus our superset
  security tests). The connive/recruit mutable-map fork-local fix survived
  untouched (upstream never fixed it; both sites still wrap `new HashMap<>`,
  `ConniveDiscardMapTest` present). Gate: shipping jar builds; targeted suite
  (security + QoL regression: AbuseLimits, LobbySlotAuthorization,
  PreAuthDisclosure, ReconnectCapability, ConniveDiscardMap, DeltaUpdate,
  DeckSiteSync, RotatedRect, StorageSubfolder) **30 run / 0 fail / 5 skipped**
  (the gated networked ones, by design). Pushed as a fast-forward — **the next
  release cut from `playable` is the first that ships the security work to the
  friends' builds.**
- **Anvil main pushed** (14 commits, including the hold-back release:
  `multiplayer-hardening.md`, devlogs, map, status). Pre-push hooks confirmed
  retired on both repos before pushing.
- **Fork repo tidied to exactly two branches and two worktrees.** Removed
  worktrees `forge-chat`/`forge-determinism`/`forge-fcheck`/`forge-pr`/
  `forge-pub`/`forge-sec` (all clean); deleted 16 local branches (7 `sec/*`,
  2 `security/*`, `chat-anti-spam`, `determinism-hooks`, `determinism-fcheck`,
  `gamecopier-fidelity-fixes`, `test-11161`, `playable-qol`, `playable-pub`);
  removed the closed `advisory` remote; deleted remote
  `origin/gamecopier-fidelity-fixes` (PR #11203 merged 07-12). Every deletion
  was preceded by a reachability check: PR branches live on `origin`
  (`chat-anti-spam`, `determinism-hooks` — kept remote-side), `test-11161`
  stays archived as `origin/test-11161`, the pre-rebase Anvil stack is held by
  tag `pre-rebase-20260725`, and the superseded security drafts' content lives
  on in upstream's merges and `playable`'s reworked stack. End state:
  `../forge` on pinned `master` (research fork, untouched), `../forge-play` on
  `playable`.

## Broke / gotchas

- `mvn -pl forge-gui-desktop test` without `-am` cannot resolve
  `${revision}`-versioned reactor artifacts — run targeted tests with `-am`.
  And modern surefire wants `-Dsurefire.failIfNoSpecifiedTests=false`, not
  `-DfailIfNoTests=false`.
- `playable-qol`'s "32 unique commits" were a scare that patch-id dissolved:
  every QoL commit was patch-identical on `origin/playable`; its only real
  uniques were pre-rework security drafts, superseded on `playable`.

## Next session picks up

- First real in-app update pass rides the **next** playable release (needs a
  publish cut from the consolidated branch — `scripts/release-playable.sh`).
- Track residue unchanged: T2 rescale (queued; workaround = launch at play
  size), first live bulk sync + synced-deck game.
- Watches: #11457 and #11285 reviews; M4 D3 next step (fork-session ingest +
  `drill` provenance tag + sampled+μ backend → first drill-mixed run).
