# 2026-07-17 (session 3) — M3 planning + the community thread catches fire

The dedicated fresh-context M3 planning session ADR-0020 called for — plus
the #ai-plotting follow-through, which ended up shaping the plan in real
time.

## Done

- **Worktree cleanup**: the Discord-exploration worktree
  (`suspicious-bhaskara`) was fully merged — removed it and its branch.
  `forge-pr1` remains (intentional).
- **#ai-plotting replies posted** (user's voice, drafted here): khaliostr
  (steer joint-issue → draft PR, determinism-PR scope, SVar flag) and the
  thread outcome exceeded the plan — **Hanmac filed
  [forge#11260](https://github.com/Card-Forge/forge/issues/11260)** ("Move
  nextId statics into Game methods", self-assigned + tool4ever +
  MostCromulent) within hours. Our design-input comment posted same day:
  sibling-statics enumeration (SpellAbility, SpellAbilityStackInstance,
  StaticAbility, ReplacementEffect, Trigger, CombatView), the
  **counter-must-travel-on-copy/snapshot constraint** (from #11203 id
  preservation — the collision risk nobody had named), single-shared-counter
  alternative, forkcheck verification offer.
- **manabrew's Fireball SVar claim probed — does NOT reproduce.**
  `FireballRaiseCostTest` (fork `023e8c5da9`, sim-test, passes on stock
  script): the per-extra-target raise applies correctly (X=2: 1 target CMC
  3, 2 targets CMC 4); `CostAdjustment.java:~155`'s collision guard
  pre-resolves the SVar from the static as designed; script unchanged
  upstream for years. Likely their harness's own payment path or a
  misdiagnosis. Excluded from the joint PR (worklist updated, Anvil
  `ecdc9e9`) — sets the every-upstream-claim-carries-a-test precedent.
- **M3 planned and opened**: [m3-plan.md](../design/m3-plan.md) +
  [ADR-0021](../decisions/ADR-0021-m3-opening-sequence.md). User decisions:
  **split milestone, RL spine dominant** / **#11161 rebase = closing
  dataset-boundary event** (fork pinned through all M3 RL runs; pre-shuffle
  sort lands fork-side only at the boundary) / **Grindstone = flex slot**.
  Spine: D1 commander-tax fix + first-attempt-veto metric (user lean:
  introduce the feature early) → D2 guarded scaling (reserve levers ordered:
  counterspell-hold examination → §3d veto penalty → per-head entropy
  floors). Parallel D3 upstream track at community pace. Done-when #2 =
  beat iter-012 outside noise at the 2,000-game corrected read (user: no
  absolute-winrate bar; restructuring along the way is expected).
- CLAUDE.md Status (M3 OPEN bullet) + project-map sweep (M3 entry, D6/Ante/
  Grindstone pills, loop label, ADR-0021 row, footer).

## Broke / surprised me

- Nothing broke. The surprise was positive: the determinism centerpiece
  went maintainer-led within a day of the introduction post — the "steer
  toward a draft PR" advice was mooted by Hanmac just... filing the issue
  and assigning three maintainers. Our authored surface narrows to
  ThreadLocal MyRandom + pre-shuffle sort + tests.
- Transient: the Bash permission classifier was briefly unavailable at
  session start (retry worked).

## Next session picks up

- **M3 D1**: commander-tax derivability check against the obs stream, then
  featurizer/loader + `load_compat` shim; first-attempt veto metric into
  monitor.jsonl + census. VRAM elasticity (task #12) before the next long
  run.
- **D3 watches**: #11260 activity (verify any branch with forkcheck),
  khaliostr's reply on PR logistics, #445 review when there's a quiet slot.
- Flex: iter-013 forensics dive is done (fed the plan); Grindstone template
  extraction if the spine tracks.
