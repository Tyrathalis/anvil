# 2026-08-21 — session 6 (playable fork: v16 upstream sync)

## Done

- **Playable v16 PUBLISHED** (`291a866fa3`, `2.0.15-SNAPSHOT-08.22`, desktop +
  Android together): merged `upstream/master` `23c3d2a85d` into `playable` —
  218 upstream commits, and notably the same engine commit the research fork's
  M9 boundary bundle rebased onto, so both tracks now sit on the same upstream
  base. Upstream bumped versionCode 2.0.14→2.0.15, so the strictly-newer update
  gate clears without any fork-side stamp fiddling.
- **Merge was nearly free — the v14 fixes are upstream now.** 6 conflict hunks
  in 4 files, every one resolved to upstream's side: FTextField commit-only
  CHANGE + PlayerPanel team-echo guards landed upstream verbatim (rewrapped
  comments were the whole conflict), and upstream deliberately removed the iOS
  RGB565 card-texture downgrade + the Documents/cache AssetManager bypass
  (`640f4eede5`, `20f07a3441`) — took the removal; the fork's
  mipmaps-for-rotated-cards concern survives via `getTextureFilter()` on every
  platform we ship (upstream's iOS-only path is Linear, but we don't ship iOS).
- **Full post-publish routine passed:** published jar sha256 = local (GH asset
  digest `9e706c29…`), version/build stamps match, published `manifest.txt`
  parses through the shipped jar's own `DeltaManifest.parse` (55,003 entries,
  commit `291a866fa3` — pushed), res raw-fetch spot-check hash-matches at the
  manifest commit. Android APK signed [v2,v3] with the same debug keystore;
  the stale `forge-android-2.0.14-…` release asset deleted.
- **Release-pipeline bug found twice in one session, fixed (`2dcb0bdfef`):**
  both `release-playable.sh` and `build-android.sh` selected build artifacts
  by alphabetical glob (`ls … | head -1`); with two version eras in `target/`
  the stale 2.0.14 jar/apk sorts before fresh 2.0.15 — the desktop one was
  caught pre-assembly, but the Android script actually signed and staged the
  Aug-2 APK before the filename/version mismatch gave it away. Both scripts
  now pick newest-by-mtime.
- Docs swept: worklist v16 entry, CLAUDE.md playable bullet re-pinned,
  project-map (Now card, release row, footer stamp — footer was stale from
  08-12; research sessions since hadn't swept it).

## Broke / surprised me

- The stale-artifact glob bit **twice** — same defect class, both scripts,
  first upstream versionCode bump since the scripts were written. The Android
  instance was the dangerous one: it signed the wrong APK and the only tell
  was the filename version disagreeing with the parenthesized version in the
  script's own log line.
- CLAUDE.md's playable bullet was two releases stale (said v14/08-02; v15
  shipped 08-07 with whose-action tracking + the Android coexistence build) —
  the check-git-log-before-building habit caught it before any work was
  duplicated.

## Next session picks up

- Interactive pass on v16 owed at next game night (v15's whose-action
  indicator pass is still owed too — same night can cover both).
- Research track: M9 D4 recipe session (unchanged; this session touched only
  the playable branch — research fork master untouched).
