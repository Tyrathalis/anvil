# 2026-09-06 (session 3) — the M12 scoping session; ADR-0101 ACCEPTED with addendum

## Done

- **Reviewed the rechartered plan** ([m12-plan](../design/m12-plan.md)) against
  [ADR-0101](../decisions/ADR-0101-architecture-review-m12-recharter.md): coherent, not
  restructured; what was missing was arithmetic and about eight pins. The arithmetic: 300K games
  at 800–1,600 g/h is 8–16 days flag-off; a leaf evaluation is a copy + every intermediate
  decision + one value call, so the search multiplier (budget × decisions-per-leaf) was never
  priced — ×3 is 3–7 weeks, ×10 is 2.5–5 months.
- **Pinned with the user (all proposals accepted):** Build 5 envelope = four to six weeks of
  unattended box time, games × budget derived; the value head inside the shared trunk from
  Build 1, so the day-zero read has four arms (`iter-019` alone / Build 1 ckpt alone /
  + lookahead / heuristic + lookahead); Build 1 GO one-ply Spearman ≥ 0.35 and/or state-ranking
  ≥ 0.50, KILL if neither clears 0.32; day-zero in-band (0, 1.5) proceeds to Build 3 but blocks
  the launch until a post-Build-4 read ≥ +1.5pp; control arm within 1.0pp = "the value head
  carries it," not a kill; fork A = the acting seat's next quiescent window, intermediates
  greedy both seats, budget unit = network forward calls; fork G = no Pauper in M12 (pool dir
  is a flex list; depends on Build 4); fork H = Android ship of `iter-019` DURING Build 5 (no
  ONNX in the tree; featurizer needs a Java port).
- **Fork I (new, user-raised): full multi-format readiness lands this round**, second-format
  training decided later. Surveyed what is Commander-bound: the ruleset is already a flag
  (`AnvilRun -f`; Forge has a Pauper deck format under Constructed; one commander-player branch),
  the pool pipeline is generic (`anvil.pool.pauper`), the Python schema clips life to [−10, 150]
  and uses relative race features, cards are text-embedded. Missing: format-as-features (design
  §2, never emitted), the pool-derived `sa_vocab` (ADR-0012; Build 4 retires it with a
  text-hash-keyed cache), explicit format/pool ids on rows + per-pool `CURRENT`, and the
  per-format asset list (pairs/fixed population, Ante cert, ladder anchor, calibration maps) →
  a recipe doc written with the Build 4 format block.
- **Two plan amendments:** Build 4½ shakedown run (~20–30K games, every flag on: the lr 1e-5 /
  KL 0.06 / replay 4 settings were tuned for sparse PG and dense distillation turns the KL guard
  into a brake; also the slope for the power statement) and the post-Build-4 re-warm on banked
  labels as a named step (`sa_emb` at `model.py:77` is a learned 64-dim input path that the
  text projection re-initializes).
- Docs: ADR-0101 status → ACCEPTED + addendum (12 items, two standing rules); m12-plan (title,
  header, Build 0/1/2/4/4½/5, forks A/G/H/I, done-when, kills, routed, running record);
  standing-rules +2; CLAUDE.md Now bullet; design doc §13 M12 clause; map (pills, Now card,
  M12 block, ADR row, footer); this devlog.

## Broke / surprised me

- The plan demanded ≥ 300K games and named throughput as binding, yet nowhere priced the run;
  "evaluations" as the budget unit was ambiguous between leaf count and forward calls, and the
  difference is the whole cost model.
- Multi-format readiness is mostly already there; the one representation item (format-as-
  features) has been in design §2 since the start and was never emitted.

## Next session picks up

1. **Build 0 opens in the fork** (`../forge`, research pin `23c3d2a85d` / bundle tip
   `2f87180cdf`): exact payability filter first (the M9 `PaymentEnumerator` as a candidate
   filter; the veto tripline should drop to ~0), then game-time/repetition caps with cap-aware
   reward, then the budgeted directive with forward-call + leaf-evaluation telemetry, the surface
   enumerators, and format id + pool id on every row. One boundary, forkcheck-proven flag-off.
2. **Build 1 in parallel** on the 106K drill-fork games, the cl2 forks store and the
   harvest/mint composites: rank/regression loss for the masked head inside the shared trunk;
   read by the ADR-0098 cells against the pinned bars.
3. Housekeeping unchanged: the recording jar's ADR-0025 proof before any jar generates a training
   store; cl2 forks store kept through Build 1.
