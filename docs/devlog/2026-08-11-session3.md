# 2026-08-11 session 3 (D3 stability pass: two crash classes killed, one stale, one bounded)

## Done

- **MinMaxBlocker realizer gap: ALREADY FIXED — the carried list was
  stale.** Landed 2026-07-25 (fork `ca401b60b3` + `MenaceBlockRealizerTest`),
  copied forward as "carried" by ADR-0033 anyway. Verified on master.
  (check-git-log-before-building-queued-work, again.)
- **MayPlay `.get(0)` copy-crash class FIXED (fork `b361dfcb8f`).**
  The documented ordering hypothesis was wrong (remembered wiring
  precedes the copy's checkStateEffects); real cause = `copyGameState`
  restored `effectSource` links ONLY for battlefield cards, so
  command-zone Effect cards (the Prepared mechanic's
  `MayPlayPlayer$ EffectSourceController` — Emeritus/Skycoach cards in 6
  of the 16 repro decks) resolved `findEffectRoot` null in copies.
  Fix: restore the link across the full cardMap. No resolution-site
  guard (blind-guard risk stands). `EffectSourceCopyTest` fail-first
  (exact field IndexOOB) → green; full desktop suite green.
- **The carried normal-game IndexOOB: a DIFFERENT class, also FIXED
  (fork `9f0a2c0886`).** Traced replay (server + worker reconstruction
  of run13-finalarm-s0 g351, `-Danvil.crash.trace`) captured the
  never-captured stack: `ComputerUtil.chooseTapType` applies the STATION
  power filter AFTER the size guard, then indexes past the shrunk list.
  dc-863943's correlation = Synthesizer Labship + Krang's 0-power
  artifact boards. Fix: re-check size post-filter → null (clean payment
  decline). `StationTapCostTest` fail-first pair; g351 now plays to
  completion (won t15 vs crash t7). MayPlay-same-class hypothesis
  falsified (reproduced on the fixed jar; normal games never copy).
  Clean upstream candidate.
- **Targeting-retry wedge: CLOSED AS BOUNDED** (upstream-worklist
  disposition note). Era-exact repro unreachable post-ADR-0052 (argmax
  mainline replay retired; g734 re-play drifts and ends turn 27).
  Watchdog question answered by code reading: `isGameOver` IS checked
  per mainLoopStep; the wedge lives inside a single controller/resolve
  call. Guards keep it bounded + visible; watch-list trigger = any wedge
  row in the corrected-era campaign (re-open with crash.trace armed).
- **Forced-seq labels-row extension SHIPPED (fork `b3b33f153a`,
  ADR-0054 pin 1):** SeqDirective captures the act arm's first realized
  cast as `Census.str` (the sa_vocab basis); rows emit `act_first`
  counts + `act_none` + `act_first_modal`/`act_first_agree` over the
  triple filter. Smoked on the seqsmoke2 recipe (g54: modal "Play land",
  agreement 1.0, valid JSON).
- Crash-telemetry survey en route: mainline crash rate ~0.1% across
  July–Aug (NPE 115 / Timeout 112 / IndexOOB 69 in ~260k games); the
  "~12%" is ADR-0040's re-launch waste, not a crash rate (docs agent
  correction).

## Broke / surprised me

- Two of the four "carried" items were not what the docs said: one was
  already fixed (stale carry), one had a wrong root-cause hypothesis
  recorded as likely. The stability pass was really a diagnosis pass.
- The seqsmoke2 `cmd.txt` is per-WORKER: the drilled games (54, 96) ran
  in inv-0003/0005 with different ranges — reproducing from inv-0000's
  cmd alone drills nothing.
- `mvn -pl forge-gui-desktop test` without `-am` can't resolve sibling
  modules (`${revision}` pom); the `-q` flag also hides surefire
  summaries — read target/surefire-reports/*.txt, and a compound
  command's trailing grep sets the exit code (false "suite failed").

## Next session picks up

1. **D3 boundary close (task #6):** Talor #8 fold-in + the anvil#4
   94-file format sweep (mu-determinism-gated, reviewed as a unit) →
   `forge forkcheck` FIXED_HASH pair → overnight re-baseline final_read
   (~2,000 games, quiet box — schedule with the user).
2. **ADR-0049 instruments → scripts/** during the re-baseline wait
   (per the interleave plan); run them over the fresh re-baseline stores
   for the gate's baseline read.
3. Then the C-bundle build (rl.py L_seq + masked-head aux;
   finetune_value C2a aux; first-attempt-only λ=0.01 port of the
   chain-grouping into `rejected_events`; selfplay forced-seq phase).
