# 2026-07-26 — D4: the rebase lands, and the scoreboard moves under us

One continuous session from the evening of 07-25. Covers the whole D4 arc:
trial rebase → promotion → gates → an inherited connive regression → the
four-cell re-baseline → closeout ([ADR-0025](../decisions/ADR-0025-d4-rebase-closeout.md)).

## The rebase itself was the easy part

`git merge-tree` said conflict-free before anything was attempted, and the
trial rebase confirmed it: **28/28 commits replayed, zero conflicts**, with
`42e15f4822` dropped explicitly. The payoff of PR #11203 showed up exactly as
predicted — `GameCopier.java`, `Game.java`, `Player.java` came back
**byte-identical** and left the fork diff entirely. The fork's whole engine
delta is now `AiController.removeUnpayableAttackers` made public plus
`PhaseHandler.devResumeAtPriority`: **14 lines in two files.**

Pin choice: `3e3818f1ba` (last 07-24 commit), not tip. That captured all three
determinism merges while skipping 17 same-day commits including a
landed-and-reverted AI change and an hours-old one. The reasoning was that the
pin becomes the teacher and opponent for all of M4 — and it held up, though not
for the reason expected (see connive).

Promotion needed a `reset --hard`, not a fast-forward — rebasing rewrites. Old
tip tagged `pre-rebase-20260725` first, which turned out to be load-bearing
within hours when the control read needed the old engine rebuilt.

Gates, all green: 292 Java / 98 Python; **`forkcheck -twin` divergence
16.5%→7.0%, chain 2/200→1/200** (the #11360 dividend landing where the worklist
said it would); **`-grpc` twin 40/40** certified with `d5-combat` (a stronger
gate than 07-10's, since it exercises the combat heads); 1,701/1,701 pool cards
resolve; census 120/120 decisive with turns median 21 vs 21 and callbacks/game
1,117 vs 1,136.

## Connive: a bug we inherited, found only because the user asked

The user mentioned hearing connive was broken. Three connive cards are in our
pool (Illuminator Virtuoso, Ledger Shredder, Lethal Scheme). The break is
**not** #11388 (the connive AI commit deliberately excluded by the pin) — it
came in via `28431f2647` **"Recruit: add Effect" (#11355)**, whose title
mentions neither connive nor a bug.

`SpellAbilityEffect.discard()` writes the post-move collection back into the map
it is handed (`:906`) so its own `DiscardedAll` loop can read it. #11355 changed
`ConniveEffect` to pass an immutable `Map.of(...)`, so every connive resolution
throws. `RecruitEffect`, added by the same PR, shipped with the identical bug.

Cost: **a full 4,000-game arms read.** 28 `UnsupportedOperationException`
crashes in 2,000 games (1.4%) against 4 in all pre-rebase history, concentrated
in the five pool decks carrying connive cards — a per-matchup bias, not uniform
noise. Fixed at both sites, pinned by `ConniveDiscardMapTest` validated failing
first. Post-fix: 0 UOE in 2,000 games.

**Deliberately not upstreamed.** Hanmac took responsibility and is fixing it; no
caller of `discard()` reads the map back, so every plausible maintainer fix is
behaviorally identical to ours. The commit message carries the instruction to
drop ours at the next rebase.

## The re-baseline, and what it cost to learn

Four 2,000-game Ante-corrected reads, run sequentially at matched load:

| | old engine | new engine | Δ |
|---|---|---|---|
| RL `run7b-i14` | 0.5530 ± 0.0109 | **0.5121 ± 0.0110** | −4.09 ± 1.55 (t=−2.64) |
| BC `d5-combat` | 0.4616 ± 0.0109 | **0.4452 ± 0.0109** | −1.64 ± 1.54 (t=−1.06) |

- **RL over BC survives: +6.69pp ± 1.55 (t=+4.32)** on the new engine. The core
  M2/M3 claim holds; ckpt of record unchanged.
- **Against the heuristic the agent is at parity: +1.21pp ± 1.10.** ADR-0019 and
  ADR-0023's "first teacher-surpassing read" was measured against the *old*
  heuristic.
- **DiD −2.45pp ± 2.19 (t=−1.12): not significant.** The control was run
  specifically to separate uniform-shift from RL-specific and it does not do so.
  Stacking two differences inherits SE 2.19pp — arithmetic that belonged before
  the runs, not after. Called decisive in advance; it wasn't.

Leading explanation, and the user's read: the heuristic got stronger (26
`forge-ai` commits; maintainers actively tuning). Supported by BC's
first-attempt veto *falling* 0.1395→0.1072 — actions land more easily, the
opponent just plays better.

## Learner speed: measured, diagnosed, not yet fixed

Benchmark on a quiet GPU (`scripts/bench_learner.py`, run-8 iter-005 replayed):
throughput **flat in `--seg`** (64/128/256/512 → 0.410/0.442/0.419/0.423
traj/s) and **worse with more workers** (6→12 = 0.442→0.391), GPU busy ~10%.
Both hypotheses dead. Phase instrumentation: **load 86.9%**, fwd_nograd 5.7%,
fwd_bwd 3.7%. Process sampling: main process 83% CPU *while waiting*, six
workers idle at 23–33%, box at load 1.5/32.

Cause: `RlTrajectories` yields per-window example dicts and `collate` runs
main-process-side, so each trajectory ships thousands of small tensors through
the DataLoader shm+pickle path to a single consumer. Fix designed, not built:
collate inside the worker — and pre-collating at the current seg chunking makes
the segmentation identical, so a **strict byte-identical equivalence gate** is
available rather than a tolerance argument.

Corollary: the VRAM elasticity work bought robustness, not speed. Run-8 spending
44h at `seg 128` cost nothing measurable.

## The collate refactor landed — 4.7-6.7x, bit-identically

Built it same session. Worker-side collate at exactly the learner's seg size:

| | before | after |
|---|---|---|
| traj/s, 6 workers | 0.442 | **2.062** (4.7x) |
| traj/s, 12 workers | 0.391 | **2.979** (6.7x) |
| GPU busy | 0.12 | 0.32 / 0.41 |
| phase `load` | 86.9% | **48.1%** |

More workers now HELP, which is the funnel being gone. Run-level: run-7b's
16.6h train phase becomes ~2.5h, so a 26.8h run lands near **12.7h** and
GENERATION becomes the dominant cost (~80%).

**The equivalence gate, and a correction.** I claimed a byte-identical gate
was available because pre-collating at the same boundaries keeps segmentation
and padding identical. It is — but the learner is **not bit-reproducible on
GPU** (bf16 autocast / TransformerEncoder reduction order): new-vs-new differs
by 2.95e-05, the *same magnitude* as old-vs-new, so a GPU comparison at that
resolution proves nothing either way. I asserted a property of the measurement
without checking it — the same error shape as the DiD power calculation.
On CPU the model IS deterministic, which makes it a real gate: new-vs-new
bit-identical (control), **old-vs-new bit-identical across all 195 tensors**,
metrics matching exactly.

## Generation is the new bottleneck, and 8 workers is already its ceiling

`scripts/bench_generation.py`, config mirroring the RL loop exactly (both
seats model-driven, sampling server at tau=1, `-reask`, obs+census), chunk
sized for whole rounds in every arm:

| workers | games/h | decisive |
|---|---|---|
| **8** | **1,150.5** | 240/240 |
| 16 | 1,078.5 | 240/240 |

Doubling workers is ~6% SLOWER. At 591 req/game those rates imply **189 req/s
at w=8** against the ~188 rps micro-batching ceiling measured at D6 open — w=8
already sits on it, extra workers only queue. So the `--chunk` follow-up is
moot; parallelism was never the constraint.

Same shape as the learner: the GPU is not the constraint, the Python around it
is. 189 rps for a 42M model at <=16 micro-batching is ~5.3 ms/request, far
above the forward cost — per-request serving overhead (featurization, gRPC)
is the suspect. Not chased; fresh-context task.

Incidental: 1,150.5 g/h post-rebase vs the standing 1,146 pre-rebase. Not
strictly comparable (different engine), but landing within 0.4% is a side
signal that the rebase did not move generation cost.

## What broke in my process (worth more than the bugs)

- **A crash class is a stack trace, not an exception type.** I dismissed the
  first connive crash as pre-existing because the exception *type* had 4
  historical occurrences. That shortcut cost the 4,000-game read.
- **Verify the instrument before trusting a gate.** My first "pool smoke gate"
  silently ran `--bridge local-random` (the harness default) with one worker
  (`chunk=200 ≥ 120 games`). Its alarming numbers were properties of the random
  arm. `forge census` was the right instrument and had a recorded baseline.
- **Arm the watcher in the same action as the launch.** Two ~2h reads went out
  detached with nothing watching, so completion reached nobody and the user had
  to ask whether notifications were broken. They weren't. Memory note updated.
- **A watcher that cries wolf is worse than none.** The failure hook I added to
  `final_read` caught `BaseException`, so `--help` (which raises `SystemExit(0)`)
  pushed "read FAILED" with no run in flight. Fixed to notify only on a nonzero
  exit code. Same failure family as the block-buffered log: a signal that does
  not mean what it appears to mean.
- **Establish the noise floor before believing a delta.** The collate
  equivalence check "failed" against a GPU baseline that cannot reproduce
  itself. Run the control first.

## Landed

- Fork `master` → `5fbc2ac98d` (upstream `3e3818f1ba` + 30 Anvil commits);
  tag `pre-rebase-20260725`; worktrees `forge-rebase`/`forge-pr1` retired.
- Anvil `47fea51` (forkcheck `FIXED_HASH` instrument, `rl.py --max-traj`,
  bench script), `4038d96` (learner phase timing — `phase` shares now in every
  metrics row, so starvation regressions are visible without a special run),
  `c09a755` (worker-side collate), `e012736` (rl-workers 6->12, shared
  notifier, `final_read` provenance + line buffering).
- Upstream worklist: configurable tap angle queued (no prior discussion exists
  upstream; constants located on both UIs, with the nearest-90 image-rounding
  wrinkle flagged before anyone scopes it).
- `CLAUDE.md` fork-discipline convention amended: behavior-identical patches are
  not dataset boundaries, proven **empirically** by matching forkcheck trace
  hashes, never by argument. A crash fix never qualifies.

## Next session picks up

1. **M3 closeout decision** — all four done-when clauses are resolved.
2. **Serving-path profiling** — generation is now ~80% of run wall clock and
   the server sits on a ~189 rps ceiling that looks like per-request Python
   overhead, not GPU compute. Instrument phases, establish the noise floor,
   then believe the delta.
3. Grindstone/M4 design session — seeds: the 531-loss curation list, ADR-0024's
   signal-source argument, and the new 0.5121 baseline.
4. Small carried items: the `IndexOutOfBoundsException` class (2 crashes, both
   deck `dc-863943`, early + seed-pinned — replay with `-Danvil.crash.trace`;
   plausible upstream filing); `final_read.py` riders (notify hook, stdout
   line-buffering, `pool_version` provenance); pool-manifest mtime hazard;
   the offered `ConniveDiscardMapTest` for Hanmac's thread if wanted.
