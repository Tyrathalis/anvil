# 2026-07-15 — d6-run1: first V-trace self-play run complete (overnight)

## Done

- **d6-run1 ran to completion: 12 iterations × 480 games = 5,760 self-play
  games (~9h wall), zero unhandled failures after the session-3 first-contact
  fixes.** Per-iteration cadence ~45 min (gen ~21 min at fleet rate, train
  ~24 min / ~960 trajectories / ~240 optimizer steps). The guards all earned
  their keep in production: ingest dropped diverged re-issue mu records
  (game 237 recurred deterministically and was absorbed), the tripwire
  dropped 1–2 trajectories/iteration (bf16 tail, per-head debug confirmed
  soft-head noise, all picks matching), worker OOMs → chunk re-issue →
  torn-line-tolerant monitor, phase-idempotent resume never repeated work.
- **Loop health over 12 checkpoints:** entropy 0.131 → 0.165 (bonus working,
  no collapse — mild exploration growth); per-iteration KL(π‖μ) steady
  0.001–0.006; ρ_mean 0.94–0.99 (mild off-policyness from the replay
  mixture, V-trace ratios engaged); critic v0 tracked realized reward within
  ~1.5pp every iteration — **the §6 anomaly rule stayed quiet the whole
  run**; turns median 21–22 flat (no stall drift); 480/480 decisive every
  iteration.
- **Arms trajectory (paired seeds, standing schedule):** iter004 0.4525 ±
  0.0249 → iter009 0.4925 ± 0.0250 → **final ckpt on 2,000 games: 0.478 ±
  0.011** (1,997/2,000 decisive, 3 crashes). Paired against the D5 arm on
  the 400 shared seeds: **+3.76pp ± 2.22 (t=1.69; 47 flips up / 32 down)**.
  The iter009 0.4925 partly regressed on the 5× larger unseen-seed set —
  the 400-game readings carry real noise, exactly why the final read was
  sized at 2,000.
- **Honest verdict: +2–4pp at ~85–95% confidence — the first V-trace run
  IMPROVES the arms but not yet outside noise. M2 done-when #4 remains
  open.** The machinery deliverable ("a loop that runs, doesn't collapse,
  and improves the arms") is met on the first two clauses and suggestive on
  the third.

## Findings for run 2

- **The policy barely moved: cumulative KL from init ≈ 0.05 after 12
  iterations at lr 1e-5.** The signal is there at tiny step sizes; run 2
  should push more iterations and/or lr (with the entropy/KL monitors
  already in place to catch collapse).
- **Veto drift is real and RL-relevant: 14.1% → 19.2% sampled across the
  run, 16.6% argmax on the final ckpt (D5 baseline 13.9%).** RL is drifting
  toward cast attempts the executor vetoes into passes — a reward leak the
  policy cannot see. This promotes the serve-time re-ask-on-veto lever from
  "queued behind D6" to the top of the run-2 prerequisite list.
- Combat-terminal fallbacks 2–10/iteration (~0.1% of combat windows) under
  sampled declarations — known class, stable.
- bf16 tripwire tail reached 0.44 total-logp on soft heads (picks always
  match). If it fattens further, switch the tripwire to pick-match + loose
  logp bound.

## Next session picks up

- Decide run-2 shape: lr/iterations scaling vs veto re-ask first (my read:
  re-ask first — it's a structural reward leak that grows with RL pressure).
- Consider Ante-ledger arms reads (certified ~5% var cut at the omniscient
  tier; every pp of SE matters near the 2σ bar).
- ADR when done-when #4 resolves either way; Status/map updated this
  session with the run-1 record.
