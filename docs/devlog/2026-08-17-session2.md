# 2026-08-17 session 2 — M8 D1 designed, built, RUN, and RESOLVED: gate FAILED (no within-natural timing variation), pivot to D2′

## Done

- **D1 design session (user-approved via three decisions):** the
  ADR-0051 resolvability check fired on the proposed gate — K=32
  cannot resolve RMS 0.10 through the variance decomposition
  (within-natural bins can't pair; the independent binomial floor puts
  the signal below 1σ of estimator noise both directions). Amended:
  **K=64, two-bin primary contrast (in-window vs deferred), fine
  4-bin descriptive only**; binning on first SPELL cast
  (isSpell — lands AND activated abilities excluded); single-natural-arm
  flag added in the same jar change. **Gate PINNED in m8-plan before
  any generation:** abundance ≥30% (minority ≥12.5%) · RMS ≥0.10 ·
  headroom ≥1pp (play-weight = 99 active windows / 2,000 source games
  = 0.0495, fixed before the read).
- **Extension built (fork `b2d7ca2ec2` + `fe11f57a28`):**
  SeqMode.OBSERVE (records, never forces; untilTurn=MAX), per-completion
  labels arrays (out / first_sa / first_t / land_t), `-seqarms nat`;
  `--seq-arms` threaded through harness + grindstone;
  `scripts/natural_timing_read.py` (per-row seat parse — the arms
  drill different seats); orchestrator injects DISPLAY/XAUTHORITY
  defaults. Suite 162 green throughout.
- **Boundary-exemption proof PASSED at the instrument's own noise
  floor** ([ADR-0059](../decisions/ADR-0059-instrument-corrections.md)):
  era-vs-ext FIXED_HASH 500-game pair 497/500 exact, divergence
  tallies identical — and an era-vs-era replication of the SAME jar
  also agrees only 497/500 (main-hash flips included), so the deltas
  are the instrument's ~0.5% wall-clock class, not a jar delta.
- **Two instrument discoveries en route (ADR-0059):**
  (1) `run_forkcheck.sh` picked jars alphabetically — ALL THREE
  ADR-0055 fidelity runs (incl. the FIXED_HASH determinism annex) ran
  the stale pre-rebase 2.0.14 jar; the annex compared a jar to itself
  and its determinism claim is retracted. True era fidelity measured
  today: 11.2–11.4%. Script fixed (newest-mtime + warning + JAR=
  override); stale jar archived to `data/forkcheck/jar-archive/`.
  (2) `AnvilRun.jstr` never escaped control chars — modal spell text
  (newline bullets) split labels rows; the probe's first launch lost
  78/114 rows, caught live, jar fixed, probe relaunched. M7's campaign
  labels had the same hole with SILENT seqlabels drops (stores
  deleted, rate unmeasurable; today's analog 68%): M7 verdict stands,
  dose was smaller than designed. seqlabels drops are loud now.
- **The probe ran clean** (2×~1h arms, w=16): 99/99 points fired,
  6,335/6,336 completions, 1 crash, 0 anomalies, 0 bad rows.
- **D1 RESOLVED — gate FAILED, decisively**
  ([ADR-0060](../decisions/ADR-0060-d1-natural-timing-probe.md)):
  split abundance **3.0% vs the 30% pin** (clause 3 also fails:
  0.55pp); 71.8% of natural completions cast in-window and modal first
  casts are frequently identical across all 64 samples of a point.
  Within-natural timing variation does not exist at plan granularity —
  ADR-0051's falsification extends from single decisions to plans.
  **Nothing for a natural-timing target to grade ⇒ the pre-registered
  pivot: D2′ curriculum × rank-critic curation.**

## Broke / gotchas

- Forge jars exit 1 with ZERO output when DISPLAY is unset (Sentry
  swallows the AWT crash) — agent/tty shells hit this; memory +
  orchestrator env-defaults added. Diagnosis pattern: `-verbose:class`
  death inside io.sentry.transport.
- Under `-XX:hashCode=3` the identity-hash counter is positional:
  single-seed forkcheck reruns are NOT comparable to in-sequence
  results — flip adjudication needs full same-sequence replication
  runs.
- `uv run` rebuilt the venv this session (interpreter patch bump to
  CPython 3.14.7) — harmless for generation; watch for it before any
  training resume.

## Next session picks up

1. **D2′ audit design session:** pin the Spearman-vs-rollout-truth
   threshold for the `rank-critic-c2v3` ordering audit (ADR-0036
   caution) BEFORE generating audit labels; then the audit → critic-
   ordered (or corrected-map fallback) curation → one run vs
   0.5373 ± 0.0112.
2. Still pending from session 1: the M7 runs-store kill-list `rm`
   (~4.5G, user execution).
3. Stale-data candidates noted in ADR-0060 (smoke dirs now; proof jars
   at the next boundary).
