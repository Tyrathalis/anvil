# 2026-08-13 — d6-run14 launch → GUARD HALT iter 2 → ADR-0056 seq fix → seqfix smoke

## Done

- **d6-run14 LAUNCHED 08-12 21:16** per the session-6 checklist (pre-flight
  clean, detached launch, watchd self-registration verified, persistent log
  monitor + 2h telemetry cron armed, coverage enumerated). Early health all
  in-band: overlap tracks interleaving as designed (gen ~26 min ‖ campaign
  ~2h40m — the campaign at P=99 is the pole; the 2h05m estimate came from
  the 20-point smoke → real pace ~2h45m/iter), join 93/99 then 97/99,
  w_seq calibrated 0.00255 at run start and correctly carried at iter 1
  (the carry-forward fix's first live test), battery per-iteration rows
  landing. Benign oddity chased: identical mu-record counts across an
  iteration's three ingests = the run-level server-side mu file (run7b
  shows the same pattern; disjoint start-indices, no (g,s) collision).
- **GUARD HALT iteration 2 (~05:52):** kl_mu 2.64 vs 0.06, veto 0.384 vs
  1.5×0.211. Driver rejected the ckpt, exited clean, self-unregistered.
- **Forensics ([ADR-0056](../decisions/ADR-0056-run14-seq-divergence.md)):
  the L_seq contrast is unbounded and ran away under the frozen w_seq.**
  seq_raw −0.22→−0.31 (i0), −0.53→−0.65 (i1), −0.75→−8.48 (i2, lockstep
  with kl 0.046→20.2 and rho_mean 0.98→0.23). w_seq·|L_seq| ended ~40× the
  10% PG-mass target. Calibrate-once assumed |L_seq| stationary — false
  (3× growth across two *in-band* iterations). Downstream: rej 5→9→13,
  in-play veto 0.211→0.230→0.384 (iter-001's accepted ckpt already
  damaged — acceptance-before-play latency), hold-then-cast 0.241→0.194
  moving AWAY from heur 0.345 (indiscriminate earlier casting, not
  timing). Irony on record: the smoke-era per-invocation recalibration
  "bug" was an accidental stabilizer; the carry-forward implemented the
  pinned design faithfully — the defect is the unbounded objective.
  **Verdict: bug, NOT falsification — the hypothesis (seq credit at ~10%
  of PG mass) was never tested; the 10% invariant is what broke.**
- **Fix built (user-approved scope), suite 162 green (+3 tests):**
  1. `seq_pass` hinge: contrast clamped to ±`--seq-margin` (default 6.0,
     rl.py + driver pass-through); saturated windows give zero gradient;
     |L_seq| ≤ seq_clip·margin = 1.5. margin=0 = old form (era repro).
  2. `seq_share` = w_seq·|L_seq per step| / mean|PG per traj| logged per
     window (= seq_frac at calibration by construction), meaned into the
     monitor row (`_rl_summary` now per-key presence + seq keys), guarded
     by `--guard-seq-share` (default 0.3 = 3× target) in `guard_flags`.
  3. Battery: seq_raw + seq share curves; anomaly on |seq_raw| >3× growth.
  4. rl.py `--kl-abort` (driver passes 5× guard-kl): ends a diverging
     phase at the crossing log-window; driver guard still rejects.
- **Not changed (preserving the pinned experiment):** λ, K/N/P, drill K=2,
  seq_frac, recipe, evalset v4, era jar, selection. Pre-registered
  secondary read: hold-then-cast trajectory toward ~0.345.
- Docs: ADR-0056; CLAUDE.md M7 bullet; map (Now panel, M7 section, run14
  ledger row HALTED, footer). Scripts: `launch_seqfix_smoke.sh` (2 iters),
  `launch_d6_run15.sh` (run14 + ADR-0056 deltas, seed_base 20260815,
  gated on the smoke).

## Broke / gotchas

- run14's iter-000/001 ckpts + stores are contaminated (veto damage) — do
  NOT reuse in any mixture; run15 restarts from iteration 0.
- The seqfix smoke must be TWO iterations: the failure signature is
  cross-iteration seq_raw growth; run14's 1-iteration smoke could not
  have caught it (and didn't).

## Next session picks up

1. Read the seqfix smoke (launched this session, ~5.5h): pass = seq_share
   ≈0.1 flat across the iteration boundary, |seq_raw| bounded, w_seq
   carried, guards quiet, join ≥ ~18/20.
2. On pass: launch `scripts/launch_d6_run15.sh` per the standing checklist
   (detached + monitor incl. `KL ABORT` tripline + glances; ~55h) → the
   0.5373 ± 0.0112 gate → M7 closeout ADR.
3. On fail (share drifts or seq_raw grows under the hinge): the contrast
   formulation itself is suspect — back to ADR-0056's "evidence against
   this formulation" branch before spending the 55h.
