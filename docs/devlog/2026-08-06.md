# 2026-08-06 (M6 D2-A.1: the labeling re-price)

## Done

- **D2-A.1 labeling re-price DONE
  ([ADR-0040](../decisions/ADR-0040-d2a-labeling-reprice.md)): the
  labeler is engine-bound; ADR-0015's "2–4× from micro-batching"
  falsified.**
  - Instrument: `scripts/bench_labeler.py` (modeled on
    `bench_generation.py`; ADR-0015 constants embedded; dedupes labels
    by (game, fork point) — crash re-launches replay partial games and
    re-append rows, 85/689 in this bench). Smoke = the parked
    `-points` sampling mode's first run since M2 D4: clean on the
    post-rebase jar.
  - The priced arm: 160 pool games, w=16, K=8, 4 turn-stratified
    points, argmax iter-019 on the micro-batching server, census off,
    chunk clamped to 5. **604 unique labels / 3.41h = 11.1
    pos/h/worker (177/h fleet)** — 0.65× ADR-0015 per-worker, ~1.3×
    fleet. 50K ≈ 11.8 days (the stale 15 retired). Server log shows
    batching fully engaged (sizes 2–16) — the cost is JVM
    game-playing (fork-block p50 124s / p90 223s / max 978s),
    consistent with ADR-0032's waiting ceiling.
  - **The measured cheap path is drill mode:** `drill-map-r11i19-k8`
    did 565 labels / 2.59h at w=8 = **27.3 pos/h/worker** — 2.5× the
    sampler per worker despite deeper positions (exact replay to the
    fork turn + `-drillstop`, no mainline completion). Loss-adjacent
    positions — the ADR-0039 curve's population — are the cheap ones.
    Tranche plan recorded: drill mode over expanded loss-adjacent
    positions, 5–10K/era ≈ 1–2 days of box time per era.
  - Crash tax with numbers: 37/4,832 completions (0.77%), 83
    invocations vs 32 spans, ~12% duplicate rows — the D2-A.2 fork
    stability pass is now number-justified.
  - Docs: ADR-0040, m6-plan D2-A.1 DONE block, CLAUDE.md, map (Now
    pill/bullet, ADR index, stamp).

- Community note (Discord, user-forwarded): LordOfThePigs' draft agent
  — gen-3 reportedly out-drafts Forge's drafter after switching
  offline → online training (10-pod batches); reward = margin vs
  pod-average deck score, emergent hate-drafting; win-rate validation
  pending. Recorded on the map's watch list — not Anvil-relevant today
  (no limited scope), adjacent prior work if that ever changes.

## Broke / surprised me

- **The per-worker number went DOWN vs 2026-07-13** (11.1 vs 17)
  despite micro-batching + w=16. Explained, not mysterious: iter-019
  vs the BC-era policy, post-rebase engine, `-reask`, and completions
  that play today's longer games to the end. The real lesson: serve
  optimizations never had leverage here — the labeler was always
  engine-bound.
- Mid-run accounting trap: raw `labels.jsonl` line counts overstate
  throughput (crash re-launch duplicates). Bench script now dedupes.
- The stall alarm fired on the tail game (134): not a wedge — a
  pathological game with 159s fork blocks that had also killed a
  worker. The fork-side watchdog budget covered it; run completed
  160/160 (156 decisive).
- Ops note: bench ran with a live desktop + a Kopia upload in the
  background — checked 0% iowait / ~40% CPU idle, contention
  negligible; conditions recorded in the bench JSON.

## Next session picks up

- **The label-expansion tranche (ADR-0039/0040):** expand
  loss-adjacent positions from the era stores (sweep-style anchor
  offsets per loss + more losses from closing-read arms), generate in
  drill mode at K=8, extend the ADR-0039 learning curve; the path A/B
  commitment is decided on that curve (user decision point).
- D2-A.2 fork stability pass can interleave (pre-campaign gate, now
  priced: 0.77% completion crashes, ~12% re-launch waste).
- D3 (curriculum arm) still parallelizable; D4 isotonic wiring still
  open, small.
