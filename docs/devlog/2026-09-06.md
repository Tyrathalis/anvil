# 2026-09-06 — Build 0 full read (cl2) after the overnight OOM; Fork C adjudication

## 00:03 overnight — the cl2 READ OOM-killed the box (lanes + ingest had completed)

- cl2 (all 806 windows, 9 lanes) finished its lanes at 23:34 (6.1 h wall, no lane failures,
  95,753 completion rows) and ingested 95,616 fork frames (893 MB store, manifest OK) by 23:41.
- The READ then held every decoded completion trajectory in one dict (~90 obs-carrying decisions
  each): the process reached **51.6 GB RSS and the kernel OOM-killer took it at 00:03:49 — and the
  Claude Desktop scope died in the same event** (`app-com.anthropic.Claude-4560.scope: oom-kill`);
  the box was rebooted in the morning. cl1's 24K-frame read had fit at 39 GB and hid the scaling
  (decoded obs ≈ 10× raw JSON in Python objects; 9.9 GB raw is not a memory estimate).
- Also lost with the session: the two persistent monitors (read-landed / unregister-on-landing).
  watchd had no registration after the reboot (boot-time change ⇒ removed after its alert).
- **Fix (09:12):** `read()` rewritten to STREAM — the (fork point → completion ids) map comes from
  `games.jsonl`'s `fork` key (no decoding), each window's completions are decoded, valued by both
  critics and dropped; big reads launch under `systemd-run --user --scope -p MemoryMax=36G
  -p MemorySwapMax=0` so a runaway kills only itself. Memory note saved
  (`stream-fork-store-reads-under-memory-cap`). Re-read launched on the existing store.

## 09:19 — cl2 READ (streaming, 378 s, peak well under the cap): HYBRID 0.301 ± 0.022 → Fork C ADJUDICATED

800/806 windows read (6 lost at replay), 650 with ≥ 3 scored arms; 212,288 states valued across
both critics. **Headline 0.301 ± 0.022 — HYBRID by the pre-registered bars** (4.5 SE under ADOPT,
4.6 SE over RETIRE); the 200-window subset (0.279 ± 0.044) replicates. Full table + the decision in
[ADR-0098](../decisions/ADR-0098-build0-critic-lookahead-read.md). The four facts that shape it:
(1) one two-turn rollout's composite (0.47) beats the one-turn critic (0.30) at ~2× copy cost — K is
the cheap-label dial; (2) the critic's max-arm Δ aims at AUC 0.65 zero-shot vs the learned
pivotality head's 0.69 for free — aiming goes to the read-out; (3) the masked value head tracks
full-vis within 0.03 at every horizon — the deployment critic's reliability is measured (0.28 at
eot); (4) horizon is the mechanism: at h2 over 8 rolls the critic reads 0.46. Strata: certified
windows 0.37, natural 0.26. Docs: ADR-0098, m11-plan (Fork C + done-when 1), CLAUDE.md M11
bullet, standing-rules (+2), map (Now panel, ADR row, footer).

## Next session picks up

1. **Build 1: option-content encoding + scorer head on a trainable copy of the trunk**, fitted on
   mint + harvest spreads (3,475 windows); read vs the frozen probe at equal N + the learning curve;
   **KILL: Spearman > 0.3** (the Build 0 number gives it a second reference: the one-turn critic
   itself ranks at 0.30). Pre-register the fit recipe (init from the cast head, the ADR-0088
   grounded-driver machinery for the label term) before the data.
2. Build 2 pins to carry: K per window as a budget dial (Fork D), the void legality pre-filter
   (46% of arms void, 17% of windows below 3 scored arms).
3. Housekeeping: cl1/cl2 instrument stores kept through M11 (1.1 GB); the recording jar's
   ADR-0025 proof is owed before any jar generates a training store.

## Build 1 opened — the OPTION SCORER fit (afternoon)

Four forks put to the user, all on the recommendation: (1) **bands** at the probe's N=607: ≤ 0.15
KILL / > 0.30 PASS / between → the full-N curve decides (rising and > 0.30 passes); pivotality AUC
≥ 0.70 alongside; (2) **trunk mode**: full fine-tune (trunk 1e-5, heads 1e-3) + a frozen-trunk twin
as the probe's control; (3) **init**: `m10-sched-init` (the ckpt of record's trunk) with the option
keys and scorer query copied from the executor's own pointer; (4) **encoding**: pooled keys (mean ⊕
sum ⊕ length embedding → linear → q·v), the plan's §1 shape, directly comparable to the probe.

Built: `AnvilNet.score_options` + `opt_query/opt_len_emb/opt_pool` in the model (`opt_` tolerated
by `load_compat`; suite 283 green); `scripts/option_scorer_fit.py` (`build` = harvest spreads + mint
spreads recomputed from the mint's lane rows via the pinned select/score scoring, featurized at the
emission window on the legal candidate basis, streamed per store; `fit` = the learning curve in
both modes with early stopping on holdout Spearman; `report` = the verdict against the bands).
The mint never wrote spread files — its 400K lane rows and 51K arm lines were intact, so the
spreads are a recomputation, not a re-roll. Pre-registration pinned in m11-plan Build 1.

## 09:44 — Build 1 curve READ: KILL by the pre-registered curve (ADR-0099 PROPOSED)

3,655 windows / 37,257 pairs (793 harvest + 2,862 mint; 789 holdout); 24 fits in 12 min. Full-trunk
scorer **0.158 at N=607** (between the bands) → **0.204 at full N** (rising, under 0.30) → KILL by
the curve. Frozen twin 0.150 → 0.183 (the trunk's gradient buys +0.02 at every N). Pivotality AUC
0.53–0.57 (bar 0.70; the state-level classifier read 0.69). Fits peak at epochs 0–6 and overfit
fast. Two references that frame it: the untrained init reads −0.06, and **arm LENGTH alone reads
0.148** on the same holdout — the fits at the probe's N learn a length prior and the full-N fit adds
+0.05 on top. Slope ≈ +0.03 per doubling → the 0.30 bar is ~16× the data away. Harvest windows are
harder than mint (0.12 vs 0.22 at full N). Docs: ADR-0099 (PROPOSED, routes R1–R5 with the lean),
m11-plan done-when 2 KILLED, CLAUDE.md, map (Now panel, ADR row, footer). Instrument assets:
`m11-scorer-b1/` (windows.pt 44 MB, two 276 MB best ckpts — kill-list candidates at the route).
