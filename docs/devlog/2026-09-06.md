# 2026-09-06 — Build 0 full read (cl2) after the overnight OOM; Fork C adjudication

## 00:03 overnight — the cl2 READ OOM-killed the box (lanes + ingest had completed)

- cl2 (all 806 windows, 9 lanes) finished its lanes at 23:34 (6.1 h wall, no lane failures,
  95,753 completion rows) and ingested 95,616 fork frames (893 MB store, manifest OK) by 23:41.
- The READ then held every decoded completion trajectory in one dict (~90 obs-carrying decisions
  each): the process reached **51.6 GB RSS and the kernel OOM-killer took it at 00:03:49 — and the
  Claude Desktop scope died in the same event** (`app-com.anthropic.Claude-4560.scope: oom-kill`);
  the box was rebooted in the morning. cl1's 24K-frame read had fit at 39 GB and hid the scaling
  (decoded obs ≈ 10× raw JSON in Python objects; 9.9 GB raw is not a memory estimate).
- Also lost with the session: the two persistent monitors (read-landed / unregister-on-landing).
  watchd had no registration after the reboot (boot-time change ⇒ removed after its alert).
- **Fix (09:12):** `read()` rewritten to STREAM — the (fork point → completion ids) map comes from
  `games.jsonl`'s `fork` key (no decoding), each window's completions are decoded, valued by both
  critics and dropped; big reads launch under `systemd-run --user --scope -p MemoryMax=36G
  -p MemorySwapMax=0` so a runaway kills only itself. Memory note saved
  (`stream-fork-store-reads-under-memory-cap`). Re-read launched on the existing store.

## 09:19 — cl2 READ (streaming, 378 s, peak well under the cap): HYBRID 0.301 ± 0.022 → Fork C ADJUDICATED

800/806 windows read (6 lost at replay), 650 with ≥ 3 scored arms; 212,288 states valued across
both critics. **Headline 0.301 ± 0.022 — HYBRID by the pre-registered bars** (4.5 SE under ADOPT,
4.6 SE over RETIRE); the 200-window subset (0.279 ± 0.044) replicates. Full table + the decision in
[ADR-0098](../decisions/ADR-0098-build0-critic-lookahead-read.md). The four facts that shape it:
(1) one two-turn rollout's composite (0.47) beats the one-turn critic (0.30) at ~2× copy cost — K is
the cheap-label dial; (2) the critic's max-arm Δ aims at AUC 0.65 zero-shot vs the learned
pivotality head's 0.69 for free — aiming goes to the read-out; (3) the masked value head tracks
full-vis within 0.03 at every horizon — the deployment critic's reliability is measured (0.28 at
eot); (4) horizon is the mechanism: at h2 over 8 rolls the critic reads 0.46. Strata: certified
windows 0.37, natural 0.26. Docs: ADR-0098, m11-plan (Fork C + done-when 1), CLAUDE.md M11
bullet, standing-rules (+2), map (Now panel, ADR row, footer).

## Next session picks up

1. **Build 1: option-content encoding + scorer head on a trainable copy of the trunk**, fitted on
   mint + harvest spreads (3,475 windows); read vs the frozen probe at equal N + the learning curve;
   **KILL: Spearman > 0.3** (the Build 0 number gives it a second reference: the one-turn critic
   itself ranks at 0.30). Pre-register the fit recipe (init from the cast head, the ADR-0088
   grounded-driver machinery for the label term) before the data.
2. Build 2 pins to carry: K per window as a budget dial (Fork D), the void legality pre-filter
   (46% of arms void, 17% of windows below 3 scored arms).
3. Housekeeping: cl1/cl2 instrument stores kept through M11 (1.1 GB); the recording jar's
   ADR-0025 proof is owed before any jar generates a training store.

## Build 1 opened — the OPTION SCORER fit (afternoon)

Four forks put to the user, all on the recommendation: (1) **bands** at the probe's N=607: ≤ 0.15
KILL / > 0.30 PASS / between → the full-N curve decides (rising and > 0.30 passes); pivotality AUC
≥ 0.70 alongside; (2) **trunk mode**: full fine-tune (trunk 1e-5, heads 1e-3) + a frozen-trunk twin
as the probe's control; (3) **init**: `m10-sched-init` (the ckpt of record's trunk) with the option
keys and scorer query copied from the executor's own pointer; (4) **encoding**: pooled keys (mean ⊕
sum ⊕ length embedding → linear → q·v), the plan's §1 shape, directly comparable to the probe.

Built: `AnvilNet.score_options` + `opt_query/opt_len_emb/opt_pool` in the model (`opt_` tolerated
by `load_compat`; suite 283 green); `scripts/option_scorer_fit.py` (`build` = harvest spreads + mint
spreads recomputed from the mint's lane rows via the pinned select/score scoring, featurized at the
emission window on the legal candidate basis, streamed per store; `fit` = the learning curve in
both modes with early stopping on holdout Spearman; `report` = the verdict against the bands).
The mint never wrote spread files — its 400K lane rows and 51K arm lines were intact, so the
spreads are a recomputation, not a re-roll. Pre-registration pinned in m11-plan Build 1.

## 09:44 — Build 1 curve READ: KILL by the pre-registered curve (ADR-0099 PROPOSED)

3,655 windows / 37,257 pairs (793 harvest + 2,862 mint; 789 holdout); 24 fits in 12 min. Full-trunk
scorer **0.158 at N=607** (between the bands) → **0.204 at full N** (rising, under 0.30) → KILL by
the curve. Frozen twin 0.150 → 0.183 (the trunk's gradient buys +0.02 at every N). Pivotality AUC
0.53–0.57 (bar 0.70; the state-level classifier read 0.69). Fits peak at epochs 0–6 and overfit
fast. Two references that frame it: the untrained init reads −0.06, and **arm LENGTH alone reads
0.148** on the same holdout — the fits at the probe's N learn a length prior and the full-N fit adds
+0.05 on top. Slope ≈ +0.03 per doubling → the 0.30 bar is ~16× the data away. Harvest windows are
harder than mint (0.12 vs 0.22 at full N). Docs: ADR-0099 (PROPOSED, routes R1–R5 with the lean),
m11-plan done-when 2 KILLED, CLAUDE.md, map (Now panel, ADR row, footer). Instrument assets:
`m11-scorer-b1/` (windows.pt 44 MB, two 276 MB best ckpts — kill-list candidates at the route).

## Afternoon — routes R1 + R2 built and READ (both ≤ 1 day as priced; both negative)

- **R1 (distill one-step lookahead)**: cl2's critic Δ (eot, K=1 and K=8) joined to the harvest
  windows of the scorer corpus (655 windows; `--targets` on the fit script). Learnability 0.16–0.25
  vs the critic targets at N=502 (bar 0.5), 0.03–0.14 vs spreads; seed variance ±0.08; no rise with
  N. KILL — the critic's ranking lives in the post-action state the head never sees.
- **R2 (payment single-option surface)**: `build-pay` joins the 293 observed drills (evalset v2 +
  retired phyrexian) to their batches' certify rows — per-option margin over auto by the shape
  score, paired by roll (arm ids = option indices, 293/293) → 276 windows / 1,258 options / 64
  positives; scores = the pointer logits minus auto's. Holdout hash fixed to the full key (the
  (store, g) rule collapses pay windows onto four batch names); three salted splits × 2 seeds.
  Mean 0.287 (full) / 0.282 (frozen) — BETWEEN — but **auto-correct stratum 0.42 vs POSITIVE
  stratum −0.10**: the head learned the sign prior ("options are worse than auto"), not which
  option wins. Funding N on the between-band mean would fund the base rate.
- ADR-0099 → ACCEPTED with the addendum (three surfaces at or below a prior). Remaining: R3
  (serve-side masked-head lookahead as the acting rule, a bounded day-zero read) / R5 (close M11
  negative, scoping session). Lean R5. Instrument dirs `m11-scorer-r1-*`, `m11-scorer-r2-pay`
  (~2.2 GB ckpts) are kill-list candidates.

## Evening — M11 CLOSED (ADR-0100); M12 charter DRAFTED (search as the behavior policy)

- The user's framing set the next priority: "we can identify opportunities for strength, but we
  can't teach them to the model." Assessment recorded (chat → ADR-0100 §diagnosis): signal is weak
  at three layers (outcome→decision credit; the value head's state ranking 0.27–0.48; option
  quality from the pre-action state 0.20) but strong in the simulator (0.30 / 0.46 one step ahead,
  masked head within 0.03); luck correction is worth ~5% until the critic improves; the amplifier
  not yet used is search as the BEHAVIOR policy, amortized at store scale, with the value head as
  the thing to sharpen.
- ADR-0100 accepted: mechanism negative; done-when 1 met / 2 killed / 3–6 not reached; certifier +
  spreads retire to read instruments; two standing rules; routing by name; kill list (≈7.4 GB:
  cl1/cl2 worker raw output, cl1 forks store, scorer instrument ckpts) PENDING SIGN-OFF — nothing
  deleted yet. CLAUDE.md M11 bullet → archive verbatim + compact summary; map (Now panel, M11
  closed block, M12 open block, ADR row, footer).
- m12-plan.md DRAFT: charter (network + bounded engine lookahead valued by the masked head = the
  behavior policy; distillation + PG amortize), the directive with depth / breadth / leaf rolls as
  flags (the user's requirement: deeper search = an easy scaling lever), forks A–F with leans,
  build order 0–4 with pre-registered bars at Build 0 (≥ +1.5pp GO / ≤ 0 → value head first, one
  re-read), done-when, obligations (masked head only in the behavior search; flag-off jar identity
  proven before generation; compute accounting per game from birth).

## Next session picks up

1. **M12 scoping session**: adjudicate forks A–F; pin Build 0's margin bar / softmax temperature
   pre-data; pre-register Build 1's value-head numbers against the Build 0 cells.
2. ~~Kill-list sign-off (ADR-0100 table), then delete.~~ DONE — signed off and executed the same evening (≈7.4 GB; the cl2 forks store, corpora, curves and reads kept).
3. **Build 0**: Java `-lookahead d b K` + `anvil.value` + the acting rule; smoke under CRN; cost per
   searched window; the day-zero paired read on the fixed population.

## Evening — documentation restructure (before M12 scoping)

**Survey findings** (link graph healthy: zero broken links, zero orphans; the problems were
staleness and duplication): `docs/README.md` frozen at 08-12 (M7 open, ADRs to 0055); root README
Status at M2; field guide's newest citation ADR-0017; design doc §13 annotated through M0 only,
§3d′ ledger still "M9 in flight" / "M10 candidate", §15 one row resolved of thirty, six invariants
vs CLAUDE.md's seven; CLAUDE.md 23 KB with 16 KB of Status (nine closed-milestone bullets read every
session); the map's Now panel 42 KB of M10 narrative and a 22-entry footer session log (a second
devlog); `status-archive.md` with an EMPTY M6 section (the bullet was moved at `85d3e6c` and lost
later) and non-chronological sections; the map's state-of-record table carrying 13 ADR rows, six
of which (0095–0100) were missing from the ADR index.

**Four forks put to the user, all on the recommended lean:** §15 FROZEN (pre-M0 estimates; outcomes
live in closeout ADRs + the milestone table; ADR template line dropped) · the open milestone's
running record lives in its PLAN DOC (m12-plan "Running record" section; the archive-snapshot
ritual ends; CLAUDE.md holds one capped paragraph per track) · map volatile panels are HAND-TRIMMED
COPIES of CLAUDE.md's Status (no generator) · the design index is DERIVED from per-file status lines.

**Done:** CLAUDE.md rewritten as constitution + Now block + state-of-record table + milestone table
+ tiered session wrap-up checklist (23.0 → 12.9 KB; the old Status section archived verbatim) ·
`status-archive.md` M6 filled from git, reordered M0→M11 then tracks, contents list · every
`docs/design/*.md` stamped `**Doc status:** tier · hook` (living 7 / reference 11 / survey 6 /
historical 27); `scripts/docs_index.py` (with `--check`) generates `docs/README.md` · map: Now panel
rewritten from CLAUDE.md (249 → 189 KB), footer one stamp, ADR rows moved from the state table to
the index, M10/M11/M12 blocks in order, the TAKEN watch item removed · design doc: §3d′ ledger items
1/1a/2/3/4/5/10 re-pointed (M9 built, ADR-0080 re-deferrals, ADR-0083 landed, M12 queue), §13
measured record M1–M12, §15 freeze note, seventh invariant · ADR + devlog templates · root README
Status · m11-plan header (ADR-0099 ACCEPTED, milestone closed) · m12-plan Running record section.

**Deferred by name:** the field guide extension with the M3–M11 traps (AiCache leak ADR-0078,
Obs.mark replay integrity ADR-0089, serving jitter ADR-0096, the aux-term fixed point ADR-0085,
STOP-class decode ADR-0090, entropy guards) — a writing session, listed under "documentation passes"
in the checklist.

## Next session picks up

1. **M12 scoping session**: adjudicate forks A–F in `m12-plan.md`, pre-register the Build 1 numbers
   against the Build 0 cells, then Build 0 (the `-lookahead` directive + the day-zero paired read).
2. Chronicle D5 dogfood gate is due ~09-07 (two weeks from 08-24).
3. Field guide extension (documentation pass, when convenient).
