# 2026-09-23 (session 2) — the shakedown check-in, the state-ranking eval, ADR-0118

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **The review (user: "the documentation pass is next; check the shakedown; decisions?").** The chain
  healthy under the launcher since the 12:34 relaunch: the recipe arm closed 04:30 at 0.5240 ± 0.0112
  (day-zero 0.5185, +0.55pp, noise); the alloc arm at iteration 9 with 13.2 h of its 30 h used → closes
  ≈ 13:00 09-24 with its read, then shallow (≈ 20:30 09-25) and deep (≈ 03:30 09-27). Decisions taken
  (user): retired standing rules move to a SEPARATE file (`docs/standing-rules-retired.md`, archived not
  deleted); §15's probability rows are annotated with their outcome ADR, not re-estimated; the search
  section is a new design-doc §3e with the flag table in the quickstart; the state-ranking eval runs
  BEFORE the pass (an hour, CPU, a number for the arm reads and the big run's tripline); the pacman pin is
  in place (verified: `IgnorePkg` kernel + headers + nvidia-open module + nvidia-utils and companions +
  jdk-openjdk; the running kernel 7.2.6-1 = the installed one).
- **The alloc arm's admission drift read off its stores** (`search.jsonl` `ev:alloc` vs `ev:search`
  rows): the head admits 51% of windows at iteration 0 (= the b4post read's 51.3%) → 69% at 4 → 66% at 8
  as the per-cycle tau fell 0.369 → 0.045 → ≈ 0.02 under the trained head (its probability scale moved;
  in-loop AUC 0.65–0.76 vs 0.80 at the fit); floor admissions 5–8%; the head's windows cost 34 copy calls
  vs the recipe's 27–29 (it picks the bigger windows); per-game wall 171 s vs 198 s (−14%) → ≈ 20
  iterations in 30 h against the recipe's 16. Recorded for the arm's read; the verdict rule (gain per
  box-hour) prices it.
- **The state-ranking checkpoint-eval path** (`anvil.training.value_pretrain eval --ckpt … [--device cpu]
  [--write …]`; branch `state-ranking-eval`, worktree `../anvil-wt-staterank`, merged fast-forward into
  main as `8099b8d`): the frozen 1,197-row holdout of the state benchmark scored by any checkpoint's
  value head, CPU by default (a GPU eval beside the fleet trips the yield and eats an arm's box time; the
  run at `OMP_NUM_THREADS=4 nice -n 19` ≈ 25 s per checkpoint); the bank's iter-019-era examples widened
  9 → 14 globals with the vocab's format scalars (what the loader emits for these records today; a
  pre-Build-4 checkpoint is zero-padded there by `load_compat` and reads the same either way); a
  bootstrap SE per row. Validation: `m12-build1` 0.3921 (record 0.3919), `m12-build1-stopstate` 0.374
  (record 0.375). Tests 3 (`test_value_pretrain_eval.py`). Rows in `data/runs/shakedown/state_ranking.jsonl`.
- **THE FINDING → [ADR-0118](../decisions/ADR-0118-value-head-drift-under-the-loop.md):** day-zero 0.374
  → recipe iter 0 / 1 / 2 / 4 / 9 / 12 / 15 = 0.321 / 0.343 / 0.344 / 0.275 / 0.302 / 0.299 / **0.256**;
  alloc iter 0 / 4 / 8 = 0.333 / 0.275 / **0.256**; `iter-019` 0.335. ≈ four SE, from the first cycle, in
  both arms, while network-alone strength stayed flat and the lookahead gap drifted up (+0.3 → +3.3pp at
  ± 2.5). Mechanism: the loop trains the head on V-trace outcome targets (`--value-weight 0.5`, no
  critic on the chain) with nothing anchoring it to the rollout composites it was fit on, and the head is
  the search's leaf. Neither the Build 5 kill nor the tripline — a third shape, the landmine the shakedown
  exists to catch. Decisions (user): the arms run to their verdict; the Spearman becomes a per-iteration
  battery row + guard (worktree, lands after the shakedown); the settings pass opens with a value-anchor
  arm (a replay term on the Build 1 state + leaf banks; pre-registered bar: within one SE of 0.374 across
  the pass, strength not worse than the un-anchored winner); the eval merged now. Standing rule
  (training-loop design), plan record, Now block, map (Latest + the ADR index).

## Broke / surprised me

- `_p()` resolves REPO-relative paths against the WORKTREE root: the checkpoints' `abilities` stem is
  repo-relative → `data/embeddings` symlinked into the worktree (the e4head worktree's pattern).
- The bank's examples carry 9 globals (the M9 fmt one-hot included); every Build 4 net is 14 wide even
  for pre-Build-4 checkpoints (load_compat pads the weights, not the inputs) → the widening is on the
  example side, with the format's true scalars.

## Next session picks up

- **The documentation pass (4¾)** — this session continues into it: the retired-rules file + the prune,
  the design doc's §3e (search as the behavior policy), §3d′ (targets served, the gate retired), §13
  (Build 0–4 + the shakedown), §15 annotations, the Build 5 tripline's third shape, the quickstart's
  `-search*` flag table + the fleet, the field guide's new traps, the Now block trimmed (the narrative
  archived verbatim in `status-archive.md`), the canonical register review, the map, the index.
- After the shakedown (one worktree, lands with `nan-guard` + `certifier-merge`): the value anchor
  (`--value-anchor`), the Spearman battery row + guard, the `pause` STOP scope fix.
- The alloc arm's read ≈ 13:00 09-24: add its Spearman row by hand (`value_pretrain eval` on its final).

## The documentation pass (Build order 4¾) — DONE, this commit

- **Standing rules:** eight rules moved verbatim to the new `docs/standing-rules-retired.md` (seven the
  M10 planner route's emission / schedule / binding-execution mechanics, one the ADR-0114 strike), each
  with its date and reason; the active file 109 → 102 rules, its maintenance note now says "move, never
  delete"; the `format-onboarding.md` pointer annotated (the doc is UNWRITTEN; the quickstart is its
  Constructed instance — routed to the closeout).
- **Design doc:** a new **§3e "Search as the behavior policy"** (what it is — one ply, determinized,
  learned leaf at the next quiescent window, CRN rolls, the natural line always in the set; the acting
  rule; the surface round + the lifted value; the pay slot; the deep slot and its price; the allocation
  head; the loop's merged record + in-line distillation; what did not work — the offline re-warm; the
  measured numbers; what it is NOT — MCTS, serve-time search); §3d′ brought to Build 4 (targets landed at
  the AI's trigger-preparation site, the mode gate retired, the served set of record, the pay head
  withheld to the loop, the coverage bound); §4 the value head's M12 note (built at Build 1, drifts under
  the loop, audited + anchored — ADR-0118); §6 the distillation note; §13 the Build 0–4 + shakedown
  record; §15 rows annotated with outcomes (lookahead worth, offline distillation negative, format
  features emitted, the GPU never binding).
- **Quickstart §7:** what the search is (for the reader who arrives from the recipe string), the recipe of
  record, a 19-row `-search*` flag table with defaults read off `AnvilRun.java`, and the fleet paragraph
  (`--servers N`, N = ceil(workers / 12), the 24 × 2 recipe, `YIELD`, load as part of the recipe).
- **Field guide:** seven new traps — the game-neutral probe (≈ 2.7pp), the copier's AI-memory gap +
  trajectory continuation, TestNG class-order pollution, the row-space convention check (46% self-targets),
  the untrained served head (−2.6pp), the value head's drift off rollout truth, the subset distillation
  anchor; a search paragraph (not MCTS; the lookahead is worth +2.5pp to ANY policy; the void class); four
  reusable pieces added (the search directive, the run launcher, the state-ranking audit).
- **Canonical register:** an M12 line on each of the eight entries (the schedule machinery gone; plan-level
  search BUILT in its option form; plan-complete slots LARGELY BUILT as surfaces; payment matching → the
  withheld pay head; credit → the leaf's anchor question).
- **CLAUDE.md:** the Now block's research bullet trimmed from ≈ 4,000 words to running / next / pins; the
  09-06 → 09-23 text moved verbatim to `status-archive.md` (a dated snapshot section + its Contents line);
  the checklist's documentation-pass line re-dated.
- **Map:** the Now header + M12 pill + running/next paragraph, the fork-pin row (05fea7938d ← cbe386d2c6 ←
  57337a7e38), the generation-recipe row (the fleet), a served-build row, the stats (53.5% merged-jar era,
  +2.3pp one ply of search, 76.1M params, ~350 g/h searched), the throughput-levers watch item retired.
- **Plan:** 4¾ marked DONE; the kill list gains the tripline's third shape (ADR-0118). `docs_index.py`
  names the retired file; the index regenerated.
