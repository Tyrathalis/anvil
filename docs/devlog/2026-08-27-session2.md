# 2026-08-27 session 2 (the M10 BUILD session — graft R1–R4 land end-to-end)

## Done

- **Session-open adjudications (user, all four on the recorded leans):**
  trigger-2 = ANY opponent action, trigger-3 = END STEP, slot cap =
  6 + mask, failed-slot = consumed by revision; jar plan approved.
  Recorded in [m10-plan.md](../design/m10-plan.md) with a new
  build-era operational-pins section.
- **ADR-0025 jar gate PASSED for `7c4af49fa4`** (both mechanical
  smokes rerun vs the proven-jar banked outputs: schedfile/choicefile
  byte-identical, 72/72 + 84/84 labels rows and 9,969 + 17,409 census
  rows identical modulo ms, ZERO diff classes — the patch is
  harness-side only). Build-era jar pinned; the crash-class watch
  rides future generation free via `crash_why`.
- **[m10-build-spec.md](../design/m10-build-spec.md) born** — the
  implementation mechanics of record under the adjudicated structure.
  Key honest pin: attention-grounded slot tokens cannot be strictly
  bit-identical at day zero (v1's additive injection rode a token M1
  pre-paid; six new positions change attention by PRESENCE) — the v2
  identity contract is absent/closed exact + content-invariant at
  zero init + presence MEASURED AND BANKED as the day-zero floor.
- **R1 model surgery** (`6209549`): StateAssembler slot tokens
  (zero-init `sched_proj`, entity-grounded, cap 6), AnvilNet decode
  head (autoregressive pointer, STOP at the PASS slot, `cand_mask`
  reuse), E head ([PLAN] → 7 EOT axes), R head (per-step decoder
  state → 2 ledger axes — corrected from slot-token outputs: R is an
  emission-window prediction per the probe), `act(sched_decode=True)`
  greedy emission. 5 contract tests green.
- **R2 loader/losses** (`55babb8`): `sched_targets.py` (E/R/decode
  targets; census conventions IMPORTED from the pinned scripts —
  single source), `game_trajectories(sched=True)` + side tensors,
  sched aux term (CE + E/R smooth-L1 at emission rows, clamps at
  birth), w_sched ADR-0057 calibration + share + guard, **PG staged
  mask = advantage mask at pay_class rows** (label-zeroing would
  poison the mu tripwire — route 1), lr split sched_ 1e-3 /
  assemble.sched_ 1e-4 from first launch. Integration smoke clean.
- **R3 serve carry, proven on the real bridge** (`348bd29`):
  `sched_serve.py` (emission at first own MAIN1 window, statuses
  advance, revision ONLY at the 4 triggers, exhaust refire-guarded,
  failed slots consumed by revision), mu `sched` field = verbatim fed
  serialization + emission record, loader reads it verbatim. 4-game
  smoke ×2: **trigger-2 needed an opponent-priority-PASS filter**
  (485 false revisions → 185); zero-init runs row-identical across
  different revision content (content-invariance live); **round trip
  ingest → loader → learner ran with TRIPWIRE 0 VIOLATIONS on a
  live-conditioned store** — the discrete carry's bit-exactness
  claim, proven end-to-end.
- **R4 telemetry + day-zero banking**: `graft_sched_init.py` →
  `m10-sched-init` (pay params KEPT at design init — ADR-0073
  retirement executed loudly; v1 plan params stripped); pinned
  reliance population `m10-reliance-pop-20260827` (24 fresh
  graft-era games, seed base 20530827); `sched_reliance.py`
  driver-wired per accepted iteration; SchedServe counters →
  `<mu>.counts.json` at server stop (SIGTERM = SIGINT path now);
  battery series registered. **Day-zero banked: presence floor
  argmax_flip 0.0125, content_flip 0.0 EXACTLY (4,715 windows), aux
  CE 2.609 / E 0.522 / R 1.800, sched_rms 0.**
- **Pay surface live from the graft**: the pay tag auto-advertises
  off `has_pay` — 286 pay windows / 24 games (11.9/g, the known
  rate) answered auto at the +2.0 init, each arriving WITH schedule
  conditioning (slot-conditions path live; the marked-candidate bit
  is R5).
- **Found + fixed in passing:** `_rl_summary` omitted `plan_share`
  ⇒ the ADR-0057 plan-share guard was DEAD across run20
  (regression-tested); `d6-run18-i000` had been deleted with the M9
  stale-data pass, silently skipping the v1 loader test and dangling
  the v1 reliance default — RESTORED from kopia daily-7 (36.9 MB).

## Broke / surprised me

- The day-zero presence floor (1.25% argmax flip from six zero
  tokens) is 2.5× v1's absolute KILL threshold — **the v2 kill/FUND
  numerics must be posed relative to the banked floor, never
  absolute** (flagged in the spec for the numerics session).
- Day-zero emitted schedules are max-length garbage (527/602 at
  length 6) — expected from a fresh decode head; the ambition
  telemetry's reference dist is the training readout.
- Trigger-2's first cut counted opponent priority PASSES as actions
  (~11 revisions/turn). The wire-history filter (priority entries
  need a host) got it to plausible rates; false-trigger rate stays a
  first-class no-op-revision readout.

## Next session picks up

- **R5**: ADR-0075 label ingestion (seqlabels-join genre; update the
  `dataset.py` pay-invisibility comment LOUDLY), `cand_paymark`
  marked-candidate bit at pay windows, evalset repair, cousins +
  costmod + pool-tie engine touch, best-arm seed supervision.
- Then the **training probe launch session**: kill/FUND/unmask +
  read-protocol numerics pinned pre-data (floor-relative flip
  thresholds), recipe script, detached-run checklist.
- Suite at 252 green; all four rungs committed.
