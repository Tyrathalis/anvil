# 2026-07-17 — run-3: first guard halt, and done-when #4 resolves TRUE

Overnight session (the run launched 2026-07-16 morning; this file carries
its close). Full resolution: [ADR-0019](../decisions/ADR-0019-d6-donewhen4-resolution.md).

## What happened

- **run-3 (guarded, lr 2e-5, `-reask`, hinge floor) ran 13 healthy
  iterations and then halted itself** — the veto guard fired at 41.3% (1.5×
  the 25.1% baseline) and the driver rejected iteration 13's checkpoint and
  exited 3. First live guard halt; cost ~10 minutes of compute where run-2's
  unguarded collapse cost ~5 hours. Entropy never moved (0.17–0.21 all run —
  the hinge held), KL never approached its tripline: this was pure veto
  drift, run-1's structural leak continuing at 2× lr, NOT a run-2-style
  entropy runaway.
- **Strength grew the whole way:** 400-game arms init 50.75 → i4 50.0 →
  i9 52.5 → i12 54.75. Last accepted checkpoint = strongest.
- **The 2,000-game corrected read on iter-012: raw 0.5220 ± 0.0112 /
  corrected 0.5213 ± 0.0110. Paired vs the D5 BC arm: +6.75pp ± 2.31,
  t=2.92 — done-when #4 SATISFIED. The agent is ~2σ better than the
  heuristic it imitated.** Record: `data/runs/run3-final-arms-report.json`.
- Mid-run: two GPU-cotenancy OOMs beside ComfyUI (10.45 GiB resident) —
  first fixed by expandable-segments (fragmentation), second by
  `--rl-seg 128` (true peak halved, ~8% throughput cost); both baked into
  the driver (`bbf8f1d`). VRAM-elasticity QoL item queued (task list +
  ADR-0019 notes): per-phase seg autotune + OOM-catch-retry.
- Babysitting practice validated end-to-end: per-iteration monitor events,
  two false-positive staleness alarms tuned out (90→150 min for arms
  iterations), the guard halt surfaced within minutes and the closing evals
  ran overnight unattended.

## Findings / open front

- **Veto drift is the named next problem** (ADR-0019 #3): 25→41% over 13
  iterations with re-ask plugging the reward leak — the policy still learns
  to prefer engine-rejected casts. Levers queued: small veto penalty in
  reward (needs a §3d pin), first-attempt veto rate as the monitor metric
  (chains inflate the current one), per-head entropy floors, X-inclusion
  refinement. Rescue rate declined 10.6→8.4% as drift rose (drifting toward
  *unrescuable* casts) — consistent with the penalty framing.
- All four M2 done-when clauses are now satisfied. **Closing M2 / opening
  M3 planning is the next session-level decision** — deliberately not made
  unilaterally overnight.

## Session 2 (same day): M2 closeout

- **M2 CLOSED ([ADR-0020](../decisions/ADR-0020-m2-closeout.md))** after the
  agreed closing actions:
  - **forkcheck `-grpc` re-cert under micro-batching** — a real finding: the
    3-cell matrix (record `data/forkcheck/run-20260717-grpc-mb/`) exonerates
    the batcher bit-for-bit at the BC ckpt (40/40 twin, D1-identical) and
    attributes twin-determinism loss to the RL ckpt itself (36/40 solo,
    argmax near-ties from entropy growth) plus bf16 batch-composition
    sensitivity under load (32/40). 0 statics anywhere. Gate protocol
    amended: engine bumps certify with the BC ckpt.
  - **Fallback class closed**: all 78 = combat-declaration answer edge
    (~0.1% of combat windows); "run-1 had 0" was a misread.
  - **Iter-013 veto forensics**: 76% unpayable, 73% of commander vetoes
    post-first-cast — the commander-tax observation gap is the leading
    veto-drift cause candidate ([run3-veto-forensics.md](../design/run3-veto-forensics.md)).
- **[m3-candidates.md](../design/m3-candidates.md)** drafted (4 tracks +
  the strength-vs-platform framing question) — the seed for the dedicated
  fresh-context M3 planning session.
- Ops: a mis-launched control run parked Forge's modal crash dialog on the
  desktop (ForkFidelityCheck lacks the worker path's headless handler —
  worklist item filed); the pkill self-match gotcha bit a third time —
  memory note now prescribes the `[.]`-bracket pattern outright.

## Next session picks up

- M2 closeout discussion (plan doc + closing ADR) and M3 seeding — with the
  veto-drift lever list and the community-archaeology follow-ups (the
  #ai-plotting reply is drafted and waiting; the #11161 rebase items are
  spec'd in the worklist).
- VRAM elasticity (task #12) — now safe to implement, nothing running.
- Flexible: iter-013's rejected checkpoint is kept for forensics (what does
  a 41%-veto policy actually try to cast? cheap census dive, might sharpen
  the penalty design).
