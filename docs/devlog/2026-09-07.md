# 2026-09-07 — M12 Build 3 opens: the decision surfaces (evening 1: entity one + entity set) and the ability representation pin

**Doc status:** devlog (session record). Plan: [m12-plan.md](../design/m12-plan.md). ADR: [ADR-0105](../decisions/ADR-0105-m12-build3-decision-surfaces-and-ability-representation.md).

## State review (afternoon)

Build 2 closed on both reads (ADR-0104 addenda; the morning's control-arm read is in
[2026-09-06-session6](2026-09-06-session6.md)). The Build 3 review found the Java side complete and
proven (20 callbacks named in the observation, seven answer shapes enumerated, `sub` rows) and the
Python side untouched, plus three gaps the plan never named: no wire tag for any surface (a
trained head could not be served or read), no mainline surface acting, no payment-class tag. Free
labels: the six Build 2 arms = 12,000 sv=3 games with the heuristic's surface answers (~18.5
non-trivial per game). No `sub`-row pool (the arms ran surfaces off). Option strings for modes and
triggers are ~99% outside the pinned SA vocabulary.

Decisions (user, 14:00–15:30): train and serve together, one surface per evening; one option-set
decoder with three decoding modes rather than three heads; **the system's ability representation
= the pinned LLM over the ability's canonical engine text (script parameters + description), keyed
by hash, one shared table** (fork K resolves: re-segmentation by construction, the auxiliary loss
= the frozen probe first); imitation warm start then sub-row distillation; the sub-row pool
generated first; evening order entity one/set → mode → ordering + damage → payment tag → mainline
acting; naming to the closeout.

## The surface-label run

`scripts/build3_surface_labels.sh` — bridged self-play on the day-zero ckpt from a worktree pinned
at `343901c` (`../anvil-wt-b3label`, `data/` symlinked), jar snapshot of `b4825285529`, rate 1 /
rolls 1 / `-searchsurf 2 -searchsurfcap 8` / `-searchact 0.10 -searchtemp 0.025`, obs + census +
labels, 1,000 games, 8 workers. **First launch (14:42) poisoned every game at turn 1**: the
8-worker first-window burst under the expansion round pushed an ask past the worker bridge's 5 s
default; the same seed plays clean single-worker (146 windows, 9 sub rows, window ms p50 398 / max
2,466). Relaunched 14:56 with the worker deadline at 20 s (`ANVIL_EXTRA_JVM_OPTS`, the orchestrator's
hook); first 4 games clean, ~30 sub rows / game, act rate ~7%, window ms p50 325 / p90 1,394 / max
9,272. Throughput ~460 games/h alone, ~120–250/h while the fits and builds share the box. Armed:
watchd (stall 60 min), notify in the wrapper, a session monitor on poison/crash/completion.
Follow-up routed: the orchestrator should raise the deadline itself when `-searchsurf` is in the
forge args.

## The ability representation (fork `b0567608938`, then `a0ed9e314b5`)

- `AbilityKey` — `canon(sa)` = host (+ origin), `T:` trigger params, `K:` keyword original,
  `ALT:`/`OPT:`, `A:` api + sorted script params per chain link (AI hints and description keys
  dropped), `D:` the full description; key = SHA-256/16 hex. `"ak"` on every ability-bearing
  option entry and answer (`decPriority`, `peekPriority`, `castPlan`, `Surfaces.optJson`); the
  session emits each key's text once per game as `"abil":[…]` on the dec that introduced it.
  `AnvilRun -abilities <names> <out.jsonl>` enumerates the pool inside a throwaway Game (the
  null-owner UI path left spells and keyword traits out: 1,424 abilities / 484 empty cards; with a
  Game **1,701 cards → 5,148 abilities, 0 errors**; the 6 "empty" names are split-card faces).
  `AbilityKeyTest` (3) passes. Cache: `anvil.encoder abilities` → `abil-cf2ca6ba-qwen3`
  (5,148 × 2,560, Qwen3 rev `5cf2132abc99`) + the description-only control.
- **The frozen probe** (`scripts/ability_effect_probe.py`, `data/runs/build3-ability-probe/`):
  every effect class (32 with ≥ 20 positives) linearly decodable from the canonical-text embedding
  at AUC 0.98–1.00; the description-only control 0.80–0.99; the canonical text adds most where
  the script carries what the sentence does not — ChangeZone destination 0.84 vs 0.68 (majority
  0.32); produced-mana color 0.875 vs 0.909 (majority 0.26). **The auxiliary effect loss stays an
  instrument** (ADR-0105 item 7).
- The second fork commit: the resolving ability's key rides the surface dec as `args.sak` (the
  generator passes the `sa` object; `Surfaces.dec` turns it into the key), and **the serve wire**
  (below).

## The Python side (Anvil `<commit>`)

`anvil/policy/surfaces.py` (the option-set fields, shared by loader and featurizer; `AbilityCache`);
`dataset.py` `surf_*` tasks (`args.surf` names the shape) + collate; `AnvilNet._surface_decode`:
the target decoder restricted to the named option set — entity options key on their row, players
on the player key, abilities on host row + the ability-table vector, each + a kind embedding, STOP
= the option count; query = `surf_query([state, src, prev]) + slot`, src = the resolving ability's
vector + shape + method embeddings; masks: padding, picked, STOP closed below min / forced at max.
`surf_query`/`surf_key` start as copies of `tgt_query`/`tgt_key` (load_compat), so priority
windows are byte-identical and the surfaces train freely. Serve: tags `mtg.surface.entity_one` /
`entity_set` (featurize + server, gated on a fitted decoder in the ckpt), Java hooks ask
`selectOne` / the new `selectSet(min, max)` when the seat bridges the tag. **Wire smoke**: 3
self-play games on a small fitted ckpt — 21 bridged surface answers, 21 accepted, 0 poison.

`anvil/training/surface_fit.py`: imitation on the 12 ingested Build 2 arm stores (chained per
store; MultiStore refuses overlapping game ranges), cross-fit by (store, game) hash over 5 folds,
per-shape / per-method held-out agreement with the heuristic. **Fold 0, adapter-only** (shared
decoder frozen): surf_one 0.257 (n 20,647; first-option rate 0.198; tutor/fetch
`chooseSingleCardForZoneChange` 0.232 at 12.4 options), surf_set exact 0.355 (n 6,365) — the
frozen query has no room; the role copies run next (numbers below).

## Next session picks up

1. Fold 0 with the role copies → folds 1–4 → `--build` → `data/training/m12-build3-e1/last.pt`;
   the read (`--read`).
2. Forkcheck of `a0ed9e314b5` vs the 08-21 seeds (both fork commits recording/serve-only) once the
   label run frees the box; then the surface smoke with the served head (`CKPT=` on
   `build3_surface_smoke.sh`) and the 600-game paired read (2 × 300 vs the heuristic, the fitted
   ckpt with / without the surface tags).
3. The label run's read: sub rows per kind, act rate, misses; ingest the store (the first with
   `sak`/`ak` on every option); the distillation term from `sub` rows.
4. Evening 2: mode (the shape with visible headroom).
