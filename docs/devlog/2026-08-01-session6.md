# 2026-08-01 — session 6

## What was done

- **Chronicle provenance UI built (the D2-backlog item, user's pick):**
  context menu on the binder spread's card grid (`fedfea5126` on
  `playable`). Owned card → "Opening history" (first-pull ordinal, copies
  owned, one line per opening: Day N + product, xN for in-pack duplicates;
  pre-journal acquisitions degrade to "Acquired before you kept records"),
  any card → "Where to find" (`sourcesFor` + today's in-print /
  not-yet-released / out-of-print status). New `copiesIn` helper on
  `ChronicleAcquisitionLog` keeps the identity string encapsulated;
  covered in the provenance round-trip test. Suite 35 green.
- **Context-menu positioning bug found by the user's play pass, diagnosed,
  fixed (`10c22003df`):** menus anchored on `getSelectionBounds()`/scroller
  `screenPos` (global window coords) but positioned in the dropdown
  container's local space — in mobile-dev's desktop shell the screen sits
  right of the nav sidebar, so every context menu drew shifted right by
  exactly the sidebar width (252px at default size). Stock latent bug,
  invisible on phones (screen at 0,0). Fix translates both rectangles by
  the container's screen position — identity on stock devices; upstream
  candidate (queue now five items).
  - **Diagnosis method worth keeping:** computer-use screenshots couldn't
    unhide the game window (compositor filter case-mismatch), so:
    temporary `ANVILDBG` instrumentation → second instance on an isolated
    profile (`-Duser.home` at scratch, prefs+chronicle save copied; user's
    live run untouched) → driven headlessly-ish via `xdotool` window-
    targeted clicks + `import` window captures (works under Wayland
    because the game is XWayland). Numbers nailed it in one shot:
    selection right edge 765 global, menu drawn at 1017 = 765 + 252.
- **v12 snapshot published** (`af21a8a423`, `2.0.14-SNAPSHOT-08.02`):
  merged 24 upstream commits (HOB/HOC editions, card fixes — zero
  conflicts), full desktop suite 419 green post-merge, release assembled
  (54,800-file manifest) and uploaded to the fork's `daily-snapshots`.
  Post-publish routine in full: jar sha256 = local = manifest entry;
  manifest parses through the published jar's own `DeltaManifest`
  (jshell); spaced-path raw res fetch at the manifest commit
  hash-matches; atom feed 200. Deployed clients see the update prompt.
- User's desktop game instance restarted onto the fixed jar mid-session.

## What broke / near-miss

- **The first `gh release upload` targeted CARD-FORGE's repo.** This
  clone's gh default repo is upstream (set for PR work), and upstream
  uses the identical release tag (`daily-snapshots`) and even the same
  `build.txt`/`version.txt` asset names. Only the account's lack of
  write access stopped the clobber (asset-delete 404; verified nothing
  landed via asset timestamps). Script now prints `-R Tyrathalis/forge`
  on both publish lines with a warning (`bd6eb87ada`); hazard banked in
  session memory (`gh-default-repo-targets-upstream`).
- Two silent zero-test runs: surefire `-Dtest='forge.gamemodes.chronicle.*'`
  matches nothing under the TestNG provider ("No tests to run", exit 0)
  — the working filter is the bare class pattern `-Dtest='Chronicle*'`.
  Caught by checking surefire-report timestamps before trusting green.

## Next session picks up

- Chronicle: D3 iteration continues through play (200th-pack bar, Pocket
  side-by-side); D4 Android SDK decision; remaining D2 backlog =
  save-slot browser, desktop scroll-bar indicator.
- Upstream-candidate queue (now five): fetcher `o:`, `imageKeyFileExists`
  fallthrough, FCollectionTest race fix, dead cardforge.org list entries,
  context-menu embedded-screen offset.
- M4: d6-run11 babysit glances / closing protocol when it ends; backfill
  session 4's baseline-eval RESULT placeholder.
