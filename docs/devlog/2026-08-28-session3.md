# 2026-08-28 session 3 (m10-probe1: launch → guard halt → read; ADR-0085)

## Done

- **Pre-launch review + LAUNCH:** verified every recipe asset on disk
  (repointed cousins dirs, m10-sched-init, label stack, jar from fork
  HEAD `5bbc397412`), all 42 flags against the driver argparse, fresh
  port 50070 / seed 20280828 collision-checked, suite re-run green.
  `scripts/launch_m10_probe.sh` (commit `20028fd`) with the ADR-0084
  gates verbatim in the header; detached launch, watchd self-registered,
  log watcher + per-iteration glance armed.
- **Found live: the counts-dump defect.** `_stop_server` sends SIGINT;
  under the detached launch the server inherits SIGINT=SIG_IGN (CPython
  installs no KeyboardInterrupt handler — verified in /proc SigIgn on
  the live process), so the R4 stats/counts path was skipped into
  30s-timeout + SIGKILL. Worked around mid-run with an external rescue
  daemon (SIGTERM into the idle stop window each iteration) —
  recovered the counts for iterations 1–2; iteration 0's serve counters
  lost. Paymark iter-0 baseline recovered offline from mu rows
  (178/5,195 = 3.43% → unmask bar 0.0685;
  `data/training/m10-probe1/paymark-iter0-baseline.json`).
- **The run: iter 0 accepted (content_flip 0.0138 off exact-zero in one
  iteration — D6-speed consumption), iter 1 accepted (content retreat
  0.0036, emissions 62.3% pure-hold / len 0.59), iter 2 GUARD HALT**
  (`sched_share 1.504 > 0.3`, ckpt rejected). User adjudicated: stop
  and read, don't push through.
- **The read ([ADR-0085](../decisions/ADR-0085-m10-probe1-read.md)),
  user-adjudicated: NO FUND, NOT a channel-KILL.** Forensics: the halt
  mean was spike-dominated (median share 0.178 under the guard; max
  step CE 7.7→46.8→543.5 at a stable ~3.2 median = decode-confidence
  blowup); mu-file emission trajectory shows a ONE-STEP collapse
  (day-zero 72% garbage-full-6 → 62.3% empty after one RL iteration,
  then flat with marginal recovery). Mechanism: the dense decode aux
  trains on the policy's OWN carried schedules and the head IS the
  emitter — self-referential loss, degenerate fixed point at empty;
  immediate-EOS is the min-CE hedge on garbage targets; the
  void-arms-are-free derivation (sched_penalty_derive) confirmed in
  vivo; the seed anchor (frac 0.05, 170 windows) ~20× too weak.
  Degeneracy-veto conditions met on both accepted ckpts. Aux heads on
  the pinned population stayed healthy → failure localizes to the
  emission loop, not surface/carry/conditioning. **Surgery routed:
  (1) ground dense decode targets in certified/minted schedules,
  (2) anchor the emission head (supervised emission / non-zero empty
  cost).** Standing rule born → standing-rules.md.
- **Housekeeping landed:** `_stop_server` SIGINT→SIGTERM; share guards
  (all five) read the step MEDIAN with mean fallback; new
  `--guard-sched-spike` (100× median step CE) names the blowup class;
  `_rl_summary` surfaces `med` + `spike`; regression test from the real
  iteration-2 numbers. Suite 254 green, ruff clean.
- CLAUDE.md M10 bullet snapshotted verbatim to the status archive
  (2026-08-28 probe-read compression pass) and compressed in place.

## Broke / gotchas

- The share guard fired on an outlier-dominated MEAN (median was under
  the bar) — statistically accidental, substantively correct (the
  spike growth is the disease). Now split into median-share guard +
  spike tripline.
- Watcher false-positive class: the paylabels join prints its 12
  known misses as `FRAME ANOMALY` lines every iteration — filter that
  exact pattern, and trip instead if the join summary deviates from
  263/12/5.
- 8 BridgePoisonedException game skips in iteration 0 = the known
  copy+resume watch-list family, rate unchanged.

## Next session picks up

- **The emission-grounding design round** (ADR-0085 surgery items 1–2):
  decide the dense decode target source (certified/minted schedules;
  the 96k sweep mint + seed pipeline exist) and the emission anchor
  (supervised emission vs certified best arms and/or empty-emission
  cost), then re-graft if params change (day-zero floors re-bank) and
  relaunch the probe on the standing ADR-0084 gates.
- Probe stores/ckpts kept for the design round (`m10-probe1`,
  iteration-2 REJECTED marker in place); the rejected iteration's
  artifacts must be cleared per the ADR-0076 relaunch rule if the
  recipe changes.
