# 2026-08-01 (session 3): repo hygiene + Status consolidation

## Done

- **ADR numbering collision found and fixed.** Two parallel workstreams both
  claimed ADR-0029 (`chronicle-scheduling`, 07-31, and
  `d3-experiment-a-resolution`, 08-01). The Chronicle ADR came first and keeps
  0029; the d6-run10 resolution renumbered to
  [ADR-0030](../decisions/ADR-0030-d3-experiment-a-resolution.md). All
  references updated (CLAUDE.md, m4-plan, devlog 08-01, project map — which had
  the collision too: its ADR index listed 0029 = d3 and had no Chronicle row at
  all).
- **Project map refreshed to 08-01:** ADR index rows 0029 + 0030, Chronicle
  card updated (D2 gate PASSED, feedback round 1, nav-trap fix, pill →
  "in flight — D2 ✓"), footer stamp + next-steps rewritten (was stuck at
  07-28).
- **Status section consolidated — CLAUDE.md 136KB → ~35KB.** All closed
  history (M0/M1/M2/M3 bullets + the playable-fork track's blow-by-blow) moved
  **verbatim** to [docs/status-archive.md](../status-archive.md) (script-
  verified: 29 bullets byte-identical in the archive, zero kept lines lost).
  Each closed track now has one compact bullet: headline numbers, ADR links,
  standing rules/hazards born there, archive pointer. Active tracks (M4,
  Chronicle) untouched. Workflow section now codifies the convention:
  on close, full bullet → archive, compact summary stays.
- **Everything pushed.** Fork `playable` (the 3 Chronicle commits
  `bb7e670cff`/`59adfb8707`/`003a941818`, upstream tracking now set) and Anvil
  `main` (was 2 ahead + this session).

## Broke / surprised me

- The collision was invisible from inside either workstream — each session
  checked the decisions dir at a different time. The archive convention plus
  smaller Status section should make the next-free-ADR-number check cheaper;
  lesson recorded to memory (check `docs/decisions/` for the next free number
  at ADR-writing time, not from the Status narrative).
- `security/playable-multiplayer` (local-only Anvil branch) reviewed and left
  deliberately: its file self-describes as the process record kept off `main`
  because it is long and process-shaped, not because anything is still
  embargoed. Note: the pre-push hook that once guarded it is retired, so a
  `git push --all` would publish it — harmless per its own header, but worth
  knowing.

## Next session picks up

- Research: stratified evalset re-split → `select` with ahead-positions → the
  (a2) run vs the 0.5121 baseline (per ADR-0030).
- Chronicle: D3 reveal scene + D4 Android spike (parallel-early).
