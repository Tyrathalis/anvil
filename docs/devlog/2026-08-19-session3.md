# 2026-08-19 — session 3: the D2a session — sickness spot-check flips an ADR claim, instrument v2 re-pin, D2a pins landed, coverage ledger born

## Done

- **D2a decision review (user-approved):** six pins landed verbatim in
  m9-plan D2 — label = raw engine verdict (positives first-attempt
  `unpayable`, negatives accepted casts) with AUC stratified by D1
  verdict; substrate `[STATE] ⊕ candidate-entity token` primary
  (recorded extension of the M6 probe rule); 5-arm baseline ladder with
  the ≥0.03-AUC margin over obs-arithmetic (ADR-0043 reconstruction
  discipline); gate 0.75 / 0.60; **D2b routing inverted pre-data** —
  high AUC ⇒ skip D2b entirely (forfeited secondary falsification noted
  deliberately); fit sampled trio, transfer-read argmax + elevated.
- **The sickness claim flipped (user question → spot-check):** ADR-0063
  said sickness was absent from the obs schema; it is NOT —
  `ObsSnapshot.java:256` emits `sick:1` from engine `isSick()`,
  transform feeds it as entity feature #5. Spot-checks against raw
  observations: `tap_ability_sickness` was never sickness (12/12 hosts
  entered turns earlier, sick=0; mostly no_shape_fit outside the gate);
  `payable_needs_creatures` was MAJORITY real sickness visible in the
  obs (8/14 all-sick) — belongs in `knowable` — plus a previously
  unmodeled family: spend-restricted / board-cost production (Delighted
  Halfling `RestrictValid$`, Urza `tapXType`).
- **Instrument v2 (sick-aware) + baseline RE-PIN (user-approved):**
  ProdUnit(needs_tap/conditional/zone), three nested availability views
  (now ⊆ +conditional ⊆ +sick), new verdicts `sickness_short` /
  `ability_sick` / `conditional_production`, guessing branches retired,
  `ActivationZone$ Hand` mana (Simian) counts from hand never
  battlefield, tapped hosts keep non-tap-cost abilities. One first-pass
  regression (tapped/sick variable sources blocking knowable verdicts)
  caught by the v1→v2 window diff, fixed, regression-tested. 32
  instrument tests, suite 198 green. **Re-pinned (ADR-0063 addendum):
  knowable 0.5392 / 0.5097 / 0.5336 / 0.6044; collapse baseline 0.0588
  sampled / 0.0435 argmax; validity improved everywhere (0.986–0.991
  main, 0.9846+ climb); climb signature unchanged (i011 kvr 0.1049).**
  v1 outputs kept at `*-v1` (43M, regenerable) — M9-close re-price.
- **Combo-enabler direction (user):** Sprawl/Earthcraft-class reasoning
  must be *plausible*, not hand-held — perception floor now, targeted
  Grindstone drills later. Audit: rules text in embeddings ✓,
  attachments ✓, Earthcraft loops expressible via SA interface today ✓,
  **choice-state MISSING from obs** ("as enters, choose" — engine
  `getChosenColor()` exists, never emitted) → queued as boundary-bundle
  rider. Yield-differing taps pinned consequential in the D3 flag spec
  (Sprawl forest taps for 2). Pool facts: Earthcraft/Sprawl/Wild
  Growth/Arbor Elf in `cf2ca6ba`; Squirrel Nest not.
- **Decision-surface coverage ledger added to anvil-design-v2.md §3**
  (census-grounded): model answers priority/mull-keep/trigger-yn/
  binary/number/combat; excluded families ranked — payment (M9),
  mid-resolution object choices (tutor/fetch targets — the sleeper),
  stops (M10), cost-composition cousins (convoke/improvise/delve — D3
  must record stance, line added to m9-plan), trigger ordering, modal
  re-entry (measured as no_shape_fit), library ordering,
  replacement/static, chooseCardName, mulligan bottoming, concession.
  Standing rule: one decision surface per milestone, §3c template.

## Broke / surprised me

- The v1 instrument's two "sickness" buckets were both wrong in
  *different* directions — the guess-based taxonomy survived the
  validity bar because accepted casts never exercise the guessing
  branches. Lesson: negative-control bars validate arithmetic, not
  attribution; attribution claims need spot-checks against raw state.
- My first v2 cut broadened `variable_amount_source` scope (any host
  incl. tapped/sick) — the v1→v2 migration diff caught 249 windows
  wrongly downgraded from knowable. The diff-before-re-pin habit paid
  for itself immediately.
- Delighted Halfling's restricted mana and Simian's hand-activation
  were both silently miscounted in v1 — optimistic-direction-safe, but
  the new `conditional_production` family is a real D3 enumeration
  input, found only because the sickness spot-check dragged the
  none-sick cases into the light.

## Next session picks up

- **D2a build:** feature dump (frozen_probe pattern; `[STATE]` +
  candidate token at veto/accept windows from the v2 instrument's
  joined labels), 5-arm ladder fits, stratified AUC read vs the pinned
  gate. Then the D2a ADR + D2b routing decision.
- Boundary bundle now carries: rebase + §3c surface + store-namespace
  fix + multi-format + forensics + **obs choice-state emission** +
  2-arm trim verdict.
- M9-close stale-data candidates accumulating: run17 climb dirs (2.1G),
  v1 classifier outputs (43M).

---

## Addendum (same session, continued): D2a BUILT and RESOLVED (ADR-0064)

- **Build:** `scripts/affordability_probe.py` (labels → features →
  probe; 7 tests, suite 205). `source_views()` extracted from
  `classify_window` (behavior-identical, tested). Labels: 2,448/5,962/
  27,203 pos across sampled/argmax/elevated; negatives = accepted
  first-attempt casts, land/pass excluded, transfer-capped 40k logged.
  Features: `ev.example` for collate-exact tensors + one extra
  `assemble` for `entity_row_of` (first attempt hand-mirrored the
  tensor dict and died on collate drift — the lesson is use the
  producer, not a mirror). Leakage guard tested both ways:
  obs-arithmetic features respond to the obs (sick flag) and are
  invariant to engine-verdict fields (`corroborated=False` everywhere).
- **Result — gate PASS, high branch:** `[STATE]⊕cand` holdout AUC
  **0.8809** (pins 0.75/0.60), margin over explicit obs-arithmetic
  **+0.0378** (pin ≥0.03), transfer no-refit **0.8967 argmax / 0.8917
  elevated**. Veto gap = behavioral/interface. **D2b SKIPPED** (pinned
  routing). Ladder: cost_pips 0.7163 / obs_arith 0.8431 / state 0.7671
  / state⊕cand 0.8809; prevalence 0.155.
- **The finding inside:** on the auto-payer-artifact stratum
  (obs-payable, engine refused) the arithmetic arm is **chance
  (0.5385)** while the trunk reads **0.8422** — the model partially
  predicts `ComputerUtilCost`'s own willingness refusals. The entire
  arithmetic margin lives there. Banked as a D3 design input: the
  engine payer is not the oracle; the model already out-predicts it on
  its own refusal families.
- **Premise chain now:** vetoes knowable (D1) ∧ affordability
  represented (D2a) ⇒ collapse should follow expression — a D5
  falsification would indict the surface/incentive, never the
  representation.

## Next session picks up (updated)

- **D3 rung 1: the engine capability audit** — can the fork engine
  *execute* a directed chained-activation payment if handed one?
  (`ComputerUtilMana` archaeology; result recorded either way; boundary
  re-price if no.) Then the fork-delta design (consequential-payment
  flag with yield-difference rule + cost-composition-cousins stance;
  legality-derived class enumeration) and the payment drill evalset —
  all before any run.
- Stale-data candidates: + `affordability-probe-d2a` (249M).
