# 2026-08-01: d6-run10 closes — reads, drill evals, ADR-0029

## Done

- **d6-run10 guard-halted at iteration 16 (KL 0.085 > 0.06; veto and
  entropy moving with it)** — a genuine multi-signal drift, halted by
  design; 16 accepted iterations stand, iter-015 = last accepted.
- **Closing reads (2,000 games each, paired, sequential matched load):**
  iter-004 0.5254 ± 0.0110 / iter-015 0.5273 ± 0.0109 corrected;
  vs baseline 0.5121: +1.40/+1.45pp at t≈1.4 (ties — gate NOT cleared);
  **vs the run's own init: +0.25pp (dead tie); iter-015 vs iter-004:
  +0.05pp (dose-response exactly flat).**
- **Drill evalset evals decomposed the flat result into a mechanism:**
  iter-015 on held-out drills: lost +6.6pp, long_shot +6.7pp (real,
  ~2.5σ, generalization to unseen positions), winnable **−9.7pp**
  (~5σ) — self-cancelling. Root cause found in the design: the D2.4
  evalset held out ALL winnable+coin drills, so the training list was
  ~100% behind-positions by construction. The model learned to fight
  from behind at the cost of converting from ahead.
- **Rej-split forensics:** drill trajectories carry 1.65× the rejection
  density of full games (2.44 vs 1.48 /100 windows) — the sustained
  §6c pressure behind the shaped-vs-masked anomaly flag that preceded
  the halt by 4 iterations (the flag was the right early warning).
- **[ADR-0029](../decisions/ADR-0029-d3-experiment-a-resolution.md):**
  experiment (a) at f=20% resolved strength-neutral with mechanism;
  one bounded rebalanced (a2) run licensed by the mechanism before
  escalating to (b) — direction put to the user. Ckpt of record and
  baseline unchanged.

## Broke / surprised me

- **The self-match class got a third scalp:** the read-chain waiter
  captured the i015 read's PID via `pgrep -f "final_read.py …"` — which
  matched the waiter's OWN quoted command line — so it waited on itself
  forever while both reads finished and notified hours earlier. The
  standing watcher correctly flagged the phantom STALL. Lesson now
  absolute: PID acquisition must be self-reported (`$!` at spawn or the
  process writing its own pidfile), never pattern-derived. The drill
  evals ran with `$!` capture and behaved perfectly.
- The iteration-9 wedge game recurred on its rotation and cost exactly
  one quarantined frame — the fix class working as a statistic.

## Session close-out (same day)

- **Decision (user): option A — the rebalanced (a2) run is the current
  plan** (ADR-0029 resolution appended): evalset re-split (stratified
  fraction per bin) → `select` with ahead-positions → (a2) vs the same
  baseline; (b) armed behind an (a2) negative.
- **Watcher self-registration SHIPPED:** `notify.py` gained
  `watch_register/watch_unregister` (the process reports its OWN pid —
  the pgrep-derived-pid class fully retired); selfplay driver registers
  at start and unregisters at all three deliberate exits (complete,
  STOP-file, guard halt) so a GONE alert always means a real crash;
  `final_read.py` likewise. `anvil_watchd` gained an `ANVIL_WATCH_DIR`
  test seam + 3 subprocess tests (roundtrip, dead-pid GONE + prune,
  register-refuses-dead-pid).

## Next session picks up

- **(a2) prep:** evalset re-split (stratified fraction per bin — whole
  bins never again consumed by holdout), `grindstone select` re-run
  with ahead-positions in training, launch (a2) with the standing
  recipe; consider the drill-side penalty basis while configuring.
- Queued: targeting-retry wedge forensics (worklist entry, repro in
  hand); KDE Connect channel when the user has it paired; D4 noise
  floor whenever the GPU idles.
