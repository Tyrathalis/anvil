# 2026-07-10

# Session 2 (daytime): D8 BUILT AND MEASURED IN ONE DAY — M1 CLOSED ([ADR-0009](../decisions/ADR-0009-m1-closeout.md))

## Done

- **CastPlan executor arc, morning-to-afternoon:** proto (CastPlan target
  refs/EntityRef/GameStart.header), Java realizer (legality-only; ladder
  shape→pay→kind→order; ~230 lines), oi labels + history ring + wire capture
  in Obs, GrpcBridge CONSTRUCT path, server model mode (AnvilNet.act greedy
  decode; featurizer loader-parity-tested), harness --census/--pairs-file/
  --bridge-seats, mixed-seat worker arg.
- **Pre-build measurement that shaped the design:** host-vs-SA ambiguity =
  23.6% of expert actions (5K games); ~31% of multi-SA picks have
  string-ambiguous ground truth (commander permission-route duplicates +
  cost variants) → first-fit ladder + `oi` logging now, SA-level candidates
  at M2 (where RL needs them anyway). Kind-prior tiebreak (land>spell>ability)
  measured from the same sweep.
- **Four smokes, two real bugs:** (1) fallback-flag misread as flag=false →
  infinite London mulligan (14.8K asks, 1GB obs cap — RAW_CAP guard earned
  its keep); fixed + observation bytes on all bridged requests + mulligan
  insurance cap. (2) divided-as-you-choose NPE at resolve — realizer now
  even-splits allocations. Modal spells (226/227 of remaining no-fit vetoes)
  cast mode-less per the M1 scope line. Smoke 4: 20/20 decisive, veto 7.9%.
- **Arms (1,000 games, 200 valpair pairs mirrored, ~1.5h wall):** model
  **46.8% ± 2.5pp** (δ=0) / **45.5%** (δ=+2.169, tie; halves vetoes) vs the
  heuristic; 398/400 decisive per arm, 0 draw clocks, 0.5% crashes; model
  arms 927–1,068 g/h vs heuristic 959 — **inference in the loop is
  operationally free at w=8**. M0's 65% veto → 3.1–7.8%, decomposed by
  reason. All three M1 done-when criteria satisfied → **M1 CLOSED**.

## Broke / surprised me

- The fallback-oneof default bug is the classic protobuf trap (absent oneof
  reads as zero value) — the fix is a protocol rule: NEVER read an answer
  field without checking fallback first.
- A pure imitator at ~1σ below parity with its teacher, through a day-old
  executor that garbles some ambiguous casts — better than I'd have priced.
  The order-rung (30% of multi-SA casts) and modal simplifications are the
  obvious recoverable winrate.
- jar rebuilds invalidate harness replay (sha256 pin, by design). Crash
  replays now: rerun the worker command from cmd.txt with
  -Danvil.crash.trace=true (added), from forge-gui cwd.

## Next session picks up

- **M2 entry**: fork-API hardening (prereq for rollouts) → rollout-labeled
  value targets → RL fine-tune from d7-ep3. Winrate arms (~12 min) are the
  standing strength eval per checkpoint.
- M2 action schema, by measured mass: SA-level candidates (oi labels
  accumulating from every future run), mode heads, allocation heads.
- Housekeeping: model server on :50051 (δ=0) may still be running — kill or
  keep for ad-hoc games; d8smoke* run dirs are disposable; the 5 arm dirs +
  d8-arms-report.json are the D8 record.

## Post-close retrospective (same day): M2 opening order resequenced

[ADR-0010](../decisions/ADR-0010-m2-opening-sequence.md): SA-level action
schema moves ahead of RL. The trigger was a play-vs-corpus measurement gap —
the executor's order rung handled 31–36% of casts in the arms vs the 13.6%
corpus-side estimate that had scoped it as a tail (battlefield permanents
with multiple abilities are offered every window). Also amended: rollout
value labels come from the BC agent, not the heuristic; D9 combat gets an
explicit slot (its "if M1 runs long" framing expired). Both repos pushed;
project-map.html (new living overview, docs/) updated. M2's first
deliverable = the M2 plan doc, seeded from ADR-0010.

# D7 CLOSED overnight: gate resolved TRUE (honest 0.9758), 3-epoch recipe, X un-plateaued, pass calibration done

Autonomous overnight session (user authorized "carry on into D8 if the epoch
answer is clear" — it was). Sweep launched 2026-07-08 evening, ep3 finished
~04:30 on the 10th; report + calibration ran unattended.

## Done

- **Pre-launch fix that mattered: per-epoch reshuffle** (`f150afc`). The
  loader's game shuffle was seeded once, so epoch 2+ of a multi-epoch run
  would have replayed epoch 1's exact order — silently contaminating the
  repetition-vs-diversity control. Now the epoch counter reseeds the shuffle
  (persistent workers re-call `__iter__` per epoch). Verified: orders
  distinct per epoch, deterministic across instances, eval splits untouched.
- **D7 epoch sweep** (three from-scratch runs, matched cosine schedules,
  merged D4 architecture 37.3M, pw=0.1, combined corpus 113,592 games,
  launched from a pinned worktree per the 07-07 lesson; chain exit 0,
  ~27.7h wall total, steady ~3,980 win/s):

  | run | steps | honest | nonpass | target | value BCE | wall |
  |-----|-------|--------|---------|--------|-----------|------|
  | d7-ep1 | 258K | 0.9647 | 0.9043 | 0.9719 | 0.573 | 4.6h |
  | d7-ep2 | 516K | 0.9719 | 0.9229 | 0.9749 | 0.571 | 9.2h |
  | d7-ep3 | 774K | **0.9758** | **0.9339** | 0.9754 | 0.574 | 13.9h |

  600-batch finals, val split, n_honest ≈ 148.9K. Per-tag (ep3): mulligan
  0.9969 (n=639), tuck 0.868 (n=250), binary 1.0 (n=39), number 1.0 (n=50),
  trigger 1.0 (n=4). Valpair +0.0–0.4pp above val throughout (standing
  denominator quirk; zero held-out-matchup gap). Value flat ~0.57 across all
  recipes — exactly as the 07-06 diagnosis predicted.
- **[ADR-0008](../decisions/ADR-0008-d7-gate-resolution.md): §15's high-80s
  row resolves TRUE, conservatively** — mid-90s honest at a pass-weight that
  sacrifices honest points. **Recipe: 3 epochs** (each epoch paid outside
  noise: nonpass +1.86pp then +1.10pp at SE ~0.33pp; returns halving, 4th
  epoch not scheduled). **ADR-0006 residual resolved: repetition works,
  diversity was not binding** — the corpus stop is retroactively vindicated.
- **X head un-plateaued (+20pp): the climb ADR-0006 saw stop was
  compute-starvation, not corpus limitation.** On the identical 672-window
  pilot-val basis (curve history: 0.573/0.637/0.674/0.647), the sweep reads
  **0.795/0.839/0.874**; combined-val basis (n=1,518, SE ~1.3pp) agrees to
  the third digit. `eval_x` gained `--store` to reproduce historical bases
  (split is a pure function of game index, so a store subset = its old basis).
- **PASS-logit calibration (D8 item 1) built and run:**
  `anvil/training/calibrate_pass.py` — caches per-window logit scalars once,
  bisects the offset to the expert pass rate, writes `pass_calibration.json`
  beside the ckpt. On d7-ep3: model under-passes 1.7pp raw (0.8861 vs expert
  0.9029, n=149.7K multi windows); **δ=+2.169 matches the rate and lifts
  honest 0.9755→0.9841** — the D5 "recalibratable post-hoc" premise confirmed
  exactly. Calibrated nonpass 0.8932 by construction (marginal action windows
  flip to pass); both operating points recorded, D8 play decides.

## Broke / surprised me

- Nothing broke: chain exit 0, no OOM, no quarantined-frame trips. One loss
  spike at step 750K of ep3 (2.29 vs ~0.4) was a single batch that happened
  to carry X windows (x-term 2.844, neighbors 0.0) — val evals on both sides
  unmoved. The 100-step loss print is a single-batch sample; remember that
  before reacting to spikes.
- The X result is the surprise of the arc: rare-label heads at 113K steps
  simply hadn't converged on ANY corpus size, and the "X plateau" that fed
  the stopping decision was a step-count artifact. (The stop was still right
  — repetition delivered the compute the rare labels needed, for free.)
- Honest landing ~9pp above the §15 prediction, at pw=0.1. The metric's
  candidate basis (timing-legal, payability unfiltered) is stricter than the
  design doc imagined, and the model cleared it anyway.

## Next session picks up

- **D8 remainder (the executor arc):** CastPlan executor in the fork — Java
  side that takes the model's composite answer (host + targets + X + costs)
  one-shot via the AI play path (`canPlaySa` with pre-set targets, per the
  M0/census map); expect the 65% M0 veto rate to collapse. Then real
  observations over the bridge + server-side inference (batch across
  workers), complete games vs the heuristic, **winrate recorded not gated**,
  bridge-tax re-measurement with real payloads. Executor scoping notes:
  serve mode answers at both census-mapped interception points; mid-resolution
  stays micro-step (ADR-0001); fallback provenance must stay tag-confined for
  the M1 done-when #2 claim.
- Choice to make at D8 eval time: play at δ=0 (action-rich) vs δ=+2.169
  (rate-matched) — run both arms, the winrate delta is the answer.
- **Decision (morning session): X overflow class 17 (">16") clamps to 16 at
  execution, logged when it fires.** ~3 casts per 100K sample; revisit only if
  the executor telemetry shows it firing on spells where X=16 is materially
  wrong (mass X-draw/X-damage finishers wanting exact lethal).
- Housekeeping done tonight: worktree removed, `d7-smoke` deleted, monitor
  stopped. Run dirs `d7-ep{1,2,3}` all kept (150MB ckpts; ep1/ep2 are the
  recipe evidence — prune after D8 if disk wants it).
