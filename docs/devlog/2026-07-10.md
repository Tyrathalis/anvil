# 2026-07-10

# Session 2 (daytime): D8 BUILT AND MEASURED IN ONE DAY — M1 CLOSED ([ADR-0009](../decisions/ADR-0009-m1-closeout.md))

## Done

- **CastPlan executor arc, morning-to-afternoon:** proto (CastPlan target
  refs/EntityRef/GameStart.header), Java realizer (legality-only; ladder
  shape→pay→kind→order; ~230 lines), oi labels + history ring + wire capture
  in Obs, GrpcBridge CONSTRUCT path, server model mode (AnvilNet.act greedy
  decode; featurizer loader-parity-tested), harness --census/--pairs-file/
  --bridge-seats, mixed-seat worker arg.
- **Pre-build measurement that shaped the design:** host-vs-SA ambiguity =
  23.6% of expert actions (5K games); ~31% of multi-SA picks have
  string-ambiguous ground truth (commander permission-route duplicates +
  cost variants) → first-fit ladder + `oi` logging now, SA-level candidates
  at M2 (where RL needs them anyway). Kind-prior tiebreak (land>spell>ability)
  measured from the same sweep.
- **Four smokes, two real bugs:** (1) fallback-flag misread as flag=false →
  infinite London mulligan (14.8K asks, 1GB obs cap — RAW_CAP guard earned
  its keep); fixed + observation bytes on all bridged requests + mulligan
  insurance cap. (2) divided-as-you-choose NPE at resolve — realizer now
  even-splits allocations. Modal spells (226/227 of remaining no-fit vetoes)
  cast mode-less per the M1 scope line. Smoke 4: 20/20 decisive, veto 7.9%.
- **Arms (1,000 games, 200 valpair pairs mirrored, ~1.5h wall):** model
  **46.8% ± 2.5pp** (δ=0) / **45.5%** (δ=+2.169, tie; halves vetoes) vs the
  heuristic; 398/400 decisive per arm, 0 draw clocks, 0.5% crashes; model
  arms 927–1,068 g/h vs heuristic 959 — **inference in the loop is
  operationally free at w=8**. M0's 65% veto → 3.1–7.8%, decomposed by
  reason. All three M1 done-when criteria satisfied → **M1 CLOSED**.

## Broke / surprised me

- The fallback-oneof default bug is the classic protobuf trap (absent oneof
  reads as zero value) — the fix is a protocol rule: NEVER read an answer
  field without checking fallback first.
- A pure imitator at ~1σ below parity with its teacher, through a day-old
  executor that garbles some ambiguous casts — better than I'd have priced.
  The order-rung (30% of multi-SA casts) and modal simplifications are the
  obvious recoverable winrate.
- jar rebuilds invalidate harness replay (sha256 pin, by design). Crash
  replays now: rerun the worker command from cmd.txt with
  -Danvil.crash.trace=true (added), from forge-gui cwd.

## Next session picks up

- **M2 entry**: fork-API hardening (prereq for rollouts) → rollout-labeled
  value targets → RL fine-tune from d7-ep3. Winrate arms (~12 min) are the
  standing strength eval per checkpoint.
- M2 action schema, by measured mass: SA-level candidates (oi labels
  accumulating from every future run), mode heads, allocation heads.
- Housekeeping: model server on :50051 (δ=0) may still be running — kill or
  keep for ad-hoc games; d8smoke* run dirs are disposable; the 5 arm dirs +
  d8-arms-report.json are the D8 record.

## Post-close retrospective (same day): M2 opening order resequenced

[ADR-0010](../decisions/ADR-0010-m2-opening-sequence.md): SA-level action
schema moves ahead of RL. The trigger was a play-vs-corpus measurement gap —
the executor's order rung handled 31–36% of casts in the arms vs the 13.6%
corpus-side estimate that had scoped it as a tail (battlefield permanents
with multiple abilities are offered every window). Also amended: rollout
value labels come from the BC agent, not the heuristic; D9 combat gets an
explicit slot (its "if M1 runs long" framing expired). Both repos pushed;
project-map.html (new living overview, docs/) updated. M2's first
deliverable = the M2 plan doc, seeded from ADR-0010.

# D7 CLOSED overnight: gate resolved TRUE (honest 0.9758), 3-epoch recipe, X un-plateaued, pass calibration done

Autonomous overnight session (user authorized "carry on into D8 if the epoch
answer is clear" — it was). Sweep launched 2026-07-08 evening, ep3 finished
~04:30 on the 10th; report + calibration ran unattended.

## Done

- **Pre-launch fix that mattered: per-epoch reshuffle** (`f150afc`). The
  loader's game shuffle was seeded once, so epoch 2+ of a multi-epoch run
  would have replayed epoch 1's exact order — silently contaminating the
  repetition-vs-diversity control. Now the epoch counter reseeds the shuffle
  (persistent workers re-call `__iter__` per epoch). Verified: orders
  distinct per epoch, deterministic across instances, eval splits untouched.
- **D7 epoch sweep** (three from-scratch runs, matched cosine schedules,
  merged D4 architecture 37.3M, pw=0.1, combined corpus 113,592 games,
  launched from a pinned worktree per the 07-07 lesson; chain exit 0,
  ~27.7h wall total, steady ~3,980 win/s):

  | run | steps | honest | nonpass | target | value BCE | wall |
  |-----|-------|--------|---------|--------|-----------|------|
  | d7-ep1 | 258K | 0.9647 | 0.9043 | 0.9719 | 0.573 | 4.6h |
  | d7-ep2 | 516K | 0.9719 | 0.9229 | 0.9749 | 0.571 | 9.2h |
  | d7-ep3 | 774K | **0.9758** | **0.9339** | 0.9754 | 0.574 | 13.9h |

  600-batch finals, val split, n_honest ≈ 148.9K. Per-tag (ep3): mulligan
  0.9969 (n=639), tuck 0.868 (n=250), binary 1.0 (n=39), number 1.0 (n=50),
  trigger 1.0 (n=4). Valpair +0.0–0.4pp above val throughout (standing
  denominator quirk; zero held-out-matchup gap). Value flat ~0.57 across all
  recipes — exactly as the 07-06 diagnosis predicted.
- **[ADR-0008](../decisions/ADR-0008-d7-gate-resolution.md): §15's high-80s
  row resolves TRUE, conservatively** — mid-90s honest at a pass-weight that
  sacrifices honest points. **Recipe: 3 epochs** (each epoch paid outside
  noise: nonpass +1.86pp then +1.10pp at SE ~0.33pp; returns halving, 4th
  epoch not scheduled). **ADR-0006 residual resolved: repetition works,
  diversity was not binding** — the corpus stop is retroactively vindicated.
- **X head un-plateaued (+20pp): the climb ADR-0006 saw stop was
  compute-starvation, not corpus limitation.** On the identical 672-window
  pilot-val basis (curve history: 0.573/0.637/0.674/0.647), the sweep reads
  **0.795/0.839/0.874**; combined-val basis (n=1,518, SE ~1.3pp) agrees to
  the third digit. `eval_x` gained `--store` to reproduce historical bases
  (split is a pure function of game index, so a store subset = its old basis).
- **PASS-logit calibration (D8 item 1) built and run:**
  `anvil/training/calibrate_pass.py` — caches per-window logit scalars once,
  bisects the offset to the expert pass rate, writes `pass_calibration.json`
  beside the ckpt. On d7-ep3: model under-passes 1.7pp raw (0.8861 vs expert
  0.9029, n=149.7K multi windows); **δ=+2.169 matches the rate and lifts
  honest 0.9755→0.9841** — the D5 "recalibratable post-hoc" premise confirmed
  exactly. Calibrated nonpass 0.8932 by construction (marginal action windows
  flip to pass); both operating points recorded, D8 play decides.

## Broke / surprised me

- Nothing broke: chain exit 0, no OOM, no quarantined-frame trips. One loss
  spike at step 750K of ep3 (2.29 vs ~0.4) was a single batch that happened
  to carry X windows (x-term 2.844, neighbors 0.0) — val evals on both sides
  unmoved. The 100-step loss print is a single-batch sample; remember that
  before reacting to spikes.
- The X result is the surprise of the arc: rare-label heads at 113K steps
  simply hadn't converged on ANY corpus size, and the "X plateau" that fed
  the stopping decision was a step-count artifact. (The stop was still right
  — repetition delivered the compute the rare labels needed, for free.)
- Honest landing ~9pp above the §15 prediction, at pw=0.1. The metric's
  candidate basis (timing-legal, payability unfiltered) is stricter than the
  design doc imagined, and the model cleared it anyway.

## Next session picks up

- **D8 remainder (the executor arc):** CastPlan executor in the fork — Java
  side that takes the model's composite answer (host + targets + X + costs)
  one-shot via the AI play path (`canPlaySa` with pre-set targets, per the
  M0/census map); expect the 65% M0 veto rate to collapse. Then real
  observations over the bridge + server-side inference (batch across
  workers), complete games vs the heuristic, **winrate recorded not gated**,
  bridge-tax re-measurement with real payloads. Executor scoping notes:
  serve mode answers at both census-mapped interception points; mid-resolution
  stays micro-step (ADR-0001); fallback provenance must stay tag-confined for
  the M1 done-when #2 claim.
- Choice to make at D8 eval time: play at δ=0 (action-rich) vs δ=+2.169
  (rate-matched) — run both arms, the winrate delta is the answer.
- **Decision (morning session): X overflow class 17 (">16") clamps to 16 at
  execution, logged when it fires.** ~3 casts per 100K sample; revisit only if
  the executor telemetry shows it firing on spells where X=16 is materially
  wrong (mass X-draw/X-damage finishers wanting exact lethal).
- Housekeeping done tonight: worktree removed, `d7-smoke` deleted, monitor
  stopped. Run dirs `d7-ep{1,2,3}` all kept (150MB ckpts; ep1/ep2 are the
  recipe evidence — prune after D8 if disk wants it).

# Session 3 (evening): M2 OPENED — plan doc landed, D1 fork-fidelity arc: both static bug classes fixed, the 50% divergence root-caused to card-id renumbering

## Done

- **M2 plan doc landed: [m2-rl-plan.md](../design/m2-rl-plan.md)** (seeded from
  ADR-0010, M1-plan pattern; Anvil `8a3be2a`). D1 fork hardening → D2 SA-level
  schema + retrain → D3 arms reprice → D4 rollout-labeled asymmetric critic +
  Ante mirror certification → D5 combat (flexible slot) → D6 first V-trace.
  Key framing: the rollout contract is **state fidelity + determinism, not
  trajectory reproduction**. Ante cert restored from §13 (ADR-0010 omitted it);
  yield macros stay parked (decision #10).
- **D1 same evening. Both ADR-0002 static bug classes root-caused and fixed**
  (fork `42e15f4822`): (a) face-down exiled cards rebuilt face-up from paper
  (45/60 mismatches) — exile copies now retain face-down + foretold; (b)
  "Keyword Effects" duplication (15/60) — Player's 7 field-managed effect
  cards (keyword/monarch/initiative/blessing/sprocket/radiation/speed) were
  copied by zone but never field-wired; new `Player.copyEffectCardsToSnapshot`
  follows the commanderEffect idiom; copier's `setBlessing` call (same latent
  dup) removed; gamePieceType now copied. **All 60 repro seeds: 0 static
  mismatches** (redistribute 39 divergence / 21 clean = background rates).
- **The 50% trajectory divergence root-caused: GameCopier renumbers card ids**
  (`nextCardId()` in zone-traversal order vs the original's history order; id
  order is AI-visible via Card.compareTo + id-keyed collections). Explains all
  three ADR-0002 signatures: deterministic across JVMs (value-keyed, not
  identity-keyed), copying canonicalizes (zone-traversal ids are stable),
  2.4%/fork fresh drift. **Fix: copies keep original ids** (id-taking factory
  overloads existed on all four copy paths; new
  `Game.dangerouslySyncCardIdCounters` prevents collisions). **Probe: 36/40
  previously-divergent seeds now clean; 20/20 clean controls hold; 204
  forge.ai.simulation tests pass.**
- **Residual divergence class isolated**: 4/40 still diverge, all reproduce
  EXACTLY across JVMs — deterministic second-order cause, likely the documented
  this-turn copy TODOs (creatureAttackedThisTurn/thisTurnCast/
  countersAddedThisTurn; seed 20260772 forks turn 12, 1-life delta turn 13).
  Not rollout-blocking (forks are internally deterministic; statics clean).
  Repro seeds: 20260714/20260764/20260772/20260783.
- **forkcheck grew `-seeds` (explicit repro lists) and `-twin` mode** (two
  independent mainline copies replayed from the same RNG snapshot = the
  rollout-determinism certification; fork `2e3c7e0c65`).
- **Overnight (running):** fresh 500-game baseline (same seeds/decks as 07-03,
  `data/forkcheck/run-20260710/`) + 200-game twin certification
  (`run-20260710-twin/`), both nice-19 sequential.

## Broke / surprised me

- The whole ADR-0002 "broken-but-characterized" fork story reduced to ~10
  lines of id plumbing + effect-card wiring. The id-renumbering hypothesis
  fell out of reading createCardCopy with the three ADR-0002 signatures in
  hand — the measurement arc (deterministic? canonicalizes? fresh drift?) had
  already eliminated everything else.
- `AnvilLobbyPlayer extends LobbyPlayerAi`, so `clonePlayer`'s instanceof
  check REUSES it — forked games get real Anvil controllers wired to the same
  bridge. The override-plan landmine is softer than feared; the real D1(b)
  work is bridge/obs binding for forked games (Obs/Census are static
  single-game writers — rollout mode should run obs-off with a derived wire
  gameId).
- Checkstyle failure mode: an import used only via method chaining
  (`setGamePieceType(c.getGamePieceType())` names the type nowhere) counts as
  unused. Also `-q` + piped tail masked a failed build once — check jar
  mtime, not exit codes of pipelines.

## Next session picks up

- Read the overnight results: expect ~0 static mismatches and divergence
  collapsed toward the residual floor (~5%?) on 500 games; twin run should be
  ~100% chain_clean (any twin divergence = copier/play nondeterminism — dig
  immediately).
- D1 remainder: rollout-contract mode in forkcheck (fork → completion under
  LocalRandomBridge seats with derived gameId, obs off; then BC-policy
  throughput measure with the real server) — the AnvilLobbyPlayer reuse
  finding makes this mostly harness work.
- Residual divergence chase (4 repro seeds, task list) — upstream-PR filler,
  not blocking.
- Upstream PR #1 assembly: re-author the static fixes + id preservation on a
  clean upstream branch once the 500-game numbers are in (evidence package:
  ADR-0002 + before/after rates + repro seeds).

## Overnight results (came back same evening — runs finished in ~90 min)

- **500-game baseline** (`run-20260710/`): **86.6% clean / 13.4% divergence /
  0 static mismatch / 0 crashes** vs 07-03's 38.2/49.8/12. Copy cost median
  7ms p99 16ms. 35/67 residual divergers still reach the identical outcome.
  The residual class = the deterministic this-turn copy gaps (67 fresh repro
  seeds in the results).
- **200-game twin certification** (`run-20260710-twin/`): **198/200
  twin-clean (99%)**. Among the 33 mainline-divergent games, 31 twin pairs
  agreed with each other — forks are internally deterministic; residual
  mainline divergence is a copy-gap artifact, not nondeterminism.
- **The 2 twin failures reproduce exactly across JVM invocations** (seeds
  20270184, 20270279; chain_divergence@13/@18 both times). Not wall-clock.
  Suspect: identity-hash iteration order (F1/F2 allocate at different JVM
  lifetimes; reproducible because the allocation sequence replays
  identically). Bounded 1% tail — affects exact replay of a specific
  rollout, not label validity (labels average over rollouts). Chase the
  iteration site later; these 2 seeds are the repro.
- D1 fidelity picture after one evening: statics dead, divergence 50%→13.4%
  (root cause fixed), twin determinism 99%. Rollout-contract remainder is
  harness work (fork→completion under bridge seats, derived gameId, obs off)
  + throughput measure.

## Session 3 continued (late evening): rollout-gate machinery + the twin-instability amplifier

- **forkcheck `-bridge` mode landed** (fork `dc5b46e06e`): seats are
  PlayerControllerAnvil answered by LocalRandomBridge; forked games inherit
  Anvil controllers (clonePlayer reuses AnvilLobbyPlayer — verified in play).
  The "completion under the bridge" contract leg now runs end-to-end.
- **Twin+bridge = a stress amplifier for the 1% replay-instability tail.**
  Random-index answers convert decision-path iteration-order wobble directly
  into divergence: 7/10 twin-divergent (vs 2/200 heuristic), three-way
  deterministic (main/F1/F2 each play a different game), late onset (10-70
  turns post-fork), 10/10 exact repro across JVM runs. Same root as the
  heuristic tail (hash-ordered collections consulted in decision paths),
  ~40x exposure (3x longer random-legal games x weirder states). Does NOT
  bias rollout value labels (legal playouts, averaged over K); bounds
  bit-exact replay of individual rollouts at ~1% at realistic lengths. Fix =
  ordered collections in decision paths — Forge-wide, upstream-conversation
  material (task list).
- **gRPC/model leg blocked on a real design point, deferred with eyes open:**
  the model server featurizes from Obs wire records, and Obs is a static
  single-game writer — isLogging(fork)=false means forks produce no
  observations (good: no store pollution; bad: model policy can't drive a
  fork yet). The rollout runner needs fork-side obs re-binding (wire records
  on, store off, derived gameId for the server's history ring). Next session's
  chunk, with the throughput measure behind it.
- **D4 pricing preview from tonight's numbers:** copy 7ms (negligible);
  rollout ~ remaining-game playtime ~ half a game. Naive K=8 x 100K positions
  ~ 400K game-equivalents ~ weeks — the plan-doc risk is real; the D4 pilot's
  levers (fork-during-generation, smaller K, fewer/better positions,
  short-horizon rollouts) will decide.

## D1 scorecard after day one

Statics 12%→0 (60/60 repro seeds + 500-game run). Divergence 50%→13.4%
(root cause: id renumbering, fixed; residual = this-turn copy gaps, 67 repro
seeds). Twin determinism 99% (heuristic lengths), instability class isolated
+ amplifier instrument built. Bridge-completion leg certified with
LocalRandomBridge. Remaining for D1 close: fork-side obs re-binding +
model-policy rollout smoke + throughput measure; upstream PR #1 assembly.

# Session 4 (night): D1 rollout contract CERTIFIED under the BC policy — fork-side obs re-binding, 100% twin determinism, rollout throughput priced

## Done

- **Fork-side Obs re-binding (fork `3aa1995658`):** Obs record-building state
  moved into per-Game wire sessions; the store frame stays a single static
  binding, so the corpus writer is untouched. New `startWireGame`/`endWireGame`
  give forked games observations for the bridge with the store off; the fork's
  history ring deep-copies the parent's at the fork point (host ids valid since
  id preservation). `AnvilBridge.gameStart(id, seed, header)` overload lets
  fork drivers announce derived game ids and re-announce the mainline on
  resume (the server holds one header per stream). Store path verified
  **byte-identical** on same-seed 5-game obs runs (diffs confined to the
  documented payManaCost wobble + end-record ms; same-jar run-to-run wobble
  was larger than the before/after delta). twin+bridge regression: 6/10
  twin-divergent, 0 statics — matches the amplifier baseline.
- **forkcheck `-grpc host:port`:** seats on a real GrpcBridge, tags from the
  server hello, wire sessions for mainline (`fc<seed>`) and forks
  (`.f1`/`.f2`); `forkPlayMs`/`chainPlayMs` recorded per game. The full
  rollout-contract gate now runs end-to-end against the model server.
- **Rollout smoke (d7-ep3 δ=0, 10 games): 10/10 clean** — mid-game forks
  complete under the BC policy with trajectories IDENTICAL to their
  mainlines. 0 statics, 0 transport failures, 0 model errors, 0 fallbacks.
- **Twin certification (40 games): chain_clean 40/40 — rollout determinism
  100% under the model** (vs 99% heuristic, 30% under the random-index
  amplifier). Main-vs-fork 39/40; the 1 divergence is the residual
  copy-gap signature exactly (seed 20260955, fork@8, diverges@24, twins
  agree with each other, same winner). Results in
  `data/forkcheck/run-20260710-grpc/`.
- **D4 pricing numbers:** copy 7ms median; rollout (fork -> completion under
  the model) **median 4.4s / mean 6.3s / p90 12.9s** at realistic game
  lengths (median 29 turns, fork turns 3-12); 40 games (mainline + 2
  rollouts each) in 818s single worker. Server: 59 rps batch-1,
  75.7K requests, pass:cast = 63.5K:11.9K. Naive K=8: ~50s/position/worker
  -> ~70 positions/h/worker; a w=16 fleet is GPU-bound at batch-1, so
  server micro-batching is the first D4 lever, then the plan-doc levers
  (smaller K, short-horizon, fork-during-generation).

## Broke / surprised me

- **The BC policy is a fidelity amplifier in the GOOD direction.** Greedy
  decode + realistic lines means the model neither consults the this-turn
  fields the copier drops (13.4% heuristic divergence -> 2.5% here) nor
  touches the hash-order decision paths at random-policy rates (1% twin
  tail -> 0/40). The rollout labeler runs under exactly this policy, so the
  contract is cleanest precisely where it matters.
- zsh eats bare `===` as a glob qualifier (`(eval):1: == not found`) — quote
  separators in inline scripts.
- Python stdout is block-buffered when redirected: the server's ready line
  sat invisible in the buffer while the process was already serving. Probe
  the port, don't wait for the print.

## Next session picks up

- **Upstream PR #1 assembly** (the last D1 done-when): re-author the two
  static-fix classes + id preservation on a clean upstream branch;
  evidence package = ADR-0002 rates -> run-20260710 baselines + repro seeds
  (60 statics, 67 divergence, 20260955 from tonight); forkcheck as the
  differential test.
- D1 close-out ADR once PR #1 is out (fork contract: statics 0, divergence
  13.4% heuristic / 2.5% BC, twin 99% heuristic / 100% BC, throughput table).
- Then D2 (SA-level schema + BC retrain) per the plan doc.
- Housekeeping: model server on :50051 stopped (stats in run meta);
  obs-regress scratch runs are disposable; run-20260710-grpc/ is the D1
  certification record.

# Session 5 (late night): upstream PR #1 SUBMITTED (Card-Forge/forge#11203), Anvil public, D1 CLOSED ([ADR-0011](../decisions/ADR-0011-d1-closeout.md))

## Done

- **Upstream drift review (57 commits, pin → `1eec01434e`) before assembly.**
  Headlines recorded in the worklist drift-watch section: **the #11161 race
  fix is merged upstream** (our designated rebase anchor now concretely
  exists — the worklist had already caught this on 07-06, ahead of me);
  **`thisTurnCast` changed type Card→SpellAbility** (`2fa0705c78`) — any
  residual-divergence copier fix must be authored against the new
  representation (no Anvil code reads the changed API, checked); upstream
  seeded the unseeded `drawStartingHand` shuffle we still carry latently
  (doesn't fire in our configs per determinism measurements); five
  AI-behavior commits = teacher-policy drift at rebase (fresh arms baseline
  required, as the boundary rule already demands). Only one upstream commit
  touched our fix files; **cherry-pick onto upstream master: conflict-free**.
- **PR #1 assembled and submitted:
  [Card-Forge/forge#11203](https://github.com/Card-Forge/forge/pull/11203).**
  Branch `gamecopier-fidelity-fixes` @ `2437820aee` (worktree `../forge-pr1`,
  stays until the PR resolves), upstream-facing commit message, PR body =
  three bugs + harness methodology + before/after table + AI-assistance
  disclosure. Three synthetic TestNG regression tests in
  `GameSimulationTest` (face-down exile, effect-card wiring via
  keyword/blessing/monarch observable behaviors, id preservation with burned
  ids); **negative check run by reverting the three source files: 3/3 fail
  pre-fix**; full forge-gui-desktop suite green (285 tests).
- **Forge's CONTRIBUTING.md has an explicit AI-agents policy** (identify via
  co-author trailer or body note — we do both) and warns against gratuitous
  agent-added tests (ours are differential regressions, validated failing,
  which the body says explicitly). **157 AI-co-authored commits merged
  upstream in 2026** — Claude-co-authored PRs are routine there, including
  the #11161 fix itself.
- **Anvil repo made PUBLIC** (backs the PR's background claim; fork was
  already public). Pre-publication pass: secrets/history/large-blob scan
  clean (`data/` was never in git); **LICENSE added** (GPL-3.0, matching
  Forge and our own pyproject claim); README status refreshed off "Pre-M0"
  (nine months stale) to the M0/M1 numbers + M2 arc.
- **Monitoring recorded where it will be seen:** worklist top section ("check
  at start of each session until resolved" — user confirms session cadence
  makes manual checks sufficient; no scheduled poller), CLAUDE.md Status,
  project map. Stale-bot pressure noted: ~35 days idle closes it.
- **[ADR-0011](../decisions/ADR-0011-d1-closeout.md): D1 CLOSED.** Contract
  table (statics 0, divergence 13.4%/2.5%, twin 99%/100%, rollout 4.4s
  median), workaround retirement (fork-at-start, digest tripwire),
  D4 lever order, residual classes as upstream-conversation material.

## Broke / surprised me

- **My first id-preservation test passed against the UNFIXED copier** — in a
  tiny synthetic game, creation order coincides with zone-traversal order, so
  renumbering reproduces the same ids by accident. Fixed by burning ids
  between adds (as real games do). Lesson restated: a differential test isn't
  one until you've watched it fail on the broken side.
- **The unfixed copier NPEs outright with a monarch/blessing card in play**
  (mid-copy duplicate has no original in remembered-object fixup) — bug 2 was
  a latent crash, not just corruption. Found only because the negative check
  ran; went straight into the commit message and PR body as ammunition.
- `-P windows-linux` doesn't exist for module-scoped builds (it's a
  forge-installer profile) — plain `mvn -pl forge-gui-desktop -am test` is
  the right invocation; also surefire 3.x wants
  `-Dsurefire.failIfNoSpecifiedTests=false`, not `-DfailIfNoTests`.

## Next session picks up

- **Check [#11203](https://github.com/Card-Forge/forge/pull/11203) for
  maintainer feedback** (standing worklist entry, start of every session).
- **D2: SA-level action schema + BC retrain** per the plan doc (pointer
  scores candidate rows, order rung deleted, string-join labels ~69% +
  exact `oi`, nested-window loader fix rides, 3-epoch recipe, fresh δ table).
- Housekeeping: `../forge-pr1` worktree persists deliberately;
  `data/upstream-pr1-body.md` is the submitted-body record; scratch build
  logs are disposable.
