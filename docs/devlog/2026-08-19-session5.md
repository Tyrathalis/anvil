# 2026-08-19 — session 5 (M9 D3 rung 2: the payflag census + the §8 read — truncation gate FIRED)

## Done

- **The pre-training census read is DONE
  (`data/census/run-20260819-payflag`, 74M — M9-close re-price):**
  500 games / 100 pairs / 4 nice-19 lanes, branch jar `3e3dfbd6…`
  (fork `m9-payment-surface` @ `eb774b5d03`), `-paytelemetry`, all
  lanes exit 0. Read via `scripts/payment_read.py`:
  - **scoped windows 29.9/g** (the 61/g budget line was conservative —
    zero-mana windows never enumerate);
  - **consequential 0.6943 of scoped = 20.79/g** — bridge-tax budget
    comfortable (~+2% round-trips on the ~1,000/game baseline);
  - **forced windows 32** (0.21% of scoped) — but see the confound;
  - **TRUNCATION 0.3911 of consequential vs the pinned 0.05 gate —
    FIRED**, exactly what the gate existed for. Monotone in turn
    (0.000 at t<5 → 0.591 at t25+); truncated windows average 11.5
    atoms vs 6.0 — wide-board combinatorics under the exact-multiset
    class key. Histogram bimodal: 4,325 windows @ 1 class, 4,233
    pinned at the 8-cap, 284 @ 0.
- **Enumerator bug found by the crash guards, fixed
  (`f98a555a95`):** `ConcurrentModificationException` ×144 — DFS
  option (a) iterated `st.floats` for-each while recursion pushes/pops
  it. Index-based scan; suite 13/13. The guards did their job — zero
  games killed.
- **The zero-class/forced forensics converge on ONE family:
  cost-modified casts** (delve: Tasigur/Murktide/Hogaak; Emry
  affinity-reduction + graveyard casts; Leyline Binding). Confirmed in
  source: the controller's `toPay` is the RAW mana part
  (`CostPartMana.getManaCostFor`); `CostAdjustment.adjust` applies
  inside `payComputerCosts` — so cousin-family windows reach
  enumeration in forms v0 can't reconstruct → 0 classes (auto handles
  them, v1 scope working as designed) — **but the `forced` marker is
  polluted on those spells** (canPayManaCost on the raw cost says
  unpayable while auto pays the adjusted one).
- Launch lesson re-confirmed: driver lanes inherit the tool shell —
  no `DISPLAY`/`XAUTHORITY` = instant exit-1 with empty logs;
  relaunch with env exported.

## Broke / surprised me

- The truncation gate fired at 8× its threshold on the first smoke
  and held at 500 games — the exact-multiset class key is too fine
  precisely where boards are wide.
- Delve/affinity showed up in telemetry within 20 games of the flag
  existing — the cousins the scope pins deferred are not hypothetical
  traffic.

## Next session picks up

- **The chartered pre-D4 revisit session (one session, three coupled
  pins — all touch the §4 flag / §2 signature):**
  1. **K_MAX/truncation design** — coarser class signature on wide
     boards vs diversity-pruned truncation vs higher K + hierarchical
     head; the by-turn curve + atoms split are the evidence.
  2. **Cost-modified-window scoping** — likely: detect
     adjusted-vs-raw cost divergence → out-of-scope v1 (auto), a
     `costmod` telemetry kv; makes the cousins deferral explicit at
     the flag rather than accidental at the 0-class fallthrough.
  3. **Forced-marker cleanup** — `forced` only meaningful on
     unmodified costs; it is D5's cleanest collapse channel and must
     not be polluted.
  Then re-run the payflag census (CME fix + revisit pins) for the
  final pre-D4 baseline read.
- **Rung 3: payment drill evalset + payment sub-head design** after
  the revisit pins land.
