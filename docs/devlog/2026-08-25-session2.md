# 2026-08-25 — session 2: the M9 closeout

## Done

- **M9 CLOSED ([ADR-0077](../decisions/ADR-0077-m9-closeout.md)), user
  decision on the done-when-5 fork: close-and-carry, not v2-within-M9.**
  The three-part case the user endorsed: (1) v2's coherent experiment is
  the unified competency — running it plan-only inside M9 rebuilds the
  split-across-surfaces defect the unification framing names; (2) a
  proper v2-within-M9 requires a planning ceiling measurement first (the
  ADR-0073 rule), making it a second milestone wearing M9's number;
  (3) the one measured path clearing the gate floor with margin
  (+2.96pp/game) is the ADR-0075 supervised-conditional competency, an
  M10 build. Done-when 5 resolved as SUPERSEDED with recorded reason;
  done-when 6 resolved by the ADR itself. No promotion; ckpt of record
  stays `d6-run11/iter-019` at 0.5279 ± 0.0110.
- **Payment-completion queue routed by name (no-silent-loss):**
  cousins (2) + costmod per-spell (4) + pool-tie residual (5) SCHEDULED
  into M10's actuation build; resolution-effect payments (3)
  RE-DEFERRED with reason (own probe-then-build genre; flagged
  not-re-deferrable-again without a measured argument); pin-12 forced
  re-mine RETIRED with reason (no consumer — ADR-0031 regenerates from
  the current era anyway). Evalset repair rides with M10 label work.
- **M10 opened at SCOPING:
  [m10-plan.md](../design/m10-plan.md) skeleton drafted** — charter
  (unified resource-scheduling competency: v2 plan target + re-advertised
  actuation + ADR-0075 conditional labels), named alternative (§3b
  stops), design-round obligations (planning ceiling, v2 target probe
  INSIDE the design round, kill signal, evalset repair), open forks,
  drafted done-whens. Deliberately did NOT run the v2 offline probe now:
  cheap, but it would pin a target shape before the unified design round
  can co-design it with the actuation surface.
- **The veto-trajectory figure ADR-0076 owed** landed as the
  consolidated four-run table in ADR-0077 (run17/18/19/20 — same
  equilibrium, three channels, conditioning fastest); run20 iteration
  stores thereby prune-eligible.
- Docs pass: CLAUDE.md M9 bullet archived verbatim
  (status-archive.md) + compact summary + M10 SCOPING bullet;
  m9-plan done-when 5/6 annotated; project-map.html swept (pills, Now
  card → M10 scoping, M9 details closed w/ ladder+rules bullets, new
  M10 details block, run20 ledger row, ADR-0077 row, watch-list §3b
  line, footer stamp).

## Broke / surprised me

- Nothing broke — a pure adjudication-and-docs session. The one
  judgment call worth recording: the "probe v2 now since it's cheap"
  temptation was explicitly declined (reason above); if the M10 design
  round later wants exactly the v1-style probe anyway, the machinery
  (ADR-0074 pattern + plan_probe.py) is unchanged and ready.

## Next session picks up

- **Stale-data kill list execution** (presented this session, pending
  user sign-off): run18/19/20 eval stores in `data/runs` (~4.3G),
  their `data/trajectories` stores, training dirs trimmed to the
  run16 shape (~9G → ~50M each, keeping analysis/monitor/config),
  m9control drillmix scratch (~100M), optional paygoals census.jsonl
  trim. Keeps: d6-plan-init, plan-probe-r1, payment-evalset-v1,
  veto-knowability-*, m9-rebaselinearm, ratesweep + ceiling census.
- **The M10 design round** (its own session, user-adjudicated forks):
  charter ranking vs §3b stops → planning ceiling measurement design →
  v2 target probe pins → actuation advertisement shape → competency
  read instrument + certification horizon → supervised-conditional
  wiring. m10-plan.md carries the fork list.
- Watch item unchanged: Chronicle D5 dogfood clock running; the
  Android res-delta staleness question.
