# 2026-07-23 — run-7b closes D2: the critic lever lands the first outside-noise supersession

Continuation of the 07-21→23 arc (see 2026-07-21-session2.md for the τ
falsification, §6f build, and run-7's halt). Today: run-7b finished, the
closing read resolved done-when #2, and the upstream card-addition story
got its design sketch during the wait.

## run-7b: 20/20, zero guard halts, zero tripwires ([ADR-0023](../decisions/ADR-0023-d2-closeout.md))

- The lr 1e-5 diagnosis held: KL 0.002–0.010/iter all run (run-7 at 2e-5:
  0.014–0.059), veto equilibrium 0.08–0.13 with gentle oscillation where
  run-7 diverged, casts/game 36–39, entropy 0.143→0.130, critic calibrated
  within ~2pp of realized nearly every iteration, v0_masked never diverged.
- Arms: 0.4950 (i4; real dip, −4.50 ± 2.14 paired vs init — a conservative
  consolidation phase, veto/casts bottomed together, then recovered) →
  0.5525 (i9; +5.75 ± 1.80 over the dip) → 0.5625 (i14, high-water) →
  0.5475 (i19; paired tie with i14, −1.50 ± 2.00). Checkpoint of interest
  = iter-014 (best-by-arms, run-5 precedent).
- En route ops: the desktop suspended overnight BETWEEN runs (07-22, idle
  gap after run-7's halt — no run ever spanned a suspend; my initial
  suspend-mid-run reading was wrong and is corrected in-conversation);
  sleep-inhibitor-during-runs convention adopted (`systemd-inhibit` holder
  keyed to the driver's lifetime); timing-anchors memory note saved
  (artifact timestamps, never assumed reply latency).

## The closing read (record `data/runs/run7b-best-arms-report.json`)

- 2,000/2,000 decisive, 0 crashes; first-attempt veto 0.0899 (bar 0.1358).
- **Raw 0.5545 ± 0.0111 / corrected 0.5530 ± 0.0109.**
- **Paired vs run-3 iter-012: +3.26pp ± 1.16 (t=2.80, 302/237) —
  done-when #2 SATISFIED; first outside-noise beat of a ckpt of record.**
- Paired vs its init (run-6 iter-19): +2.20pp ± 1.01 (t=2.17).
- **RL checkpoint of record → `d6-run7b/iter-014/train/last.pt`.**

## The D2 arc in one paragraph (for the wrap)

The ceiling datum said the old recipe couldn't pass iter-012. §6d bought
stability without gains; the τ curve falsified temperature and reframed
the tax as a CREDIT defect; §6f put the sharp critic in the loop; run-7
proved the signal was now strong enough to be dangerous (lr is
per-signal-regime — ADR-0017 extended); run-7b at half the step size
converted the same signal into +3.3pp outside noise. Three levers, two
negative results, one supersession — every step measured on paired seeds.

## Upstream-track design (while runs baked)

- **[card-addition-pipeline-sketch.md](../design/card-addition-pipeline-sketch.md)**:
  tier 0 add-card (CPU, minutes; quantized/hosted encoder) / tier 1
  validate (CPU games + standing checks → PR telemetry) / tier 2
  per-RELEASE batch retrain (consumer GPU, hours; never per-author).
  Cadence argument (user): Forge adds cards continuously (teasers, weekly
  customs) vs batched retraining ⇒ the confidence gate is REQUIRED, and
  the M0 fallback path already IS the gate plumbing. Two-sided gate
  (user): RemAIDeck/blind-spot cards route TO the model (bar = beats
  never-casting); Grindstone drill seeding = the same work upstream-first.
  Prerequisite pinned: ID dropout (text-channel atrophy is the cold-start
  blocker). Mobile port discussion recorded there too: featurizer port +
  parity harness = the real work, int8 = free win, arms = the acceptance
  gate for any precision change.

## Next session picks up

1. **Lever sequencing decision (user, fresh context)**: scale at the now-
   stable critic-loop operating point (more iterations / bigger batches at
   lr 1e-5) vs Grindstone drill seeding (blind spots + early-doom
   analysis) vs D4 rebase timing (#11161 dataset boundary).
2. **Early-doom analysis** (queued, cheap): full-vis critic over existing
   2,000-game stores → luck-locked loss fraction → ceiling estimate +
   curation signal.
3. QoL riders: driver-side guard-halt push hook; driver-owned sleep
   inhibitor.
4. Upstream watches: #11285 review, #11260, khaliostr SVar repro.
