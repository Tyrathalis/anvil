# 2026-09-16 (session 2) — the upstream survey before the rebase; community watch

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **State review** (the user's ask): Build 3 closed (ADR-0108), the completeness audit accepted
  (ADR-0109), fork pin `5e333e96930`, box idle, tree clean at `d4793d9`. The user's question —
  push the targets surface and the throughput work ahead of the rebase? — answered: **the rebase
  first**; the targets surface lands on the census controller, the layer the rebase conflicts on
  most, and its label pool + three-arm read double as the new jar's first real runs; **the
  allocation head's first fit starts in parallel** (Python over ≈ 10⁵ existing search rows; the
  labels are policy-conditional and refit per cycle anyway); the void skip rides the rebase as
  pinned. The user agreed, then asked for the upstream survey first.
- **The upstream survey** (`23c3d2a85d` → upstream tip `97535e047f`, 09-16 10:36):
  - **Scale:** 283 commits (223 non-merge), 820 files; ≈ 45 engine / AI commits, the rest
    Adventure, localization, editions, 171 new card scripts. **The controller API is unchanged**
    — the `PlayerController` + `PlayerControllerAi` diff is one storage-land hunk.
  - **Overlap:** 13 files. Two real conflicts: `ComputerUtilMana` (our mana-source memo, 56 lines,
    vs the AiCardMemory refactor #11667, 138 lines) and `AiBlockController` (talor's cache, 195
    lines, vs #11790, 50 lines). `forge-gui-desktop/pom.xml`: powermock removed (#11830) vs our
    protobuf plugin block — trivial. `Game`, `Player`, `GameCopier`, `AiController`,
    `ComputerUtil`, `CostAdjustment`: small disjoint hunks.
  - **#11916 and #11925 are merged (09-14, 09-16) and are khaliostr's (Manabrew), not ours** — we
    had queued #11916 for the rebase as a pending upstream PR; nothing to carry, no fork copy.
  - **Four changes that touch our decisions:**
    1. **#11667 (AiCardMemory refactor) reopens the probe leak.** Memory is keyed by a typed
       `MemoryType`; a new set `MemorySetMana.UNPAID_COSTS` is written on the test-mode payment
       path (`payManaCost(test=true)`, `ComputerUtilMana:806`) — what `autoPayable` runs inside
       `quietProbe` — and `ChangeZoneAi:563` reads it. `PROBE_SETS` names six card sets → after
       the rebase the bridged path leaks again (the ≈ 2.7pp class). Fix: snapshot every entry of
       the memory map (the refactor makes it natural); a unit test; the divergence read on the
       new jar (the forkcheck cannot see bridged-path changes).
    2. **#11780 (skip GUI updates when simulating) is the views lever we dropped 09-14.**
       `Game.setNoGUIUser()` → `DummyCardView` (the only stub: `updateAbilityText`);
       `GameCopier` sets it on every copy, so search copies get it free. Action: a flag on
       `AnvilRun` / `CensusRun` / `ForkFidelityCheck`, a flag-on vs flag-off forkcheck on the new
       jar, a one-worker re-profile.
    3. **#11778 = CR 605.1a**: an activated ability whose cost or effect moves a card to or from
       a library is no longer a mana ability (uses the stack, becomes a priority option). A format
       change under the invariant; pool impact = **Chromatic Sphere only** (scan of all 1,701
       pool scripts). One line in the boundary ADR.
    4. **#11861 rewrote the AI timeout machinery** (`AI_CAN_USE_TIMEOUT` / `canUseTimeout` gone,
       interrupt checks, join 2,000 ms). Our bridged priority path overrides above `AiController`;
       the heuristic seats move. Check the search clock / worker deadline against the new join.
  - **What moves the reference:** heuristic changes in blocking (#11790), pump timing (#11739,
    #11742), any-color mana (#11809), storage lands (#11672), reveal effects (#11712), combat
    prediction (#11920, #11925). **8 of 1,701 pool scripts changed**: Hogaak (the no-mana
    restriction moved to a static), Emrakul the World Anew (protection scope widened), Atraxa,
    Endurance, Sylvan Library, Dance of the Dead, Exhume, Orochi Hatchery (flag / AI-hint
    cleanups). → the ability-key re-dump + diff on the new jar (those cards' `ak` shift); **not
    verified: what a served option whose key is outside the table does** — check at the smoke.
    Three LKI NPE fixes (#11917, #11683, #11821) may retire part of the copy-crash class — read it
    off the new baseline. Lazy card loading (#11763) is off by default (pref) — an opt-in startup
    timing later. #11830 un-skips silently skipped tests — fork test classes may newly run.
  - **Recommendation (open):** pin the tip `97535e047f`; land it as a **merge** with a pre-merge
    tag — ADR-0025's D4 bump was a true rebase (history rewritten), the 08-22 bump at
    `23c3d2a85d` a merge; since then twelve fork tip hashes are cited by run headers, ADRs and
    forkcheck records, and a rebase rewrites all of them. Sizing: ≈ a day of archaeology + a box
    day (the baseline forkcheck, the heuristic + `iter-019` reference reads); the probe extension
    and the views flag share the jar and the forkcheck.
- **Community watch** (the user pasted two threads): the survey doc's 09-16 follow-up
  ([discord-ai-plotting-survey.md](../design/discord-ai-plotting-survey.md)); the §1 talor and
  manabrew rows corrected; **talor's block-legality cache (Tyrathalis/forge#1) queued on the
  upstream worklist as the first post-launch patch**, khaliostr's evaluation budgets as a
  determinism watch item (our position: count-based, flag-gated); Shedletsky's runner fleet
  (≈ 1,000 games/min, 4 machines, 92 workers, heuristic-only) noted; the plan record entry.

- **The merge landed** (the user: a merge is fine, the pin at the tip, the archaeology resized).
  Tag `pre-merge-20260916`; worktree `../forge-merge` on `merge-upstream-20260916`; two conflicts
  resolved as ADR-0110 records, one rename fixed; the three riders + `AiMemorySnapshotTest`;
  the fork's seven test classes 69 + 2 pass under an isolated `user.home`; the jar snapshotted
  to `data/runs/merge-boundary/forge-merge-1db054ade4.jar`; `run_forkcheck.sh` gained
  `JVM_ARGS`; the boundary queue launched 09:40 (`merge-boundary`: baseline → nogui proof).
  [ADR-0110](../decisions/ADR-0110-m12-upstream-merge-20260916.md), the standing rule, the
  plan's Build order 4 + record, the map row, CLAUDE.md Now + state of record.

## Broke / surprised me

- Nothing ran. Three surprises in the reading: the probe fix's snapshot list is silently
  incomplete on the new jar (a typed memory set the refactor added); upstream shipped the views
  lever we had judged rebase-fragile; the controller API did not move at all in 283 commits.

## Next session picks up

- The boundary queue's verdicts (`data/runs/merge-boundary/queue.log`): the baseline's drift vs
  08-21 (for the record) and the nogui proof (identical → the default flips). Then: master
  fast-forwards to `1db054ade4`, the fork pin + `compare.py`'s default baseline move, the reads on
  the merged jar (heuristic ref + `iter-019` at 2,000 games, the three-cell g/h read, the probe's
  divergence read), the ability-key re-dump + diff (+ the OOV-key serve path check), Build 4 with
  the targets surface first. The allocation head's first fit in Python alongside.
