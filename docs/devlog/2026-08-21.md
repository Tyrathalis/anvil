# 2026-08-21 — certify2 read + the color_hold top-up (certify3)

## Done

- **certify2 COMPLETE (600 jobs, ~19k rows overnight, zero lane
  failures): certified 26/600, auto-correct 113, salvage gate 0.0022
  [ok].** Merged with batch 1: **38 positive drills (bp 13 / ch 8 /
  wc 17) + 142 auto-correct**; positive margins 2.0–24.2, p50 5.25.
- **Yield DECAYED with pool depth** — bp 4.2% (proj 7.5%), ch 2.5%
  (proj 5%), wc 8.3% (proj 17.5%): the top-40 slices yielded ~2× the
  next-600. The batch-1 "rank non-predictive" read was small-N; rank
  is weakly predictive at scale. Consequence absorbed by the pinned
  yield-driven composition — no redesign.
- **New salvage family recorded (non-blocking): Arena of Glory** — all
  32 salvaged rows are `costs:` failures on that one card (4 jobs,
  none certified; gate passed at 0.0022). Known enumerator edge for
  the next payment-family touch; task chip filed.
- **Floor check (pinned ~10/shape): bp 13 ✓ wc 17 ✓ color_hold 8 ✗**
  → the pre-registered top-up taken: **certify3 LAUNCHED** — 240
  color_hold-only jobs, next slice (720 prior windows excluded,
  overlap verified 0), expected +5–6 ch drills at depth-decayed
  yield, ~2.5h. Full coverage: setsid wrapper (pid 3957950) → lanes →
  reader (`certified3-`/`autocorrect3-drills.jsonl`) →
  `certify3.DONE` → self-unregister; watchd (stall 30m); DONE-keyed
  waiter. Lanes verified writing 25s post-launch.
- Waiter hardening en route: the certify2 waiter false-alarmed on a
  lane that FINISHED early (out file quiet ≠ dead); stall check now
  requires the lane log to not end in `certify done`.

## Broke / surprised me

- Row estimate 14,680 was arm-0-blind (actual ~19k) — progress ETAs
  ran optimistic. Estimate = (arms+1)×k next time.

## Next session picks up

1. **Read certify3** → merge batches 1+2+3 into the evalset of record
   (positive drills + a capped auto-correct subset as the separate
   metric — cap size is a session decision, 142 available).
2. Hand-built phyrexian drills (10–15, incl. the pool-tie board) +
   hand-built forced/Signet boards (ADR-0065, ddmin rule).
3. D4 accuracy scorer over certified+autocorrect (per-kind).
4. D4 gate session (gates PIN-AT-DESIGN).
