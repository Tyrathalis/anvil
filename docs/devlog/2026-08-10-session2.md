# 2026-08-10 session 2 (census runaway cap; forced-branch harness BUILT + smoked)

## Done

- **Census runaway guard landed (fork `f9ec33ca8a`):** root cause of the
  6/6 stale-data-pass census trims diagnosed from the surviving
  `out.log` of run13-i006 inv-0015 — game 3115 wedged in a decision loop
  to the 360s hard cap, hit Obs's 1 GiB `RAW_CAP` (guard existed there
  since the 571 MB incident) while census had NO cap. Fix mirrors the
  Obs pattern: per-game byte cap (`anvil.census.rawcap`, default 16 MiB
  ≈ 1000× a healthy game's ~15 KB), one loud `census_cap` marker row,
  `cap:true` on the end record, reset per game. Smoke at rawcap=8192:
  fires every game, bounded, resumes; default run zero firings and
  identical winners/turns (census is off the game path). Readers
  (`selfplay.py`, `d8_report.py`) key on `m`/`by` — marker ignored.
- **Forced-branch design PINNED (user, this session) + recorded in
  [m7-plan.md](../design/m7-plan.md) D2:** act = on-policy constrained
  pick (pass masked; source-action replay rejected as era-fragile),
  hold = one forced pass then free, **no forced-branch obs in v1**
  (labels-only — the branch label IS the action, Δwr joins the mainline
  `Obs.mark` window), pairing = shared rollSeed per (fp, r) with
  crashed/skipped pairs dropped whole, loud feasibility guards
  (`branch_skip`, bridge-seat-only), one row per fork point both
  branches, sizing read at K ∈ {4, 8, 16}.
- **Forced-action harness BUILT end to end (fork `694e9c93f9` + Anvil
  this commit):**
  - Protocol: `forbid_decline` on DecisionRequest (game-agnostic "null
    answer disallowed"); both proto copies synced, Python stubs
    regenerated (grpc stub import hand-fix reapplied).
  - Server: pass-logit mask (−1e9 rides the existing per-request
    `pass_delta` plumbing, kills pass under argmax AND Gumbel);
    `--fork-instrument` mode = sampled serving for wire-only fork
    sessions (g=−1) with **no mu records** — the standing forkobs guard
    stays armed otherwise.
  - Controller: one-shot forced directive keyed on Game identity
    (WeakHashMap — Obs stale-thread pattern); ACT re-asks on veto with
    pass still masked regardless of global `-reask`, exhaustion = SKIP
    (never a silent pass); HOLD passes without asking; `ForcedResult`
    enum reports CAST/HELD/SKIP_* back to the runner.
  - AnvilRun: `-forcebranch` (requires -drillfile + -rollout; excludes
    -forkobs by design), 2 branches × K per fork point, branch pairs
    share rollSeed (identical determinization + downstream RNG +
    announced noise seed), wire sessions announce the COMPLETION seed,
    labels row carries both branches + full skip/crash accounting.
  - `scripts/forced_branch_read.py`: coverage, per-point paired Δwr,
    empirical paired SE vs the independent binomial floor, ADR-0051's
    var_signal decomposition, direction read.
- **Smoke green (3 games, K=2, iter-019 sampled instrument serving):**
  forced points pairs 2/2, zero skips; hold branches consumed (4
  `by:"forced"` census rows = 2 fp × K); seat guard fired loudly on
  opponent-active fork turns (`seat_skip` rows); labels valid JSON
  through the read script; mu strictly mainline (g ∈ {0,1,2}) — pin 3
  honored. One bug found in review-by-running: `jstr` doesn't quote;
  fixed both call sites, re-smoked.
- Full pytest suite 139 green.

## Gotcha for the file

The smoke server launched without `PYTHONUNBUFFERED` looked hung for 15
minutes (2-line log, futex_wait) but was READY — stdio buffering ate the
`[server]` line. `selfplay._start_server` sets `PYTHONUNBUFFERED=1` and
waits on the PORT, not the log. Manual server launches should do the same.

## Next session picks up

1. **The K-sizing read (m7-plan pin 7, task open):** 20–30 REAL drilled
   fork points (regenerate curation from iter-019's losses per standing
   rule, or reuse cycle3 selection's drill manifest) at K ∈ {4, 8, 16},
   server in `--fork-instrument --sample`; `forced_branch_read.py`
   prices the K dial. Watch skip_act rate (pin 5: high = hold-only
   drilled states) and seat_skip (manifest turns must target model-seat
   windows).
2. C3 pre-work read (unchanged from session 1's list).
3. D3 stability pass rides with the first real forced-branch campaign
   (fork era boundary: forkcheck + re-baseline before D2's training run).
