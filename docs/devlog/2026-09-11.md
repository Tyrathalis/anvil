# 2026-09-11 — the h2 pool lands; the h2 head withheld at argmax; the bar arms

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## The pool (15:10) and the chain (→ 17:17)

The label run closed 15:10 after a five-worker tail (20 chunks of 50 on 16 workers; two chunks
re-issued after worker recycles; 21 of 1,000 games clipped at 3,600 s). Pool read 15:11: 22,722
usable groups (23.1 / game), positives at 0.03 2,598 (11.4%) — the calibration's 2× at rate 0.5.
Folds 15:12–16:06 (≈ 10 min each), the build 16:19, the smoke 16:20 (8/8, 143 bridged windows, 11
goals directed, 0 errors), the paired read 16:20–17:17 (16 workers, no search: ~30 min per arm).

## The read: on − off −1.36 ± 1.20 at argmax on 0.77 deviations / game → withheld

The head deviates 6.7× more than the eot-trained e4s head and reads the same sign. Tie deviation
doubled in the fit (0.045 → 0.097). The bar arms (0.2 / 0.5) launched 17:31 vs the same off
reference (`data/runs/build3-e4h2/bar_arms.sh`, watchd `build3-e4h-bararms`, notifies;
`read-bars.md` ≈ 18:45).

## Next session picks up

1. `data/runs/build3-e4h2/read-bars.md`: a bar arm > 0 → the serve rule is that bar (ADR-0105
   closing addendum; the served set gains the pay tag); every bar ≤ 0 → the bridged-seat read on
   the h2 judge (the on arm's seeds, `-search -searchpay 4 -searchpayleaf h2`, acting off, the e4h
   ckpt) attributes pick-by-pick — `pay_calibration.py` extended to join the mainline's
   `directed_ok` census rows to the sub rows by (seed, t, sa).
2. Merge `qol-device` once nothing imports main (the bar arms run final_read from main).
3. The throughput week: model-server scaling first, then the bench, the JFR profile, the tail
   chunks. Evening 5 after.

## 19:05 — the bar arms: every bar negative

argmax −1.36 / bar 0.2 −1.74 / bar 0.5 −0.87 (± 1.2) on 0.77 / 0.58 / 0.42 deviations per game. The
bar arms at 16 workers without the raised bridge deadline threw 15–16 poison crashes each (a load
artifact; the pairs exclude them). Next: the offline pick-vs-leaf read on the pool (the head's
deviations valued by the h2 leaf: pick − auto), then the head fix or the transfer read.

## 19:15 — the pick-vs-leaf read (offline, `pay_fit --eval`)

e4h in-sample +0.024 per deviation (positives +0.104 × 1,064, ties −0.023 × 1,804); e4s
out-of-sample −0.001 (158 × +0.083 vs 500 × −0.028); e3 −0.003. The picks are the leaf's on
positives; the tie deviations cancel them; the head cannot tell a positive window from a tie and
the softmax margin cannot either (the bar arms). Proposed: the positive-window gate.

## 19:40 — the deviation gate built; the gated chain launched

`pay_gate` + the gate BCE + the gate curve / `gate_pstar` + `--pay-gate` + the chain knobs (`98c4f8e`,
tests 44). Chain `build3-e4h2g` on the h2 pool (gate weight 1, pos-weight 8) → `m12-build3-e4hg` at
the pooled p* → smoke → the paired read `b3e4hg` (off / on at argmax behind the gate) ≈ 22:15.
