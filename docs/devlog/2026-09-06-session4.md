# 2026-09-06 (session 4) — M12 Build 0 opened: ADR-0102 pins, the boundary bundle in the fork, the smoke chain, the search directive scaffold

## Done

- **State review before Build 0** (the user asked for decisions first). Six things the plan
  assumed were different in the tree — recorded in
  [ADR-0102](../decisions/ADR-0102-m12-build0-pins.md): a payability filter already exists
  (`PAYCHECK`, off since M1 for late-pricing + cost reasons) and the apply-time veto uses the same
  Forge predicate; a 300 s wall-clock draw clock exists and the trainer already scores caps as 0;
  no Java leaf-value callback exists; the bridge hardcodes the format tag; the "106K drill-fork
  games" double-counts cl2 (real: 1,321 drill fork points + 806 harvest windows + 3,294 mint
  per-arm rows + ~6K cl2 leaves ≈ 10⁴); the game-time tail is wide boards as much as long games
  (slowest decile 31% of wall time, 1.75× per-turn cost).
- **Six pins, all accepted by the user:** the executor's own predicate as the mask filter
  (enumerator = rescue only if the smoke says so); the boundary = mask + caps + provenance header,
  the directive and enumerators after it as ADR-0025-exempt commits; deterministic window/turn
  caps at p99.5, repetition deferred on a tripline; provenance in the game header + manifest under
  sv=3; **fork J** — search copies determinized to the acting seat's information set (uniform now,
  belief-sampled next as the belief head's first consumer — the user asked whether a values-only
  channel closes the leak: no, the leak is what the value is conditioned on); the Build 1 label
  count corrected. Two standing rules.
- **The boundary bundle landed in the fork** (`5d5283eb233` on `master`, on top of the recording
  jar): `AnvilOptions.payable` = the legality subset of `canPayCost` (extra-mana taxes, ward mana
  post-targets, `canPayManaCost` + `canPayAdditionalCosts`; NONE of the judgment calls — the
  planeswalker-ultimate coin flip was an RNG draw on the scan path), `PAYCHECK` default ON, the
  realizer on the same predicate, `-Danvil.scan.payshadow=on` re-tests rejected options with the
  M9 enumerator (one `paymask` census row per window), mask-cache key + mana pool + life;
  `GameRules.anvilTurnCap/anvilWindowCap` counted in `PhaseHandler` (defaults 52 / 1,650 = the
  m9-rebaseline p99.5: priority windows per game p50 529 / p99 1,374 / p99.5 1,614 / max 18,086;
  turns p99.5 51), ending as a Draw with `Game.anvilCapReason`; **a latent bug found at the sanity
  run: a forced Draw marks every survivor won, so `isDraw()` was false and the 300 s draw clock
  had recorded a WIN with an arbitrary winner since M0 (~1 game per 2,000)** — the mainline now
  uses the unique-winner rule; `Obs.SCHEMA_VERSION` 3 with `pool` + `fork_commit` on the header
  and `cap` on the end record; `GrpcBridge` format tag from `-f`, fork commit populated.
- **Python side of the boundary** (`de3d23a`): `OBS_SCHEMA_VERSION` 3 with the reader taking the
  version per store manifest and `MultiStore` refusing to join across (obs_schema, pool_version);
  the harness passes `-pool`/`-forkcommit`; `scripts/obs_diff.py` (the 08-11 obs-diff gate as a
  standing instrument), `scripts/build0_smoke.sh`, `scripts/build0_smoke_read.py`.
- **The smoke chain launched 15:12** (`data/runs/build0-smoke`, watchd `build0-smoke`, stall
  75 m): A = 600 bridged games `iter-019` argmax vs heuristic, filter ON / cache OFF; B = same
  seeds, mask cache ON (obs-diff gate + games/hour delta); C = 200 games payshadow (the
  enumerator-rescue class); then the 500-game heuristic forkcheck vs the 08-21 seed set
  (`data/forkcheck/run-20260906-build0-boundary`) — the boundary proof, which also discharges the
  recording jar's owed proof by transitivity. Reference row (pre-boundary rebaseline arm s0):
  veto 0.1675 / first-veto 0.1217; first-veto reasons unpayable 2,993 · no_shape_fit 898 ·
  restrictions 194 · timing 31 · dangling_ref 30. **Early arm-A census (8 games): 220 casts, 1
  veto (no_shape_fit).** Caveat: 600 games at chunk 200 = 3 workers, so absolute games/hour is not
  the loop's operating point; the A–B delta is what the cache decision reads.
- **The search directive scaffold, compiled, not yet built into a jar** (the forkcheck at the end
  of the chain picks the newest jar in `target/`): `SearchDirective` (force one option at the
  copy's first window of the seat — pass, or a single SA by label under forbid-decline so the
  network fills the plan; natural play until the seat's next quiescent window = the leaf, whose
  peek — with the copy session's history ring — is captured and the copy ends), the controller
  branch, `AnvilBridge.value` / `asksSoFar` + the gRPC `anvil.value` ask (INT_IN_RANGE
  micro-units, no proto change), `AnvilRun -search [-searchrate p] [-searchrolls k]
  [-searchopts cap]` with `SearchMonitor` (active bridged seat's quiescent MAIN1/MAIN2 windows —
  GameCopier resumes at the active player's priority; rollSeed PAIRED across candidates per roll;
  fork J `determinize`: libraries reshuffled + the opponent's hand resampled from hand ∪ library)
  writing one `ev:"search"` labels row per window with per-option leaf values, kinds, forward
  calls, and the natural pick filled by the controller. Server side: `anvil.value` (the MASKED
  head on the peek), search-copy sessions (`g<i>.s<w>r<r>o<c>`) served greedy with no mu; tests.

## Broke / surprised me

- `Bash` resets the cwd after every call: a background `mvn -pl forge-gui-desktop` run from the
  Anvil dir failed with "could not find the selected project in the reactor"; always `cd
  /path/to/forge && mvn …` in the same command. Forge jars must run from `forge-gui/` (res/), not
  `forge-gui-desktop/`. `uv run` from another directory resolves another project's env.
- `canPayCost(effect=false)` is not legality-only: a planeswalker-ultimate coin flip (RNG!), the
  ward `willPayCosts` judgment, a Casualty AI filter — the realizer had been applying all three
  since M1.
- The draw-clock mislabel above (status "won" on a forced Draw).
- A `GameCopier` copy resumes only at the ACTIVE player's priority, so search windows are the
  active seat's quiescent main-phase windows — consistent with the ADR-0098 fork points.

## Next session picks up

1. **The smoke read** (`uv run python scripts/build0_smoke_read.py --ref
   data/runs/m9-rebaselinearm-s0-20260821-164002 --ingest`, then `scripts/obs_diff.py` on the
   A and B stores): residual veto classes, cap-hit rate, games/hour A vs B (cache ON/OFF decision),
   the paymask rescue class from C, the heuristic seat's expert-pick-outside-mask rate; the
   forkcheck tally vs `run-20260821-m9boundary` (expect ≈ 99% main-trace identity outside capped
   seeds). Pin the caps if the smoke moves them; record in ADR-0102's consequences.
2. **Build the search jar** (`cd forge && mvn -P windows-linux -pl forge-gui-desktop -am package
   -DskipTests -Dcheckstyle.skip`) and run the search smoke: a server on `iter-019`
   (`--mode model`, the value tag is advertised automatically), `java -jar … anvil -d … -b
   grpc:localhost:PORT -search -labels search.jsonl -obs … -census …` for ~20 games; read the
   search rows — leaf kinds (leaf / end / void / timeout / unserved), forward calls per option,
   decisions per leaf, ms per window, the margin distribution vs the natural pick — the per-game
   search multiplier that sizes Build 5; then its ADR-0025 proof (500-game forkcheck, flag off).
3. Then the Build 3 enumerators (Java only) as exempt commits; **Build 1 in parallel** on the
   corrected label set.
