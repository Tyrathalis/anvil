# 2026-07-11

# Session 1 (overnight from the 10th): D2 SA-level schema built end-to-end in one session — masked mass 0.004% not 31%, retrain launched

## Done

- **PR #11203 checked (standing entry): open, zero comments/reviews.** Nothing
  to act on; monitor continues next session.
- **D2 groundwork measurement (`scripts/d2/measure_sa_labels.py`, full-corpus
  sweep in `data/training/d2-sa-label-measure-full.json`):** 113,592 games /
  65.8M priority windows / 6.11M casts. **The label story collapsed in our
  favor:** multi-SA hosts = 16.7% of casts (corpus basis; play-time 31–36%
  per ADR-0010); the sa-string join resolves **99.92% exact + 0.08% via
  prefix-min fallback; masked = 228 casts (0.004%)** vs the ~31%/69% the plan
  priced. The trick: the "ambiguous" class is almost entirely
  *identical-string duplicates* (commander permission routes, same-rendering
  cost variants) — collapsing identical (host row, normalized sa) pairs into
  one candidate makes the label exact at candidate level, since the model
  couldn't distinguish identical descriptors anyway; the executor keeps
  first-fit for the engine-side residual tie. The 228 residual masks are one
  engine quirk (mode-bound modal option strings rendering a different mode
  than the plan string — genuinely ambiguous, masking correct).
- **SA-string vocab pinned: `anvil/training/sa_vocab_v1.json`, 33,691
  normalized strings** (X-suffix stripped), held-out OOV 0.03–0.05% of option
  mentions → the plan's first-choice featurization (learned embedding + kind)
  is viable; the frozen-text escape hatch stays untouched. NEVER regenerate
  the file in place — checkpoint embedding rows key off string order.
- **Loader (`dataset.py`): candidates = (host dedup row, normalized SA) pairs**
  in option order, identical keys collapsed, PASS candidate 0; labels resolve
  oi → exact string → prefix-min → masked (-1, policy-loss-excluded, still
  trains value + counts in host-level metrics via new `label_row`). Store
  reader joins `oi` from ret records (was silently dropped) and stamps
  `_pos`/`_retpos` record positions.
- **Nested-window history fix (the D2 rider):** `history_tokens(now_pos=...)`
  exposes a prior decision's host only if its ret arrived before the current
  window — mirroring the Java ring's back-fill-at-ret semantics exactly.
  **Measured skew mass on the heuristic corpus: 0** (open parents exist —
  `playChosenSpellAbility`, 31 in last-K across 50 games — but their rets
  carry no extractable host). So the fix is pure serve-parity insurance and
  cannot confound the D2 schema attribution. Good news for D3.
- **Model: pointer key += learned SA descriptor** (64-dim string embedding +
  8-dim kind, projected to d_model; PASS/pad slots contribute nothing);
  n_sa=0 reproduces the M1 architecture so old checkpoints load unchanged.
  Pointer plumbing refactored into `_pointer_logits` shared by
  forward()/act() — the two can no longer drift. 37.3M → **39.5M params**.
- **Both-bases eval:** every eval row now carries `agree_honest` (SA basis,
  the new headline) + `agree_honest_host` (M1-continuity basis) + `n_masked`.
- **Serve path: zero Java changes needed.** Server answers the model's exact
  option index with `host_level=False` (from ckpt config `sa_vocab_size`);
  GrpcBridge passes it through; `oneShotCast` wraps the singleton SA and the
  realizer's kind/order rungs can't fire — census rung telemetry will read
  "single" for all D2 casts, which is exactly D3's ~0-order-rung check.
  Old checkpoints keep `host_level=True` + full ladder.
- **Parity test updated and green** (SA candidates byte-compared, ring-faithful
  history emulation incl. the retpos rule); 49 other tests green; 300-step GPU
  smoke clean (loss falls, both bases print, act==forward).
- **Retrain LAUNCHED: `data/training/d2-sa`, 774K steps (D7 3-epoch recipe
  verbatim), nice-19, from pinned worktree `../anvil-d2train` @ `31664a1`**
  (data/ symlinked per-subdir — note: `data/pool` exists in worktrees because
  flex/overrides are force-tracked, so symlink subpaths, not `data/` itself).
  ~14h ETA. Log: `d2-sa.log` (redirect, no tail pipes).

## Broke / surprised me

- **The plan's "~69% recoverable / ~31% ambiguous" estimate was an artifact
  of matching at the wrong level.** Matching option strings against each
  other (M1's measurement) sees duplicates as ambiguity; matching the
  *chosen plan* against *collapsed candidates* sees them as one action. Four
  orders of magnitude less masked mass than priced. Lesson: measure the
  label algorithm you'll actually run, not a proxy of it.
- The 4K-game sample read a 6.5K vocab; the full corpus reads 33.7K — the SA
  string tail is long (mode-bound modal variants, X renders). OOV stayed
  ≤0.05% either way, so the vocab-embedding decision survives, but sample
  vocab sizes are not corpus vocab sizes.
- `ln -s repo/data worktree/data` lands INSIDE the checked-out `data/pool`
  (force-tracked config makes `data/` exist in worktrees) — creating
  `data/data`. Symlink the subdirs.
- prefix-min join alone (without exact-first) converts 1.9% of casts into
  *distinct-string* multi-matches — order the stages: exact, then prefix.

## Next session picks up

- **Check [#11203](https://github.com/Card-Forge/forge/pull/11203)** (standing).
- **Read `d2-sa` results** (~14h from 03:10 launch): both-bases finals vs
  0.9758 host-level baseline; per-tag; masked-mass report from metrics.
  Then **fresh pass calibration** (`calibrate_pass.py` on d2-sa/last.pt).
- **D3 arms reprice** (same harness, 200 valpair pairs mirrored, 1,000 games,
  δ=0 + rate-matched): the winrate delta vs 46.8% prices the schema change;
  census rung telemetry should read "single"≈100% / order≈0.
- Housekeeping: `../anvil-d2train` worktree stays until the run finishes;
  `d2-smoke` deleted; the sweep JSON + vocab are the D2 measurement record.
- D2 close-out ADR once the retrain + arms land (the masked-mass number
  corrects ADR-0010/plan-doc claims — ADR-worthy).

# Session 2 (daytime): first maintainer review on PR #11203 — consolidation direction, replied same day

## Done

- **tool4ever reviewed PR #11203 (CHANGES_REQUESTED, ~14:46):** substance
  accepted (`dangerouslySyncCardIdCounters` explicitly endorsed); the ask is
  architectural — with ids preserved, `GameCopier` vs `GameSnapshot` loses
  its load-bearing difference, so consolidate on the
  `EXPERIMENTAL_RESTORE_SNAPSHOT` path and delete the duplicated copy code.
- **Archaeology before replying:** GameCopier already delegates to
  GameSnapshot behind the flag (so the skeleton exists); GameSnapshot is
  id-keyed (`findById`) — exactly why id preservation unblocks it; **the
  snapshot path carries sibling versions of two of our three bug classes**
  (foretold restore commented out; only commanders re-wired → the 7 effect
  cards lazily re-create + orphan) — the maintainer's fixes-diverge argument,
  demonstrated. Gaps before snapshot can own simulation copies:
  `advanceToPhase` ignored in the delegation (their TODO), `PRUNE_HIDDEN_INFO`
  (dormant) has no snapshot equivalent.
- **Replied (user-approved):** keep PR #1 scoped to the differential-tested
  fixes; volunteered the consolidation as a follow-up PR **gated by
  forkcheck** (the 500-game digest + twin determinism harness is exactly the
  validation the switch needs); offered to fold the two snapshot-path fixes
  into this PR if preferred.
  [Comment](https://github.com/Card-Forge/forge/pull/11203#issuecomment-4946992438).
- **Worklist updated:** review state recorded; new "queued follow-up PR"
  section with the work items. Sequencing: after D2/D3 (not on the M2
  critical path).
- **d2-sa retrain health check: step 262K/774K (~34%), honest 0.9638 / host
  0.9645 / nonpass 0.8833 / valpair 0.9632** — tracking D7's trajectory
  exactly (d7-ep1 read 0.9647 honest at 258K); ~3.7K win/s steady.

## Next session picks up

- **#11203: await tool4ever's pick** (fold snapshot fixes into PR #1 vs
  follow-up-only). If they want the fixes folded: uncomment foretold restore +
  call `Player.copyEffectCardsToSnapshot` from `assignPlayerState`, add
  snapshot-path variants of the differential tests (SimulationTest has
  EXPERIMENTAL_RESTORE_SNAPSHOT coverage to build on).
- d2-sa finals + calibration + D3 arms (unchanged from session 1).

# Session 3 (evening): d2-sa retrain DONE — SA-level agreement at parity with the host-level record, order rung retired at zero agreement cost

## Done

- **d2-sa completed: 774K steps, 15.12h, exit 0, no anomalies** (rate
  3.6–3.7K win/s throughout; loss-print spikes were single-batch samples as
  usual). 600-batch finals (val, n_honest=148,899):

  | metric | d7-ep3 (host-level) | d2-sa (SA-level) |
  |---|---|---|
  | honest | 0.9758 | **0.9748 SA-basis / 0.9751 host-basis** |
  | nonpass | 0.9339 | 0.9247 (strictly harder basis: names the SA) |
  | target | 0.9754 | 0.9769 |
  | X (eval_x, n=1,518) | 0.874 | 0.852 (−2.2pp, ~1.7σ — watch) |
  | value BCE | 0.574 | 0.573 (floor, as diagnosed) |
  | mulligan | 0.9969 | 0.9969 |
  | n_masked | — | **1** in 600 val batches (the 0.004% class) |

  valpair ≥ val on every metric (honest 0.9777, nonpass 0.9288) — the
  zero-held-out-matchup-gap quirk holds on the new basis.
- **The headline: host-basis agreement 0.9751 vs 0.9758 = parity within run
  noise, while the model now names the exact SA.** The 31–36% of play-time
  casts the executor's order rung used to decide by scan order are now
  decided by the model at 0.9247 nonpass agreement. Action identity gained,
  agreement kept.
- **Fresh pass calibration:** model under-passes 1.7pp raw (0.8863 vs expert
  0.9029 — D7 read 0.8861, near-identical structure); **δ=+2.180**
  rate-matches and lifts honest to 0.9837 (D7: +2.169 → 0.9841).
  `data/training/d2-sa/pass_calibration.json` beside the ckpt.
- Housekeeping: `../anvil-d2train` worktree removed (run complete);
  `data/training/d2-sa/` is the record (ckpt + metrics + calibration).

## Broke / surprised me

- Nothing broke. The mild surprise is how exactly the pass-calibration
  structure reproduced across an architecture change (+2.169 → +2.180, same
  1.7pp under-pass) — the pass boundary really is a stable property of the
  corpus + pw=0.1 recipe, not of the pointer architecture.
- X head −2.2pp on the full-val basis: possibly noise (~1.7σ), possibly the
  X head paying for conditioning on the SA-level choice. Not blocking; if
  D6-era X telemetry shows it mattering, eval_x across intermediate ckpts
  will say when it diverged.

## Next session picks up

- **D3 arms reprice** (everything is ready): server with
  `--ckpt data/training/d2-sa/last.pt` (answers `host_level=False`
  automatically), δ=0 arm + δ=+2.180 arm, 200 valpair pairs mirrored,
  1,000 games each, census+obs on. Verify: rung telemetry reads
  "single"≈100%, order rung 0; veto decomposition vs D8's 3.1–7.8%.
  The winrate delta vs 46.8% prices the schema change.
- **D2 close-out ADR after the arms** (masked-mass correction to
  ADR-0010/plan: 0.004% measured vs ~31% priced — the collapse insight).
- #11203: await tool4ever's reply (fold-in vs follow-up).

# Session 4 (late evening): D3 arms — statistical tie, order rung retired in play, D2+D3 CLOSED ([ADR-0012](../decisions/ADR-0012-d2-d3-closeout.md))

## Done

- **Four arms on d2-sa, same 200 valpair pairs + same seeds as D8 (paired
  comparison), 400 games/arm:** δ=0 **0.4625 ± 0.0249** (D8: 0.4675) /
  δ=+2.180 **0.4400 ± 0.0248** (D8 dcal: 0.4550) — statistical ties;
  δ=0 ≥ calibrated again (RL init stays action-rich). 800/800 decisive,
  **0 crashes** (D8: 0.5%), 0 transport failures, **0 server fallbacks**
  (117K requests, 67 rps). Report: `data/runs/d3-arms-report.json` via new
  `scripts/arms_report.py` (formalized per the reuse rule; validated by
  reproducing the D8 report numbers exactly).
- **Order rung retired in play, exactly as designed:** rungs =
  {single: ~10.5K, modal: ~155} per arm vs D8's {single: 8.1K, order: 4.1K,
  kind: 79, shape: 597, pay: 282}. The model owns action identity now.
- **The honest headline ([ADR-0012](../decisions/ADR-0012-d2-d3-closeout.md)):
  the 31–36% order-rung mass did NOT cash out as winrate.** Kind-prior
  first-fit was a good proxy for the model's intent, and the exact-SA answer
  forfeits the ladder's rescue (unpayable pick → pass instead of
  rescue-cast: veto 7.8%→10.0%, casts/game 33.5→26.8). D2/D3's real
  purchase = the RL prerequisite (action identity + exact labels + per-SA
  veto decomposition) at zero agreement cost. New named leak candidate: the
  veto class (~10% of cast attempts); queued behind D4/D6.
- X overflow clamp fired 40/11,136 calibrated casts (0.4% vs 3-per-100K
  corpus estimate) — model over-predicts ">16" in play; watch in D6.
- **M2 done-when #2 SATISFIED** (SA ckpt both bases + repriced arms + order
  rung retired). Next deliverable: **D4** (rollout-labeled critic + Ante).

## Broke / surprised me

- **`pkill -f` matched the compound command that was also LAUNCHING the new
  server** (pattern appears in the relaunch text on the same command line) —
  killed both old and new. Separate the kill and the relaunch into different
  tool calls. (Same self-match family as the pgrep memory note.)
- The pure-heuristic arm names EVERY seat "Anvil(n)-deck" (only bridged runs
  name the opponent "Heur(n)") — winner-string sniffing gives winrate 1.0.
  arms_report reads `bridge_seats` from run.json instead.
- SIGTERM'd server loses its stats (they print on KeyboardInterrupt);
  use SIGINT (`pkill -INT`).

## Next session picks up

- **D4: rollout-labeled asymmetric critic + Ante mirror certification** per
  the plan: position sampler over the store, fork-at-decision via forkcheck
  machinery, K rollouts under d2-sa δ=0 on both seats; PILOT first (label
  economics: ~70 positions/h/worker naive → server micro-batching is lever
  #1); gate = beat the ~0.60 matchup-prior BCE floor, lift final-turn AUC
  off 0.68.
- #11203: await tool4ever's reply on fold-in vs follow-up (standing check).
- Housekeeping: d3arm run dirs + d3-arms-report.json are the D3 record;
  scratchpad server logs disposable.
