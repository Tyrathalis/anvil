# 2026-08-05: d6-run12 complete — flat held-out drill read; the closing gate is running

## Done

- **`d6-run12` ran clean end to end:** 20/20 iterations, ZERO guard
  trips, no watchd stalls, exit 0. Throughput held the w=16 band
  (~55 min/iter including arms + drill-eval phases). Anomaly flags all
  routine-class with run11 precedent (fallbacks ≤6, tripwire ≤3, the
  shaped-reward-vs-head gap — which narrowed as the run progressed:
  0.27–0.32 vs 0.48 at iters 0–1 → 0.39–0.40 vs 0.50 by iters 12–13).
- **Mid-run drill-eval phase worked as designed (first run to carry
  it):** iter 9 = 0.3416 vs baseline 0.3488 (flat overall; lost +5.3pp,
  upper bins slightly down — continue verdict, matching run11's flat
  iter-9 profile on v2); **iter 19 = 0.3467 vs 0.3488 (−0.2pp, FLAT)**
  with the conversion trade visible (lost +2.6, long_shot +0.6, coin
  −4.0, winnable −4.7). Contrast: run11's closing read on ITS evalset
  was +3.9pp. First hard signal leaning toward the migration read's
  on-file prediction (Δ2 < Δ1 via transfer/conversion).
- **Closing gate launched:** `final_read` on
  `d6-run12/iter-019/train/last.pt` (`run12-i019-finalarm`, standing
  protocol defaults, quiet box). Δ2 headline vs 0.5316 expected
  ~3 h out.

## Broke / surprised me

- Nothing broke. The drill-eval arc (iter 9 lost-bin +5.3pp fading to
  +2.6pp by iter 19 while winnable/coin went negative) is the
  interesting curve: the drills still convert dead positions early,
  but the gains partially trade away rather than compound.

## Next session picks up

- Read `run12-i019-finalarm` corrected result → Δ2 vs Δ1
  (+1.98pp ± 0.71). Fresh-seed confirmation if marginal (standing
  tiebreaker). Then the closing ADR with the slope verdict + the
  decomposition (curation profile [already: stock undiminished] vs
  held-out transfer [drill-eval: flat] vs conversion trade [winnable
  regression visible]) — either direction closes honestly.
- D3 critic calibration remains (the ~1,900 + ~1,700 new banked
  K-rollout labels vs the 0.58-on-0.23 critic).
