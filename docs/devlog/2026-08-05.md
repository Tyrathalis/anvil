# 2026-08-05: d6-run12 complete and RESOLVED — Δ2 = −0.58pp ± 0.73, one-shot verdict (ADR-0035)

## Done

- **`d6-run12` ran clean end to end:** 20/20 iterations, ZERO guard
  trips, no watchd stalls, exit 0. Throughput held the w=16 band
  (~55 min/iter including arms + drill-eval phases). Anomaly flags all
  routine-class with run11 precedent (fallbacks ≤6, tripwire ≤3, the
  shaped-reward-vs-head gap — which narrowed as the run progressed:
  0.27–0.32 vs 0.48 at iters 0–1 → 0.39–0.40 vs 0.50 by iters 12–13).
- **Mid-run drill-eval phase worked as designed (first run to carry
  it):** iter 9 = 0.3416 vs baseline 0.3488 (flat overall; lost +5.3pp,
  upper bins slightly down — continue verdict, matching run11's flat
  iter-9 profile on v2); **iter 19 = 0.3467 vs 0.3488 (−0.2pp, FLAT)**
  with the conversion trade visible (lost +2.6, long_shot +0.6, coin
  −4.0, winnable −4.7). Contrast: run11's closing read on ITS evalset
  was +3.9pp. First hard signal leaning toward the migration read's
  on-file prediction (Δ2 < Δ1 via transfer/conversion).
- **Closing gate launched:** `final_read` on
  `d6-run12/iter-019/train/last.pt` (`run12-i019-finalarm`, standing
  protocol defaults, quiet box). Δ2 headline vs 0.5316 expected
  ~3 h out.

- **The gate resolved same day ([ADR-0035](../decisions/ADR-0035-d2-compounding-read-resolution.md)):**
  standing-seed read 0.5442 ± 0.0109 (paired vs run11 stores +1.20pp ±
  1.04, t=1.15 — marginal), so the standing tiebreaker fired: fresh-seed
  confirmation on seed base 20260711 (deliberately matching run11's
  confarm base so `paired_arms.py` joins game-by-game). Fresh half:
  0.5335 ± 0.0110, paired **−2.35pp ± 1.03**. **Combined Δ2 = −0.58pp
  ± 0.73 (t=−0.79, 3,995 games) vs Δ1 = +1.98pp ± 0.71 ⇒ ONE-SHOT.**
  Gate not cleared, no promotion; baseline stays 0.5316. The 3.5pp
  disagreement between seed halves (~2.4σ) is itself a lesson: a
  single-set read at this effect size can mislead in either direction.
- **The decomposition was unanimous and pre-registered:** stock
  undiminished (D1 curation), held-out transfer flat (drill-eval), the
  conversion trade returned (winnable −4.7pp at iter 19). The migration
  read predicted this from curation-stage data at ~1/40th the compute —
  ADR-0035 promotes it to a standing tool (run before pricing any
  cycle).
- Note-to-self recorded en route: the per-arm ante jsons report the
  SEAT-0 perspective — arm s1's "0.4434" is the opponent's number, the
  model's is 0.5566. Misread it live before checking run11's arms.

## Broke / surprised me

- Nothing broke. The drill-eval arc (iter 9 lost-bin +5.3pp fading to
  +2.6pp by iter 19 while winnable/coin went negative) is the
  interesting curve: the drills still convert dead positions early,
  but the gains partially trade away rather than compound.

- **D3 RESOLVED same session ([ADR-0036](../decisions/ADR-0036-d3-critic-calibration.md)),
  with the user-agreed residual-decomposition rider folded into the
  plan first.** `scripts/critic_calibration.py` (the audit invariant,
  mechanized): 3,750 labels (1,879 c1 + 1,871 c2, zero trace-join
  misses), per-era × per-critic, deterministic 80/20 game split.
  **Split verdict:**
  - Absolute scale: isotonic takes held-out ECE 0.30–0.36 → **0.025–
    0.048** (≈ repeat-noise floor 0.04–0.07) on every era × critic
    slice. ADOPTED, era-scoped, for cycle-3 curation/doom labels.
  - Ranking: Spearman **0.26–0.29** everywhere vs an achievable
    **0.94–0.97** (Spearman between two independent K=8 re-measures of
    the same positions — the evalset baseline pairs). Post-remap
    residual by true bin: winnable −0.56, coin −0.30, lost +0.16;
    global (worst decks ±0.10–0.25 @ n≈10, turn buckets modest). The
    critic collapses the curated-loss population to one value. No
    monotone map can fix ranking ⇒ the rider's reading (2):
    **representation-level blindness — the measured M6 candidate.**
  - Footnote: Platt Newton diverged on one slice (c1/v_era);
    isotonic is the adopted form.

- **M5 CLOSED same session (user-confirmed,
  [ADR-0037](../decisions/ADR-0037-m5-closeout.md)):** all three
  clauses TRUE; honest headline = the drill loop is a one-shot
  corrector per curation method, and the measured reason is
  representation. Close discussion settled two things first (user
  round): no rechecks with the recalibrated critic (calibration fixes
  aggregates, not the per-position blindness — the rollout maps
  already supersede critic-classified numbers; Ante corrections are
  self-centering so closed baselines stand), and the frozen-trunk
  ranking probe belongs at the START of M6, not the end of M5.
  Status bullet archived verbatim (status-archive M5 section), compact
  summary in CLAUDE.md, map swept (M5 collapsed/pill, Now → M6
  scoping, header pills incl. the stale "M4 open", ADR index 0035–0037,
  module-card 51.2%→53.2% refresh, stamp).

## Next session picks up

- **M6 planning session.** Inputs on file in ADR-0037: the deciding
  probe (fresh head on 3,750 rollout labels vs frozen trunk features —
  ranks ⇒ rollout-label targets/ADR-0015 path; doesn't ⇒ encoder
  work); curriculum composition as the proven cheap lever; Ante riders
  + isotonic wiring riding with the critic work; carried fork items
  unchanged.
