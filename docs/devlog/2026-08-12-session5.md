# 2026-08-12 session 5 — recipe PINNED, assets built, overlap orchestration

## The recipe pin (user, this session)

1. **Mixture: windows-only** — drill fork stores serve ONLY as L_seq window
   sources, never the training mixture (`--drill-windows-only`); the bundle
   is the sole training-signal delta vs the base recipe. Drill phase drops
   to K=2 (frames exist only to carry windows).
2. **Anchor: crash−2 in-band** — the ADR-0053 evidence population.
3. **Evalset: rebuilt fresh** from the d3rb curation with a current-era
   baseline (D2.4 pairing).
4. **Constants: ADR-0054 as built** (λ=0.01 first-attempt-only, N=4, K=16,
   P≈100, w_seq auto-calib, 3-arm campaign on the era jar). User leaned
   thorough (P=100 stays); speedup via ORCHESTRATION not design trims.

## Done

- **drill-evalset-v4**: 138 held out (winnable 32 / coin 41 / long_shot 37 /
  lost 28 — the ⅓-per-bin convention) from the crash−2 map. Baseline
  measurement (iter-019, D2.4) launched detached + watchd
  (`evalset-v4-baseline`) + artifact-keyed waiter.
- **drill-selection-v5**: 217 in-band [0.25, 0.85] crash−2 points, evalset
  holdout subtracted at the boundary. P=100 rotates the pool every ~2.2
  iterations.
- **Driver: `--overlap-campaign`** — generation ‖ (drill → campaign) as two
  concurrent tracks (ThreadPoolExecutor over the existing subprocess
  phases; campaign servers on `--campaign-port`, default port+2). Both
  tracks serve ckpt_k, so the trained gradient is recipe-identical to
  sequential — pure wall-clock overlap, ~3h/iter → ~2h20m at P=100. Fleet
  interference is a measured question: per-track walls now land in the
  monitor row (`campaign_s`) and the battery plots them.
- **Driver: `--drill-windows-only`** — fork stores excluded from the
  mixture group, still threaded to --seq-stores.
- Port-threading bug caught by the suite: the blanket `args.port`
  replacement hit `_drill_eval_phase` (no port param) — NameError, fixed;
  suite 159 green.

## Speedup dispositions (user asked for leads)

- **Adopted:** within-iteration overlap (above). **Queued:** w-scaling
  bench 16/24/32 (`bench_generation.py`, runs after the baseline eval
  frees the box — informs the fleet split); adaptive micro-batch window =
  optional, decide on the smoke reading (behavior-safe by design — seeded
  sampling is batch-composition-invariant — and obs-diff-gate verifiable).
- **Declined:** cross-iteration pipelining (on-policyness delta, M6
  deferral stands), 2-arm jar change mid-era, every-other-iteration
  campaign (fights the freshness pin), K/N trims (weaken the measured
  design).

## Addendum (same session): baseline pinned, w-bench read, smoke LAUNCHED

- **Evalset v4 baseline: 0.4778** (winnable 0.857 / coin 0.569 / long_shot
  0.329 / lost 0.107) — the D2.4 reference for the run's drill-eval phase.
  Much richer than v3's 0.3488 (more contested map population, new era).
- **w-bench (256 games/arm, quiet box): w=16 → 2,111 g/h, w=24 → 2,408
  (+14%), w=32 → 2,068 (REGRESSION)** — the 32-core box saturates near 24
  total workers. Report: `generation-bench-w162432.json` (bench pins its
  own ckpt, run7b-i014 — fine, w-scaling is relative). Two launch failures
  first (`--ckpt` not an arg; 240 games fails the chunk-divisibility
  guard → 256), both caught instantly by the waiter's crash branch —
  live-fire proof of the exit check.
- **`--campaign-workers` added** (threaded like the port): under overlap
  keep gen+campaign totals near the w=24 knee; the run's likely split is
  gen 8 + campaign 16 with the campaign as critical path.
- **Smoke LAUNCHED** (`scripts/launch_bundle_smoke.sh`, 1 iteration, 160
  games, P=20 campaign, K=2 drill windows, λ=0.01+first, overlap ON at
  16/16 — deliberately the saturated config so the interference cost is
  measured, not assumed). Driver self-registers watchd; artifact waiter
  keyed on the run-end battery report. Both tracks confirmed interleaving
  in the log at launch.

## Next

1. **Smoke reading** (waiter fires): join counts, w_seq calibration
   sanity, critic aux rows, guard status, gen_s vs campaign_s under
   contention → pick the run's fleet split → micro-batch-window decision.
2. Check run11's loop_config workers before pinning the final run command
   (run13's says 8; the recipe of record says w=16).
3. The run: 20 iterations vs the 0.5373 gate (~2 days wall at P=100 with
   overlap).
