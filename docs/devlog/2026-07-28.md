# 2026-07-28

## Done

- **Playable fork: first release PUBLISHED.** The remaining human step from the
  updater track (items 3+2+1) executed end-to-end:
  - The parallel workstream's commits on `playable` are not yet public
    (per user: patches in flight upstream; do not publish that material), so
    the release was cut from a new **publish branch `playable-pub`**
    (worktree `../forge-pub`): the 19 QoL/updater commits cherry-picked onto
    the shared pre-workstream base `5fbc2ac98d`. Conflict-free; verified zero
    file overlap between the two ranges, and the resulting tree differs from
    `playable-qol` by exactly the other workstream's files.
  - Gates re-run on the publish branch: shipping jar builds
    (`2.0.14-SNAPSHOT`, 63 MB); targeted suites
    DeltaUpdateTest + DeckSiteSyncTest + RotatedRectTest **16/16 green**.
  - Pushed as **`origin/playable` @ `cf8e71deef`** — the branch name the
    repointed commits atom tracks. Local `playable` ≠ `origin/playable`
    until the other workstream's commits become public; reconcile then with
    a disjoint-file merge.
  - `daily-snapshots` prerelease created on `Tyrathalis/forge` with
    `forge-playable.jar` / `manifest.txt` (54,654 files) / `version.txt` /
    `build.txt` (2.0.14-SNAPSHOT-07.29).
  - **Live verification against the real network:** version/build/manifest
    URLs serve from the release; `commits/playable.atom` returns 200; a raw
    res fetch at the manifest commit **hash-matches the manifest** — the
    delta fetch shape works for real, not just over `file://`.

- **Session continued: fresh-install test passed, first live bulk sync
  crashed, fix round republished same evening.** The user installed from the
  release (runs, shows the QoL changes) — then bulk sync crashed on first
  use. Trace pulled from the user journal (app stdout goes to the session
  socket): `StorageNestedFolders.add` is an unimplemented stock stub, and
  `getOrCreateSubfolder` builds children on the parent's serializer —
  `getFolderOrCreate` never worked upstream; item 7 was its first caller.
  Fixed via the load-time nested factory; `StorageSubfolderTest` validated
  failing first with the production exception, then green (full desktop
  suite 347 green). Riders: `LoadingOverlay.runBackgroundTask` hides on the
  error path (the crash dialog was unreachable behind the modal overlay),
  and the **>23h update gate retired** — prompt on any strictly-newer build,
  empty delta plan = silently current (the old gate would have hidden this
  crash fix from every <1-day-old install). Second publish same day:
  `origin/playable` @ `31ce920154`, build stamp 2026-07-29 02:01:15,
  `upload --clobber` flow verified. Storage fix queued as an upstream
  candidate (latent-correctness class).

- **First successful live sync: 133 decks in 5–10 min (the politeness math,
  working as designed) — followed by a sync UX round (`5dcebd13ff`),
  published as release v3:** Enter submits input dialogs (the text field's
  edit-mode handler was swallowing it; opt-in submit handler, other fields
  untouched), pasted Archidekt profile/search URLs parse to a username
  (live-probed the real link shape: `/search/decks?owner=` — `/u/` 404s;
  unrecognized input passes through verbatim; prompt text ×9 locales), and
  the syncer's per-deck progress messages — previously dropped on mobile —
  drive the overlay caption live via a `runBackgroundTask` variant with a
  caption updater. v3 is the first release the user's install offers
  **in-app** (their build carries the retired-gate change), so accepting it
  = the owed real delta-update pass.

- **The first genuine in-app update attempt failed — and found a bug no gate
  could have seen (`4f1897779e`, release v4):** the prompt + changelog worked
  (retired-gate change verified live), but `DeltaManifest.parse` rejected the
  real manifest: the res tree contains
  `res/adventure/common/maps/map/aerie/wastetown..tmx`, and the path-safety
  check refused `..` as a *substring* — every real delta plan aborted to the
  legacy fallback (which can't work: we don't publish an installer jar).
  Fixed segment-wise (`..`/`.`/empty segments, absolute paths, backslashes
  still refused), test carries the real filename. Lesson extended: the
  fixture manifests were all safe synthetic names, and the earlier live
  verification curl'd manifest+file without going through parse — **verify
  with the production artifact through the production code path**, which is
  exactly what the user's real attempt was. Game with a synced deck also
  played (user) — item 7 play residue closed.

- **Item 8 + the pool hash-check rider, both built and shipped (session
  close):** Adventure's zone-layout selector revived (`a5e819166a` — the
  option existed as `UI_ALT_PLAYERZONETABS` Off/Vertical/Horizontal; the
  Adventure settings entry was a commented-out stale checkbox from before
  the pref widened; Adventure boot forces Vertical over a legacy boolean
  pref, which is exactly the vertical hand the user saw). Published as v5
  (`8a04677902`, stamp 2026-07-29 02:53:10, production-parse check green) —
  the vehicle for the user's real in-app delta update pass. Research-side:
  `launch --pool` now gates on installed-deck content hashes (Anvil
  `89178d7`, `verify_installed_decks`, pool suite 15 green) — the shared
  user-store hazard is now loud instead of silent. Also learned: a fix to
  the updater cannot arrive through the updater it fixes — the v2 install's
  buggy parser rejected v4's manifest too; one final manual jar refresh was
  the unavoidable cost.

- **Session close: the v4→v5 self-update ran end-to-end (user: "it
  downloaded the patch and restarted itself") — the updater track is fully
  closed and the publish pipeline proven.** Horizontal hand verified in play
  (cramped on narrow windows — the display covers half the bottom width,
  upstream's layout; very reasonable fullscreen ultrawide; noted, not
  scheduled). Every founding worklist item plus item 8 is now built,
  shipped, and user-verified. Track residue: rotate-180 two-human check,
  desktop shallow-angle look, T2.

- **Desktop shallow-angle VERIFIED (user, desktop client launched from the
  worktree, 30°): same look as mobile, "overlap looks intentional" — which
  also closes item 5's step-4 draw-order question. The inverted Swing
  z-order costs nothing in practice; the z-order surgery item is retired
  unbuilt.** Track residue is now just the rotate-180 two-human check + T2.

## Broke / surprised me

- `playable-qol`'s ancestry still carries five early other-workstream commits
  (it was cut from `playable`'s tip on 07-26, before that branch was later
  rewritten — the merge-base fell back to `5fbc2ac98d`). Pushing `playable-qol`
  itself would have published them. The publish-branch cherry-pick exists
  precisely to avoid that; keep using it until that work is public.
- Maven surefire quirk: `-pl forge-gui-desktop -am test -Dtest=...` fails in
  `forge-core` ("no tests matching pattern") unless
  `-Dsurefire.failIfNoSpecifiedTests=false` is passed.

## Next session picks up

- **First real in-app update pass** — needs a second release to move *to*;
  lands naturally with the next publish (boot the older install, accept the
  prompt, confirm delta apply + auto-restart).
- The ~30-line research-side rider: `launch --pool` hash check of installed
  `dc-*` decks vs `data/pool/decks/`.
- Play-pass residue: first live bulk Archidekt sync + a game with a synced
  deck; the rotate-180 two-human check; desktop shallow-angle look.
- Once the other workstream's commits are public: merge local `playable` with
  `origin/playable` and retire the publish-branch dance.
- (Research spine, unchanged: M3 closeout decision, then Grindstone/M4 design;
  collate refactor before the next long RL run.)
