# 2026-08-10 session 3 (ran into 08-11: K-sizing read → map-serving mismatch → corrected read; ADR-0052)

## Done

- **Harness plumbing:** `--force-branch` through `anvil.bridge.harness launch`
  (manifest key + `-forcebranch`, mirroring `-forkobs`); smoked on a 28-point
  run before the real reads.
- **K-sizing read v1 (crash-anchor, m7-plan pin 7):** K ∈ {4,8,16} full-165
  s0 drillfile. Instrument mechanically flawless (100% pairs, 0 crashes,
  paired SD < independent floor) but SD tracked 1/√K exactly — null signal —
  and per-point detail showed mean completion wr **8%** where the selection
  band promised 0.25–0.85.
- **Chased the discrepancy to ground** (argmax v2 read falsified the
  serving-mode theory; natural-rollout control + per-point joins nailed it):
  **`grindstone generate` serves the mainline replay argmax (`sample=False`,
  always has); argmax replay of sampled sources diverges; the drill map has
  been pricing states that aren't the source games' states** since curation
  went sampled-source. Map wr 0.374 vs true 0.062, corr 0.23. Hold-branch vs
  natural-control corr 0.885 = forced harness exonerated. Full story +
  blast-radius table: [ADR-0052](../decisions/ADR-0052-ksizing-read-map-serving-mismatch.md).
- **Fix landed:** `--sample-mainline` in `gs generate` (+`instrument` param in
  `_start_server`); `mainline_serving` stamped into map manifests. 139 tests
  green.
- **Corrected three-anchor true maps built** (`data/runs/drill-map-cycle3-true/`,
  o2/o4/peak, 96%+ fire rate): true crash states nearly dead (9% band mass);
  crash−2 contested (wr 0.491, 52% band); peak most alive (0.556).
  Reconstructed minimal `cycle3-s0/s1` source dirs (run.json + pairs.txt,
  sha-verified) — the stale-data pass had deleted what `gs plan` needs.
- **Corrected K-sizing read run and resolved** (59 forced in-band points at
  crash−2, both seats, K ∈ {4,8,16}): **null at every K — a real verdict
  now**. RMS true Δwr ≤ ~0.08 at K=16, pin 0.10 unreachable, SD pure-noise
  scaling. Per-decision Δwr labels falsified as a training signal at
  affordable K; the signal lives in sequences (composes with ADR-0049).
- **User-approved rereads recorded:** run11 = distributional supplementation,
  not case-drilling (a→a2 flip, one-shot fit); case-drilling never actually
  tried; blast-radius standing rule (nulls suspect, positives survive).
- **Talor's fork PR #1 reviewed** (AiBlockController block-legality cache,
  claimed −15.6%): design sound, call-site coverage grep-verified complete,
  residual risk = helper-mutation staleness → empirical gate. Verification
  in flight at session close: pr1-verify worktree jar built; **two 500-game
  forkchecks running in parallel (FIXED_HASH=1, same seeds)** for
  cross-jar `mainTraceHash` identity per ADR-0025. Bench on our recipe
  queued behind them. No PR comment/merge yet — user sees numbers first.

## Broke / surprised me

- `pkill -TERM -f "anvil.bridge.server"` in the same compound command as a
  new server launch killed its own shell (self-match). Use a
  non-self-matching pattern: `pkill -TERM -f "[b]ridge.server"`.
- The stale-data pass had deleted `cycle3-*/run.json` that `gs plan` needs —
  keep source run.json + pairs.txt (bytes) in future passes.
- The drill map's fire rate (75% at argmax vs 96% at sampled serving) was
  itself a hidden symptom of the serving mismatch.
- s1 had 2 hold-only in-band points (SKIP_EXHAUSTED = every act attempt
  vetoed) — the loud-skip design worked; s0 had none.

## Next session picks up

1. **Forkcheck pair verdict + bench — RESOLVED overnight:** behavior-identical
   (499/500 cross-jar mainline identity; the 1 mismatch = seed 20260844
   same-jar launch nondeterminism, baseline disagrees with itself 1-in-3;
   discriminator: 3× single-seed rerun per jar via `-seeds`). Speedup does
   NOT reproduce on our workloads: genbench 1882.5 → 1867.1 g/h (−0.8%),
   fork-replay segments −1.4% (parallel-run caveat), distribution identical
   p50→max. Merge decision + reply to Talor = user's call; candidate homes
   where the cache plausibly matters: upstream Forge, `playable` AI latency.
2. **D2 routing decision (user):** tier-3 search targets vs
   corrected-population mixtures + C2a vs sequence-level contrastive
   design round. C3 pre-work read still queued.
3. Selection re-derivation on corrected maps if any drill mixture is funded.
4. D3 stability pass still rides with the first heavy fork campaign.
