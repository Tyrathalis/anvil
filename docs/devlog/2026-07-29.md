# 2026-07-29 (overnight continuation of the M4 design session: D1 run + read + closeout)

## Done

- **`d6-run9` (M4 D1 adaptation probe) ran overnight: 10/10 iterations,
  zero guard halts, zero tripwire violations.** KL/iter 0.004–0.009,
  entropy pinned ~0.125, ρ ~0.99; 3 crashes in 4,800 games (known classes,
  incl. one carried `IndexOutOfBoundsException`). First production proof of
  the collate fix: train ~3 min vs generation ~28 min per iteration.
- **Closing read launched at ~05:30 and landed by ~07:15:** iter-009 raw
  0.5230 ± 0.0112 / corrected 0.5233 ± 0.0110 (2,000 games, seeds 20260710
  = fully paired with the post-rebase baseline read); Ante zero-mean both
  arms. **Paired vs run7b-i14: +1.15pp ± 0.90 (t=1.28, 174 up / 151
  down) — directionally-positive tie.**
- **[ADR-0028](../decisions/ADR-0028-d1-adaptation-probe.md): D1 CLOSED,
  M4 done-when #1 satisfied.** Verdict: **the heuristic got stronger** —
  the fast-recovery (opponent-fit) hypothesis resolved negative; residual
  ≲1pp. Baseline stays 0.5121, ckpt of record unchanged. Veto re-fit was
  real and strength-neutral (first-attempt 0.118→0.082) — the wasted-
  intent lesson's third confirmation. `d6-run9/iter-009` designated D3
  init (adapted, veto-clean); the probe also re-confirms ADR-0024's
  absent-signal diagnosis on the new engine.
- Plan/status/map updated in sync (m4-plan done-when #1 struck, CLAUDE.md,
  ADR index, footer).
- **Watcher hygiene en route (prior evening):** the stall tripline the
  user asked for was added (wake if no run artifact written in 60 min
  while the driver is alive) — and its first version false-tripped
  instantly because system `find` is `bfs`, which rejects relative
  `-newermt` timestamps (error swallowed by `2>/dev/null` reads as "no
  fresh files"). Fixed with `-mmin`; memory note written
  (`find-is-bfs-no-relative-newermt`). Lesson: dry-run watcher triplines
  against the live system, without stderr suppression, before arming.

- **D2.1 DONE (same morning, user go-ahead):** early-doom/curation
  regenerated on the `run9-finalarm` stores. **The shape survived the
  rebase almost exactly:** 584 addressable losses (61.2% of 954 vs old
  531/59.6%), ceiling 0.82–0.91, luck-locked 18–38%; 208 single-turn
  ≥30pp collapses across 82 model decks (old 185/102), median crash turn
  13. Two-critic agreement (run9-i009 critic vs `d4-critic-fullvis`)
  within 0.3pp on every headline — tighter than the old-scale ~1pp.
  Records `data/runs/early-doom-run9-{i009,d4crit}/`; the new-engine
  drill seed list is `early-doom-run9-i009/curation.jsonl`. Each trace
  ~200s. User corroboration for the D1 verdict: Discord shows active
  bot-tuning testing in the rebase window.

- **D2.2+D2.3 (same day, user go-ahead "carry on"): drill machinery built
  end-to-end and smoke-gated.** Fork `46e2ec7c16`: `-drillfile` (explicit
  per-index fork turns replace `-points` sampling; unlisted indices skip
  without creating a game, `drill_skip` results rows keep harness resume
  exact; labels rows gain `tt`) + `-drillstop` (mainline ends at its last
  fork). Anvil `b1b6117`: `anvil.grindstone plan|generate` (curation →
  per-arm drillfiles + manifest carrying the source run.json replay recipe
  verbatim; generate mirrors the source environment — obs ON, census off —
  and serves the manifest ckpt argmax); harness `--drill-file/--drill-stop`
  passthrough (drillfile copied into the run dir like pairs.txt); 3 new
  pinned-fixture tests, suite 103 green.
  - **Determinism gate: 7/8 source games reproduce exactly** (status/
    winner/turns) — also the empirical jar-equivalence proof for the
    rebuilt jar. The 1 divergence = the known ~1–2% heuristic wall-clock
    replay-drift class; a diverged replay misses its fork turn and yields
    NO label (miss-rate = accounted statistic, not silent loss).
  - **Economics: ~47 s/drill all-in at w=4/K=8** (~12 s/completion;
    rollout block ≈ 93 s) ⇒ 584-drill sweep ≈ 2.5–4 h at w=8. The
    batch-1-era 17 positions/h/worker price is dead.
  - **The smoke's 48 completions already reshape D3: model wins ~33%
    from crash windows the critic priced at 70–90%** — two games 0/8
    (the decision error is UPSTREAM of the crash window), one 8/8 (the
    source loss was concrete draw order). Fork-at-crash is often too
    late: `--turn-offset`/`peak_turn` targeting is the first D3 sweep
    axis, and the K-outcome labels are themselves the value-correction
    signal (the rollout-label thesis).
  - Quirk pinned: `-drillstop` mainlines get a Forge-computed
    pseudo-winner (not a draw as intended) ⇒ drill-run games.jsonl
    outcomes are not game outcomes; drill MAINLINE stores must never
    enter training ingest (D3 must ingest fork sessions only).

- **The full 584-drill ground-truth map (K=8, `drill-map-r9i9-k8/`),
  `grindstone report` verb formalized** (multi-run aggregation, later runs
  supersede per game — the re-drill flow; tested): 578/584 labeled,
  replay-miss 6 (1.0%, the predicted drift class), **rollout winrate 23.7%
  vs mean v_before 0.584**. Bins: **lost-at-crash 330 (58%) / long-shot
  123 (22%) / coin 50 (9%) / winnable 66 (12%)** — most value-crash
  windows sit AFTER the real decision error ⇒ the turn-offset/peak-turn
  sweep is D3's first experiment.
- **The map caught two engine crash classes (44 all-crash positions):**
  1. **Monarchy-transfer copy bug — FIXED (fork `6d728677d1`):**
     `Zone.remove()` never clears the removed card's zone field; the
     ex-monarch's cached `monarchEffect` keeps a stale pointer;
     `copyEffectCardsToSnapshot`'s pointer guard fed it to `find()` →
     every `makeCopy` of a monarchy-transferred game threw "Couldn't map
     The Monarch". Initiative identical. Fix = zone-list membership guard;
     `MonarchTransferCopyTest` ×2 validated failing first; desktop suite
     299 green; 35/44 positions recovered on re-drill. Upstream PR
     candidate queued (bundle w/ the `getMonarchSet` inverted ternary
     spotted en route). Rollout catches gained `-Danvil.crash.trace`
     stack traces (the silent (8,0) pattern cost a diagnosis round);
     `ANVIL_EXTRA_JVM_OPTS` passthrough added to the harness.
  2. **MayPlayPlayer `.get(0)`-on-empty — DIAGNOSED, deferred:** 9
     positions crash every copy at `StaticAbilityContinuous:900`
     (`getDefinedPlayers(...MayPlayPlayer...)` empty mid-copy — statics
     re-applied before remembered wiring, most likely). Upgrades the
     carried post-rebase IndexOutOfBounds item with a deterministic repro
     (`drill-crash2-rows.jsonl`); honest fix wants the ordering question,
     not a blind guard — its own session or the consolidation PR.
- Lesson re-learned expensively: I ran the desktop suite and the jar
  build CONCURRENTLY on the same maven tree — clobbered classpath, one
  wasted suite run. One maven invocation per tree at a time.

## Broke / surprised me

- The `bfs` `-newermt` incompatibility (above) — a watcher that guards
  against silent starvation almost became a silent false alarm.
- Nothing else: the run, the read, and both notify hooks behaved exactly
  as designed. The i4 arms dip (0.4950) reproduced run-7b's early-
  consolidation shape and recovered on schedule.

## Next session picks up

- **D2.4 — drill eval suite:** fixed held-out drill set, per-drill
  winrate/value per checkpoint (the frozen-anchor instrument).
- **D3 integration:** fork-session store ingest + `drill` provenance tag
  (mainlines excluded — pseudo-winner hazard), dual-policy serving
  question (pinned replay policy vs live drill policy, routable by wid),
  the turn-offset/peak-turn sweep, then the first drill-mixed run.
- **Full-list drill sweep** (~2.5–4 h): run the whole 584-row curation at
  K=16 with `--turn-offset` variants — the ground-truth value map of the
  addressable-loss set, and the miss-rate at scale.
- **D4 can interleave:** `bench_generation.py` noise floor whenever the
  GPU is otherwise idle.
- Ante draw-poison coverage restated by the read (17,705 skipped nodes) —
  still queued with the next critic retrain.
