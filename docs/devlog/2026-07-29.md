# 2026-07-29 (overnight continuation of the M4 design session: D1 run + read + closeout)

## Done

- **`d6-run9` (M4 D1 adaptation probe) ran overnight: 10/10 iterations,
  zero guard halts, zero tripwire violations.** KL/iter 0.004–0.009,
  entropy pinned ~0.125, ρ ~0.99; 3 crashes in 4,800 games (known classes,
  incl. one carried `IndexOutOfBoundsException`). First production proof of
  the collate fix: train ~3 min vs generation ~28 min per iteration.
- **Closing read launched at ~05:30 and landed by ~07:15:** iter-009 raw
  0.5230 ± 0.0112 / corrected 0.5233 ± 0.0110 (2,000 games, seeds 20260710
  = fully paired with the post-rebase baseline read); Ante zero-mean both
  arms. **Paired vs run7b-i14: +1.15pp ± 0.90 (t=1.28, 174 up / 151
  down) — directionally-positive tie.**
- **[ADR-0028](../decisions/ADR-0028-d1-adaptation-probe.md): D1 CLOSED,
  M4 done-when #1 satisfied.** Verdict: **the heuristic got stronger** —
  the fast-recovery (opponent-fit) hypothesis resolved negative; residual
  ≲1pp. Baseline stays 0.5121, ckpt of record unchanged. Veto re-fit was
  real and strength-neutral (first-attempt 0.118→0.082) — the wasted-
  intent lesson's third confirmation. `d6-run9/iter-009` designated D3
  init (adapted, veto-clean); the probe also re-confirms ADR-0024's
  absent-signal diagnosis on the new engine.
- Plan/status/map updated in sync (m4-plan done-when #1 struck, CLAUDE.md,
  ADR index, footer).
- **Watcher hygiene en route (prior evening):** the stall tripline the
  user asked for was added (wake if no run artifact written in 60 min
  while the driver is alive) — and its first version false-tripped
  instantly because system `find` is `bfs`, which rejects relative
  `-newermt` timestamps (error swallowed by `2>/dev/null` reads as "no
  fresh files"). Fixed with `-mmin`; memory note written
  (`find-is-bfs-no-relative-newermt`). Lesson: dry-run watcher triplines
  against the live system, without stderr suppression, before arming.

- **D2.1 DONE (same morning, user go-ahead):** early-doom/curation
  regenerated on the `run9-finalarm` stores. **The shape survived the
  rebase almost exactly:** 584 addressable losses (61.2% of 954 vs old
  531/59.6%), ceiling 0.82–0.91, luck-locked 18–38%; 208 single-turn
  ≥30pp collapses across 82 model decks (old 185/102), median crash turn
  13. Two-critic agreement (run9-i009 critic vs `d4-critic-fullvis`)
  within 0.3pp on every headline — tighter than the old-scale ~1pp.
  Records `data/runs/early-doom-run9-{i009,d4crit}/`; the new-engine
  drill seed list is `early-doom-run9-i009/curation.jsonl`. Each trace
  ~200s. User corroboration for the D1 verdict: Discord shows active
  bot-tuning testing in the rebase window.

## Broke / surprised me

- The `bfs` `-newermt` incompatibility (above) — a watcher that guards
  against silent starvation almost became a silent false alarm.
- Nothing else: the run, the read, and both notify hooks behaved exactly
  as designed. The i4 arms dip (0.4950) reproduced run-7b's early-
  consolidation shape and recovered on schedule.

## Next session picks up

- **D2.2 — drill machinery:** drill manifest format + harness verb to
  fork at curated windows (re-aim `-rollout K -points M`), seeded from
  `early-doom-run9-i009/curation.jsonl`; re-price economics under
  micro-batching. (D2.1 done — see above.)
- **D4 can interleave:** `bench_generation.py` noise floor whenever the
  GPU is otherwise idle.
- Ante draw-poison coverage restated by the read (17,705 skipped nodes) —
  still queued with the next critic retrain.
