# 2026-09-14 (session 3) — the design session before evening 5

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **State review** (user's ask): Build 3 one evening from closing, six surfaces + tuck served
  (all −0.17 ± 1.80 vs withheld), the pay head withheld → the loop, the throughput week closed,
  the box idle, the tree clean at `e928c4b`, the fork at pin `1dd36f7342`; upstream 263 commits
  past the engine pin (08-22 → 09-15). The day-zero gate arm's +0.92 carries the probe cost — the
  post-Build-4 read re-measures it.
- **Evening 5 pinned** ([ADR-0106](../decisions/ADR-0106-m12-evening5-surface-acting-and-search-shape-reads.md) A):
  seven decisions, all agreed — the (option, answer) pair at the priority window, the served
  behavior as the natural line, bar 0.10 / T 0.025 / rolls 2, kind-agnostic with modes first,
  the multiplier measured, the 600-game read with its retire-the-gate rule.
- **The rebase placed** (user): between evening 5 and Build 4 — one era for the endgame, the
  shakedown as the landmine catcher, drift only grows. Sizing re-issued: 235–350K games with
  surface acting; launch two to three weeks out, close six to nine.
- **The search-shape reads** (user: worth the week; build the infrastructure; weave it into the
  model): the priority-slot leaf calibration first (hours), the equal-box-time multi-arm
  shakedown (≈ a week), the partial-expansion slot, fork L = the allocation head. The two-ply
  shape gated on the deep arm. Standing rule: search recipes compare at equal box time.
- Docs: ADR-0106; plan (Build 4 / 4½, fork F amended, fork L, the running record);
  standing-rules; CLAUDE.md Now.

## Broke / surprised me

- Nothing ran. One correction to my own framing mid-session: fork F's two-budget read measures
  the serving budget on a fixed checkpoint, not the training signal per box-hour — the user's
  question was the latter and nothing on file reads it.

## Next session picks up

1. **Evening 5's fork code**: `SearchDirective.Pending.decide` over the `sub` rows (the pair), the
   mainline `SurfaceDirective` arm consumed at the callback, census fields per kind, the
   priority-slot leaf plumb (`-searchleaf`), the partial-expansion slot; tests; smoke; forkcheck.
2. **The calibration read on the priority slot** (ADR-0106 C1) on the box while (1) is written:
   three heuristic-control arms (next / h2 / end) on one seed set, the 09-11 instrument.
3. Then the 600-game paired read (A6) → the rebase (B) → Build 4.
