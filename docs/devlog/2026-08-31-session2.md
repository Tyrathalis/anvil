# 2026-08-31 — session 2 (upstream review; parallel to the mint session)

Read-only session run alongside the phased mint (no tree edits beyond this file, no jar work, no checkouts in either worktree).

## Done

- **Forge Discord contribution-questions review** (Manabrew lazy loading thread + Nerdslayer AI proposal):
  - **Manabrew lazy/thread-safe CardDb patch** (Anacleto, `witchesofthehill/forge@1b515ac64d`, may come upstream as an off-by-default PR). Touches `CardStorageReader`/`StaticData`/`CardDb` plus five forge-game effect classes (`ChooseCardNameEffect`, `CopyPermanentEffect`, `MakeCardEffect`, `PlayEffect`, `PlayLandVariantEffect`) — i.e. **game-path files even with lazy mode off**, so if it lands it goes under normal boundary discipline (ADR-0025 empirical forkcheck, no exception). For us lazy mode itself is a determinism hazard, never to be enabled without proof: named-card choice effects enumerate the full db (the thread's own Phyrexian Revoker example), and cache-miss live loads change behavior. Upside worth remembering: per-JVM resident memory is our fleet-lane cost, and a matured lazy mode could raise lane density. Also: **Manabrew is a determinism-hooks consumer** — our own #11285 comment already cites their harness — a natural ally for reviving that PR and the longer-run `game.getRandom()` direction.
  - **Forge-in-browser via GraalVM Web Image** (khaliostr): `@manabrew/forge-wasm` npm package + writeup at manabrew.app/blog/graalvm. A wrapper/build around the engine, not an engine patch — zero boundary implications. Reference for far-future Mentor (client-side engine in a web product) and possibly playable-fork distribution.
  - **Nerdslayer's AI interaction-graph proposal** (index effect productions/consequences, score battlefield synergies for removal targeting). If it lands, heuristic-AI strength shifts — absorbed by era pinning like any engine change, but a stronger heuristic opponent/teacher is worth tracking. More useful as a **maintainer-posture data point for our eventual contribute-back**: TRT wants "small contributions, not having your AI go wild"; difficulty levels rejected ("just give it worse cards"); precedent PR #10176 (key cards). Incremental, profile-compatible pieces are the acceptable shape.
- **Upstream sweep: 66 commits on `23c3d2a85d..upstream/master`** (fetch + log only). Nothing demands action now; all of it lands at the next boundary event. Notables:
  - **#11654 touches `GameCopier`** (`find()` genericized to `<T>`) + a `Card` face-down/`mayPlay` ability refactor. Not obviously a fix for our ordering-sensitive copy+resume crash class, but the next rebase should re-run the crash-class check after it.
  - **#11667 AiCardMemory refactor** reaches into forge-game (`TriggerHandler`, `AbilityManaPart`, `PlaySpellAbility`, `Player`) — game path, forkcheck territory.
  - **Pool intersection: 2 of 46 changed card scripts are in cf2ca6ba.** *Emrakul, the World Anew*: protection line corrected (`Spell.ThisTurnCast` → all spells) — a real rules-behavior change. *Hogaak, Arisen Necropolis*: rescripted from the `ManaRestriction$ None` alt-cost hack to a proper `S:Mode$ ManaRestriction` static (#11700) — **payment-surface relevant** (convoke+delve, adjacent to the M10 forced family); at the next boundary, payment enumeration on Hogaak windows should be spot-checked.
  - ~12 heuristic-AI behavior commits (PumpAi timing ×2, ritual/sacrifice/land counting fixes, reveal-count, Embargo/Donate, cloning, token copies, storage lands, ComputerUtilMana NPE) — opponent/teacher strength drift, absorbed at the boundary.
  - **#11723 DeltaSyncManager**: keeps the game running when delta collection hits a ConcurrentModificationException (retry walk, ship partial, skip checksum) — playable-branch relevant, rides the next upstream sync of `playable`.
  - Rest is editions/translations/adventure-mode UI/mobile rendering — irrelevant to research.
- **Our upstream PR status**: #11285 (determinism hooks) OPEN, stalled since our 07-20 reply to Hanmac's `InheritableThreadLocal` question — no review since. **Verified NOT merged separately**: upstream `MyRandom.java` still plain static SecureRandom (untouched since before the PR), `Match.java` last changed by pre-PR June commits, and no per-Game random exists in `Game.java`. #11285 is the only copy of that work. #11457 (chat rate limiting) OPEN — user: deliberately parked as low priority, don't chase.

## Broke / surprised me

- Nothing broke (read-only). Surprise: upstream already rescripted a pool card's cost-payment representation (Hogaak) while our M10 payment work is mid-flight — a concrete example of why per-era re-mints and boundary discipline matter for the payment label stack.

## Next session picks up

- Nothing scheduled from this session. Items filed for the **next dataset-boundary rebase checklist**: (1) forkcheck proof mandatory — multiple game-path touches in range; (2) re-run the copy+resume crash-class check after the #11654 GameCopier change; (3) spot-check Emrakul/Hogaak behavior + Hogaak payment enumeration; (4) playable sync picks up #11723.
- Optional queue item: nudge #11285 — answer is with Hanmac; Manabrew's interest (and possible lazy-loading PR) is leverage for the `game.getRandom()` direction.
