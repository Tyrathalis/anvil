# 2026-09-07 (session 2) — Build 3 evening 2 scoped: the mode enumerator, the keyed pool, the distillation join

**Doc status:** devlog (session record). Plan: [m12-plan.md](../design/m12-plan.md). ADR: [ADR-0105](../decisions/ADR-0105-m12-build3-decision-surfaces-and-ability-representation.md). Session 1: [2026-09-07](2026-09-07.md).

## State review (evening)

Box quiet, tree clean at `0db92ca`, fork at pin `1ac2ec8dc0b`. Evening 1 closed; the label run's
pool ingested. Read for evening 2 (mode):

- **The enumerator gaps account for every mode miss** (2,519 of 12,881 mode answers; label run
  sub rows characterized by `(n, min, max, |natural|)`): 858 windows of "choose three, repeats
  allowed" at n 2 → 2 misses each (no multiset path: a size-2 and a size-1 answer); 266 at n 3
  → the add-one neighbour of an empty natural is size 1; 536 "choose exactly two" windows with an
  empty natural → the same size-1 neighbour. The `SearchDirective.Surface` record traced on
  copies carries no repeat flag, so the fix threads the flag from the callback through the trace
  into the sub row. Four pool cards allow repeats (Eldrazi / Fiery / Mystic Confluence, Season of
  Loss).
- **Finding: the heuristic answers no modes in roughly half of all traced mode windows** — all
  but 12 of 1,136 Confluence windows and all 120 Jitte windows resolve with an empty choice
  (`CharmAi` resets the chosen list, `chooseMultipleOptionsAi` finds no `AILogic$Good` mode, the
  spell resolves doing nothing). A modal spell the network casts frequently fizzles by the
  heuristic's hand; the leak is shared by every network arm in the Build 2 reads. The mode head
  addresses it directly — the first evening whose paired read has a plausible real strength
  effect, not only a "nothing broke" read.
- **No store carries ability keys.** The Build 2 arms and the label run both predate
  `b0567608938`, so the modes of one card are indistinguishable to the model (host + kind). The
  mode imitation and the distillation both need a new pool on the proven jar.
- **The distillation term has no loader, and the join is the design gap.** A sub row lives on the
  mainline window's search row; its surface happens on a copy after the forced option resolves.
  Joining to a mainline surface frame works only when the mainline applied the same option:
  mode 8% of sub rows, entity_one 19%, entity_set 17%.

## Decisions (user, this session)

1. **The distillation state = the copy's surface-window frame captured into the sub row** at the
   directive match (the `peekPriority` pure-snapshot pattern; ~26 frames/game), not the parent
   mainline frame and not the 8% mainline join. Rides the enumerator commit and its forkcheck;
   lands before the keyed pool is generated.
2. **The keyed label run regenerates tonight** (1,000 games) on the evening-1 ckpt with the entity
   surfaces served — the behavior policy of record, trunk frozen at day zero — launched alongside
   the forkcheck off a snapshot jar (write while arms fly, prove after).
3. **The mode serve wire adds a repeat flag** to the bridge's set constraints (proto3 bool,
   backward compatible); Java validates distinctness from the callback's flag.
4. **Enumerator scope**: multisets under repeat, every emitted size inside [min, max]; the
   natural's size ± 1 stays the only size neighbourhood when min < max.
5. **Small routed items ride tonight**: the `mtg.mulligan_tuck` serve mapping, the orchestrator
   raising the worker deadline itself when `-searchsurf` is armed, enumerator tests.

Order: fork commit (enumerator + repeat flag + sub-row frames + `mtg.surface.mode`) → tests +
8-game smoke → forkcheck ∥ the keyed label run overnight → the Python side written while they fly
(`surf_mode` loader with repeats, the decoder's repeat mask, the server's mode answer, the
distillation loader + term) → fits and the paired read tomorrow.

## Community watch (Discord, 09-07) — the first external Anvil run

**Kryptic** ran Anvil end-to-end on a mono-green stompy mirror (his own `.dck`, `--format
Constructed`, a custom `--pool-version`), the deck he had tried on Austinio's pipeline (BC on
4,000 games, then 200 PPO rounds → 30–35%, flat). On Anvil with the same 4,000 BC games
(`--batch 32 --lr 3e-4 --warmup 500 --steps 200000 --pass-weight 0.1`): **BC 38.9% vs the
heuristic** (2,000 games, se ±1.1) — already at or above his previous pipeline's best — then
`anvil.training.selfplay` (25 iterations × 480 games, `--heur-frac 0.5 --reask --penalty 0.01
--penalty-grouping first --chunk 10 --guard-veto-mult 4.0 --lr 1e-5 --rl-seg 64 --traj-per-step 4
--epochs 1`, arms every 5): **55.2% after 25 iterations** (1,000 games, se ±1.6). Monitor
attached to his post: reward 0.44 → 0.52 and v0 0.43 → 0.54 climbing; entropy 0.27 → 0.18
monotone; kl_mu 0.006 → 0.011; veto rate 0.015 → 0.038 (2.5× iter 0, under his 4× guard);
rejected/traj 0.24 → 0.49; casts/game 19 → 16; pay deviation 0.41 → 0.30; train_s ~290 / gen_s
~1,400 per iteration. The first independent replication of the Anvil loop on a different format
and pool, and the first external read that the BC → V-trace spine beats the PPO pipelines the
channel had plateaued on. Not Anvil-changing: no action; noted in the map and the survey doc.
Watch items his monitor shows (ours to mention gently, not his to fix): the entropy slope and the
rejected-intent climb are the ADR-0017 / ADR-0072 triplines — the M9 finding is that veto
collapse was not the strength mechanism, so the numbers are informational unless the entropy
floor is reached; his 1,000-game arm read is sound (se ±1.6, decisive 995).

**LordOfThePigs** (earlier 09-07, banked in [session 1 / session 6](2026-09-06-session6.md)):
effect-grounded embeddings as his new basis; routed as a Build 4 fork.

### Draft replies (the user posts; nothing posted from here)

To Kryptic:

> Not "nothing groundbreaking" at all — this is the first time anyone but me has run the whole
> Anvil loop, on a format and a deck I never touched, and it landed where the design says it
> should: BC at ~39% where the PPO pipelines plateaued, then the self-play spine carrying it past
> the heuristic (55.2% at n=1,000, se 1.6 is a clean read). Thank you for the detailed settings —
> that command line is going straight into the docs as the reference Constructed recipe. Two
> things from your monitor, both informational: the entropy slope (0.27 → 0.18) and the
> rejected-intent climb (0.24 → 0.49/traj) are the two curves the guards watch; our M9 read was
> that a rising veto rate is not by itself the strength mechanism, so I would keep going and let
> the 2,000-game read every 5–10 iterations be the judge. If you feel like it, `final_read.py` is
> the paired 2,000-game protocol I use for anything I would quote. Very glad you shared it.

To LordOfThePigs:

> The effect-grounded embedding direction is the one I keep coming back to as the strongest
> challenge to what Anvil does. For what it is worth, this week Anvil pinned its ability
> representation as the LLM embedding of the ability's *canonical engine text* (the script
> parameters plus the rules text, hash-keyed), and a frozen linear probe found every effect class
> — mana produced, zone moved, damage, counters, draw — decodable from it at AUC 0.98–1.00 versus
> 0.80–0.99 from the description alone. So the engine text carries the effect; whether the
> *observed* effect carries more is exactly your experiment, and I would read your results with
> real interest. Please post them when they land, negative or positive.

## Next session picks up

- The fork commit for evening 2 (enumerator fix + repeat flag + sub-row frames + `mtg.surface.mode`
  + the proto repeat flag), tests, smoke, forkcheck ∥ the keyed label run.
- The Python side while they fly; fits + paired read tomorrow.
- Kryptic's Constructed recipe → a docs reference (the format-scope memory: recipe first).
