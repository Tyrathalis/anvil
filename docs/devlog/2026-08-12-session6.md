# 2026-08-12 session 6 — smoke CLEAN (two bugs fixed), selection screened, d6-run14 LAUNCH-READY

## Done

- **Bundle smoke read (d6-bundle-smoke, 1 iteration, overlap ON at 16/16):**
  160 games (159 decisive, 1 timeout = 0.6%, historical band), zero guard
  trips, one tripwire violation (single trajectory dropped, bf16-tail
  class). Overlap worked — both tracks interleaved; campaign_s 1590 vs
  gen_s 815 (campaign-bound as designed). **Label→window join 7/7 (100%)**,
  all specific-cast targets at 1.0 modal agreement, 16/16 triples, zero arm
  crashes. w_seq calibrated (0.00329; full inputs in
  `seq_calibration.json`). C2a aux loaded + trained. Battery fired
  end-to-end incl. the behavioral delta (exploratory: 6.3% cast-changed /
  84% to-pass at tiny KL — marginal flips; the run's curve is the real
  read). Critic era-health check rode along: baseline AUC 0.871 on
  current-era windows.
- **Two design bugs found by the smoke, FIXED (`6916d03`):**
  (1) **w_seq recalibrated per rl.py invocation** — every iteration's first
  50 optimizer steps (~28% of an iteration) would have run with the seq
  term silently off; now iteration 0 calibrates, `loop_state` carries the
  value, `--seq-w` skips (ADR-0054's calibrate-at-run-start realized).
  (2) finetune_value's aux logging off-by-one — post-increment %200 never
  coincides with %10 aux steps; 49 aux steps trained silently in the
  smoke; now logs every aux step.
- **Selection screened for model-seat-activity** (the ADR-0054 selection
  rule): K=1/N=1 forced-seq pass over all 217 points (~15 min) + new
  standing `scripts/filter_active_points.py` → **`drill-selection-v5-active`
  = 99 active points (46% activity; 116 seat_skips)** — the campaign's
  P=99 ≈ the ADR's P≈100 exactly, all points fresh every iteration.
- **w-bench conclusions folded into the recipe:** single-fleet peak w=24
  (+14% over 16), w=32 regresses ⇒ the run splits gen w=8 (what run11/13
  ACTUALLY ran — both configs say workers 8; the ADR-0032 w=16 adoption
  never reached a gated run's launch args) + campaign w=16 = 24 total at
  the knee. `--campaign-workers` added for the split.
- **watchd false-GONE explained (user caught it):** the seq-screen alert
  was the session-side unregister race — the process exited cleanly and
  watchd's 10-min tick landed before my unregister. Not a watchd bug; the
  fix is the wrapper-unregisters-itself pattern (memory updated). The
  selfplay driver self-unregisters, so d6-run14 is safe by construction.
- **`scripts/launch_d6_run14.sh` = the approved launch command of record**
  (base run13 verbatim; deltas = bundle + orchestration; flagged for the
  user and approved: gen w=8, drill-eval every 5 on evalset v4 — an
  addition over run13, advisory-only — and fresh seed_base 20260813).
  Estimate ~2h05m/iter → ~42h ≈ 1.75 days.

## Next session picks up — LAUNCH d6-run14 (fresh session)

1. `setsid nohup nice -n 19 bash scripts/launch_d6_run14.sh >> data/training/d6-run14-driver.log 2>&1 < /dev/null &`
   (the driver self-registers watchd + line-buffers; PYTHONUNBUFFERED via
   the driver's own reconfigure).
2. Arm at launch, same action: persistent log monitor on the driver log
   (`GUARD HALT|ANOMALY|battery anomalies|Traceback|DRIVER CRASHED`) +
   periodic telemetry glances (babysit rule; battery per-iteration rows +
   `analysis/monitor.png` are the glance surface; watch iteration-0's
   `w_seq calibrated at run start` line and the first `campaign_s` for the
   real interference number).
3. Enumerate the armed coverage in the launch message (the checklist).
4. Mid-run watch items: seq join counts per iteration (labels ≥ ~90/99),
   act_first_agree distribution, holding trajectory (the ADR-0049 axis —
   movement is the hoped-for event), drill-eval decomposition at iters
   5/10/15/20, kl_mu shape under λ=0.01.
5. At close: the standing combined paired read vs **0.5373 ± 0.0112**
   (final_read; battery eval read rides), then the M7 done-when-4
   closeout ADR with the credit-assignment verdict.

## State of the tree

Committed through `6916d03` + this commit; suite 159 green; no
uncommitted work. Background jobs: none running (map chain, baseline
eval, w-bench, screening all complete + unregistered). The box is free
for the run.
