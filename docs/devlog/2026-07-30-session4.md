# 2026-07-30 (session 4)

## Done

- **v7 published: the playable fork's daily-snapshots release now serves the
  consolidated `playable` branch** (`origin/playable` @ `2900701083` = QoL +
  updater + current upstream master + the security stack). This is the publish
  that ships the multiplayer-hardening work to friends' auto-updating clients
  — the 3 upstream-merged advisories arrive upstream-side, the #11457 chat
  limits and both declined patches ride fork-side.
- Build → `scripts/release-playable.sh` → `gh release upload --clobber`, all
  from the `../forge-play` worktree (already on the consolidated tip, clean).
  Version `2.0.14-SNAPSHOT-07.31` (build stamp is UTC), manifest 54,727 files.
  Jar grew 69→84 MB — the upstream-master merge brings months of new editions
  the old `playable-pub` cherry-pick base didn't carry; the delta prompt shows
  clients the real size, as designed.
- **Full post-publish routine run (the v4 lesson, exercised end-to-end):**
  re-downloaded the published jar — sha256 matches both the manifest entry and
  the local build; parsed the published manifest through the published jar's
  own `DeltaManifest` via jshell (54,727 entries, correct version/commit, and
  a real `wastetown..tmx` double-dot entry resolves); raw res fetch at the
  manifest commit hash-matches its manifest line; `commits/playable.atom` 200.
- Docs swept in the same batch: worklist publish log gains the v7 entry (and
  the "patches are not in the published build" note resolved), CLAUDE.md
  status + project-map playable card updated. First release cut from
  `playable` directly — `playable-pub` era over, branch-split note retired.

## Broke / surprised me

- **v7 was dead on arrival in the field.** The first real update attempt (the
  user's own install) aborted with `HTTP 400 for .../res/adventure/Realm of
  Legends/decks/legends/nicol_bolas.dck` — the delta URL builder concatenated
  paths into URLs without percent-encoding, and the res tree carries 30K+
  paths with spaces (plus `#` `'` brackets `&` `!` unicode and a literal `%`).
  Every v1–v6 delta happened to touch only URL-safe paths; the v7 delta (months
  of upstream res churn) was the first big enough to hit the class. Exactly the
  v4 `..`-substring incident's sibling: a property the artifact always had,
  unexercised until a real plan touched it. My post-publish routine verified
  manifest *parsing* through the shipped jar but not URL *construction* — the
  gap is now closed (the routine fetches the production-built URL for a
  space path live and hash-matches it).
- Nothing was half-applied anywhere: downloads abort before any apply, so all
  clients sat safely on ≤07.29 with a "failed to download" dialog.
- **v8 published same evening (fork `09974ab3ea`) with the fix and a fully
  automatic migration:** (1) segment-wise `DeltaUpdater.encodePath` — pinned
  by a new HTTP end-to-end test with hostile filenames, validated failing
  first on the exact field path (a real HttpServer rejects raw spaces like
  GitHub; the existing file:// e2e was too lenient to ever catch this);
  (2) bridge manifest — a fix to the updater cannot ship through the updater
  it fixes, so plain entries list ONLY the jar (release asset, URL-safe — the
  one plan ≤07.29 parsers can always complete) and the full 54,727-entry list
  rides on `#2 ` lines legacy parsers skip as unknown future headers;
  (3) res self-heal — after the jar-only hop the local build EQUALS the
  published one, so the strictly-newer gate would never re-open and res would
  silently stay stale under the new jar; on equal builds with no sync stamp, a
  size-only diff (no speculative 2GB hash pass; same-size content changes
  self-correct at the next release's hash-verified delta) offers to complete
  the update, stamp written after any successful apply. DeltaUpdateTest
  10/10 (3 new). Pre-publish gate extended: the bridge manifest parsed with
  the user's actual shipped 07.29 jar (1 entry, jar only) AND the v8 jar
  (54,727 entries); the encoded URL for the failing path fetched live and
  hash-matched. Post-publish routine re-run in full on the published assets.

- **The live two-hop pass RAN and PASSED (user's 07.29 install):** hop 1 jar
  swap + applier auto-restart clean (journal: old jar 18:25:20 → new jar
  18:25:30), hop 2 self-heal found and completed the stale res, stamp
  written; install verified fully consistent (jar sha = published, stamp =
  published build.txt). **URL-encoding incident closed.**
- **Its one wart unmasked a second stock bug, same defect class one layer
  down:** after the res-only apply the app exited without reopening. Stock
  `RestartUtil` builds one command *string* for `Runtime.exec(String)` —
  whitespace tokenization, no quoting: the quoted java binary never exec'd
  on Linux, the ".../Forge Fork/" install path splits mid-path, and the
  failure is swallowed inside a shutdown hook. Stock desktop restart likely
  never relaunched for anyone; hop 1 worked only because our UpdateApplier
  already used a ProcessBuilder argument list. Rebuilt on ProcessBuilder
  (`57e345f922`): jar path from the code source (the one space-safe source),
  trailing args recovered past the jar filename, unreconstructible commands
  refuse instead of relaunching garbage. RestartUtilTest 4 new; suite 14/14.
  **v9 published same night** (full gates: legacy parse now runs against the
  archived v7 jar since the user's install is v8; encoded-URL live fetch
  hash-matched) — friends' hop 2 relaunches correctly because the fixed jar
  lands before their res pass runs. Upstream candidate queued
  (upstream-worklist: "RestartUtil never relaunches").

- **v9 update user-verified, then a third find, fixed and shipped as v10
  (`68f48b77f7`): the resize-layer census gains member six — HEADERS.**
  Field report at a half-ultrawide (≈square) tile: New Game showed a
  one-item "Constructed" bar at the top, no other mode selectable until the
  window got wider. Diagnosis: `FScreen.doLayout` chooses top-bar vs
  sidebar from the HOSTED screen's `width > height` (the home sidebar eats
  `0.35×height` of the window width), while `MenuHeader` re-derived the
  mode from the global `Forge.isLandscapeMode()` — in the disagreement
  zone the popup menu is painted as a "sidebar" into the top header strip,
  one item high. Unreachable in stock (fixed launch size); T1's resizable
  window opened the zone. Fix: `Header.sidebarLayout` flag records which
  layout FScreen actually chose; `MenuHeader` and `TabHeader`/`TabPage`
  key every sidebar branch on it — covering the same latent defect in
  Settings and Load Game (TabPageScreen/MenuHeader over the home backdrop,
  same zone) and making bottom tab strips horizontal by construction.
  Android behavior unchanged (global and chosen layout always agree
  there). Not verifiable by reading (no mobile test infra; XTEST wall) —
  the standing lesson applies; publish gates all green.

- **v10 menu fix user-verified. The first Quest-mode session then found a
  STOCK quest bug — fixed and shipped as v11 (`692d166633`).** Report: new
  quest says "make a deck," editor offers no cards. The save's whole pool
  was the automatic snow-basics grant — starter generation contributed
  zero. Hunt: headless jshell probes (GuiDesktop + FModel against the
  research pin, the playable build, and the live install's res) showed
  every default configuration generating 150–290 cards; the journal's
  `QuestDataIO` reflective-mutation warning (a LOAD-path signature) dated
  the quest to an earlier session and killed a wrong "created-just-now"
  assumption; the user's exact settings closed it: **BALANCED distribution
  with ALL SIX colors selected → 0 cards, silently** (any five → 244).
  Cause: `populateBalancedFilters` uses the NON-selected colors as the
  repetition multiplier for preferred-color filters — all-colors leaves it
  empty, both loop branches build nothing, and `generateCards` runs out of
  misses without a word. Floor-at-one fix; `QuestStartingPoolTest` rides
  `AITest`'s card-DB init (all-colors validated failing-first, two
  controls, 3/3 post-fix). Upstream candidate queued (clean standalone
  patch + test). Also noted, unfixed: Boosters distribution accepts count
  0 (silent empty pool), and a world with its own format silently
  overrides the whole starting-pool section (stock UX trap).

## Next session picks up

- User updates to v11 (routine jar hop), deletes the empty "Tyrathalis"
  quest, recreates with the same settings — should get a ~330-card pool
  (Commander-doubled). That run is the live verification.
- Playable track residue otherwise unchanged: T2 (queued; workaround = launch
  at play size) + incidental play checks. Friend clients migrate to the
  consolidated build automatically, fully hands-off.
- Research side: d6-run10 (drill-mixed, 20 iters ≈ 14 h) should be finishing —
  paired 2,000-game corrected read vs 0.5121 is the gate, plus post-hoc drill
  evalset eval per candidate ckpt.
