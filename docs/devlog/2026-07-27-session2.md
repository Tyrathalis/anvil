# 2026-07-27 session 2 — QoL item 5 step 1: the hit-tests stop assuming 90°

Second QoL build session, picking up exactly where the morning session's
"next" pointed: item 5 step 1, hit-test generalization at an unchanged tap
angle, with the geometry-unit-test shape instead of screenshots.

Branch hygiene held: the Anvil checkout was (still) on the security track's
branch; all code went to `../forge-play` (`playable-qol`), all docs to `main`
via a scratchpad worktree, and nothing was pulled over from
`security/mp-hardening` (its worktree untouched, its extra commit not
cherry-picked).

## The two open decisions, resolved

- **Where the helper lives: `forge-gui`, shared.** A static pure-geometry
  class (`forge.util.RotatedRect`: `contains` + `boundingBox`) with zero
  libGDX/Swing imports. That dissolved the "add test infra to
  forge-gui-mobile?" question — the test (`RotatedRectTest`, 5 TestNG
  methods) sits in `forge-gui-desktop`'s existing tree, which already depends
  on `forge-gui`. Desktop's own hit-test round later reuses the same class.
- **Angle convention:** the helper speaks mobile's `startRotateTransform`
  convention (stock tap = −90); Swing callers will pass the negated
  `Graphics2D.rotate` angle in degrees — which is *also* −90 for stock tap,
  so the conventions coincide exactly where it matters.

## The identity gate, run honestly

- Hit-test: **bit-identical to the legacy swap box at −90 over ~60M grid
  points** (4 geometries including awkward floats, zero mismatches). Trig is
  snapped exactly at multiples of 90 to make that achievable.
- Bounding box (feeds only the arrow anchor): identical to ~1e-5 — the noise
  floor is call-site float pivot rounding, measured by the test's first
  failure (`2.0000038` vs `2.0` under a `delta=0` assert) and then asserted
  at `1e-4` *with the reason written down*. The strict claim stays on the
  hit-test, where a flipped comparison could matter.
- Arbitrary angles are checked against a double-precision forward-transform
  oracle (inside/outside lattices with a boundary band excluded), so the
  helper is already known-correct for the future configurable angle, not
  just at ±90.

## The derivation found a real bug before any angle became configurable

Working out the composed transform for **180-rotated fields** (local
two-human non-hotseat matches — precisely the Commander-night mode) showed
the current hit-box is provably misaligned: `CardAreaPanel.draw` wraps cards
in an outer 180° rotation about the untapped rect center and flips
`getTappedAngle()` to +90, but the angle-blind hit-test kept the −90 swap
box. Net: **the drawn tapped card sits offset by h−w from where taps are
accepted** — the top ~40% of the drawn card misses, and a band of empty
space below it hits. Input coordinates never see draw transforms, so nothing
downstream compensates.

Fix: `CardAreaPanel.renderedCardContains` undoes the outer 180 (a point
reflection about the center — identity for untapped cards) before the base
test; targeting arrows had the same offset and now anchor to the drawn box.
This is a *deliberate* behavior change in that mode, documented as such in
the commits; the default orientation is covered by the bit-identity gate.

## Landed

- `playable-qol` `d3bd019dc6` (RotatedRect + RotatedRectTest), `d52a43e6aa`
  (hit-test wiring + rotate-180 fix), `514efc0dc7` (arrow anchors).
- All 5 geometry tests green; `forge-gui-mobile` compiles; the
  `forge-gui-mobile-dev` shipping jar builds.
- Worklist item 5 updated in place (step-1 record + the rotate-180 finding).

## Owed / next on the track

- **Human pass:** tapped-card taps + targeting arrows in a real local
  two-human match (the rotate-180 fix is formula-verified only; same XTEST
  wall as items 4/6). Normal matches need no re-verification beyond ordinary
  play — behavior there is bit-identical by test.
- **Item 5 step 2:** route the angle through one accessor per client; kill
  the `CardUnTapAnimation:288` hardcoded `-90`.
- Desktop `PlayArea.getCardPanel` generalization (same helper, negated-angle
  convention note in the worklist).
- Standing non-QoL next items unchanged: M3 closeout decision, Grindstone/M4
  design session, collate refactor before the next long RL run.

---

## Continued same session: steps 2+3 and the desktop hit-test — item 5 BUILT

The session carried straight on and closed the rest of item 5. Three more
commits on `playable-qol`:

- **`484fdb3df5` (step 2):** the untap animation delegates to
  `getTappedAngle()` — the `:288` hardcode was also wrong on 180-rotated
  fields (swept from the wrong side), so this was a fix twice over.
- **`ac824cc606` (desktop hit-test):** `PlayArea.getCardPanel` inverse-rotates
  via a new `RotatedRect.inverseRotate`, which returns the local point so the
  caller keeps its exclusive-bounds comparisons (mobile's are inclusive — the
  helper doesn't impose a convention). New desktop-form identity test over
  full integer pixel grids: zero mismatches. `isBadgeHit` already did this
  and needed nothing.
- **`33d8c64b41` (step 3):** `FPref.UI_TAP_ANGLE`, default `"90"`, values
  90/75/60/45, localized in nine languages. Mobile: cached `Forge.tapAngle`
  (the `animatedCardTapUntap` pattern), defensive parse, SettingsPage +
  adventure SettingsScene. Desktop: `TAPPED_ANGLE` became a lazily-read
  value with **degrees as the source of truth** — `toDegrees(toRadians(90))`
  is not guaranteed to be exactly 90, so storing degrees keeps the snapped
  hit-test math exact; radians are derived for paint. The PlayArea
  in-flight-animation guards read the same accessor.

Verification: geometry tests 6/6, full desktop suite **336 green**, mobile
compiles, shipping jar builds.

**Step 4's draw-order question, answered statically:** mobile draws children
forward (rows add left→right), so a tapped card's overhang lands UNDER its
right neighbour — correct table look by construction. **Desktop is
inverted** (Swing paints lower-index children on top). Fixing that is
z-order surgery that interacts with the deliberate left-on-top stack
fanning — recorded in the worklist to assess visually when the desktop tilt
is actually played, not engineered blind. Unreachable at the default 90°.

Upstream entry updated in sync (build status + what the pitch can now say;
hit-test fix to be offered separately from the preference, Discord first).

## Owed after this session

- **Mobile play pass at 60°**: legibility, tapping the drawn face, arrows,
  animation endpoints, overhang direction. This is the one that matters for
  the Commander night.
- The rotate-180 two-human check from step 1 stands.
- Desktop shallow-angle look (overlap direction) whenever desktop is played.

---

## Live test round (user at the wheel)

The fork was launched for a visual pass. **Verdict on the angles: verified in
real use** — "moving correctly and look sharp even rotated." Two follow-ups
came out of it, both landed the same hour:

- **The mode-selector overlap is fixed (`4ad8b4c94b`).** The 07-26 "left
  alone" call didn't survive contact: the resizable window's launch-time
  restore made the stale-button-bounds overlap show on every boot. Bounds
  math moved into `layoutSelectorButtons()`, wired to the previously-empty
  `doLayout`; skipped during the exit animation.
- **Angle list extended with 30 and 15 (`255df64491`)** — play feedback: the
  friends want genuinely shallow tilts and 45 wasn't shallow enough. Parse
  range already covered (0, 90]; three settings lists touched. At 15–30° the
  drawn footprint is *narrower* than the 90° row reservation, so overlap
  consequences are milder than at 45–60.

Ops note for the record: `pkill -f` on the jar name self-matched the tool
shell's own command line and killed it (the known zsh/-f footgun — memory
note exists); the `[s]`-bracket pattern trick is the fix. The relaunch after
rebuild went out detached with a clean log.

---

## Second live-feedback round: tighter tapped rows, single-column hand

Two more play-driven changes, both landed and running (`2e8cbd49b7`,
`02b57b43aa`):

- **Battlefield slots pack to the configured angle.** `FieldRow` gave every
  card a square slot — "room to tap" sized for exactly 90°. At 30/15° that
  room is mostly air. New pitch: `w/cos(angle)`, capped at the historical
  square — 90/75/60/45 lay out exactly as before, 30/15 pull the row
  together; tapped-beside-untapped may overhang slightly, drawn under its
  right neighbour (the user pre-accepted the overlap). Ripple handled: slots
  narrower than the square break the `w==h` card-rect special case, so the
  five derivation sites clamp to aspect instead. One knowing sub-pixel-class
  change: draw() clamps before the hover-enlargement mod (~2% wider hovered
  battlefield cards) — clamping after would shrink hover growth in hand
  zones, a real regression.
- **`UI_SINGLE_COLUMN_ZONE_DISPLAY`** (default off, nine languages, both
  settings surfaces): the landscape side zone display halves in width and
  lays out one column — same card size, battlefield gains the room. The
  overlap-before-scroll window scales with columns so the same card count
  fits. Applies to all vertical side zones so the panel width doesn't jump
  when switching tabs (the user asked about the hand; uniform is the less
  jarring generalization — flag if they want it hand-only).

Ops lesson, sharpened from this round: the `pkill -f` self-match hits even
with the `[s]`-bracket trick **if the same compound command later contains
the literal string** (the relaunch path did). Kill and relaunch must be
separate tool calls. Memory note updated.

Owed: play pass on both (tapped-row density at 30/15 with mixed
tapped/untapped, and the one-column hand incl. a large hand's
overlap-fan/scroll behavior).
