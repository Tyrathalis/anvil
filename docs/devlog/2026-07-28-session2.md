# 2026-07-28 — session 2

## Done

- **M3 CLOSED ([ADR-0026](../decisions/ADR-0026-m3-closeout.md)).** The
  closeout ADR lands the milestone formally: all four done-when clauses
  resolved (ADR-0022 veto cause / ADR-0023 first outside-noise supersession /
  D3 upstream surface shipped / ADR-0025 rebase), checkpoints of record
  unchanged (RL `d6-run7b/iter-014` at 0.5121 corrected post-rebase, BC
  `d5-combat` for engine-bump certs), and the honest headline recorded: the
  platform moved under the measurement — strength was proven against the old
  heuristic, then re-scaled by the rebase to parity against the new one,
  with RL-over-BC intact at +6.69pp (t=4.32).
- The ADR pins the **M4 design-session agenda**: signal source (Grindstone
  drills vs rollout labels vs post-rebase re-probes — the ADR-0024 "absent
  signal" argument), the re-baseline mechanism question
  (heuristic-got-stronger vs opponent-specific fit, deliberately unresolved),
  milestone identity, and which old-scale analyses merit re-running. Hard
  prerequisite before the next long RL run: the collate refactor.
- Doc sweep in the same batch: `m3-plan.md` gains the closed banner +
  strikethroughs on done-whens #2–#4; CLAUDE.md M3 bullet re-headed to
  CLOSED with the M4 seeds up front; project map updated (pills, Grindstone
  card → M4 candidate, M3 details closed, ADR index row 0026, footer).
- Session opened with a state review: confirmed #11285 still OPEN with zero
  reviews (untouched since 07-20) — the queued #11360 complementarity
  comment is the natural nudge vehicle, not yet sent.

## Broke / surprised me

- Nothing broke; docs-only session. Diff audited before commit per standing
  practice.

## Next session picks up

- **The Grindstone/M4 design session** (fresh context), agenda per ADR-0026.
- Before the next long RL run: the collate refactor (byte-identical
  equivalence gate available by construction).
- Independent side items, any time: the #11360 complementarity comment
  (doubles as the #11285 review nudge); `final_read.py` riders; the
  MinMaxBlocker realizer gap; the `dc-863943` IndexOutOfBounds replay;
  pool-manifest mtime hazard.
