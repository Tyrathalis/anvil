# 2026-08-21 (session 3) — the Arena-of-Glory salvage family diagnosed and fixed (ADR-0067)

Parallel-session note: ran concurrently with the evalset-merge session
(which held certify3 job 202's drill out pending this diagnosis) and
received the job-202 single-window repro from it mid-session via
cross-session message. This session owned the forge-m9pay worktree.

## Done

- **The certify2/3 `costs:Arena of Glory` salvage family diagnosed:
  enumerator over-admission in `chainOrderFeasible` — three gaps of
  one genre ([ADR-0067](../decisions/ADR-0067-certify-salvage-chain-color-feasibility.md)),
  falsifying ADR-0066's "chainOrderFeasible NOT implicated" line.**
  The old check was (1) count-based and color-blind (a Plains float
  "covered" a {R} activation cost), (2) allowed an atom's OWN units to
  pay its own activation shard (mana that doesn't exist yet), and
  (3) solved over color MASKS while the executor expresses each unit
  at its single materialized plan color (a Shivan Reef U|R unit
  committed to U cannot pay {R}).
- Chase: Exert ruled innocent (pays-from-source) → `payComputerCosts`
  re-entry traced (`CostPartMana.payAsDecided` → controller →
  `ComputerUtilMana`, pool-first) → minimal boards. Mountain + Arena
  vs {R}{R} PASSES pre-fix (fungible red float rescues self-payment —
  why the defect was census-rare yet per-plan deterministic);
  Plains + Arena vs {1}{W} FAILS pre-fix with the exact census
  signature `costs:Arena of Glory#2@1`.
- **Fix (fork `m9-payment-surface`): `chainOrderFeasible` replays the
  executor's exact activation order** — shared `EXEC_ORDER` comparator
  (one artifact, the layers can't drift), exact small backtracking
  over materialized unit colors with temporal limits (a chained atom's
  activation cost payable only from initial pool + strictly-earlier
  atoms' yields, fungible), main cost from the remainder,
  snow/phyrexian honored, 20k budget/plan with admit-on-exhaustion
  (never silently censor).
- **Diagnosis channel extended: salvage rows now carry `plan`**
  (executor-order atom dump, `PaymentEnumerator.describePlan` →
  `PayDirective.planDesc` → certify row). Gap 3 was found from ONE
  such row after the first re-adjudication pass left job 202 arm 7
  still salvaging — without the dump it would have been another blind
  family.
- Regression tests: `testColorStarvedChainNotAdmitted` (census
  signature; failed pre-fix) + `testExertCostedManaAbilityChainExecutes`
  (the legitimate exert chain stays admitted — over-tightening guard).
  Fork payment suite 20/20 (enumerator 9 / wiring 4 / certify 3 /
  audit 4).
- **Re-adjudication (all five affected jobs, fixed jar): salvage
  0.0000.** certify2 227/358/382/470: formerly-salvaging arms →
  `directed_ok` where a real composition existed (227 arm 1), phantom
  options GONE elsewhere (option counts 358: 2→1, 382: 3→2, 470:
  2→1 → those arms resolve `no_such_option`). certify3 job 202: **all
  7 arms `directed_ok` 8/8, all 7 options intact** — arm 7's goal
  re-materialized with the Reef unit expressed red; the held drill's
  directed arm is REAL, not phantom (→ evalset re-adjudication).

## Broke / surprised me

- **The first fix pass was incomplete** — gaps 1–2 cleared the four
  certify2 jobs but job 202 arm 7 still salvaged 8/8 with the same
  exec_why. The plan dump made the residual a one-row read. Lesson
  re-learned from ADR-0066: extend the diagnosis channel BEFORE
  re-adjudicating, not after.
- Verbatim-jobs replay needed the lane-format jobs file (planner rows
  with shape/source/tags are rejected by `-certify`).
- The test-suite runner needs
  `-Dsurefire.failIfNoSpecifiedTests=false` with `-am` (forge-core has
  no matching tests); the `windows-linux` profile doesn't exist in
  this worktree's pom (plain `mvn -pl forge-gui-desktop -am` works).

## Next session picks up

- **Job 202 re-adjudication result → evalset session** (held drill in
  `data/runs/payment-evalset-v1/held-drills.jsonl`; merge subcommand
  re-derives suspects).
- **Baseline staleness routing (ADR-0067 consequence 2): the
  paygoals4-or-accept-the-bound decision goes to the D4 gate session
  BY NAME.** The fix only removes plans; paygoals3's consequential
  15.28/g is now an upper bound.
- Fork commit on `m9-payment-surface` (this session); D4 evalset work
  continues on the merged 56+193 set.
