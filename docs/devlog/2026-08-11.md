# 2026-08-11 (Talor PR merged; performance audit: blocker cache → mask cache → the serving-bound diagnosis)

## Done

- **Fork PR #1 (Talor, AiBlockController block-legality cache) MERGED**
  (`c758be07a6`) after the overnight verification (behavior-identical
  499/500, speedup neutral on our workloads — see 08-10-session3).
- **Improvement audit on the merged cache (user-directed):** decomposed
  `CombatUtil.canBlock` (3-arg = combat-dependent chain ending in the pure
  2-arg pair check; `mustBlockAnAttacker` fans out O(attackers) static
  walks per miss — the hidden quadratic). Shipped the wrapper improvements
  (`e598d20bdd`): pair cache survives combat mutations, 3-arg-true feeds
  pair-true for free, pair-false short-circuits the full walk. Identity
  pair: 499/500 with the ONE mismatch being seed 20260844 — the same seed
  proven same-jar-nondeterministic yesterday. Ceiling measured small
  (~1–2%): post-cache profiling shows canBlock residual at 1.8% of
  in-game CPU on our pool.
- **JFR profiling (2 runs: heuristic-vs-heuristic; both-seats-bridged):**
  the whale is CAST ENUMERATION — `AnvilOptions.priorityOptions` is
  **55.5% of in-game worker CPU** on the loop's generation config
  (ComputerUtilAbility 50.4%, statics/layers 48.6%); blocker paths ~1.5%.
  And 64.3% of priority asks repeat the seat's previous mask verbatim.
- **Mask cache BUILT + equivalence-proven, committed DEFAULT OFF**
  (`4c7d21cd6a`). Obs-diff gate v1 FAILED and the failures are the
  documentation: (i) ability stack entries never enter the stack ZONE —
  key on `MagicStack.size()`; (ii) hidden-zone arrivals (tutor-to-hand)
  do NOT bump `game.getTimestamp()`; (iii) tapped state is
  timestamp-invisible. Key v2 = timestamp + turn + phase + MagicStack +
  landsPlayed + seat content hash (5 zones, id×2+tapped). Gate v2 CLEAN:
  identical outcomes, **3,346/3,346 priority windows byte-identical**
  (masks AND answers), same seeds cache-on vs cache-off.
- **The throughput punchline (why default OFF): generation at w=16 is
  NOT engine-CPU-bound.** Quiet-box genbench with the mask cache:
  1845.1 g/h vs 1867.1 (PR-only) / 1882.5 (pre-PR) — all within noise.
  Wall-clock is bound by ~1,000 serialized bridge round-trips per game
  (each paying the gRPC hop + 3 ms micro-batch window) — worker CPU
  savings vanish into wait time. Empty-mask asks (auto-pass candidates)
  are only 0.6%. ADR-0032's "the ceiling is waiting" now has its
  mechanism quantified from the worker side.
- **New standing instrument: the obs-diff gate** — same-seed A/B runs
  compared window-by-window on the obs stream's recorded masks/answers.
  Caught 3 real staleness bugs in one afternoon; this is the required
  re-enable protocol for the mask cache after any engine rebase.
- Talor reply drafted with corrected framing (his cache is correct and
  effective; our workload is serving-bound and blocker-light) — user
  sends it.

## Broke / surprised me

- First mask-cache genbench read −13% — contaminated by the forkcheck
  pair still grinding on the box. Quiet-box rerun = wash. (Calibrated
  numbers need a quiet box; the standing rule held, I violated it.)
- `jfr print` defaults to stack-depth 5 — always pass `--stack-depth 64`
  before attributing samples.
- The obs-diff gate's v1 failure localized three engine-state-versioning
  gaps in ~20 minutes from first-divergent-window dumps. Strong pattern:
  put the cache's OUTPUT in the recorded stream, and equivalence checking
  becomes exact and cheap.

## Next session picks up

1. **M7 D2 routing decision (user)** — unchanged from 08-10-session3:
   tier-3 search targets vs corrected-population mixtures + C2a vs
   sequence-level contrastive design round. C3 pre-work read queued.
2. **Bottleneck tests, parked deliberately (user: do not interrupt M7):**
   (a) w-scaling retest 16/24/32 — the D4 worker ceiling was set under
   the old CPU profile; (b) adaptive micro-batch window (close early on
   queue drain) — attacks the 3 ms floor under every ask. Either is one
   unattended evening.
3. Cleanup candidates when convenient: fbks*/obsdiff scratch dirs,
   forge-pr1 worktree + pr1-verify/pr-1-review refs, forkcheck pr1-*/
   patch-*/repro-* dirs (keep the two identity-pair results.jsonl sets
   until the ADR-0052 era closes).
