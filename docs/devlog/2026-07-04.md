# 2026-07-04

## Done

- **`PlayerControllerAnvil` landed** (fork `56c96c6c40`, `forge anvil` command): extends `CensusPlayerController` so the census log doubles as the provenance log (`by=bridge` vs implicit heuristic-fallback); M0 tag set (priority, mulligan/tuck, triggers, binary, number) routed through the new `AnvilBridge` seam; `LocalRandomBridge` answers from `MyRandom` (deterministic per seed). Priority materializes engine-legal options (pass + castable spells + land drops); picked spells run through `canPlaySa` to get targets/X pre-set AI-path style, veto→pass. Smoke: 5/5 decisive, 5.8 s/game, ~656 bridged windows/game. **Finding: 65% veto rate** — `canPlaySa` applies heuristic *judgment*, not just legality, so M0 plays "random proposal, heuristic-gated"; fine for bridge-tax (round-trips unaffected), revisit with M1 CastPlan targeting.
- **gRPC bridge live end-to-end** (fork `4d6f929057`, Anvil `b9ea529`): canonical proto (`anvil/bridge/proto/`, full protocol-v0 message set incl. M1 `CastPlan`/`Construct`), Python decision server (`python -m anvil.bridge.server`, echo + random modes, server-driven `bridged_tags`), Java `GrpcBridge` (one bidi stream, blocking round-trips, deadline→local-answer fallback; gRPC deps confined to forge-gui-desktop). **Echo instrument**: worker pre-draws answers from the seeded RNG, server echoes — gRPC-arm games bit-identical to local-arm (verified across 20 seeds). **First bridge-tax number: ~2.3% single-worker** (132s vs 129s / 20 games).
- **Batch orchestrator landed** (Anvil `8b5037c`, fork `80b66aa262`): `python -m anvil.bridge.harness launch|pause|resume|status|replay|summarize` per the harness spec — chunked worker invocations, immutable `run.json` pinning manifests (jar sha256 re-verified per launch), resume by rescanning per-game JSONL, STOP-file pause, per-game skip rule + zero-progress abort guard. Verified: pause mid-run → resume → 24/24 unique games, no dups/gaps.

## Broke / surprised me

- **My speced seed function was wrong and my own test caught it**: `SplitMix64(base ^ i)` collides structurally across nearby bases (`1000^1 == 1001^0`). Fixed to a keyed stream (`mix(base + i·GOLDEN)`) on both sides; `tests/test_seeds.py` pins Java↔Python lockstep so drift can't silently void replay guarantees. Spec corrected.
- **A worker that can't open its results file exited rc=0 with zero games** (exception swallowed), which the scheduler read as per-game crashes and skip-marched 16 games. Fixes: absolute paths in the orchestrator; results-open failure now fatal in the worker (the progress record is load-bearing); systemic guard aborts after 3 consecutive zero-progress exits instead of skipping.
- **Replay drifts from in-run instances** (54 vs 55 turns, same winner) while being perfectly self-consistent solo — warm-JVM identity-hash allocation order + the heuristic AI's wall-clock timeouts, i.e. ADR-0002's nondeterminism surfacing at harness level. Spec amended honestly: stable solo repro serves debugging; bit-identical in-run reproduction arrives when all decisions are bridged and replayed from the logged answer stream.

## Next session picks up

- **Calibrated three-arm bridge-tax run** (launched end of this session, quiet box, w=16, 320 games/arm, same seed base for local vs gRPC-echo arms → identical workloads): heuristic-only / local-random control / gRPC echo. This is the last M0 deliverable — the games/sec-with-bridge number that calibrates every schedule after it.
- Then: M0 close-out (status + probably an ADR folding measured numbers into §15) and M1 planning (encoder/featurization, CastPlan, real observations over the bridge).
- Parallel tracks still open: DC card pool / decklist pipeline; survey reading list (§6); census re-run when the DC pool lands.

---

# Session 2: calibrated three-arm bridge-tax run — M0 measurement set complete

## Done

- **Calibrated run** (w=16, 320 games/arm, chunk 20, quiet box, orchestrator-managed, same seed base across arms): heuristic-only 382 s (3,016 g/h) / local-random 650 s (1,772 g/h) / gRPC-echo 667 s (1,727 g/h) → **bridge tax 2.6% at w=16**. Zero transport failures; one Python server served all 16 workers. Full table added to bridge-protocol-v0.md.
- **Echo instrument held at scale:** 314/320 games bit-identical across transport; all 6 divergences share seed *and winner* (lengths differ) — the heuristic-fallback nondeterminism floor (identity-hash allocation order + AI wall-clock timeouts), consistent with ADR-0002.
- All three verbs of the harness exercised in anger (launch/status/summarize); 960 games, zero skips, zero draw-clock hits.
- **M0's calibrating number: ~1,700+ games/h with the bridge in the loop** (~29 games/min; ~41K games/day dedicated). §15's schedule assumptions survive with margin.

## Broke / surprised me

- First launch attempt went out through the session's background-task path (10-min cap would have killed it mid-arm); relaunched detached (`setsid nohup`) with a log monitor. Harness runs should always be launched detached — worth folding into the orchestrator docs eventually.
- Heuristic arm under the anvil launcher: 3,016 g/h vs 3,818 via `sim` — mostly 20-game chunks amortizing 16 JVM startups poorly (production chunk 200 is 10× better) plus small n; not investigated further, not concerning.

## Next session picks up

- **M0 close-out ADR**: fold the measured numbers (scaling curve, fork fidelity, bridge tax, callback census) into §15; declare M0 done; sequence M1 (encoder/featurization, CastPlan one-shot, real observations) vs the fork-API upstream track.
- Parallel tracks: DC card pool / decklist pipeline; survey reading list; census re-run on DC pool.

---

# Session 3: M0 close-out ADR

## Done

- **[ADR-0003](../decisions/ADR-0003-m0-closeout.md): M0 declared done.** Records the complete measurement set (three-arm calibrated run, scaling curve, soak, fork fidelity, census); folds the numbers into the design doc — §15 throughput row resolved true (6× margin, pre-optimization, 16 cores), §14 "risk concentrated in the M0 number" retired (BC corpus ≈ 6 days wall-clock; calendar risk shifts to M1 representation quality + M2 RL machinery), §13 M0 line marked done. Notes the 1,700 g/h number is the *protocol floor* (echo answers instantly; M1 adds inference latency, batched).
- **Sequencing recorded:** main line M1 (encoder/featurization, real observations, CastPlan — also the veto-rate fix path); parallel Java track = upstream PR #1 (static fork-bug fixes per ADR-0002, non-blocking); parallel data track = DC pool + census re-run. Full fork API stays gated before M2.
- **M1-planning caveats ledgered in the ADR** so they don't evaporate: 65% veto rate, replay-drift caveat, RSS-drift recycling, chunk 200 production default, detached-launch rule.
- CLAUDE.md status updated: current milestone is now **M1 (BC)**.

## Next session picks up

- **M1 plan doc**: encoder/featurization scope (§1–2), observation schema over the bridge, CastPlan design.
- DC card pool / decklist pipeline kickoff (also unblocks the census re-run).

---

# Session 4: DC pool pipeline — spec, implementation, first real pool

## Done

- **Spec: [dc-pool-pipeline.md](../design/dc-pool-pipeline.md)** — acquisition (network, incremental, append-only raw) split from derivation (offline, deterministic; manifest content hash = pool version). Sources verified live before speccing: mtgtop8 `f=EDH` chain (format page → events → `mtgo?d=` export; Sideboard section = command zone by DC convention), duelcommander.com banlist (static `data-card-name` attrs with section headers).
- **`anvil/pool/` landed**: `fetch|banlist|build|install|status`. Name resolution vs the fork's cardsfolder (all `Name:` lines, cached per fork commit; normalize = casefold + strip diacritics; ladder ends in `overrides.json`). Gates: shape (100 cards, singleton-except-basics, 1–2 commanders) → Forge resolution → current banlist (banned / banned-as-commander sections). 12 offline tests in `tests/test_pool.py`.
- **First real pool: version `f568b187` — 1,679 cards, 111/120 decks included** (20 events since 2025-07; banlist snapshot 2026-07-04, 112 cards). Exclusions all correct on inspection: 6 banned (Edgar Markov ×2 as commander, etc.), 1 malformed source list (2× Minamo), 2 with `Greymond, Avacyn's Stalwart` — **a genuine Forge card-script gap; first entry on the upstream-gap worklist**.
- **Smoke-load gate passed**: built decks install to `~/.forge/decks/commander/` and play seeded games via `forge anvil` — single-commander pair (161 turns, 72s) and a partner deck (Tana/Yoshimaru, 24 turns, 5s).
- `.gitignore` carve-out: `data/` stays out of git, but `data/pool/flex.txt` + `overrides.json` are force-tracked (hand-curated config, not data).

## Broke / surprised me

- **The first 12-deck build caught two real fetcher bugs**: mtgtop8 serves **iso-8859-1** (decoding as utf-8 mangled Théoden/Mjölnir into permanent mojibake — 5 raws purged and refetched; now decode by response header), and split cards are spelled `Fire/Ice` bare, no spaces (resolver ladder extended).
- **Partner commanders**: first shape gate assumed exactly one commander and wrongly excluded 5 legal partner decks (Tana+Yoshimaru et al.). Sideboard = command zone, 1–2 cards; Forge's `[Commander]` section takes both.
- **Real DC decks play long**: one heuristic smoke game went 161 turns/72s vs the precons' ~5.8s average. n=1, but expect the census re-run to revise games/h downward for the real pool — the M0 calibrating number was measured on precons.

## Next session picks up

- **Census re-run on the DC pool** (now unblocked): `forge census` over pool decks; recalibrates M1 bridged-tag coverage + games/h expectation on real decks.
- **M1 plan doc**: encoder/featurization scope (§1–2), observation schema, CastPlan design (MageZero `StateEncoder.java`/`runner.py` + ygo-agent readings feed this).
- Pool follow-ups when needed: archive-page pagination for historical corpus; automatic smoke-load sample in `build`; Greymond upstream gap (script it in the fork or file upstream).

---

# Session 5: census re-run on the DC pool

## Done

- **Driver: `scripts/census/run_dc_census.py`** — 100 deterministic pairs from the pool manifest (seeded shuffle passes, every deck ≥1×), 5 games/pair, 4 sequential lanes at nice 19, one `forge census` invocation per pair with `.tmp`→rename for crude resume; `run.json` pins pool version + fork commit + jar hash. Launched detached per the ledgered rule; ~80 min wall for 500 games.
- **Results appended to [callback-census-results.md](../design/callback-census-results.md).** Headlines: 568K callbacks, **mean 1,136/game (+46% vs precon) on turns median 21 (+22%)**; **64/109 methods fire** (silent list 64→45); priority still dominant at 51%, top-5 = 80%, **rank-robustness prediction held**; M0 tag coverage ~52% measured on pool (near-silent tags woke: `chooseBinary` 0→55, `chooseNumber` 26→109, `playTrigger` now 11/game); cast-path structure fully survives (22% zero-callback casts, targets/X still never callbacks, modes still two interception points).
- **Recalibrations:** M1 corpus schedules on ~⅔–¾ of precon-calibrated g/h; bridge-tax class unchanged (~580 priority round-trips/game, +33%, wall grows in step); phase-2 serialization order updated — replacement/static-choice family (`chooseSingleReplacementEffect`/`chooseSingleStaticAbility`/`chooseCounterType`) now outranks scry/surveil ordering. ADR-0003's provisional-tag-coverage caveat discharged.
- 500/500 decisive, zero draws; the 161-turn pipeline smoke game confirmed an outlier (p90 = 34 turns).

## Broke / surprised me

- **`chooseCardsForConvokeOrImprovise` hit 2,788 calls in a single game** (mean 28.4 in the 16% of games where it fires) — smells like an AI re-evaluation loop on convoke/improvise costs. Logged as a look-before-M2 flag; not investigated.
- The driver's first smoke run reproduced the *exact* relative-path-vs-`cwd=forge-gui` bug the batch orchestrator hit two days ago. Lesson now applied twice; `a.out.resolve()` with a comment naming the trap.

## Next session picks up

- **M1 plan doc** (nothing blocks it now): encoder/featurization scope (§1–2), observation schema over the bridge, CastPlan design; MageZero `StateEncoder.java`/`runner.py` + ygo-agent `ygopro.h` readings as inputs.
- Parallel: upstream PR #1 (static fork-bug fixes); Greymond card-script gap; convoke/improvise loop flag when convenient.

---

# Session 6: the "Greymond gap" wasn't a gap — FlavorName indexing

## Done

- **Retraction: no Forge card-script gap exists.** `Greymond, Avacyn's Stalwart` is the Universes Within twin of `Rick, Steadfast Leader`, which the fork fully scripts with `Variant:UniversesWithin:FlavorName:Greymond, Avacyn's Stalwart`. The real bug was pipeline-side: the cardsfolder scan only indexed `Name:` lines. This is a **182-card class** (all UW twins), not a one-off; `AlternateName:` checked as a possible second class — zero occurrences.
- **Fix in `forge_db`**: `parse_card_names()` maps faces to themselves and FlavorNames to the file's primary `Name:`; cache bumped to `forge-names-v2-*`; 2 new unit tests (14 total).
- **Pool rebuilt: version `cf2ca6ba` — 1,701 cards, 113/120 decks** (the 2 Greymond decks recovered; remaining 7 exclusions all legitimate: 6 banned, 1 malformed source). `.dck` output uses canonical names (Greymond decks play Rick — cosmetic only). Smoke-load: recovered decks played a decisive seeded game.
- **Upstream-gap worklist is empty** — Forge scripts every card in 120 competitive DC decklists. Good coverage signal for the format bet.
- Note: the 2026-07-04 census ran on predecessor pool `f568b187` (111 decks); 2 recovered white-Humans decks don't move its conclusions.

## Next session picks up

- **M1 plan doc** (unchanged, still unblocked).
- Parallel: upstream PR #1 (static fork-bug fixes); convoke/improvise callback-loop flag when convenient.

---

# Session 7: M1 plan — decisions talked through, plan doc + ADR written

## Done

- **[m1-bc-plan.md](../design/m1-bc-plan.md)**: nine deliverables in dependency order — observation schema v1 + trajectory store v0 → CastPlan **label extractor** → ~50K pilot corpus → encoder/trunk/heads → pilot training + embedding ablation → 500K full corpus (overnight chunks, ~22–28 nights planning assumption; CPU-only VPS as calendar contingency) → gate training → eval path (real observations over the bridge + CastPlan **executor**, the veto-rate fix) → combat constructs as stretch. Done-criteria and risk list included.
- **[ADR-0004](../decisions/ADR-0004-m1-scope.md)** records the scope decisions: **featurization line refined** (Java serializes versioned entity-level records; tensor assembly is a deterministic Python transform — forced by the replay-drift caveat: observations must be logged at generation time, so raw-ish records keep feature iteration regeneration-free); **labels for all live callbacks, heads cut at rung 1** (CastPlan-composite priority + M0 one-field tags; combat = stretch); **CastPlan split in two** (the label extractor is corpus-*blocking* — census proved targets/X/modes/optional costs never surface as callbacks, so an uninstrumented corpus has no imitable cast labels); **Qwen3-Embedding-4B pinned** (BGE-M3 alternate, in-domain pilot ablation decides; Tagger tags + LLM gap-filler deferred — scrape project, ablation-addable later); **corpus staged and meta-pool-only** on pool `cf2ca6ba`, personality-randomized, precon/low-power slices as a provenance-tagged later ablation; **yield macros → M2** (amends ADR-0003's M1 line; BC has no stop-setting labels and pseudo-labels would bake heuristic judgment into §3b's protected decisions).
- **Metric sharpened before it's a gate**: held-out agreement excluding single-legal-option decisions, per-tag, with pass-excluded priority agreement alongside raw (pass-dominance must not inflate the number); second split held-out-by-deck-pair as a generalization probe. §15's high-80s row resolves against *this* definition.
- **Disk/bandwidth arithmetic**: ~4MB raw/game (≈2TB raw at 500K) → per-game zstd frames target **~100–200GB**, inside §9's store budget; keyframe+delta is the fallback lever; corpus is regenerable, no backup needed. Bandwidth trivial everywhere (generation ~3MB/s worker-local; eval observations ~2MB/s loopback). Project-wide survey: M2 self-play (1–3M games, ~0.5–1TB class, retention/reservoir-managed) is the only larger consumer; checkpoints tens of GB; VOD caching (§8) only if we choose it.
- Embedding-model research (web, mid-2026 landscape): Qwen3-Embedding family tops open-weights MTEB (Apache 2.0, selectable output dims); BGE-M3 the MIT production workhorse. MTEB doesn't measure rules text — hence the in-domain ablation as decider; 4B over 0.6B since the embed run is independent and rarely repeated.
- CLAUDE.md status updated (M1-plan bullet).

## Broke / surprised me

- Nothing broke (docs-only session). Two planning surprises worth naming: **CastPlan moved earlier than expected** — it's a corpus-generation dependency (label extraction), not just the eval-time veto fix; and the replay-drift caveat quietly dictates the observation-logging architecture (log at generation time or lose it), which is what pushed the Java/Python featurization line to "structured records in Java, tensors in Python."

## Next session picks up

- **D1: observation schema v1 + trajectory store v0** (corpus-blocking; Java serializer extends the census logger, Python reader + tensor transform in `anvil/encoder/`, per-game zstd frames + provenance fields).
- Then D2 (CastPlan label extractor) → D3 (pilot corpus, which measures bytes/game and the serialization g/h haircut).
- Parallel: upstream PR #1 (static fork-bug fixes); convoke/improvise callback-loop flag when convenient (could inflate corpus wall-clock — watch it in the pilot).

---

# Session 8: M1 D1 — observation schema v1 + trajectory store v0, end to end

## Done

- **[observation-schema-v1.md](../design/observation-schema-v1.md)**: JSONL records in per-game zstd frames (same idiom as the census log it extends; the identical JSON bytes ride the bridge's `observation: bytes` at D8); **full-state records with per-entity visibility, information set enforced by the Python transform** (forced by replay drift + M2 belief-head ground truth; guarded by a dedicated leak test); **`dec`/`ret` record split** (observation at callback entry before delegation mutates state, answer joined on seq — handles nested mid-resolution callbacks with zero buffering); **libraries not serialized** (derivable as decklist minus observed owned non-token entities; scry-to-top knowledge is a named v2 gap); schema version at frame granularity; no delta/dedup in v1 (fallback levers named, D3 decides).
- **Fork `c9c0191b84`**: `Obs` writer (zstd frames + idx sidecar, zstd-jni added to forge-ai), `ObsSnapshot` state walker (glob/players/entities/stack/combat, visibility deviations via `mayPlayerLook`), generated `CensusPlayerController` regenerated with `Obs.dec`/`Obs.ret` around all 109 callbacks, bridged overrides in `PlayerControllerAnvil` log options + chosen, `-obs <path>` on the anvil command. Writes gated on Game identity so a hard-cap-abandoned game thread can't leak records into the next frame.
- **Anvil side**: `anvil/store` (TrajectoryStore reader, `python -m anvil.store ingest|status`, frame-index consolidation that naturally skips orphaned bytes from crashed games and dedups re-issued games), `anvil/encoder/transform.py` (tensor assembly v0: multiset dedup + count features, canonical row order, perspective enforcement) + `vocab_mtg.json`; harness `--obs` flag; 14 new tests (29 total pass).
- **Smoke, 5 pool games end to end**: 4,310 decisions ingest, decode, and transform cleanly from both perspectives; zero serialization errors; ret-join 3,769 (rest are void callbacks by construction).
- **Numbers**: ~2 MB raw/game (in the plan's 2–6 KB/obs band); **zstd-3 frames compress ~70× → 28 KB/game → 500K games ≈ ~14 GB**, an order of magnitude inside the 100–200 GB target. **Serialization haircut ~4.3%** single-worker (small sample; D3 is the calibrated number). Same seeds with/without `-obs`: **bit-identical outcomes** — the serializer is a pure observer.

## Broke / surprised me

- **`Game.getPlayers()` drops eliminated players and reindexes** — late-game records shrank the players list and would have corrupted every seat reference (caught by the smoke run's IndexError, would have poisoned the corpus silently otherwise). Fixed: all seat indices come from `getRegisteredPlayers()`; eliminated players carry `"lost":1`. Recorded in the schema doc.
- **The leak test caught a real leak on first run**: transform row order followed record order, so opponent hand *ordering* was observable. Rows now leave in sorted-dedup-key order (order is non-semantic by schema).
- Compression came in ~70× vs the assumed 10–30× — near-identical consecutive observations are even more redundant than priced.

## Next session picks up

- **D2: CastPlan label extractor** (corpus-blocking; hook the AI's cast decision, serialize chosen SA — spell/targets/X/modes/folded optional costs; replay-validation sanity gate).
- D1 residue, non-blocking: pool_version should flow into `run.json` at launch instead of an ingest flag; `iter_decisions` may want a method→tag mapping table once D2 defines tags for more callbacks.
- Parallel: upstream PR #1 (static fork-bug fixes); convoke/improvise loop still a D3 watch-item (its 2,788-call game would now serialize 2,788 observations — the ref-dedup lever exists if D3 shows it matters).

---

# Session 9: M1 D2 — CastPlan label extractor, end to end

## Done

- **CastPlan labels live (fork `106d13b0f0` + Anvil), schema doc amended.** `Obs` now serializes every SpellAbility-valued answer as a CastPlan: host entity, targets in the observation idiom (`{"e"}`/`{"pi"}`/stack refs joinable on stack host ids), X, alternative cost, optional costs, multikicker, bound modes (recursive), sub-ability-chain targets. Archaeology (targets/X pre-set in `canPlaySa`; the returned SA *is* the `addOptionalCosts` copy; modes usually bind later at `chooseModeForAbility` — itself already a labeled dec/ret) is recorded in the schema doc.
- **Priority decs log structured options** (`{"e","sa","kind"}` per engine-legal, payable SA + land drops): the logged legality basis the gate metric's single-legal-option exclusion needs, and the training-time action mask — logged at generation time because replay drift forbids recomputing legality later. Generator gained a one-entry `DEC_OVERRIDES` table (`Obs.decPriority`); scan shared with the M0 bridged path via new `AnvilOptions`; runs only when a frame is open.
- **Python contract:** `anvil/store/castplan.py` (parser + five-check validator: parse, host-in-obs, target refs, chosen-among-options, per-seat `playChosenSpellAbility` string match), `python -m anvil.store validate` verb, 7 new tests (36 total pass).
- **Sanity gate: PASSED on the second 20-game batch** — 9,547 windows / 860 cast labels (178 targeted, 57 with X, 49 with optional costs), zero errors. 42 KB/game at 75× (500K ≈ ~21 GB; options lists are the growth over D1's 28 KB).
- **The validator caught a real corpus-poisoning bug on its first batch:** Snuff Out cast for 4 life (`AltCost`) wasn't in the logged options — the M0 scan payability-checked base SAs only, so spells playable *only* via alternative costs were missing from the legality mask (it would have forbidden the expert's own action). Fix: `AnvilOptions` iterates the AI's own `getOriginalAndAltCostAbilities` set so every variant is payability-checked individually.

## Broke / surprised me

- **The option scan is not a pure observer.** Same seeds with vs without `-obs`: 14/20 identical, 4 winner flips — the scan's allocations/ordering perturb *which* deterministic trajectory a seed plays (no information reaches the AI). With logging ON, same-seed runs reproduce outcomes 20/20; only mana-payment micro-order wobbles (3/20 frames, `payManaCost` land-tap order — engine-default payment, unlabeled, ADR-0002's floor). Consequences: harness `replay` now passes `-obs` for obs runs (else replays silently diverge); with/without-obs distributions are same-policy, different draws; D1's pure-observer line in the schema doc scoped to the serializer alone.
- **Cost flag for D3: obs+scan haircut is +52% single-worker** on identical-trajectory games (D1 serialization alone: 4.3%). The legality scan duplicates the AI's own availability work every priority window. D3 measures at w=16; the lever if it holds is sharing the AI's scan instead of re-running it (fork surgery, priced only if the pilot says so).
- **Two tool-shell traps burned a smoke batch each:** the session shell is zsh (login shell is fish; harness falls back) — zsh doesn't word-split unquoted vars, so `$OBS` passed `-obs path` as ONE argument and the flag silently vanished; and `pgrep -f`/`pkill -f` self-match the script's own command line (deadlocked a wait-loop, then killed its own shell). Both pinned in memory: flags inline, match by PID.

## Next session picks up

- **D3: pilot corpus (~50K games)** — now measures four things: bytes/game, the +52% haircut at w=16 (decide on scan-sharing), label sanity at scale, convoke/improvise loop wall-clock impact.
- D2 residue: bridged-path opts still plain strings (fine for M0 instrumentation; unify when D8 eval lands); known-position library cards still the named v2 gap.
- Parallel: upstream PR #1 (static fork-bug fixes).

---

# Session 10: D3 opened — pool schedule + personalities landed; A/B and 320-game validation say "not yet"

## Done

- **Corpus-generation machinery for D3 (fork `13d86c9c81`, Anvil `12f66e6`).** Worker: `-pairs <file>`/`-gpp <n>` (game index → deck pair from a tab-separated schedule; deck cache; mutually exclusive with `-d`) and **seeded per-seat AI personalities** (sorted profile list Cautious/Default/Experimental/Reckless; pure function of the game seed, so the expert mix reproduces from `(seedbase, index)`; empty profile list is fatal — silent enum-default seats would fake provenance). Both logged in results JSONL (`decks`, `profiles`) and the Obs game record (D1's `profile` hook was already there). Harness: `launch --pool` (pair schedule from the latest pool manifest, census-driver pattern; `pool_version`/`pairs_sha256`/`n_pairs`/`games_per_pair` pinned in `run.json` — D1's pool-version residue discharged), `summarize` gains obs bytes/game + wall tail (p90/max, 10 slowest games = the convoke watch), `ingest` defaults pool version from `run.json`. 41 tests pass.
- **Smoke (20 pool games, chunk deliberately misaligned with pair stride): green end to end.** Provenance verified in both logs; ingest + castplan validate zero errors; replay bit-matches the in-run record through the new path.
- **Calibrated w=16 A/B, 320 games/arm, same seeds (obs off vs on): the D2 +52% flag got worse, not better.** Off arm 923 s = **1,248 g/h** (the true pool+personalities baseline — well under the ~2,300–2,900 precon-derived band; personalities and pool tails are the delta). On arm 1,638 s = **703 g/h → +77% haircut at production concurrency**. At 703 g/h the 500K corpus is ~30 days of 24/7 box — **scan-sharing is now required, not a lever to price later**. Bytes/game held at 48 KB (500K ≈ 24 GB, fine). Draw-clock hits 2.2% (7/320) vs M0's 0.4% — pool+personality games run longer.
- **The on-arm doubled as a 320-game label-sanity batch and caught 74 errors in two systematic classes** (~0.04% of 187K windows, concentrated on DC staples — 20-game batches statistically couldn't see them):
  1. **Library-top casts (63):** Mystic Forge / Oracle of Mul Daya / Courser of Kruphix. `getAvailableCards` includes the library top (that's how the AI casts it) but `ObsSnapshot` never serializes library entities — the label's host isn't in the observation. D2's "known-position library cards" v2 gap, now measured as a real corpus hole.
  2. **Cost-modifier payability divergence (11):** `AnvilOptions` prices `canPayCost` on the raw SA (no target context, no reductions/additional costs); the AI prices after adjustment. Mystical Dispute / Dargo / X-spells / commander casts get omitted from the logged mask, then the expert casts them — the mask forbids the expert's own action, the Snuff Out failure mode one layer deeper.

## Broke / surprised me

- **The 10-minute background-task cap nearly ate the A/B again** — the exact M0 trap, caught seconds after launch this time; killed, cleaned the partial run dir, relaunched via `setsid nohup`. The ledgered rule stands: harness runs always launch detached, no exceptions for "short" ones.
- **zsh kills a whole command line on one unmatched glob** (`no matches found` while the on-arm dir didn't exist yet) — third tool-shell trap this week; `ls -d ... 2>/dev/null` instead of bare globs in checks.
- A smoke game hit the 300 s draw clock and still recorded `won`: the AI's internal per-decision timeout fired during a 63-turn grind and a real win landed at 300.2 s, racing the draw-clock's `setGameOver`. Both facts recorded honestly (`status:won`, `draw_clock:true`); no corpus issue.
- **The validation errors are the D3 process working**, not a setback: the plan gates the corpus on label sanity at scale, and a free 320-game batch found what 40 smoke games couldn't — *before* a 50K burn, not after.

## Next session picks up

- **Scan-sharing archaeology (started end of this session):** capture the AI's *own* availability/payability set at the priority window instead of re-deriving it in `AnvilOptions` — one surgery that plausibly fixes the +77% haircut *and* error class 2 (payability parity by construction). Find the capture point in `AiController.getSpellAbilityToPlay` / `canPlaySa`; mind the dec/ret record ordering (options may need to move to the ret side or an amend record — Python contract update).
- **Library-top visibility fix (`ObsSnapshot`):** serialize library-top entities when engine-visible (`mayPlayerLook`), per-entity vis flags as usual; schema doc v1.1 amendment; transform's derived-library logic treats observed-top as position-known.
- **Then:** fresh 320-game validation batch (must be zero errors) → re-A/B (expect ~1,100+ g/h obs-on) → 50K pilot launch (`scripts/d3/launch_pilot.sh`, ready).
- Watch items into the pilot: draw-clock rate at 2.2%, convoke loop (nothing alarming in the smoke/A/B tails yet), 300s-cap races.
