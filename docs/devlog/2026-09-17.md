# 2026-09-17 (session 2 — the allocation head)

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- The state review after the re-warm's close: box idle, main clean, no worktree ahead. The
  allocation head had only its frozen probe; the era's search rows = `b4-tgtlab` (69,380).
- **The finding:** the loop does not run the search yet — `selfplay.py` passes no search flags,
  `rl.py` has no search-row / behavior-logp term (the harness's `forge_args` + the row's `logp`
  exist). Routed as Build 4½ pre-work (ADR-0112 item 5).
- Decisions (user took the recommendations): the `anvil.alloc` ask on the value wire (the rate
  draw fires before the priority ask — the "extra output" route needed a restructure); p ≥ tau +
  a seeded floor; the frozen-trunk fit with a fit record now; the order head → read → loop wiring
  → route (b) → docs → shakedown.
- Fork `a37bc6a8b4` (the ask, the rows, the census, the pins; off = byte-identical) + Anvil
  `6d12084` (`alloc_head`, the server's gated tag, `alloc_fit.py fit`, the smoke reader, the
  launcher knobs, `build4_alloc_chain.sh`). [ADR-0112](../decisions/ADR-0112-m12-build4-allocation-head-served.md).
- The fit: AUC 0.800 OOF, tau 0.369 at 90% recall (OOF 0.87 at cost share 0.62) →
  `m12-build4-e1a`. The unfitted wire smoke clean (224 asks declined, 7 ms each). Tests 320.

## Broke / surprised me

- The act smoke needs an ABSOLUTE `JAR` (the java launch cds into forge-gui/): the first
  unserved smoke exited in 0 s on "Unable to access jarfile" — the launcher's own comment
  already warned about OUT; the same holds for JAR.
- `alloc_fit.py build` on 69K windows takes 3 minutes on the GPU — the first fit's featurization
  was never the bottleneck.

- **The GPU yield gate paused the `act` arm for ≈ 3 h (WoW on the GPU, 19:00–20:15)** — by design
  (the fleet's foreign-GPU-job rule); resumed on "gpu quiet" at ≈ 20:20, 473 → 864 in 30 min.
- **A false STALLED alert class:** the chain is launched with `--dir data/runs/build4-alloc`, so the
  supervisor's stall tick watches THAT dir — but the read's arms write under `data/runs/b4post-*` /
  `build3-surface-read-b4post/`, so every 60 min of a healthy read raises STALLED (19:32 and 20:34;
  the first coincided with the real yield pause). Patched live with a heartbeat loop touching
  `data/runs/build4-alloc/heartbeat` every 5 min. **Routed (the ~30-line fix):** `anvil.runs launch
  --watch <dir>...` (extra artifact roots for the stall tick) or a heartbeat in every chain script's
  own dir — the 09-04 paired-read false stall was the same class.

## Next session picks up

- **`build4-alloc-chain` is running (launched 15:14, pid 56516; `data/runs/build4-alloc/queue.log`):**
  forkcheck `run-20260917-build4-alloc` (`data/forkcheck/run-20260917-build4-alloc/compare.txt`;
  499–500/500 with the standing seed = PASS → the fork pin `a37bc6a8b4`) → the read
  `data/runs/build3-surface-read-b4post/` (`on.log` / `act.log` / `alloc.log`, then `read.json`,
  `read-alloc.json`, `alloc-census.txt`). ETA ≈ 04:00–05:00 09-18. Verdicts pre-registered in
  ADR-0112 item 4: act − on ≥ +1.5pp = the launch condition (in-band / ≤ 0 = the Build 2 rules);
  alloc − act within one SE = the head allocates freely; negative at t ≥ 2 → tau / floor move.
- Then: the ADR-0112 addendum with the read + the pin (state of record, the map's pin + ledger
  rows), the served-build row → `m12-build4-e1a` if the read is clean.
- Then the loop wiring (ADR-0112 item 5; Build 4½ pre-work): search flags on `selfplay.py`'s
  harness launch (the recipe + `-searchalloc` from the ckpt's record); `rl.py`'s search-row
  terms (the pick distillation where it acted, PG under the search's logp, the alloc head's BCE
  on the rows' margins — `search_distill`'s join + anchor exist); the head regenerated per cycle.
- Then route (b) (the realized-plan recording, fork), the documentation pass (Build order 4¾),
  the shakedown with the allocation arm.
