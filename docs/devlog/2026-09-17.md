# 2026-09-17

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **Community watch (Discord, read 09-17; nothing posted from here beyond the user's two Jev
  lines):** three threads recorded in the [survey doc](../design/discord-ai-plotting-survey.md)'s
  09-17 follow-up.
  - **TRT's condition on talor's cache PR** ("existing systems need to be reused for less
    technical debt — in this case AiCache"): recorded on the [upstream worklist](../design/upstream-worklist.md)
    item — `AiCache` is a global, once-per-priority-cleared multimap with no per-invocation
    scope (its own TODO), so the PR becomes a storage rewrite (a scope for `AiCache`, the block
    cache its first user), identity-gated again before proposal.
  - **The anytime-search / metrics thread:** Shedletsky's "winning line" proposal and his
    card-correlated hang censor (~1% of random-deck games) = the search directive's count
    budget + the loop guard + the census-by-seed; chrismaghuhn's four metric families
    (environment / behavior / learning / strength) = the monitor's panels; Dan B's MageZero +
    17lands plan (deck-local warning restated by chrismaghuhn); Shedletsky's deck-evolution
    fitness audit (standard candles + resurrection re-elimination) → routed by name to Tutor.
  - **Kryptic's second Anvil run** on four mono-coloured decks (`constructed-four-rl-4000`):
    BC 40.6% → iter 30 51.1% / iter 40 50.6% (n 8,000) vs a 40,000-game heuristic matrix — a
    plateau from iter 20 (−0.5 ± 1.5 between 30 and 40, not a decline); Blue Tempo +5 to +11
    where the heuristic plays it at 15–27%; the red mirror 35–39% the one structural deficit;
    the guard curves ×5–10 from iter 20 with entropy rising; pay deviation (sampled) 0.9 with
    zero directed failures — a watch item for him (our five served pay heads ≤ 0; the pre-09-09
    probe cost if his jar predates `15863de0b4a`). Decks + workbook kept at
    [community/constructed-four/](../design/community/constructed-four/) as the quickstart's
    second smoke pool. Draft replies to Kryptic and Shedletsky in the survey doc.

## Broke / surprised me

- The BC's Red Aggro row in Kryptic's workbook (15–18% off-diagonal, 16.7% in the mirror where
  the heuristic itself sits at 51.6): imitation on 8,000 games could not play burn at all; the
  loop recovered +20pp of it. Burn's decisions (face vs creature, Eidolon, Searing Blaze, Rift
  Bolt, Shard Volley) are the target-and-sequencing class Build 4's targets evening serves here.

## Next session picks up

- The 09-16 session-3 pickup stands (route (b): the target decoder co-distilled on realized
  plans; the allocation head's served output; the post-Build-4 read on e1; the documentation
  pass). From today: the four-deck pool as the quickstart's second smoke pool at the
  documentation pass; chrismaghuhn's four-row metric framing for the quickstart's "what to
  watch" paragraph; Tutor's scoping gets the resurrection re-elimination audit by name.
