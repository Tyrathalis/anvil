# 2026-07-30 session 2 (morning): D3 per-game drill-point selection

## Done

- **`grindstone select` (the sweep's design consequence made mechanism):**
  per-game drill fork-turn selection from K-rollout ground truth.
  - Rule: **latest labeled fork turn whose rollout winrate lands in the
    trainable band [0.25, 0.75]** — later = closest to the decision
    error and cheapest completions; band = outcome variance exists.
    Fallback: latest turn *above* the band (position winnable early —
    converting won positions is still a drill). Excluded if nothing
    clears the floor anywhere (the luck-locked profile).
  - Label sources are dirs holding `drills.jsonl` (map, sweep arms),
    merged **later-supersedes at the same fork turn** — re-measures
    listed after selection-time labels, per the D2.4 lesson.
  - **Evalset holdout subtracted here** — `select` is the boundary
    where training lists are built; `--holdout` reads the evalset's
    `held_out` membership.
  - Output rows = curation rows + `drill_turn`/`sel_rule`/`sel_wr`, and
    `plan` gained `ANCHOR_FIELD["selected"]` so `plan --anchor selected`
    consumes the selection with zero further changes.
- **The v1 training drill list (`data/runs/drill-selection-v1/`):**
  **349 drill points** from the 584-game curation — 334 band + 15
  above-band; 44 excluded luck-locked; 176 held out. Mean winrate at
  the chosen fork **0.416** (≈ variance-maximal). Offset profile: 93 at
  crash (the non-lost bins), 128 at −2, 87 at −4, 41 earlier (peak
  anchors). **Accounting closes exactly: 584 = 569 usable labels + 9
  all-crash (the MayPlay class) + 6 replay-missed.**
- Verified end-to-end: `plan --anchor selected` plans all 349 drills
  across the two source arms. Tests: selection rule (band-latest /
  above-fallback / exclusion), supersession, holdout subtraction, plan
  integration — grindstone suite 11, full suite 111 green.

## Broke / surprised me

- Nothing. The selection accounting reconciling to the game against the
  map's `all_completions_crashed` and replay-miss counts was the
  designed check and it held.

## Next session picks up

- **D3 integration (the remaining pre-run work, in order):**
  fork-session store ingest + `drill` provenance tag (drill MAINLINE
  stores never enter training ingest — pseudo-winner hazard), the
  sampled+μ drill backend (training drills need sampled completions
  with μ records; everything so far is argmax measurement mode), then
  the first drill-mixed run at a priced fraction vs the 0.5121
  baseline.
- **Critic sharpening** rides whenever the next critic retrain happens
  (~1,900 K-rollout labels; + queued Ante items).
- **D4 noise floor** (`bench_generation.py` repeats) whenever the GPU
  is idle.
