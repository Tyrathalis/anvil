# 2026-08-26 (the M10 build design session — forks adjudicated on the funded candidate)

## Done

- **The two remaining named exploratory reads FUNDED + RUN first**
  (user call: let them inform the discussion; logic committed
  `d55e348` before any output was looked at —
  `scripts/schedule_explore2.py` → `marginal-read.json` /
  `binned-read.json` + perturn files; write-up in
  [m10-ceiling-exploratory-reads.md](../design/m10-ceiling-exploratory-reads.md)
  §5–6, commit `7b5203b`):
  - **Marginal attribution (197 both-read marginal turns): the
    schedule leg carries the joint ceiling at h2 grain.** Joint 26.4%
    vs auto 23.9% certification; 83% of joint-certified schedules
    survive auto-pay; same-schedule twin deltas = a sparse ONE-SIDED
    tail (137/197 exactly zero; all 13 |d|≥2 positive, +2.1…+11.9);
    twin degrade rates identical (72.85% vs 72.91%). No h2-visible
    super-additivity; the ADR-0075 conditional shape re-confirmed for
    payment. Caveat recorded: uniform stratum ⇒ ~96% of turns never
    contained a consequential payment window — the standalone +2.96
    leg stands.
  - **Critic-binned gain (583 turns, era iter-019 + d4-fullvis,
    cross-critic ρ=0.98): cert rate is flat across pre-turn value;
    conversion is NOT** — Δwr by quintile +11.7/+16.7/0.0/+4.6/−3.3pp;
    split at v=0.45: **+14.1pp (65 pos) vs +0.5pp (105 pos) — the
    whole +5.69 lives in behind/contested states.** Spearman(v, Δwr)
    −0.21 ≈ 2× the h2 margin's 0.109; the detector-not-ranker caveat
    is now LOCALIZED (improvement certifies everywhere, converts only
    where contested). The LordOfThePigs instrument prototype WORKS.
- **Five forks adjudicated (user present all session), recorded in
  [m10-plan.md](../design/m10-plan.md) per-fork with reasoning:**
  1. **Resource component: BOTH, joint multi-task, R1 discipline**
     (afford-at-execution = ledger/validity half; EOT untapped/
     floating = selection/hold half) + a THIRD probe target minted
     from the sweep rows: schedule feasibility/degrade-point from
     state (96k forced executions — the realization-validity surface
     the veto-dominated divergence names as binding). Contingent-line
     rider (user's counter-mana hold-then-dump example): conditional
     schedules stay EMERGENT (ordering + hold-set + policy authority
     + revision), three first-class fingerprints recorded
     (deviation-class for reserved-mana exercise; A-claim graded vs
     latest revision — couples to cadence; hold-set-preserving
     affordability = v2+ named).
  2. **Cadence: REVISE-ON-TRIGGER** — 4 engine-detectable triggers
     (own veto / opponent action / EOT entry / exhausted), unprovoked
     revision structurally disallowed (follow telemetry stays
     falsifiable; both poles rejected with reasons), stop-grad carry,
     no-op revisions first-class, priced at the smoke rung. **Model-
     placed triggers (expectation watch-sets) SET ASIDE as canonical**
     — the missed-revision residual is their funding instrument.
  3. **Ingestion: SCHEDULE TOKENS + [PLAN] summary readout** — pointer
     grounding (entity-bound slots), execution status as carry
     bookkeeping, and the DISCRETE CARRY (bit-exact loader
     reconstruction; the v1 tripwire's float-drift class eliminated).
  4. **Payment re-advertisement: SLOT-CONDITIONS with marked
     candidate** (soft, uniform with emission philosophy); scope =
     in-scope + cousins + costmod refinement, effect payments held
     for their probe; **PG STAGED MASK** (the marginal-collapse path
     would silently re-enter via on-policy PG traffic — masked at
     birth, pre-registered unmask); ADR-0073 attribution stripping
     RETIRED loudly (marginal read = the replacement instrument).
  5. **Supervised-conditional wiring: DIRECT AUX at birth, drill
     families follow-on** — labels are era-assets, the sweep is a
     re-runnable label mint; Grindstone targets the gaps the birth
     telemetry reveals (substrate corollary), never blind. Evalset
     repair rides the label batch.
- **Kill-signal + telemetry package STRUCTURE ADJUDICATED** (numerics
  at probe launch pre-data): 5-family telemetry roster
  (consumption / follow-deviation taxonomy / ambition / validity /
  payment), conjunctive KILL over all aux heads, FUND with a
  DEGENERACY VETO (veto-shaped, user-adjudicated), guard posture from
  birth (proj-lr split; read-before-halt on veto trips),
  **probing-dissolution pre-registered secondary read** (M10 removes
  the auto-payment ignorance twice — probing declining = the
  interface account's cleanest confirmation), PG-unmask with a
  ≥4-accepted-iterations floor (user call), invalid-schedule penalty
  rule pinned (knowability-gated, ADR-0053-calibrated from sweep
  degraded-arm deltas).
- **[canonical-register.md](../design/canonical-register.md) BORN**
  (user direction: philosophically-correct future forms noted across
  the board): 8 entries, each with staged form / canonical form /
  named promotion instrument — watch-sets, plan-level serve search
  (the sweep oracle IS its offline prototype), belief-state opponent
  modeling (the counter-deck scenario presupposes it), cross-turn +
  off-turn persistence, plan-complete slots, combat-inclusive plans,
  in-graph plan credit, exact payment matching.
- Charter ranking marked settled on ceiling grounds; read-protocol
  fork annotated as THE one remaining (prototype in hand; pin rides
  the probe-launch numerics session).

## Broke / surprised me

- Nothing broke — a clean discussion session. The surprise was
  substantive: **payment's within-schedule contribution is nearly
  invisible at h2 grain** (137/197 exact-zero twins) while the
  **conversion locus is sharply state-dependent** (+14.1 vs +0.5pp
  across v=0.45). Both reads reshaped the label-design leans the same
  night they ran — the fund-the-cheap-reads-first call paid for
  itself within the hour.

## Next session picks up

- **The v2 offline target probe build** (design-round obligation 2,
  now fully specified): labelers for the three targets (EOT resource
  summary — the (c) genre extension; running-affordability along
  realized sequences — veto_knowability + resolve_cost machinery;
  feasibility/degrade-point — sweep rows), frozen iter-019 trunk,
  R1 baseline ladder, gates pinned at probe launch pre-data.
- **The two funded M11-routing ceiling probes** (obligation 6:
  tutor/fetch targets, resolution-effect payments) — session-scale,
  ADR-0053 forced-branch machinery.
- **Numerics pinning** (kill/FUND/unmask thresholds + read-protocol
  pin) at probe launch, day-zero banked first.
- The design round's closeout ADR waits on the probe + the two
  ceiling probes (done-when 1).
- Watch (carried): mainline draw-clock both-win wart — route the fix
  with the M10 label work.
