# 2026-09-24 — status checks + the head-swap read

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · the running record (mNN-running-record.md) · Now block · commit). -->

## Done

- Two status checks on the shakedown (21:12 on 09-23, 09:35 on 09-24): the alloc arm resumed cleanly
  from the certifier-merge pause, 414–430 g/h, iteration 17 at 27.1 / 30 h; close ≈ 12:00–13:30.
  Interim paired reads iter-004 / 009 / 014 = 0.515 / 0.5475 / 0.5375 network-alone (± 2.5pp).
- Hand Spearman rows for alloc iter-012 / iter-016 (0.277 / 0.213) appended to the run's JSONL.
- The head-swap read (CPU, scratchpad checkpoints, no pause): the drift is the trunk's, not the head's
  weights — addendum on ADR-0118, running-record entry, rows in
  `data/runs/shakedown/state_ranking_headswap.jsonl`.

- ADR-0119: the value-function-first sequence for the settings pass (anchor → Ante re-measure +
  shuffle records → corrected reads → amortized advantages, each gated), from the user's noise
  frame. Found on the way: the shakedown reads skip Ante; the ledger's reduction is ≈ 0.6–5%
  and 69% of draws are poisoned. Plan item 4 amended, the routed section, the Now block.

- The alloc arm closed 14:27 at 0.5405 ± 0.0111 (+2.2pp; 20 iterations, 32.65 h). Now block,
  running record. Shallow launched by the chain.

## Broke / surprised me

- The user proposed pausing the run after the alloc close to fit the swap in; not needed — the eval is
  deterministic given the checkpoint and runs on CPU at nice 19 beside the fleet.
- The head's weights moved more than the trunk's (1.8% vs 0.4% relative L2) yet contribute nothing to
  the ranking loss; a small trunk shift is enough to break the head's input.
- iter-009 / iter-014 paired reads carry one to four Java NPE crashes per side (`skips`), unread.

- The alloc arm ran ≈ 2 h past its 30 h box: the wall check is per iteration boundary and the
  paired read counts. Routed: check the wall before the paired read.

## Next session picks up

- The alloc close (read.md row, the Spearman column by hand), then shallow / deep.
- Post-run worktree adds: a per-term trunk gradient-norm row in `rl.py`; `--swap-head` on
  `value_pretrain eval`.
- A glance at the crash counts in the alloc arm's final read.
