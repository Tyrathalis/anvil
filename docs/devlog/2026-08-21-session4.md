# 2026-08-21 (session 4) — D4 gate session: gates pinned, four routed decisions closed

Discussion-only session (no code); pins landed in
[m9-plan](../design/m9-plan.md) D4 + the payment-completion queue,
pointer in [m9-rung3-draft](../design/m9-rung3-draft.md) §4.

## Done

- **D4 gate PINNED (supersedes the PIN-AT-DESIGN slots), re-posed as
  movement-from-baseline** — the day-zero read's 8.3% argmax deviation
  made the "leaves auto at all" floor vacuous, so it can no longer
  fund:
  - **FUND (⇒ D5):** positive-drill argmax accuracy **≥ 7/68
    (~10.3%)** at any accepted iteration (≥5 net newly-correct drills
    over day-zero 2/68, ~2.5× binomial SE) **AND auto-correct ≥ 85%
    (≥189/222) at the same iteration.** Asymmetry is principled:
    positive drills are the only population that can show capability
    the auto-payer structurally lacks; auto-correct starts
    near-saturated (91.9%) from the +2.0 init and can only show the
    deviates-wrongly failure mode — guardrail, never target, never
    blended.
  - **CLEAN NEGATIVE:** argmax deviation < 2% (collapse-to-always-auto)
    with positive ≤ 4/68 across the probe. Fallback routing unchanged
    (Option B head variant, or straight to §3a).
  - **Between = discuss-zone**, read session adjudicates.
  - Baselines: drill-window = banked day-zero scores
    (`score-dayzero-iter019.jsonl`); live-window `pay_deviation`
    baselines at the run's own iter-0, argmax/sampled separate
    (ADR-0063). Observe frames are ckpt-independent ⇒ per-iteration
    drill curves are near-free; every accepted iteration gets scored.
- **Bias stays +2.0.** Movement-from-baseline absorbs the 8.3%; the
  PASS anchor raw score is context-dependent (−2.43 spot-checked) so
  no scalar guarantees argmax-clean; raising starves the ~10–12%
  sampled exploration the sparse deviation reward needs. D5 boundary
  bit-identity is a wire property either way.
- **paygoals4 NOT run — bound accepted.** 15.28/g stands as an upper
  bound (ADR-0067 removes plans only, salvage 0.22% of directed
  rows); bridge-tax budget has slack; the post-boundary re-baseline
  census supersedes it.
- **Pool-tie residual → payment-completion queue item 5** (lex-hidden
  `min_life` plan, fork-test genre): fix + regression test on the
  next payment-family touch, **never mid-era** — enumeration changes
  de-sync the certification jar from the training jar
  (`option_mismatch` drift; phy floor margin is 3).
- **New boundary obligation (done-when 3 amended): post-boundary
  evalset revalidation pass** — re-run observe lanes on the bundle
  jar (~35 min), count drift exclusions, re-check shape floors,
  re-bank day-zero scores on the post-boundary init.
- **Forced family restated in the pins:** D4 midpoint re-mine from the
  run's own stores, hb-signet self-pair under the D4 ckpt as
  accelerant; resulting drills = a D4-era **addendum set scored
  separately, never retroactively in the 7/68 gate**; empty even with
  hb-signet ⇒ revisit the declined cast-directive alternative.

## Broke / surprised me

Nothing — discussion session; all inputs were pre-routed
(day-zero baselines, ADR-0067 consequence, pool-tie re-type).

## Next session picks up

1. **The boundary bundle** (the last gate before D4 launch): upstream
   rebase + §3c payment surface (fork `m9-payment-surface` tip) +
   fork-index store-namespace fix + multi-format enablement +
   copy-state forensics + obs choice-state emission + obs schema
   bump; 2-arm trim landed or explicitly retired; forkcheck
   certification; 2,000-game re-baseline (re-pins the M9 gate
   number); era-scoping sweep; **the new evalset revalidation pass.**
2. Then the D4 probe run (~5 iterations, standing loop, post-boundary
   baseline) against the pinned gate.
