# 2026-08-01 (session 4): (a2) prep — evalset re-split, selection v2, d6-run11 launch

## Done

- **Evalset v2 (`data/runs/drill-evalset-v2/`):** the ADR-0030 re-split —
  stratified *fraction* per bin, whole bins never again consumed. Counts:
  winnable 22 / coin 17 / long_shot 40 / lost 40 = **119 held out** (was
  176). Rationale: ~1/3 of each small bin (so 2/3 of winnable+coin release
  to training — the point of (a2)); long_shot stays 40 for v1
  comparability; lost rises 20→40 (the bin where run10's gain lived gets
  real power; a 12% draw from 330 still leaves 249 in training). Zero code
  changes — `grindstone evalset` already took per-bin counts.
- **Baseline re-measurement** run per the D2.4 protocol (pinned run9-i009
  re-measured under eval conditions; `baseline_eval` set in meta) — evals
  never pair against selection-time labels. Result: 118/119 labeled (1
  replay-miss, the known ~1% class); per-bin winnable 0.875 / coin 0.551
  / long_shot 0.298 / lost 0.047. **Instrument cross-check:** on the 99
  games shared with v1's holdout, this re-measure agrees with v1's
  baseline eval within noise in every bin (winnable 0.869 vs 0.875, lost
  0.044 vs 0.044) — reproducible. Side-finding: v1's famous winnable
  regression (0.865→0.794) lived entirely in the 44 winnable games v2's
  draw did NOT pick; the 22 picked re-measure at their selection-time
  level. Regression-to-mean is per-game, and the pairing protocol
  (baseline_eval rows, never map labels) is what makes that harmless.
- **Selection v2 (`data/runs/drill-selection-v2/`):** same label sources
  (map + 4 sweep arms), holdout = evalset v2. **409 training points**
  (370 band + 39 above / 41 excluded), mean fork winrate 0.470 (v1:
  349 pts, 0.416). Composition by map bin: lost 249 / long_shot 83 /
  **coin 33 / winnable 44 ⇒ 18.8% ahead-positions** (mean sel_wr 0.714;
  the winnable pick via the above-band rule, coin mostly in-band).
  Accounting exact: 119 + 409 + 41 = 569 usable; the 77 games released
  from v1's holdout are exactly the 77 ahead-points in training;
  selection ∩ v2-holdout = 0.
  - Rotation check: per-15 slices carry 2–4 ahead points, one zero-ahead
    slice in a 29-slice cycle — with §6d fresh→replay aging smoothing
    across iterations, no driver-side stratified interleave needed.
- **d6-run11 LAUNCHED (the (a2) run, `data/training/run11-launch.sh`):**
  run10 recipe **verbatim** except the three deltas: name, fresh seed
  base 20261031, `drill-selection-v2`. Init run9-i009 (train + critic),
  mainline replay pinned run9-i009, 20 iterations ≈ ~14 h, ADR-0017
  guards, arms every 5, driver self-registers with `anvil-watch.timer`
  (verified active). One variable changed vs run10: mixture composition.
- **Gate (standing):** paired 2,000-game corrected read vs 0.5121;
  post-hoc per-bin `grindstone eval` on candidates **against evalset v2**
  (v1 stays valid for run10-era ckpts only). The crisp hypothesis on the
  table: run10's winnable −9.7pp was the balancing term; ahead-weight in
  the mixture converts the held-out lost/long_shot gains (+6.6/+6.7pp)
  into net strength. Escalation (b) stays armed behind an (a2) negative.

## Broke / surprised me

- First baseline-eval launch used bare `python` → system interpreter →
  bridge server died on `import grpc`. The grindstone verbs that worked
  earlier just don't import grpc. Relaunched with `.venv/bin/python`.
  (Lesson: anything that reaches `_start_server` needs the venv
  interpreter explicitly when launched outside `uv run`.)

## Next session picks up

- run11 telemetry glances (babysit cadence ~1–2 h; watcher armed), then
  the standing closing protocol: 2,000-game paired read vs 0.5121 +
  evalset-v2 decomposition on candidate ckpts → (a2) resolution ADR.
- Chronicle: D3 reveal scene + D4 Android spike (parallel-early).
