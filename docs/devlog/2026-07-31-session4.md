# 2026-07-31 — session 4: Chronicle D1 built and gated

## What was done

**Chronicle D1 (headless core) is BUILT and the done-when gate is GREEN**
(fork `playable` @ `574abedc08`) — the simulated-fortnight test passes on the
shipped data files and the real card DB, alongside the per-service unit tests
(30 new tests, 4 classes); full desktop suite 400 green. Chronicle remains
invisible (no menu entry — that's D2's pref-gated hook).

### Archaeology first (4 parallel scouts over the fork)

- **Seed integrity works today**: every RNG site on the 1993–94 booster path
  routes through `MyRandom`, which the determinism-hooks rebase made
  thread-local + settable — `MyRandom.setRandom(new Random(contentsSeed))`
  with save/restore around `BoosterGenerator.getBoosterPack(template)` gives
  committed pack contents. Reseed per pack (the foil rejection loop burns a
  variable draw count even for `Foil=NotSupported` sets). One landmine noted
  for post-MVP eras: `BoosterSlot.replaceSlot()` uses raw `Math.random()` —
  unreachable before ~2018 sets, must be fixed upstream before the window
  crosses that line (comment pinned in `ChroniclePackGenerator`).
- **`SaveFileData` is libGDX-bound** (module cycle + Pixmap/Vector2 overloads)
  ⇒ headless port `ChronicleSaveData` in forge-gui, keeping the
  `DecompressibleInputStream` migration substrate and the IOException-sentinel
  protocol, dropping the toolkit overloads. Versioning: Quest's explicit
  version-int ladder (Adventure has NO version number — its shims are
  unbounded) + Adventure's containsKey defensiveness. GZIP container,
  header-first for the slot browser.
- **No FModel entanglement**: Quest's `FModel.getQuest()` has 238 call sites —
  the anti-pattern. Chronicle follows Adventure's own-singleton shape;
  shared-file hooks so far = **3 additive lines in `ForgeConstants`**, zero
  elsewhere (dir creation via the QuestDataIO static-init idiom, so no
  PROFILE_DIRS edit either).
- **Test infra**: desktop tree only; `AITest` base gives the one-time card-DB
  load (~3.4 s shared per JVM); tests see the real `forge-gui/res/`; save IO
  is File-pathable so tests use temp dirs, never the real profile.
- **Edition data confirmed**: all 9 window sets carry era-authentic `Booster=`
  lines (8-card ARN/ATQ/DRK/FEM, `UncommonRare` combined sheets, † and
  a/b/c/d art variants); starters exist for the four core sets only
  (historically correct); ATQ prints the same name at split rarities
  (Urza's Mine 83a/b U vs 83c/d C) ⇒ notables priced by NAME, never
  name+rarity; collation identity stays at the PaperCard key.

### Built (fork, `forge-gui/.../gamemodes/chronicle/` + `res/chronicle/`)

- `ChronicleSeeds` — (run seed, day index, domain) / (run seed, domain,
  item id) derivation, FNV-1a + SplitMix64 finalizer.
- `ChronicleTimeline` — played-day tick fired on ration collection, ≤1 per
  real calendar day (local + 4am grace), first collection = day 0, skipped
  days don't happen; **monotonic effective-day guard** (see below).
- `ChronicleRelease`/`ChronicleCalendar` — release calendar w/ in-print /
  releasing-on / leaving-within queries (the paper's event hooks).
- `ChronicleConfig`/`ChroniclePricing`/`ChronicleWallet` — knobs; buylist =
  rarity base × name-keyed notable multipliers; stipend on the played-day
  schedule, payday 0 first (funds the day-one starter).
- `ChronicleCollection` — printing×finish inventory on forge-core `CardPool`,
  NEW-badge tracking (per-card clear-on-seen + ever-seen set), per-set
  completion vs an injected universe; persistence = sorted identity lines
  re-resolved at load (saves survive card-DB updates; resolver injectable).
- `SealedItem`/`ChronicleSealedInventory` — monotonic ids; **contents seed
  committed at acquisition**; boxes materialize at purchase as component
  boosters, each with its own committed seed (box-as-item deferred to stage 2;
  the commitment property stage 2 needs is already built).
- `ChronicleLgs` — seeded daily stock roll (4 slots, 0–15% discounts,
  booster/box/starter mix), Adventure's seed-not-inventory pattern but with a
  LOCAL Random (their version mutates a shared world RNG — fragile, not
  copied); only purchase counters persist.
- `ChroniclePackGenerator` — committed-seed MyRandom wrapper; bypasses the
  `fromSet` wrappers (they burn extra RNG).
- `ChroniclePackEv` — the Ante EV ledger stub: **exact** expected buylist
  value from the real print sheets (weighted mean × slot count).
- `ChronicleRun`/`ChronicleController` — aggregate root with run-id +
  **separate meta blob** (prestige-proof schema); daily-session orchestration
  (collect→stipend→ration, shelf MSRP + LGS deals, open, sell, autosave),
  injectable Clock; `ChronicleSaveIO` — version int + header + main, GZIP,
  backup/sentinel/restore write protocol, header-only slot-browser read.
- **Data files** (`res/chronicle/`, ship automatically with the res tree):
  `releases.txt` (9 products; Alpha day 0 shelf 7 / Beta 14 / FEM 56 glut;
  MSRPs $2.45/$1.45/$8.95/box $79.95), `economy.txt` (ration 2 packs,
  stipend $10 per 7 played days, buylist 1/2/10/40¢), `notables.txt` (the
  1994 tier table = the famous-card allowlist file's MVP column: Lotus ×30 =
  $12 buylist ≈ the period-correct dozen packs; P9 ×20, duals ×8, Juzám ×10,
  13 notables ×5, 15 staples ×2 — every name grep-verified against the
  edition files; Juzám keeps its accent in Forge).

### Gates (all green)

- `ChronicleCoreTest` (15, pure): seeds, tick semantics incl. the grace
  boundary at exactly 04:00, calendar windows, parsers, stipend pay-once,
  save-data/IO incl. sentinel refusal + garbage handling, LGS determinism +
  sell-out + day reset.
- `ChroniclePackTest` (7, DB): same seed ⇒ byte-identical pack; era sizes
  (15/8/8/60); all calendar products have templates; **every shipped notable
  resolves in the card DB** (a typo would silently price the game's best pull
  as bulk); collection roundtrip w/ NEW flags; completion vs the ARN
  universe; committed seeds survive reload.
- `ChroniclePackEvTest` (2, DB): **pack-EV-negative invariant holds for every
  product even at the max LGS discount and at the per-pack box price**; exact
  EV agrees with the empirical mean of 400 generated packs (the ledger is
  downstream-verified against the generator it describes).
- `ChronicleFortnightTest` (3, DB): **the D1 gate** — 14 scripted days: day-0
  starter moment (stipend 1000 → starter 895 → 60 cards), Alpha last-chance
  warning, day-7 shelf turnover (Alpha off, Beta releases) + payday,
  mid-fortnight save at day 7, days 8–13 replayed from the reload with
  **digest-identical continuation** (packs, stock, economy, meta); plus
  reload-can-never-reroll-a-pull and autosave-loads.

### Found en route (the suite caught a real design hole)

The tick guard was first written as effective-day *inequality*; the
grace-window test tripped it and exposed the exploit: **winding the device
clock backwards a day would re-arm the tick**. Fixed to a strictly monotonic
comparison — clock rollback can never re-arm; the grace window still covers
the honest midnight opener. The seed-integrity pin protected pulls from
savescumming, but the tick needed its own monotonicity to resist clock
scumming — recorded as an addendum on the plan's pin 5.

## State / next

- **D1 done-when: SATISFIED** (fortnight green in the desktop tree, unit
  tests per service; full desktop suite 400 green — ForgeConstants was the
  only shared file touched, 3 additive lines). Pushed as `574abedc08`.

## Same session, part two: D2 screens v1 BUILT (`bb7e670cff`)

Three more scouts (screen framework/menu/prefs, ItemManager+ImageView
internals, Quest screen patterns), then the full D2 surface:

- **Pref-gated entry**: `CHRONICLE_MODE_ENABLED` (default OFF) — New Game
  menu item skipped in `buildMenu()` (re-runs per open, so the Settings
  toggle applies without restart); Chronicle never persists as the preferred
  New Game screen (the Adventure rule); Settings→General toggle; en-US keys
  (other locales fall back per Localizer).
- **Screens** (`forge-gui-mobile/src/forge/chronicle/`): home (session-script
  spine: status, collect-ration flow w/ product choice → day-summary dialog →
  open-now), binder shelf (booster-art tiles + completion bars + NEW counts),
  binder spread (full-set collector-order grid), LGS (MSRP shelf + seeded
  deals w/ sold-out), paper (masthead + headlines + store notes + flavor),
  and a deliberately-placeholder tap-through opener (D3 owns the real
  reveal). `ChronicleHub` = Adventure-style own singleton; all controller
  work off the GL thread via `LoadingOverlay.runBackgroundTask`.
- **The four binder gaps closed generically** (upstreamable): ImageView
  gained NEW/OWNED column-override plumbs — modeled on the LIVE `fnPrice`
  pattern, not the dead `fnIsFavorite` one the scout caught — driving the
  NEW badge, owned-count pips, and unowned rendering (grayscale via the
  existing `Graphics.drawCardImage(..., drawGrayscale, ...)` when the image
  is on disk; dimmed composed placeholder otherwise — deliberately NO
  network fetch for unowned cards); group captions get a composer hook for
  completion stats; `ItemManagerConfig.CHRONICLE_BINDER` (image view,
  collector-order sort, `pileBy=null` — pile-by renders fanned stacks, not
  grids, another scout catch).
- **Headless additions** (desktop-tree tested): `ChroniclePaper` (templated
  issue + seeded era flavor, 15 period lines in `paper-flavor.txt`),
  `ChronicleData.setUniverse` (collector-order printing resolver — the test
  pins the ATQ split-rarity gotcha: all four Urza's Mine variants distinct),
  `ChronicleController.markSeen` batch (spread exit → one autosave).
- **Gates**: suite now 32 green; mobile-dev jar packages and boots clean
  (assets resolve from the module dir — running from repo root picks the
  wrong `../forge-gui`, worth remembering). **D2 done-when (daily session
  start-to-finish on mobile-dev) is OWED to the author's hands** — the
  XTEST/ydotool wall makes click-through unautomatable, same as QoL items
  4+6. Script for the pass: Settings→General→Enable Chronicle → New
  Game→Chronicle → Begin → collect ration → open packs → store → binder.

- Next: the **human D2 pass**, then **D3 reveal scene** (bar pinned in the
  sketch) and the **D4 Android spike** (parallel-early). Push of
  `bb7e670cff` pending user say-so (D1's push was explicitly asked-for;
  not generalizing it).
- Carried notes: box-as-item for stage 2; `BoosterSlot.replaceSlot()`
  Math.random landmine before any post-2018 window; `kind` field
  (rider/stream) ships dormant as designed; per-cell grayscale batch
  flushes = a measured-on-device question for the D4 spike.
