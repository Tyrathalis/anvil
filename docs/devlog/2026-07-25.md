# 2026-07-25 — run-8 concludes on a guard halt with a new high-water; the pings get real

Covers the run-8 shepherding arc since [2026-07-23-session2](2026-07-23-session2.md)
(one continuous session across 07-23 → 25).

## d6-run8 (960 g/iter, traj-per-step 8, init run7b iter-014): GUARD HALT iter 18, best-by-arms i14 = 0.5675

- **Trajectory (400-game arms, same paired seeds as every run):** i4 0.5325
  → i9 0.5275 → **i14 0.5675 (highest arms read of any run)** — no run-7b-
  style early dip, flat consolidation through i9, then the tail SEPARATED
  UPWARD where run-7b's declined (0.5625 → 0.5475). The batch lever's
  prediction (credit noise binding ⇒ halved-variance updates climb late)
  is what the shape shows.
- **Ending: guard halt at iteration 18** — veto 0.217 > 1.5× baseline
  (0.1387), KL 3× step to 0.033, and the §6 anomaly rule fired WITH it
  (shaped reward 0.383 vs masked head 0.492 = penalty mass from the veto
  blowup). iter-018 REJECTED, iters 0–17 accepted. The tail oscillated
  wider than run-7b all along (veto 0.11↔0.18 vs 7b's 0.08↔0.13):
  2× batch at lr 1e-5 is NEAR the stability edge, not comfortably inside
  it — consistent with ADR-0017's lr-per-signal-regime, now with a
  batch-size axis.
- No i19 arms (halted first). 18 accepted iterations, zero mid-run
  incidents, fallbacks/tripwire flags in the known-benign bands all run.
- **2,000-game closing read LAUNCHED on `d6-run8/iter-014/train/last.pt`**
  (`final_read.py`, name run8-best, detached + watcher). Decision read:
  paired vs run7b iter-014 (ckpt of record). Lands overnight.

## The pings weren't real — found by the user asking "have you actually gotten any?"

- **Zero driver-narration lines reached driver.log in 36h**: a detached
  Python driver's stdout to a redirected file is BLOCK-buffered; every
  `===== iteration` marker sat in memory while subprocess output (own
  fds) interleaved normally. The log-tail monitor's filter had nothing to
  match — silence indistinguishable from health. Terminal events were
  covered regardless (exit flushes + pgrep watcher + notify-send hook
  bypasses stdout), and the exit flush at the halt delivered the entire
  36h narration in one batch, confirming the diagnosis exactly.
- Fixes: (1) mid-run telemetry now watched from `monitor.jsonl` (driver
  writes it line-buffered — verified live on iters 14–18 including the
  guard event); (2) driver `sys.stdout.reconfigure(line_buffering=True)`
  (merged); (3) memory note updated — **verify a watcher fires on a real
  event before trusting it**.
- The driver-notify rider worked at the halt (desktop notification) +
  mobile push sent.

## Landed in main (merge `f642b15`, 98 tests green)

- **VRAM elasticity (task #12), built in a worktree during the run:**
  OOM-halving retry in `rl.py`'s segmented forwards (sticks for the run;
  factory-lifted for testability — the lift caught a latent collate-import
  NameError), gradient-accumulating batch split in `finetune_value.py`
  (effective batch preserved exactly), driver per-phase seg autotune from
  free VRAM (`--rl-seg 0` = auto, default; thresholds from the run-3
  incident), driver stdout line-buffering.
- Upstream worklist: **determinism convergence recorded** — #11360
  (hash-order iteration in AI decision paths = our D1 twin-residual class,
  fixed upstream), #11358 (trigger order), #11365 (sim seed CLI), all
  merged 07-24; #11285 unaffected (MERGEABLE, awaiting review); queued
  user decision: possible complementarity comment on #11360 (deliberately
  not sent same-evening). Rebase dividend + `-XX:hashCode=3` forkcheck
  instrument noted.

## Next session picks up

1. **run8-best 2,000-game read** (report `data/runs/run8-best-arms-report.json`
   when the watcher pings): paired vs run7b-iter-014 — outside-noise win
   promotes iter-014 (run-8) to ckpt of record; either way run-8's verdict
   on the batch lever gets an ADR (stability-edge finding included).
2. D4 rebase (#11161 anchor) — now also picks up #11360/#11358/#11365.
3. User decisions queued: #11360 comment; lever plan if the read lands
   positive (one more scale step vs rebase now).
