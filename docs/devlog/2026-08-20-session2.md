# 2026-08-20 — session 2: evalset assembly decisions + the certify2 overnight launch

## Done

- **Evalset-assembly decisions pinned (user, three questions):** scale
  via an overnight ~600-job certification batch from the EXISTING
  candidate pool (no new census — the bottleneck is certification
  compute, not candidates); auto-correct drills adopted as a separate
  metric (pinned reason in m9-rung3-draft.md: D4 failure modes are
  two-sided, and the +2.0 auto-bias init would inflate a blended
  number); composition re-pinned yield-driven with a ~10/shape floor.
  Decision basis measured first: threshold sweep 1.0→2.0 moves yield
  only 12→16 (threshold-lowering confirmed dead); certified
  ranks-within-shape spread across the full top-40 ⇒ miner rank does
  not predict certifiability ⇒ deeper cuts yield flat.
- **`payment_certify.py` extended** (suite 222, +3 tests):
  - `plan --exclude` (skip previously-planned windows) +
    `--counts shape=n,…` (per-shape quota overrides);
  - `read` best-arm fix: best = best POSITIVE margin (a stronger
    negative arm could mask a cleared positive one — measured 0 jobs
    affected in batch 1; batch-1 re-read bit-identical 12/120);
  - `read` now emits `autocorrect-drills.jsonl` (`kind:
    "auto_correct"`, `best: 0`, worst arm + its negative margin);
    batch 1 yields **29** (bp 14 / ch 14 / wc 1, margins to −20.3);
  - `lanes` derives filenames from the jobs-file stem (a second batch
    can't clobber banked lane files) and resolves paths absolute (the
    cd-to-forge-gui + relative-path trap, caught at sanity check).
- **`certify2` STAGED — launch PAUSED at user request:** 600 jobs
  (bp 240 / ch 240 / wc 120; banked 120 excluded, overlap verified 0),
  14,680 paired continuations, est ~5.5h on 4 lanes at banked
  throughput. The run briefly went live (lanes verified writing
  certify rows 20s post-launch — the pipeline is proven end-to-end),
  then the user asked to pause pre-overnight: process group killed,
  watchd unregistered, partial outputs removed; staged artifacts kept
  (`certify2-jobs.jsonl`, lane scripts, `certify2-run.sh`). Relaunch =
  the full checklist again: setsid nohup `certify2-run.sh` (wrapper →
  4 lanes → concat → reader → `certify2.DONE` → self-unregister) +
  watchd register (stall 30m) + DONE-keyed waiter with per-lane stall
  detection.

## Broke / surprised me

- The first lane generation embedded RELATIVE -certify/-certout paths
  (invoked with a relative --jobs; the scripts cd to forge-gui) —
  caught before launch, `lanes` now resolves absolute.

## Next session picks up

1. **Read certify2** (`certify2-read.log` / certified2 + autocorrect2):
   check per-shape yield vs the 7.5%/5%/17.5% projection and the
   ~10/shape floor; merge batches 1+2 into the evalset of record.
2. Hand-built phyrexian drills (10–15, incl. the pool-tie board) +
   hand-built forced/Signet boards (ADR-0065 + variants, ddmin rule).
3. D4 accuracy scorer over certified+autocorrect (per-kind accuracy,
   argmax lands in the certified-best class).
4. D4 gate session (gates PIN-AT-DESIGN).
