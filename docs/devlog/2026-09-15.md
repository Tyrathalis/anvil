# 2026-09-15 — community watch: the Discord read (pre-release freeze, tinymtg, card win rates, GUI-owned combat legality)

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- Read the user's paste of #ai-plotting (09-14 → 09-15) and #contribution-questions (09-15); banked
  in the survey doc's 09-14/15 follow-up ([discord-ai-plotting-survey.md](../design/discord-ai-plotting-survey.md))
  and the plan's running record. Nothing posted from here.
- Significant for us, in order: (1) **Jetz confirmed combat / damage-assignment legality is
  GUI-owned** and that moving it into forge-game is desirable but large — the field guide's
  "the engine never validates AI combat" from a maintainer; an engine-side legality surface
  (our block requirement fixed-point + the modern-rule lethal assignment) routed to the upstream
  worklist for the rebase era. (2) **chrismaghuhn's pre-release freeze** — a cheap external
  validation protocol for a Limited set; routed as a closeout-era read. (3) **LordOfThePigs's
  card win-rate table + Bo1 outcomes** on Drive — a Tutor asset, not downloaded. (4) talor's
  `tinymtg` and itemfive's `mtgish` — noted; the Rust-subset item stays gated. (5) manabrew is
  dropping its Rust port — the survey table corrected.
- Box idle; no runs in flight; the fork pin unchanged (`e63a0cac20`).

## Broke / surprised me

- Nothing ran.

## Next session picks up

- Unchanged from 09-15 night: the partial-expansion slot, Build 3's closeout ADR, the rebase, Build 4.
- If the user wants LordOfThePigs's data: the Drive links are in #ai-plotting (09-15 05:17 / 05:42);
  pull into `data/external/` with provenance when Tutor opens.
