# 2026-08-05 (session 2): M6 OPENED — the representation question (ADR-0038 + m6-plan.md)

## Done

- **M6 planning session, structure user-agreed and OPENED same session**
  ([ADR-0038](../decisions/ADR-0038-m6-opening-sequence.md) +
  [m6-plan.md](../design/m6-plan.md)). The milestone question: can we
  change what the gradient sees — and does that reopen improvement?
  Headline gate = standing combined paired read vs 0.5316, honest
  either way.
- **The shape is probe-then-path:** D1 = the deciding probe (fresh
  heads — ridge/k-NN/small MLP with learning curves at 500/1K/2K/all —
  on the 3,750 banked K=8 labels vs frozen trunk features from BOTH
  trunks, per-era on the `critic_calibration.py` split). Pre-registered
  thresholds: held-out Spearman ≥~0.7 rising ⇒ path A (rollout-label
  value targets; ADR-0015 machinery unparked; critic replacement);
  ≤~0.4 FLAT ⇒ path B (encoder enrichment / partial unfreeze, gated
  behind its own design session); between ⇒ price both. The
  learning-curve guard exists because ~3,000 training labels could
  false-negative an MLP even where features carry the signal.
- **Efficiency discussion resolved into the plan (user round):** the
  in-loop frontier is closed by measurement, not neglect (ADR-0032
  serve-path negative, w=16 harvested, learner 4.7×) — so no
  speculative loop tuning; iteration pipelining rejected as an
  on-policyness recipe delta; eval thinning rejected on the run12
  seed-half lesson. The one efficiency item with a guaranteed customer:
  **the labeling re-price** (ADR-0015's ~17 pos/h/worker was
  batch-1-server-era; micro-batching + the w=16 recipe never touched
  the labeler path; 2–4× was called plausible then) — it opens D2 under
  path A, with the carried fork stability pass (IndexOOB, targeting
  retry, MayPlay `.get(0)`, MinMaxBlocker) as the pre-campaign gate.
- **Curriculum composition = D3, an independent arm** (user-agreed):
  recomposes selection v3 (still valid — no M5 promotion) against the
  winnable −5.1pp residual; never folded into D2, so both levers keep
  clean attribution.
- **D4 riders:** isotonic wiring into curation/doom tooling early and
  unconditionally (ADR-0036 adopted the maps but nothing consumes them
  yet); Ante correctness items ride any critic replacement.
- **Disk pass scheduled as the opening chore (user-agreed):** inventory
  found ~150G of 213G `data/runs` is six forensic dumps from closed
  runs (run2-i000/i005 53G, run4-i000/i001 46G, run9-i002 18G,
  run11-i011 22G, run10-i007 7.4G, + `_bench_scratch`/`_contaminated`)
  — every question they answered has a closed ADR. Kill list → user
  sign-off → delete or zstd cold archive. Keep-list pinned in the plan
  (ckpts of record, drills.jsonl dirs, selection/evalset assets,
  baseline-era finalarm/confarm stores, Ante certs).
- **Pool expansion explicitly deferred (user confirmed the instinct):**
  expansion rides on text-embedding generalization — exactly the
  representation under indictment — so the D1 probe doubles as the
  first measured evidence for when new cards are safe. Also a
  quasi-dataset-boundary event that would confound every paired read.
- **Docs:** ADR-0038, m6-plan.md, CLAUDE.md M6-OPEN bullet, map swept
  (header pill, start-here pointer, Now panel → M6 open w/ done-when,
  M6 `<details open>` block, ADR index, run-ledger next row, footer
  stamp). The sweep also caught three stale spots left from the M5
  close: the 51.2% stat tile (→53.2%), the w=8 throughput tile (→w=16
  recipe numbers), and the drill-assets state-of-record row (v2→v3).

## Broke / surprised me

- Nothing broke (docs-only session). The surprise was how much of the
  disk footprint (~70%) is closed-run forensics nobody will reopen —
  worth the standing habit of pruning at milestone close, which the
  M6 opening chore now models.

## Next session picks up

- **D1: the frozen-trunk ranking probe.** Build the feature-dump path
  (frozen trunk pooled features at the 3,750 labeled positions, both
  trunks, riding the `critic_calibration.py` trace join), then the
  probe family + learning curves; resolve against the pre-registered
  thresholds in an ADR.
- The disk-pass kill list can run as a side chore any session (needs
  user sign-off before deletion).
