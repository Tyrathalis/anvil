# 2026-08-21 (session 2) — payment evalset of record assembled

Parallel-session note: the Arena of Glory enumerator diagnosis ran in a
separate instance concurrently; this session stayed off the fork and the
salvage family except to route one overlap finding to it (below).

## Done

- **`payment-evalset-v1` ASSEMBLED — the M9 rung-3 payment drill evalset
  of record** (`data/runs/payment-evalset-v1/`): **56 positive drills
  (bp 13 / ch 26 / wc 17, all shapes ≥ the 10 floor) + 192 auto-correct
  (bp 78 / ch 106 / wc 8) + 1 held**, from certification batches 1+2+3
  over `run-20260820-paygoals3`. Zero duplicate windows across the 249
  banked rows (planning exclusion held). README.md in the dir carries
  the session decisions; meta.json the mechanical record.
- **Auto-correct cap decision: NONE — all mergeable rows in (192/193).**
  Recorded reason: the metric is scored separately so N cannot inflate
  the headline; larger N strictly tightens the deviates-wrongly read
  (~5% wrong-deviation ≈ 10 drills at N=192, invisible at N≈50); eval
  cost negligible. wc auto-correct (8) is indicative-only, never a gate
  input.
- **Merge instrument built as a standing subcommand**
  (`payment_certify.py evalset`, +2 tests, suite 222→224): merges
  batches with batch provenance, fails loud on cross-batch duplicate
  windows, checks the per-shape floor, and **re-derives salvage-suspect
  drills from the certout files itself** — any drill whose job carried a
  fired non-directed_ok arm is HELD with its exec_why, not merged
  (the drill's verdict rests on an unverified arm) and routed by name
  in meta.json. Future merges auto-catch the class.
- **The one suspect found: b3 job 202** (auto_correct, color_hold,
  pair-004 g1 seed 20264705, t19, Aragorn) — arms 1–6 directed_ok 8/8,
  arm 7 deterministic salvage `costs:Arena of Glory#4@5` 8/8. Its
  "auto is best" claim is incomplete until arm 7 is verified. Held in
  `held-drills.jsonl`; repro + routing sent to the diagnosis instance
  (re-adjudicate the single job post-fix, re-run the merge).

## Broke / surprised me

- Nothing broke. The only drill-vs-salvage overlap in 960 jobs was the
  one held row — the directed_ok-only certification rule kept the other
  39 Arena salvage rows out of every verdict on its own.

## Same session, later: the held drill resolved (job 202 RETIRED)

The Arena of Glory diagnosis landed as ADR-0067 (fork `b49e95eea8`:
`chainOrderFeasible` was count-based/color-blind/self-paying — job 202's
arm 7 was REAL, blocked by a Shivan Reef unit materialized U that the
old check treated as "could be red"). Re-adjudication on the fixed jar
(rows from the diagnosis session's verbatim re-run, banked as
`certify3r202.out.jsonl` + a 1-job reader pass beside the batch files):
**failed_predicate** — all 7 arms directed_ok, but no arm clears
2.0 @ 0.75 consistency. The b3 auto-correct verdict (worst arm 5,
−5.125) did NOT reproduce (now −0.625 @ 0.88); the newly-executable
arm 7 is noise (+0.125 @ 0.50). The −5.125 was continuation-luck; the
window is noise and leaves the evalset entirely.

Mechanism: `evalset --retire BATCH:JOB=REASON` added (+1 test, suite
225) — a drill removed by name after external re-adjudication, recorded
in meta.json, never silently dropped. Re-merge: **56 positive + 192
auto-correct unchanged, held empty, 1 retired.** The hold was the right
call either way — the row would have been wrong-by-luck evidence in the
auto-correct metric.

## Next session picks up

1. Hand-built phyrexian drills (10–15, incl. the §12a pool-tie board) +
   hand-built forced/Signet boards (ADR-0065, ddmin rule).
2. D4 accuracy scorer over positive + auto-correct (per-kind).
3. D4 gate session (gates PIN-AT-DESIGN; owes the ADR-0067
   paygoals4-vs-accept-the-bound routing).
