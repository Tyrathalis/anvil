# 2026-08-10 (index fix landed; M6 closed / M7 opened; P0 probe resolved NOT MET)

## Done

- **Fork-index namespace fix LANDED (fork `a73ee9d4e4`, pushed):** the
  chip from the run13 crash, taken now because we sit exactly at the era
  boundary the deferral was waiting for. Root cause located at
  `AnvilRun.java` (synthetic id `(gameIdx*100+fp)*100+r` collides with
  mainline indices iff gameIdx=0); verified the id is a PURE join key
  (nothing decodes it on either side — provenance travels in the fork
  header's pg/fp/r; no int32 packing, no modular splits, noise seeds key
  on game_seed not g). Fix: `FORK_G_BASE = 1e12` + `synthG` int→long
  through Obs (`forkGameIdx`, `buildHeader`). **Smoke: a real gameIdx=0
  `-forkobs` run → fork ids at 1e12+, mainline+fork ingest, MultiStore
  join passes (the exact run13 crash operation), labels cross-check
  agrees.** Era-scoped: pre-base stores keep un-based ids; never mix
  eras in one MultiStore join.
- **M6 CLOSED / M7 OPENED (user-approved,
  [ADR-0050](../decisions/ADR-0050-m6-closeout.md) +
  [m7-plan.md](../design/m7-plan.md)):** M6's charter is answered
  (representation partially reachable, not binding; bottleneck =
  learning-signal density per ADR-0049). M7 = the credit-assignment
  question. User pins recorded BEFORE numbers: P0 gate (split frac
  ≥ 0.30 AND RMS true Δwr ≥ 0.10 + directional check), boundary lands
  after P0 clears, milestone split. Status bullet archived verbatim;
  map swept (pills, Now card, state-of-record, ledger, ADR index,
  details blocks, stamp).
- **P0 decision-delta probe BUILT + RESOLVED
  ([ADR-0051](../decisions/ADR-0051-p0-decision-delta-probe.md),
  `scripts/decision_delta_probe.py`, zero box time): gate NOT MET.**
  Design insight that made it free: within a fork point the K=8 sampled
  completions share an identical state, so first-action-conditioned
  outcome differentials are unconfounded — no forced-action machinery
  needed, run13's in-loop fork stores already contain the data.
  Results: run13 era 299 points — split frac **4.0%** (the sampled
  policy is confident at ~95% of drilled decisions), RMS true Δwr 0.082
  (n=12, weak); old-era support 797 points — split frac 6.5%, **RMS
  true 0.000** (observed spread fully explained by binomial noise);
  directional hold-then-act read null both eras. Power arithmetic: the
  pinned 0.10 threshold sits BELOW the estimator's noise floor at any
  achievable split count — the method fails as an instrument.

## The read

The pre-registration paid out in one session: a decisive negative at
zero cost, with the routing already agreed. Natural-variation dense
labeling is dead; the antidote must be search-shaped. The concrete D2
candidate — forced-branch paired rollouts at drilled decisions (force
act vs hold post-fork, K per branch, paired seeds, K = a resolution
dial) — is simultaneously tier-3's first rung, design §6's dormant
contrastive-pair generator, and the instrument P0 lacked. C2a (fork-
point wr aux value targets) rides with any run; C3 (§6c re-tune) is
untouched by P0. D3's trigger re-decided: the stability pass rides with
the forced-branch build.

## Next session picks up

1. **Forced-action harness design** (with the user): scripted first
   decision post-fork in AnvilRun/the bridge path; then the
   paired-variance sizing read at small K on a handful of drills —
   prices the K dial before any campaign.
2. **C3 pre-work read** (cheap, run13 stores): decompose the 3.8–8.3
   rejected-intents/trajectory into re-ask chains vs independent events
   → λ decay vs first-attempt-only pricing.
3. Productionize the audit's scratch instruments (behavioral-delta /
   holding reads) into `scripts/` as standing per-run instruments.
4. ~~M6 closeout stale-data pass~~ DONE (user signed off same session):
   six runaway single-worker `census.jsonl` files trimmed (run13
   i006/i008h0/i014 + pre-M6 leftovers run4-i008/run5-i017/run7-i005h0,
   dirs and obs/mu kept — the standing cheap-prune pattern, now 6/6
   occurrences) + six raw generation dirs whose products are
   ingested/merged (c2ext-s0/s1, c2t3-s0/s1, cycle3-s0/s1 run dirs;
   labels in `data/runs/labelset-c2-v3`, stock in
   `data/trajectories/cycle3-*`). `data/runs` 83G → 63G (~20G freed).
   Kept-asset verification: labelset-c2-v3, frozen-probe stores,
   unfreeze-probe instruments, finalarm/confarm stores, tranche-c2
   record, all 21 cycle3 drillmix fork stores — all present.
