# 2026-09-19 (M12) — route (b) re-scoped: the void-rescue instrument (ADR-0114)

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **The review (user: "route (b) is up next; any decisions before we proceed?")** read the fork
  and the pools before building and found route (b)'s premise false on a network arm. ADR-0111's
  04:40 addendum said search copies realize the forced option through the heuristic's planner
  and the mainline through the model's CastPlan. The fork: a copy's seats get their controllers
  from `AnvilLobbyPlayer` with the mainline's bridged tags, so the copy's forced window is a
  single-option ask to the model realized by `CastPlanRealizer`; `heuristicRealize` fires only
  on the unbridged control arm (`!bridged(TAG_PRIORITY)`, `searchForcedAsk`'s control path) and
  behind the off `-vetofallback`. The pool: 88,395 of 90,955 void first-ply copies on
  `b4-tgtlab` made exactly one forward call — the forced ask. The census: the act arm's 14%
  "mainline" veto rate (ADR-0112, banked) counts the copies' forced asks (622K single-rung asks
  vs the on arm's 58K) — the void class again. So the leaf values are under the model's own
  plan, the acted window's label is that plan (ADR-0113 decision 2 already relies on it), and
  there is no train/serve plan gap to distill across. The re-warm's harm mechanism stands on the
  fallback read. **What flips is the Build 5 risk's sign: the search's ceiling is the decoder's
  coverage** — 35.8% of first-ply candidate copies void (43.8% of spells, 33.7% of abilities,
  1.1% of lands; 98% `no_shape_fit`), never valued, never actable.
- **Decisions (user: the recommendations):** route (b) as written closed; its replacement
  measured before it is built — the void-rescue instrument, read-only; recorded as
  [ADR-0114](../decisions/ADR-0114-m12-route-b-rescoped-void-rescue.md) (a new ADR, since it
  reverses an accepted finding); the standing-rules line under ADR-0111 struck and rewritten, a
  coverage-bound rule born, the Now block corrected, ADR-0112's banked item closed.
- **Built (fork `57337a7e38` on the pin `a37bc6a8b4`):** `-searchvoidrescue` — a first-ply
  candidate whose roll-0 copy voided gets one more copy on the same roll seed with the forced
  option realized by the heuristic's planner on the bridged seat
  (`SearchDirective.heuristicForce` → `PlayerControllerAnvil.heuristicForce`: `canPlaySa`, the
  plan via the new public `Obs.planJson`, or the `AiPlayDecision` refusal); `searchVoid` takes a
  reason and `OneShot` carries the realizer's veto code, so every void copy records why (`vr`:
  `no_shape_fit` / `dangling_ref` / `illegal` / `unpayable` / `pass_masked` / `veto_cap` /
  `no_oneshot` / `heur_refuse`); the rescue lands under the option's `h {kind, v, calls, ms,
  plan, refuse}`, outside the value arrays the acting rule reads; census `searchRescue`; header
  pin `voidrescue`. Off = byte-identical. Python: `scripts/void_rescue_read.py` (the void class
  by reason / group, the rescue rate + refusals, the rescued leaf value vs the best valued
  candidate and the natural on the same roll seed, the windows clearing the bar, the price, the
  plan census), the `vrescue` arm in `build3_surface_read.sh`, `scripts/void_rescue_chain.sh`.
- **Smoke (4 games, the recipe, 4 workers × 1 server):** 139 rows, 117 void candidates / 117
  instrumented, 33 rescued to a leaf, 84 refused (`CantPlayAi`), every void `no_shape_fit` but
  two `unpayable`, every rescued plan targeted (e.g. "target opponent reveals their hand" —
  a player target the decoder could not shape), +3.9% calls; 3/139 windows clear the bar over
  the best valued candidate (right at the pre-registered 2% — the real read decides).
- **Launched 20:31 through `anvil.runs`** (`build4-voidrescue-chain`, `data/runs/build4-voidrescue/`,
  `--watch` on the arm dirs + the forkcheck dir; check-in self-test OK): forkcheck
  `run-20260919-build4-voidrescue` (detached) ∥ the arm `b4vr` (150 / seat vs the heuristic,
  the recipe WITHOUT the allocation head + the instrument, 24 × 2) → the read
  (`data/runs/build3-surface-read-b4vr/void-rescue-read.{txt,json}`) → the forkcheck compare.
- **Discord (Kryptic's four-deck run; user):** the cross-deck over-generalization hypothesis
  (one deck's tactics applied to all). From the workbook alone it is not separable from "burn is
  the deck imitation learns worst"; the one signal in hand is his single-deck Green mirror
  (55.2%) vs the pooled run's Green mirror (≈ 49–50%) on more Green games — the negative-transfer
  shape; the discriminating read is a per-deck decision census on his arm stores (our store
  format), the decisive test a fine-tune of the pooled ckpt on one mirror. Not addressed now;
  kept in mind for our own reads (a memory note).

## Broke / surprised me

- The re-warm session's fork read was wrong about which seat realizes a copy's forced option;
  the store's `calls` column on void copies (exactly one) would have shown it in a minute. A
  standing habit from here: a mechanism claim about the copies is checked against the rows'
  per-copy call counts before it is written down.
- The `-search` arm's census-level veto rate is not a mainline number on any searched arm.

## Next session picks up

- **The read** (`void-rescue-read.txt` in the b4vr read dir; the chain's `queue.log`): the
  pre-registered rule — the acting extension if rescues clear the bar on ≥ 2% of searched windows
  at ≤ +30% calls, else route (b) closes on the number. Then the forkcheck verdict → the fork pin
  (`57337a7e38` if PASS); the state-of-record table row.
- If built: the rescued values as candidates in the acting rule, the mainline playing the
  heuristic's plan on acted rescue picks (the built `-vetofallback` path restricted), the plan
  label = that plan, the pick-distillation CE extended to the plan factors; a 600-game paired read.
- Then the documentation pass (Build order 4¾) → the shakedown through `selfplay.py`.
- Scratch to delete on sign-off: `data/runs/vrsmoke*` + their stores.
