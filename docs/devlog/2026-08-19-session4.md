# 2026-08-19 — session 4 (M9 D3 open: scope pins + payment-completion queue + rung-1 capability audit RESOLVED)

## Done

- **D3 opened with four scope pins** (m9-plan "D3 scope pins" block,
  user-settled after the pre-decision session):
  1. **Wire = follow-up micro-decision, permanently** — not a CastPlan
     slot (origin-agnostic windows; conditioning answers the
     joint-decision worry; enumeration only cheap at the actual window
     where `ManaCostBeingPaid` exists). bridge-protocol-v0's in-plan
     payment-picks line retires at the boundary.
  2. **v1 window scope = casts + activations `effect=false`** (~61/g).
     Traffic re-measured on `run-20260704-dcpool`: `payManaCost` is
     **189/g**, not the doc's ~120 — ~73/g mana-ability
     (zero-mana/nested) windows + ~54/g `effect=true` resolution
     payments, both out of v1. Ledger item 1 corrected, new item 1a.
  3. **Cost-composition cousins OUT-graftable** (resource-commitment-
     set abstraction; `SELECT_K` already on the wire; ledger item 4
     stance recorded).
  4. **Audit-fail routing pre-registered**: scope-to-executable;
     executor funded only by a confirmed D5 mechanism.
- **Payment-completion queue born (user direction — "keep these top of
  mind until they're done")**: m9-plan section with priority order
  (1. executor completion [conditional], 2. cousins, 3. effect
  payments) + the **no-silent-loss rule** — every item routed BY NAME
  at M10 scoping and in the closeout ADR; **done-when 6 amended** to
  enforce it. Ledger items cross-reference their queue positions.
- **D3 rung 1 RESOLVED
  ([ADR-0065](../decisions/ADR-0065-d3-engine-capability-audit.md)):
  the engine CAN execute directed payments, chained activation
  included — YES branch, bundle not re-priced, queue item 1 moot.**
  - Archaeology: the blind spot is ONE candidate-filter line —
    `getAIPlayableMana` skips mana abilities with mana costs ("the AI
    will miscalculate", ComputerUtilMana.java:1668); the execution
    body (~769–777) is per-SA and generic; `CostPartMana.payAsDecided`
    re-enters the controller's `payManaCost` for nested costs ("restore
    old matrix during payment chains" + `costPaymentStack`); color
    direction reachable via express choice / `chooseColor` /
    `specifyManaCombo` (all `PlayerControllerAnvil`-overridden).
  - Empirical probe: **`DirectedPaymentAuditTest` 4/4 green** (fork,
    `forge-gui-desktop/.../ai/simulation/`, ConniveDiscardMapTest
    precedent — standing regression asset). Heuristic refuses the
    payable I+I+Dimir-Signet board vs {1}{U}{B} (Swamp positive
    control passes); the directed chain executes and casts end-to-end
    (Signet's {1} paid from float via the nested re-entry, real cast
    path pays from pool, spell resolves); express choice steers BoP to
    the directed color.
  - **v1 executor = float-then-apply**: directed mana-ability
    activations via the existing execution primitive, then pool
    payment. Zero new engine execution code.
  - **Rung-2 hard rule:** enumerate from `Card.getManaAbilities()` +
    `canPlay()`, NEVER `getAIPlayableMana` — that helper IS the
    interface trap, concretely.
  - Bonus: `AI:RemoveDeck:All` cards (Signet scripts) are **revealed,
    not stripped** (`complainCardsCantPlayWell`) — they sit in decks as
    auto-payer blanks; pool carries the class (Boros Signet = true
    chained producer). Sharpens D5's strength story + mechanically
    explains part of ADR-0064's artifact-stratum signal.

## Broke / surprised me

- The plan's `payManaCost` ~120/g was stale — real figure 189/g; the
  in-scope slice is ~61/g (sparsity budget better than feared).
- Test run needed `XAUTHORITY` alongside `DISPLAY` (the standing
  forge-jars-need-display-env lesson; `:0` alone fails on this box).
- Forge's own card scripts mark Signets AI-unusable
  (`AI:RemoveDeck:All`) — the blind spot is institutionalized upstream,
  down to deck generators avoiding the cards.

## Done (continued — rung 2 design + core build, same session)

- **Rung-2 design PINNED:
  [m9-payment-surface-spec.md](../design/m9-payment-surface-spec.md)** —
  hook (`PlayerControllerAnvil.payManaCost`, effect=false, nonzero
  mana); atoms + source-class signatures (name excluded); DFS
  enumeration with chained admissibility; flag = `|classes| ≥ 2`;
  **wire = `SELECT_ONE` + new tag `mtg.pay_mana_class`, ZERO proto
  change** (recorded simplification of the plan's "one new answer
  shape"); float-then-apply executor, reason-coded failures
  (`directed_salvage`/`directed_fail` — never a veto); telemetry incl.
  census telemetry-only mode; `K_MAX=8` + logged truncation + 5%
  revisit gate; sa_vocab untouched.
- **Rung-2 core BUILT on fork branch `m9-payment-surface`
  (`fd756cc321`; research master untouched):**
  `PaymentEnumerator.java` (enumeration + directed executor; the
  never-`getAIPlayableMana` rule enforced in code comment + structure)
  + `PaymentEnumeratorTest` **6/6 green** (single-source not
  consequential / dork-vs-land split / chained Signet class enumerated
  AND executed with exact float / Overgrowth yield split / phyrexian
  life-vs-mana split / K_MAX truncation flagged). Both payment classes
  together: **10/10**. First-run bug caught at review: paying-unit was
  hardwired to unit 0 (a Signet paying a B shard must pay with its B
  unit) — fixed pre-compile.

## Done (continued — rung 2 wiring, same session)

- **The wiring landed on the branch (`e857277117`), payment suite
  13/13:** `PlayerControllerAnvil.payManaCost` override (in-scope
  guard → enumerate → flag → bridge → execute; auto/completion via
  `ComputerUtilMana` directly to avoid census double-records);
  `mtg.pay_mana_class` tag — `GrpcBridge.selectOne` already attaches
  `Obs.lastDecForBridge()`, so the observation rides the wire with
  ZERO transport changes; obs `decBridged` carries
  cost/fpool/classes/trunc/forced + structured class labels
  (ents/pool/phy — the pointer-head substrate); census telemetry-only
  mode via `PaymentTelemetry.rec` routed from the GENERATED
  `CensusPlayerController` through a new generator `REC_OVERRIDES`
  hook (regen verified surgical — one line), armed by AnvilRun
  `-paytelemetry` (trajectory-perturbing like `-obs`; runs pin it).
- **FLAG RULE AMENDED — the wiring test caught the draft rule failing
  its own motivating example.** The I+I+Signet board has exactly ONE
  class (the chain is the only payment), so draft `|classes| ≥ 2`
  never bridged forced-chain windows. Now: `consequential :=
  |classes| ≥ 2 ∨ (|classes| ≥ 1 ∧ ¬auto-payable)` — the
  auto-payability probe only WIDENS the surface (trap-safe); forced
  windows offer {auto, class}, the auto answer fails exactly as today
  (day-zero bit-identity holds); `forced` telemetry marker. Spec §4
  amended.
- `PaymentWiringTest` 3/3: single-class auto-payable never bridges;
  chained class answer pays (signet committed, zero float residue);
  auto answer keeps the blind spot (documented through the wire).

## Done (continued — Python side, same session)

- **`scripts/payment_read.py`** — the §8 census read: scoped windows/g
  vs the 61/g budget line, consequential rate + /g, forced rate, class
  histogram, truncation rate vs the pinned 5% gate (exceeded ⇒ loud
  banner), bridged pick/exec distributions, float-residue watch;
  `--json` for machine use. Legacy census (no `-paytelemetry`) parses
  fine, reports zero coverage.
- **Obs-reader round-trip proven**: the bridged payment dec (args-kv +
  quoted opts + ret) decodes through the standing frame codec unchanged
  — additive kv needs no sv bump (the bump rides the bundle). Test
  mirrors the real `Obs.decInternal` record shape (kv under `args`,
  decBridged opts are plain quoted labels).
- **Fallback-semantics hole found + fixed (fork branch `bcd56878cf`):**
  `GrpcBridge.selectOne`'s local echo on server decline was RANDOM for
  every tag — a model server without the payment head would have picked
  random payment classes. The payment tag now echoes option 0 = auto;
  `TAG_TASK` carries the reserved-tag note (the `pay_class` task lands
  with the sub-head at rung 3/D4).
- Suites: Python **208 green** (+3), fork payment suite 13/13 with the
  echo fix compiled.

## Next session picks up

- **Rung 2 remaining (the read itself):**
  build a branch jar → census run with `-paytelemetry` →
  `scripts/payment_read.py` over the output (consequential + forced
  rate vs the ~61/g budget, truncation vs the 5% gate; standing launch
  checklist; pre-training, per the spec §8 obligation).
- **D3 rung 3: payment drill evalset** (mining rule: source tapped for
  payment consequential within 1–2 turns; ddmin fallback) — before any
  run.
- Executor-fail-path riders from the rung-1 pins are all dead — no
  re-price, no scoped-claims caveat needed in D5's attribution.
- Known v0 enumeration edges (executor-adjudicated, all logged):
  computed combo Amounts skipped; pool-snow payment untracked (atoms
  only); chain feasibility count-based (colored-exactness at the
  executor).
