# 2026-07-18 — run-5 proves the §6c pair, prices the ceiling; the upstream track ships everything

Day + overnight (into 07-19 early). d6-run5 (feature + penalty) ran its full
20-iteration schedule — the first run in the project to complete one — while
the upstream track delivered its whole M3 D3 surface in parallel.

## d6-run5 (feature + λ=0.02 rejected-intent penalty, guard-kl 0.06)

- **The §6c theory confirmed in one iteration**: first penalty-trained ckpt
  dropped sampled first-attempt veto 0.340 → 0.216; equilibrium ~0.10 by
  iter 5 (run-4's feature-only arm never bent from 0.34–0.43). casts/game
  held at baseline throughout (~51 vs 54; floor 43) — no passivity. The
  penalty self-extinguished on schedule (rej 20.8 → ~8/traj; KL 0.05 →
  0.016; the shaped-return anomaly flag converged quiet by iter 4).
- **Arms trajectory (paired seeds, 400g)**: i4 0.5400 / i9 0.5375 /
  i14 0.5225 / i19 0.4750 — strength peaked at iters 4–9 and declined
  late; iter-9 = checkpoint of interest. First-attempt veto argmax
  0.0806–0.0966 all reads — **below the D5 teacher's 0.1358 bar**
  (done-when #1 quantitative clause met; commander forensics on the iter-4
  arms showed the class collapse: Spider-Man 207→65, Aang 109→21 per 400g,
  total first-attempt vetoes −84%).
- **The 2,000-game corrected read (iter-9, exact run-3 protocol,
  `data/runs/run5-best-arms-report.json`): raw 0.5185 ± 0.0112 / corrected
  0.5171 ± 0.0110; paired vs run-3 iter-012 game-by-game: −0.30pp ± 1.12
  (t=−0.27, 246 up / 252 down) — a dead statistical tie.** The 400-game
  peak reads were noise-high.
- **Honest verdict**: the §6c pair fixed the structural veto leak at zero
  strength cost — and zero strength gain. Re-ask was already absorbing the
  drift's game cost, so eliminating it bought cleanliness (60% less wasted
  intent, exact intent-pricing, a self-extinguished penalty), not winrate —
  the D3 lesson's RL-era echo. **Done-when #2 stays open**, and the late
  decline + flat read = the first real ceiling datum: this recipe (lr 2e-5,
  480 games/iter, masked critic) does not push past iter-012. Strength now
  needs a different lever (scale, full-vis critic in the loop, rollout
  labels), not more of the same.
- Run health: 20/20 iterations, zero guard halts, entropy 0.21 → 0.13
  (glide, floor never hit), tripwire 0–2/iter (known class), fallbacks
  4–14/iter (combat-declaration class).

## Upstream track (all M3 D3 committed surface now delivered)

- **Determinism draft PR OPENED: [Card-Forge/forge#11285](https://github.com/Card-Forge/forge/pull/11285)**
  (branch `determinism-hooks`, `f4fc37db3e`): InheritableThreadLocal
  MyRandom + sorted deck-section iteration in `preparePlayerZone` —
  re-authored from manabrew's `d658cbc757` with credit
  (`Co-authored-by: Emanuele`), two refinements (inheritance for helper
  threads; sorted *iteration* fixes RNG-consumption and id-assignment order
  too). 3 new tests + full suite 289 green. **Forkcheck gate on the
  verification jar (fork base + cherry-pick, BC ckpt via gRPC): twin 40/40,
  0 statics, 0 transport failures** — record
  `data/forkcheck/run-20260719-determinism/`. Gotcha: forkcheck must run
  with cwd=forge-gui (res/ lookup).
- **#11260 second comment posted** (user's voice, verified): getCardForUi
  cards never join a Game → static negative-counter fallback for the
  gameless path (CombatView precedent), per-Game counters where
  determinism lives.
- **manabrew protocol #445 review DELIVERED** ([manabrew-protocol-445-review.md](../design/manabrew-protocol-445-review.md)):
  6-item gap list posted (provenance field, per-game seeds, mid-cast
  abort/composite-cast offer, GameOver outcome, noncombat allocation,
  prompt_id join semantics) — M3 D3(c) done. khaliostr digging up the SVar
  repro; allentiak/itemfive channel chatter noted, no action.

## Next session picks up (with the user — deliberately not closed overnight)

- **D1 closeout decision**: quantitative clause met; remaining = the
  pre/post-first-cast commander split on residual vetoes (needs the obs
  cmdcast join on the arms stores — cheap, do it in-session).
- **The strength conversation (D2)**: the ceiling datum reframes it — 
  candidate levers: iteration scale at lower lr, full-vis critic in the
  loop, rollout-labeled targets under micro-batching, larger per-iter game
  batches. Also worth deciding what run-6 is FOR before launching it.
- iter-9 ckpt = run-5's checkpoint of interest; chain records in
  `data/training/d6-run5/`.
- Upstream watches: #11285 review feedback, Hanmac on #11260, khaliostr's
  SVar repro, #445 responses.
