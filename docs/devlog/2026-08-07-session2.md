# 2026-08-07 session 2 (M6 D2-B: BOTH probes built + resolved — B-1 negative, B-2 CLEARS)

## Done

- **B-1 harness built end-to-end** (the first of ADR-0042's two parallel
  levers): `anvil/encoder/derived.py` — 38 derived-state features in the
  five ADR-0042 families (race/lethality, clock, castability-vs-mana,
  material/card-advantage, commander-tax), computed transform-side from
  one logged `dec` record, info-set-respecting via the transform's
  `visible_to` (leak invariant tested), card statics from the pinned
  fork's cardsfolder with graceful token-miss degradation.
  `tests/test_derived.py`: 14 tests green.
- **`scripts/feature_probe.py`**: `features` (compute at the 6,117
  benchmark positions — same turn-join convention as `frozen_probe.py`,
  refuses partial dumps) + `probe` (ridge on `[STATE] ⊕ features`,
  per-family attribution: state+F / F-alone / leave-one-out, plus curve
  reads; split/CV/ridge imported from `frozen_probe` so numbers are
  comparable by construction). Harness validation: `state` reproduces
  ADR-0041's 0.4552 exactly.
- **B-1 VERDICT ([ADR-0043](../decisions/ADR-0043-b1-feature-probe-verdict.md)):
  does NOT clear the 0.46 gate.** Best `state+race` 0.4597 (+0.0045,
  noise); `state+all` 0.4357; LOO deltas ≈ 0; feats-alone 0.23–0.25.
  Extended-alpha and MLP robustness reads agree. **Reconstruction
  diagnostic: `[STATE]` linearly decodes the features at median R² 0.65 —
  best exactly on the high-correlation ones (lethal margin 0.94, life
  diff 0.88, TTD 0.86) — the trunk already carries the arithmetic; the
  arithmetic doesn't carry the 0.46→0.94 residual.** B-1 out of the
  graduated bundle; B-2's prior raised (deficit is not at the input).

- **B-2 harness built + VERDICT same session
  ([ADR-0044](../decisions/ADR-0044-b2-unfreeze-probe-verdict.md)):
  CLEARS the gate — the first lever past the frozen ceiling.**
  `scripts/unfreeze_probe.py`: `prep` banks the 6,117 masked-path value
  windows once; `sweep` = top-N × lr × seed grid with RankNet
  |Δwr|-weighted pairwise loss (pairs gated ≥ 2/8), game-grouped inner
  split for early stopping, final read on the frozen holdout. Cells ~0.3
  min (est. was GPU-hours — off ~100×), so replication was free:
  5 seeds/N, shared-holdout paired — N=0 control 0.4528 ± 0.0017 (=
  the plateau; ranking-trained head recovers the linear ceiling exactly,
  BCE head reads 0.335), **N=2 0.4769 ± 0.0015 (every seed above every
  other cell)**, N=4 0.4672 (overfit), N=1 0.459. **Label curve rising +
  steepening (1K 0.443 → 2K 0.450 → 3.6K 0.477)** — the lever is
  label-starved; label expansion re-priced parked → recommended-first.

## Broke / surprised me

- **The conditioning failure's third sighting** (after transform v2/v3):
  first probe run had `race_power_ready_opp` std 408 and `race_life_diff`
  std 803 — infinite-combo boards (six-digit power) and drain kills
  (six-digit-negative life) owned the stds, so standardization crushed
  the normal-game range of exactly the strongest features to ~0. Fixed
  at source (PT_CAP=50 per entity, life clipped [-10, 150]); race-alone
  0.21→0.23. Standing rule recorded in the ADR: every engineered
  aggregate gets a clip at birth + a probe-side per-feature std read
  (the harness now prints/writes both).
- Statics misses at the benchmark: 245/1,842 names, ALL token-class
  (Airbend transform-copies, creature/Blood tokens, Boo) — the designed
  degradation path, verified rather than assumed.

## Next session picks up

- **USER DECISION: label expansion before the graduated build?** The
  rising curve is the pre-registered buy-labels trigger; drill-mode
  tranche 5–10K/era ≈ 1–2 days of box time (ADR-0040), then the re-probe
  costs minutes and the curve extends for free. Fork stability pass
  (IndexOOB, targeting-retry, MayPlay `.get(0)`, MinMaxBlocker) rides as
  pre-campaign gate if the tranche launches.
- **The graduated build** (either immediately or post-tranche): B-2-only
  bundle at N=2, value-tower/partial-layer discipline to protect the
  shared policy trunk, era-scoped assets, mini-run validation, then the
  standing 2,000-game combined paired read vs 0.5316.
- D3 (curriculum arm) and D4 (isotonic wiring) remain open, independent.
