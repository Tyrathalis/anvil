# 2026-09-02 — session 3 (probe6 adjudication + the step-back round)

Read-and-design session on top of the probe6 read ([ADR-0093](../decisions/ADR-0093-m10-probe6-read.md)); no run launched. GPU used only for a diagnostic.

## Done

- **Probe6 assessed for the user.** Loop mechanics and the emitter confirmed as the ADR reads them. Two things the ADR did not ask: (1) the pinned reliance population's fed schedules are day-zero six-slot emissions (86% length 6, 36% one card repeated six times), so `content_flip` probes following on garbage-shaped inputs; (2) whether the consumer reads content on LABEL-shaped schedules. Built the probe: the ADR-0092 follow batch with the certified arm fed (496 in-sample windows) and, via a `src` switch on `build_follow_batch`, the natural line fed at 539 windows the pointer never trains on; reads fed vs mask-closed follow accuracy and the argmax change under a slot 0↔1 swap.
- **Result: the coupling is a few percent real.** Certified windows: follow 9.7%→19.0% fed but 9.1%→14.1% closed (≈5pp schedule-conditioned, the rest BC of certified first casts); swap-flip 0→2.8%. Natural windows: fed = closed (67%/68%) at init AND at iter-5 — the 32–52% live follow rate is the policy playing its own line. Content ≈3% on label-shaped inputs vs 0.6% on the pinned population: the plateau is real on both, not an instrument artifact.
- **The two ADR-0093 open findings are one fact:** PG never reaches the emitter (its logits appear in the learner only in the grad-free live-CE block), and the live-gap ratio `sched_live_ce / seedlab_raw_step` read 3.3/3.1/3.5/3.8/4.9/5.7 — the ADR-0088 staleness tell, above 3× throughout.
- **User adjudicated NO-FUND / no KILL** and asked the design question directly: is a learned follower the way toward "plan turns ahead to enact ordering and purposeful passing"? Answer given: it fixes fidelity, not planning; the funded ceiling was measured under binding `-forceschedule` execution while the live surface is advisory; the planner is a supervised snapshot. User agreed to keep eyes on the prize and asked for a step back on strategy.
- **Step-back written up:** [m10-reset-draft.md](../design/m10-reset-draft.md) — four underlying issues (signal density answered with small batches; surfaces before couplings + accretion: 8 terms / 13 guards; two action abstractions deciding the same thing; inverted reads), the planner-hierarchy statement, six forks with leans (binding execution; planner PG anchored by mint CE; certifier as async era-weighted labeler; stratified paired strength read as the primary gate; probe7 on a fixed budget with a pre-registered negative outcome; payment under the same principle, named), a retirement list, deferred alternatives by name (the own-plan follower, the contrastive term, the content probe as telemetry), build order + hazards.
- Record: ADR-0093 addendum (adjudication + the corrected reading + supersession of the routed round); standing rule (serve-side follow/utilization counters inflate on natural-line plans — read consumption fed-vs-closed on label-shaped inputs); `scripts/sched_content_probe.py` promoted from the scratchpad; CLAUDE.md status; map Now pill / ADR row / footer.

## Broke / surprised me

- The reliance population's schedules being one card ×6 in a third of windows — the "content invariance at zero init" contract was satisfied by construction on inputs nobody would ever emit after iteration 1.
- The zsh `for f in $F` no-word-split trap and `--include=*.py` globbing bit again (memory rule holds); sed with a scratchpad path full of slashes — use Python for patches.

## Next session picks up

- **Adjudicate the six forks** in m10-reset-draft.md; stamp ADOPTED + an ADR; then build in the drafted order (serve rule → loader action → planner PG + KL twin → certifier daemon + era pool → stratified read script → smoke → pre-flight pins → probe7 launch). Budget: two build sessions + one probe.
- Pre-flight owes: the planner KL bar, the forced-veto KILL bar, the stratified FUND bar at the ADR-0078 threshold scale, the era-weight window, the day-zero re-banks under binding execution.
- The M10 Status bullet in CLAUDE.md is far past the ~2–3 KB compaction rule — snapshot verbatim to the status archive and compress at the ADOPTED stamp.
