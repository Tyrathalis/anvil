# 2026-09-06 (session 6) — M12 Build 2: the acting rule in the fork, the day-zero read moved to the 2,000-game instrument and launched

## Done

- **State review for Build 2** ([ADR-0104](../decisions/ADR-0104-m12-build2-acting-rule-and-dayzero-read.md)).
  Three findings against the plan: the search monitor lives on the mainline game only, so the
  pinned fork-window paired read could not search inside its own completions (nested copies,
  dual-policy routing, census regeneration, replay-parity proof — one to two extra evenings
  and new failure classes); in self-play completions both bridged seats would search and the
  with-lookahead arm would read ≈ 0 by symmetry; the plan's reference number (0.5279) lives on
  the 2,000-game-vs-heuristic scale while the pinned instrument was the fork-window read.
  **User decisions:** the instrument is the standard 2,000-game read vs the heuristic (arms
  paired game-by-game on the final_read pairs file + seed base); the three network arms first
  (ref / dz / dzla), the heuristic control arms trail on their own fork commit; bar 0.05 for
  the gate with 0.10 as a fourth arm; T = 0.025; rate 1, rolls 1, surfaces off; the control
  Java written while the arms fly, its forkcheck after the read.
- **The acting rule in the fork** (`SearchDirective.Pending.decide` + the controller's
  `chooseSpellAbilityToPlay` / `searchForcedAsk`; `AnvilRun -searchact <bar> [-searchtemp <T>]
  [-searchseats <csv>]`; `Obs.searchPins` → the header's `search` object; the labels sink
  null-safe): margin ≥ bar → sample from the leaf-value softmax (the natural stays in the
  distribution; splitmix64 draw on (seed, turn, window), never the game RNG), a sampled option
  realized by a single-option forbid-decline re-ask, a veto falls back to the natural line
  (`applied:act_void`). `SearchActTest` (7 cases) — it caught two real bugs: the softmax was
  normalized inside the sampling loop (p summed to 1.018) and `java.util.Random(seed).nextDouble()`
  over nearby seeds sampled the argmax at 0.81 against the 0.865 the softmax says.
- **Anvil side:** harness `--forge-args` / `--labels` / `--jar` (manifest `forge_args`,
  `labels`; the jar path override lets a read run off a snapshot while the fork rebuilds — the
  harness verifies the jar sha per worker launch, so an in-place rebuild would have failed
  every later chunk); `final_read.py --forge-args/--labels/--jar/--skip-ante`;
  `scripts/build2_act_smoke.sh`, `scripts/build2_read.py` (smoke + arms + the gate verdict),
  `scripts/build2_dayzero_read.sh` (the detached, resumable chain).
- **Acting smoke 1** (8 games seat 0 vs the heuristic, rate 1, bar 0.05, T 0.025; the
  pre-fix sampler): 378 windows, act rate 24% (search 88 / search_nat 3), applied act 79 /
  pass 7 / act_void 2 (0.5%), no `nat_unsearched` / `nat_unvalued`, sampled ≠ argmax 15%, 0
  crashes, 8/8 games decisive, model 5/8; 33 s/game (~3× the flag-off wall), call multiplier
  1.36× on the census basis (both seats' asks). Seat scoping confirmed (every row seat 0).
- **Acting smoke 2** (the final jar, fork `103747691cc`): 353 windows, act rate 24% (search 81 /
  search_nat 3), applied act 70 / pass 9 / act_void 2, sampled ≠ argmax 25%, margins p90 0.16 /
  max 0.52, 0 crashes, 8/8 decisive, 31 s/game; the header carries the `search` object.
- **Forkcheck of the acting-rule jar** (flag off): 498/500 main-trace hashes identical to the 08-21 baseline (`run-20260906-build2-act`; fork fidelity 448/52 vs 450/50); the two misses are the standing pair — 20260969 (launch-unstable) and 20260739, which replayed twice on the same jar to the baseline hash `9e0365815606ddf6` (the identity-hash residual) — **PASS at the ADR-0025 standard; the research fork pin moves to `103747691cc`**.
- **The day-zero read chain LAUNCHED** 23:16 09-06, `scripts/build2_dayzero_read.sh` detached (chain pid 1872752, watchd `build2-dayzero` stall 60 min + each arm's own `b2-<arm>-read` registration, notify on completion / failure), jar snapshot `data/runs/build2-dayzero/forge-build2.jar` (sha `812ea09e…`, pinned in every arm's manifest); arms ref → dz → dzla (bar 0.05) → dzla10, 2 × 1,000 games each at eight workers; ETA ~80 min per plain arm, ~2.5 h per search arm → the read lands ~07:00 09-07 in `data/runs/build2-dayzero/read.json`.

## Broke / surprised me

- `build2_act_smoke.sh` with a relative `OUT` wrote into `forge-gui/` (the launch cds there)
  — the 09-06 `run_forkcheck.sh` trap again; absolutized.
- Rebuilding the jar under a running smoke JVM did not crash it (its classes were loaded), but
  the harness's per-launch sha check would have failed a multi-chunk run — hence `--jar`.
- `mvn -pl forge-gui-desktop test` without `-am` cannot resolve the sibling modules (nothing is
  installed); the reactor form with `-Dsurefire.failIfNoSpecifiedTests=false` runs one test.

## Babysit notes (09-07, 03:07 tick — the dzla05 arm at 939/1,000 on s0)

- Search arms run at ~515 g/h against 2,054 plain (×4 wall, not ×3: eight workers' copies share
  one server). Three counted classes to carry into the ADR-0104 addendum, none a chain stop:
  ~0.5 per game `MODEL ERROR on mtg.trigger: decision has no observation` (a copy-session
  trigger ask with a null obs record → heuristic fallback on the copy; absent from the plain
  arms); 8 `crash_or_hang:BridgePoisonedException` games per 939 (gRPC deadline on
  `mtg.priority` under search load — the game drops, the pair drops); a `paperCard is null`
  NPE class on GameCopier copies (174 `copy_crash` of 169K candidate rows). Leaf kinds: leaf
  69% / void 30%. Games: 924 won / 5 draw of 939 rows.

- **Community watch (09-07, LordOfThePigs on Discord):** effect-grounded embeddings (tokens tied
  to their observed game effect) as his new basis, with the argument that general text embeddings
  cannot carry MTG meaning; the bias in his models attributed to the optimisation target, not the
  teacher. Banked in the map's watch entry + the Discord survey doc; routed by name as a Build 4
  fork (effect-grounded vs the pinned LLM text embeddings).

## The loop game (09-07 morning)

- **The loop game (09-07, 09:51–10:56):** the dzla10 arm's game 989 spent 65 min in one MAIN1 window — the bridged seat activated Dark Sphere's prevention ability with an empty stack, `ChooseSourceEffect`'s unbounded `do … while (o == null)` re-asked the AI's `NeedsPrevention` chooser (which only answers from the stack or unblocked attackers) 150K+ times, no priority pass so no cap could count it, and the wall-clock guard under search sat at 300 + 3,600 s. The heuristic never enters that state (its `canPlayAI` gates the activation); a learned policy picking from the legality mask does — the same shape as the Cabal Coffers refund bug (engine code assuming the AI's own gating). **Fixed in the fork (`b4825285529`, user-approved 09-07):** the engine re-ask bounded (two attempts, then the first real source — upstream PR candidate, `upstream-worklist.md`); `Census.loopCheck` — consecutive identical controller callbacks per game past 256 cap the game as a Draw with reason `loop:<method>` and the Surfaces force hooks answer the first option so any such loop exits (counted like a cap; the 0.5% tripline reopens repetition detection); the search allowance on the wall-clock guards 3,600 → 900 s. Replay of the loop game on the fixed jar: 20 turns, 36 s, exactly two source asks per activation. Routed by name to the shakedown: a state-hash repetition detector at priority grants for model-driven no-progress cycles (bounded today by the window cap); a per-turn discount as the training-side answer to progress-without-winning (token doubling), not a guard. One clock hit in ~8,000 games — under the tripline.

## The day-zero read (09-07, 11:01) — IN-BAND

| arm | games | winrate vs the heuristic | vs `ref` (paired) | vs `dz` (paired) |
|---|---|---|---|---|
| `ref` — `iter-019` alone (Build 2 jar) | 1,984 | 0.538 ± 0.011 | — | — |
| `dz` — day-zero ckpt alone | 1,972 | 0.522 ± 0.011 | −1.63pp ± 1.15 (t −1.4) | — |
| `dzla` — day-zero + lookahead, bar 0.05 (**the gate arm**) | 1,974 | 0.530 ± 0.011 | −0.77 ± 1.20 | **+0.92 ± 1.10 (t 0.8)** |
| `dzla10` — bar 0.10 (the bracket's second arm) | 1,978 | 0.538 ± 0.011 | −0.10 ± 1.21 | +1.58 ± 1.04 (t 1.5) |

Acting telemetry (75K / 72K searched windows): bar 0.05 acts at 15.4% of windows (applied act 12.0%,
pass 2.9%, `act_void` 0.11%), bar 0.10 at 9.0% (act 7.4%, pass 1.5%, void 0.06%); sampled ≠ argmax
19% / 14%; margins p90 0.086 / 0.092, ≥ 0.05 at 15.6% / 16.5%, ≥ 0.10 at 8.5% / 9.1% (lower than
the 40-game smoke's 19.5% / 11.5%: the heuristic opponent's boards are narrower than self-play's);
`nat_unvalued` 1.4–1.5%; leaves 70% / void 29%; forward-call multiplier **1.76× / 1.74×**, wall
**2.09× / 2.07×** at eight workers; ms per window p50 260 / p90 1,050. Drops: 16 / 21 / 10 / 9
draws (caps), 7–16 crash-or-hang games per arm (gRPC deadline under search load, the copy NPE
class, one StackOverflow) — pairs drop, n stays ≥ 1,956.

**Verdict: IN-BAND on the pre-registered gate arm** (dzla − dz = +0.92pp, inside (0, 1.5)): Build 3
proceeds; **the big run cannot launch without a post-Build-4 read ≥ +1.5pp**; the value-head pass
for that re-read (ADR-0101 §3 item 4) = one more banked-label fit + this run's own composites.
Read honestly: the bar-0.10 arm sits at +1.58pp on the point estimate, but it is the bracket's
second arm, not the gate arm, and a post-hoc bar pick is exactly what the pre-registration forbids;
what it says is that the acts between margins 0.05 and 0.10 are net negative (≈ −0.7pp ± 1.0 for
~6% more acted windows) — the bar for the shakedown starts at 0.10, pinned before that read.
Two facts beside the gate: (1) **the day-zero checkpoint alone reads 1.6pp below `iter-019`** (t −1.4)
despite 99% argmax agreement on held-out windows — the value-head pretrain cost policy strength
at the ~1% of windows that differ, and the with-lookahead arms only recover to `ref`'s level
(dzla10 − ref = −0.1pp); network-alone for Build 5 therefore starts from `dz`, below the reference,
and the post-Build-4 re-warm is read against `ref`, not `dz`. (2) The search multiplier at eight
workers is 1.75× forward calls and 2.1× wall against the heuristic — lower than the self-play
smoke's 2.4× (one seat searches; half the windows) — the Build 5 sizing number for a
vs-heuristic population; self-play doubles it.

## The control-arm smoke (09-07, 11:05, the loop-guard jar `b4825285529`)

- 8 games, no seat bridged, seat 0 (heuristic) searched at rate 1 / bar 0.05: 346 windows, act
  rate 11.6% (applied act 27 / pass 12), 0 crashes, 0 fallbacks, 8/8 decisive, seat 0 won 5. Known
  property of the control arm: **void 53% of candidate copies** (bridged: 29%) — a heuristic seat
  realizes a forced option through its own `canPlaySa`, which is a JUDGMENT call, so the control's
  lookahead ranks only the options the heuristic would itself realize; the natural line is always
  among them. The reader's forward-call multiplier is undefined without bridged asks (None).

## The combined tip proven; the control chain launched (09-07, 11:20)

- forkcheck `run-20260907-build2-control-loop` 498/500 vs the 08-21 seeds (fork fidelity 449/51), the standing two misses, 20260739 replays to the baseline hash `9e0365815606ddf6` twice on the same jar — **PASS; the research fork pin moves to `b4825285529`** (the control arm `1d4b2d817c3` + the loop guard).
- **Control chain launched 11:20 09-07** (`scripts/build2_control_read.sh`, chain pid 519465, watchd `build2-control` + per-arm registrations, notify on completion; jar snapshot `data/runs/build2-control/forge-control.jar` sha `8a5aab9c…`): `heur` (no seat bridged, `-searchseats` names the read seat, no search) then `heurla` (bar 0.05 / T 0.025 / rate 1), read against the day-zero arms; ETA ~14:40.

## Next session picks up

1. DONE 09-07: the read adjudicated IN-BAND (ADR-0104 addendum). Open: the control arms, then Build 3;
   the shakedown's bar pinned at 0.10; the post-Build-4 re-read is against `ref`.
2. The heuristic control arms: the Java is committed (fork `1d4b2d817c3`, + the loop guard `b4825285529`: `-searchseats` names a
   heuristic seat; it honours the directive through its own realization); after the read:
   rebuild the jar, a `build2_act_smoke.sh` run with `SEATS=0` on a heuristic seat (`-bridgeseats 2`),
   the 500-game forkcheck, then `scripts/build2_control_read.sh` (heur + heurla).
3. Build 3 Python side when its evening comes (unchanged).
