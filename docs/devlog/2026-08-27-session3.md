# 2026-08-27 session 3 (M10 build R5 — the label round; the evalset's "unreachable" positives were the scorer's fault)

## Done

- **Evalset repair RESOLVED with a finding
  ([ADR-0082](../decisions/ADR-0082-payment-evalset-repair.md)):** the
  ADR-0069 "unreachable as constructed" positives were an INSTRUMENT
  defect — the scorer promised outcome-equivalence-class scoring and
  checked exact index, while the certify read's `best = max(cleared)`
  tiebreaks margin-TIED arms by highest arm index. Re-applying the
  read's own predicate per arm over the banked certouts: 11/17
  wide_choice, 9/26 color_hold, 7/13 phyrexian, 3/13 blocker windows
  carry ≥2 independently-certified arms (many margin-identical). The
  ADR-0066 rule a second time; standing-rules corollary added.
  Repair: `cls` classes on positives (invariant best ∈ cls verified),
  class scoring in the drill scorer, phyrexian's 13 retired with
  reason (value-free at game end, ADR-0073). Assets:
  `payment-evalset-v2` (56/224/13) and `payment-holdout-v1` (the
  uniform-drawn ratesweep set, 19+123, same treatment — the
  pre-registered conditional holdout, never ingested; the
  train/holdout split also fixes the instrument conflict of training
  on the score-set's own windows).
- **ADR-0075 label ingestion built** (`anvil/training/paylabels.py`,
  the seqlabels genre): class-CE (−log Σ_cls p) on the pay pointer at
  the post-boundary revalidation observe frames — 265/280 v2 drills
  joined (12 miss / 3 option-mismatch, loud). w_paylab ADR-0057
  calibration + paylab_share guard + per-kind CE telemetry (positive
  vs auto NEVER blended). The `dataset.py` pay-invisibility pin got
  its scope stated loudly: it bars HEURISTIC answers and stands;
  certified labels are the opposite provenance. Discovered en route:
  the original `run-20260821-observe` frames are PRE-boundary (obs
  sv=1, reader-refused by design) — the revalidation frames are the
  post-boundary bank.
- **Holdout observe frames minted on the build-era jar**
  (`run-20260827-holdout-observe`): plan → 4 lanes → 142/142 windows
  reproduced, zero misses. Day-zero holdout score banked on
  `m10-sched-init`: positives 0/19, auto-correct 119/123 = 96.7% (the
  +2.0-bias calibration point). The probe recipe points
  `--pay-drill-dir` here.
- **`cand_paymark` landed** (actuation pin 1): zero-init
  `pay_mark_emb` into the marked candidate's key (day-zero no-op,
  test-pinned); SchedServe computes the schedule-consistent option
  (max remaining-slots-affordable after taps, most-flexible-spare,
  never auto), mu row carries `mark` (loader parity),
  `paymark_follow/deviate` counted. Recorded honestly: the mark is
  live-but-untrained at birth (label windows carry no schedules).
- **Best-arm seed supervision minted + wired**:
  `seed_sched_labels.py` → 170/170 certified arms matched (zero
  drops) → `seedlabels.py` fixed-batch decode CE (exact rejoin by
  emission dec id, era-recorded), w_seedlab at frac 0.05.
- **Full-stack integration smoke green**: sched aux + pay labels +
  seed labels calibrate together (w_sched 0.00055 / w_paylab 0.0030 /
  w_seedlab 0.00054), tripwire 0 on the live-conditioned store.
  Suite 253 green, ruff clean. Graft regenerated with `pay_mark_emb`
  (3 pay params); day-zero re-banked (flip/content identical —
  behavior unchanged).

## Broke / surprised me

- The margin-tie tiebreak-by-arm-index was invisible for two
  milestones because the docstring SAID equivalence class — the code
  drifted from its own comment at birth. Every historical
  positive-accuracy series undercounted (the D4 verdict stands — it
  rested on the discrimination statistic).
- The pre/post-boundary observe-frame split (sv=1 vs sv=2) cost one
  confused smoke until RUN.md explained it; the revalidation dir was
  the answer and run18's own recipe already knew it.

## Next session picks up

- **The COUSINS TOUCH (by name, before the probe):** convoke/
  improvise/delve/`payCombatCost` + costmod per-spell + pool-tie
  residual in one Forge-fork touch, ADR-0025-proven; serve contract
  pins cousins in scope from birth.
- **Then the training-probe launch session**: recipe script (sched +
  pay-labels + seed-labels + holdout scoring + reliance + guards),
  kill/FUND/unmask + read-protocol numerics pinned pre-data
  (floor-relative flip thresholds vs the banked 0.0125).
