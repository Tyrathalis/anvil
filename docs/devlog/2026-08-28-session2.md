# 2026-08-28 session 2 (M10 probe pre-flight — remints/census clean, numerics pinned pre-data)

## Done

- **Pre-flight compute on the cousins jar `5bbc397412`
  ([ADR-0084](../decisions/ADR-0084-m10-probe-preflight.md)):** one
  orchestrator (`data/census/run-20260828-preflight.sh`) ran both
  observe mints (8 lanes) then the paygoals4 census (4 lanes, verbatim
  paygoals2/3 recipe — same 100 pairs + seeds).
  - **Holdout remint ZERO-drift** (`run-20260828-holdout-observe-cousins`):
    142/142 scored, no misses, no option_mismatch; day-zero identical
    to the R5 bank (0/19 pos, 119/123 = 96.7% auto).
  - **Evalset remint drift bounded + loud**
    (`run-20260828-revalidation-cousins`): 263/280 joined
    (51 pos / 212 auto), 12 miss (same count as R5), option_mismatch
    3→5 — all five mismatches show +1/+2 options vs certify-time =
    exactly the ADR-0083 predicted spare_pool/costmod class (the one
    drifted positive: b1:101 wide_choice). Floors survive
    (bp 12 / ch 25 / wc 14).
  - **Census read** (`run-20260828-paygoals4-cousins`, 500g):
    costmod 25.47% → **4.35%** (the predicted recovery, ~21pp
    returned); consequential 19.79/g (budget ✓); goal-trunc **0**
    (GOAL_MAX 24 never binds); nodecap 0.0068 (gate 0.01 ✓); leak 0;
    **forced REBORN as the delve/improvise family** (129 windows:
    Tasigur 60, Murktide 44, Hogaak, Temporal Trespass, Kappa
    Cannoneer — heuristic casts on the discount, raw auto-pay can't =
    the capability-unlock universe, now measured); **combat 0/500g**
    natural (removeUnpayableAttackers prunes upstream). Cousin option
    rates are bridge-only (`cousins` kv) — ride the probe's day-zero
    census per the pinned routing.
- **Numerics pinned pre-data, user-adjudicated 4/4 on the drafted
  leans**
  ([m10-probe-numerics-draft.md](../design/m10-probe-numerics-draft.md)
  = statement of record, ADOPTED): (1) KILL/FUND **content-primary** —
  content_flip carries v1 thresholds verbatim (<0.005 / ≥0.02) on its
  true-zero floor, presence flip joins KILL floor-relative
  (−0.012513), presence-only movement can't FUND → **standing rule
  born** (conditioning-surface flip gates read the content channel;
  presence floors banked and subtracted). (2) Degeneracy veto:
  pure-hold >25% / mean length <1.0 / utilization <25%, 2-iter
  sustained. (3) PG-unmask: ≥4 masked + pay-CE plateau + follow
  ≥max(2× iter-0, 0.05) + auto ≥85% + realization-failure share <0.5.
  (4) Competency read (the last design fork, CLOSED): scalar gate =
  mean stage-2 Δwr on the v<0.45 stratum vs banked +14.1pp; sweep
  re-run population (600 turns, both critics); curve never gates.
- **Invalid-schedule penalty DERIVED TO ZERO** — the build never
  actually derived it (routed as "at build," nothing existed); new
  standing instrument `scripts/sched_penalty_derive.py` (read logic
  committed pre-output) on the banked h2 sweep: void arms −0.188
  composite vs natural (median 0.0, n=25,494), degraded −0.384,
  within-arm contrast support nearly empty (16 mixed arms —
  degradation is arm-deterministic). The deterred behavior is ≈free ⇒
  ADR-0053 cap binds at ≈0 ⇒ **the term is not built**; validity
  telemetry watches, contingent re-derivation pinned (>50%
  knowably-invalid sustained).

## Broke / gotchas

- The paygoals lane scripts have no `cd` (they ran with
  cwd=forge-gui); the regenerated paygoals4 lanes got an explicit
  `cd` line. Observe-lane logs end in AI-timeout stack dumps that look
  like crashes but aren't (the watchdog prints and play continues) —
  check `certify done` + DONE markers, not log tails.
- `payment_read.py` needed no extension: costmod/nodecap/trunc are
  standard; combat + forced composition read with greps (payCombatCost
  rows = 0, so no combat read code owed).

## Next session picks up

- **LAUNCH THE TRAINING PROBE** (D4-shape, ~5 accepted iterations,
  standing recipe + R5 label stack). Recipe repoints:
  `--pay-observe data/census/run-20260828-revalidation-cousins`,
  `--pay-drill-dir data/census/run-20260828-holdout-observe-cousins`
  (+ `--pay-drill-embed`). Every kill/FUND/veto/unmask/read number is
  pre-registered (ADR-0084); guards: sched projections in the 1e-4
  group, veto-trip → knowability decomposition first.
- Riding the probe: cousin/combat rates from the day-zero bridge
  census (`cousins` kv); directed-convoke capability read on the
  129-window forced family base; probing-dissolution secondary read;
  paymark follow iter-0 baseline recorded at launch.
