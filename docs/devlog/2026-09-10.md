# 2026-09-10 — the calibration read lands

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## The chain's day (03:05 → 17:49)

The restart at the eot/h2 boundary (03:05, the chunk fix) and the second at 03:08 (the clock 7,200 →
2,400 s) are in [yesterday's devlog](2026-09-09-session2.md). h2: seat 0 03:08–05:52, seat 1
05:52–08:52; end: seat 0 08:52–13:29, seat 1 13:29–17:49; the read 17:49. Every seat run spent
its last 1–3 h in a tail of one or two workers finishing 13-game chunks whose late games are wide
boards (h2 copies 8 s per answer p50 / 83 s p99; end 49 s mean / 106 s p90). The clock clipped 4
h2 games and ~8% of end games. The AI eval-thread timeout class showed three times (one 6,928 s eot
game; two end workers with 27 / 12 timeouts) → a named item for the throughput week's JFR list.

## The read (ADR-0105 addendum 17:49)

Identity: 1,252 / 1,252 / 0. Conversion of the eot leaf's positives by the outcome: +4.2 ± 2.7pp (t
1.6) at 0.03, +2.9 ± 1.4 (t 2.0) at 0.01; ties ≈ 0; no sign flip → the target is not myopic. The
h2 leaf: 133 positives at 0.03 converting +5.8 ± 2.0 (t 2.9); agrees with eot (t 5.2); 3× eot's copy
cost vs 12× for the outcome leaf, whose best-by-outcome is winner's-cursed at 4 rolls. Proposed:
h2 = the target going forward; the bridged-seat read on the h2 judge next.

## Community watch (09-10)

The Forge-MCP / "UCI for MTG" thread banked in the survey doc with the assessment (not on the
loop's path; the UCI answer is the engine's callback surface + `AbilityKey`; the Manabrew protocol
is where community MCP/UCI efforts should start; Anvil stays direct on the controller).

## Next session picks up

State at 21:50: the h2 relabel (`b3-surflab4`, rolls 2 / rate 0.5 / clock 3,600, 16 workers) and
the pay chain waiting on it are running, watchd-registered, notifying; wakeups 23:07 / 03:07 /
08:07. Tree clean at `158b163`; the fork pin `287e8cca45`; the QoL branch `qol-device` unmerged
(the pay chain imports main). The user's decisions today: the certifier merge routed (Build 4½ /
the closeout); the Forge-MCP thread banked (not for us; the Manabrew protocol is the community's
starting point); **swap the payment target to h2 now** (launched); model-server scaling routed to
the throughput week ahead of the worker bench.

1. The pool lands ≈ 07:30 09-11 (`data/runs/build3-surface-labels4/DONE`); the pay chain runs
   unattended: `data/runs/build3-e4h2/pool-read.md` (usable groups / game, positives at 0.03 — the
   eot pool had 42.9 / 5.7%; h2 at rate 0.5 should give ≈ 21 groups with ≈ 2× the positive rate),
   `payfit-read.md` (the set-keyed head at pos-weight 8: pos top-1 / pos dev / tie dev vs the e4s
   fit's 0.094 / 0.155 / 0.045), the smoke, then `data/runs/build3-surface-read-b3e4h/read.json`
   (on − off at argmax vs the e4s arm's −1.02 ± 0.72 on 0.115 deviations / game). The verdict rule:
   on − off > 0 with deviations ≥ 0.15 / game → the head serves (ADR-0105 closing addendum + the
   served set gains the pay tag); ≤ 0 → the head stays withheld, the bridged-seat read on the h2
   judge attributes (its picks vs the h2 leaf's).
2. Merge `qol-device` after the chain (the device passthrough + final_read's chunk default).
3. The throughput week: model-server scaling first (the harness `--bridge` list, N servers per
   launcher, the autoscale rule), then the 16- vs 24-worker bench, the JFR profile (the AI
   eval-thread timeout class, the straggler tail → work-stealing chunks at the tail).
4. The bridged-seat read on the h2 judge (the bar0 seeds, `-searchpay 4 -searchpayleaf h2`) if
   item 1's verdict needs attribution; otherwise it folds into the loop.
5. Evening 5 = mainline surface acting (modes first) — the closing Build 3 evening; the certifier
   merge and the puzzle battery stay routed.

## 20:00 — the h2 relabel launched (user: swap now)

Two chains, both detached, watchd-registered, notifying: the label run (`b3-surflab4`: the
evening-4 recipe with `-searchpayleaf h2 -searchrolls 4 -searchclock 2400`, 16 workers, the e3
ckpt serving every tag but pay, the pin's jar) and the pay chain (`build3-e4h2`: waits on the
pool's DONE → pool read → the set-keyed head cross-fit at pos-weight 8, min-rolls 3 → the build
`m12-build3-e4h` → smoke at argmax → the paired read `b3e4h` off / on, 2 × 300). Knobs added:
`CLOCK` on the label launcher, `POS_WEIGHT` on the pay chain. The command of record is
`scripts/build3_h2_relabel.sh`. Expected: the pool ≈ 08:00, the read ≈ 16:00 09-11; babysit
wakeups at 23:07, 03:07, 08:07.

## 21:42 — the relabel restarted at rolls 2 / rate 0.5 / clock 3,600

The first launch (rolls 4, rate 1, clock 2,400) ran 48 games in 100 min on 16 workers: the model
server's Python process at 110% CPU, the GPU at 52%, the workers at 30% waiting on the bridge,
31% of games clipped at the clock (p50 wall 1,671 s). The cause: on self-play the h2 pay copies
are NETWORK-played for 2–3 turns (the calibration's copies were heuristic-played), so at rolls 4
the ask volume saturates the single-threaded server — 16 workers cannot help. Restarted 21:44 with
4× fewer asks (rolls 2 = the eot pool's recipe, rate 0.5, clock 3,600); the pay chain's min-rolls
2 to match. Expected ≈ 115 g/h → the pool ≈ 07:30 09-11. The partial pool deleted (clipped, biased).
Lesson for the JFR week: the model server is one Python thread — a second server per 8 workers is
the throughput lever the harness lacks (`--bridge` takes one address).
