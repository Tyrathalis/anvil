# 2026-09-18 — the read's close; the check-in moves into the supervisor

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **The post-Build-4 read closed** (ADR-0112 addendum 09:30): act − on +2.29 ± 1.02 → the launch
  condition cleared; alloc − act +0.25 ± 0.61 at 51.3% of the windows searched → the head
  allocates freely; served build → `m12-build4-e1a`.
- **Why nobody was notified overnight** (user's question): the launcher's sinks reach the user,
  not the session; the scheduled check-in task had been off since 09-16 (an in-flight-only
  practice the launching session had to remember); no session-side wait was armed. And the CLI's
  OAuth login had silently expired (found by the first headless test).
- **ADR-0107 addendum (user: the wrapper invokes the LLM itself):** `anvil.runs` gained the
  event-driven `claude -p` check-in (failed / stalled / gone / long done; pushes, messages the
  supervising session, acks; its outcome a `checkin` queue record), the launch self-test in the
  coverage line, `--watch` globs for the stall tick (the false-stall class), `sweep` +
  `install-sweep` for dead supervisors (the OOM / reboot case — the child's OOM is the ordinary
  `failed rc=-9` path). Tests 12 in `tests/test_runs.py`. **Verified end to end 10:23:** a
  deliberately failing launch → pushed, this session messaged, acked, in 15 s.
- The scheduled task `anvil-run-checkin` retired (left disabled); the ops prompt = the fallback.
  The quickstart's §7 rewritten for the check-in; the standing rule amended.

## Broke / surprised me

- `claude -p` swallowed a positional prompt after the variadic `--allowedTools` — the first
  end-to-end check-in failed with "Input must be provided…"; the `checkin` queue record showed
  it (the visibility the design wanted). Fixed: the prompt on stdin.
- The test fixture must pin `ANVIL_CHECKIN=none`: with the real CLI on PATH the first run of the
  new tests invoked it from a failing test run (auto mode).

## Next session picks up

- Enable the sweep timer if the user says so (`uv run python -m anvil.runs install-sweep`; then
  `loginctl enable-linger`). Ack / prune the `checkin-e2e` test run.
- The loop wiring (ADR-0112 item 5): search flags on `selfplay.py`'s harness launch (the recipe +
  `-searchalloc` from the ckpt's record), `rl.py`'s search-row terms (the pick distillation where
  it acted, PG under the search's logp, the alloc head's BCE on the rows' margins —
  `search_distill`'s join + anchor exist), the head regenerated per cycle; the first look at the
  searched arms' 14% mainline veto rate.
- Then route (b), the documentation pass (Build order 4¾), the shakedown with the allocation arm.
