# 2026-08-01 — session 2: Chronicle D2 gate passed; feedback round 1; nav-trap fix

(Continuation of the 07-31 session-4 arc: D1 + D2 built there; this session
closed the D2 gate and absorbed the first real play feedback.)

## What was done

- **D2 gate PASSED (author's interactive pass):** begin, paper, allowance,
  pack opening, binder all work on `forge-gui-mobile-dev`. D2 is DONE.
- **Feedback round 1 (`59adfb8707`):**
  - **ChronicleAcquisitionLog** — the provenance journal: every sealed
    opening recorded (day, product, contents) with per-card first-pull
    ordinals; persisted in the run (schema landed now, while saves are
    young); `eventsFor`/`firstAcquiredOrdinal` + static `sourcesFor`
    (which products yield a printing — basics are starter-only under
    Forge's era sheets). Tests: record/roundtrip/ordinal + sources (34
    green).
  - Binder **"Sort: New" = true acquisition order** (first-pull ordinal,
    not the binary flag).
  - **Store: deals section above the shelf** (user's option A; the
    best-price-merge alternative noted as a revisit if this doesn't land).
  - **Reveal: long-press → match-inspect CardZoom** (oracle flip; only
    cards revealed so far — no peeking) + **pack-wide art prefetch at
    open** (all images start fetching immediately; blocking preload
    belongs to D3's reveal scene). The binder spread already had CardZoom
    via ImageView's built-in long-press.
- **Navigation trap found by the user and fixed (`003a941818`):** opening
  Chronicle set it as the in-memory preferred New Game screen (my
  `setPreferredScreen` guard only sanitized the DISK write), so the home
  New Game button reopened Chronicle all session — and ChronicleHomeScreen
  carried no NewGameMenu header, so other modes were unreachable until
  restart. Fix: Chronicle is excluded from preferred-screen tracking
  entirely, and its home screen now carries the standard NewGameMenu
  hamburger. Lesson: a "don't persist X" guard that leaves the in-memory
  state dirty isn't a guard — the session IS the state.

## Fidelity answers recorded (plan doc)

15-card ABU boosters accurate; no basics in boosters = Forge's engine-wide
era-sheet model (real ABU common sheets carried basics — documented
deviation, not Chronicle-local to change); random 5-color starters =
period-accurate; collector-number order = the canonical set order
(retroactive color-then-alphabetical for pre-Exodus sets — our resolver
follows it).

## Backlog from the pass (plan doc)

Provenance UI (log is ready: tap a card → openings list; missing card →
sources), desktop scroll-bar indicator on FScrollPane grids, stronger art
preloading (D3), save-slot browser (today = one implicit autosave run,
loaded automatically on entry; ChronicleSaveIO slot files already exist).

## State / next

- Fork `playable` local: `574abedc08` (D1, pushed) → `bb7e670cff` (D2) →
  `59adfb8707` (feedback) → `003a941818` (nav fix). **D2+feedback commits
  not yet pushed.**
- Next: **D3 reveal scene** (bar pinned in the sketch; gather Pocket
  reference footage at build time) + **D4 Android spike** (parallel-early).
  D5 dogfood can arguably begin on desktop meanwhile — the daily loop is
  live and the author is already playing it.
