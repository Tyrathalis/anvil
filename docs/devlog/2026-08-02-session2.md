# 2026-08-02 (session 2): d6-run11 completes; (a2) resolves POSITIVE — first outside-noise drill-signal win

## Done

- **d6-run11 ran its full 20 iterations, zero guard halts** (run10
  halted at 16). Telemetry clean end-to-end: KL peaked 0.024 (guard
  0.06), rejection density flat (~3.1–4.7, no run10-style climb), the
  shaped-vs-masked flag fired once (iter 7) and never recurred, the
  iter-9 wedge class produced zero incidents. In-run arms flat ~0.51
  (as with run10, the arms instrument couldn't see the story).
- **Closing protocol, mechanism-first:** evalset-v2 decomposition on
  iter-019 (~45 min) before any 2,000-game read — the ordering the
  mid-run-eval sketch argues for, validated by hand here.
- **Mechanism confirmed:** lost +7.5pp (t=2.4) / long_shot +7.4pp
  (t=2.0) held-out gains persisted; winnable regression halved
  (−9.7 → −5.1pp) and **shrank with dose** (iter-9 −8.0 → iter-19
  −5.1) — opposite of run10's dynamics. Net drill-pool +3.9pp.
- **Strength reads:** standing seeds +2.00pp ± 1.00 (t=2.01) — exactly
  at the line, so a **fresh-seed paired confirmation read of BOTH
  ckpts** (seed base 20260711, sequential matched load) was run:
  +1.95pp ± 1.02 (t=1.92). Replication at near-identical magnitude;
  **combined +1.98pp ± 0.71, t=2.77 over 3,994 paired games — done-when
  #3 SATISFIED.** [ADR-0031](../decisions/ADR-0031-a2-resolution.md);
  promotion of iter-019 to ckpt of record put to the user (recommended
  per the ADR-0023 convention).
- Also this session (before the run closed): the mid-run drill-eval
  driver-phase sketch (advisory STOP-file semantics, ~20 lines,
  `--drill-eval-every 10`) agreed with the user as part of the
  efficiency push, alongside D4 next and escalation-(b)+critic-
  sharpening as the signal-per-game path.

## Broke / surprised me

- Both ckpts read ~+2.4pp higher raw on the fresh seed set — seed-set-
  level shared luck at exactly the scale of the effects we chase. The
  paired protocol absorbed it; an unpaired confirmation would have been
  garbage. Also reinforced: the iter-19 arm's elevated first-veto
  (0.140) did not reproduce in the big reads' census (0.116/0.125) —
  400-game arms are noisy in census space too.
- First baseline-eval launch this arc used bare `python` → system
  interpreter → `import grpc` death (bridge server). Anything reaching
  `_start_server` outside `uv run` needs `.venv/bin/python` explicitly.

## Next session picks up

- User's promotion call → fill ADR-0031 resolution, update state of
  record everywhere (CLAUDE.md, map ledger/Now/state table), archive
  sweep if M4 D3 closes.
- Then the post-run block: mid-run decomposition driver phase (build +
  smoke), D4 noise floor (`bench_generation.py` repeats) → profile →
  one lever.
- If promoted: regenerate drill curation from iter-019's own losses
  before any future drill run (the run9-derived selection is stale
  against the new policy).
