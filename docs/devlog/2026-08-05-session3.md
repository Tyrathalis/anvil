# 2026-08-05 — session 3 (M6 opening chore: disk pass)

## Done

- **Disk pass executed end-to-end (m6-plan §Opening chore): nine dirs
  deleted on user sign-off, `data/runs` 213G → 68G (~145G freed).**
  - Inventory: each `d6-runX-iYYY` forensic dump = driver metadata
    (~35M: `run.json`/`summary.json`/`games.jsonl`/`mu.jsonl`) +
    `workers/inv-*`, with the bulk always one worker's forensic
    `census.jsonl` (17–26G). `_bench_scratch` = five learner-bench
    segment dirs with throwaway `last.pt` (recreated by
    `bench_learner.py`). `_contaminated` = quarantined run7b
    post-rebase arms, superseded by the `postrebase2arm` stores that
    remain at `data/runs/` top level.
  - Reference verification before the list: zero operational refs to
    any candidate in `docs/decisions/`, `scripts/`, `anvil/` (only
    `bench_learner.py:146`, which recreates the scratch dir);
    `critic_calibration.py`/`migration_read.py` read only from
    `critic-calibration-v1`, `drill-map-*-k8`, `drill-evalset-v2/v3`,
    `drill-selection-v2`, `drill-sweep-*`, `early-doom-*` — none
    inside a candidate; no drills.jsonl/ckpt/selection/evalset assets
    or symlinks inside any candidate; no file touched in 3 days;
    ckpts of record (`d6-run11/iter-019`, `d5-combat`,
    `d4-critic-fullvis`) confirmed under the separate
    `data/training/` tree.
  - Kill list presented; user chose **delete all nine** (no cold
    archive). Deleted: `d6-run2-i000` 23G, `d6-run2-i005` 30G,
    `d6-run4-i000` 26G, `d6-run4-i001` 20G, `d6-run9-i002` 18G,
    `d6-run10-i007` 7.4G, `d6-run11-i011` 22G, `_bench_scratch`
    889M, `_contaminated` 420M.
- Docs swept: m6-plan §Opening chore DONE block, CLAUDE.md M6 bullet,
  project-map (Now panel chore line, M6 details bullet, footer stamp
  session-3 line).

## Broke / surprised me

- Nothing broke. Confirmation of session 2's observation: the entire
  weight of every forensic dump was a single worker's `census.jsonl`
  — the forensic-worker full-census flag is the thing to prune at
  milestone close, not the run dirs wholesale.

## Next session picks up

- **D1: the frozen-trunk ranking probe** (unchanged from session 2):
  feature-dump path at the 3,750 labeled positions (both trunks,
  riding the `critic_calibration.py` trace join), probe family +
  learning curves, resolve against the pre-registered thresholds in
  an ADR.
