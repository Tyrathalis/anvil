# 2026-08-29 (the emission-grounding round: ADR-0086 surgery + probe2 relaunch)

## Done

- **The design round resolved in one sitting
  ([ADR-0086](../decisions/ADR-0086-emission-grounding.md), all leans
  user-adjudicated):** the two ADR-0085 surgery items collapse into ONE
  move — the decode head IS the emitter, so supervised decode on
  certified arms *is* supervised emission. No second mechanism needed.
- **Surgery landed:**
  - Own-emission dense decode CE RETIRED (not gated) from `rl.py`'s
    sched-aux block; `sched_term` = E/R only. `--sched-frac` 0.1→0.05
    (mass-preserving: E+R's ≈47% share of the old day-zero bundle).
  - Seed supervision promoted to the PRIMARY (only) decode/emission
    signal: `--seedlab-frac` 0.05→0.1, `--guard-seedlab-share` 0.3
    (3×-target convention), new `--guard-seedlab-spike` 100×-median
    tripline (ported ADR-0085 class; on a fixed certified batch a
    spike = head divergence, regression-tested). `_rl_summary` surfaces
    `seedlab_raw` med + max; battery gains seedlab rows.
  - Empty-emission cost NOT built: the 170-arm mint includes the 11
    certified pure holds (hold-vs-emit is in the supervision); a
    blanket empty penalty would fight exactly the correct holds. The
    ADR-0084 penalty contingency stays pinned.
- **FUND decode leg restated** (the one ADR-0084 number whose statistic
  died with the retired term): seedlab CE ≤ 0.8× day-zero **2.730022**,
  banked exactly at the init ckpt by the new
  `scripts/seedlab_dayzero.py` → `m10-sched-init/seedlab-dayzero.json`
  (cross-validates probe1's iter-0 calibration read 2.7301). Stated
  loudly as weakly discriminating — probe1's seedlab CE hit 0.43 while
  emissions sat degenerate; the degeneracy veto carries the
  emission-health burden of FUND. All other gates verbatim; **no graft
  change ⇒ presence/content floors stand, no re-bank.**
- **Relaunch armed: `scripts/launch_m10_probe2.sh`** — probe1 recipe
  verbatim except the surgery pins; fresh name/seed 20280829/port 50075
  (both zero-hit collision-checked vs every loop_config/run.json + live
  listeners; 50071–50074 all had historical hits). Fresh run name
  satisfies the ADR-0076 rejected-artifact clearing rule by
  construction (no phase reuse); `m10-probe1` stays frozen as the
  forensic asset. All 44 recipe flags verified against the driver
  argparse.
- Docs swept in the batch: build-spec R7 as-built, CLAUDE.md M10
  bullet, map (Now stamp + pill, milestone bullet, summary line,
  ADR-0086 index row, footer narrative). No new standing rule
  (ADR-0085's covers; 0086 is its execution).
- Suite 255 green (new: seedlab spike-guard regression), ruff clean.

## Broke / gotchas

- The seedlab CE trajectory in probe1 (2.73→1.03→0.43) is the loud
  caveat: a fixed certified batch is learnable even while live
  emissions collapse — never read seedlab CE improvement as emission
  health. The veto owns that read.
- Port collision discipline: 50071 looked free by loop_config but
  appears in old drill run.json `bridge:` strings — the grep must cover
  run.json too (50075 was the first zero-hit port).

## Next session picks up

- **Read m10-probe2** (launched this session — see launch note): gates
  = ADR-0084 verbatim + the ADR-0086 restated decode leg. Watch
  specifically: pure-hold/len trajectory (does the certified anchor
  hold emission non-degenerate with nothing dense pushing?),
  content_flip (consumption re-demonstrated?), seedlab_raw med/spike.
- If FUND: the per-era re-mint (sweep certifier on on-distribution
  states) is the routed follow-on, by name in ADR-0086.
- Watcher note carried: filter the paylabels 12-known-misses
  `FRAME ANOMALY` pattern; trip if the join summary deviates from
  263/12/5.
