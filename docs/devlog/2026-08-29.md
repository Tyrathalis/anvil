# 2026-08-29 (the emission-grounding round: ADR-0086 surgery + probe2 relaunch)

## Done

- **The design round resolved in one sitting
  ([ADR-0086](../decisions/ADR-0086-emission-grounding.md), all leans
  user-adjudicated):** the two ADR-0085 surgery items collapse into ONE
  move — the decode head IS the emitter, so supervised decode on
  certified arms *is* supervised emission. No second mechanism needed.
- **Surgery landed:**
  - Own-emission dense decode CE RETIRED (not gated) from `rl.py`'s
    sched-aux block; `sched_term` = E/R only. `--sched-frac` 0.1→0.05
    (mass-preserving: E+R's ≈47% share of the old day-zero bundle).
  - Seed supervision promoted to the PRIMARY (only) decode/emission
    signal: `--seedlab-frac` 0.05→0.1, `--guard-seedlab-share` 0.3
    (3×-target convention), new `--guard-seedlab-spike` 100×-median
    tripline (ported ADR-0085 class; on a fixed certified batch a
    spike = head divergence, regression-tested). `_rl_summary` surfaces
    `seedlab_raw` med + max; battery gains seedlab rows.
  - Empty-emission cost NOT built: the 170-arm mint includes the 11
    certified pure holds (hold-vs-emit is in the supervision); a
    blanket empty penalty would fight exactly the correct holds. The
    ADR-0084 penalty contingency stays pinned.
- **FUND decode leg restated** (the one ADR-0084 number whose statistic
  died with the retired term): seedlab CE ≤ 0.8× day-zero **2.730022**,
  banked exactly at the init ckpt by the new
  `scripts/seedlab_dayzero.py` → `m10-sched-init/seedlab-dayzero.json`
  (cross-validates probe1's iter-0 calibration read 2.7301). Stated
  loudly as weakly discriminating — probe1's seedlab CE hit 0.43 while
  emissions sat degenerate; the degeneracy veto carries the
  emission-health burden of FUND. All other gates verbatim; **no graft
  change ⇒ presence/content floors stand, no re-bank.**
- **Relaunch armed: `scripts/launch_m10_probe2.sh`** — probe1 recipe
  verbatim except the surgery pins; fresh name/seed 20280829/port 50075
  (both zero-hit collision-checked vs every loop_config/run.json + live
  listeners; 50071–50074 all had historical hits). Fresh run name
  satisfies the ADR-0076 rejected-artifact clearing rule by
  construction (no phase reuse); `m10-probe1` stays frozen as the
  forensic asset. All 44 recipe flags verified against the driver
  argparse.
- Docs swept in the batch: build-spec R7 as-built, CLAUDE.md M10
  bullet, map (Now stamp + pill, milestone bullet, summary line,
  ADR-0086 index row, footer narrative). No new standing rule
  (ADR-0085's covers; 0086 is its execution).
- Suite 255 green (new: seedlab spike-guard regression), ruff clean.

- **PROBE2 LAUNCHED, HALTED, AND READ same session
  ([ADR-0087](../decisions/ADR-0087-m10-probe2-read.md)): GUARD HALT at
  iteration 0** (`kl_mu 0.06128 > 0.06`). Read, not amended — the
  ADR-0085 precedent. Run stopped, watches stood down.
  - **Not a marginal overshoot:** KL rises within the iteration to
    **0.103** (probe1's iteration 0 rose to 0.051); probe2 runs ~2×
    probe1 throughout.
  - **Mechanism — a calibration defect, M10-independent:** the fixed
    170-window seed batch is memorized in ~10 optimizer steps
    (CE 2.728 → 0.421 → 0.046). ADR-0057 calibrates *pre-application*
    then freezes `w`, so the first applied steps deliver full
    frac-scale mass onto the shared trunk. `seedlab_share` DECAYS as
    it fits (0.070 → 0.017) ⇒ **the share guard is structurally blind**
    — it never could have fired (the run20 `plan_share` genre again).
  - **Conditioning went the WRONG way:** content_flip 0.013786 →
    **0.001909** (below the KILL bar) with presence flip UP 0.030541 →
    0.044751, and decode CE on live trajectory emission rows 2.609 →
    6.404. Presence-heavy/content-quiet = the ADR-0084 unfundable
    pattern ⇒ **the retired own-emission term WAS the
    content-consumption driver**; 170 boundary-era windows don't
    replace it. ADR-0086's "170 is enough" lean FALSIFIED; per-era
    re-mint promoted follow-on → **prerequisite**.
  - **Emission-collapse question stays OPEN:** iteration 0's emissions
    come from the *init* ckpt, so the serve counters just reproduce day
    zero (6.01% pure-hold / 71.4% full-6 / mean len 4.85 vs banked
    6.3% / 72%). Iteration 1 never generated.
  - Healthy/unchanged: tripwire 0; E 0.361 / R 0.371 both under FUND
    bars; paylab positive CE 0.766; holdout auto 96.7%; 8
    BridgePoisonedException = the watch-list family at unchanged rate.
  - **Two standing rules born** → standing-rules.md (fixed-batch
    calibration unsoundness; small fixed batch ≠ dense conditioning
    driver). ADR-0086 marked amended (not reverted).

## Broke / gotchas

- The seedlab CE trajectory in probe1 (2.73→1.03→0.43) is the loud
  caveat: a fixed certified batch is learnable even while live
  emissions collapse — never read seedlab CE improvement as emission
  health. The veto owns that read.
- Port collision discipline: 50071 looked free by loop_config but
  appears in old drill run.json `bridge:` strings — the grep must cover
  run.json too (50075 was the first zero-hit port).
- **The seedlab promotion was my recommendation and the probe
  falsified it in one iteration.** Worth recording as a reasoning
  lesson, not just a number: "promote the enrichment term into the
  retired term's slot" treated loss-mass as fungible across terms with
  very different batch structure — a dense per-window term and a fixed
  170-window batch do not interchange at equal `frac`. The tell was
  available pre-launch in probe1's own artifacts (seedlab CE already
  falling 2.73 → 0.29 at HALF the weight) and I read it as healthy
  learning rather than as a memorization curve.

## Next session picks up

**A short build round, in this order (ADR-0087 decisions 2–3):**

1. **The calibration fix FIRST** (ahead of any M10 relaunch — the
   defect is loop-wide, not M10-specific): per-window `w`
   recalibration instead of freeze-at-calibration, and/or per-step
   subsampling of a fixed batch so it can't be fitted in ~10 steps.
   Needs a guard that can actually see the impulse (the current share
   telemetry starts after the collapse). `paylab` is the same shape
   and should be audited in the same pass.
2. **The mint-scale decision** — the per-era re-mint is now a
   prerequisite, not a follow-on. The sweep certifier is the
   machinery; `m10-probe1`/`probe2` stores are certifiable
   on-distribution material. The open design tension to resolve while
   scoping it: the own-emission term is degenerate (ADR-0085) *and*
   was the content driver (ADR-0087) — a grounded dense term at
   trajectory scale is what both findings point at.
3. **Relaunch as `m10-probe3`** (fresh name per the ADR-0076 rule).
   ADR-0084 gates + the ADR-0086 restated decode leg carry unchanged;
   no graft change ⇒ floors still stand.

- Watcher note carried: filter the paylabels 12-known-misses
  `FRAME ANOMALY` pattern; trip if the join summary deviates from
  263/12/5.
- `m10-probe2` iteration-0 artifacts kept (rejected ckpt + the
  memorization curve + the probe1/probe2 paired iteration-0 read).
