# 2026-09-09 — evening 4's overnight lands; community watch

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Babysit picks (00:53–08:53)

The pool ran at 73–76 g/h through the night (402 at 00:53, 697 at 04:53, 861 at 06:53, 973 at
08:53); one wall-clock timeout + the standing poison on one worker (04:55, recycled), the known
AIOOBE engine class once. The chain and the ablation arm waited. Both forkchecks PASS (09-08
evening): pins `e44d83a8327` → `c26e99824b8`.

## Community watch (Discord 09-08/09; user brought the thread)

Banked in [discord-ai-plotting-survey.md](../design/discord-ai-plotting-survey.md) with the draft
replies: LordOfThePigs' effect-model design doc (read: script-surface per-ability encoder + an
effect head over seven observed-effect record kinds; not yet run) — fork K stands, two devices
noted to borrow at Build 4 (keyword-expansion dropout; the identity-only baseline for the held-out
card probe); Kryptic's benchmark question and chrismaghuhn's rejected/traj question with the user's
answers; talor's Modal (5× his Mac on the train step) + M4 Pro 48 GB; khaliostr's
`@manabrew/forge-wasm` (GraalVM Web Image, Node/browser, JSON protocol v5) — assessed not relevant
to the loop (engine-bound: bridge tax 2.6%, serve path wait-dominated, the train phase
loader-bound with the GPU ~90% idle; the prompt surface vs our 64 callbacks; no search machinery),
relevant later only as a browser Mentor surface with fork H; the RL-harness needs list drafted for
his ask. The M4 commentary: engine-bound → `--workers` ≈ cores − 2, server on mps/cpu (verify the
bf16 autocast), train ~20% of an iteration so a 5× slower train step ≈ 1.8× iterations, `selfplay`
lacks a `--device` passthrough (a small patch), generation-local / training-on-Modal feasible by
hand via the standalone rl step.

## Next session picks up

- The chain's reads as they land (`data/runs/build3-e4/pool-read.md`, `payfit-read.md`,
  `pay-smoke/read.md`, `build3-surface-read-b3e4/read.json`), then the ablation arm's
  `build3-e3-nopay/read.json` → the ADR-0104 term.
- Small QoL: `selfplay.py --device` forwarded to rl + server; a `--no-autocast` switch (for the
  Mac users) — after the chain unregisters (no tree edits while it runs).

## The pool landed (11:55) — the pool read; the pay-only head does not learn (folds 0–2)

**Pool** (`data/runs/b3-surflab3-20260908-193402`, 1,000 games, 16.3 h at ~73 g/h on 8 workers):
**42,638 usable payment groups (42.9 / game)**, all at the eot leaf; margin p50 0.000 / p90 0.016 /
p97 0.057 / p99 0.137; **positives at bar 0.03: 2,423 (5.7%)** (0.01: 12.9%, 0.05: 3.4%, 0.08: 2.1%
— ADR-0075's 3.2% certifiable sits between). Copy payment windows 736K: directed 249,044 ok / 733
fail (0.3%) / 182 salvage, 41 misses. **Resolution-effect census (the queue's measured argument):
48,758 windows / 1,000 games, 48,353 zero-cost (99.2%), 405 with mana, 230 consequential — 0.23 per
game, all auto-payable**, vs 9.4 mainline consequential windows per game (2.4%).

**The cross-fit, folds 0–2 (pay-only params, bar 0.03, T 0.025, lr 1e-3, 3,000 steps):** CE 0.45 →
0.39 by moving TOWARD auto — pos_top1 0.02 → 0.01, pos_dev 0.04 → 0.03, tie_dev 0.03 → 0.016.
**The pay-only head cannot represent the positives**: a positive's target is mostly one goal among
goals of the SAME kind (the smoke's `paykind [-1,2,2,2,-1]`), and `pay_bias` + `pay_kind_emb` can
only shift kinds against auto; the entity ranking sits in the frozen pointer. The learnability
question moves to capacity, not the target. Launched 12:45 the fold-0 variants `--unfreeze 2` and
`--unfreeze 4` (top trunk layers; lr 3e-4) → `data/runs/build3-e4/variant/`; the chain continues
(the built pay-only head serves ≈ auto under the bar — the paired read becomes the "nothing broke"
+ the rescue arm's read; the served-head question waits on the variants and a drift read).

## 13:10–13:30 — the chain's smoke rc=1 (a relative path), the capacity probes, the set-keyed head

- **The chain stopped at its smoke**: `SMOKE="$OUT/pay-smoke"` was relative and the java launch cds
  into `forge-gui/`, so the log redirect failed before the jar ran (rc=1, no anvil.log; nothing written
  into the fork checkout). Fixed to `$REPO/$OUT/pay-smoke` (the 09-06 absolutize rule again). The
  build landed (`data/training/m12-build3-e4/last.pt`, the pay-only head); the smoke + the paired read
  re-run by resuming the chain once the box is free.
- **The chain's exit released the ablation arm** (`build3-e3-nopay` started 13:10; ~1.5 h).
- **The capacity probes are negative too**: unfreeze-2 pos_top1 0.020 → 0.016 (tie_dev → 0.006);
  unfreeze-4 0.020 → 0.023 (tie_dev → 0.008). Trunk capacity does not rescue the positives —
  consistent with **the featurization diagnostic: 56% of positives' best goals share their
  (representative entity, kind) key with another goal in the window** (78% of positive windows have
  all goals of one kind); the M9 one-representative key bounds any head.
- **Built on the worktree `anvil-wt-e4head` (branch `e4-payhead`)**: the featurizer emits each goal's
  plan as an entity SET (`cand_ents`, K = 8, lowest ids), collate carries it, `AnvilNet` scores pay
  windows with `pay_query` / `pay_key` (load_compat copies of `ptr_query` / `ptr_key`) over the pooled
  set + the kind embedding (`torch.where` on the task, every other window untouched),
  `pay_distill_loss(pos_weight)` + `pay_fit --pos-weight`. Fold-0 variants launched from the
  worktree: pos-weight 1 and 8.

## 13:40 — the set-keyed head, fold 0 (pos-weight 1)

The set key carries the signal: **pos_ce 5.90 → 3.59** (the pay-only head: 6.16 → 5.62; unfreeze-4:
→ 4.98), tie_dev 0.017 → 0.0035 (confidently auto on ties) — but the argmax stays auto: pos_top1
0.007 → 0.018, pos_dev 0.020. The class balance (94% ties + the +2.0 auto bias) is the remaining
lever; the pos-weight-8 fit decides. Trainable 529,936 (the role copies + the pay pieces).

## 13:40 — the served-tag ablation: +2.56 ± 1.88pp

`build3-e3-nopay`: nopay − init +2.56 ± 1.88 (t 1.4, n 586). The untrained pay head cost every
M12 network arm ~2.5pp vs `iter-019`; dz − ref corrects to ≈ +0.9 ± 2.2, heurla − dzla to ≈ −3.0 ±
2.3 (ADR-0104 addendum). The never-serve-fresh-init rule now covers the pay head (the server gate
reads `pay_fit`; built on the worktree). The chain resumed from its smoke at 13:45 (the box free).

## 13:55 — the served-head smoke (the pay-only e4 build, `--pay-bar 0.2`)

Run by hand (the resumed chain skipped it on the stale rc=1 DONE — the script now treats a failed
smoke as not done): 8/8 won, 156 payment windows bridged, 154 auto / 2 goals (`directed_ok` 2), the
bar passed 1 and held 1, 0 server errors. The pay-only head under the bar ≈ auto, as the folds said.
The paired read `build3-surface-read-b3e4` (off / on / rescue) runs meanwhile (off arm from 13:39).

## 13:50 — the set-keyed head learns at pos-weight 8 (fold 0)

| config (fold 0, bar 0.03) | pos CE | pos top-1 | pos dev | tie dev |
|---|---|---|---|---|
| before (e3, init) | 5.90 | 0.007 | 0.020 | 0.017 |
| pay-only head | 5.62 | 0.014 | 0.027 | 0.016 |
| unfreeze 4 trunk layers | 4.98 | 0.023 | 0.032 | 0.008 |
| set key, pos-weight 1 | 3.59 | 0.018 | 0.020 | 0.0035 |
| **set key, pos-weight 8** | **2.17** | **0.090** | **0.146** | 0.051 |

The first configuration whose argmax leaves auto where the search says to: 9% of positives get the
search's best goal (62% of its deviations on positives hit the best goal), at 5.1% deviation on
ties (ties are within the leaf noise of auto by definition; the serve bar trims them). The 5-fold
cross-fit + build launched 13:48 at pos-weight 8 (`data/runs/build3-e4s/`, ckpt
`m12-build3-e4s`, watchd `build3-e4s-fit`); its paired read follows the pay-only one (the branch
merges into main first — the server needs the set-key featurizer).

## 14:40–15:00 — the pay-only head's paired read: on − off −2.91 ± 1.85; the probe-path hypothesis

`build3-surface-read-b3e4` off / on (the rescue arm re-running after an argparse quirk: a
`--forge-args -payrescue` value read as a flag in the read script AND one level down in
`final_read.py` → both now pass `--forge-args=`): **on − off = −2.91pp ± 1.85 (t −1.6, 50 up / 67
down, n 585)** — the same size and sign as the ablation term (+2.56 withholding), yet the pay-only
head under the bar deviated on **0.137 windows per game** (81 `directed_ok`, 0 failures, 57 games
with any deviation), and the games WITHOUT a deviation read −2.8 too (n 536). So the cost is not
the head's answers. What differs when the tag is bridged: on every in-scope window (9.5 / game)
the mainline path runs the M9 enumeration and the auto-payability PROBE — `ComputerUtilMana
.canPayManaCost` in test mode, which draws `MyRandom.percentTrue` per candidate source
(`isManaSourceReserved`, the ADR-0102 mask-cache class) and clears / writes AiCardMemory
reservation sets — before the same auto payment the unbridged path runs. Whether that alone costs
strength is testable in one arm: **the tag bridged with the server answering auto on every
window (`--pay-bar 100`)** vs the off arm on the same seeds — queued behind the rescue arm
(`data/runs/build3-e4-autoonly/`). If it reads ≈ −2.8 the probe path is the cost (a fork fix:
snapshot the memory sets + a scratch RNG around the probe, serve-only); if ≈ 0 the two reads are
noise around a small deviation cost. (A first diagnostic scored every game as a loss — the game
record's `winner` is a player name, not a seat index; the corrected pairing agrees with
`build2_read`: −2.78 ± 1.84.)
