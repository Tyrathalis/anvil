# 2026-07-27

Short session closing out the first QoL build round: human verification of
items 4+6, one question traced to ground, and the harness question for future
UI work answered with evidence.

## Done

- **Human pass on items 4-T1 and 6 — both VERIFIED in real use (user).**
  Resizing and KWin edge-snapping "work great"; "the Provide Deck URL folder
  works great." That was the only thing left on both items. Residue: a full
  match at half-screen/maximized hasn't been specifically reported — expect it
  incidentally.

- **The "dc-863786" decks question traced end-to-end.** They are the 113
  research-pool decks (`anvil.pool install` → `~/.forge/decks/commander/`),
  visible because the playable build shares Forge's user deck store with the
  training harness — the worker resolves decks *by name* in that store
  (`build.py:87` bare filenames → `orchestrator.py:121` → `AnvilRun.java:302`
  via `SimulateMatch.deckFromCommandLineParameter`). Unrelated to the URL
  import (own store). **User decision: leave them visible.** New worklist
  section records the real hazard — GUI edits to `dc-*` files would silently
  change research generation; nothing verifies installed files at launch —
  plus the queued ~30-line rider: launch-time hash check against
  `data/pool/decks/`, next to the existing jar-hash verification.

- **"Was it the monitor?" — no, with evidence.** Session was `Active=yes`,
  output enabled, and the app visibly re-rendered during the resize tests (that
  is how the projection bug was caught). The failure is input policy: XTEST
  pointer injection is refused under this Wayland compositor while window
  management (`xdotool windowsize`) is honored. **`ydotool` (kernel uinput
  path, daemon already running) also failed to reach the app** — accepted
  commands, no reaction, so no quick on-desktop fix exists.

- **Harness recommendation for future UI work, grounded in the code:**
  item 5's mobile hit-test (`FCardPanel.renderedCardContains:74`) is **pure
  geometry** — no GL, no display. So step 1's pixel-identity gate is better
  served by a unit test (new implementation vs exact-90 original over a dense
  point/size grid) than by screenshots. Cost surfaced: `forge-gui-mobile` has
  **zero tests and no test dependency today** (desktop: 65). For the genuinely
  visual residue (draw order, "looks right"), Xvfb (`xorg-server-xvfb`, in
  repo, not installed) is the candidate — a real X server where XTEST just
  works, off-desktop; risk is llvmpipe GL for libGDX, unproven.

## Broke / surprised me

- **The Anvil checkout was on `security/playable-multiplayer`** (the parallel
  workstream's branch) when this session went to update docs — my grep for
  yesterday's passages came back empty and the recent log was someone else's
  commits. Resolved without touching the checkout: temporary `git worktree` on
  `main` in the scratchpad, edit, commit, remove. Good pattern for doc updates
  whenever the shared checkout is claimed by the other track.

- `ydotool mousemove -a` coordinate mapping is version-dependent; one stray
  click may have landed near the desktop's top-left during testing.

## Next session picks up

- **Item 5 step 1** (hit-test generalization at unchanged 90°), per the agreed
  4→6→5→7 order — with the geometry-unit-test shape rather than screenshot
  verification. Decide where the helper lives (a static `(x, y, w, h, angle)`
  function keeps it libGDX-free and dodges the `Utils` class-init landmine) and
  whether to add test infra to `forge-gui-mobile` or host the test elsewhere.
- Optional pre-step: Xvfb spike (does the mobile build boot on llvmpipe?) — only
  worth it if we want automated visual checks for item 5 steps 2+.
- Research-side rider when convenient: the pool-deck hash check at
  `launch --pool` time.
- Standing non-QoL next items unchanged: M3 closeout decision, Grindstone/M4
  design session, collate refactor before the next long RL run.
