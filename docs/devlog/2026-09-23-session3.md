# 2026-09-23 (session 3) — the fluency pass

## Done

- Reviewed the docs for context cost and readability. The big costs: CLAUDE.md at 25.8K chars (a third of it the fork pin cell), and m12-plan.md at 168K (129K of it the running record).
- **Running record split out:** `docs/design/m12-running-record.md` holds the record verbatim; m12-plan.md is now 36K (charter, build order, forks, kill conditions). `docs_index.py` names the record file next to the open plan when one exists.
- **Fork lineage:** the pin cell's commit chain became `docs/design/fork-lineage.md`, one table row per tip (what, scope, forkcheck, PASS, ADR) with the recurring misses explained once. The state-of-record cell now holds just the pin.
- **CLAUDE.md** 25.8K → 17.4K: the Now block rewritten into short sub-bullets, the served-build row trimmed. The old Now block and both rows went verbatim to status-archive.md.
- Going forward: a docs writing-style line under Hard conventions. The remaining review items (standing-rules line caps, §3d′ ledger cut, memory consolidation) are routed by name to the next documentation pass in the wrap-up checklist.

## Broke / surprised me

- The recipe flag string was only in CLAUDE.md and the quickstart (§7), not in the plan; the Now block points at the quickstart.

## Next session picks up

- Append to `m12-running-record.md`, not the plan. Nothing running was touched (docs + `scripts/docs_index.py` only).

## Follow-up (same session)

- The milestone table moved to the root README (`README.md#milestones`), unchanged; the README's stale Status paragraph (still saying "M12 in scoping", 52.8%) was refreshed. CLAUDE.md keeps a one-line pointer + the never-compare-across-eras warning.
- The Build 1 day-zero row left State of record (never promoted; verbatim in status-archive.md).
- The launcher convention was split into sub-bullets without the dated add-ons.
- The items routed to the next doc pass moved from the checklist into m12-plan's "Out of scope / routed by name", plus a new post-run item: one `RECIPE` source for the four `scripts/*_chain.sh` copies (not editable while `shakedown_chain.sh` runs).
- CLAUDE.md 25.8K → 14.1K over the session.

## Quickstart pass (same session)

- §7a rewritten for a reader new to M12: what the search does (option search, acting, the follow-up choice, where to search, what the network learns) before how to turn it on; a four-term glossary; the recipe string once (the `--search-recipe` value; the driver's alloc flags described, not repeated); how to tell whether it helps (network-alone vs with-lookahead); the flag table in plain words, internal ADR citations dropped. Framed honestly as optional and untested outside the project.
- A *Formats: what works today* table and the `--format Constructed` vs `--pool-format pauper` distinction; step 3's broken numbered list as bold labels; `FORGE_DIR` exported right after the clone (the fallback is the author's path); §8's number to 53.5% on the current engine; a search row in the cost table.
- **Found:** the Constructed slot's build drops every deck containing a Pauper-banned card — undocumented, and a trap for any non-Pauper list. Documented an empty-snapshot workaround (by code reading of `latest_banlist()` / `build()`, not run); a `--banlist none` flag and a sane `FORGE_DIR` fallback routed to the post-run worktree (m12-plan, routed by name).

## Format onboarding (same session)

- `docs/design/format-onboarding.md` written: the four layers a "format" touches (game type, pool slot, model row, research assets), what runs today, the settings per format, the add-a-format checklist, and the research-grade assets.
- The quickstart is format-agnostic: a *Pick your format* block sets `SLOT` / `GAME` / `POOLDIR`, and every command uses them; step 3 covers both slots' deck shapes and banlists; three new troubleshooting entries.
- **Found:** Constructed raises `VocabError` on main since 08-21 (no model row), so the quickstart's own headline path is blocked. The quickstart says so up front. The fix has a trap: `load_compat` pads at the end of the globals, but the format scalars now follow the one-hot. Routed to the post-run worktree with its identity test. Kryptic's 09-07 run must have predated this or patched around it; worth asking.
- Also found: the build needs a `<id>.json` beside each decklist (`{}` works); now documented.
