# 2026-09-25

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · the running record (mNN-running-record.md) · Now block · commit). -->

## Done

- 09:00 status: shallow at iteration 16, 17.7 / 30 h, ≈ 66 min per iteration; Spearman at
  iterations 8 / 12 / 15 = 0.303 / 0.262 / 0.246 — the drop arrived, same floor as the other arms,
  same curve by box time; the search-depth hypothesis is out (running record).
- 20:40 status: shallow at iteration 25, 27.0 h at its start. The first heuristic store sat at 26 / 120
  for ≈ 2.1 h — the GPU yield to Tabletop Simulator (≈ 18:30 → 20:38), by design; resumed at once.
  The wall clock counted the yielded time: WALL-STOP after iteration 25 (≈ 21:10), ≈ 28 productive
  hours, 26 iterations. Recorded for the verdict's per-box-hour pricing (running record).

## Broke / surprised me

- A yielded harness prints the same `N/M (g/h)` progress line every tick with a decaying rate and no
  yield marker on the line; the `YIELDING:` / `resumed: gpu quiet` lines are ≈ 130 lines apart and
  the loop log has no timestamps. First read looked like a dead stall (no `java` in `pgrep -c java`
  because the workers are children named `java` with arguments — `pgrep -c "^java$"` misses them).
  Routed: the progress line carries `(yielding: <proc>)` while yielded; the loop log gets timestamps.
- The wall budget counts yielded time — routed post-run: subtract yielded seconds (≈ 30 lines).

## Next session picks up

- Shallow's close (≈ 22:00–22:15): the 2,000-game read into `read.md`; deep launches from the chain
  (≈ 30 h + reads → ≈ 09-27 05:00). Price shallow at ≈ 28 productive hours.
- The launcher's stall tick (180 min) would have fired ≈ 21:30 on a benign yield — consider a yield-aware
  stall check (the harness's `gpu-yield.json` is right there), post-run.
