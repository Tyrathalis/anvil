# 2026-09-06 (session 2) — architecture review before M12 scoping; M12 rechartered

## Done

- **Architecture review** at the user's request ("are we focusing our efforts in the right
  places? plausible structural mistakes?"): read the design doc, m12-plan draft, standing rules,
  M3–M11 closeouts; surveyed the package as built (AnvilNet 46M trainable / 10-layer d=512 trunk,
  one sigmoid value head, no belief head, Tutor/Mentor/heads stubs; V-trace loop 480 games/iter,
  lr 1e-5, KL 0.06, replay 4; bridge families; `final_read.py` shape) and the run/store telemetry
  (806 stores, ~317K selfplay-heuristic + 106K drill-fork games; 299 iterations, 122 h gen +
  109 h train across every RL run; 800–1,600 g/h bridged; game time median 18 s / p90 342 s).
- **Findings** ([ADR-0101](../decisions/ADR-0101-architecture-review-m12-recharter.md)): (1) the
  value function is the central asset and was never a milestone's object — rollout labels sit
  in the store as audits; (2) the loop is ~100× under the design's own sample budget and
  throughput is binding, mis-read as "signal density" alone; (3) the veto economy is an inexact
  legality mask (Forge `canPlay` not payment-aware; the M9 enumerator can make it exact); (4) the
  hybrid agent's excluded surfaces were read one at a time under a ~2pp floor that hides their
  sum; (5) Commander is the worst-case research testbed; (6) focus: named deliverables unbuilt,
  the Android ship of `iter-019` is available through the playable track, cadence + docs load.
- **Direction agreed with the user** (chat → ADR + plan, per the no-chat-only rule): M12
  rechartered as a staged build to ONE BIG RUN — engine bundle → value head → search (the one
  gate + a heuristic+lookahead control arm) → the whole decision surface (three answer shapes,
  natural line always in the option set, smoke reads not gates) → representation completions →
  the run (≥ 300K games, power statement). Search = drill-finding; budgets in evaluations, anytime
  (Gumbel-shaped), pivotality-allocated with a uniform floor, per-device calibration at deploy;
  the network-alone gap is the kill condition; ladder of own checkpoints + ε-margin difficulty
  dial now, skill token after the run. Turn-plan latent inert; planner/scorer stay retired.
- Docs: ADR-0101 (proposed; scoping session adjudicates), m12-plan.md rewritten (charter, shape,
  build order, forks A–H with B–F adjudicated, done-when, kill conditions, running record),
  CLAUDE.md Now bullet, design doc §13 M12 clause, standing-rules (+5, new section), map (start
  line, Now card, M12 block, ADR row, footer), docs/README.md regenerated.

## Broke / surprised me

- The M0 "throughput risk retired" verdict was computed against the BC corpus at heuristic
  speed; nobody re-priced it against the RL budget at bridged speed with training in the loop.
- ~800 windows at 10³ labels was the label scale of every learnability read; the design's own
  sample budget is 10⁶ games. Seven TIEs are consistent with under-powered runs.

## Next session picks up

1. **The M12 scoping session** against ADR-0101 + m12-plan: pin the Build 1 numbers (one-ply
   ranking 0.28 →, state-ranking Spearman →), the day-zero re-read rule, the big run's power
   statement shape; adjudicate forks A (leaf event), G (Pauper testbed), H (Android ship of
   `iter-019`); decide the cadence / standing-rules prune items.
2. Then **Build 0 opens in the fork** (exact payability filter first — it's also the cheapest
   way to see the veto tripline drop to ~0 — then caps, then the directive) and **Build 1 in
   parallel** on the existing stores (the 106K drill-fork games, cl2 forks, composites).
3. Housekeeping unchanged: the recording jar's ADR-0025 proof is owed before any jar generates a
   training store; cl2 forks store kept through Build 1.
