# 2026-08-17 — M8 planning session → M8 OPENED (m8-plan)

## Done

- **Rebase assessed and DECLINED:** the research era `d798917ae5` sits
  on upstream of 2026-08-11 (the ADR-0055 boundary rebase). Delta since
  = 80 commits / 6 days: 131 `forge-gui/res` files (45 card scripts,
  **zero intersection with pool `cf2ca6ba`**), 7 engine-path commits,
  none on our game path (AI X-sizing fix targets two out-of-pool cards;
  LandMana LKI cache = perf refactor; rest GUI/edge). A boundary would
  cost forkcheck + a 2,000-game re-baseline for nothing. Stay on
  `d798917ae5` through M8.
- **Carried-items audit — the M4 fork items are ALL already resolved**
  (they landed in the ADR-0055 D3 boundary; CLAUDE.md's M4 bullet was
  stale): MayPlay `.get(0)` = GameCopier effect-source restore
  (`b361dfcb8f`), IndexOOB class = STATION tap-guard (`9f0a2c0886`),
  MinMaxBlocker fixed 07-25, targeting-retry closed-as-bounded (reopen
  trigger armed). Also stale: the M3 bullet's "still open" mtime-hazard
  line (retired at M4 close via the pool `CURRENT` pin). Both CLAUDE.md
  lines fixed.
- **Next-boundary bundle recorded on the map watch list** (user call:
  one era event when it pays for itself): upstream rebase +
  **multi-format learning enablement, model side** (user 08-17; the
  pool scaffold landed as fork #8 at ADR-0055 — what remains is format
  conditioning / per-format training paths) + 2-arm forced-seq
  campaign trim + copy-state divergence forensics + upstream filings.
- **M8 OPENED ([m8-plan.md](../design/m8-plan.md), user-approved
  shape):** one lever to a promotion attempt. D1 = the natural-timing
  probe (single NATURAL arm, K=32/N=4 on `drill-selection-v5-active`,
  labels-row extension for the natural arm's first realized cast +
  turn offset; ~1h box time) measuring the untested substrate —
  within-natural timing variation at plan granularity (ADR-0051 killed
  it at single decisions; ADR-0053 only measured forced arms). Gate
  proposed, to PIN at design before numbers: split abundance ≥30% ·
  RMS true Δwr ≥ 0.10 · implied whole-game headroom ≥ ~1pp. Funded ⇒
  D2 natural-timing formulation (argmax timing bin incl.
  holding-where-holding-wins) + one run vs 0.5373 ± 0.0112. Failed ⇒
  D2′ pre-registered pivot: curriculum × rank-critic curation, entry
  gated on a rollout audit of `rank-critic-c2v3` ordering (ADR-0036).
  **Pre-commitment: a timing-branch TIE closes the credit-assignment
  family at this scale.** Riders: battery `--compare` overlay;
  throughput only if economics demand.
- **M7 stale-data pass, resumed:** discovered the previous pass
  partially ran — `data/training/d6-run14`/`d6-run15` already deleted
  (driver logs kept). Reconstructed the runs-store kill list
  (references grepped clean; keeps honored: finalarm gate stores +
  Ante ledgers, seqp32 probe stores, drilleval, `training/d6-run16`):
  `d6-run14-i00*` ~340M · `d6-run15-i00*` ~530M · `d6-run16-i0*`
  ~3.3G · `d6-run16-arm-*` ~270M · `d6-seqfix-smoke-*` ~126M ≈ 4.5G.
  The `rm` was classifier-blocked in-session; **PENDING user
  execution:**
  `cd data/runs && rm -rf d6-run14-i00* d6-run15-i00* d6-run16-i0* d6-run16-arm-* d6-seqfix-smoke-*`
- Docs swept: CLAUDE.md M8 bullet + stale-line fixes; map (pills,
  start-here, Now panel → M8 card, M7 details completed with the
  run14/15/16 arc + closeout and its missing `</details>` fixed, M8
  details block, watch-list bundle item, throughput wording,
  drill-assets row → v5-active/evalset-v4, footer stamp).

## Broke / gotchas

- The map's M7 `<details>` was left unclosed by the 08-16 sweep (15
  opens / 14 closes) — everything below M7 nested inside it. Fixed.
- Status bullets can lag boundary ADRs: the "carried fork items" list
  survived two milestones after the items were fixed. When quoting a
  carried/queued list from CLAUDE.md, verify against the latest
  boundary ADR first.

## Next session picks up

1. User runs the pending kill-list `rm` (above).
2. D1 design session: pin the probe gate numbers (before any
   generation), spec the natural-arm labels-row extension, then run
   the probe → ADR.
3. Playable-fork and Chronicle tracks unchanged.
