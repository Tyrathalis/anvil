# 2026-08-12 (overnight autonomous close: D3 boundary ADR, new gate 0.5373)

## Done

- **D3 boundary CLOSED ([ADR-0055](../decisions/ADR-0055-d3-boundary-close.md)):
  new standing gate 0.5373 ± 0.0112** (`d3-rebaseline`, 1,999 games,
  iter-019 vs the current heuristic on fork era `d798917ae5`). Supersedes
  0.5316. **iter-019 HELD through the rebase** — unlike both prior
  boundaries, no re-scale down, despite #11436 strengthening the
  heuristic path. Run hygiene: 0 crashes (prior-era reads carried 2–3 —
  the stability pass visible in production), 0 poison/deadline events
  (#9 validity trivially clean).
- **Chain v2 executed end-to-end unattended:** forkcheck-v2 (500 games,
  fixed jar) → detached handoff → final_read → completion notify — the
  session-independence rebuild proven in one pass.
- **Fork fidelity: 11.6% divergence (442/58), statics 0** vs 7.0%
  prior era; #11436's simulation validation = prime suspect
  (AI_TIMEOUT-sensitive eval on the heuristic path). `FIXED_HASH=1`
  discriminator launched on the quiet box post-read (watchd-registered
  at launch); result annexes into ADR-0055.
- **#9 label-span sweep RESOLVED (owed from the fix): ≤181 suspect
  rows, upper bound, nothing threatened.** 84 in the parked July
  rollout pilot; rest ≤15/invocation across the drill era. The
  140-event drillo1 worker yielded only 2 label rows — post-desync
  answers were garbage, games crashed instead of labeling: the bug
  self-limited its contamination. No re-runs; ADR annotation suffices.
- CLAUDE.md gate line + map swept (state-of-record, run ledger boundary
  row, ADR index, next-row gate).

## Broke / surprised me

- Nothing broke overnight. The surprise is the good kind: the first
  boundary where the ckpt of record did not lose ground to the
  heuristic, and the first fully-unattended boundary close.

## Next session picks up

1. **FIXED_HASH discriminator read** → annex into ADR-0055 (same-rate ⇒
   copy-state investigation; lower ⇒ #11436 nondeterminism class,
   accept).
2. **DOCUMENTATION CLEANUP PAUSE (user-pinned, blocks all further
   implementation):** trim CLAUDE.md Status + project map, index/
   reorganize docs/, consolidate memory (incl. the mechanism-candidates
   sweep). Then:
3. ADR-0049 instruments → scripts/ (baseline read = the d3-rebaseline
   stores), then the C-bundle build vs the 0.5373 gate.
4. Upstream filings queue (station guard, GameCopier effect-source,
   monarch NPE) + Talor follow-ups (#9 reply posted; he closes #8).
