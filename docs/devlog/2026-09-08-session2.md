# 2026-09-08 (session 2) — Build 3 evening 4 opened: the payment tag

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## State review (18:30)

Evening 3 closed at 16:30; six surfaces + tuck served from `m12-build3-e3` on fork pin
`41ac60d6b21`; box quiet, tree clean. The record's next step = evening 4 (the payment tag), then
evening 5 (mainline surface acting) — Build 3's done-when (every family answered *under search*)
needs evening 5. User: "section 4" = evening 4.

The facts that reshaped the evening (checkpoints + today's on-arm census + the fork):
- **The pay head has never been trained**: `pay_bias` 2.000 / `pay_kind_emb` 0 / `pay_mark_emb` 0
  in the day-zero ckpt AND e3. On arm s0 of the e3 read: 2,805 bridged consequential windows /
  300 games (9.4 per game), pick auto 2,729 / goal 76 (2.7%, every one `directed_ok`), forced 30.
- **Search copies bridge the pay tag** (GameCopier keeps the seat's bridge + tag set; no flag to
  stop it) → the untrained head's random deviations sat inside every copy of every read since
  Build 2.
- **The surface machinery had no payment kind** (no kind, no directive variant, no serve tag);
  the M9 enumerator + directed executor exist; `PAYSHADOW` counts the rescue class census-only.
- **The expansion round takes only the first traced surface per candidate** — a payment fires at
  cast and would have crowded the entity / mode surfaces out.
- Imitation is empty for payment (auto = option 0 = the init); the M9 pin stands at zero cost.
- Headroom (ADR-0075): 0.32 certifiable windows / game × +9.2pp ≈ +3.0pp / game; 3% of windows.
- The ADR-0077 queue: cousins / costmod / pool-tie CLOSED at ADR-0083 (costmod auto-pays 4.7% of
  in-scope windows now, not 25%); resolution-effect payments (51 / game, unbridged) the only open
  item, its consequential fraction never measured.

## Decisions (user, 19:00–19:20)

The ADR-0105 addendum carries them: the asymmetric target (auto unless a goal clears a margin bar;
rolls ≥ 2; ties / positives read apart), the end-of-turn leaf for payment answers (horizon +
variance, not depth; the horizon-2 certify rollout as the calibration instrument, never the label),
the copy-side pay gate, the payment surface's own expansion slot, the rescue bundle as a
flag-gated second commit with its own arm, a serve-side margin bar, the queue untouched
(resolution-effect payments → the Build 4½ opener conditional on the calibration; the measured
argument paid by a census row), "train enough" = a learnability read on positives vs ties.

## Built (19:00–19:35)

**Fork `e44d83a8327`** (on `41ac60d6b21`): `Surfaces.PAY` (kind 7) + the enumerator case;
`PlayerControllerAnvil.copyPay` (the acting seat's consequential windows on a copy: M9 enumeration
as on the mainline, `Obs.decBridged` on the copy's session = the frame, a PAY `SurfaceDirective`
answered through `executeDirected` + auto completion with cousins armed, every window traced) and
the gate `copyPayBridge` (default off; `-searchpaybridge`); `SearchDirective.leafAfterTurn` +
`window(options, quiescent, turn)`; `AnvilRun -searchpay B -searchpayleaf eot|next`, the pins on the
search header, `expandRound` (both slots through one helper; the natural re-run under eot; `"leaf"`
on the sub row); `-paytelemetry` → one census row per resolution-effect window (`reff`; zero-cost
counted, else goals / plans / conseq / autoable / costmod, scratch RNG). `SurfacesEnumerateTest`
17: **the two evening-3 damage assertions had never passed** (`damageFromSequence(a(1,0))` realizes
`[2,3,1]`, per-blocker lethal; the "[3,2,1]" expectation was wrong — the stale-per-class-report
trap) → corrected. mvn package clean; the test class 17/17.

**Anvil**: `anvil/training/pay_distill.py` (`pay_groups` → the asymmetric target; `PaySubRows` over
the frames through `Featurizer.example(frame, header, "pay_class")`; `collate_pay`;
`pay_distill_loss` — CE from the target to the pay head's candidate softmax, stats per class),
`anvil/training/pay_fit.py` (cross-fit by game seed, `--unfreeze pay` = 4,624 params, before /
after reads, `--read`, `--build`), `AnvilNet.act` returns `policy_logits`, the server's `--pay-bar`
(a goal stands only where its log-prob clears auto's by the bar; counts `pay_bar_auto/goal`),
`surface_served_census.py` payment rows (mainline / copy / resolution-effect), the label script's
`PAY / PAYLEAF / PAYTEL / TAGS` knobs. Smoke-tested end to end on the 8-game smoke: 102 pay sub
rows → 97 ties / 5 positives at bar 0.03, 88 examples, the loss and a 60-step fit run. pytest 294.

## Smokes (19:20–19:45)

`data/runs/build3-e4-smoke` (pre-census-fix jar) and `build3-e4-smoke2` (= the snapshot), 8 games
each, rate 0.3, rolls 2, `-searchsurf 2 -searchpay 2` eot, `-paytelemetry`, the e3 ckpt serving:
pay sub rows with a frame on every row; copies: natural 701 / directed 726 (`directed_ok` 554,
0 misses); the first traced window a 4-way tie (identical values), later windows spread; cost
4.89 pay copies per searched window at ~25 asks each vs 31 first-ply asks (≈ 4×; mean 3.1 s per
window). Resolution-effect rows: **170 / 170 zero-cost** in smoke 2 (8 games). The known
`CHRONICLE_BINDER` prefs warning in the log (the playable-branch enum), harmless.

## Launched (19:33–19:34)

- **Forkcheck `run-20260908-build3-e4`** on the snapshot `data/runs/build3-e4/forge-b3e4.jar`
  (sha `5b01fe00…`, commit `e44d83a8327`, dirty 0), 500 games, seed 20260703; watchd
  `forkcheck-b3e4` (stall 30 m), the waiter notifies. Read: `scripts/forkcheck/compare.py`.
- **The payment pool `build3-surface-labels3`** (`data/runs/b3-surflab3-20260908-193402`): 1,000
  games, 8 workers, the label worktree at `c1edc0d`, the e3 ckpt with server tags = everything but
  the pay tag (mainline payment = auto; copies gated), `-search -searchrate 1 -searchrolls 2
  -searchsurf 2 -searchsurfcap 8 -searchact 0.10 -searchtemp 0.025 -searchpay 2 -searchpayleaf eot
  -paytelemetry`, seed base 20260909, deadline 20 s; watchd `build3-b3-surflab3` (stall 60 m),
  notifies DONE / FAILED. Sized ≈ 6 h from the smoke's per-window cost.

## Next session picks up

- The forkcheck read (`compare.py` vs the 08-21 baseline) → the pin `e44d83a8327`.
- The pool read: pay sub rows / game, positives at bar 0.03 (and the margin distribution — the
  bar is a fit knob, not a pin), the resolution-effect census (the queue's measured argument).
- `pay_fit.py` cross-fit (5 folds, `--unfreeze pay`) → `--read` → `--build` →
  `data/training/m12-build3-e4/last.pt` → the served-head smoke with `--pay-bar` → the paired
  read (`build3_surface_read.sh` NAME b3e4: off = TAGS_OFF without the pay tag but WITH the
  surfaces + tuck, on = + pay) — pre-registered: nothing broke + the learnability read
  (positive top-1 / deviation vs tie deviation) + the sign banked.
- The rescue bundle (fork commit 2, flag-gated: admit `payable || enumeratorPlan`, the directed
  plan as the natural line at that window, the realizer's legality accepting it) → its own
  forkcheck (flag off) → the third arm.
- The horizon-2 calibration on a sample of the pool's payment windows (the certify path).

## The rescue bundle + the overnight chain (19:45–19:55)

- **Fork `c26e99824b8`**: `-payrescue` (AnvilOptions.PAYRESCUE + `payableOrRescue`, the realizer's
  two payability checks, `rescuePay` / the bridged auto pick / the copy's natural pick paying
  directed at forced windows, the header pin). Smoke (8 games, no search): 8/8 won; 457 admitted of
  6,326 rejected (22% of 28,878 scanned); 3 rescue windows: 1 paid, 2 `directed_fail`. Snapshot
  `data/runs/build3-e4/forge-b3e4r.jar` (sha `c92ccc08…`); forkcheck `run-20260908-build3-e4r`
  QUEUED behind `run-20260908-build3-e4` (the waiter polls its pid), watchd `forkcheck-b3e4r`.
- **`PaymentWiringTest` 2/3 failures were test-order dependent** (the game-RNG reservation roll):
  3/3 alone on this tree, 3/3 on a worktree of `41ac60d6b21`. Memory written. The first rescue
  smoke drew 8/8 at the turn cap because I omitted `-obs` (no observations → every ask a silent
  decline): the smoke script's mistake, fixed.
- **`scripts/build3_pay_chain.sh` launched 19:51** (watchd `build3-paychain`, notifies): pool DONE
  → pool read → 5-fold `pay_fit` → read → build `m12-build3-e4` → smoke (`--pay-bar 0.2`) → the
  paired read b3e4 (off / on / rescue, 2 × 300, the rescue snapshot jar). `final_read.py
  --server-args` + the read script's `ARMS` / `FORGE_ARGS` / `SERVER_ARGS` knobs landed for it.
  **Hazard: the chain's fit / smoke / read run from the MAIN tree** (the label worktree is pinned
  before `pay_fit.py`) — no tree edits while `build3-paychain` is registered; edit on a worktree.
- Timeline: pool 18/1,000 at 77 g/h and rising at 19:48 (ETA ~08:00–10:00 09-09); forkcheck e4
  240/500 at 19:48 (ETA ~20:10); e4r follows (~20:50).

## The forkcheck (20:15) — PASS, fork pin `e44d83a8327`; the served-tag confound (user review)

499/500 identical, the one miss 20260969 the standing crash (turns 27 vs 29), fidelity 450/50 both →
PASS; pin moved (CLAUDE.md row, map, ADR-0105 addendum). e4r started behind it (38/500 at 20:15).

User asked whether tonight's findings touch earlier judgements: **yes, one number** — every M12 network
arm since Build 1 served the untrained pay head (≈ 0.25 random goal payments / game, copies too) while
`iter-019` never carried it, so dz − ref (−1.63 ± 1.15) carries an unmeasured term with a sign (the
gate dzla − dz and the surface reads shared it across arms). Decision: no read redone; **one ablation
arm** (the e3 ckpt with the pay tag withheld, paired against the evening-3 on arm on the same seeds,
2 × 300) queued behind the pay chain → `data/runs/build3-e3-nopay/`; ADR-0104 addendum; standing rule
(gating/reads): served-tag parity or a declared arm. Also on the record: the evening-3 "tests 16"
line was unverified (the damage class had not run), the resolution-effect slice looks decision-free
(170/170 zero-cost so far), the ADR-0102 rescue count is an upper bound (2/3 directed_fail).

## The rescue forkcheck (20:55) — PASS, fork pin `c26e99824b8`

`run-20260908-build3-e4r`: 499/500 identical, 20260969 the standing crash (turns 27 vs 29), fidelity
450/50 both → PASS; pin moved (CLAUDE.md row + Now, map, ADR-0105 addendum, plan record). Pool
105/1,000 at 80 g/h (20:53); the chain waits on the pool, the ablation arm waits on the chain.

## Babysit (22:15) — the pool's early read

183/1,000 at 69 g/h (ETA ~10:00 09-09): 175 won / 7 draws / **1 crash `ArrayIndexOutOfBoundsException`**
(game 372, `ComputerUtilCost.checkTapTypeCost` ← `canPayManaCost` ← `AnvilOptions.payable` in the
option scan; a pay-slot copy `g372.s95r0o6p0` under the eot leaf met it first, then the mainline) —
**a known Forge engine crash class**, in the July d6ext / run10 / drill logs and the 07-06 devlog's
crash census, not a landmine of tonight's code; the longer eot copies simply meet the engine's own
crash classes more often (caught per copy: `kind: crash`). **Resolution-effect census (8,811 windows,
183 games): 8,733 zero-cost (99.1%); 78 with mana, 41 of them consequential (≥ 2 goal options; all
auto-payable, 2 cost-modified) ≈ 0.22 per game** vs 9.4 mainline consequential windows — the queue
item is ~2% of the payment decision traffic; the 1,000-game figure closes it. The chain and the
ablation arm still waiting.

## Babysit (04:55) — the pool at 697/1,000

75 g/h; 658 won / 36 draws / 3 crash rows: the known AIOOBE, and a pair from one worker — game 723
hit the per-game wall clock at 12 turns / 239 windows / 1,260 s (a slow-board game under the eot
copies), its dying priority ask tripped the bridge deadline → the worker poisoned (game 724 discarded,
the standing `BridgePoisoned` class) and drained; the harness recycled the chunk. Two games lost, no
new class. ETA ~09:00.
