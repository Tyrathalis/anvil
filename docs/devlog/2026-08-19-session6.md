# 2026-08-19 — session 6 (M9 pre-D4 revisit: tail probe → the goal surface → final baseline, ALL GATES PASS)

## Done

- **Tail probe built and read (`run-20260819-paytail`, 60 games, same
  seeds as the census's first 12 pairs; fork `c4ddbc0ff4`,
  `-Danvil.pay.tailK=64`; reader `scripts/payment_tail_read.py`):**
  the question was "is K the fix?" and the answer was NO, decisively —
  consequential class-count p50 5 / p75 16 / p90 55 / p95 censored at
  64; coverage K=8→62%, K=16→75%, K=32→85%, no plausible K closes it.
  **The cause finding that decided the design: distinct source classes
  p50 6 / p90 8 / max 11 on the over-8 set while compositions run
  26–64+** — assignment combinatorics over few residual types, and DFS
  compute is free (nodes p50 105 / p90 1,625).
- **Revisit session RESOLVED (user-pinned via three decisions, spec
  §12):** the decision object is a preservation GOAL —
  `spare(k)` per source class (min-taps soft semantics, pinned over
  hard zero-taps) + `min_life` on phyrexian costs; no explicit `chain`
  goal (test-verify-first pinned); costmod two-pronged detector +
  out-of-scope v1; `forced := ¬auto-payable ∧ ≥1 plan ∧ ¬costmod`.
  Build-time refinement recorded in §12a: per-goal argmax ties break
  by SPREAD (max-entropy residual) then lex — pure lex could represent
  "spare the dork" as a degenerate double-tap of one land class.
- **Fork build (`bf6d700ef2` + `531dafdff4`, branch
  `m9-payment-surface`, new worktree `../forge-m9pay`):** enumerator
  per-goal argmax + outcome dedupe (K_MAX deleted; GOAL_MAX=16
  defensive, node budget the only real bound); `costModified()` static
  prong (reduction keywords + ReduceCost-static presence — over-flag =
  conservative, documented); controller/telemetry goal-era kvs
  (`goals`/`plans`/`costmod`/`costmod_late`/`nodecap`). **Suite 14/14
  incl. the new `testChainReachableViaSpareGoal` — the §12a pinned
  reachability check: on I×3+Signet vs {1}{U}, `spare(Island)`'s
  argmax routes through the Signet chain. Evidence, not assumption.**
- **Reader dual-era (`c26b710`, Python suite 209):** goal-era gates
  (goal-trunc 0.5%, nodecap 1%), costmod rate + leak backstop, plans
  histogram (true composition counts, uncensored).
- **Census re-run ×2 → FINAL PRE-D4 BASELINE
  (`run-20260819-paygoals2`, 500 games, ALL GATES PASS):**
  consequential **15.99/g** (was 20.79 K_MAX-era; bridge tax ~1.6%);
  **goal truncation 0.0000** (options p50 2, max 9); **nodecap 0.0073**
  — the first run (`run-20260819-paygoals`, 200k budget) fired the 1%
  gate at 1.25%, profile showed 141 late-game monster boards (atoms
  p50 16 / p90 36) still surfacing 6–7 options each ⇒ NODE_BUDGET
  re-pinned 200k→2M (`531dafdff4`), re-run cleared it 141→82;
  **forced 6 CLEAN** (was 32 confounded); **costmod 25.48%,
  `costmod_late` leak = 0** — presence-scoping inflation, refinement
  routed by name as **payment-completion queue item 4** (m9-plan).
- Docs: spec §12 written + baseline read block; m9-plan D3 section +
  queue item 4; CLAUDE.md status tail; this devlog.

## Broke / surprised me

- The census-vs-probe nodecap divergence (0.2% → 1.25%) was
  self-inflicted and instructive: the probe's K=64 cap pruned DFS
  early on monster boards; removing the class cap exposed the true
  node cost. The gate did its job twice in one day.
- My own stall detector false-alarmed at 88/100 pairs — I used
  relative `-newermt`, the EXACT hazard in my memory notes
  ("find is bfs — no relative -newermt; use -mmin"). Re-armed with
  `-mmin`. The memory was right; the application failed.
- costmod at 25.48% of in-scope traffic is much larger than the
  zero-class family suggested (284 windows) — presence-scoping over
  ReduceCost statics (medallion-class permanents flag everything while
  on board). Leak = 0 proves over-coverage; queue item 4 is the
  refinement.

## Next session picks up

- **Rung 3 (drill evalset + payment sub-head design).** Overnight
  groundwork: candidate miner over paygoals2 + design draft
  (`docs/design/m9-rung3-draft.md`) for morning review — sub-head
  pins stay PIN-AT-DESIGN, the draft proposes, the session decides.
- Boundary-bundle rider check: the §12 goal surface changes the obs
  payment-window labels (goal names now in `opts`) — confirm the obs
  schema-version bump note in the bundle covers it.
- CLAUDE.md M9 bullet is past the ~2–3 KB snapshot threshold —
  archive-snapshot candidate at the next natural checkpoint.
