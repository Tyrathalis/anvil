# 2026-08-28 (M10 build R6 — the cousins touch: the payment family completes before the probe)

## Done

- **The COUSINS TOUCH landed in one fork touch
  ([ADR-0083](../decisions/ADR-0083-cousins-touch.md), fork commit
  `5bbc397412` = the new build-era jar).** Four sub-pins
  user-adjudicated at session open, all on the recorded leans: delve IN
  with type-grouped classes; `payCombatCost` bridged now; costmod
  refinement applicability-only; GOAL_MAX 16→24 pre-data.
- **Engine archaeology first** (Explore fan-out over the payment
  adjustment path): cousins apply inside
  `CostAdjustment.adjust(ManaCostBeingPaid,…)` one frame BELOW our
  `payManaCost` override; the ONLY directed seams are the two
  controller callbacks (they return the shard assignment itself,
  applied unvalidated); convoke pays own-colors-or-generic (never
  snow/{C}), improvise generic/2-hybrid, delve strictly GENERIC;
  `payCombatCost` reaches `PlayerController.payManaCost` with
  effect=true + a degenerate EmptySa and CostAdjustment no-ops there
  (the CLEAN case); `checkRequirement` is the engine's own per-spell
  applicability predicate, deterministic. Bonus finding: the heuristic
  AI returns an EMPTY convoke map pre-attacks on its own turn (engine
  TODO) — directed convoke is a capability unlock, the ADR-0065
  finding-6 genre.
- **Build**: cousin atoms as restricted pseudo-atoms in the same DFS
  (main-cost-only via the initial-shard-index test; excluded from
  `allAtoms` so the sched scorer's residual-mana math stays honest);
  `spare(k)`/spread/lex/dedupe/`chainOrderFeasible` all extend;
  `spare_pool` goal fixes the pool-tie residual; `CousinDirective` +
  generated FORCE_OVERRIDES hooks for directed execution (armed on
  EVERY directed payment — empty maps suppress heuristic convoke
  deliberately); combat marker on the nested window; detector rewrite
  with `CostAdjustment.staticAppliesTo` (additive fork-local accessor).
  Python twin: `PAY_KINDS` += spare_graveyard=6/spare_pool=7.
- **Proofs banked**: PaymentEnumeratorTest 16/16 (6 new), Wiring 3/3,
  Certify 4/4, Audit 4/4; Anvil suite 253 green; **ADR-0025 jar gate
  ZERO diff classes** (sched smoke 9,969 census + 72/72 ×2 labels +
  schedfile byte-identical; choice smoke 17,409 census + 84/84 ×2 +
  choicefile byte-identical; internal determinism both). Graft
  regenerated (`pay_kind_emb` (8,512) all-zero); **day-zero reliance
  re-banked IDENTICAL**: argmax_flip 0.012513, content_flip 0.0 exact,
  reliance_l1 0.786172, sched_rms 0 (aux head losses moved within
  fresh-init RNG noise, the R5 precedent).

## Broke / surprised me

- The fork test suite wouldn't boot: a `CHRONICLE_BINDER` pref from the
  playable branch leaked through the SHARED user profile into
  `FModel.initialize` (the known shared-user-store hazard, now biting
  tests). Isolated `-Duser.home` via surefire argLine fixes it —
  worth folding into a test runner script if it bites again.
- Stale surefire per-class reports (08-19, mid-M9-build failures)
  masqueraded as fresh failures — multi-class `-Dtest` runs aggregate
  as "TestSuite" and don't rewrite per-class txt files. Check
  timestamps before believing a report.
- The certify path has no post-super hook for disarming the cousin
  directive — solved by sweeping stale arms at every `payManaCost`
  entry (both controllers), with the leak bounded to test-probe
  callbacks of the same payer.

## Next session picks up

- **The training-probe launch session.** Pre-flight owes on the
  cousins jar (`5bbc397412`): observe-frame remint (evalset
  revalidation + holdout lanes) + label rejoin with `option_mismatch`
  counted — spare_pool/costmod scope changes CAN drift banked phy
  windows; census read (post-refinement costmod rate vs 25.48%,
  cousin window/option rates, nodecap + goal-trunc gates under
  gy-widened boards, combat window rate). Then kill/FUND/unmask +
  read-protocol numerics pinned pre-data — FLOOR-RELATIVE flip
  thresholds against the banked 0.0125 presence floor.
- Named read riding the probe: directed convoke as capability unlock
  (the empty-map engine TODO means the heuristic literally cannot).
