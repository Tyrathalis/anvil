# 2026-08-04: M5 D1 — the migration read, recorded before any cycle-2 training: one-shot-consistent

## Done

- **Cycle-2 curation online (method verbatim per ADR-0034 decision 4):**
  `early_doom` trace+analyze on `run11-i019-finalarm-s{0,1}` with the
  iter-019 critic → `data/runs/early-doom-run11-i019` (938 losses,
  substrate winrate 0.5303, 576 addressable / 61.4%), plus the standing
  d4-critic-fullvis cross-check → `early-doom-run11-d4crit` (580 / 61.8%
  — the two instruments agree). Cycle-1 comparison numbers re-derived
  first from `early-doom-run9-i009` and reproduced exactly (584 / 61.2%,
  208 ≥30pp collapses across 82 model-decks).
- **`scripts/migration_read.py`** — the cycle-over-cycle comparison as a
  reproducible script (profiles, deck overlap, per-(seat,seed) migration
  — the substrates share standing seeds, so games match per seed —
  and the drilled-vs-undrilled conversion split given a selection file).
  Run on both critic pairings: the on-policy pair (i009 vs i019,
  method-verbatim) and the d4crit pair (fixed instrument). Every
  headline number replicates across both.
- **THE MIGRATION READ (m5-plan D1 section, recorded before `d6-run12`
  exists): verdict = one-shot-consistent.** Against the pre-registered
  signatures:
  - Addressable stock STABLE: 576 (61.4%) vs 584 (61.2%) — the ratchet
    signature's stock half holds.
  - But the migration half fails cleanly: luck-locked sweep 18.9–44.9%
    vs 19.4–46.0%; ≥30pp collapses 215/80 decks vs 208/82 (overlap 68);
    crash-turn quartiles 9/13/19 vs 10/13/20; peak_v mean 0.726 vs
    0.723. Nothing moved later or higher-value.
  - Seed-level: 28.9% of cycle-1's addressable losses converted to
    wins; **63.5% are still addressable losses with the crash turn
    unmoved (median Δ 0.0)**; 35.6% of the cycle-2 stock is new seeds.
    Same-instrument: 29.3% / 66.6% / +0.04.
  - **Memorization check (supplementary): drilled games converted at
    28.9% vs undrilled 29.1% (z = −0.05)** — the +1.98pp was fully
    general; drilling a window did not preferentially fix that game.
  - Reading: cycle 1 was a diffuse general improvement that drained
    ~29% of the stock uniformly while it replenished at the same level
    — drainage, not up-leveling. Prediction on file: Δ2 < Δ1 with the
    binding constraint at transfer/conversion, NOT stock. The run still
    decides.
- Two plan risks resolved by the read: no profile ambiguity, and no
  selection-v3 sizing shortfall (576 points ≥ the ~300 floor; f≈20%
  rotation sizing carries over).
- Docs swept: m5-plan D1 migration-read section + risk strikethroughs,
  CLAUDE.md M5 bullet, map (Now card, M5 details block, footer stamp).

## Broke / surprised me

- Nothing broke. The surprise is how clean the one-shot profile is —
  the pre-registered ambiguity risk was priced and didn't materialize:
  distributions are pinned to two decimal places across cycles, on both
  critics.
- The drilled = undrilled conversion split cuts both ways and is worth
  keeping in mind for the D2 decomposition: it acquits the drill loop of
  memorization, but it also means the drills' effect shows up everywhere
  EXCEPT preferentially at the drilled windows — consistent with the
  a2 mechanism story (curriculum composition, not window-specific
  correction).

- **K=8 map DONE same session (`drill-map-r11i19-k8`):** 565/576
  labeled, replay-miss 11 (1.9%, same drift class as cycle-1's 1.0%),
  all-completions-crashed 6. **Rollout winrate 22.9% vs mean v_before
  0.585 — cycle-1's calibration-gap number was 23.7% vs 0.584.** Bins:
  lost 331 / long_shot 114 / coin 65 / winnable 49 (cycle 1: 330 /
  ~115 / ~65 / ~50 shape). The migration read's "distributions
  unmoved" verdict echoes in the ground truth: the map profile is a
  near-clone of cycle 1's.
- **`drill_sweep.py` latent break found by pre-launch inspection and
  fixed (`82fca66`):** it builds `generate()`'s Namespace by hand and
  predates the D3 flags — `generate()` reads
  `sample_forks`/`fork_obs`/`drill_ckpt` unconditionally since
  `7823144`, so the sweep would have AttributeError'd at arm o0. Three
  defaults added.

## Next session picks up

- **Anchor sweep RUNNING (`drill-sweep-lost-20260804`, launched
  detached ~4–5 h):** cycle-1 arms verbatim (o0 baseline / o2 / o4 /
  peak) on the 331-game lost bin. When it lands: read summary.json
  (recovery profile), then `grindstone select` v3 (in-band per-game,
  later-supersedes) + evalset v3 (⅓-per-bin holdout + pinned-policy
  baseline re-measure per D2.4) — holdout subtracted at the selection
  boundary. Then D2 `d6-run12`. No tree edits while the sweep runs
  (forkserver rule).
- Then D2 `d6-run12` launch (run11 recipe verbatim; iter-019 init/pin,
  selection v3, w=16, fresh seeds), gated vs 0.5316.
- The migration read's prediction (Δ2 < Δ1) is on file — the compounding
  run is now the test of the recorded prediction, exactly as designed.
