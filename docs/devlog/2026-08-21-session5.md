# 2026-08-21 (session 5) — the M9 boundary event: bundle landed, certified, gate re-pinned

The ONE boundary event ([ADR-0068](../decisions/ADR-0068-m9-boundary-bundle.md),
Reads complete). Every queued item landed or got an explicit verdict; the
gate re-pinned same evening.

## Done

- **Pre-flight:** Python suite 230 green pre-boundary; fork clean; upstream
  fetched (108 commits ahead, we carry 50+14); conflict scout found ONE
  overlapping file (Player.java, unrelated hunks); `pre-rebase-20260821`
  pinned.
- **§3c payment surface merged:** `m9-payment-surface` tip `116476eebb` was
  a clean fast-forward (14 commits, branched at master's exact tip).
- **Upstream rebase:** onto tip `23c3d2a85d` (no fresh AI churn near tip —
  ADR-0025 pin discipline); 64/64 commits replayed, zero conflict stops;
  both Player.java fork fixes verified present; full desktop suite green on
  the rebased base.
- **Store-namespace fix (run17 iter-2):** fork `-forkns` (`FORK_G_BASE +
  ns*1e9 + offset`, overflow loud, ns=0 = old encoding) + grindstone plan
  assigns per-source-store ns (sorted ordinal, manifest-recorded) +
  harness `--fork-ns` threading. Plan namespace test added.
- **Obs schema v2:** entity choice-state kv (`cho`) — VIEW-sourced so
  secret choices stay hidden until revealed (`ObsChoiceStateTest` 4/4 incl.
  the no-leak pin); sv bump blesses the payment-window kv; Python reader
  strict check = the era gate; ENTITY_FEATURES +7 (cho color bits/num/has)
  appended so `load_compat`'s ent_proj zero-pad keeps pre-boundary ckpts
  byte-identical. Residuals recorded in the ADR (dir/eo/mode not emitted;
  typ/nam text-only).
- **Multi-format enablement (model side):** fmt one-hot in GLOBAL_FEATURES
  via the new vocab `formats` registry (loud on unknown; lockstep check);
  `load_compat` state_proj INSERT zero-pad (fmt sits mid-input) asserted
  against the real d5 ckpt. Content growth explicitly did NOT ride.
- **2-arm trim RETIRED** (explicit verdict): campaign is opt-in `--seq-n`
  (absent from the standing recipe), act−hold retired at ADR-0058/0062,
  `-seqarms nat` covers the chartered natural-timing future. No code.
- **Instrument fixes (ADR-0059 genre):** harness `_find_jar` newest-mtime +
  warning (was alphabetical); run.json `obs_schema` pins the constant (was
  hardcoded 1 — caught live by the first pilot smoke).
- **Bundle jar built + end-to-end smoke:** 60-game obs pilot on the bundle
  jar — 60/60 frames sv=2, choice-state live on 3,422 windows (common, not
  exotic), payment decs present, assemble+fmt green on every frame.
- **Era-scoped test fixtures:** `tests/conftest.py era_store` skips loudly
  on pre-boundary stores (17 tests skip pending fixture regeneration);
  `pilotv2` (100 games, bundle jar) regenerating as the parity fixture
  store; the mu-covered run6 fixture (test_critic_loop) repoints at the
  first D4-era sampled store — recorded gap until then.
- **Collaborator check:** no open PRs, no active forks of Tyrathalis/forge
  — master force-push is safe.

## Evalset revalidation pass DONE (same session)

`run-20260821-revalidation` (4 lanes, bundle jar, 293 observe jobs):
- **option_mismatch = the SAME 3 jobs as day-zero (9/119/138)** — zero new
  enumeration drift from the rebase.
- **NEW: 12 window misses (4.1%; 4 positive / 8 auto-correct)** — replay
  drift on the rebased engine; excluded, recorded, floors survive
  (bp 12 / ch 25 / phy 13 / wc 14, all ≥ 10 ✓).
- **Compat CONFIRMED EXACTLY: 278/278 windows scored in both eras give
  IDENTICAL argmax picks** — the zero-pads are behavior-clean in
  production.
- **Re-banked post-boundary baselines: positive 2/64 (3.1%), auto-correct
  196/214 (91.6%), deviation 24/278 (8.6%)** (same decisions; % shifts
  purely denominator). Gate arithmetic carries: FUND ≥7/64 + auto-correct
  ≥182/214.
- Lane-log noise (CHRONICLE_BINDER prefs enum, replay TimeoutException)
  verified pre-existing in the day-zero lanes — not bundle regressions.

## Fixture store + fork push (same session)

- pilotv2 (100 games, bundle jar, obs sv=2, pool provenance) ingested;
  sampling/serve-parity/pay_class fixtures repointed (block windows ~9:1
  outnumbered → block-scan widened, thresholds unchanged). Suite **227
  passed / 6 skipped** (the 6 = test_critic_loop's mu-covered store,
  repoints at the first D4-era sampled store).
- Fork pushed: `pre-rebase-20260821` (new branch) + master force-updated
  `008ea151c4` → `2f87180cdf` (collaborator check clean: no open PRs, no
  active forks).

## The chain completed same evening — ADR-0068 Reads COMPLETE

- **Forkcheck (`run-20260821-m9boundary`, 500g): 450 clean / 50
  divergence = 10.0%, statics 0, crashes 0** — at/below the pre-boundary
  11.2–11.4%; fidelity not degraded. The 50 rows = the forensics
  substrate.
- **`m9-rebaseline` final_read (1,999 games, 0 crashes, all decisive,
  turns median 21): raw 0.5283 ± 0.0112, corrected 0.5279 ± 0.0110 —
  THE M9 GATE NUMBER.** Pre-boundary 0.5373 is reference-only (−0.94pp =
  re-scale, not a verdict). Veto 0.1591 (first-attempt 0.1151).
  **Battery anomaly: seed-half gap 7.15pp (0.4925 vs 0.5640) > 2×SE —
  the run12 class.** Recorded exploratory per protocol; binds forward:
  D4/D5 reads within ~1pp of gate get fresh-seed paired confirmation as
  a matter of course. The never-cast/never-seen pool-census rider did
  not run (still unbuilt — noted loud in the ADR).

## Next session picks up

1. **D4 recipe session → launch** (~5 iterations, standing loop,
   iter-019 init + payment sub-head; live `pay_deviation` baselines at
   the run's own iter-0; drill scoring per accepted iteration against
   the re-banked 2/64 / 196/214 baselines; gate per the pinned
   movement-from-baseline thresholds, denominators 64/214).
2. Copy-state forensics substrate: FIXED_HASH same-sequence triplet on
   the bundle jar (daytime); then the trace-diff work.
3. `m8d1-proof` jar prune (183M → results+meta) — the m9-boundary
   forkcheck now supersedes it.
4. critic_loop fixture repoint at the first D4-era sampled store.

## Broke / surprised me

- The ADR draft briefly carried invented read numbers (caught and replaced
  with PENDING placeholders before commit — reads fill in only from real
  run output).
- run.json `obs_schema` was hardcoded `1` in the orchestrator — the smoke's
  provenance pin caught it; same hazard genre as ADR-0059's stale-jar glob,
  fixed alongside `_find_jar`.
