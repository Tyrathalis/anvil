# 2026-07-12

# Session 1 (morning): Ante mirror certification PASSES ([ADR-0014](../decisions/ADR-0014-ante-certification.md)) — the ledger's own zero-mean test caught a modeling error and the fitted-β estimator delivers convergence; PR #11203 MERGED upstream

## Done

- **PR #11203 MERGED (2026-07-12 10:35 UTC, tool4ever, merge `1922ce411a`).**
  Approval note: "acceptable — the Copier/Snapshot duplication is a longer
  technical debt (and getting rid of that should also help with your
  project)." The scoped-PR-plus-follow-up proposal was accepted verbatim;
  the consolidation follow-up is now maintainer-blessed (worklist updated;
  off the M2 critical path). The fixes return via the next fork rebase
  (anchor #11161); `../forge-pr1` worktree is removable. Submitted 07-10 →
  reviewed 07-11 → merged 07-12.
- **Mirror batch landed clean:** 12,800/12,800 games (12,793 decisive,
  7 crashes, 162 draw-clock hits, ~21.9 s/game median), ingest verified
  (16.8M decisions decode, 616 MB at 82.9×, 48 KB/game), validator OK with
  **zero winner mismatches on the new jar** (the ADR-0013 cross-check's
  first live pass).
- **Certification run (96 min GPU, 156K corrected nodes, `d4-valuefix`
  critic):** raw seat-0 winrate **0.4953 ± 0.0044** (mirror sanity ✓);
  draws zero-mean at n=124K (t=−0.33); **openers read t=+2.72 — and the
  deal-index split localized it entirely to mulligan re-deals** (deal #0:
  t=+0.52 clean; deal #1: +0.0076/node, t=+5.46).
- **Root cause (engine archaeology):** Forge's London mulligan **tucks
  before the keep decision** (`mulliganDraw()` draws 7 → immediately asks
  `tuckCardsViaMulligan` → keep dec fires) — so a re-deal keep window shows
  a **choice-filtered best-k-of-7, not a chance outcome**. v0 treated it as
  a uniform k-subset; the ledger's own unbiasedness test caught the error
  at 12.8K-game resolution. This also explains the corpus's hand=6 mull-#1
  keeps (post-tuck), which v0 had mis-read as "Forge deals 6 directly."
- **Ante v1.1 (Anvil `9692bfe`):** re-deal opener keeps excluded (6,572 of
  31,840 nodes; correct anchor = the tuck dec's pre-choice 7, queued with
  the critic upgrade); **fitted control-variate coefficient** added
  (split-half β̂, out-of-sample per game; zero-mean preserved for any β);
  `certify --from-ledger` re-aggregates a prior run's per-game ledger under
  current semantics without GPU.
- **The certificate (v1.1 aggregation, `data/runs/ante-cert-20260712-v11.json`):**
  every class zero-mean (opener t=+0.52 / draw t=−0.33); **β=1 var ratio
  1.074 (noisy-critic AIVAT adds variance) but fitted-β̂≈0.21 gives var
  ratio 0.9936, bootstrap CI90 [0.991, 0.996] — strictly faster than raw.**
  corr(raw, ledger) = 0.08 caps today's reduction at ~0.65% — the binding
  constraint is critic quality, exactly §7's split ("unbiased regardless of
  critic quality; critic quality sets variance removed"). §7's 3–4×
  multiplier awaits the D4 critic + fuller chance-node coverage (poison
  rule skips 69% of draws). Re-certification after any critic upgrade:
  `--from-ledger` (free) or re-eval (96 min).
- **Model mirror launched** (`ante-model-mirror-20260712-090518`, 512 games
  8 decks × 64, all seats bridged to d2-sa δ=0 via server @50061,
  seed-base 20260713) — the same-ledger distribution check.

## Broke / surprised me

- **AIVAT with an honest-but-weak critic makes eval WORSE at β=1** (var
  ratio 1.074): each correction is unbiased but its noise exceeds its
  signal at ρ=0.08. The classic control-variate shrinkage (β̂ = cov/var)
  is not an optional nicety — it's the deployment form; it subsumes pure
  AIVAT (β̂→1 as the critic sharpens).
- The engine's mulligan flow hides a decision inside what the callback
  stream renders as "the dealt hand" — chance-node extraction has to model
  engine mechanics, not rules-as-written. The certification apparatus
  itself is what caught it, which is §7 working as designed.
- Draw-class game-sum t=1.95 (node-level clean; ~7 t-stats computed →
  look-elsewhere-unremarkable). Watch item for the next pass.

## Next session picks up

- **Model-mirror certify** when generation lands (~1h): ingest →
  `certify --ckpt d4-valuefix` → compare class stats vs the heuristic
  mirror (value head slightly off-distribution on model games — expect
  weaker ρ, zero-mean must still hold).
- **D4 second half: the critic pilot** — position sampler over the store
  (stratified turn/phase), fork-at-decision, K rollouts under d2-sa δ=0
  both seats, label = outcome mean; server micro-batching = throughput
  lever #1 (~70 positions/h/worker naive). Gate (ADR-0013 re-base): beat
  `d4-valuefix` in the turns-from-end 5–16 band (AUC 0.70–0.86). Candidate
  cheap rung first: full-visibility critic tower on corrected OUTCOME
  labels (the existing corpus, free) before pricing rollout labels.
- Re-deal opener nodes anchored at the tuck dec's pre-choice obs (with the
  critic upgrade); revisit the draw poison rule (69% skip) — shuffle-aware
  seen-set clearing recovers coverage.
- Consolidation follow-up PR (GameCopier→GameSnapshot) — maintainer-blessed,
  off critical path; coordinate timing on the merged PR thread.

## Session 1 addendum: model-mirror v1 wedged — OOM → GUI bug-report dialog in headless workers (fixed, relaunched)

- **v1 (`ante-model-mirror-20260712-090518`) stalled at 278/512:** two of
  three workers each stuck ~40+ min inside one game, GPU idle. jstack:
  **main parked in `Dialog.show` under `dispatchUncaughtException`** — an
  `OutOfMemoryError: Java heap space` escaped the per-game
  `catch (Exception | StackOverflowError)`, reached Main's registered
  `ExceptionHandler`, and opened a **modal Swing bug-report dialog in a
  headless worker**. (Third worker finished its 112-game range clean.)
- **Fixes:** fork `67e55ba1c1` — headless default uncaught-exception
  handler installed before the game loop (log, never dialog) + per-game and
  outer catches widened to `Throwable`; Anvil `0443a18` — workers now run
  with `-XX:+ExitOnOutOfMemoryError` (an OOM'd worker dies cleanly, the
  chunk mechanism re-issues its games; zero-progress guard catches a
  deterministic OOM seed).
- **Relaunched as `ante-model-mirror-20260712-101309`** (same seed-base
  20260713, chunk halved to 100 so workers recycle sooner); stall-aware
  monitor armed.
- **D6 flag (memory):** both wedged workers OOM'd at similar uptimes
  (~70–90 games) under both-seats-bridged mode — smells like per-game
  accumulation specific to model-vs-model (candidate: D1's per-game wire
  sessions with two bridged seats). Self-play IS model-mirror at scale;
  profile worker RSS/heap across a bridged run before D6. Heuristic-mirror
  workers lived 200-game chunks at 2g without issue.
- The wedge class was invisible to every prior run shape: heuristic runs
  (99.95%+ decisive, caught-crash paths only) and single-seat-bridged arms
  never hit an uncatchable Error mid-game. First all-seats-bridged run at
  scale found it in an hour — cheap tuition for D6.

## Session 1 addendum 2: model-mirror v2 clean — ledger zero-mean holds on the model distribution

- **v2 (`ante-model-mirror-20260712-101309`): 512/512 games, 510 decisive,
  2 crashes (caught NPEs — the wedge fix held: no dialogs, no stalls, no
  OOM deaths), 5 draw-clock hits.** Ingest verified (610K decisions, 45
  KB/game); validator OK, zero winner mismatches.
- **Ledger over the model distribution (records
  `data/runs/ante-model-cert-20260712.json{,.ledger.jsonl}`):** raw seat-0
  0.4765 ± 0.0221 (1.1σ from 50% ✓); **all classes zero-mean** (opener
  t=+0.23 with the v1.1 re-deal exclusion live in extraction — 250 skips;
  draw t=−0.57; overall t=−0.66). corr(raw, ledger) = 0.05 (vs 0.08 on the
  heuristic mirror — the value head is mildly off-distribution on model
  games, as predicted); at n=510 the fitted-β variance effect is noise
  (ratio 1.0008). The distribution check's job was unbiasedness on model
  games, and it passes — variance reduction on model games re-measures
  with the D4 critic like everything else.
- **Ante half of D4 is DONE:** apparatus certified on both distributions
  (ADR-0014); remaining D4 = the critic pilot (gate per ADR-0013: beat
  d4-valuefix in the turns-from-end 5–16 band).

# Session 2 (midday): critic pilot opened — full-vis critic training, rollout labeler built and certified, overnight labeling pilot chained

## Done

- **Critic rung 1 LIVE: full-visibility critic tower** (design §4 asymmetric
  input). `transform.assemble(full_vis=)` bypasses the info-set gate —
  critic instrument only, never a policy input; leak test untouched;
  `finetune_value --full-vis --trainable all` trains the whole net as a
  critic (policy heads in that ckpt = garbage by construction, never
  serve). Init d4-valuefix (warm trunk+head), lr 1e-4, 100K steps, worktree
  `../anvil-d4critic` → `data/training/d4-critic-fullvis`. Health: evals
  oscillating through mid-run (full-trunk at 1e-4 on a shifted input
  distribution runs hot — bce 0.60–0.79, auc 0.71–0.77); letting the cosine
  decay decide; if the 100K final doesn't clearly beat d4-valuefix, relaunch
  at 3e-5.
- **Rollout labeler BUILT and certified (fork `80bd8ded76`, Anvil
  `818e666`/`c3674e6`):** `forge anvil -rollout K -points M -labels out`
  — at M meta-RNG-sampled turns in [2,16], fork the live game
  (ForkFidelityCheck discipline: quiescence drain, active-player priority,
  RNG snapshot/restore), complete K copies under the bridge; forks =
  wire-only obs sessions; **each rollout re-randomizes both libraries
  (Zone.setCards, trigger-silent) + its own RNG stream** — determinization,
  so labels approximate E over unseen order instead of replaying the one
  concrete order nobody observed. Obs gains `mark` records (shared dec seq
  counter) keying each fork point to its training window; harness passes
  `--rollout-k/--rollout-points`; ingest merges labels.jsonl. **Design
  choice recorded: labeler mainlines are MODEL games (all seats bridged),
  not corpus replays** — forks inherit Anvil controllers for free
  (D1-certified), positions are on-policy (what the RL-era critic must
  track), and the 512-game model mirror is the held-out outcome-labeled
  eval basis for the gate comparison.
- **Smoke (2×, same seeds): 8/8 points labeled, 8/8 mark→window joins valid
  (same-turn, active-player), 0 crashes, outcome vectors IDENTICAL across
  runs** (seed-determinism holds through fork+rollout). Real label spread:
  4-0 / 3-1 / 2-2 / 0-4. Found + fixed en route: mark kv key "k" collided
  with the record-kind key (duplicate JSON keys resolve last → every mark
  read as unknown kind); renamed + reserved-key guard in Obs.mark.
- **Pricing (smoke, K=4):** early-turn points ~60–90 s (rollout replays
  most of a model-vs-model game), late points ~5 s; copies ~15 ms. At K=8 /
  4 points: ~8–10 min/game of fork time on top of ~1.5 min of mainline.
- **Overnight pilot CHAINED behind critic completion** (GPU isolation:
  training + inference server would push bridge latency into
  deadline→fallback territory, and fallback rollouts are silently heuristic
  — the standing fallback rule applied to labels): 512 pool-scheduled games
  (varied matchups, not mirrors), K=8, 4 points/game → ~2,000 labeled
  positions, w=8 chunk=50, seed-base 20260715, `d4-rollout-pilot`.

## Next session picks up

- **Critic finals** (`d4-critic-fullvis`, ~100K): eval_value final +
  diagnose_value (full-vis auto-detected) vs d4-valuefix's
  0.5495/0.788/by-band table. Oscillation caveat above; relaunch at 3e-5 if
  the final disappoints.
- **Labeling pilot lands ~morning**: ingest (labels merge + marks), then
  build the label-join fine-tune (window = first priority dec after mark;
  target = w[p]/k soft label, BCE-with-soft-targets) and the three-way
  5–16-band comparison on model-mirror windows: d4-valuefix vs
  full-vis-outcome critic vs rollout-label critic. That comparison IS the
  D4 pilot verdict + the rollout-label economics table.
- Micro-batching (server-side) = first throughput lever if the verdict
  says scale labels.
