# 2026-09-04 — the adjudication resolved: route 1 gated, the hand-basis planner built and read

The overnight and day-long thread is logged as dated addenda in [2026-09-03.md](2026-09-03.md) (one continuous session across midnight); this file is the day's summary and the pickup.

## Done

- **Overnight (from 09-03):** two full-scale reads of the executor-strategy planner: release rule −1.1 ± 0.6pp (the day-zero number of record), the empty-emission variant identical (+0.1 ± 0.3 on shared windows). Certified-on-parity experiment: the 543 certified arms are not learnable at that size on any foundation (exact ≤5%).
- **User adjudications:** route 1 stands; the plan's basis must be the HAND (land timing is strategic; rocks/rituals change mana mid-plan); abilities in the plan (activations in, mana abilities deferred); §J decision-surface completeness candidates adopted with the information-set principle; the two WAIT refinements routed into session two.
- **Hand-basis planner built** (reset draft §I; `scripts/mine_ability_table.py`, featurizer `sched_cand_*` superset, `_sched_keys`, `sched_serve` basis="hand" with WAIT semantics, server/driver/distill flags): ability table 1,726 cards; corpus targets 1.85 slots at emission (unmatched −45%); distilled head first-cast agreement 92% (was 77%). First read stopped on three wait-rule defects (mana abilities as virtual candidates; used activations waited on; resolved hosts waited on), fixed; **the read: −1.8 ± 0.7pp, the same as the legal basis on shared windows (−0.7 ± 0.75); WAIT = 46% of bound windows.** Reading: the day-zero cost is commitment; the hand basis buys expressiveness (lands, post-land casts, activations, certified arms representable).
- Records: ADR-0095 addenda (route 1 gate, full-scale reads, certified-on-parity, hand-basis read), reset draft §I/§J, standing rule (day-zero-gated binding), CLAUDE.md, map.

## Broke / surprised me

- Argmax serving is not bit-deterministic across runs (micro-batch composition jitters near-tied logits): ~10% of completions differ between identical-config runs; within-run CRN pairs stay exact. The 56-window diagnostic subset reads ~2pp hot on every variant — subsets guide, full reads decide.
- The engine's ability labels are rules text (not constructible from card data) ⇒ the mined table; the mined "activate" bucket includes mana abilities, which the engine offers as options in some windows.
- Combo turns (one with 2,774 windows) dominate naive corpus statistics; filtered at fit.

## Next session picks up — SESSION TWO (the learner side), on the hand basis + release rule

1. WAIT refinements: sorcery-speed-only WAIT (speed from the card table); no virtual activations for tapped/sick/used board hosts.
2. Loader: the schedule as a scored action at its window on the superset basis (`sched_cand_*` in the RL loader; the mu row's `sched.new`/`lp`; forced rows out of the cast PG via `cand_allow`, already reconstructed).
3. Learner: planner PG term + planner KL guard (0.06 twin); follow term off; the mint term on search-labeled rows.
4. Inline certifier: the per-fork schedule directive over the wire (Java, the ChoiceDirective idiom), Python arm enumeration/scoring at the fork point on the hand basis, labels + arm spread on the row, workers 4g, rate knob 2% + off switch, era-zero anchor rule.
5. Driver: `--sched-binding all --sched-basis hand --sched-empty-rev release`, the paired read wired at day zero + terminal with the halt rule (K=8/N=600 stands).
6. Smoke, pre-flight pins (planner KL bar, forced-veto KILL bar, the stratified bar), launch probe7 from `m10-planner-distill-hand` (day-zero baseline −1.8 ± 0.7 vs advisory).

## Addendum — SESSION TWO, part 1 (afternoon): items 1 and 5 built; a serve-rule defect found en route

**Built (code + tests green: 276 passed / 6 skipped; new tests in
`tests/test_sched_serve.py`, `tests/test_selfplay_sched_flags.py`):**

- **Refinement (a), WAIT closes sorcery-speed spells only.** `SchedServe.bind`:
  under a WAIT the spell mask opens Instant-typed and Flash-keyworded spells
  (card knowledge via the veto_knowability card table; unknown names read as
  sorcery-speed = closed; flash-GRANTING effects are not card knowledge of the
  spell and stay closed); HOLD unchanged; telemetry `sched_bind_wait_open(_spells)`;
  `speed_of` injectable for tests.
- **Refinement (b), board activations of tapped / sick / spent hosts leave the
  virtual superset.** `featurize.board_activation_open`: the option scan does not
  filter by payability (AnvilOptions `PAYCHECK` off), so an absent board activation
  is absent for a non-mana reason; three such reasons never clear within the turn
  and are readable from visible state — tapped or summoning-sick host with a
  {T}/{Q} cost, and **spent**: a loyalty-cost or "activate only once each turn"
  ability absent at a QUIESCENT MAIN window (mid-stack the absence is timing and
  the candidate stays). "Used" is thus derived from visible state, not tracked
  per turn (the wire history carries no turn/sa; a Java obs flag would be a
  serializer change needing the ADR-0025 forkcheck proof). Isolation on the
  census store (500 games, build with vs without the filter): later-window
  matches −2.1%, emission −1.4% — the price is untap lines (Retrofitter Foundry's
  own untap, Tezzeret's 0) and no-target loyalty; accepted.
- **Driver (item 5).** `selfplay.py`: `--sched-binding/--sched-basis/--sched-empty-rev/
  --sched-empty-emit/--ability-table`; ONE derivation `sched_flags(args)` feeds every
  server the driver starts (generation and arms). Found: the pre-reset driver never
  passed binding to any server (the parameter existed, no call site used it).
  `--paired-read PLAN_DIR` runs the stratified paired read on the init ckpt at day
  zero (HALT ⇒ `PAIRED-HALT` + notify + exit 5, the mid-point rule) and on the final
  ckpt at the terminal (verdict + day-zero baseline ride the COMPLETE notify);
  `--paired-every N` for informational mid-run reads; records in `loop_state.json`
  (`paired_*`), idempotent on resume; `read.json` copied beside the loop.

**Found — `quiescent_main` read the stack from entities only (predates today).**
The obs carries the stack two ways: cards on the stack are entities (`z="stack"`),
but triggered/activated abilities never enter the zone and live only in the
separate `obs["stack"]` list (the Java option scan keys sorcery-speed legality on
MagicStack for the same reason). Census: 9,300 of ~79K priority windows had an
ability on the stack and no stack entity — all read as quiescent. So binding rule 3
(NEXT absent at a quiescent main window ⇒ failed slot) fired at trigger-on-stack
MAIN windows and failed sorcery-speed slots for TIMING in every day-zero read so
far (the hand-basis read's 13.4K "unactivatable" pile was partly this: Quintorius
+1 absent under its own token trigger, activated at MAIN2 as planned); land-first
misfired the same way on the legal basis; and the new spent-rule misread 117 of
1,010 realized activations in 150 census games (8 after the fix). Fixed in one
place (`featurize.quiescent_main`; `SchedServe.quiescent_main` delegates). Recorded
in the reset draft §I; the earlier day-zero numbers stand as read but carried the
misfire.

**Re-distilled:** `m10-planner-distill-hand2` (same 1,900 games, refined superset
+ fixed helper; emission targets mean len 1.88, unmatched 4,529 vs 5,304 — the freed
virtual cap admits real candidates). Holdout CE 0.6928 (was 0.7019), emission
first-cast agreement 91.5% (was 91.7%): fidelity unchanged. Note: later-window
unmatched 989K vs 809K is the 09:54 mana-ability filter on the m9 stores' combo
turns (thousands of rock taps), which the fit drops (>40 windows/turn) — not the
board filter (isolation above).

**Launched 16:25 — the day-zero re-read THROUGH the driver wiring** (`m10-dayzero-hand2`,
`--iterations 0 --sched-binding all --sched-basis hand --sched-empty-rev release
--paired-read data/runs/sched-paired-pop`, ckpt `m10-planner-distill-hand2`): the
same 800-window population, K=8, release rule; 6 lanes/side at 3g; ≈3.6 h. Armed:
watchd (driver self-registered, stall 75m), the driver's own HALT rule + notify, a
session monitor (progress every 30 min, halt/error/exit states). Result lands in
`data/training/m10-dayzero-hand2/paired-dayzero.json`; the run dir is
`data/runs/sched-paired-m10-dayzero-hand2-dayzero-20260904-162541`. **This number
= probe7's day-zero baseline** (replaces −1.8 ± 0.7, which carried the rule-3 misfire).

**Next session picks up:** read the re-read (expect ≥ −1.8: fewer false fails, fewer
holds on instants); then session two items 2–4 and 6 (loader schedule action on the
superset basis, planner PG + KL twin, inline certifier, smoke + pre-flight pins;
probe7 init = `m10-planner-distill-hand2`).

## Addendum — evening follow-ups (user request): classifier copy fixed, §J item 10, the WAIT guard audited

- `veto_knowability.classify_window`'s timing branch had the same entities-only
  stack test — fixed to both representations (it misrouted ~1 window per report
  into `timing_unexplained`; M9's knowability numbers untouched).
- §J item 10 recorded: abilities on the stack reach the policy only as a count
  (`stack_size`); a pending trigger's identity/targets never reach the model.
- **WAIT affordability guard audit** (details in the draft §I): at genuinely
  quiescent main windows the guard is exercised on 54 of 9,867 realized casts
  (300 census games) because payability is not filtered from the option scan —
  a held spell is almost always listed, so absence is legality/timing, never mana.
  Its 15 "unaffordable" verdicts were all alt-cost variants (printed-cost cast
  legal, the variant's condition unmet); 11 "land" verdicts were extra land drops.
  So post-fix the guard barely decides anything and is wrong when it does; the
  89.8K WAITs in the hand-basis read were overwhelmingly rule-3 firings at misread
  mid-stack windows. Rule change deferred to the re-read (no serve-rule change
  mid-read).
- Re-read progress at 18:00: ~30% of rolls, 34 crashes symmetric on two windows
  (the known copy+resume class), servers up.
- **False STALLED notice at 17:50 (watchd, 85 min):** the driver registers on its own
  loop dir, and during a paired read nothing is written there (the script narrates only
  at start/end; progress lives in `data/runs/<paired run>/lanes-*/`). The run was healthy
  (lanes writing, rows advancing). **Absorbed (user request):** `_paired_read` now Popens
  the script and appends a telemetry row every 5 min to `paired-<tag>.progress.jsonl`
  beside the loop (per-side rolls + crashes from the lane outputs) — the watcher's
  heartbeat and the read's monitor curve in one artifact; `paired_progress()` tested.
  Takes effect from the terminal read / probe7 onward (the live driver keeps its loaded
  code).
