# 2026-07-14

# D5 CLOSED ([ADR-0016](../decisions/ADR-0016-d5-closeout.md)): retrain finals, executor end-to-end, arms tie — D6 unblocked

## Done

- **Retrain finals (`d5-combat/last.pt` = checkpoint of record):** 795K
  steps / 16.23h, clean exit on the chain's gate (v2 corpus re-measure =
  0 violations over 1.41M attack + 382K block windows, all 113,591 games).
  600-batch finals: combat attack row 0.954 / window-exact 0.923 / target
  0.993 / count 0.94–0.98; block row 0.857–0.866; valpair ≥ val everywhere.
  **Honest 0.9671 vs d2-sa 0.9748 (−0.77pp, real)** — multi-task cost at
  combat_weight=1.0, accepted per the pre-RL scope rule; arms priced it at
  zero. **Value BCE 0.4531** — first policy ckpt trained end-to-end on
  ADR-0013-corrected labels; the old 0.57-floor era is formally dead.
- **Executor end-to-end in one arc** (proto → Java → Python → tests → smoke
  → arms), against yesterday's archaeology with zero design surprises:
  - Proto: entity-ref AttackMap/BlockMap (v0 index form never implemented;
    reshaped, fields reserved); fork synced; stubs regenerated.
  - Fork `380ec69d71`: `CombatMapAnswer` + `AnvilBridge` defaults +
    `GrpcBridge` CONSTRUCT round-trip (failure ⇒ empty map, counted, never
    heuristic substitution); `CombatRealizer` — attack per-assignment
    `canAttack` gate + `removeUnpayableAttackers` pre-drop (widened public)
    + self-validate tiers (union-merge `getLegalAttackers` → wholesale);
    block `canBlock` gate + `mustBlockAnAttacker` forced-add fixed point +
    terminal AiBlockController fallback; census
    applied/dropped/forced/fallback per window.
  - Python: featurizer combat candidates via the loader's own
    `_eligible_rows` (skew-free by construction); server translates per-row
    `act()` picks to entity-ref maps (dedup rows expand to the count head's
    k first-fit members; **combat player positions are self-first and map
    via seats — caught that the target decoder uses absolute-pi, a
    convention mismatch that would have silently poisoned serve**); combat
    tags auto-advertise only with trained combat heads. 63 tests green incl.
    combat parity + translation units.
- **Smoke (16 games) + arms (400 games): everything clean.** Telemetry at
  arm scale: attacks 3,919 assignments / drop 0.18% / 0 forced / 0 fallback;
  blocks 298 / drop 3.7% (superset meets menace/flying/protection at
  realization) / 0 forced / 0 fallback; 0 transport failures anywhere.
- **Arms tripwire: TIE — 0.4550 ± 0.0249 vs D3's 0.4625 ± 0.0249** (paired
  seeds), 400/400 decisive, 0 crashes. ADR-0012's prediction held exactly.
  Record: `data/runs/d5-arms-report.json`. Veto_rate read 13.9% (D3 ~10%) —
  same named leak class, re-read at D6.
- **D6 data loop verified closed:** smoke run ingested (16 games, 18K
  decisions, zero errors) and the bounded join extracts combat labels from
  MODEL games with 0 violations.
- ADR-0016 written; Status + map updated; training worktree removed.

## Broke / surprised me

- The "stuck" model server was Python block-buffering stdout to a file —
  the process was loaded and listening the whole time. A port probe beats
  staring at a silent log.
- First smoke run forgot `--census` (the flag exists per-run; --obs alone
  doesn't imply it). The standing rule says census+obs for eval runs —
  cheap re-run, but worth remembering the flags are independent.
- Player-index conventions differ per head family (target decoder:
  absolute registered index; combat heads: self-first position). Both are
  internally consistent train↔serve, but the serve translation is where a
  mix-up would have been silent. Caught by reading, not by a test — the
  combat parity test now pins the position convention.

## Next session picks up

- **D6: first V-trace self-play** (m2-rl-plan): δ=0 init from
  `d5-combat/last.pt`, cap-aware reward, concession disabled, anomaly
  monitor from day one; first runs 10s–100s of K games. Prerequisites all
  green: combat expressiveness (D5), rollout contract (D1), exact labels
  (D2), critic + Ante (D4), headless wedge fix (D4).
- Pre-D6 flagged items: both-seats-bridged per-game memory profiling
  (flagged at the D4 model-mirror); server micro-batching (59 rps batch-1 —
  the first throughput lever); X ">16" clamp watch (0.4% of casts).
- Parked-with-owners: veto class re-read (~13.9%) + block-drop class (3.7%)
  → serve-time re-ask lever, post-D6; Ante draw-bias archaeology; re-deal
  re-anchoring; GameCopier→GameSnapshot follow-up PR.

# Session 2: D6 OPENED — micro-batching built; both-seats memory bounded

## Done

- **Server GPU micro-batching** (`_Batcher` in server.py): streams featurize
  in parallel, one thread drains ≤16 requests / 3ms window into a single
  collated `act()`; per-item views keep translation unchanged; `pass_delta`
  rides per-item as a (B,1) tensor (mixed batches calibrate correctly);
  per-request lock gone. 63 tests green.
- **Validation run (240 games, both-seats-bridged, `d6-prep-mirror-20260714-152848`):**
  **73 rps sustained with only 2 concurrent workers** (chunk 120 limited
  parallelism — deliberate for the memory profile) — already above the
  59 rps batch-1 ceiling; batch-2 engaged on 21% of forwards at just 2
  streams (depth scales with stream count). Both-seats demand measured:
  **608 requests/game** (~2× single-seat) → ~169 rps at the 1,000 g/h
  target with 8 workers. Combat served at scale: 3,027 attack + 773 block
  requests, 0 fallbacks. Median 10.1s/game both-seats (7.8s single).
- **Both-seats memory profile: BOUNDED — the D4-flagged class is heap fill,
  not a leak.** All 4 worker JVMs plateau at ~2.4 GB (= Xmx2g + native),
  zero drift past the ceiling over 120-game chunks; game pace stable
  through the chunk. 120-game chunks are safe for D6 actors at current
  heap settings.
- Watch item moved: X ">16" clamp at 0.7% of casts this run (was 0.4%).

## Next session picks up

- **Fleet throughput number** (chunk 30, w=8, ~15 min): the g/h the V-trace
  actor fleet actually generates under deep batching — the missing number.
- **The V-trace loop build** (plan §D6): actors = harness chunks in serve
  mode streaming into the provenance store; learner on the 4090; V-trace +
  replay mixing; cap-aware terminal reward; concession disabled; anomaly
  monitor + per-checkpoint arms from day one. Init `d5-combat/last.pt`, δ=0.
