# 2026-08-07 (M6 path verdict: PATH B)

## Done

- **The path verdict landed
  ([ADR-0041](../decisions/ADR-0041-extended-curve-path-verdict.md)):
  PATH B — representation-blind confirmed at the feature level.**
  - Tranche component B completed overnight (6.5h, `tranche_b.py`,
    smoke-validated before launch): 1,600 fresh iter-019 eval-style
    games (seed base 20260806) → ingest → dual-critic early-doom
    traces → 437 fresh addressable losses (rate matches run11) →
    crash map (432 labeled, wr 0.258 ≈ the original map's 0.229 —
    population reproduces) + o2/o4 arms (426 each) = **1,274 labels
    from 426 NEW loss-games, zero trace-join misses**.
  - Final c2 benchmark: **5,356 labels / 985 games**
    (`frozen-probe-ext2-c2/`: dataset + both trunks' features at
    6,117 positions + probe report + ablation).
  - The deciding curve (c2/policy ridge): 0.361 → 0.453 → 0.434 →
    0.427 → 0.446 → 0.449 → 0.459 → 0.454 → 0.455 across 500 → 4.2K
    train labels — **flat from 1K at 2.8× ADR-0039's label count and
    ~1.8× its game diversity**.
  - **The diversity ablation closed the last confound:** ridge at
    fixed n on an identical holdout (24% fresh games), old-games-only
    vs mixed training: 0.432 vs 0.426 (n=2K), 0.444 vs 0.447 (n=3K),
    3 subsample seeds each — indistinguishable. Game-saturation
    excluded; the plateau is the representation's.
  - Verdict recorded with consequences: D2-B design session next (its
    own ADR before any build); **standing acceptance gate — any
    candidate representation must beat the 0.46 plateau on the frozen
    `frozen-probe-ext2-c2` benchmark before it earns a training run**
    (probe-then-path, recursively applied); rollout-label campaign
    stays parked; pool expansion deferred with sharper evidence.
  - Docs: ADR-0041, m6-plan (tranche status RESOLVED block),
    CLAUDE.md, map (Now pill, M6 details, ADR index, stamp).

## Broke / surprised me

- **Two monitor false-alarms, both instructive:** (1) the v1 tranche-B
  watch keyed on the driver log, which legitimately goes silent for
  40+ min during drill generation — watch label-file mtimes instead;
  (2) the v2 watch's find used globs for sweep-arm dirs that didn't
  exist yet, and zsh aborts the whole command on an unmatched glob, so
  the probe silently degraded to the quiet log — glob-free
  `find -path` fixed it (v3). The standing lesson ("dry-run watcher
  triplines") now explicitly includes the not-yet-existing-dir case.
- The fresh-game curation rate (437/1,600 ≈ 27%) and map winrate
  (0.258) both reproduced the original population closely — the
  pipeline is stable across generations, which is itself evidence the
  benchmark measures the representation and not batch idiosyncrasy.

## Next session picks up

- **The D2-B design session** (user decision point): scope
  structured-feature enrichment (live-vs-dead correlates), partial
  trunk/fusion unfreeze during value training, and the §1 encoder-swap
  escape hatch; price dataset-boundary implications; output = its own
  ADR. The frozen benchmark makes candidate evaluation cheap — design
  options can be probe-tested before any training commitment.
- D3 (curriculum arm) remains open and independent; D4 isotonic wiring
  remains open, small.
