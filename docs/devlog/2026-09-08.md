# 2026-09-08 — Build 3 evening 3 opened: ordering + damage

**Doc status:** devlog (session record). Plan: [m12-plan.md](../design/m12-plan.md). ADR: [ADR-0105](../decisions/ADR-0105-m12-build3-decision-surfaces-and-ability-representation.md). The morning's evening-2 close is in [2026-09-07-session2](2026-09-07-session2.md).

## State review (13:15)

Box quiet, tree clean at `0bcfc98`, fork at pin `bc01efe1609`; evening 2 closed at noon with all
four surfaces served. Read for evening 3 (ordering + damage), the keyed pool and 300 games of its
mainline census:

| surface | mainline windows, n ≥ 2 (300 games) | sub rows (1,000 games) | search headroom Δ ≥ 0.02 |
|---|---|---|---|
| trigger ordering (`orderSimultaneousSa`) | ~810 | 2,726 (n = 2 for 2,595) | 2.6% pooled, p90 0.004 |
| move-to-zone ordering (`orderMoveToZoneList`) | ~730 | 3,069 (mostly graveyard: mill, wraths, sac-all) | same pool |
| blocker / attacker ordering | 0 | 0 | — |
| damage assignment (`assignCombatDamage`) | ~106 (1,147 more at one blocker) | **0** | none |

- **Combat ordering never fires**: the engine runs the modern no-assignment-order rule
  (`GameRules.orderCombatants` defaults off; `Combat.orderBlockersForDamageAssignment` short-circuits),
  so "ordering" is trigger order + move-to-zone order. The trigger windows are real decisions
  (Solitude's ETB vs its evoke sacrifice, n = 2); most move-to-zone windows are graveyard orderings
  where the order is irrelevant, the library ones (Brainstorm-style put-backs) the few that matter.
- **Damage has zero sub rows by construction, not by bug**: a search window is a quiescent
  MAIN1/MAIN2 window and a copy stops at the seat's next quiescent window — on the pass path the
  start of combat — so no copy crosses blocks and damage; the leaf value has never seen a damage
  assignment. The mainline carries ~350 multi-blocker windows per 1,000 games (imitation labels).
- **The enumerator's damage family assumed the legacy order** (lethal in list order, the remainder
  on one blocker); under the rule in force any split is legal and the AI orders internally.
- Evening-2 lesson check: neither ordering callback bundles a joint decision (trigger targets are
  chosen as each trigger hits the stack, after the ordering; the damage map is the whole decision
  and the engine validates the sum) → no gate. Distillation has nothing to teach here (ordering's
  single-roll leaf σ 0.065, the largest kind, against a headroom at noise) → imitation only.

## Decisions (user, 13:30)

1. Imitation only for ordering and damage, distillation off for them (the entity + mode heads keep
   e2's recipe so the only change vs e2 is the two new heads). RL reaches them in the loop (Build 4½
   / 5) through the served head's behavior logp; the leaf-value channel cannot reach damage until
   search runs from combat windows.
2. Both ordering callbacks served; windows past the decoder's 12 slots fall back to the natural line.
3. Damage ships imitation-only this evening (a "nothing broke" component; 0.35 windows per game, the
   600-game read cannot see it).
4. The wire: ORDER_N (already on the proto) for both; damage as a kill-order sequence, realized as
   amounts by the engine (no new response field needed).
5. Ride-alongs on the one fork commit: the damage enumerator family fix, the copy-state snapshot null
   guard.
6. Evening order after this one unchanged: evening 4 = payment tag, evening 5 = mainline surface
   acting with modes first.

## Built (13:30–13:45)

**Fork `41ac60d6b21`** (on the gate `bc01efe1609`): tags `mtg.surface.order` / `mtg.surface.damage`
(the AnvilRun default set); `AnvilBridge.order` (ORDER_N; a server fallback returns the echo object
and reads as "not answered" → the natural line, never an identity ordering); `Surfaces.askOrder`
(a validated permutation, `ORDER_MAX` 12) inside `forceOrderSa` / `forceOrderCards` (the callback
name threaded from the generator); `Surfaces.askDamage`: the model's kill order over the blockers
(+ the defender last under trample) → `damageFromSequence` (lethal in sequence while damage lasts;
**the remainder tramples over when the attacker tramples — `distributeAIDamage`'s own rule — else
lands on the last blocker picked; a defender pick = "stop killing here"**) → the amounts map; the
damage dec's option list carries the defender under trample plus `lethal` per blocker and
`trample`; the DAMAGE enumerator family = every prefix of every blocker permutation, each also
closed by the defender under trample; `ObsSnapshot.look` = a null-safe `mayPlayerLook` (the pool's
null-obs decs carried `Cannot invoke Player.getView() because "player" is null`: an option card
outside the walked zones on a copy has no controller). `SurfacesEnumerateTest` 16 (the damage
family rewritten + `damageFromSequence`), mvn package clean.

**Anvil `ee6c512`**: `surf_order` / `surf_damage` built in `anvil/policy/surfaces.py` (order: lo = hi
= n, identical options fill distinct slots, n > SURF_MAX → not a decision; damage: lo 1 / hi n, the
label = blockers dealt lethal — without `args.lethal`, any damage — in descending amount then list
order, a partial blocker last, the defender only where the heuristic trampled while leaving a
blocker untouched); the featurizer tags; the server answers ORDER_N (an early-stopped ordering
completed in option order, counted; a damage sequence truncated at the defender); the generator's
new hooks. pytest 34 pass. Loader check on the keyed pool (150 games, the b3s2 side-table cache):
order 785 examples (5.2/game; n = 2 for 594), damage 56 (0.37/game), 0 ak misses, 0 label misses.

## Launched (13:45)

- **Forkcheck `run-20260908-build3-e3`** on the snapshot `data/runs/build3-e3/forge-b3e3.jar`
  (commit `41ac60d6b21`), 500 games, seed 20260703; watchd `forkcheck-b3e3` (stall 30 m), the
  waiter notifies on exit. Read: `scripts/forkcheck/compare.py` vs the 08-21 baseline.
- **The fit chain `build3-e3`** (`data/runs/build3-e3/`): five tasks, the e2 recipe (stores b2-* +
  the keyed pool, distillation on the pool's frames for entity + mode at T 0.025 / w 1.0, the b3s2
  cache), 5 folds × 4,000 steps → the read → the build `data/training/m12-build3-e3/last.pt`; watchd
  `build3-fit` (stall 45 m), notifies DONE / FAILED.

Next: the forkcheck read → pin; the chain's read (the order / damage cross-fit) → the served-head
smoke on the e3 ckpt with the new jar (`build3_surface_smoke.sh`, CKPT + JAR) → the 600-game paired
read (`build3_surface_read.sh`, NAME b3e3: served = all six tags vs withheld) — pre-registered:
"nothing broke" (no new crash class, misses counted, strength within noise), the exposure split on
games with a served ordering / damage answer.

## Routed by name

- A fresh keyed pool on this jar (the new damage dec shape: the defender option + `lethal`; stable
  keys) — rides the next label regeneration (the shakedown era), not this evening.
- Search from combat windows (the only way the leaf value reaches damage) — the closing evening's
  mainline surface acting, or Build 4½.

## The forkcheck (14:30) — PASS, fork pin `41ac60d6b21`

498/500 vs the 08-21 baseline (fidelity 448/52 vs 450/50): 20260969 the standing crash, 20260853
replayed twice on the same jar to the baseline hash `6ec06cb75d96f2b2` → PASS, pin moved (CLAUDE.md,
map, ADR addendum). Folds 0–1 of the chain: order exact 0.742 / 0.729 — trigger ordering 0.852
against the keep-the-input-order baseline 0.770, move-to-zone 0.645 *below* it (graveyard orderings
the heuristic leaves alone; a first-fit observation, the paired read decides) — damage 0.381 / 0.369
(slot-0 0.597 vs first-option 0.509, n ~600 per fold); entity + mode at e2 levels.


## The chain read, the smoke, the paired read launched (15:08–15:35)

**Pooled cross-fit (`data/runs/build3-e3/read.md`)**: order 0.732 (n 62,078) — trigger ordering
0.846 vs the keep-the-input-order baseline 0.774, move-to-zone 0.626 *below* its 0.757 (the head
flips orderings the heuristic leaves alone, mostly inert graveyard orderings; library put-backs are
in there too — the read's per-callback split watches it); damage 0.376 (n 2,841; slot-0 0.607 vs
first-option 0.487). The entity + mode heads moved a little with two more tasks on the shared
decoder: set 0.450 → 0.429, one 0.338 → 0.330, mode 0.393 → 0.387 (banked; the read decides).
Build ckpt `data/training/m12-build3-e3/last.pt` (15:22).

**Served-head smoke (`data/runs/build3-e3-smoke`, 8 games, rate 0.25, `-searchsurf 2`)**: 8/8 won,
rc 0, 214 s; the server advertises all six surface tags; **served answers accepted: trigger
ordering 64/64, move-to-zone 53/53, damage 2/2 (k = n: full kill orders), entity 262/262, mode
25/32 (gate pass 11 / defer 7 / free 14); 0 MODEL ERRORs; 68 sub rows, 0 misses, 0 null-obs
frames** (the snapshot guard; was 1.3% of frames). Read: `surface_served_census.py` +
`surface_smoke_read.py`. Script fix: `build3_surface_smoke.sh` ignored the `JAR` env (an
unconditional assignment) — the run was saved by `target/` still equalling the snapshot (sha
`d8cfb4b0…`); fixed.

**Launched 15:35: the paired read `build3-surface-read-b3e3`** (the e3 ckpt on the snapshot jar,
2 × 300 games per arm vs the heuristic: `off` = the server withholding the surfaces + tuck, `on` =
all six surface tags + tuck; watchd `build3-read-b3e3`, notifies; `read.json` ~18:00).
Pre-registered: "nothing broke" (no new crash class, misses counted, on − off within noise); the
exposure split by served ordering / damage answers, per callback.

## The paired read (16:26) — EVENING 3 CLOSED

`read.json`: off 0.504 / on 0.503, **on − off +0.52 ± 1.82 (t 0.3, 57 up / 54 down, n 580)**; 4
BridgePoisoned per arm (the standing class), no new crash class; served census on the on arms:
trigger ordering 741/741, move-to-zone 844/844, damage 61/61 accepted (k = 1 for 23), mode gate
96 / 150 / 45; 0 server MODEL ERRORs. Nothing broke; six surfaces + tuck served from here.

**The exposure split is selection.** Keyed on the ON arm's game: trigger-exposed +9.6 ± 2.5 (t
3.8) / unexposed −6.9 ± 2.5. Keyed on the OFF arm's game (the counterfactual): −2.2 ± 2.7 / +3.0
± 2.4. The sign follows whichever arm defines exposure — a multi-trigger window fires when that
seat's board survived, i.e. in that arm's better games. Symmetric key (fired in both): +4.7 ± 2.3 /
−6.3 ± 2.9, a length selection. Standing rule written (gating/reads); evening 2's attribution rests
on its ladder arms, not its split. `surface_exposure_read.py` gained `--key fired --arm on|off|both`
and the caveat.

Box quiet 16:30; nothing registered.

## Next session picks up

- **Evening 4 = the payment tag** (the ADR-0102 rescue class): the payment enumerator on search
  copies (the M9 payment surface as the option set), distillation into the pay head, the serve
  path unchanged (`mtg.pay_mana_class` already served), the paired read. Read the M9 payment
  surface spec + ADR-0075 (perfect-payment headroom +2.96pp/g) before scoping.
- Evening 5 = mainline surface acting, modes first (the principled replacement of the gate).
- Routed: the fresh keyed pool on `41ac60d6b21` at the next label regeneration; search from combat
  windows; the move-to-zone head's below-baseline agreement (watch on library put-backs).

