# 2026-09-05 — harvest h1 read; distill on harvested labels; toward the gate re-read

Continues the 2026-09-04 session (one continuous session across midnight).

## Overnight — harvest h1 (auto-stopped at the third batch boundary, 04:21)

| batch | points | certified | natural | holds | wall |
|---|---|---|---|---|---|
| b00 | 269 | 53 | 200 | 12 | 141 min |
| b01 | 262 | 59 | 202 | 14 | 132 min |
| b02 | 274 | 47 | 226 | 14 | 149 min |
| **total** | **806 → 799 labels** | **159 (20%)** | **628** | **40** | 7.0 h |

- Certified rate 20% = the mint's band (19–28%); mean certified composite +4.6 (b00).
- Crashes: 0.3–0.4% of completion rows, all whole-point clusters (every copy of one fork
  state fails) — the copy+resume class; no OOM, server clean throughout.
- The stopper worked as designed (kill of the process group seconds into batch 3; the
  partial `sched-harvest-h1-b03-*` run dir is un-ingested scratch for the stale-data pass).
- Cost: ~2.4 h per 480-game batch at 5%; void arms 32% of rollout time (yield fix = a Java
  legality pre-filter, deferred until the distill/gate says a longer harvest is worth it).
- Loader flags in `data/runs/sched-harvest-h1/harvest-manifest.json`.

## 04:45 — carrying on: distill on harvested labels + the jar-identity proof, in parallel

- **Distill build launched** (`m10-planner-distill-hand3`): the 1,900-game executor corpus on the
  hand basis + the three harvest label files joined as roles cert/natcf (`--certified`); fit
  next with the v2/v3 recipe (`--init-from-cast-head --lr 3e-4`) and `--cert-weight 10` (the
  certified-on-parity experiment's oversampling).
- **Jar-identity proof launched** (`data/forkcheck/run-20260905-certify-jar`): the standing
  `run_forkcheck.sh` (heuristic-only fork fidelity, 500 games, seed 20260703) on the new jar
  (fork bcf0fc73c5f) — the same seed set as `run-20260821-m9boundary` (engine 2f87180c), so the
  per-seed `mainTraceHash` match rate against that run is the ADR-0025 empirical proof for the
  whole chain of engine commits since the boundary. Expectation: ≈90% identical (the 08-21 run's
  own fork-vs-main residual was 50/500 — hash-ordered iteration across JVM launches); a systematic
  game-path change would read near 0%. Note: the runner needs an ABSOLUTE out dir (it cd's into
  forge-gui before opening its log; first attempt failed on that).

## 05:15 — distill on harvested labels: NOT learnable at 159 certified; gate read NOT launched

- `m10-planner-distill-hand3` (executor corpus + 799 harvest labels, cert ×10): holdout CE 0.7119
  (hand2: 0.6928), emission exact 0.539 / slot-0 0.912 (hand2: 0.564 / 0.915); **certified holdout
  (n=20): exact 5%, slot-0 18%, Jaccard 0.18**; natural-confirmed (n=71): slot-0 0.89. The same
  answer as the 09-04 certified-on-parity experiment at 543: the certified signal does not fit at
  this scale, and the planner is effectively the hand2 graft — a gate read would re-measure −2.3pp,
  so 4 h of GPU were NOT spent on it.
- **What the certified labels are** (159 windows vs the natural line at the same window): disjoint
  card sets 47%, overlapping-but-different 28%, subset (holds) 14%, superset (adds casts) 10%,
  identical 1%, **pure reorderings 0%**. The search's improvements are content, not order — which is
  why a head that clones the executor cannot interpolate to them, and why volume is the lever.
- **Scale arithmetic:** 20% certified × ~330 points/batch ≈ 66 certified windows per 2.4-h batch →
  a mint-scale certified pool (~2,000) is ~30 batches ≈ 70 h at 8 workers (≈ 32 h with the void
  pre-filter + 12 workers). That is the promotion-run-scale accumulation the reset draft assumed
  (labels accruing across ~30 training iterations), not an overnight harvest.
- **Morning decision (user):** (a) accumulate labels inside probe7's generation as designed
  (inline, 2–5%, ~30 iterations) and distill/PG on the growing pool — the labels ride training
  rather than precede it; (b) a multi-day dedicated harvest first (with the pre-filter + 12
  workers); (c) change the learning target toward the arm SPREAD (pivotal-moment head) rather than
  exact arm reproduction. My lean: (a) — it is the design as adjudicated, and the day-zero
  baseline for it is already read (−2.33 ± 0.70).

## 05:40 — jar-identity proof PASSED (fork tip bcf0fc73c5, the certifier jar)

`data/forkcheck/run-20260905-certify-jar` (500 heuristic games, seed 20260703 — the 08-21 boundary
run's seed set): **498/500 main-line trace hashes bit-identical to `run-20260821-m9boundary`**
(engine 2f87180c); fork-fidelity tally 449 clean / 51 divergence vs 450 / 50. The two mismatching
seeds (20260853, 20260969) replayed twice each on the NEW jar gave two different hashes each time —
launch-to-launch instability on one jar = the documented identity-hash residual (hash-ordered
iteration; FIXED_HASH=1 is the diagnostic that pins it), not a game-path change. So every engine
commit since the boundary (f9eadfa Obs.mark fix, 599142d parser, bcf0fc7 certify) is proven
behavior-identical on this seed set at the ADR-0025 standard; the census replay (the paired read's
population) stays valid on the certifier jar. Map's fork-pin row updated. Note for the runner: the
main-line hash IS stable across JVM launches for 99.6% of seeds — cross-jar identity checks can use
it directly, with the residual named per seed by a same-jar re-run.
