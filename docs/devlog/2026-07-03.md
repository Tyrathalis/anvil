# 2026-07-03

## Done

- **Overnight soak verdict: Forge forward-play is leak-free** (ADR-0001 M0 deliverable (a), first half of the fork-API gate). 6,000/6,000 headless Commander games in one JVM, 8.18 h, exit clean at 05:14. Heap-after-GC flat-to-negative (233 → 227 MB quartile averages, −3.6 MB/h over 10,467 GC events) — **LearnForge's 2016 killer is dead**. RSS drifted ~26 MB/h post-startup (native/JIT/metaspace, not heap) → harness should recycle workers periodically as routine hygiene, no bug hunt warranted.
- **First sustained throughput number: 734 games/h single-thread** (4.9 s/game; heuristic-vs-heuristic, precon decks, 4g fixed heap). The §15 claim "post-optimization 10+ games/min on 32 cores" now looks conservative for raw forward-play — one thread already does 12.2 games/min. Caveats: neural inference + featurization + gRPC not in the loop yet; random-legal games may run longer; allocation pressure (~2.3 GB/game) is the multi-worker scaling risk per MageZero's heap-bound precedent.
- Slow-match tax measured: 24/6000 games (0.4%) hit the 120 s draw clock, costing ~10% of wall clock — cap-aware handling (§3d) has a measured baseline.
- Exception census: 259 heuristic-AI per-decision `TimeoutException`s (complex targeting, e.g. kicked Comet Storm — AI falls back and plays on) + 18 clock-related `InterruptedException`s. Zero crashes/OOMs. "Engine gaps become agent gaps" file: the incumbent AI *times out* on exactly the decisions Anvil should handle natively.

- Planning checkpoint decisions:
  - **Hardware:** current box measured — Ryzen 9 7950X (16c/32t), 62 GB RAM, RTX 4090. This *is* approximately the design doc's target spec; the "buy a 32-core box" budget line is deferred until M0's scaling curve says whether it's needed.
  - **Community outreach: deferred entirely** until there's a working artifact (post-M2-ish). Amends ADR-0001's "community actions unlocked" on timing only — the *how* (Discord/PRs, never issues) stands. Accepted risk: canonicalization patch gets built without early maintainer input.
  - **DC card pool + decklist pipeline: greenlit as a parallel track** (spun off as its own task): gather DC meta lists, convert to .dck, coverage-check against Forge's cardsfolder, verify DC rules are expressible in Forge's format config.
  - Scaling test approach settled: separate JVM processes (not threads — MageZero's halving was shared-heap GC contention), heaps tuned down to 1–2 GB given ~230 MB live set.

## Broke / surprised me

- Nothing broke overnight. Surprise was positive: single-thread throughput and stability both beat the design doc's implicit expectations.

## Next session picks up

- Second half of the fork-API gate: **`GameCopier` fork-fidelity differential test** (fork + no-op replay must reproduce mainline trajectory). Then ADR-0002 resolves the gated fork-API bet with both measurements in hand.
- Multi-worker scaling curve: same soak workload at 1/2/4/8/16 parallel JVMs (or threads) — watch games/h/worker vs. GC pressure (MageZero's halving is the failure mode to test for). This yields the real M0 games/hour number.
- M0 bridge protocol draft (ADR-0001 invariants) + `PlayerController` override plan.
- Survey reading list (prior-work-survey.md §6) still open.

---

# Session 2 (evening): fork-fidelity harness built; measurement pipeline running overnight

## Done

- **Fork-fidelity differential test designed, built, validated, and running** (ADR-0001 M0 deliverable (b)). Design note: [fork-fidelity-test.md](../design/fork-fidelity-test.md). Implementation is `forge forkcheck` in the fork (commits `d68d3ed`, `8f6f09e`): forks a live game via `GameCopier` at a quiescent active-player MAIN1 priority point, replays the fork to completion with a serialized clone of the `MyRandom` state, diffs per-turn state digests (life/counters/mana + per-zone card multisets, library **in order**) against the mainline, then compares outcomes.
- **Archaeology findings that shaped the harness:** `GameEventTurnBegan` fires mid-phase-advance (fork there and the copy never untaps); `GameCopier` doesn't copy the waiting-trigger queue (drain triggers before forking, defer if the stack is non-empty); a fresh copy's `PhaseHandler` silently *skips priority for the forked phase* — fixed with a 1-method engine patch (`devResumeAtPriority()`), which is the first concrete brick of the "single-step fork API" upstream story. Thread-interrupting timeouts replaced with watchdog draw-clocks so a hung fork can't leak a thread that keeps consuming the shared RNG singleton.
- **Detector validated end-to-end** with a `-perturb` mode (1-life delta injected into the fork): 3/3 detected at exactly the first turn boundary after the fork, diff sample pinpointing the life line.
- **First fidelity numbers (run 1 in flight, v1 semantics, n=41 at time of writing):** ~37% clean (fork reproduces mainline trajectory *and* outcome exactly), **~56% trajectory divergence** (typically manifesting turns later as library-order drift — i.e., an earlier decision delta desyncing the RNG stream), **~7% static mismatch** = the copy differs at copy time. Two concrete `GameCopier` bug classes already isolated, both seed-reproducible: **face-down/hidden exiled cards come back face-up in the copy** (seeds 20260719, 20260741; the PRUNE_HIDDEN_INFO TODO made flesh), and **a duplicated "Keyword Effects" object in the Command zone** (seed 20260723). Copy cost is a non-issue: 7–30 ms.
- Fork commits: `d68d3ed` (forkcheck v1 + `devResumeAtPriority`), `8f6f09e` (v2: fork only at active-player priority windows; `-freshrng` discriminator).

## Broke / surprised me

- Smoke test came back 3/3 clean and looked like GameCopier vindication; 41 games later the divergence rate is ~56%. Small-n optimism, the old story.
- The heuristic AI consults `MyRandom` at 82 call sites — so *any* single decision delta between mainline and fork silently desyncs the RNG stream, and the first visible symptom (library order after a shuffle) can be many turns downstream of the actual divergence point. Trajectory comparison catches this; single-point state comparison (all upstream has today) never would.

## Overnight pipeline (chained, unattended)

1. **Run 1** (in flight): 500 games, v1 fork semantics → `data/forkcheck/run-20260703/`.
2. **Scaling sweep** auto-starts when run 1 exits: 1/2/4/8/16 parallel JVMs × 300 games, 2 GB heaps → `data/soak/scaling-20260703/` — the real M0 games/hour number.
3. **Run 2**: 500 games, v2 semantics (active-player-priority forks only).
4. **Run 3**: 500 games, v2 + `-freshrng` — the discriminator: if divergence with a *different* RNG matches the same-RNG rate, RNG cloning isn't the binding factor and object-identity effects (hash iteration order) are implicated as the deep cause.

## Housekeeping decisions (user)

- **Long-running jobs at low priority:** all three launcher scripts now `nice -n 19` (desktop preempts workers; idle box measures the same). In-flight run 1 keeps normal priority; everything queued behind it picks the change up.
- **Pause/resume designed-in where convenient:** recorded in design §9 ("disposable workers") — graceful stop + seeded game-granularity resume as first-class M0-harness features; training side already pause-friendly (checkpoints + V-trace actor tolerance).
- **Deckbuilding coaching added to Mentor's scope** (design §11, lower priority): deck advice as a product surface over Tutor's evaluation machinery + Ante correction + the evidence-packet narration pipeline; no new ML; sequenced after gameplay coaching.

## Afternoon results (all measurements landed same-day; see [ADR-0002](../decisions/ADR-0002-fork-api-gate-resolution.md) for the full evidence tables)

- **Scaling curve:** 922/677/688/375/242 games/h/worker at w=1/2/4/8/16 → **3,866 total games/h (64 games/min)**, monotonic, no MageZero collapse. §15's post-optimization target beaten 6× pre-optimization; 32-core purchase declined. Efficiency staircase fits boost-clock + split-L3 physics (box confirmed quiet — user works on laptop).
- **Fork fidelity, 4 × 500 games:** v1 and v2 semantics identical (38% clean / 50% divergence / 12% static mismatch — priority-window suspect exonerated). Divergence is **deterministic** (bit-identical across JVM invocations — 3 repeats + original on seed 20260704), implicating copy-construction insertion order. **freshrng discriminator:** RNG cloning necessary (removing it: 38→14% clean) but not binding (50% diverge with a perfect clone). **Chain run:** copying canonicalizes — 96% of F1-divergent games have F2≡F1; fresh drift 2.4%/fork; the duplicated-effects bug compounds per copy (25% of static-mismatch games re-corrupt).
- **Decisions (with user):** fork API bet **fires** — flagship upstream contribution, sequenced before M2, not an immediate detour; two static bug classes → root-cause + fix as upstream PR #1 post-M0 (60-seed repro set as test suite); interim: fork-at-start + 6ms digest tripwire. All recorded in ADR-0002.
- Extension sweep (w=16 control/20/24, `-XX:ActiveProcessorCount=2` GC caps, 200 games/worker) launched at day's end — answers SMT value + GC-thread-oversubscription share of the efficiency droop. **w=16 capped verdict (in-flight read at n≈1,600 games): null — 243 g/h/worker vs 242 uncapped.** The efficiency droop is hardware physics (clocks + split L3 under allocation traffic), not GC-thread oversubscription; allocation profiling confirmed as the top throughput lever. **Decision (user): keep `-XX:ActiveProcessorCount=2` as the worker-JVM default anyway** — conceptually correct (JVM sized for its actual slice), protects against the pathology reappearing at other heap sizes/worker counts, measured cost zero. Goes into the M0 batch-harness spec. w=20/24 SMT results append here when the sweep finishes.

## Next session picks up

- Extension-sweep results → M0 harness defaults (worker count, JVM flags).
- M0 bridge protocol draft (ADR-0001 invariants: legal-actions-only materialization; one-shot-or-micro-step answering) + `PlayerController` override plan + batch-harness spec (disposable workers / pause-resume per design §9).
- Survey reading list (prior-work-survey.md §6) still open.
