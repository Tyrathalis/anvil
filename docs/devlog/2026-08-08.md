# 2026-08-08 (campaign resolution + two probes: otag ✗, hidden-info ✗)

## Done

- **Label campaign RESOLVED
  ([ADR-0046](../decisions/ADR-0046-label-campaign-resolution.md)):**
  12.6h, freeze-proven, `labelset-c2-v3` = 8,683 train / 1,197 frozen
  holdout. Deciding sweep picks **N=4 = 0.4829 ± 0.0010**; head-only
  rose to 0.4693 (the frozen plateau was partly sample-limited);
  capacity follows labels as ADR-0044 predicted. Label expansion priced
  (~+0.006/doubling) — no further buying. Mid-campaign: the two-round
  ck1 early-stop-drift fix (inner-val pool pinning, now standing) and
  the ck2 lesson (checkpoints sweep the two leading Ns).
- **Otag probe ([ADR-0045](../decisions/ADR-0045-otag-feature-probe.md)),
  prompted by talor's clustering artifact: NEGATIVE for the critic** —
  no functional family lifts 0.4552; reconstruction INVERTS B-1
  (function unencoded at R² 0.26 yet not the missing signal).
  Pool-expansion caution strengthened; co-occurrence filed for Tutor.
- **Hidden-info probe ([ADR-0047](../decisions/ADR-0047-hidden-info-probe.md)):
  full-vis windows buy ZERO ranking** (N=4 0.4790 vs masked 0.4829;
  N=0 0.4664 vs 0.4693) — replicating ADR-0039's d4-trunk observation
  from a second direction. Belief head deprioritized for ranking; the
  graduated build stays on the masked path. `ValueEvaluator` grew a
  caller-side `full_vis` override; `unfreeze_probe prep --full-vis`.
- Repo pushed through the day (talor may fork; fork master was already
  current).

## The elimination chain (the week's shape)

Not arithmetic (0043) · not function (0045) · not hidden info (0047) ·
not label count at current slope (0046) · partially gradient-reachable
(0044/0046: 0.4829). Remaining candidates for 0.48→0.9: deeper
capacity / B-3 encoder, or a static-evaluation ceiling — rollouts
search, a head does not (the tier-3 argument, now a named M6-closeout
question).

## Next session picks up

- **The graduated build at N=4, masked path** (ADR-0046 decision 1):
  era-scoped value fine-tune on `labelset-c2-v3`; value-tower /
  partial-layer discipline for the shared policy trunk; mini-run
  validation; the standing 2,000-game combined paired read vs 0.5316.
- D3 (curriculum arm) remains open, independent.

## Addendum: the graduated critic asset (user approved path A — value-tower)

- **`data/training/rank-critic-c2v3/`** — the N=4 ranking fine-tune
  productionized (`unfreeze_probe build`: 3 seeds, selected by
  INNER-VAL — the frozen holdout stays a pure reporting read; chosen
  seed 1). Round-trip validated through ValueEvaluator: holdout
  Spearman **0.4833**. CRITIC ckpt: never serve policy from it.
- **Era-scoped isotonic map banked**
  (`isotonic-maps-rank-critic-v1.json`, key `c2/v_rank`): the ranking
  loss partially self-calibrated (raw ECE 0.11 vs the old critic's
  0.24–0.35); remapped 0.03 holdout / 0.0004 asset.
- Next: the curation cycle that consumes it — **migration read first**
  (standing M5 rule: gatekeeps cycle pricing), then fresh curation from
  the ckpt-of-record's losses with the new critic + calibrated doom,
  D3 composition fold-in, training run, standing 0.5316 paired gate.

## Addendum: cycle 3 launched (d6-run13)

Full pipeline landed same day: migration read POSITIVE (addressable
576→422 on same games; 31% of the old curriculum reclassified
luck-locked; crash windows ~2.5 turns earlier; 27 new games) → fresh
critic-untainted stock (1,599 games, 45.4% addressable — replicates the
same-game read) → map (mean crash-anchor wr 0.341 vs 0.229–0.258 old
method) → o2/o4/peak arms → **selection v4** (321 drills, D3 folded in:
18.7% ahead-anchored per the a2 recipe, winnable slice 20% via band
0.25:0.85, mean wr 0.495; `compose_selection.py` = the anchor-mix
adjuster). **d6-run13 LAUNCHED** (user-approved): run11 recipe verbatim,
init iter-019, in-loop critic unchanged (attribution: the cycle's delta
= curation method + composition only), 20 iterations, watchd
self-registered + sleep inhibitor held. Gate: standing 2,000-game
combined paired read vs 0.5316 + evalset-v3 decomposition (the winnable
residual is D3's read). ~2–3 days.

## Addendum: run13 iteration-0 crash + relaunch (fork-index collision)

First launch crashed at the iteration-0 critic phase: MultiStore refused
the replay-mix join — fork stores encode game indices as
`source_g*10000 + fork_k`, and selection v4 contained a source game with
**g=0** (encoded forks 0..7 collide with mainline indices; mainline maxes
at 9,599 over 20 iterations, so g=0 is the ONLY colliding case — run11's
selection simply never drew game 0). Fix: the one g=0 row dropped from
selection v4 (321 → 320, shares unchanged; recorded in meta), iteration-0
partials wiped (no loop state existed), identical relaunch — running.
Latent hazard filed as a spawn-task chip: offset the fork encoding /
namespace MultiStore indices, NEXT era (store-format change = era-scoped).
