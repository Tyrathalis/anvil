# 2026-08-11 session 4 (D3 boundary: upstream rebase + Talor #8 + format sweep; overnight gates armed)

## Done

- **Fork rebased onto upstream/master: 45 fork-local commits over 210
  upstream commits, ZERO conflict stops** (backup branch
  `pre-rebase-20260811` kept; PR #1's merge linearized). Full desktop
  suite green on the new base — our fail-first tests
  (EffectSourceCopy/StationTapCost/MenaceBlockRealizer/ConniveDiscardMap)
  all ride and pass, so the connive question self-resolved. Upstream did
  NOT fix the station bug (our fix stands as an upstream candidate).
  Notable upstream change: **#11436 gives the heuristic depth-zero
  simulation validation** — GameCopier is now hot in NORMAL games (our
  effect-source fix got more load-bearing) and the heuristic likely got
  stronger again (the re-baseline will price it).
- **Monarch set-code NPE fixed fork-side** (`46c0c0893e`, the inverted
  ternary vs its correct initiative twin) — deferred until now purely on
  boundary discipline; still an upstream candidate (#11203 sibling).
- **Anvil #4 format sweep LANDED (`c383bec`, 88 files)** with the pinned
  mu-determinism gate passed properly: pre-format worktree server vs
  post-format server, same seeds/jar, obs ON — **outcomes identical and
  1,408 mu records byte-identical**. (First A/B attempt was vacuous:
  without `-obs` the bridge declines and both sides fall back to
  heuristic — 0 mu rows. The M1 rule again: eval runs keep obs on.)
- **Talor #8 adapted + landed (`8320550`, author preserved):** pool/ →
  pool/dc/ + pauper/ sibling, --format threading (default dc), DC
  UN-migrated (data/pool/ flat, CURRENT pin verified intact:
  pool-cf2ca6ba, 1,701 cards). Adaptations: dropped his get_torch_device
  import in encoder (our #7 scope: offline tools only) + the
  schemas.manifests import (never adopted). Kept his late-binding
  exclude() closure fix. 150 pytest green (11 new), pushed (pre-push
  gate green).
- **Overnight chain ARMED:** post-rebase forkcheck (500 games, default
  mode, `data/forkcheck/run-20260811-postrebase/`) → on completion the
  chain auto-launches `final_read --name d3-rebaseline` (iter-019,
  ~2,000 games; watch-registered; anvil-watch.timer confirmed active).

## Broke / surprised me

- The obs-off A/B looked green (identical outcomes) while testing
  NOTHING — the model never played. Empty mu files were the tell.
  Determinism gates need a positive control (mu row count > 0).
- A compound kill/relaunch one-liner killed its own process group
  (exit 144) — separate launch commands per server; the pgrep
  self-match memory exists for a reason.
- His #8 was stacked on the full #4 sweep, so `main...talor-8` showed
  105 files; the real commit was 18. Isolate the commit before judging
  scope.

## Next session picks up

(Re-sequenced per user, late addendum: fork master force-pushed after
checking Talor's fork was quiet — backup branch `pre-rebase-20260811`
pushed too; user warns him on Discord. Watchd + monitor stall coverage
armed for the overnight chain AFTER the user asked — third occurrence of
the arm-at-launch miss, recorded in memory; the pgrep self-match class
also recurred in the first monitor script. **User pinned a documentation
cleanup PAUSE: after the rebaseline read + boundary ADR, before any
further implementation.**)

1. **Read the overnight gates:** forkcheck fidelity numbers (compare
   divergence vs the 7.0% era baseline; statics should stay 0) + the
   d3-rebaseline final_read → **new baseline number** → boundary ADR
   (supersedes 0.5316 as the standing gate).
2. **DOCUMENTATION CLEANUP PAUSE (user-pinned, blocks implementation):**
   trim CLAUDE.md Status (the M7 bullet is a wall — archive-move +
   compact rewrite), trim project-map.html (unbounded footer stamp,
   accreted Now panel), reorganize/index docs/ (54+ ADRs, ~20 design
   docs, ~40 devlogs), consolidate the memory directory. Driver:
   recurring process misses (stall-check arming, pgrep self-match)
   partly attributed to top-heavy docs that stopped biting.
3. **ADR-0049 instruments → scripts/** (after the pause; run over the
   fresh re-baseline stores for the gate's baseline read).
4. **C-bundle build** (rl.py L_seq + masked-head aux; finetune_value
   C2a aux; first-attempt-only λ=0.01 in rejected_events; selfplay
   forced-seq phase; w_seq calibration at run start).
5. Upstream filings queue: station tap guard, GameCopier effect-source,
   monarch NPE (all clean candidates off the fresh base).

## Late addendum 2 (post-crash: the pgrep hook, issue #9, chain v2)

- **pgrep self-match guard MECHANIZED (user-approved):** global
  PreToolUse hook (`~/.claude/hooks/pgrep-selfmatch-guard.py`, wired for
  Bash+Monitor in `~/.claude/settings.json`) DENIES any `pgrep/pkill`
  full-cmdline match (`-f`/`-af`/`--full`) whose shell segment lacks a
  bracket class, with the corrective message delivered at the moment of
  the mistake. 10-case pipe test + live-fire both directions. Rationale
  recorded: the `sh -c` execution model inverts a near-universal shell
  prior — a write-time hazard no documentation fires on (1–2 dozen
  recurrences per user). Cleanup-pause rider: sweep memory for other
  recurring "always/never" rules that are really mechanism candidates.
- **Claude Code session crash mid-evening:** the (session-owned) chain
  v1 and monitor died with it — forkcheck survived (setsid) but the
  rebaseline would NEVER have launched. The handoff-failure scenario the
  monitor existed for, caused by the monitor's own host dying. Standing
  conclusion: chains must be session-independent (chain v2 below).
- **Talor issue #9 (grpc off-by-one after >5s response) CONFIRMED +
  FIXED (`d798917ae5`, pushed):** requests carried `decision_seq` since
  M0, server echoed it, client never checked — the bridge-protocol-v0
  mismatch clause was specified but unimplemented. Measured exposure:
  183 deadline events / 22 worker invocations all-time, ALL in
  drill/labeling/eval runs co-running with training (GPU contention);
  ZERO in training-trajectory stores; the drillo6-c2t3 log shows his
  cascade signature verbatim (consecutive deadlines seq 4089/4090 →
  unlogged silent misassociation after). Fix = his contained tier +
  poisoned-stream state machine on our disposable-worker machinery:
  seq check on all three decision paths; deadline/non-response/mismatch
  → `BridgePoisonedException` (game recorded crash status, discarded),
  worker drains, harness recycles + replays. `GrpcBridgeSeqTest` (3
  tests, scripted in-process grpc server) pins the invariant incl. the
  cross-game clause. Deadline now `-Danvil.bridge.deadline.ms`
  (operational knob, default unchanged). Deferred to a v1 protocol rev:
  unary Decide RPC, control-message dispatch split. OWED in the
  boundary ADR: sweep of drill/label rows from affected worker spans
  (drillo1 = 140 events, c2-campaign era) + tonight's poison-event
  count. Issue reply posted with data + credit; PR #8 comment posted
  (no close permission — Talor closes).
- **Rebaseline RESTARTED with the fix (user decision):** free, since
  the crash had killed the handoff anyway and forkcheck's results were
  superseded. Chain v2 = `data/training/d3-boundary-chain.sh`, DETACHED
  (setsid, survives app death): forkcheck-v2 on the fixed jar (meta
  pins `d798917ae5`, clean tree) → final_read `d3-rebaseline`.
  Coverage: watchd d3-forkcheck-v2 (30m, registered IN the chain at
  launch) + final_read self-registration (45m) + completion notify =
  session-independent; session monitor v2 = handoff alarm, console
  stall, live poison-event watch (informational — self-healing under
  the fix). Gotcha found: `run_forkcheck.sh` cds into the forge tree
  before writing run.pid — out dir must be ABSOLUTE. Residual gap
  (accepted, cleanup-pause item): a launch-time final_read crash with
  no live session alerts nobody; fix idea = chain registers a
  d3-handoff watchd entry keyed on the console file.
