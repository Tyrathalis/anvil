# 2026-08-24 — the ceiling measurement (ADR-0072's gating read, same-day)

## Done

- **Reviewed run19 / ADR-0072 and routed the session onto the ceiling
  measurement first** — the cheap read that gates every remaining §3c
  decision, ahead of the closeout and the D6 design session.
- **Designed the instrument as a both-horizon paired replay** — the one
  upgrade over the ADR's sketch: both job sets reuse the revalidation job
  ids and seeds, so `rollSeed = f(seed, job, roll)` matches and each
  (job, arm, roll) is the SAME determinized trajectory truncated at
  horizon 2 vs game end. Exact per-roll proxy→outcome join, plus a free
  in-era re-certification arm. Zero Java delta: `HorizonStop` already
  stops at `t + horizon` OR natural game end, and certify rows already
  carry `winner`/`ended`.
- **Pinned the reads pre-launch and committed them before any result
  existed** (`cec0150`): denominator rule, paired win-diff primary,
  SURVIVES / DOES-NOT-CONVERT branches, the 70% recert drift guard,
  per-shape + Spearman secondaries, the gate-scale arithmetic, and the
  recorded limitation (heuristic continuation — model-squandering not
  measurable here).
- **Built `scripts/payment_ceiling.py`** (plan/read; lanes reused from
  `payment_certify.py`), smoked 3 drills k=1 end-to-end (margins
  reproduced in-era: 14.0 vs 11.5, 7.0 vs 5.9, 27.5 vs 24.2 — and the
  smoke already showed a certified-best arm flipping a game), launched 8
  lanes detached per the checklist (setsid + watchd `ceiling-20260824` +
  persistent monitor + notify + artifact-keyed completion), ~5,750 games
  in ~3.5h, zero draw-clock hits.
- **The read (ADR-0073):** denominator 65/69; pooled Δ = +4.62pp ± 2.15
  (z = +2.15); **recert subset Δ = +12.5pp (z = +3.74) vs non-recert
  −1.7pp**; Spearman(margin, win-diff) +0.465 drills / +0.408 all-arms;
  phyrexian exactly 0.0; gate-scale lower bounds +0.52–0.73pp/game vs
  the ±1.1pp floor. The recert guard FIRED (44.6% < 70%) → session
  adjudication.
- **Adjudication + routing (user):** guard = winner's-curse
  mis-calibration, not era drift (12/65 sign-flips; margin>0 converts
  +6.6pp). Verdict: proxy predictive and converting where it holds,
  aggregate sub-gate — **payment surface = infrastructure; D6 (§3a)
  takes the promotion slot; supervised-conditional-signal = named M10
  candidate contingent on the window-rate bound** (exhaustive sweep ~one
  night, deferred by name). ADR-0073 written; m9-plan, CLAUDE.md status,
  project map swept (incl. marking the watch-list boundary-bundle item
  LANDED at ADR-0068 — it had gone stale).

## Broke / surprised me

- **The recert guard fired for a reason the pin never priced.** I set
  70% expecting to detect era drift; what it detected was selection
  regression — certified margins were chosen on old-jar noise, so
  re-measurement regresses them even under a frozen engine. The Δ split
  (+12.5 recert / −1.7 non-recert) is what rescued the read from its own
  guard. Standing rule born: a re-certification threshold on a selected
  population must price winner's-curse regression, not only drift.
- **The measurement design almost cost a subtlety:** renumbering jobs
  would have silently changed `rollSeed` and broken the cross-horizon
  pairing; reusing the revalidation ids was the fix. Worth remembering —
  the roll seed is derived from (seed, job id, roll), so job identity is
  part of the determinization contract.
- **The positive-but-subthreshold stratum reads −4.9pp (z = −2.7)** —
  small in-era board advantages converting slightly against. Recorded as
  an exploratory anomaly in the ADR, not interpreted.
- Background-Bash waiters cap at 10 min — the completion hook had to be
  a persistent Monitor instead. Worked cleanly (fired on read-landed).

## Next session picks up

- **The D6 design session** (§3a turn-plan latent, the promotion slot).
  Must pin: (a) why the latent escapes the marginal-vs-conditional
  failure mode that took the payment head — it trains on the same
  trajectory returns; (b) a pre-registered early kill signal.
- **Deferred by name there / at the closeout:** the exhaustive
  certification sweep (window-rate bound, ~one night — decides whether
  supervised-conditional-signal stays an M10 candidate); pin-12 forced
  re-mine (re-deferral with recorded reason now the likely routing);
  evalset repair with the ADR-0073 empirical shape verdicts (phy
  value-free at both horizons; wc repair = reachability).
- The M9 closeout ADR still owes: strength verdict (the D6 run),
  payment-completion queue routed by name.
