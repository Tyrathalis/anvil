# 2026-07-02

## Done

- Project initialized from `anvil-design-v2.md` (now at `docs/design/`).
- Git repo created (branch `main`); repo-local git identity set (name "tyrathalis" — placeholder, fix if wrong).
- Decided: private GitHub remote; docs + Python package skeleton now, code at M0; devlog-per-session + ADRs for design-changing decisions.
- Scaffolded: `CLAUDE.md` (invariants, conventions, workflow), `README.md`, `.gitignore`, `pyproject.toml`, `anvil/` package with one subpackage per design-doc section, `docs/devlog/` and `docs/decisions/` with templates.
- Structural decision: Forge fork will be a **separate repo** pinned by commit hash from this one (fork discipline, upstream PR hygiene). Not yet created.
- GitHub remote created and pushed (Tyrathalis/anvil, private).
- **Pre-M0 prior-work survey done** (three parallel research agents) → [../design/prior-work-survey.md](../design/prior-work-survey.md). Headlines:
  - LearnForge post-mortem found in the author's own university report: killed by a Forge memory leak under headless self-play + lossy pooled state encoding + dead NN library + term deadline. His proposed-never-built fixes are basically Anvil's architecture.
  - MageZero is alive (June 2026), collaboration-friendly, ~250 games/hr on 13 threads with 300-sim MCTS, and **heap-bound** (8-thread MCTS halved per-thread sims). Two-process Java-engine + Python-inference-server pattern proven.
  - ygo-agent: zero-IPC C++ envs at ~200 steps/s/core set the throughput ceiling; env-side serialization of multi-part decisions + flat legal-action scoring is the proven alternative to our pointer decoder; card embeddings initialized from LLM text embeddings validates the §1 cold-start idea. Unmaintained since Aug 2024; use the M-YGO-Agent fork.
  - Forge maintainer interest verified verbatim on the wiki AI page, but the stale bot kills issues in ~35 days → design talk goes in PRs/Discord. A Forge contributor calls the `GameCopier`/simulation layer "not very stable" — raises risk on state forking (§9), which is load-bearing for four systems.

- Discussed the survey's strategic implications and recorded the outcome as [ADR-0001](../decisions/ADR-0001-prior-work-responses.md): Forge stands (XMage has the same heap wall + no Android); sequencing stands with soak + fork-fidelity tests added to M0; new gated bet — stable single-step fork API as flagship upstream contribution, pending M0 numbers; bridge protocol gets two day-one invariants (legal-actions-only materialization, one-shot-or-micro-step answering); UPGO noted for M2; JAX and turn-speed reward shaping rejected.

- **Forge forked, built, and smoke-tested.** Fork: `Tyrathalis/forge` (upstream = Card-Forge), cloned bloblessly to `../forge`, pinned at `0bfdaa5` (2026-07-02). User-local Maven 3.9.16 at `~/.local/opt/maven`. Full build green in 4:47 (`-P windows-linux -DskipTests -Dcheckstyle.skip`), all 12 modules incl. Android. Headless sim verified: 3 Commander games (precon vs precon) via `sim -d ... -f Commander -n 3 -q`, **~4.2–4.5 s/game single-threaded** with the heuristic AI — first real throughput data point, comfortably fast vs. the §15 target's implied budget.

- **Soak test built and launched overnight** (ADR-0001 M0 deliverable #1). `scripts/soak/run_soak.sh` (detached sim run, fixed 4g heap, GC logging, 30s RSS+game-count sampler, engine-hash provenance in meta.txt) + `scripts/soak/analyze_soak.py` (games/hour, RSS trend, heap-after-GC trend — the real leak signal). Trial-validated, then launched for real: **6,000 games, pid in `data/soak/run-overnight-20260702/sim.pid`**, ~3.2 s/game at launch.
- **First allocation-pressure data point, and it's spicy:** one Commander game generates **~2.3 GB of allocations** (`2309M->189M` full-GC transitions; ~525 MB/s). Also, `Match.java:103` calls `System.gc()` after every game — ~56 ms forced full-heap pause per game (~1.3% overhead; harmless now, a knob later). The survey's "allocation pressure is the recurring Java-engine killer" prediction showed up on day one.

## Broke / surprised me

- `gh` CLI not installed on this machine — GitHub repo creation deferred (see next steps). `paru` is available for install. *(Resolved later same day: user installed gh + created remote.)*
- Forge sim-mode CLI quirks (worth knowing for the harness): `-d` deck names always resolve against `~/.forge/decks/commander/` (or `.../constructed/`) — the `-D` directory flag only feeds tournament mode, so decks must be staged into the user deck dir. Dev builds hardcode assets dir to `../forge-gui/` (GuiDesktop.getAssetsDir), so run with `forge-gui/` as CWD. `sim` output goes to stdout fine on Linux (the #4419 "hardcoded to appdata" complaint didn't reproduce).
- First run printed ~10 "card not assigned to any set" warnings and read the card DB fast (821 files / 8 parts) — card scripts load lazily; fine for sim mode.

## Next session picks up

- **Morning: read the soak results** — `python3 scripts/soak/analyze_soak.py data/soak/run-overnight-20260702`. Flat heap-after-GC + flat RSS = LearnForge's leak is dead; climbing = it lives and gets an ADR. If still running: analysis works on partial data; stop with `kill $(cat data/soak/run-overnight-20260702/sim.pid)`. Record the verdict + measured games/hour in the devlog.
- **M0 harness spec**, informed by today's smoke test: bridge protocol draft (ADR-0001 invariants: legal-actions-only materialization; one-shot-or-micro-step answering), `PlayerController` override plan, and the two ADR-0001 test deliverables (multi-hour RSS/GC soak; `GameCopier` fork-fidelity differential test).
- Baseline throughput properly: many-game runs (heuristic AI and random-legal), parallel workers, measure games/hour + memory over time — the real M0 number, not today's 3-game anecdote (~4.4 s/game, 1 thread).
- Work the survey's reading list (prior-work-survey.md §6): ygo-agent `ygopro.h` message loop + `handle_multi_select`, MageZero `StateEncoder.java` + `runner.py`, LearnForge's `forge-learnedai` for which decision points it touched.
- Consider early contact: MageZero Discord, Forge Discord (per survey §5.7).
