# 2026-07-16 (session 2) — community archaeology + the public-docs pass

Run-3 trained unattended all session (guards live); this session looked
*outward*: read the Forge Discord's AI channel end-to-end, dove into the
three most relevant external repos, ran two corpus checks the archaeology
suggested, and then turned the lens around — wrote the docs that make our
work legible to the people we just surveyed.

## Done

- **Read `#ai-plotting` in full** (Forge Discord, Aug 2025 → today, ~965
  messages, read-only) → [discord-ai-plotting-survey.md](../design/discord-ai-plotting-survey.md).
  Headlines: three independent BC→RL pipelines appeared Mar–Jun 2026
  (Austinio — a Forge core dev, Kryptic, talor), all stuck at 25–35% vs the
  heuristic on walls we have ADRs for (fallback poisoning, split leakage,
  eval CI, imitation ceilings); GameCopier is used for rollouts by people
  who don't know it was broken pre-#11203; nobody has heard of Anvil; the
  monthly check-in posted today is unanswered — our natural opening.
- **Three repo dives** (subagents; reports as `survey-dive-*.md` in
  docs/design): Austinio's `ai_investigation` (his "54% parity" was a
  silent-fallback artifact; PPO honestly failed at all scales tried; his
  aura bug was in *his* encoder, not Forge; MCTS = one-ply UCB1 over unfixed
  GameCopier with no fidelity discipline), manabrew (~40-line Forge patch;
  lockstep parity harness; **found the shared pre-shuffle CardPool
  hash-order determinism hole — verified present in our fork**, queued as a
  dataset-boundary item + joint upstream determinism-hooks PR in
  [upstream-worklist.md](../design/upstream-worklist.md); they're a live
  GameSnapshot-restore consumer = consolidation-PR evidence), and
  price-predictor/MageZero (a working Tutor prototype with per-card
  counter labels + an auditor-scorer reward-hacking canary; MageZero trains
  with `see_opponent_hand: true`).
- **Corpus check 1 — aura/Attach labels: CLEAN.** Full-corpus sweep
  (113,592 games / 6.11M casts, cross-checks the D2 sweep): all 49 pool
  auras carry attach targets in `tgt`; the only targetless residue is
  Demonic Ruckus's Plot mode (correctly targetless). Record:
  `data/runs/never-cast-audit-20260716.json`.
- **Corpus check 2 — never-cast pool audit:** 138/1,701 pool cards have
  zero cast labels: 95 `AI:RemoveDeck:All`, 9 weaker-flagged, **34
  unflagged heuristic blind spots** (Intuition: 2.39M candidate windows,
  0 casts; 6 auras the AttachAi never fires; **Rick, Steadfast Leader — a
  commander never cast in 16K windows**), 6 split/room-card name
  artifacts. These are BC-blind but realizer-reachable → RL-exploration /
  Grindstone material; survey §4b has the decomposition.
- **Public-docs pass:** wrote
  [forge-ai-field-guide.md](../forge-ai-field-guide.md) (every measured
  trap, organized trap→bite→detection→fix, pinned to ADRs — written for
  the #ai-plotting audience); README gained a "Building AI on Forge?"
  section + Status refresh (#11203 = merged); project map reworked —
  flow-diagram overruns fixed, **self-play-loop row added**, checkpoint box
  un-staled (BC d5-combat · RL d6-run1), "start here" reading path,
  **milestone log made hierarchical** (M2 = six one-line nested `<details>`,
  D6 open by default), map-maintenance checklist added to CLAUDE.md.

## Broke / surprised me

- **VRAM exhaustion breaks desktop screenshots**: with run-3 + ComfyUI at
  23.8/24.5 GB, `xdg-desktop-portal-kde` SIGABRTs on EGL_BAD_ALLOC when a
  screencast session starts. Workaround: runtime-only systemd override
  forcing the portal to software rendering (evaporates on reboot).
- Austinio's dive is a catalog of our own bets validated from the other
  side: fallback provenance, arms-over-agreement, no-naive-PPO, and his
  self-caught entropy overdose ("the model doesn't need to randomly bounce
  its own creatures to 'explore'") is ADR-0017 in miniature. Also useful:
  per-head entropy floors (filed as the ADR-0017 contingency), the
  encoder-reconstruction R² probe, his veto-reason taxonomy.
- The community's "2–3K cards Forge's AI never plays" folklore is
  `AI:RemoveDeck:All` (2,534 cards) — but the flag gates deck inclusion,
  not casting: 30 flagged pool cards *do* get cast, and the real blind-spot
  list is the 34 unflagged ones.
- manabrew's Fireball "SVar misname" bug claim didn't survive verification
  as stated — filed as verify-with-a-sim-test before any upstream PR.

## Next session picks up

- **Post the check-in reply in #ai-plotting** (drafted this session):
  intro + field guide + #11203 rebase heads-up for the rollout users +
  determinism-PR offer to manabrew.
- Babysit run-3 (guards should halt on their own; glance at monitor.jsonl
  anyway per the babysitting rule); done-when #4 read at 2,000 games
  corrected when a run survives its guards.
- At the #11161 rebase: fold in the pre-shuffle sort + draft the joint
  determinism-hooks PR (worklist has the full spec).
- Flex: Fireball/Officious Interrogation sim-test repro; per-head entropy
  floors if run-3's global hinge proves too blunt.
