# 2026-09-15 (session 2, night) — the partial-expansion slot (ADR-0106 C3): the price, the shape, the code

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **State review before code** (the user's ask): Build 3 closed with evening 5; the fork at pin
  `e63a0cac20`; the tree clean at `3c3d691`; box idle; C1's salted verdict on file (next the leaf,
  h2 a hint, the deep arm provisional at top-3 × h2 × rolls ≥ 4). Five decisions put; the user took
  the recommendations.
- **The price (the finding that shaped the slot)**: from the 09-14/15 calibration arms an h2 copy
  is ≈ 10× a next copy in wall (2,877 vs 304 ms mean per valued copy; p90 5,235 vs 507) and, on
  the network arm's smoke, ≈ 13× in forward calls (≈ 65 vs ≈ 5). ADR-0106 C2 had priced the deep
  arm at ≈ 2.3× from an assumed 3× per copy; at top-3 × rolls 4 on every searched window the deep
  arm would be ≈ 8–10× the recipe per window (≈ 20 min of copies per game against the 15-min
  clock) → ⅛ the games at equal box time → unreadable. Standing rule: measure a shape's per-copy
  price before sizing its arm.
- **The gate**: not a top-two band (85% of heuristic windows have their top two within 0.10) but
  the natural margin — below 0.01 on 77% of windows; the band [0.02, 0.10) is 11% (network smoke:
  22% incl. the floor). So: the deep round runs where max V − V(natural) ∈ [lo, bar), plus a
  seeded floor on the rest (fork L's labels need every shape on ungated windows).
- **The fork code** (fork `5e333e96930`): `SearchDirective.Pending` gains
  `DeepRound` (the runner interface) + the deep parameters; `decide` runs the deep stage between
  the answer stage and the option stage — the gate (`single` / `gate` / `band` / `floor`), the deep
  set = the top-B by lifted value + the natural when outside, the runner called with each
  candidate's sampled answer index (the deep copies play the (option, lifted answer) pair), the
  option stage on the deep values over the set only under the deep bar; `deep.by nat_unvalued`
  falls back to the shallow rule; `Decision` carries `shallowMargin` / `shallowArg` / `deepSet` /
  `deepV` / `deepCopies`; the row's `deep` block. `AnvilRun`: `-searchdeep B -searchdeepleaf
  -searchdeeprolls -searchdeeplo -searchdeepfloor -searchdeepbar` (needs `-searchact`), the header
  pins, `SearchMonitor.deepRound` (copies on `rollSeedOf(turn, sw, r)` — CRN across the set, the
  first `rolls` shared with the first ply; the RNG snapshot / restore; the mainline bridge session
  re-announced after). The controller's census row names `d.by` (`deep` where it acted). Tests:
  SearchActTest 24 (10 new: off / band / gate / the seeded floor at 10% / single / the natural
  outside the top-B / the unvalued deep natural / the deep bar / the lifted answer reaching the
  runner / the row). Two test values, not code, failed first: a margin on a binary
  floating-point edge (0.70 − 0.60 < 0.10) and a negative expected margin where the natural is
  the set's max.
- **Scripts**: `build3_act_smoke.sh` gains `DEEP` / `DEEPLEAF` / `DEEPROLLS` / `DEEPLO` /
  `DEEPFLOOR` / `DEEPBAR` / `CLOCK` and absolutizes `OUT` (the first deep smoke launch passed a
  relative dir and wrote nothing — the java launch cds into forge-gui/); `deep_smoke_read.py`
  (gate census, deep-vs-shallow flips, the deep share of forward calls, wall per deep round);
  `build3_deep_queue.sh` (the forkcheck → the two 24 × 2 bench cells, recipe vs recipe + deep →
  `deep_bench_read.py`).
- **The smoke** (`data/runs/build3-deep-smoke/`, 4 self-play games, the recipe + `-searchdeep 3`,
  h2, rolls 4, lo 0.02, floor 0.1, clock 3,600, jar snapshot of the tip): 4/4 won, 399 searched windows, wall 2,648 s (p50 460 s per game vs ≈ 150 s on the recipe smoke; max 1,442 s), 0 copy crashes (the one logged exception = the playable-branch prefs leak at FModel startup). Gate census: gate 172 / single 134 / band 82 / floor 11 → the deep round ran on 93 windows (23%), set size 3.0, 18.8 s p50 / 37 s p90 / 67 s max per round; deep copy kinds leaf 871 / end 253 / void 8; forward calls per game 22,265 deep vs 4,380 first ply + surfaces (deep share 84% → the deep arm ≈ 6× the recipe in forward calls, ≈ 3× in one-worker wall); the deep argmax flips the first ply's on 55/93 rounds yet clears the bar on 9 (the mainline acted on all 9: census `by: deep` 9) — at h2's noise the deep round mostly confirms the natural; elsewhere the shallow rule acted 60/306. The mechanism is clean; the price is the read.
- Docs: ADR-0106 C3 addendum (the price, the seven pins, the routing), the standing rule, the plan
  record, CLAUDE.md Now.

## Broke / surprised me

- The deep arm's assumed 2.3× was off by a factor of ≈ 4: nobody had priced an h2 copy against a
  next copy on the same slot. The calibration arms had the number all along.
- A relative `OUT` to the smoke script is silently fatal (the launcher recorded rc 0: the script's
  last line is an echo). Fixed in the script; the launcher's rc follows the last command by
  design — a `set -e`-free chain script must `exit 1` itself.

## Next session picks up

- **Forkcheck PASS 22:15** (499/500, 20260969 the standing crash only) → **fork pin `5e333e96930`**.
- The queue's bench pair's read
  (`data/runs/build3-deep/read.md`) → the deep arm's multiplier for the shakedown's equal-box-time
  split (one cell per arm = a draw; the forward-call share is the per-lever number).
- Then: Build 3's closeout ADR, the rebase (ADR-0106 B), Build 4.
