# 2026-07-28 — session 3 (M4 design session + d6-run9 launch)

## Done

- **M4 designed and opened** — the dedicated Grindstone/M4 design session
  ADR-0026 queued, run on fresh context per the M1/M2/M3 pattern.
  - [ADR-0027](../decisions/ADR-0027-m4-opening-sequence.md): opening
    sequence + the user decisions (identity: split, Grindstone spine;
    adaptation probe approved for tonight; signal source (a) drill-mixed
    generation first, (b)/(c) as escalation; LLM filter + ddmin out of v0;
    serving-path throughput PROMOTED to a deliverable — user wants
    efficiency work mixed in; Track C stays deferred).
  - [m4-plan.md](../design/m4-plan.md): D1 adaptation probe → D2
    Grindstone v0 → D3 signal-integration runs → D4 serving throughput;
    riders (pool-manifest mtime fix, Ante items with critic retrains,
    upstream watches); done-when ×4.
  - CLAUDE.md status + project-map.html updated in the same batch (M4
    entry, Grindstone card → spine, ADR index, footer).
- **Three design findings worth the record:**
  1. The 531-loss curation list is old-scale *structurally*, not just
     numerically — drill positions reconstruct by seed-replay-to-window,
     and old seeds don't replay on the rebased engine. Curation must
     regenerate from new-engine games; the old list's value is method
     validation and tooling (`early_doom.py` reruns cheaply).
  2. Grindstone v0 ≈ re-aiming the parked D4 rollout apparatus
     (`-rollout K -points M`) from sampled quiescent windows to curated
     crash windows — most of the machinery already exists and its
     17 positions/h price is batch-1-era (needs re-price under
     micro-batching).
  3. K rollouts from one forked position collapse advantage variance
     exactly at the near-tie windows ADR-0024 diagnosed as signal-free —
     drills are the mechanical answer to the absent-signal verdict.
- **The D1 probe justification, stated honestly:** no training signal has
  ever touched the rebased engine (all late-M3 runs generated pre-rebase;
  post-rebase work was static reads). The −4pp mechanisms are
  indistinguishable by reads (DiD underpowered) but diverge under
  training: opponent-fit recovers in a few iterations, a stronger
  heuristic doesn't. User signed off on the overnight run.
- **`d6-run9` launched** (adaptation probe): run-7b config verbatim, init
  `run7b/iter-014/{train,critic}`, rebased fork `master` @ `5fbc2ac98d`
  (jar built 07-27), 10 iterations, fresh seed base 20260830, guards
  standing, detached launch per convention, watcher armed on the run
  artifact. Pre-registered interpretation in ADR-0027.

## Broke / surprised me

- Nothing broke. Pre-launch substrate check was clean on the first look
  (fork on the rebased tip, jar current, GPU idle at 1.2/24.5 GB).

## Next session picks up

- **Morning read of `d6-run9`**: monitor.jsonl health (KL/entropy/veto vs
  guards), arms at i4/i9; decide extension vs closing read. Then the
  2,000-game paired read on the best ckpt → the re-baseline mechanism ADR
  and the M4 baseline number.
- After the read: D2.1 curation regeneration on the closing-read stores
  (probe critic for traces); D2.2 drill manifest + harness verb.
- D4 can start any time the GPU is free: noise floor on
  `bench_generation.py` first.
