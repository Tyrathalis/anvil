# 2026-08-16 — run16 veto guard halt → gate sweep (both TIE) → M7 CLOSED (ADR-0058)

## Done

- **d6-run16 GUARD HALT iteration 16** (veto 0.361 > 1.5×0.2024): the
  aggression trade's cost curve completing — veto 0.245→0.302→0.361 over
  the last three iterations, rej 7.2→9.5, hold-then-cast down to 0.182,
  and iteration 16's RL phase showing the degraded store feeding back
  (kl 0.058, share 0.253). Guard caught it exactly; iter-015 = last
  accepted (veto-damaged). Clean SIGTERM-free exit (driver self-halted,
  self-unregistered). 16 accepted iterations banked at honest pricing.
- **Two-candidate gate sweep (user-approved; M6 two-leading precedent,
  arms-selected disclosed):** `scripts/run16_gate_sweep.sh` — sequential
  final_reads on iter-014 then iter-009. **Both TIE: iter-014 0.5340 ±
  0.0110, iter-009 0.5364 ± 0.0110 vs the 0.5373 ± 0.0112 gate.** No
  promotion; ckpt of record stays `d6-run11/iter-019`. iter-009's read
  impeccable (veto 0.189, below era baseline) — the tie is not veto
  damage; the trade was genuinely strength-neutral at its best point.
- **Methodological catch:** the mid-run arms elevation (0.575/0.570,
  pooled t≈2) was one ~1.5σ fluctuation counted twice — both arms evals
  run the SAME fixed 200-game seed subset. Standing rule (ADR-0058):
  fixed-subset arms reads are ONE observation, not N.
- **M7 CLOSED ([ADR-0058](../decisions/ADR-0058-m7-closeout.md)):** the
  credit-assignment verdict, split: dense per-decision signal is
  TRAINABLE and BEHAVIOR-MOVING (the machinery works, guarded end to
  end); the act−hold formulation is STRENGTH-NEUTRAL — it taught greedy
  timing, which ADR-0053 had already measured below natural. Chartered
  follow-up (new design round): the natural-timing-target formulation.
  Dose escalation of act−hold explicitly not recommended.
- Docs swept: CLAUDE.md M7 bullet archived verbatim → status-archive,
  compact CLOSED summary in place; map (Now panel, M7 details closed,
  run16 ledger row, ADR index 0056/0057/0058, header pointer, footer).
- **Milestone-close stale-data pass:** kill-list inventoried and
  presented (user sign-off pending; see session log).

## Next session picks up

1. Stale-data deletion on user sign-off of the kill list.
2. Next milestone scoping: the natural-timing-target formulation design
   round is the chartered candidate (price the natural arm's timing
   distribution; ADR-0053's measured optimum). Also on file: parked
   throughput items (2-arm campaign trim at next boundary, w-scaling
   retest, campaign fleet handoff), Trackio (watch list), battery
   `--compare` overlay (convenience list).
3. Playable-fork and Chronicle tracks unchanged (see their bullets).
