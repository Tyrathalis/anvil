# 2026-08-12 session 4 — curation regeneration (the C-bundle run-up, stage 1)

## Done

- **Fresh curation stock DECIDED = the d3-rebaseline stores themselves**
  (1,999 argmax games vs the current heuristic, era `d798917ae5`, one day
  old, critic-untainted): they ARE this era's fresh stock, zero extra
  generation — and argmax sources mean the drill mainline replay
  reproduces the curated states exactly (no ADR-0052 sampled-mainline
  requirement; the original protocol applies).
- **Trace + analyze (`early-doom-d3rb-rankcrit`):** rank-critic-c2v3 with
  the era-scoped `c2/v_rank` isotonic map, matching cycle 3's protocol.
  **418 addressable losses / 45.2%** of 925 — the substrate replicates
  cycle 3's 45.4% almost exactly across the boundary.
- **Calibration drift FLAGGED for the recipe pin:** the reliability table
  shows the c2-era isotonic map under-predicting by 15–20pp in the mid
  bins on the new era (v≈0.43 bins winning 63%) — ADR-0036's era-scoping
  doing its job. Ranking AUC healthy (0.75→0.92 by turn) so candidate
  ORDERING stands, and the K-rollout map is the authoritative pricing
  regardless (standing rule). Owed: a fresh era-scoped remap from this
  cycle's map labels (`scripts/critic_calibration.py` — the label set
  grows with every map).
- **Migration read (standing gate) RECORDED:** distribution verdict =
  **profile stable across the boundary** (addressable 45.4%→45.2%;
  luck-locked sweep near-identical; ≥30pp collapses 62→67, deck jaccard
  0.368; crash-turn/peak-v quartiles unmoved) — expected: no training
  intervened, this re-baselines the substrate rather than testing
  compounding. The seed-matched half is structurally empty (cycle 3's
  stock used fresh seeds 20260809; the rebaseline rides the standing
  final_read seeds) — `migration_read.py` gained a disjoint-seed guard
  instead of the ZeroDivisionError it threw.
- **Three-anchor K=16 map chain LAUNCHED** (new reusable
  `scripts/run_anchor_maps.sh`: plan+generate+report at crash−2 /
  crash−4 / peak, resume-safe via REPORT_DONE markers): 418 points × 3
  anchors × K=16 ≈ 20K completions ≈ 6h. Manifest era pin verified
  (`d798917ae5`). Argmax mainline (argmax sources). **Armed at launch,
  one action:** setsid/nohup/nice detach → watchd `d3rb-maps` (pid
  1541249, 75-min stall) → artifact-keyed waiter (DONE + /proc exit
  check) → chain-end notify. First arm confirmed producing output before
  the session moved on.

## Broke / surprised me

- `migration_read.py` ZeroDivisionError on disjoint seed sets — first
  cycle comparison across a fresh-stock/standing-seed boundary; guarded
  with an honest "seed sets are disjoint" report.

## Next session picks up

1. **Map chain results** (notify + waiter fire): three-anchor reports →
   band mass per anchor, pick the selection anchor (cycle 3: crash−2 at
   52% band mass).
2. **Selection + evalset build** sized for the campaign (P≈100 slice;
   composition per the recipe pin) + **the recipe pin with the user**
   (mixture, λ=0.01 first-attempt-only, seq-n 4 / seq-k 16,
   drill_points_per_iter, the calibration-drift disposition, evalset
   era question).
3. Smoke iteration → the run vs the 0.5373 gate.
