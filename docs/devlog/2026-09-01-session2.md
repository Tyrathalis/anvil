# 2026-09-01 — session 2 (Discord follow-up: lazy-loading PR opened)

Continuation of the 2026-08-31 session-2 upstream review; read-only again (gh + git log only).

## Done

- **Manabrew lazy loading is now upstream PR #11763** ("consistent lazy loading", JacopoMadaluni = Anacleto, +420/−113, same 12-file shape as the fork commit reviewed yesterday). Read the diff:
  - Design resolved the filename-heuristic problem (their "Vhal, Scholar of Prophecy" → `vhal_eager_scholar.txt` case) by **streaming the carddb once at startup to build a name→script lookup index and discarding the scripts** — lookups become identical to eager mode. Their stats: +2.5 s startup, **60 MB resident vs 450+ MB** for the full 34k carddb.
  - The forge-game touches are all one-line `StaticData.instance().ensureAllCardsLoaded()` guards before full-db enumerations (ChooseCardName random, CopyPermanent `ValidSupportedCopy`, MakeCard `Booster`, PlayEffect sorcery/instant, PlayLandVariant, `GameFormat.getAllCards`) — i.e. lazy mode degrades to a full load when an effect needs the whole db, so enumeration results match eager mode. **`ensureAllCardsLoaded()` early-returns when lazy is off** — the game path is behavior-neutral in eager mode, so if this merges it's a strong ADR-0025 exemption candidate (proof stays empirical: forkcheck at next boundary, same as anything).
  - **Fleet-relevance note (future lever, not now):** our harness is the ideal lazy consumer — the pool is 1,701 cards and deck contents are known before launch, so preloading the pool and never touching the other ~32k is exactly Jetz's test-suite usage. ~400 MB/JVM off-heap-baseline × 8–13 lanes is real headroom, though the current 4g heap pressure is AiCache mainline accumulation, not carddb. Revisit only at a boundary event that includes #11763.
  - Manabrew also holds unshared patches for **"server hosting multiple games"** per JVM (Anacleto unsure about upstreaming). Multi-game-per-JVM + thread-safe carddb would change lane-density economics; worth watching if it ever surfaces.
- Other thread notes: TRT is open to adapting forge-wasm fork fixes upstream (the wasm module itself stays external). Fuzz on Nerdslayer's proposal: "evaluation hints sounds like data we can collect" — if upstream grows a per-card AI evaluation-hint channel, that's a contribution-shaped slot where Anvil's learned eval could someday *generate* hints (fits TRT's small-contributions posture).

## Broke / surprised me

- Nothing (read-only). The lookup-index-then-discard design landing within hours of the "no heuristic can find these" complaint was a nice live design-loop to watch.

## Next session picks up

- Nothing scheduled. Boundary-checklist addition: if #11763 is merged by the next rebase, forkcheck covers it (expected clean in eager mode); the lazy-mode lane-density experiment is a named follow-up gated on that merge, not before.
