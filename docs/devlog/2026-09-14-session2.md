# 2026-09-14 (session 2) — the model-server fleet

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **Review + decisions (user):** the throughput week's first item, model-server scaling. Six
  decisions put and taken: the server module owns the fleet (one edit site, the counts merge in one
  place) rather than a loop in each launcher; static N = ceil(workers / 8) now, the full autoscale
  later, but the crude form — yield to a foreign GPU job — early (user); round-robin by invocation
  index; the batcher knobs exposed; the bridge-deadline raise folded in; the bench grid.
- **`anvil/bridge/fleet.py`** — `supervise` (N children, consecutive ports, SIGTERM fan-out, an
  unexpected child exit stops the fleet), `child_argv` (`.sN` output parts), `merge_outputs`
  (jsonl appended, counts summed, `servers: N` stamped), `servers_for`, `bridge_addrs`,
  `wait_ports`. `anvil.bridge.server --servers N` dispatches to it before loading anything.
- **The batcher** — `--max-batch` / `--window-ms` / `--stats-every`; queue wait per item, items per
  batch, queue depth, forward seconds → the periodic `[server] stats` line.
- **The harness** — `--bridge a,b,c` round-robin by `inv` (`bridge_for`); the 20 s deadline on
  every grpc run unless pinned; **the GPU yield** (`harness/gpu_yield.py`: `nvidia-smi pmon`
  parsed by header, foreign = outside our session AND (fb ≥ 1 GiB or SM ≥ 20%), 60 s on / 120 s
  off, sampled every 30 s; the `YIELD` file; `gpu-yield.json`; `status` prints it; manifest
  `yield_gpu`; `--no-yield-gpu` / `ANVIL_NO_GPU_YIELD`).
- **Launchers** — `_start_server(servers=)` + `fleet_size` / `fleet_bridge` in selfplay;
  `--servers` (0 = auto) on selfplay, final_read, grindstone generate/eval; `SERVERS` on
  `build3_surface_labels.sh` (bridge list built in shell); `build3_surface_read.sh` passes it;
  the campaign fleet's default port moves from port+2 to port+8.
- **Smoke** `fleet-smoke-20260914-112514`: 2 servers, 16 search workers, 32 games, 32/32; the
  chunks alternated ports; each server ≈ 105–135 rps, mean batch 1.9–2.5, wait p90 ≈ 16 ms,
  ≈ 80% forward-busy; the merged counts carry `servers: 2`.
- Tests: `test_fleet` (6), `test_gpu_yield` (4), `test_harness_bridge_list` (3); suite 309.
- **The bench launched 11:35** — `scripts/fleet_bench.py --name fleet-bench-20260914` (detached,
  nice, watchd `fleet-bench-20260914` stall 90 min, notifies on DONE/FAILED); cells 8:1 / 16:1 /
  16:2 / 24:3 / 32:4, 64 pool self-play games each, `-search -searchrate 1 -searchrolls 2`; rows
  append to `data/runs/fleet-bench-20260914/bench.md`; log
  `data/runs/fleet-bench-logs/fleet-bench-20260914.out`. ETA ≈ 2 h.

- **The bench READ (13:51):** steady-state g/h (the harness's peak cumulative rate; the wall
  number is one 1,200 s draw in every cell's tail) 8:1 316 / 16:1 381 / 16:2 433 / **24:3 468** /
  32:4 323. 24 workers × 3 servers is the recipe; 32 is the core ceiling (games 2.5× slower);
  the GPU never binds (forward 12 → 25 ms/batch at 4 servers, queue ≤ 3, busy ≈ 62%, mean
  batch ≈ 2 — starved). Hazard: asks/game −15% at 32 workers on the same seeds — `-searchclock`
  is wall time, so load changes the policy → the standing rule (load is part of the recipe).
  The Build 5 sizing line re-issued: ≈ 470 g/h ≈ 11K games/day at rolls 2; 300–450K games in
  the envelope. The plan record has the full read.

- **The 24-worker servers curve (15:47, user's question):** 24:2 489 / 24:3 468 / 24:4 418 peak g/h.
  A server at 12 workers is not binding; a fourth server costs 11% (GPU time-sharing, forward +35%).
  Pin → ceil(workers / 12); **the recipe = 24 workers × 2 servers (≈ 490 g/h)**. The 13:51 clock
  hazard was wrong: 0 copy timeouts in every cell; the asks/game spread is game divergence from
  micro-batch numerics on the same seeds. Standing rule + CLAUDE.md corrected.

- **JFR (the user agreed the two-recording plan):** the pipeline smoke (2 games), the idle recording (1
  worker, 16 games, 77K samples), the in-fleet recording (24:2, JFR on every worker; died at 16/64 with
  the OOM but 22 × 284 s = 248K samples). `scripts/jfr_hot.py` (streaming). The read: the payability
  mask 24% (mana sources regrouped per candidate), replacement scans 23–27%, static checks 12–24%
  (a third via `getAlternativeCosts`), GUI views 18–20% (headless!), the game copy 17–22% (cards rebuilt
  from scripts), GC ≤ 1.4%; the same shape idle vs fleet. Levers ranked in the plan record: the mask memo /
  prefilter, a headless-views flag, PR 11916 at the rebase; the copier + replacement scan upstream-scale.
- The chunk rule: four rounds of refill per worker — the harness's own `--chunk` default is now
  ceil(games / 4·workers) (`default_chunk`; was 200), grindstone's 0 = that default, selfplay's arms
  and the label chain dropped their fixed 50 (1,000 games on 16 workers was 1.25 rounds), final_read /
  fleet_bench / `batch_chunk` at four rounds. A single 1,200 s draw-clock game still ends a run
  alone; only a shorter allowance would shorten that, and it would clip games (not taken).
- PR 11916 routed to the next rebase (user); the GPU yield exempts Anvil servers by cmdline.

- **The mana-source memo** (fork `1dd36f7342`, user go): built once per scan in `ComputerUtilMana`,
  armed by `buildPriorityOptions`; the identity gate 32/32 games / 32,306 windows identical at one
  worker; forkcheck 499/500 (the standing crash) → PASS, fork pin `1dd36f7342`. The re-profile + the
  24:2 bench cell on the memo jar launched 19:30 (`data/runs/jfr-20260914-memo/`).

## Broke / surprised me

- The first smoke launch died on `-search requires -labels` (my launch, not the fleet); the second
  ran clean. The yield gate was only evaluated while chunks were pending, so a YIELD touched after
  every chunk was out logged nothing — now evaluated once per tick.
- zsh has no `/dev/tcp`; the shell chains are bash and unaffected.
- The single-server smoke of record shows the GPU starved, not saturated: mean micro-batch 2–3
  even at 80% forward-busy per server — the second process is a near-linear win until the card
  fills.

- I attributed the asks/game drop to `-searchclock` before checking the copy kinds; the labels
  carried the answer (kind = timeout never fired). Attribute from the recorded rows first.

- **OOM 17:58:** `jfr print --json` on a 77K-sample recording, twice in one command, next to 24 recording
  JVMs → the kernel killed the python (4 GB RSS) and Claude Desktop with it. The memory note's rule
  applies to every `--json` dump of a recording: stream, cap. `jfr_hot.py` rewritten to stream.
- The GPU yield fired on my own model server (started from a different shell session) — fixed by
  exempting Anvil servers by cmdline.

- The first memo gate (8 workers) diverged 13/120 games on identical masks — bf16 micro-batch
  composition flipping near-tie argmax picks, not the memo; one worker replays them identical. The
  08-11 gate instrument now runs one worker (standing rule). `obs_diff.py` needed the sv=3 fix (option
  strings) — it had not been run since Build 0.

## Next session picks up

1. Read the memo's re-profile + bench cell (`data/runs/jfr-20260914-memo/idle-read.md`,
   `data/runs/fleet-bench-20260914-memo/bench.md` vs 489 g/h); update the state-of-record fork pin
   (`1dd36f7342`) in CLAUDE.md's table + the map.
2. **The headless-views flag** (fork, engine): no `TrackableObject` updates without a GUI; forkcheck
   decides whether it is exempt (the copier's `canBeShownTo` reads views).
3. Optional: `jfr_hot.py`'s text-mode exception parser (cosmetic; the exceptions were a false lead).
2. The JFR profile of one worker per regime (the AI eval-thread timeout class, the straggler tail
   → work-stealing chunks at the tail).
3. Evening 5 = mainline surface acting (modes first) — a design session before code.
4. Routed: the occupancy autoscale rule (add a server + rebalance at a chunk boundary when the
   queue wait exceeds the forward latency — the stats line now carries both), the pay head's PG
   in the loop (Build 4½), the certifier merge, the puzzle battery.
