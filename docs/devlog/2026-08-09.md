# 2026-08-09 (cycle 3 resolved: TIE — the layer question; audit chartered)

## Done

- **d6-run13 complete + closed
  ([ADR-0048](../decisions/ADR-0048-cycle3-resolution.md)):** 20/20
  iterations, zero guard halts, arm trend 0.478→0.503→0.498→0.510.
  **Combined paired ante-corrected 0.5199 ± 0.0110 vs 0.5316 ⇒
  Δ −1.2pp ± 1.6 (t≈−0.75) TIE — no promotion**; iter-019 stays ckpt
  of record. (Mid-read note: two per-seat-perspective [ante] certify
  lines briefly misread as a −6pp regression — the combined number is
  the only real one; correction issued in-session.)
- **Decomposition (evalset-v3):** winnable −2.4pp (the D3 residual did
  NOT close), **coin −9.1pp (~3.5σ)** — the curriculum's most-trained
  bin got worse at held-out instances; lost +5.6pp. Conversion trade
  inverted. Veto elevated at close (0.215 vs ~0.10 historical).
- Launch crash diagnosed + fixed en route: fork-store index encoding
  (`source_g*10000+k`) collides with mainline indices exactly when a
  drilled source game has g=0; single row dropped, proper fix filed
  as a next-era chip.

## The read

Two weeks of instrument-grade improvements (critic ranking 0.27→0.48,
ECE→0.004, curation honesty, better anchors) and strength did not move.
The coin-bin regression proves the policy changes under training — in
the wrong direction on the trained material. With the static-critic
axis measured out (ADRs 0043/45/46/47), suspicion moves one layer up:
exploration / objective / credit assignment. The user's exploitability
argument (the heuristic spends interaction on sight; baiting is
learnable in principle) says the ceiling vs this opponent is NOT the
binding constraint until proven otherwise.

## Next session picks up — THE AUDIT (user-approved, before any spend)

1. Behavioral delta run13-final vs iter-019 (KL, agreement, where they
   diverge — the coin regression says somewhere).
2. Interaction-holding corpus probe: does hold-castable-interaction
   behavior occur in self-play at all, and did its frequency move
   across run13's iterations? (never ⇒ exploration; unreinforced ⇒
   credit ⇒ rollout-value targets / tier-3; grown ⇒ recipe/eval.)
3. Veto/§6c-penalty telemetry pass (λ=0.02 at veto 0.2 = a standing
   negative term on attempted casts).
