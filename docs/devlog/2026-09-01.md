# 2026-09-01 (through 09-02 ~05:00)

## Done

- **The fixed-jar phased mint ran end-to-end and READ CLEAN: 543
  witnessed labels, probe3 launch-ready.** Final numbers:
  - **Parity witness: 79.0% valid-turn survival** (2,690/3,406; 288
    games byte-exact incl. s, 153 sparse-flipped, 0 truncated) — the
    ADR-0089 fixes verified at fleet scale, the 50% systematic floor
    never approached; the sparse concurrency flips priced exactly as
    the addendum projected.
  - **finish: 653 certified positives, 110 dropped by the witness →
    543/543 labels minted** (zero drops at the label stage).
    Certification rate 19.2% on probe-state windows vs the census-era
    28% prior — the probe distribution certifies less (recorded; the
    prior was census-population-specific).
  - **543 < the 750 bar → the pre-adjudicated `--lab-k 4` path**
    (~1.3 visits/window/iteration, inside the ≤2 rule). No
    re-adjudication.
  - **Seedlab day-zero re-banked**: CE **2.676759** on 543/543 (zero
    misses/unmatched) at the init ckpt →
    `seedlab-dayzero-mint20260830.json`; the FUND decode leg reads
    **≤ 2.141**. Cross-validates the retired 170-batch's 2.730 —
    two independent mints, same pipeline, ~2% apart.
- **Phase operations en route** (each per the standing playbooks):
  phase-1 lane-1 whisker-miss accepted; lane-9 OOM'd at 3g (the
  bridged-JVM AiCache mainline accumulation over ~20 sequential
  replays — heap bumped to 4g fleet-wide + the sched_mint default),
  rerun, then cut at the boundary still grinding a degenerate
  artifact-loop board (partial accepted, witness-priced); a stale
  pre-fix driver chain (10 lane shells parked on the removed sentinel
  — a landmine) found and killed; the driver SIGSTOPped across the
  lane-9 rerun so phase 2 could not overlap it (cross-store carry),
  auto-resumed at 12/12; phase-2 boundary server swap worked as
  designed; phase-2 lane-6 WEDGED 7h inside one roll of g171 (census
  raw-cap monster board, AI loop unkillable by the soft game-over
  clock — the crash-class family gains a specimen) — cut at 77% of
  its share.
- Probe3 launch script updated to the realized state (k=4, satisfied
  prerequisites recorded in the header, 79.0%/543/2.6768 pinned).
- **Clean shutdown for the user's kernel reboot**: server stopped,
  watchd unregistered (no reboot-alert noise), wrappers/watchers
  swept; the box carries zero mint processes.

## Broke / surprised me

- The certification-rate prior didn't transfer: 28% (census states,
  iter-019 era) → 19.2% (probe states, init ckpt). Not alarming —
  different population, different serving policy — but the yield
  math for future mints should use the 19% figure for init-era
  probe-state substrates.
- g171 (probe2 store) is a first-class specimen for the watch-list
  crash family: one game producing 16MB of census rows and a roll
  the watchdog clock cannot kill (the loop never re-checks game-over).
  Worth attaching to the GameCopier/crash-class hunt when it opens.

## Next session picks up

1. **Launch `m10-probe3`** (post-reboot): `setsid nohup
   scripts/launch_m10_probe3.sh` per the detached checklist — watchd
   registration, log monitor (GUARD HALT / KILL / ANOMAL / KL ABORT /
   Traceback + the FRAME-ANOMALY filter), telemetry glances.
   Iteration-0 first looks: `labs_early.json` (ramp+subsample impulse
   check), `seedlab_calibration.json` raw ≈ the 2.6768 day-zero, KL
   vs probe1's 0.034 mean, `sched_live_ce` ≈ day-zero, and the
   memorize guard's first-window read.
2. Smoke-axes declaration (best-practices item f) applies to this
   launch: the probe recipe's new surface (k=4 subsampling +
   carry-w + warmup) was NOT smoke-tested in loop context — the
   iteration-0 telemetry glance carries that burden; say so in the
   launch message.
3. Stale-data pass candidates when M10 closes: sched-mint-smoke dir,
   the discarded first-mint scratch (lane logs/scratch obs are
   truncated already; the dirs remain).
