# 2026-09-01 (through 09-02 ~05:00)

## Done

- **The fixed-jar phased mint ran end-to-end and READ CLEAN: 543
  witnessed labels, probe3 launch-ready.** Final numbers:
  - **Parity witness: 79.0% valid-turn survival** (2,690/3,406; 288
    games byte-exact incl. s, 153 sparse-flipped, 0 truncated) — the
    ADR-0089 fixes verified at fleet scale, the 50% systematic floor
    never approached; the sparse concurrency flips priced exactly as
    the addendum projected.
  - **finish: 653 certified positives, 110 dropped by the witness →
    543/543 labels minted** (zero drops at the label stage).
    Certification rate 19.2% on probe-state windows vs the census-era
    28% prior — the probe distribution certifies less (recorded; the
    prior was census-population-specific).
  - **543 < the 750 bar → the pre-adjudicated `--lab-k 4` path**
    (~1.3 visits/window/iteration, inside the ≤2 rule). No
    re-adjudication.
  - **Seedlab day-zero re-banked**: CE **2.676759** on 543/543 (zero
    misses/unmatched) at the init ckpt →
    `seedlab-dayzero-mint20260830.json`; the FUND decode leg reads
    **≤ 2.141**. Cross-validates the retired 170-batch's 2.730 —
    two independent mints, same pipeline, ~2% apart.
- **Phase operations en route** (each per the standing playbooks):
  phase-1 lane-1 whisker-miss accepted; lane-9 OOM'd at 3g (the
  bridged-JVM AiCache mainline accumulation over ~20 sequential
  replays — heap bumped to 4g fleet-wide + the sched_mint default),
  rerun, then cut at the boundary still grinding a degenerate
  artifact-loop board (partial accepted, witness-priced); a stale
  pre-fix driver chain (10 lane shells parked on the removed sentinel
  — a landmine) found and killed; the driver SIGSTOPped across the
  lane-9 rerun so phase 2 could not overlap it (cross-store carry),
  auto-resumed at 12/12; phase-2 boundary server swap worked as
  designed; phase-2 lane-6 WEDGED 7h inside one roll of g171 (census
  raw-cap monster board, AI loop unkillable by the soft game-over
  clock — the crash-class family gains a specimen) — cut at 77% of
  its share.
- Probe3 launch script updated to the realized state (k=4, satisfied
  prerequisites recorded in the header, 79.0%/543/2.6768 pinned).
- **Clean shutdown for the user's kernel reboot**: server stopped,
  watchd unregistered (no reboot-alert noise), wrappers/watchers
  swept; the box carries zero mint processes.

## Broke / surprised me

- The certification-rate prior didn't transfer: 28% (census states,
  iter-019 era) → 19.2% (probe states, init ckpt). Not alarming —
  different population, different serving policy — but the yield
  math for future mints should use the 19% figure for init-era
  probe-state substrates.
- g171 (probe2 store) is a first-class specimen for the watch-list
  crash family: one game producing 16MB of census rows and a roll
  the watchdog clock cannot kill (the loop never re-checks game-over).
  Worth attaching to the GameCopier/crash-class hunt when it opens.

## Next session picks up

1. **Launch `m10-probe3`** (post-reboot): `setsid nohup
   scripts/launch_m10_probe3.sh` per the detached checklist — watchd
   registration, log monitor (GUARD HALT / KILL / ANOMAL / KL ABORT /
   Traceback + the FRAME-ANOMALY filter), telemetry glances.
   Iteration-0 first looks: `labs_early.json` (ramp+subsample impulse
   check), `seedlab_calibration.json` raw ≈ the 2.6768 day-zero, KL
   vs probe1's 0.034 mean, `sched_live_ce` ≈ day-zero, and the
   memorize guard's first-window read.
2. Smoke-axes declaration (best-practices item f) applies to this
   launch: the probe recipe's new surface (k=4 subsampling +
   carry-w + warmup) was NOT smoke-tested in loop context — the
   iteration-0 telemetry glance carries that burden; say so in the
   launch message.
3. Stale-data pass candidates when M10 closes: sched-mint-smoke dir,
   the discarded first-mint scratch (lane logs/scratch obs are
   truncated already; the dirs remain).

## Addendum 2026-09-02 08:25 — m10-probe3 LAUNCHED (post-reboot)

- Launched `scripts/launch_m10_probe3.sh` detached (PYTHONUNBUFFERED,
  setsid nohup → `data/training/m10-probe3/launch.log`); driver pid
  6047 self-registered with watchd (75 m stall); run dir
  `m10-probe3-i000-20260902-082045`; generation on the fix jar
  `fe58d517…` (game-path-identical to probe1/2's `935c2e…` — plain
  generation writes no marks; recorded in run.json by construction).
- **Cold-start finding — corrects ADR-0085/0087:** the first wave of
  8 workers ALL hit `[GrpcBridge] POISONED: deadline on mtg.priority
  seq=5` at wall=8s — the freshly-booted server's first model forward
  (CUDA context/cuDNN warm-up) exceeded the bridge deadline under 8
  simultaneous first requests; each worker discarded game 0 of its
  chunk and drained, the harness recycled the chunks, the second wave
  played clean (0 poisons). **Probe1 and probe2 iteration 0 each
  logged exactly 8 BridgePoisonedException "skips" and both ADRs read
  them as the 0.76% copy+resume crash family at unchanged rate — they
  were THIS artifact** (8 workers × 1 cold first request). Rate
  bookkeeping for the crash family should exclude the launch wave.
  Cost here: 8/480 iteration-0 games (1.7%, under the 2% non-decisive
  flag). **Run-convenience fix routed by name (~20 lines): the server
  runs one warm-up forward before binding its port** (or the harness
  extends the first-request deadline) — rides the run-infra
  hardening session.
- **Smoke-axes declaration (best-practices item f):** the k=4
  subsampling + carry-w + warmup + memorize-guard mechanics were NOT
  smoke-tested inside a live loop iteration; unit tests cover the
  sampler/ramp/guard math. The iteration-0 telemetry read carries the
  in-loop burden: `labs_early.json` (impulse shape), calibration raw
  ≈ 2.6768, first-window seedlab_raw vs the 0.5× tripline, KL vs
  probe1's 0.034 mean, `sched_live_ce` ≈ day-zero.
- Coverage armed: watchd (driver, self-registered) · session Monitor
  (per-iteration telemetry line from monitor.jsonl, GUARD HALT / KILL
  / KL ABORT / Traceback alerts with the FRAME-ANOMALY filter, driver
  liveness, 2 h heartbeat) · driver-native notify on halt/finish.

## Addendum 2026-09-02 ~09:20 — probe3 FALSE HALT read; guard re-based; m10-probe4 launched

- **m10-probe3 GUARD-HALTED at iteration 0 on my own new memorize
  guard — a FALSE positive from a units bug.** `metrics.jsonl` rows
  divide the once-per-step fixed-batch raws by TRAJECTORIES since
  flush (÷ traj_per_step = 4; the code comment says "per-TRAJECTORY
  means"); the guard compared that diluted row value against the
  per-STEP calibration raw — a comparison that fires whenever the true
  drop is < 4×, i.e. always. The unit test encoded the same mismatch
  (probe2's row-scale first window vs its calibration-scale raw). The
  mirror image of the dead-guard genre: dead-on-arrival by firing.
- **The evidence that it was false, and that the run was the cleanest
  iteration 0 of the three probes:** `labs_early.json` per-step seedlab
  CE on fresh chunks = 3.13, 2.25, 2.47, 2.29, 2.00, 1.71, 2.25, 3.46,
  2.13, 2.24 (mean 2.39 vs calib 2.68 — no impulse, no fit); row
  values × 4 track 2.26 → 2.11 across the iteration (0.79× calib);
  **KL 0.010–0.016 in the applied windows** (probe2 halted at 0.061,
  probe1 ran 0.034) — the ADR-0088 ramp + subsampling + carry-w do
  what they were built to do. Paylab true per-step ~0.6–1.0 (no
  collapse). `sched_live_ce` 6–7.5 vs day-zero 2.6 = the head moving
  toward certified arms and away from its own day-zero emissions —
  the expected sign at iteration 0; the instrument reads the TREND.
  content_flip 0.0015 / argmax_flip 0.0104 at iteration 0 (kill rule
  reads from accepted iteration 4). E/R row levels (1.3–1.5 / 1.3–2.7)
  need a like-for-like scale check vs probe2's rows at the read.
- **Collateral correction to ADR-0087's magnitudes:** its 0.421/0.046
  were row-scale; per-step they are ~1.68/0.18 (2.73 → 0.18 = 0.07×,
  still memorized — narrative intact); probe1's 0.29 is ~1.16 (0.42×).
- **Fix:** rl.py surfaces `seedlab_raw_step` / `paylab_raw_step`
  (true per-step means from the share accumulators); `_rl_summary`
  grows a `min` dict; the guard reads the iteration MINIMUM per-step
  raw vs `--guard-lab-memorize` **0.25** (probe2 0.07× fires; probe3
  0.79× quiet) — the discriminator that separates fitting from fast
  learning. Regression test rewritten on the real per-step numbers.
  Suite 259 green.
- **m10-probe4 launched** (`scripts/launch_m10_probe4.sh`: fresh name,
  seed 20280831, port 50082 zero-hit; probe3 recipe otherwise
  verbatim; probe3 dir kept as a forensic asset with its rejected
  iteration-0 ckpt). Coverage: watchd (driver self-registered) ·
  session Monitor (per-iteration line incl. the per-step keys and
  iteration-min, GUARD/KILL/Traceback/POISONED alerts, driver
  liveness, 2 h heartbeat) · driver notify. The cold-start poison
  wave is expected again at iteration 0 (8 workers; noted, not
  alarming).

## Addendum 2026-09-02 ~11:40 — m10-probe4 READ (ADR-0090): degeneracy veto FIRED on a decode-rule defect; fixed; m10-probe5 launched

- probe4 ran three accepted iterations — **the cleanest loop of the
  four probes** (KL 0.029/0.011/0.015, seedlab per-step 2.68 → 1.35
  with the re-based memorize guard quiet at min 0.50×, paylab 0.9 →
  0.6, tripwire 1 → 0 → 0, no impulse). The ADR-0088 mechanics work.
- **The first post-surgery emission test answered:** generations went
  pure-hold 6.0% → 41.8% → **52.3%**, mean length 4.88 → 0.99 → 1.01,
  utilization 0.2% → 5.5% → 5.8% — **all three veto conditions on two
  consecutive accepted generations: FIRED, pre-registered NO-FUND.**
  Content flip flat (0.002), presence at floor — nothing to consume
  on an empty surface.
- **Diagnosis — the serve decode rule, not the supervision:** mint
  labels are healthy (8.1% pure-hold, mean 2.45, mode 3). Label STOP
  marginal 8%/17%/25%/72% at slots 0–3 vs ~15 candidates splitting
  the rest; `_sched_decode_greedy`'s whole-row argmax let STOP beat
  every candidate individually once the head learned the marginal →
  length collapse to ~1 (36.6% length-1, 2.7% length-3 vs 41% of
  labels at 3). Day zero hid it (a head that knows nothing never
  STOPs). Not ADR-0085's empty fixed point — grounded targets, calibrated
  head, wrong decode.
- **Fix:** `sched_slot_pick` — STOP iff p_stop > 0.5 (beats all
  candidates combined), else argmax over candidates; no-candidate rows
  → STOP; masked never picked. Training untouched, reliance floors
  stand, mu/loader parity by construction. Unit-tested on the real
  label shapes. Suite 260 green. Standing rule born.
- probe4 stopped after iteration 2 (user call: let the veto fire
  formally), SIGTERM orphaned its 7 workers + server (swept; the
  driver's teardown doesn't cascade on SIGTERM — a small hardening
  item for the run-infra session). probe4 kept as forensic asset.
- **m10-probe5 launched** (seed 20280832, port 50083 zero-hit; recipe
  verbatim + the decode rule): the day-zero emission counters under
  stop-vs-continue are read at iteration 0 (new baseline row); whether
  emissions hold non-degenerate under the corrected rule is iteration
  1's read. Coverage: watchd (self-registered) · session Monitor (now
  printing the EMISSION veto axes per iteration alongside guard/flags/
  KL/seedlab-min/content flip) · driver notify.
