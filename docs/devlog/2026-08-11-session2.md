# 2026-08-11 session 2 (C-bundle design round: pins approved, ADR-0054)

## Done

- **C3 pre-work chain read BUILT + RESOLVED** (new standing
  `scripts/rejected_chain_read.py`; chains inferred positionally —
  consecutive vetoed priority attempts, same seat/turn — since the store
  keeps no retry marker). All 20 run13 main stores: 49,902 veto events /
  9,596 trajectories (5.2/traj, ADR-0049's band reproduced; per-iter
  means 2.5–11.0, no downtrend — twenty penalty iterations never trained
  vetoes away). **Verdict: 57.8% singletons, chains short (83% len-2, 12
  cap-hits in ~50k), first-attempt-only alone cuts only 23.5% of
  exposure — the over-pricing is λ magnitude** (0.02/event > 0.015 = one
  held turn's measured cost, ADR-0053). Kickers: 76% of veto windows end
  abandoned (penalty + tempo loss both paid); chain length is §6b
  realizer walk-down machinery, not graded intent.
- **C2a channel fact discovered:** run13's loop retrains the full-vis
  critic EVERY iteration (`finetune_value --full-vis`), and pass-A
  values — the advantage's V — come from that critic. m7-plan's "aux
  value loss" wording (masked head only) would never reach the
  advantage; the critic phase is the channel that realizes the
  mechanism-of-action statement, at zero new generation cost.
- **Campaign priced from the probe rungs:** ~3,200 completions/h at
  N=4/K=32 ⇒ 2-arm act−hold label ≈ 1.2 min/point at K=32, 0.6 at K=16.
  Labels rows don't record the act arm's realized first cast — harness
  extension queued.
- **All four pins user-approved as recommended
  ([ADR-0054](../decisions/ADR-0054-c-bundle-design.md)):**
  1. C-seq = advantage × specific-cast contrast at the mainline fork
     window (Â clipped ±0.25; cast* = act modal first cast; cast-mass
     fallback under 50% agreement).
  2. Campaign P≈100/iter at K=16, fresh every iteration (act arm is
     policy-conditional — freshness beats K=32 precision); ≈ +1h/iter;
     w_seq calibrated to ~10% of PG mass at run start.
  3. C2a = critic-phase aux BCE (capped ~10%) + same aux on the masked
     head (trunk shaping). No drill-regime task token in v1.
  4. C3 = first-attempt-only + λ=0.01: per-window exposure strictly
     below one held turn's cost — the exact ADR-0053 calibration
     statement. Total exposure → ~38% of current. Rides the D3 era
     boundary.
- Docs swept: ADR-0054, m7-plan design block, d6-vtrace-loop §6c M7
  amendment note, CLAUDE.md status, project map.

## Broke / surprised me

- The pre-registered C3 dichotomy (chains ⇒ first-attempt-only;
  independents ⇒ λ decay) was answered by NEITHER branch cleanly — the
  measured story is λ magnitude with a structural cleanup as a rider.
  Standing lesson recorded in the ADR: run the cheap decomposition
  before designing the fix.
- `mu.get(dec["s"])` on early decs returns None routinely (heuristic
  seats / non-mu windows) — the read had to filter on mu coverage, not
  dec presence.

## Next session picks up

1. **D3 boundary execution:** fork stability pass (IndexOOB,
   targeting-retry, MayPlay `.get(0)`, MinMaxBlocker realizer gap) +
   forced-seq labels-row extension (first-cast SA + agreement) +
   Talor #8 fold-in + the #4 format sweep → `forge forkcheck` →
   re-baseline final_read (~2,000 games, overnight).
2. **Bundle build (post-boundary):** L_seq + masked-head aux in rl.py;
   critic-phase aux in finetune_value; first-attempt-only veto grouping
   ported into `rejected_events` (grouping logic exists in
   `rejected_chain_read.py`); in-loop forced-seq phase in selfplay.py;
   w_seq calibration.
3. **Owed instruments:** ADR-0049 cast-suppression + interaction-holding
   reads → `scripts/` (per-run attribution for the gate).
4. Carried: never-cast/realizer-blocked study (task chip); parked
   throughput levers (do not interrupt M7).
