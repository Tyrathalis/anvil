# 2026-09-06 (session 5) — M12 Build 1 launched (the value head inside the trunk, cross-fit); Build 3 enumerators landed in the fork (Surfaces + SurfaceDirective + the search's second round)

## Done

- **Build 1 designed and launched** ([ADR-0103](../decisions/ADR-0103-m12-build1-value-head-and-build3-enumerators.md);
  `anvil/training/value_pretrain.py`, driver `scripts/build1_run.sh`). Asset inventory first
  (two exploration agents): the value head has only ever trained on terminal outcomes; the
  rank-critic precedent (top-4 trunk layers + head, RankNet, 0.483 held out) was a critic
  tower; the banked iter-019-era labels are three families — `labelset-c2-v3` (9,600 c2 rows,
  1,197 frozen holdout), the cl2 completions (800 windows, **the full-vis critic's values at
  both horizons already banked**, 107,924 values), the two m9-rebaseline stores (outcomes).
  Mint rows carry no stored post-action state (dropped); drill fork points are labelset rows;
  the `drillmix*-forks` stores are sv=1 (cannot join). Recipe: iter-019 + value head + top-4
  trunk layers, **KL anchor to the frozen teacher on live priority windows** (the day-zero
  checkpoint stays a POLICY checkpoint), three loss families (state RankNet + BCE anchor; leaf
  full-vis distillation + within-(window, roll, horizon) RankNet on the paired composite;
  outcome BCE + KL), **the one-ply read cross-fit over five game-hash folds** (a single 20%
  holdout would read at SE ≈ 0.05 against a +0.07 bar), state ranking on the frozen holdout,
  early stop on inner splits only. Banks: state 8,373 windows (117 s), leaf 106,144 (452 s),
  outcome 48,000 (3 min). Bars unchanged (GO ≥ 0.35 one-ply and/or ≥ 0.50 state; KILL < 0.32
  both). Driver running: folds 0–4 → build → read → notify.
- **Build 3 enumerators in the fork** (exempt commit pending its forkcheck): `Surfaces` (20
  callbacks through the generator's new `SURFACES` table — named option lists + unwalked option
  cards in the snapshot; force hooks; after-super traces on search copies; index enumerators
  for seven answer shapes, natural first, capped, seeded), `SurfaceDirective` (seat, kind,
  ordinal, index answer; misses counted), `SearchDirective.surfaces` (the trace), `Obs.decSurface`,
  `AnvilRun -searchsurf B [-searchsurfcap C]` with the per-copy run extracted (`runCopy`) and the
  expansion round writing `sub` rows. `SurfacesEnumerateTest` (10 cases). Smoke driver
  `scripts/build3_surface_smoke.sh` + `scripts/surface_smoke_read.py`.

## Broke / surprised me

- The generated controller's two tutor callbacks were already in `FORCE_OVERRIDES` (the M11
  choice directive); the generator now composes hooks in order (M11 first, then the surface).
- `ruff check` on a `.sh` path reports dozens of parse "errors" — lint the `.py` files only.

## Results (close)

- **Build 1 GO.** First pass: one-ply 0.277 → **0.390 ± 0.019** (cross-fit, n 647), eot/K=8
  0.42, h2/K=1 0.44, **h2/K=8 0.44 → 0.69**, state ranking 0.392 (bar 0.50 not cleared), policy
  drift KL 0.002 / argmax agree 99.3%, value AUC on held-out games 0.90; 25 min wall. The
  state-only early-stop variant: one-ply **0.397**, h2/K=8 **0.71**, state 0.375; the declared
  selector (state inner-val at the selected epoch) is a 0.0004 tie that falls to the variant →
  **day-zero ckpt of record `data/training/m12-build1-stopstate/last.pt`**.
- **Build 3 enumerators PROVEN exempt**: forkcheck 498/500 vs the 08-21 seeds; 20260969 = the
  known unstable seed, 20260739 replays to the baseline hash twice on the same jar (the
  identity-hash residual) → **fork pin `6eb64b6c538`**. Smoke: 148 windows, 630 first-ply copies,
  22% traced ≥ 1 surface, 116 sub-rows, 0 copy crashes, 1 miss; modal choice shows the visible
  headroom (Δ ≥ 0.02 at 4/6 sub-rows, max 0.26), tutor targets in the tail (max 0.10).

## Next session picks up

1. **Build 2**: wire the directive to the day-zero ckpt's masked head (the server already
   advertises `anvil.value`); run the search smoke on it (`scripts/build0_search_smoke.sh` with
   `--ckpt` pointed at the day-zero ckpt) to pin the margin bar and temperature from the
   sharpened head's margin distribution (ADR-0102 addendum: not pinned on the unsharpened head);
   the ACTING rule in the controller (margin ≥ bar → sample from the leaf-value softmax; below →
   natural) behind a flag; regenerate the ADR-0094 fixed population on the boundary jar; the
   four-arm day-zero paired read (`iter-019` / day-zero ckpt / + lookahead / heuristic + lookahead).
2. Build 3 Python side when its evening comes: the loader reads the named `opts` on non-priority
   records (`surf` kinds), the three answer-shape heads, the distillation term from `sub` rows.
3. Deferred by name (ADR-0103): open-vocabulary `chooseCardName`; counter type / replacement /
   static ordering; the enumerator-rescue payment class.
