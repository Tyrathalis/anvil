# 2026-09-14 (session 4, late evening) — evening 5's code: the leaf plumb, the calibration chain launched, mainline surface acting

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **State review before code** (the user's ask): tree clean at `af8eeae`, fork at pin `1dd36f7342`, box
  idle, ADR-0106 accepted. Four determinations put before implementation; the user took the
  recommendations:
  1. **The pair rule = the bar-lifted joint pick** (user: softmax weighting so higher-valued options
     are picked more often without excluding lower ones — the Build 2 rule extended to the answer).
     A flat softmax over all (option, answer) pairs would hand an expanded candidate with K
     near-tie answers ≈ K× the mass of an unexpanded one; max-over-answers is the winner's curse.
     So: stage 1 per expanded candidate — answer margin = max V(answer) − V(natural answer); at or
     above the bar an answer is sampled from the answers' softmax at T and the candidate's option
     value is LIFTED to that answer's value; stage 2 = Build 2's option rule on the lifted values.
     Surfaces off → byte-identical to Build 2.
  2. The natural option outside the top-B is its own `ans_unsearched` class; B stays 2.
  3. The mainline arm outlives the ask (modes are chosen at the cast, after `chooseSpellAbilityToPlay`
     returns), fires at ordinal 0 of its kind, is label-guarded (the copy's traced surface label; a
     different label = the path diverged → miss, the natural answer plays), and is taken after the
     action plays (`playChosenSpellAbility`). Where it fires it replaces the whole `askMode`, gate
     included (pin 2).
  4. The priority-slot calibration read sized at rate 0.1 / rolls 4 / 200 games per seat / 24 × 2
     (the 09-11 pay-slot settings would be ≈ 10× on the priority slot: every candidate of every
     searched window is a copy at the horizon). The rate draw is a seed hash → the same windows on
     every arm.
- **The leaf plumb — fork `de9745d089`**: `-searchleaf next|eot|h<N>|end` on the first-ply copies
  (the pay slot's family; `SearchDirective.leafAfterTurn`); the surface slot follows the first
  ply's leaf (the natural answer stays the first-ply copy under CRN; the pay slot re-runs its
  natural only when its own leaf differs); every copy under a horizon leaf snapshots the certify
  axes (`snap` per roll on the opts row); the row names its leaf; the header pins gain `leaf`.
  Smoke (8 heuristic-control games, h2, rate 0.3, rolls 2): 66 windows, snap on every option, kinds
  leaf/void/end, 0 crashes.
- **The calibration chain LAUNCHED 22:29** (`scripts/build3_prio_calibration.sh` →
  `data/runs/build3-priocal/`, jar snapshot `de9745d089`, ckpt e3, arms next / h2 / end, rate 0.1,
  rolls 4, 200 games per seat, 24 workers × 2 servers, clock 2,400 s; watchd `build3-priocal`
  stall 90 min, notify on done/fail, unregister inside the wrapper). The read:
  `scripts/prio_calibration.py` (per horizon: candidate spread vs roll σ + the resolved rate, rank
  agreement + top-pick agreement vs next, the flip rate and the end arm's verdict on flips, the
  acting rule's conversion at bars 0.05 / 0.10 by the outcome; the end arm keyed on itself = the
  in-sample ceiling). Dry-run on the 09-11 pay arms' first-ply rows: 3,471 windows joined, all
  identical, ≈ 3.1 valued candidates per window; the three arms agree (their first ply all ran the
  next leaf — the expected null). **Caveat for the read**: a heuristic seat voids options its own AI
  would not play — 61% of the 09-11 windows had ≥ 2 valued candidates; the spread reads over the
  heuristic-playable set.
- **Mainline surface acting — fork `e28f40d476`**: `Pending.SurfAnswers` (per candidate, the
  expanded surface's answers + mean leaf values + the natural answer's index), the two-stage
  `decide`, the row's `lifted` + `ans` verdict, `SurfaceDirective.armMainline / takeMainline` (per
  (game, seat), label-guarded), `Surfaces` passes the label at the sa-bearing sites, the
  controller arms after the decision and takes after the play (census `surfaceAct`:
  kind / ord / outcome act | miss:* | unfired / at played | stale), `-searchactkinds <csv|all>` (pay
  never), the header pin `actkinds`; `AnvilRun.expandRound` collects the slot's answers. Tests:
  SearchActTest 14 (7 new). Scripts: `build3_act_smoke.sh` + `act_smoke_read.py`;
  `build3_surface_read.sh` gained the `act` arm (`FORGE_ARGS` = the search recipe on every arm,
  `FORGE_ARGS_ACT` = `-searchactkinds mode`).
- **The smokes** (nice 19 beside the chain): the recipe smoke (8 games, rate 1, rolls 2, surf 2,
  bar 0.10, kinds mode): 547 windows, 259 sub rows (23 mode), option acts 44 (39 applied, 5 pass),
  3 mode windows on acted options all below the bar → no arm; ≈ 3,800 copy forward calls per game;
  0 crashes. The mechanism smoke on the fixed tip `abec2b2982` (4 games, all kinds, bar 0.02):
  394 windows, 36 lifts, answer stage search 16 / natural 23 / unsearched 354; **arms 16: 14 act at
  the next window (entity_one 7, entity_set 6, order 1), 2 unfired, 0 label misses, 0 crashes**;
  ≈ 4,700 copy calls per game. Mode acting at bar 0.10 is rare — the 600-game read says how rare.
- **Forkcheck**: `run-20260914-build3-e5` on `e28f40d476` stopped at 452 rows when the tip moved
  (450/452 identical, the standing pair); **`run-20260914-build3-e5b` on `abec2b2982`: 498/500,
  20260969 the standing crash, 20260739 replayed to the baseline hash `9e0365815606ddf6` on the same
  jar (`run-20260915-build3-e5b-replay739`) → PASS 00:30 09-15 → fork pin `abec2b2982`.** GO set;
  the queued paired read launches when the chain's read lands.
- **The paired read (A6) QUEUED**: `scripts/build3_e5_read_queue.sh` (detached) waits for the chain's
  `read.json` + `data/runs/build3-surface-read-b3e5/GO`, then `build3_surface_read.sh` NAME=b3e5,
  ARMS="on act", 300 games per seat, 24 × 2, the jar snapshot `abec2b2982` already in the read dir.

## 09-15 morning — the calibration read landed; the paired read relaunched

- **THE PRIORITY-SLOT CALIBRATION READ (07:22, `data/runs/build3-priocal/read.md`)**: 1,360 windows
  joined across next / h2 / end, all identical (the identity proof holds); 828 with ≥ 2 candidates
  valued in every arm, 3.1 per window. Per leaf:

  | leaf | spread | roll SD | resolved | ρ vs next | top agree | flips | end's verdict on flips | acting conv. bar 0.05 | bar 0.10 |
  |---|---|---|---|---|---|---|---|---|---|
  | next | 0.086 | 0.009 | 91% | — | — | — | — | +0.030 ± 0.017 (151) | +0.062 ± 0.025 (81) |
  | h2 | 0.056 | 0.073 | 35% | 0.34 | 47% | 435/828 | +0.022 ± 0.008 (87 up / 58 down) | +0.078 ± 0.019 (148) | +0.118 ± 0.029 (74) |
  | end | 0.117 (median 0) | 0.170 | 21% | 0.44 | 27% | 606/828 | in-sample | +0.293 (ceiling) | +0.294 (ceiling) |

  Reading: the next leaf is the well-resolved one (roll SD 0.009 against a 0.086 spread); the h2
  leaf is ≈ 8× noisier per roll and flips the one-ply pick on half the windows — yet by the outcome
  its flips are right more often than not (t 2.7) and the acting rule's conversion under h2 is about
  double next's at the same bar with the same positive counts. **Caveat before this sizes
  anything**: the arms share roll seeds (CRN), so an h2 copy and the end copy of the same roll share
  their first two to three turns — the deeper leaf's agreement with `end` is optimistic by
  construction (the next leaf shares a fraction of a turn). The clean read is an end arm on
  INDEPENDENT roll seeds (a `-searchrollsalt`, one more arm ≈ 5.5 h of box) — routed as the next
  chain after the paired read. Provisional answer to C1: next is the resolved leaf; h2 earns its
  place as the shakedown's deep arm but not yet as a winner; a second ply is not read here.
- **The paired read FAILED at launch (07:22)**: every worker exited with zero games — the harness
  refuses `-search` without `-labels`, and the queue script did not pass final_read's `--labels`
  (the calibration launcher does). Fixed in `build3_surface_read.sh` (`--labels` whenever the forge
  args carry `-search`); **relaunched 08:30** detached (watchd `build3-read-b3e5` + the arm's own,
  stall 60 / 45 min, notify on done / fail); the on arm's workers producing label rows.

## 09-15 midday — the paired read; run hygiene rebuilt; the entity arm

- **THE PAIRED READ (12:37, `data/runs/build3-surface-read-b3e5/read.json`): act − on = −0.69 ± 0.81pp
  (n 579, t −0.85, 9 up / 13 down).** Exposure in the act arm: 21,903 searched windows in 600 games,
  the mode answer stage reached 115 windows (a mode expanded on the acted option), cleared the bar
  9 times, 9 arms fired clean (0 misses, 0 unfired). **Mode acting at bar 0.10 is nearly inert**
  (the smoke's answer margins ≈ 0.004 vs the bar); the number is "nothing broke", not a read of the
  mechanism. The gate already decides only windows where acting does not fire (a fired arm replaces
  `askMode`, gate included), so there is nothing to retire; pin 4's "not negative" → the entity arm.
- **Run hygiene rebuilt ([ADR-0107](../decisions/ADR-0107-run-launcher-and-checkin.md))**: `anvil.runs`
  (the launcher = the checklist as code; tests 6), the alert queue, `docs/ops/run-checkin.md`, the
  four live chains ported, the quickstart's §7½, the checklist's user-facing-change trigger, the
  documentation review session routed (Build order 4¾). Merged from the worktree at 12:38.
- **The entity arm LAUNCHED 12:39 through the launcher** (its first real run: `b3e5-actent`, arm
  `actent` = `-searchactkinds entity_one,entity_set,mode` on the same reference; the on / act arms
  skipped as done). The check-in task `anvil-run-checkin` installed (7 and 37 past each hour).
- **The salt plumb** (fork: `-searchrollsalt <long>` XORed into every roll seed; the rate draw
  unsalted so arms still join per window; pinned `rollsalt`) written for the de-confounded end arm.

## 09-15 afternoon — EVENING 5 CLOSED

- **The entity arm (14:17)**: actent − on **−0.69 ± 0.91pp** (n 579, 12 up / 16 down). Exposure:
  21,872 windows, the answer stage on 1,079 expanded windows, 70 sampled deviations (entity_one 51,
  entity_set 10, mode 9), 63 arms fired clean, 6 unfired, 0 misses — ≈ 0.12 acts per game at bar
  0.10. Both reads neutral at low exposure → nothing broke; the gate is moot (a fired arm replaces
  it; it decides only unacted windows). **Recipe pin (ADR-0106 addendum): surface acting ON in the
  shakedown's recipe arm** (`-searchactkinds entity_one,entity_set,mode`); the answer bar is a
  settings-pass axis. **Evening 5 CLOSED; Build 3 closes with it** → the rebase, then Build 4.
- The salt queue moved on at 14:17: forkcheck `run-20260915-build3-e5salt` (pid 3528544) ∥ the
  salted end arm (`b3pcals-end`, 200 per seat); the four-arm read lands ≈ 20:30 in
  `data/runs/build3-priocal-salt/read4.md`.

## 09-15 evening — the salt tip PROVEN; the check-in task stuck

- **Forkcheck `run-20260915-build3-e5salt` on `e63a0cac20`: 497/500** — 20260969 the standing crash;
  20260739 replayed to the baseline hash; 20260853 replayed to a third hash twice and to the
  baseline on the third replay (four runs, three hashes: the identity-hash residual is live on
  this seed) → **PASS 18:13 → fork pin `e63a0cac20`**.
- **The check-in task never worked**: one run, started 13:12, still "running" with no activity
  after 13:12:32 — stuck on its first tool-permission prompt (the tool's own warning), and no later
  scheduled fire started while it sat. The two 14:17 alerts stayed unacked. Until the user
  pre-approves its tools ("Run now" once, approve each prompt), the check-in is not coverage;
  the session waiters are.

## Broke / surprised me

- **The first arm design never fired** (the mechanism smoke, all kinds at bar 0.02: 14 arms, 14
  `unfired` at the play boundary). Two causes: (1) a cast spell's entity choices fire at
  RESOLUTION, after `playChosenSpellAbility` returns — the arm must live to the seat's next
  quiescent window (the copy's leaf, the bound the sub row's ordinal was counted to); (2) the
  served-path traces (`askOne/askSet/askMode`) recorded the METHOD name as the surface label
  (`chooseEntitiesForEffect`) while the after-hooks and the mainline guard use the ability label —
  every guarded match would have missed. Fixed: the force hooks pass `labelOf(sa)` into the ask
  helpers' trace (served sub rows now carry the ability label, as the heuristic-path rows do); the
  arm is taken at the seat's next quiescent `chooseSpellAbilityToPlay` (census `at: next_window`).
- **The paired read's launch failure went unseen for an hour** (user: "shouldn't stall checks have
  caught that?"). The wrapper did what it was built to: notify-send "FAILED" at 07:22:45 and a clean
  watchd unregister — but notify-send reaches the desk only (the phone hop is unwired) and a fast
  clean failure is not a stall, so nothing reached the session. The launch checklist's box 3 (a
  session-side waiter keyed on the output artifact) was armed for the forkcheck and the smokes and
  skipped for the queued read. Armed for the relaunch (a waiter on `read.json` or the wrapper pid).
  The convenience this asks for: a watchd "failed" state that keeps alerting each tick until
  acknowledged, so a wrapper's failure path re-registers instead of unregistering.
- A `sed` with `#` as the delimiter against a comment line emptied the smoke launcher on the first
  try (the `&&` chain skipped the Python step; `bash -n` passed on an empty file). Copy first,
  then patch in Python.

## Next session picks up

1. The salt queue's output: the forkcheck compare (→ fork pin `e63a0cac20`) and `read4.md` (the
   h2 flips under an independent-seed outcome) → C1's verdict; the partial-expansion slot (C3)
   sized by it.
2. Build 3's closeout ADR (one row in the milestone table; the Now paragraph moves to the archive),
   then the rebase (ADR-0106 B).
3. Click "Run now" once on the `anvil-run-checkin` scheduled task to pre-approve its tools.
