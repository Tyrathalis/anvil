# 2026-09-16 — Build 3's closeout ADR

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **[ADR-0108](../decisions/ADR-0108-m12-build3-closeout.md): M12 Build 3 CLOSED.** The record of
  the five evenings (09-07 → 09-15) as one table on one reference (entity one + set +2.06 ± 1.83;
  the gated mode + the whole served set −0.17 ± 1.80; ordering + damage +0.52 ± 1.82; surface
  acting −0.69 ± 0.81 / 0.91; the payment head five negatives → withheld to the loop); the seven
  findings in weight order (the joint-realization mechanism, the served-tag + probe costs, C1's
  verdict, attribution by ablation, the search's price, the heuristic's mode leak, the throughput
  week); five decisions (the build closes, the served set pinned through the rebase + Build 4, the
  shakedown's three arms pinned incl. the deep arm from ×2.83, the pay head's home is the loop,
  twelve items routed by name); the standing rules born (one new at the closeout: a served surface
  answer is realized with everything the heuristic's answer bundles); assets carried / retired.
- The plan's Build order item 3 marked closed with the pointer; the running record's closeout
  entry; CLAUDE.md Now; the map's ADR index row; the standing rule.

## Broke / surprised me

- Nothing ran. One correction surfaced in the write-up: the ADR-0106 sizing line's "≈ 350 g/h
  with surface acting" was an estimate; the 09-15 bench cell measured the full recipe at 294 g/h
  peak — the big-run sizing line is re-issued at the rebase's three-cell read.

## Next session picks up

- The rebase (ADR-0106 B): upstream onto the engine pin's successor, carrying the fork tips
  `b0567608938` → `5e333e96930`, PR 11916, the three-cell g/h read; a dataset boundary (new
  forkcheck baseline hashes, era-scoped Ante maps, banked pools cross-era); one heuristic
  reference read + one `iter-019` read on the new jar. Then Build 4.
