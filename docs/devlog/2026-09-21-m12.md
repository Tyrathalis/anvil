# 2026-09-21 (M12) — the pre-shakedown review: the census bundle, run resilience, the shakedown scoped (ADR-0115)

<!-- Wrap-up: the session checklist lives in CLAUDE.md (devlog · plan-doc running record · Now block · commit). -->

## Done

- **The review (user: "the documentation pass is next; any decisions before?")** — the box idle, the
  fork pushed (the Now block's "unpushed" was stale). Three decisions taken: (1) the shakedown launches
  FIRST and the documentation pass runs beside it (the plan's 4¾ said "after the verdict", the Now block
  said "before" — resolved to beside); (2) the 09-19 census items land before anything else (one fork
  commit, one forkcheck); (3) drills OFF in the shakedown (the M4 drill phase would confound the arm
  read); the certifier merge lands during the shakedown week for the big run's jar, since under
  ADR-0101 item 7 the big run's drill-finding IS the search and its offline re-search needs that primitive.
- **The box across reboots.** The whole system is one LUKS volume behind a GRUB passphrase (my first
  read from `lsblk` saw only the Games mount — wrong; the user corrected it; `/proc/cmdline` confirms
  `rd.luks`), so unattended reboots stop at the passphrase and a power outage is a UPS question only
  (none attached). All recent reboots were manual maintenance (four in eight days) → the maintenance
  sequence is the thing to make cheap: `pause --wait`, update, reboot, `relaunch`. The GPU yield +
  VRAM park already make ordinary desktop / ComfyUI use cost wall only. Pacman pin for the big run:
  the kernel, `linux-cachyos-nvidia-open` (the open KERNEL MODULE, `nvidia-utils` the same proprietary
  userspace; on a 4090 the two module builds are within noise and the GPU never binds anyway) and
  `nvidia-utils` are hard-dependent and move together, plus the JDK — the user edited
  `/etc/pacman.conf` (`IgnorePkg`, nine packages, verified); the unpin is on the run-close checklist.
- **The fork's census bundle `cbe386d2c6`** (recording-only): `vr` on every voided first-ply candidate
  (flag or no flag); the mainline's forced ask returns `ForcedAsk {sas, voidReason}` → `act_vr` on the
  search row + `vr` on the census (`no_option | no_plan | the realizer's veto code | pass |
  heur_refuse`); `Census.rec` marks rows on search copies `copy:true` (`SearchDirective.isCopy`), so a
  searched arm's mainline veto rate reads from the mainline decs alone (`arms_report.py`: `veto_rate`
  mainline, `copy_veto_rate` beside it). Test `actVoidRowCarriesItsReason` (SearchActTest 25). Smoke
  (4 games, the recipe, 4 × 1): 120/120 void candidates carry `vr` (118 `no_shape_fit`, 2 `unpayable`),
  0 rescue blocks, mainline priority census 968 pass / 98 cast / 10 veto vs the copies' 1,482 / 2,364 /
  120 — the copies were 3× the mainline's rows. Forkcheck `run-20260921-build4-census` launched 11:19
  (`build4-census-chain`): RESULT BELOW.
- **Run resilience (ADR-0107 addendum 09-21):** `anvil.runs pause` / `relaunch`, `--resume-on-gone` /
  `--resume-max` (the sweep relaunches a gone run, capped), `heartbeat()` from the harness's GPU yield
  and the learner's VRAM park (the 09-17 false-stall class), the headless JVM fallback when no display
  exists (PROVEN: one game from the jar with no DISPLAY from `forge-gui/`; the first attempt from the
  scratch dir failed on `Localizer` — the language bundle resolves relative to the cwd — not on AWT),
  the sweep unit carrying `ANVIL_NOTIFY_CMD` / `ANVIL_CHECKIN`, `selfplay.py --wall-hours` (accumulated
  box time across pauses; WALL-STOP then the closing reads). Tests +4 (runs 16). The Remote Control
  trial: `anvil-remote-control.service` (tmux `anvil-rc`, `claude --remote-control anvil-box`, under
  linger) — active, the session reachable from the phone; the trial question is auth across weeks.
- **ADR-0115 — the shakedown scoped:** four arms (recipe / alloc / shallow / deep) at equal box time
  (30 h each), sequential from the e1a build on the census jar, the run of record's loop settings,
  drills off, no per-arm day-zero read, each closing with the 2,000-game read + the last lookahead arm;
  `scripts/shakedown_chain.sh` (not launched: the forkcheck + the shallow-wide bench cell first, then
  the user's go on WALL_HOURS).
- **The prep chain** (`build4-prep-chain`, behind the forkcheck): the shallow-wide bench cell (24 × 2,
  64 games) and the contention smoke (a synthetic foreign GPU job `gpu_burner.py` under `setsid`
  beside a 4-game recipe smoke; `contention_read.py`): RESULTS BELOW.
- **THE SHAKEDOWN LAUNCHED 13:00** (`shakedown`, `--resume-on-gone`, stall 180 min; the deep arm pinned to
  the priced bench arm's full flag set incl. `-searchclock 3600`); the recipe arm's iteration 0 running at
  24 × 2. A session wait armed. ETA: recipe closes 09-22 evening, alloc 09-24, shallow 09-25, deep 09-27.
- **Community watch (Discord 09-18 → 09-21):** Kryptic's iter-50 (flat within noise since iter 30; the
  monitor past our guards; the pay head's 95% deviation; the Red mirror 34%), chrismaghuhn's interference
  tests, khaliostr's wasm question (inference yes via fork H's export, training no — the games bind).
  Survey doc section + two draft replies; the memory note updated.
- Scratch deleted on sign-off: the vrsmoke dirs (3.6 MB). Alerts acked.

## Broke / surprised me

- `claude remote-control --help` starts a session instead of printing help (hung the shell; killed).
- `anvil.runs wait` needs `--name`; the first two session waits exited at once (re-armed).

## Results

- **Forkcheck `run-20260921-build4-census` PASS 11:53: 499/500, 20260969 the standing seed (43 vs 29
  turns), fidelity 451/48/1 = the baseline's → THE FORK PIN `cbe386d2c6`** (pushed).
- **The shallow-wide bench cell (12:03, 24 × 2, 64 games): 587 g/h peak / 478 wall, mean batch 3.7,
  wait p90 20 ms, busy 74%, 0 crashes** — ≈ 1.7× the recipe's 347 → ≈ 15–17K games in a 30 h arm.
- **The contention smoke (12:03–12:09): the burner (4.9 GB, 88% SM, 360 s) tripped the yield as a
  foreign job; 4/4 games won, 0 deadline / poison lines at the 20 s deadline; per-game wall median 86 s
  vs 32 s without it (max 326 vs 60 s).** Normal use costs wall only.

## Next session picks up

- The shakedown's launch (ADR-0115; WALL_HOURS is the user's call) → the certifier merge (fork) and the
  documentation pass (4¾) while it runs.
- Routed: the state-ranking Spearman checkpoint-eval path (when the first arm closes); the RC trial's
  auth-across-weeks question (check the phone after the next reboot); a UPS + BIOS power-on-after-loss
  (hardware, the user).
