# ADR-0081: M10 design round CLOSED — every obligation resolved, all forks user-adjudicated, the build proceeds on the funded unified candidate with aux roster E+R

- **Date:** 2026-08-27
- **Status:** accepted
- **Design-doc anchor:** [m10-plan.md](../design/m10-plan.md) done-when 1;
  the round's ADR chain 0078–0080

## The round's ledger (opened 2026-08-25, closed 2026-08-27)

| obligation | resolution |
| --- | --- |
| 1. Planning-ceiling measurement | **FUNDED** — +13.5pp/game central, ~6× threshold ([ADR-0078](ADR-0078-m10-ceiling-measurement.md)); charter settled on ceiling grounds, §3b shelved |
| 2. v2 offline target probe | **RESOLVED** — E PASS (+0.206, 4.1× margin), R PASS (+0.066), F FAIL ⇒ **birth aux roster = E+R**, validity predicate = R + veto-knowability splitter ([ADR-0079](ADR-0079-v2-target-probe.md)) |
| 3. Escape argument + kill signal | Structure ADJUDICATED (5-family telemetry, conjunctive KILL, FUND + degeneracy veto, PG staged mask with ≥4-iteration unmask floor, probing-dissolution secondary read); **numerics pinned pre-data at the training-probe launch** — the pinned protocol, not an open item |
| 4. Evalset repair | Routed with the M10 label work (unchanged; ADR-0069/0073/0077) |
| 5. Aux-target shape review | Done adversarially in the probe spec (E hold-degeneracy → the FUND veto's territory; R slot-index confound closed in the arith rung; F push-through-semantics caveat recorded) |
| 6. Two M11-routing ceiling probes | **BOTH RE-DEFER with numbers** — tutor/fetch 1.41pp/g, effect payments 0.69pp/g vs the 2.2 bar; ADR-0077's re-deferral condition satisfied ([ADR-0080](ADR-0080-m11-routing-probes.md)) |

All build forks user-adjudicated 2026-08-26 (recorded per-fork in
[m10-plan.md](../design/m10-plan.md)): resource component BOTH / cadence
revise-on-trigger / ingestion schedule-tokens + [PLAN] readout with
discrete carry / payment slot-conditions + PG staged mask + ADR-0073
stripping retired / wiring direct-aux-at-birth with drill families
behind birth telemetry. [canonical-register.md](../design/canonical-register.md)
carries the 8 named future forms. The read protocol: the standing
2,000-game paired read + the binned competency instrument (prototype
validated, ρ=0.98); its population/bins/type-rule pins ride the
numerics session with the kill thresholds.

## Queue routing at close (no-silent-loss, all by name)

- **Cost-composition cousins + costmod per-spell + pool-tie residual** —
  scheduled INTO the M10 actuation build (unchanged from ADR-0077).
- **Resolution-effect payments** — closed by measurement (ADR-0080);
  re-enters only on new evidence (e.g., the conditional-holdout
  telemetry showing an era stops always-paying).
- **Tutor/fetch targets** — re-rankable at M11 scoping against the
  1.41pp/g state-dependent-selection pricing, not before.
- **Nonlinear F re-probe** — named future instrument vs the banked
  96k-row label mint (ADR-0079 decision 4); funded by need at birth
  telemetry, never silently.
- **Copy+resume crash class** (0.76% of probe completions, point-shaped
  wipeouts; surfaced by the ADR-0080 run) — ACTIVE forensics under the
  `crash_why` patch (fork `7c4af49fa4`); gets its own record when
  named; the probe reads are unaffected (crash rows excluded by pin).
- **Mainline draw-clock both-win wart** — carried watch; rides the
  M10 label work (unchanged).

## What the build session inherits

Aux roster E+R (joint multi-task, ADR-0057 share instrumentation);
schedule tokens + [PLAN] readout, discrete carry, bit-exact loader
reconstruction; revise-on-trigger with the 4 engine triggers; payment
slot-conditions marked candidate, PG-masked at birth; ADR-0075
supervised conditional labels as direct aux (era-scoped weighting);
telemetry-from-birth roster; day-zero banking on the graft; then the
D4-shape training probe with kill/FUND/unmask + read-protocol numerics
PINNED PRE-DATA at its launch session. Done-when 2–5 of the plan are
the build's contract.
