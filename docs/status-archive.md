# Status archive — full history of closed tracks (moved verbatim from CLAUDE.md)

Moved out of CLAUDE.md on 2026-08-01 to keep the working Status section readable.
Every bullet below is the full running record exactly as it stood in CLAUDE.md at
archival — nothing was rewritten. Since 2026-08-12 this also holds verbatim
snapshots of OPEN tracks whose bullets outgrew CLAUDE.md (labeled as snapshots in
their section headers); the live compact summary stays in CLAUDE.md. ADRs in `docs/decisions/` are authoritative for
decisions; devlogs in `docs/devlog/` carry the session narratives. The compact
summaries that replaced these bullets live in CLAUDE.md's Status section.

## M10 (closed 2026-09-05, ADR-0096) — verbatim CLAUDE.md bullet at close

- **M10 OPEN — the unified resource-scheduling competency (charter 2026-08-25); ceiling FUNDED ([ADR-0078](docs/decisions/ADR-0078-m10-ceiling-measurement.md): +13.5pp/game central, +14.1pp on v<0.45 — measured under binding `-forceschedule` execution). [m10-plan.md](docs/design/m10-plan.md) = doc of record; [m10-build-spec.md](docs/design/m10-build-spec.md) = as-built (R1–R6: slot tokens + decode/E/R heads, revise-on-trigger serve carry, PG staged pay mask, R5 label stack, cousins jar).** SIX PROBES (08-28→09-02, ADR-0085→0093) each peeled a layer and rule-closed it: self-referential decode aux → fixed point at empty (0085/0086); fixed-batch calibration impulse + 'a small batch cannot carry a channel' → the grounded driver + labbatch mechanics (0087/0088); replay-integrity defects → the mint (0089; 543 certified → 2,157 full-support labels); decode-rule collapse → `sched_slot_pick` (0090); uncoupled consumer (0091) → feed-and-follow + full-support emitter (0092); probe6 the first to finish clean (0093): DISCUSS-ZONE, **adjudicated NO-FUND / no KILL** — the label-shaped content probe (`scripts/sched_content_probe.py`) read schedule-conditioned following ≈5pp and content ≈3%, the live follow/utilization headlines were natural-line inflation, PG never reached the emitter (live-gap 3.3→5.7× = the 0088 staleness tell). Emitter DONE as a supervised planner; consumer PARTIAL. **STEP-BACK ROUND ADOPTED ([ADR-0094](docs/decisions/ADR-0094-m10-reset.md); [m10-reset-draft.md](docs/design/m10-reset-draft.md) = statement of record, six forks user-adjudicated 09-02/03):** the ckpt of record has not moved since 08-03 across M5–M10, and the ceiling was measured under binding execution while the live surface was advisory. The reset = the planner hierarchy: (1) turn planner (schedule + hold-set, ONE log-prob'd action at emission + each revision trigger) and window executor-reactor; **BINDING execution at serve** (post-land; hold binding on spells; forced-cast vetoes attributed to the planner); (2) **reward-trained planner** (PG on the schedule action, revisions reward-only in probe7) with the mint CE anchor, planner KL guard 0.06, entropy a read, E/R stay; (3) **INLINE certification** in the generation workers (2% of live MAIN1 windows, off switch; label = search-adjudicated best of {own plan, arms}, rolled-out windows only; labels + arm spread ride the store, the replay window is the era weighting, the 08-30 mint an anchor batch until the in-store pool exceeds it; the mint term moves into the RL pass — grounded × dense × on-distribution by construction; replay parity + labbatch mechanics leave the critical path); pivotal-moment head (on the arm spread: yield / drills / deployment live search) = the named extension; (4) **PRIMARY read = stratified paired strength** (candidate vs the same ckpt under advisory serve, K=8 paired completions from a FIXED baseline-generated 600-window v<0.45 population, bar at the ADR-0078 threshold scale pinned pre-flight) with a **DAY-ZERO read** (binding execution of the distilled mint alone; below minus the bar = halt for adjudication); headroom shrinkage promotion-only; KILL = veto first-window axes or forced-veto rate, 2 iterations from the 4th; content/utilization/follow gates, live-gap ratio, content probe retired as reads; (5) **budget STAGED**: session one = binding serve rule + paired read + fixed population + day-zero read on probe6 iter-5 (also probe7's init); mid-point rule; session two = loader action + planner PG/KL twin + inline certifier + smoke + pre-flight; probe7 6×480 reads what training adds; nothing on the stratum with a clean loop = a legitimate negative answer at the hierarchy level; (6) payment under the same principle DEFERRED by name to the probe7 read. Retired: follow term, v1 plan machinery, content/utilization gates. Deferred by name: the own-plan follower (§F.1), the contrastive term, revision-window labels, head-only distillation. Adjudication principle (user): prefer the simpler, more elegant architecture that lets the model act correctly and coherently, even at more data and training time. Four standing rules born → [standing-rules.md](docs/standing-rules.md). **Session one BUILT 09-03 (draft §H): binding execution at serve (`--sched-binding off|all|forks`, executor-mirroring mask on the pointer logits, two-pass at decode windows, `bind/allow/lp` on the mu row, loader reconstructs; forced rows recompute to exactly 0), cold-start warm-up, the paired read (`scripts/sched_paired_read.py`, `PAIRED_*` pins) with the fixed population drawn from the ADR-0078 ceiling census (user pin: reuse; 600 at v<0.45 on `d4-critic-fullvis` + 200 context), both smokes clean. Found: no fork completion had ever carried a schedule (server g≥0 gate — the ceiling's natural arm and the mint's completions played mask-closed) and the ADR-0090 decode fell back to the heuristic ~1.2K times/iteration on single-slot micro-batches (fixed). **DAY-ZERO READ LANDED 09-03 ([ADR-0095](docs/decisions/ADR-0095-m10-dayzero-read.md)): HALT — binding execution of the distilled planner is worth −6.7pp ± 0.9 (z −7.6) vs the same ckpt advisory on 553 v<0.45 windows (context −12.6pp), monotone in v; the instrument is proven (CRN halves the per-window noise; read SE 0.9pp at K=8/N=600). Mechanism: 59% of emissions are trigger-2 mid-turn revisions that re-decode EMPTY at unlabeled states, and binding turns that hedge into a hold on castable spells (71% of bound windows are holds). Four serve-rule isolation passes on the same 56 windows (empty-revision no-op −9.6 / release −7.8 / release+no-land-first −8.9 / first-slot-only −4.5 vs advisory; pinned −9.6): **the damage is monotone in how much of the plan binds — the distilled planner is worse than the executor at every slot, including the first.** **ADJUDICATED (user, 09-03): ROUTE 1 — train the planner on the existing model's strategy (distill the executor's realized turn plans at scale, certified labels as overrides) before any binding regime; the gate = this read re-run on the same population (≈0 vs advisory = a faithful planner). Standing rule born (day-zero-gated binding). **GATE PASSED same night: the executor-strategy planner (`m10-planner-distill-v2`, holdout first-cast agreement 77% vs probe6's 25%) reads FLAT (+0.9 ± 1.3pp) vs advisory under the RELEASE rule (an empty re-decode at a revision trigger hands the turn back to the executor); the pinned hold rule stays −8pp because the emission basis is pre-land (plans are partial by construction) — Fork 1's hold sub-pin amended. **Full-scale read (09-04, 568 windows): −1.1 ± 0.6pp, FLAT by rule; flat at v<0.3, −3.5 ± 1.3 at v 0.3–0.45, −4.1 on context — the planner's false-empty first-window emissions (73% of remaining holds) are the residual; the empty-emission variant at full scale = the same read (−1.0 ± 0.55; +0.1 ± 0.3 vs release-only on the same 568 windows — the subset's +3.1 was a fluctuation). **Day-zero number of record: −1.1 ± 0.6pp (release rule), FLAT by the mid-point rule — training is the open question.** SESSION TWO part 1 BUILT 09-04 afternoon (draft §I status): the two WAIT refinements (WAIT opens Instant/Flash spells; board activations of tapped/sick/spent hosts leave the virtual superset — 'spent' derived from visible state: loyalty / once-per-turn abilities absent at a quiescent main window) + the driver's serve-regime flags (`sched_flags`, one derivation for every driver-started server) + `--paired-read` day-zero/terminal wiring with the HALT rule (exit 5). FOUND en route: `quiescent_main` read the stack from entities only (abilities on the stack live only in `obs["stack"]`; 9.3K/79K census windows misread) — binding rule 3 failed sorcery-speed slots for timing in every day-zero read so far; fixed in one place. Graft re-distilled on the refined superset + fixed helper (`m10-planner-distill-hand2`); **DAY-ZERO RE-READ LANDED 09-04 20:30 (ADR-0095 addendum): HALT by the mid-point rule — −2.33 ± 0.70pp (z −3.3, n 559) vs advisory, context −4.4 ± 1.4; the fixes worked as designed (absent-fails 16K→6.6K, forced casts +11%, WAIT 46%→38% with 143K instants opened) and the number did not improve (prev −1.84 ± 0.71; within run-to-run noise): the faithful planner bound at day zero is worth ≈−2pp, monotone in how much binds. ADJUDICATED (user, 09-04 evening): follow the rule — better labels before any training: session two REORDERED, the inline certifier (item 4) first, run under advisory generation to harvest search-adjudicated labels → distill → this read re-run as the gate; PG/KL after the gate.** **INLINE CERTIFIER BUILT 09-04 evening (draft §K): protocol `index_lists`, Java `-certify` rollout mode + `Obs.peekPriority`, Python `certify.py` (deterministic rate gate; sweep enumeration verbatim) served model-free, harness `--certify/--heap`, `sched_certify_finish.py` (stage-1 verbatim → full-support labels + arm spread), `sched_harvest.py`; smoke clean (9 points → 9 labels).** **HARVEST h1 (overnight 09-04/05, auto-stopped at 3 batches): 806 points → 799 labels (159 certified = 20%, the mint's band; ~2.4 h/batch; void arms 32% of rollout time). Distill on them (`m10-planner-distill-hand3`): certified NOT learnable at 159 (holdout exact 5%) — the same answer as at 543; certified labels are content changes (47% disjoint card sets, 0% reorderings), so volume is the lever (~30 batches for a mint-scale certified pool). Gate read NOT run (the planner ≈ hand2). Jar-identity proof PASSED (fork tip `bcf0fc73c5`): forkcheck 498/500 main-trace hashes identical to the 08-21 boundary run, the 2 mismatches launch-unstable on the same jar (the identity-hash residual) — the census replay stays valid on the certifier jar.** **09-05 DIAGNOSTICS → [ADR-0096](docs/decisions/ADR-0096-m10-closeout.md) PROPOSED (M10 closeout, awaiting acceptance): the generative planner route is NEGATIVE at the hierarchy level — the search's wins are real (label reliability 0.66; retest 27/40 re-certify) but sparse, state-specific and invisible to the executor's representation (exact-arm 0% at 4× data; frozen-trunk scorer Spearman 0.08 flat); PIVOTALITY is learnable (AUC 0.64→0.69 rising with data). Probe7 not run. Next charter drafted: the OPTION SCORER over any decision window + certifier generalized by tag + pivotality-aimed certification + critic lookahead; payment cash-in as the parallel strength candidate.** Next: the loader's schedule action on the superset basis, planner PG + KL twin, the inline certifier, smoke, pre-flight pins. **09-04 morning (user direction): the plan's basis must be the HAND (land timing is strategic; rocks/rituals change mana mid-plan) — **hand-basis planner BUILT + READ (§I; commits c3aff7d→71f4b48): mined ability table (1,726 cards), superset key space, WAIT semantics; distilled head first-cast agreement 92% (was 77%); day-zero −1.8 ± 0.7pp = the same read as the legal basis (−0.7 ± 0.75 on shared windows); WAIT = 46% of bound windows — the day-zero cost is commitment, the hand basis buys expressiveness (lands, post-land casts, activations) not a better number;** certified-on-parity experiment: the 543 certified arms are NOT learnable at that size on any foundation (exact ≤5%) ⇒ the certified signal needs the inline certifier's scale + the hand basis.**, then session two (learner side + inline certifier) on the release rule with the distilled graft as probe7's init.** Watch: the copy+resume crash class (0.76%, variant-random GameCopier ordering defect — own record when named); the cold-start poison wave (8 workers × 1 first request) needs the per-iteration warm-up forward before a binding surface. Verbose record: [status archive](docs/status-archive.md) (snapshot 2026-09-03).

## Playable-fork track (2026-07-26 → 07-30: founding worklist complete through v11; multiplayer hardening closed)

- **Playable-fork track NOTED (2026-07-26, not scheduled): [playable-fork-worklist.md](docs/design/playable-fork-worklist.md)** — a human-facing distribution build of the fork, distinct from the pinned research/training fork (lives on a separate `playable` branch; all items are UI/net/updater code the headless harness never loads, so training-neutral by construction). Prompted by friends weighing Forge vs TTS for a Commander night. Items: auto-updater UX (the Linux Adventure launcher drops a `forge-installer-*.jar` in Downloads + exits — make it in-place apply + auto-restart); delta/incremental update instead of the full ~160–270MB re-pull; repoint the updater from `Card-Forge/forge` to our fork (`ForgeConstants.GITHUB_FORGE_URL:30`; the snapshot channel is all-GitHub, the release channel needs `releases.cardforge.org` we don't run). **QoL track planned 2026-07-26 (items 4–5 + branch hygiene, doc updated): window resizing DIAGNOSED and is stock-upstream, not fork-specific** (`GameLauncher.java` byte-identical to the pinned base) — three layers: `config.setResizable(false)` publishes fixed GLFW size hints so compositors refuse to tile (that one line = the whole missing-snapping story), `Forge.screenWidth/Height` captured once at init but read every frame in `render()` while `resize()` never updates them, and `Utils.HEIGHT_RATIO` is `static final` so every font/touch-target is frozen at launch scale; tiered T1 (resizable + live viewport, unlocks snapping) / T2 (debounced rescale + FSkin font regen) / T3 (orientation flip). **New item 5 = shallower configurable tap angle, both clients** — angle is one line each (`FCardPanel.getTappedAngle():70` = -90, `CardPanel.TAPPED_ANGLE:89` = π/2) but three geometry consumers hard-assume exactly 90° and fail *silently* (card drawn where you can't click): mobile `renderedCardContains`/`getTargetingArrowOrigin`, desktop `PlayArea.getCardPanel`; plan lands the hit-test generalization FIRST at an unchanged 90° (pixel-identity = the refactor's own gate, upstreamable alone as a latent-correctness fix) before the `UI_TAPPED_CARD_ANGLE` pref. Latent bug found en route: `CardUnTapAnimation:288` hardcodes `-90` instead of delegating to `getTappedAngle()`. **Decisions 2026-07-26 (user): overlap over repack** — keep the 90° layout reservation and let shallow-tilted cards overhang neighbours (that's the table look, and it's the cheap option: zero row-packing changes; watch draw order so overhang reads as a table, not clipping); **mobile/Adventure prioritized, both clients eventually** — measured, not assumed: desktop is *modestly cheaper* (1 exact-90 hit-test vs 2, animation already generic vs a hardcoded `-90`, 1 settings screen vs 2 ⇒ mobile ≈2× desktop, not 10×), so the split isn't worth a split experience; mobile goes first because item 4 is mobile-only (same tree already being built/played) and upstream invests more there (12-mo: mobile 430 commits vs desktop 262 — real, but desktop is the slower of two LIVE UIs, not dead). **No version pinning needed for friends: the angle is a local render pref defaulting to 90 (= stock) and never crosses the wire.** **Deck-site sync GRADUATED into the track (2026-07-26, user) as items 6+7, sequence now 4→6→5→7:** item 6 = deck-site import on mobile — **verified the machinery already lives in shared `forge-gui/deck/` and `FDeckChooser` (desktop) is its ONLY consumer**, so mobile needs a button, not a port (watch: loader is synchronous 15s/30s timeouts — must not block the GL thread); item 7 = bulk sync by username. **The folders-vs-owner question resolved against folders:** Archidekt's owner listing (`/api/decks/cards/?owner=&ownerexact=true`, paginated) is genuinely unauthenticated, but **no public folders endpoint exists and folders are per-user organization = behind the account, so folder sync would REINTRODUCE auth rather than dodge it** — the auth-free property comes from decks being *public*, not from folders; organize instead by syncing into a *local* Forge folder per username (+ check whether the listing payload carries a category/tag for free client-side filtering). Archidekt has no API docs (open beta, warns about rate limiters) ⇒ tolerant parsing + the DC-pool ≥2s politeness norm + a hard cap; bulk = enumerate then reuse the existing per-deck provider path, not a second importer. **Storage layout decided 2026-07-26 (user asked format-folders + an invalid category; answered from code):** imported decks already live in their OWN flat store (`DeckUrlLoader.getStorage():150` → `decks/URL/`), NOT Forge's format-partitioned stores, so the layout is free — recommend **`decks/URL/<username>/` flat, format as metadata not a folder level**, because the format axis already exists twice in the UI (`FDeckChooser` is constructed per GameType `:75` and drives off `getDeckFormat()`; the URL pool goes to an ItemManager `:302` with its own filters) and because **re-sync must never move a deck** (adaptive "split when multi-format" layouts relocate decks the first time a friend uploads a second format); **mobile chooser CHECKED and the argument holds there too — DECIDED, not deferred** (`FDeckChooser.promptForDeck(…,GameType,…):90` + ctor `:126`, `DeckManager` ItemManager w/ filters); the narrower "does the picker hide format-illegal decks" question is **CLOSED** (user, code-confirmed): `FPref.ENFORCE_DECK_LEGALITY` (`ForgePreferences.java:205`, default `"true"`) runs at the lobby (`GameLobby.java`) for both clients, so the picker only routes/labels and conformance has an authoritative default-on gate downstream — which also retroactively strengthens "annotate, don't move" (quarantine would duplicate a check Forge already does at the only moment it matters). **Sequence 4→6→5→7 AGREED (user); horizon ~1–2 weeks** (Commander night ran 2026-07-26; next one is the target — no need to pull items 1–2 forward at that distance). **Items 1+2 user-confirmed from play 2026-07-26** (independently re-raised: in-place update instead of save-to-a-new-location; ~160MB "extra data download" after the update prompt on the mobile UI on desktop Linux = the empirical anchor item 2 asked for; pin whether a real save-as dialog appears or the handed-off installer is what prompts — different repro, same fix). **The "invalid decks" bucket has a prerequisite nobody would notice: there is no unknown-format state today — both providers silently collapse it to `Constructed`** (`ArchidektDeckUrlProvider:127-137` default; `DeckUrlLoader:142-148` smartValueOf-with-default + exception catch), so an Oathbreaker/unlabeled deck already files as Constructed and looks real ⇒ **first preserve the raw site format string / add an `Unknown` sentinel at the provider boundary**, then route ONLY unmapped-format decks into an **`Unsorted`** folder (not "Invalid" — the deck is fine, we just couldn't place it); non-conforming decks (`getDeckConformanceProblem()`) and unresolvable-card decks get **annotated, never moved** (they're still your friend's deck, and a deck that becomes legal later would have to move back). **Moxfield bulk DEFERRED to a stub** — gated behind a support-issued whitelisted User-Agent + Cloudflare, with a documented Nov-2025 issue where whitelisted agents still hit reCAPTCHA (relationship problem, not engineering; must not gate the feature). **Branch hygiene: never check out `playable` in a worktree** (one-worktree-per-branch would block the parallel workstream) — `playable-qol` @ `cc32912078` cut from its tip, worktree `../forge-play` (created 07-26); Anvil-side docs stay on `main`. Not on the M3/M4 path. **FIRST CODE SESSION 2026-07-26 (session 3): items 4-T1 and 6 BUILT** (scope agreed with user = these two only; `playable-qol` rebased forward onto `playable`'s tip `eed3a3e21d` first, clean FF). **Item 4 T1 took two commits because the three-layer diagnosis was one layer short — L2b, found only by running it:** `SpriteBatch`/`ShapeRenderer` each build an ortho projection from `Gdx.graphics` *in their constructor* and never revisit it, and `Graphics.begin()` only sets logical bounds (never the projection, never `glViewport`) — so with L1+L2 applied a window dragged 1280×720→1720×1382 still drew the UI at 1280×720 **anchored to the GL origin bottom-left, rest black**; fix = `Graphics.resize()` re-orthoing batch+shapeRenderer, called from `Forge.resize()` alongside `animationBatch` and `FrameRate` (`41cb5f5bc9` L1+L2, `61088aff57` L2b). Corroboration it was always the unfinished shape: **`FrameRate.resize(int,int)` already existed and was called from nowhere.** User decisions: session-only resize (no write-back to `SettingData` — the Adventure `videomode` combo stays sole authority over launch size), size floor not fixed size, **no aspect lock** (locking would refuse the very tiles T1 unlocks), L3 untouched. **Verified objectively:** `xprop` shows `minimum size: 480 by 320` and **no maximum hint** — that absence is exactly what `setResizable(false)` was publishing and exactly why KWin declined to tile; before/after captures confirm the viewport follows. **Backend question answered: only one arm exists** — Wayland/KWin session but LWJGL 3.3.3 ships no `natives-linux-wayland` classifier, so the app gets X11 GLFW via XWayland (KWin applies X11 size hints identically). **Item 6 (`db05b25b86`): mobile deck-site URL import** — `PROVIDED_DECK_URL` *is* in the shared `DeckType` arrays but mobile builds its combo from a hand-written `addItem` list that omitted it (selecting it would have hit `BugReporter.reportBug`); "New Deck"→"Provide Deck URL" opens `FOptionPane.showInputDialog` **pre-filled with the selected deck's `getSourceUrl()`** (= desktop's reload button folded in, no fifth button), fetch via `LoadingOverlay.runBackgroundTask` off the GL thread; **zero new localization keys** (all four strings already translated in all nine locales); static sweep clean on every other deck-type switch. **Both items owe a HUMAN runtime pass and it is the only thing left: `xdotool` cannot inject pointer input on this Wayland session** (XTEST warping refused — `mousemove` leaves `getmouselocation` at `0,0`; `windowsize` works because it's window management, not input), so resize was auto-verifiable and the deck chooser was not. Owed: actually *drag* to a screen edge for KWin's tile offer, play a full match at half/maximized, and paste an Archidekt link. **Lesson carried to item 5: this class of UI change is cheap to write and NOT verifiable by reading** — step 1's pixel-identity gate only means something if it is run. Left alone deliberately: `SplashScreen` has an **empty `doLayout`** and positions its buttons in `draw()`, so they can overlap the logo on resize (pre-existing, only reachable during the ~2s selector). Next on the track: item 5 step 1. **Human pass DONE 2026-07-27 (user): items 4+6 both VERIFIED in real play** (resizing + KWin snapping work; URL import works) — first QoL round CLOSED, residue = a full match at half-screen (expected incidentally). Same first run surfaced the 113 `dc-*` pool decks in Deck Manager: **the playable build shares Forge's user deck store with the research harness** (worker resolves decks by NAME via `SimulateMatch.deckFromCommandLineParameter` — `AnvilRun.java:302`; `anvil.pool install` targets `~/.forge/decks/commander/`), so GUI edits to `dc-*` files would silently change research generation (playing them is safe; user: leave them visible); worklist "shared user store" section + queued ~30-line rider = launch-time hash check vs `data/pool/decks/` beside the existing jar-hash gate. **Harness question answered with evidence: not the monitor** (session active, app visibly re-rendered) — Wayland input policy refuses XTEST *and* the ydotool/uinput path (tried, no reaction); **item 5's hit-test is pure geometry ⇒ unit-test the pixel-identity gate, no display needed** (cost: `forge-gui-mobile` has zero test infra today; desktop has 65 tests); Xvfb = the candidate for genuinely-visual residue (in repo, uninstalled; llvmpipe risk unproven). Ops note: the Anvil checkout may sit on the other track's branch — commit main-side docs via a temp scratchpad worktree (done twice 07-27). **Item 5 step 1 LANDED 2026-07-27 session 2 (`playable-qol` `d3bd019dc6`/`d52a43e6aa`/`514efc0dc7`): mobile tapped-card hit-tests + targeting-arrow anchors generalized to inverse rotation.** Open decisions resolved: shared `forge.util.RotatedRect` helper in `forge-gui` (pure-Java, no libGDX) with the TestNG geometry test in `forge-gui-desktop`'s existing tree — **no test infra added to forge-gui-mobile**; helper speaks mobile's `startRotateTransform` angle convention (Swing callers negate; both stock taps = −90 in it). Identity gate RUN: hit-test bit-identical to the legacy swap box at −90 over ~60M grid points (trig snapped at multiples of 90); bounding box (arrow anchor only) identical to ~1e-5 with the noise floor measured first (call-site float pivot rounding). **The derivation caught a live bug: on 180-rotated fields (local two-human non-hotseat matches — the Commander-night mode) the drawn tapped card sits offset h−w from the accepted tap box** (top ~40% missed, empty space below hit) because draw composes an outer 180° with a flipped +90 tap while the old hit-test was angle-blind; fixed via point-reflection before the base test, arrows likewise; untapped cards reflection-symmetric ⇒ unchanged. **Owes a human pass in a real local two-human match** (formula-verified only; XTEST wall). Remaining on item 5: step 2 (angle accessor + `CardUnTapAnimation:288` hardcode), desktop `PlayArea.getCardPanel`, then the pref (90/75/60/45). **Same session continued: item 5 BUILT end-to-end (`484fdb3df5`/`ac824cc606`/`33d8c64b41`)** — untap animation delegates to the accessor (the `:288` hardcode was also wrong on 180-rotated fields); desktop `PlayArea.getCardPanel` inverse-rotates via `RotatedRect.inverseRotate` (caller keeps its exclusive bounds; integer-grid identity test, zero mismatches); **`FPref.UI_TAP_ANGLE` default "90", values 90/75/60/45/30/15 (30/15 from play feedback), nine languages, mobile SettingsPage + adventure SettingsScene + desktop Graphic Options** (mobile caches `Forge.tapAngle` w/ defensive parse; desktop stores DEGREES as source of truth — `toDegrees(toRadians(90))` isn't guaranteed exactly 90 — radians derived for paint; in-flight guards read the accessor). Gates: geometry tests 6/6, desktop suite 336 green, shipping jar builds. **Step 4 draw-order answered statically: mobile overhang lands UNDER the right neighbour by construction (correct); desktop INVERTED (Swing paints lower-index on top)** — z-order surgery interacts with stack fanning, assess visually when desktop tilt is played; unreachable at default 90°. Owed: mobile play pass at 60° (the Commander-night check), the rotate-180 two-human check, desktop shallow-angle look. Upstream entry updated in sync (offer hit-test fix separately from the pref; Discord first). **Live test 07-27: angles VERIFIED in play** ("moving correctly, look sharp even rotated"); two same-hour riders — mode-selector overlap FIXED (`4ad8b4c94b`, the 07-26 "left alone" call reversed: the resizable window made it show every boot; bounds math → `doLayout`) and 30/15 added to the angle list (`255df64491`). **Second feedback round same session (`2e8cbd49b7`/`02b57b43aa`): battlefield slots pack to the configured angle** (pitch w/cos θ capped at the historical square — identity at 45–90°, tighter at 30/15; the five w==h card-rect sites now aspect-clamp) **+ `UI_SINGLE_COLUMN_ZONE_DISPLAY`** (side zone panels one column at the same card size, field gains the width; default off, both settings surfaces, nine languages). Owed: play pass on both — **DONE 2026-07-27 (user, screenshot): shallow rows + one-column hand both verified in a real game.** **Item 7 BUILT same session (`96e3d0942a`): bulk Archidekt sync** — live probe corrected the listing endpoint (`/api/decks/v3/?ownerUsername=`, not the recorded `/api/decks/cards/`; pageSize ignored, http next-links, owner= silently ignored ⇒ client-side exact re-check), per-username flat folders, never-move re-sync, Unknown-format sentinel + lazy Unsorted/, annotate-not-quarantine, 2s politeness + 200/10 caps, zero-request unchanged-skip via new `Sync Updated At` metadata; mobile URL button → single vs user sync; 5 fixture-pinned tests. **QoL sequence 4→6→5→7 COMPLETE.** Owed: first live sync + play with a synced deck. **Third feedback round (`bdf280ef08`): input dialogs dock bottom on desktop** (the 40% OSK reservation never applies there) **+ card textures get mipmaps when filtering is on** (the no-mipmap "drawn ~1:1" assumption broke under UI_TAP_ANGLE: minified rotated text shimmered at 30° in a real 4-player game). **"Scaling again" diagnosed = the T2 freeze, structural** (Utils static-finals baked into downstream constants; live rescale = deep refactor). Workaround exists: videomode up to 2160p — launch at play size. T2 stays queued. **Part two: the persisting artifact was NOT T2 — it was a fifth frozen-size layer (`a46c9f58b3`): landscape BACKDROP screens (home behind hosted screens) got child re-layout on resize but never their own bounds, so drawBackground painted a launch-sized rectangle. Fixed: backdrop sized with the hosted screen. Resize-layer census now: hints/bounds/projections/selector/backdrop.** Backdrop fix user-verified; residual diagonal-text grain = the FONTS (`11038b92ba`): BitmapFont atlases were Nearest-filtered (iOS-only Linear precedent generalized; applied at the loader sites where the runtime font actually comes from). Sharpness verified by user. **UPDATER TRACK BUILT same session (`fc0b546555`/`ecbffd7116`): items 3+2+1 code-complete** — constants repointed at the fork (playable-branch atom; 404s boot normally, verified live); per-file delta from manifest.txt + raw.githubusercontent at the release commit (res tree IS the repo tree — zero extra hosting), hash-verified, res applied in place at splash, jar swapped post-exit by UpdateApplier (staged-jar spawn; Windows lock safe), real delta size in the prompt, all failures → legacy fallback untouched; `scripts/release-playable.sh` derives stamps from the built jar (54,654-file manifest); DeltaUpdateTest 5 green (file:// end-to-end + corrupt-abort). **Publishing = the remaining human step** (gh release create daily-snapshots + upload; commit must be pushed). **The founding playable-fork worklist (items 1-7) is now fully built.** **PUBLISHED 2026-07-28: the fork's first release is live** — the parallel workstream's commits aren't public yet, so the release was cut from a new publish branch `playable-pub` (worktree `../forge-pub`): the 19 QoL/updater commits cherry-picked onto shared base `5fbc2ac98d`, conflict-free with zero file overlap (verified), gates re-run (jar builds; DeltaUpdate/DeckSiteSync/RotatedRect 16/16); pushed as **`origin/playable` @ `cf8e71deef`** (the atom's branch name — note local `playable` ≠ `origin/playable` until that workstream goes public, then reconcile by merge), `daily-snapshots` prerelease carries jar/manifest/version/build (2.0.14-SNAPSHOT-07.29, 54,654-file manifest); live-verified: release URLs serve, `playable.atom` 200, raw res fetch at the manifest commit hash-matches. Owed: first real in-app update pass (needs a second release to move *to* — lands with the next publish). Remaining open on the track: the ~30-line pool-deck hash-check rider (research-side), T2 (queued, workaround = launch at play size), play-pass residue (first live bulk sync + synced-deck game, rotate-180 two-human check, desktop shallow-angle look). **SAME-DAY ARC THROUGH v5 (2026-07-28, five releases, all user-verified live): fresh install ran → first bulk sync crashed on an upstream landmine (stock `StorageNestedFolders.add` is a TODO stub + `getOrCreateSubfolder` used the parent's serializer — `getFolderOrCreate` never worked upstream; fixed via the load-time nested factory, `StorageSubfolderTest` validated-failing-first; rider: `LoadingOverlay` hides on error path — the crash dialog had been walled behind the modal overlay; >23h update gate retired since the delta prices updates exactly; v2) → sync UX round from play feedback (Enter submits input dialogs via opt-in `FTextField` submit handler; Archidekt profile/search URLs parse to a username — live-probed `/search/decks?owner=`, `/u/` 404s; live per-deck overlay caption "47/133"; ×9 locales; v3; 133-deck first sync = 5-10 min politeness math, re-syncs ~free) → the FIRST REAL IN-APP UPDATE found the delta path dead-on-arrival: the manifest path-safety check refused `..` as a SUBSTRING and the res tree has 39 legitimate double-dot filenames (`wastetown..tmx`) ⇒ every real plan aborted to a fallback that can't work (no installer jar published); fixed segment-wise (v4) + standing lesson: verify the production artifact through the production code path (post-publish routine now parses the published manifest with the shipped jar via jshell); en route learned a fix to the updater can't ship through the updater it fixes (one manual jar refresh = the unavoidable bridge) → item 8 (Adventure horizontal hand: `UI_ALT_PLAYERZONETABS` Off/Vertical/Horizontal existed all along, Adventure's settings entry was a commented-out stale checkbox, Adventure boot forces Vertical over the legacy boolean default = the vertical hand observed; revived as a three-value combo, v5) → **v4→v5 self-update VERIFIED end-to-end by the user (delta downloaded, applied in place, auto-restarted) — the updater track is closed and the whole publish pipeline is proven**; Horizontal verified in play (cramped on narrow windows — half the bottom width, upstream's layout — fine on ultrawide; noted, not scheduled). Pool hash-check rider BUILT (Anvil `89178d7`: `launch --pool` gates on installed-deck content hashes beside the jar gate; `verify_installed_decks`, pool suite 15 green — the shared-store hazard is now loud). Storage subfolder fix queued as an upstream candidate. Residue left on the entire track: rotate-180 two-human check, desktop shallow-angle look, T2 (queued).** **Desktop shallow-angle VERIFIED same day (user, 30° on the desktop client: same look as mobile, overlap reads intentional) — which also closes the step-4 draw-order question (inverted Swing z-order costs nothing in practice; surgery item retired). Track residue is now just the rotate-180 two-human check + T2.** **The rotate-180 check RAN same day and found a STOCK bug deeper than the hit-test one: mobile `Graphics.startRotateTransform` idt()'s the live matrix — nested transforms NEVER composed (the rotated-field angle negation was a hand-calibration to that breakage, exact only at 90°; the empty sideways stack = the same root cause, reachable in pure stock). Fixed (`b65a74cc85`, transforms save/restore+compose, negation deleted, pixel-identical at 90°), shipped v6 (`c8ebbbd813`), user-verified by screenshot: rotated-side tapped cards now render the table look at 30°. Queued upstream (bundle with/after the tap-angle pitch). Zoom-popup-upright on rotated side = stock, noted as polish. Track residue: T2 only (+ trivial incidental checks).** **MULTIPLAYER HARDENING TRACK CLOSED 2026-07-30 and OUT OF HOLD-BACK: [multiplayer-hardening.md](docs/design/multiplayer-hardening.md) is the public record.** Ran alongside the QoL work from 07-26 and was deliberately invisible in every public artifact (no devlog, no map entry, no worklist reference, enforced by pre-push hooks on both repos) until upstream had fixes — the hold-back covered the *fact* of the work, not only its contents. Arc: 9 findings (ours + a friend-commissioned third-party audit, integrated findings-only with its unverified CVE table dropped) → 6 patches on clean upstream master → combined into 4 private advisories at the maintainer's request → **3 merged to public Forge master, byte-identical to what we submitted** (`3e01055378` lobby slot-type authorization + pre-login lobby/decklist disclosure; `49e4bca114` wire-class allowlist + proxy refusal + decompression cap; `2df8aaab07` connection caps + login deadline + log/chat text injection), plus chat rate limiting split out at his request as public PR #11457 (switchable off outright, since it is policy not correctness). **2 findings DECLINED and now recorded as documented trust-model properties rather than as vulnerabilities we are sitting on** — the host is not an authoritative referee, so a modified client can cheat (upstream: "plenty of other methods also don't validate"; ~35 `getGui()` call sites take the client's word), and a disconnected seat is reclaimable by username alone (upstream: intended, since there is no global lobby). Both declined patches live on `playable` and are ours to keep. Advisories closed **unpublished** — his reason is distribution-model, not dismissal (rolling snapshots, no supported old releases to notify), and he merged every fix he agreed with within ~2 days. No embargo ⇒ this is writable up publicly, reframed in line with his conclusions. Method notes that generalize: **every claim watched to fail first** (caught 4 would-be green ticks, incl. a `while (allowChatMessage())` drain loop that *hung* instead of failing ⇒ standing rule: a test that can hang is worse than a test that fails; bound the loop, timeout every security test); allowlist derived from **131 measured wire classes**, false-rejection measured at **zero over 115 games**; **two deser sinks, not one** (an `ObjectInputFilter` on the decoder would have missed `TrackableSerializer`'s inner stream) and **`resolveProxyClass` bypasses any name-based filter** (proxy descriptors carry no class name); size is review currency (report 1 re-cut 14 files/+769 → 2 files/+46 was taken whole, test included); **a dependent branch inherits its parent's rejected content** (report 4 was built on the full report 1 and would have silently re-submitted 32 lines of a declined fix — rebuild from current upstream and grep the diff by name); and **a guard keyed on file *presence* decays into a false positive the moment the file goes public** (the pre-push hook refused a wholly public branch once `LogSafe.java` merged — narrowed on evidence, re-exercised in all four directions, now retired to `forge-sec-patches/*.retired`). **CLEANUP DONE same day (session 3): `playable` CONSOLIDATED and pushed — `origin/playable` @ `2900701083`** = old origin/playable (QoL, conflict-free merge from shared base `5fbc2ac98d`) + current upstream master (the 3 merged advisories arrive upstream-side; conflicts resolved upstream-wins where an advisory merged, ours-wins for the #11457 chat limits, both declined patches, and the superset security tests; the connive fork-local fix survived — upstream never fixed it); gate: jar builds, targeted security+QoL suite 30/0/5-skipped (gated networked). ~~The next publish ships the security work to friends' builds~~ — **DONE 2026-07-30: v7 published from consolidated `playable` @ `2900701083`** (`2.0.14-SNAPSHOT-07.31`, manifest 54,727 files, jar 69→84 MB from the upstream-master merge; full post-publish routine green: published-jar sha matches manifest+local, published manifest parses via the shipped jar's `DeltaManifest` incl. a `wastetown..tmx` entry, raw res fetch at the manifest commit hash-matches, `playable.atom` 200) — the security work now reaches every auto-updating client. **v7 was DOA in the field same day — the first real update attempt hit `HTTP 400` on `res/adventure/Realm of Legends/...`: the delta URL builder never percent-encoded paths and the res tree carries 30K+ space paths (v1–v6 deltas only ever touched URL-safe ones; the v4 `..` lesson's sibling). v8 published same day (`09974ab3ea`): segment-wise `encodePath` (HTTP e2e test validated failing-first on the field path — file:// was too lenient to catch it), bridge manifest (plain entries = jar only, the one plan ≤07.29 parsers can complete; full list on `#2 ` lines they skip), and res self-heal (equal-build + no stamp ⇒ size-only diff offers to complete; the jar hop alone would otherwise strand stale res behind the strictly-newer gate forever). Migration fully automatic: jar hop → restart → self-heal. Pre-publish gate now includes parsing the bridge manifest with a real pre-fix jar (1 entry) and the new jar (54,727). **The live two-hop pass RAN and PASSED same evening (user's install): jar hop + applier restart clean, self-heal completed res, stamp written, install verified consistent — incident closed. Its one wart became the next find: after the res-only apply the app exited without reopening — STOCK `RestartUtil` builds a command string for `Runtime.exec(String)` (no quoting, whitespace tokenization: the quoted java binary never exec'd, space paths split mid-path, failure swallowed in the shutdown hook) ⇒ stock desktop restart likely never relaunched for anyone; hop 1 worked only because our `UpdateApplier` already used ProcessBuilder. Fixed (`57e345f922`, ProcessBuilder + arg list, jar path from code source, RestartUtilTest 4) and published as v9 same night — friends' hop 2 now relaunches (the fixed jar lands before their res pass runs); upstream candidate queued (stock bug, standalone patch). **v10 same night (`68f48b77f7`): the resize-layer census gains its SIXTH member — HEADERS.** User field report at a half-ultrawide (≈square) tile: New Game showed a one-item "Constructed" bar, no other mode selectable — `FScreen.doLayout` picks top-bar-vs-sidebar from the HOSTED screen's width>height (home sidebar eats 0.35×height of the window) while `MenuHeader` re-derived from the global `Forge.isLandscapeMode()`; in the disagreement zone the popup menu painted as a one-item "sidebar" inside the header strip. Fix: `Header.sidebarLayout` flag records the layout FScreen actually chose; MenuHeader + TabHeader/TabPage key on it (also covers Settings/Load Game — same zone — and makes bottom tab strips horizontal by construction; Android unchanged). Owed: user runtime pass at the square tile — **VERIFIED same evening. The first Quest session then found a STOCK quest bug — v11 published (`692d166633`): BALANCED starting pool with ALL SIX colors selected generates ZERO cards, silently** (`populateBalancedFilters` multiplies preferred-color filters by the count of NON-selected colors — all-colors ⇒ empty ⇒ no filters ⇒ empty pool; any five of six → 244 cards; diagnosed from the save's basics-only pool + headless jshell reproduction of the user's exact settings after every default configuration probed healthy — the journal's `QuestDataIO` load-path warning dated the quest and kept the hunt honest). Floor-at-one fix, `QuestStartingPoolTest` (rides `AITest` card-DB init) failing-first + controls 3/3; upstream candidate queued; adjacent unguarded input noted (Boosters count 0 = silent empty pool, unfixed).** Fork repo tidied to exactly `master` (pinned research, untouched) + `playable`, worktrees `../forge` + `../forge-play`, remotes origin+upstream (advisory remote removed, 16 local branches deleted after reachability checks — PR branches `chat-anti-spam`/`determinism-hooks` live on origin, `test-11161` archived as origin/test-11161, pre-rebase stack held by tag `pre-rebase-20260725`); `origin/gamecopier-fidelity-fixes` deleted (PR #11203 merged). The pinned research fork has zero delta from any of this.**

## M6 (closed 2026-08-10, ADR-0050)

## M8 (archived verbatim at close, 2026-08-19)

- **M8 OPEN (2026-08-17, [m8-plan](docs/design/m8-plan.md)): one lever to a promotion attempt — natural-timing credit, probe-gated, with a pre-registered pivot.** **D1 RESOLVED same-day ([ADR-0060](docs/decisions/ADR-0060-d1-natural-timing-probe.md)): gate FAILED, decisively — the pivot is taken.** The probe (single NATURAL arm under the new OBSERVE directive, K=64 on `drill-selection-v5-active`, gate PINNED at design before generation: two-bin abundance ≥30% at minority ≥12.5% · RMS true Δwr ≥0.10 · headroom ≥1pp at play-weight 0.0495) ran pristine — 99/99 points, 6,335/6,336 completions — and measured **split abundance 3.0% vs the 30% pin** (71.8% of completions cast in-window; modal first casts often identical across all 64 samples; headroom clause also fails at 0.55pp). Within-natural timing variation does not exist at plan granularity: ADR-0051's natural-variation falsification extends from single decisions to plans; a natural-timing target has nothing to grade. **M8 proceeds on D2′: curriculum × rank-critic curation** (the only lever family that ever promoted, +1.98pp ADR-0031, one-shot per method ADR-0035 — critic-ordered is a never-run method; **entry gate RESOLVED same-day ([ADR-0061](docs/decisions/ADR-0061-d2prime-audit-resolution.md)): PASS — Spearman 0.4205 on 492 uniform K=8 anchor points (~1.85σ clear of the 0.35 pin, ABOVE the in-era benchmark 0.377: era transfer cost the ordering nothing) → critic-ordered curation FUNDED at pool scale 4×** (selectivity curve RISES through the cap: 1× 0.593 → 4× 0.699 in-band precision — no winner's curse). Gate had been pinned at design and amended pre-labels 0.45→0.35 (the smoke's validation read measured the in-era same-population benchmark 0.377 on cycle-3's real labels — the gate tests era transfer, not the known population gap; cycle-3 premise verified: the critic screened + anchored, never ordered). Fresh 2× stock 3,197 games → 622 addressable (1.1h, ~3,500 g/h); en-route instrument fix (ADR-0059 genre): `grindstone report` joined labels per GAME — multi-fork drillfiles silently lost 124/500 points; now per (game, turn), loud counts. 4× top-up done (631 addressable; combined pool 1,253) → `drill-selection-m8-critic` = the curation of record (320 entries at 3.9× selectivity, all critic-band, 18.75% ahead) → migration read: substrate STABLE in profile mode (seed-matched core inapplicable — fresh seeds + unchanged ckpt both sides; addressable 45.4→43.4%, luck-locked curves near-identical, collapse-deck Jaccard 0.37). **`d6-run17` LAUNCHED 2026-08-18 (`scripts/launch_d6_run17.sh`): run13 recipe verbatim EXCEPT the §6c pricing runs at the standing corrected 0.01/window (user decision: "verbatim" binds structure for comparability, not superseded components; delta vs run13 = curation + pricing), seed 20260821, evalset-v4 drill-eval every 5 → close = 2,000-game combined paired read vs 0.5373 ± 0.0112** + decomposition + battery. En route ([ADR-0059](docs/decisions/ADR-0059-instrument-corrections.md)): the boundary-exemption proof for the labels extension passed at the instrument's own noise floor, and two instrument corrections landed — run_forkcheck.sh had run ALL THREE ADR-0055 fidelity checks on the stale pre-rebase jar (annex determinism claim retracted; true era fidelity 11.2–11.4% + a ~0.5% wall-clock-nondeterministic class), and AnvilRun.jstr never escaped control chars (modal-spell newlines split labels rows; M7 campaign labels silently dropped rows — verdict stands, dose smaller than designed; drops are loud now). Probe instrument = standing asset (the cheap "does the policy vary here at all" screen). **Pre-commitment: a timing-branch TIE closes the credit-assignment family at this scale.** Rebase assessed and declined at open (6-day delta, zero pool intersection, no engine-path relevance); **next-boundary bundle** on the map watch list: rebase + multi-format model-side enablement (pool scaffold #8 landed at ADR-0055) + 2-arm campaign trim + copy-state divergence forensics. Carried-items audit: the M4 fork items ALL resolved at the ADR-0055 boundary (MayPlay `b361dfcb8f`, IndexOOB `9f0a2c0886`, MinMaxBlocker 07-25, targeting-retry closed-as-bounded) — nothing carries. M7 stale-data pass COMPLETE: training dirs done earlier; the runs-store kill list (~4.5G) executed by user, verified 2026-08-17 (all five patterns gone, keeps intact).

- **M6 OPEN (2026-08-05, [ADR-0038](docs/decisions/ADR-0038-m6-opening-sequence.md) + [m6-plan.md](docs/design/m6-plan.md)): the representation question — can we change what the gradient sees, and does that reopen improvement?** Headline gate = standing combined paired read vs **0.5316**, honest either way. **D1 RESOLVED (2026-08-05, [ADR-0039](docs/decisions/ADR-0039-d1-frozen-probe-resolution.md)): INTERMEDIATE — rising, sample-limited.** `scripts/frozen_probe.py` on the 3,750 banked K=8 labels: held-out Spearman policy trunk **0.395 (c1) / 0.455 (c2)**, d4 trunk 0.336/0.356 — every probe beats the reproduced critic floor (0.26–0.29) by +0.08–0.18, none near 0.7, curves RISING (c2 policy 0.403→0.413→0.455; ridge at max alpha everywhere = sample-limited; the 2K curve point is unreachable — ~1.5K train labels/era). Trunk concat + `[PLAN]` add nothing; **policy trunk out-ranks the full-vis d4 trunk both eras**; linear probe 0.45 vs trained-head 0.27 on the same `[STATE]` vector ⇒ the critic's blindness was partly HEAD blindness (standing corollary: ask "can the model see X?" with a probe on `[STATE]`, never by reading the trained head). **Per the pre-registered "between" procedure, next = D2-A labeling re-price (the sanctioned ordering), then a ~5–10K/era label-expansion tranche to extend the still-rising curve; path commitment waits on that curve.** **Re-price DONE (2026-08-06, [ADR-0040](docs/decisions/ADR-0040-d2a-labeling-reprice.md)): the labeler is ENGINE-BOUND — ADR-0015's "2–4× from micro-batching" falsified** (batching fully engaged; cost = JVM game-playing, ADR-0032's waiting ceiling). Fresh-sampling mode: 11.1 pos/h/worker deduped at w=16 (177/h fleet; 50K ≈ 11.8 days, stale 15 retired); **drill mode measured 2.5× cheaper (27.3 pos/h/worker, `drill-map-r11i19-k8`) because `-drillstop` skips mainline completion — the tranche generated in drill mode**. Crash tax measured (0.77% completions, ~12% re-launch waste) ⇒ D2-A.2 fork stability pass number-justified. `scripts/bench_labeler.py` = standing bench (dedupes by game+fork point — crash re-launches duplicate label rows). **PATH VERDICT (2026-08-07, [ADR-0041](docs/decisions/ADR-0041-extended-curve-path-verdict.md)): PATH B — representation-blind confirmed at the feature level.** Tranche A (2,224 offset labels, same games) + tranche B (`scripts/tranche_b.py`: 1,600 fresh iter-019 games → 437 fresh addressable losses → 1,274 labels from 426 NEW games) took c2 to **5,356 labels / 985 games**; the curve is FLAT 0.43–0.46 from 1K → 4.2K train labels, and the **diversity ablation (identical holdout, fixed n) shows old-only ≈ mixed — game-saturation excluded**. Frozen-`[STATE]` ranking ceiling ≈ 0.45–0.46 vs 0.94–0.97 achievable. **D2-B SESSION HELD (2026-08-07, [ADR-0042](docs/decisions/ADR-0042-d2b-design-session.md), user-confirmed): two probe-gated levers IN PARALLEL — B-1 derived-state features (transform-side from logged full-state obs, NO dataset boundary; families: race/lethality, clock, castability-vs-mana, material/card-advantage, commander-tax; probe = ridge on `[STATE] ⊕ features` at the benchmark positions) + B-2 partial-unfreeze ranking fine-tune (top-N sweep, era-scoped if built); encoder swap parked unless both fail. Aggressive-inclusion posture adopted: probes carry per-lever attribution, the graduated run bundles every probe-cleared lever; shared-trunk feed default; headline gate vs 0.5316 unchanged. Standing gate: any candidate representation must beat the 0.46 plateau on the frozen `frozen-probe-ext2-c2` benchmark before it earns a training run. Rising priorities recorded out-of-scope (map watch list): planning §3a tiers 2–3 + conscious mana payment §3c + sequencing eval — tier-3 search needs a ranking-capable critic, so M6 is upstream.** **B-1 RESOLVED (2026-08-07, [ADR-0043](docs/decisions/ADR-0043-b1-feature-probe-verdict.md)): the derived-state feature probe does NOT clear the gate.** Harness banked (`anvil/encoder/derived.py` 38 features / 5 families, leak-tested; `scripts/feature_probe.py` per-family attribution; `state` reproduces 0.4552 exactly): best `state+race` 0.4597 (+0.0045 = noise), LOO deltas ≈ 0, feats-alone 0.23–0.25; extended-alpha + MLP robustness agree. **Sharp finding: `[STATE]` linearly reconstructs the features (median R² 0.65; lethal-margin 0.94, life diff 0.88, TTD 0.86) — the trunk already encodes the arithmetic; it isn't the missing signal. B-1 out of the graduated bundle; B-2 partial-unfreeze is the live lever (prior raised — the deficit is not at the input); B-3 unparks only if B-2 fails.** En-route standing rule (conditioning's third sighting): every engineered aggregate gets a clip at birth (PT_CAP/LIFE clips in derived.py) + a probe-side per-feature std read. **B-2 RESOLVED same day ([ADR-0044](docs/decisions/ADR-0044-b2-unfreeze-probe-verdict.md)): CLEARS the gate — first lever past the frozen ceiling.** `scripts/unfreeze_probe.py` (RankNet pairwise on the c2 train split, game-grouped early stop, frozen holdout; cells ~0.3 min ⇒ seed replication free): 5 seeds/N paired on the shared holdout — N=0 control **0.4528 ± 0.0017** (ranking-trained head = the linear plateau exactly; frozen ceiling measured a third way), **N=2 = 0.4769 ± 0.0015 (every seed above every other cell, +0.024 ≈ 14 seed-sds)**, N=4 0.4672 (overfit), N=1 0.459. **Label curve RISING/STEEPENING (1K 0.443 → 2K 0.450 → 3.6K 0.477) ⇒ the lever is label-starved — opposite of ADR-0041's flat frozen curve.** Decisions: graduated build proceeds (bundle = B-2 alone, priced at N=2, value-tower/partial-layer discipline, era-scoped, 0.5316 gate unchanged); **label expansion re-priced parked → recommended-first; B-3 stays parked.** **CAMPAIGN LAUNCHED same day (user-approved): `scripts/tranche_c2.py`** — c2-only ~13–14h drill-mode tranche (p1 offset arms + p2 fresh games, target ~9–10K total c2 labels), marker-resumable, smoke-validated end-to-end. Pre-campaign session: D4 isotonic wiring LANDED (`isotonic_maps.py` + `early_doom --isotonic`; **finding: calibrated curation collapses addressable losses 437→77 ⇒ campaign curation runs RAW** for benchmark population consistency — calibrated-vs-raw is a next-cycle design input); holdout-freeze protocol (`label_merge.py` pre-spend filter + merge belt + identity proof; new labels are train-side ONLY, the frozen holdout is untouchable); mid-campaign probe checkpoints (`tranche_checkpoint.py`, continue-at 0.478, exit-2 pause); **fork stability fixes resolved NO-CHANGE by ADR-0025 (crash fix = dataset boundary, inadmissible mid-era; ~12% crash waste accepted; pass carried for the 50K-era campaign)**; two-layer stall alerts (in-process 75-min artifact-mtime watchdog + `anvil_watchd` dead-man registration w/ heartbeat). **CAMPAIGN RESOLVED 2026-08-08 ([ADR-0046](docs/decisions/ADR-0046-label-campaign-resolution.md)): 12.6h, freeze-proven, `labelset-c2-v3` = 8,683 train / 1,197 frozen-holdout c2 labels. Deciding sweep: N=4 = 0.4829 ± 0.0010 wins** (head-only rose to 0.4693 — the frozen plateau was partly sample-limited; N=2 converged into baseline; capacity follows labels). Best-N curve 0.4769→0.4829 per 2.4× labels ⇒ label expansion priced modest (~+0.006/doubling), no further buying. Standing instrument rules born: inner-val pool pinning (extension-labeled games are wholly train — the two-round ck1 lesson) + mid-campaign checkpoints sweep the two leading Ns (ck2's N=2-only read said "flat" while N=4 emerged). Same-day: **otag probe NEGATIVE ([ADR-0045](docs/decisions/ADR-0045-otag-feature-probe.md))** — function unencoded (R² 0.26) but not the missing signal; pool-expansion caution strengthened; co-occurrence → Tutor. **Hidden-info probe RESOLVED same day ([ADR-0047](docs/decisions/ADR-0047-hidden-info-probe.md)): full-vis windows buy ZERO ranking** (N=4 0.4790 vs masked 0.4829; N=0 0.4664 vs 0.4693; 3 seeds/cell) — replicates ADR-0039's d4-trunk observation from a second direction; belief head deprioritized for ranking; the build stays MASKED. **Elimination chain: not arithmetic (0043), not function (0045), not hidden info (0047), not label count at slope (0046); partially gradient-reachable (0044/0046). Remaining candidates for 0.48→0.9: deeper capacity / B-3 encoder, or a static-evaluation ceiling (rollouts search; a head does not — the tier-3 argument, now a named M6-closeout question). Next: the graduated build at N=4, masked** (value-tower/partial-layer discipline, era-scoped, standing 0.5316 paired gate). **BUILD + CYCLE 3 RESOLVED (2026-08-08→09, [ADR-0048](docs/decisions/ADR-0048-cycle3-resolution.md)): TIE — no promotion.** Path A (user-approved value-tower): `rank-critic-c2v3` built (inner-val-selected, holdout report 0.4833; era-scoped isotonic map `c2/v_rank`, ECE 0.11 raw → 0.0004 mapped) → migration read POSITIVE (addressable 576→422 same-game; 31% of the old curriculum = luck-locked noise; crash anchors ~2.5 turns earlier) → fresh critic-untainted stock (1,599 games, 45.4% addressable — replicates) → selection v4 (320 drills, D3 folded in: 18.7% ahead, winnable 20%; fork-index g=0 collision found + worked around, encoding fix filed next-era) → **`d6-run13`** (run11 recipe verbatim, in-loop critic unchanged for attribution, 20/20 clean): **combined paired 0.5199 ± 0.0110 vs 0.5316 ⇒ Δ −1.2pp ± 1.6 TIE; iter-019 stays ckpt of record.** Decomposition: winnable −2.4 (D3 residual NOT closed), **coin −9.1 ~3.5σ (the most-trained bin got WORSE — the policy moved, wrong direction)**, lost +5.6 (conversion trade inverted); veto elevated 0.215. **Two-week shape: every sub-quantity improved, strength flat ⇒ the bottleneck is one layer up. AUDIT chartered (user-approved, before any training spend): (1) behavioral delta run13-vs-iter019, (2) interaction-holding corpus probe (bait-them-out: never-explored ⇒ exploration layer; unreinforced ⇒ credit ⇒ rollout-value targets/tier-3; grown ⇒ recipe/eval), (3) veto/§6c-penalty telemetry. Tier-3 = strategic direction, not built on an unexplained flat substrate.** **D2 = the chosen path built + gated;** path A opens with the labeling re-price (ADR-0015's ~17 pos/h/worker was batch-1-server-era; micro-batching + w=16 recipe never applied to the labeler path) and the carried fork stability pass as pre-campaign gate (IndexOOB, targeting-retry, MayPlay `.get(0)`, MinMaxBlocker — at campaign scale, hangs are throughput). **D3 = curriculum composition as an independent arm** (winnable −5.1pp residual; recomposes selection v3 — still valid, no M5 promotion — never folded into D2, attribution discipline). **D4 = isotonic wiring into curation/doom tooling (early, unconditional) + Ante riders with any critic replacement.** Opening chore DONE (2026-08-05): disk pass — kill list verified (zero operational refs; calibration/drill tooling reads only kept dirs; ckpts of record live in `data/training/`), user signed off delete-all; nine dirs removed, `data/runs` 213G → 68G (~145G freed; detail in m6-plan §Opening chore). Explicitly deferred: pool expansion (the probe doubles as its readiness evidence — text-embedding generalization is the representation under indictment), iteration pipelining (on-policyness recipe delta), eval thinning (run12 seed-half lesson).

## M5 (closed 2026-08-05, ADR-0037)

- **M5 OPENED (2026-08-03, [ADR-0034](docs/decisions/ADR-0034-m5-opening-sequence.md) + [m5-plan.md](docs/design/m5-plan.md)): Grindstone cycle 2 — the compounding question.** Hypothesis under test (user's framing): fixing bad decisions moves the collapse points up-level; training on the NEW collapse points narrows toward a higher level of play — the drill loop as a ratchet, not a one-shot correction. Deliverables: **D1 cycle-2 curation/selection/evalset v3** (pipeline verbatim from cycle 1 on the `run11-i019-finalarm-s{0,1}` stores; **the migration read comes first** — cycle-2 loss profile vs cycle-1's 584-loss profile against pre-registered signatures, recorded BEFORE the run launches; **RECORDED 2026-08-04, verdict: one-shot-consistent** — addressable stock stable (576/61.4% vs 584/61.2%) but every distribution unmoved and 63.5% of cycle-1's addressable losses re-surface on the same seeds with crash turns unmoved; drilled-vs-undrilled conversion identical (28.9%/29.1%) ⇒ the cycle-1 gain was fully general, drainage-not-up-leveling; prediction on file: Δ2 < Δ1 via transfer/conversion, not stock; both critics replicate (`scripts/migration_read.py`, m5-plan D1 section)) → **D2 `d6-run12` compounding run** (run11 recipe near-verbatim; only forced deltas: init/pin iter-019, selection v3, w=16, fresh seeds; gate = standing paired 2,000-game read vs **0.5316** + evalset-v3 decomposition; closing ADR reports **Δ2 vs Δ1 = +1.98pp ± 0.71** with the slope verdict — either direction closes honestly) → **D3 critic calibration** (promoted secondary, cycle-3-facing, firewalled from cycle 2: the ~1,900 banked K-rollout labels vs the critic's 0.58-on-0.24 miscalibration; adopt on held-out improvement or documented negative; Ante correctness riders attached). Winnable-residual levers deliberately excluded from cycle 2 (verbatim discipline — they'd confound the slope); escalation (b) parked unless flat-AND-signal-indicted. Done-when: (1) v3 assets online + migration read recorded — **SATISFIED 2026-08-04** (D1 complete in one day: K=8 map `drill-map-r11i19-k8` 565/576 at 22.9% rollout vs 0.585 v_before = near-clone of cycle 1; anchor sweep same recovery shape; **selection v3 = 410 pts** (cycle 1: 409), **evalset v3 = 116** w/ pinned-policy baseline 0.3488, eras chained via iter-019 on v2+v3); (2) compounding read resolved — **SATISFIED 2026-08-05 ([ADR-0035](docs/decisions/ADR-0035-d2-compounding-read-resolution.md)): Δ2 = −0.58pp ± 0.73 (t=−0.79, 3,995 paired games vs run11's closing stores on matched standing+fresh seeds) against Δ1 = +1.98pp ± 0.71 ⇒ the slope verdict is ONE-SHOT** — `d6-run12` (recipe near-verbatim, 20/20 iters, zero guard trips) produced no measurable gain; gate not cleared, NO promotion (ckpt of record stays `d6-run11/iter-019`, baseline 0.5316); decomposition unanimous (stock undiminished 576 vs 584; held-out drill transfer flat −0.2pp vs run11's +3.9pp; conversion trade returned) = the migration read's pre-registered prediction held end to end; cycling an identical drill loop is NOT the standing cadence; successor lever = curriculum composition (winnable residual); escalation (b) stays parked (drills taught mid-run — signal quality not indicted); (3) critic calibration measured — **SATISFIED 2026-08-05 ([ADR-0036](docs/decisions/ADR-0036-d3-critic-calibration.md)): split verdict on ~3,750 banked K=8 labels** — absolute scale FIXED (isotonic: held-out ECE 0.30–0.36 → 0.025–0.048 ≈ repeat-noise floor; ADOPTED era-scoped for cycle-3 curation/doom labels) but ranking is representation-blind (Spearman 0.26–0.29 vs achievable 0.94–0.97 repeat-measure ceiling; post-remap residual winnable −0.56/coin −0.30/lost +0.16, global not deck-local — no calibration can fix it) ⇒ the rider's reading (2): **representation is the measured M6 candidate** (encoder enrichment or ADR-0015 rollout-label value targets that bypass the critic); ranking-from-rollouts made a standing rule; audit invariant mechanized (`scripts/critic_calibration.py`). **All three M5 clauses resolved — milestone close awaits user confirmation.**

## M4 (closed 2026-08-03, ADR-0033)

- **M4 OPENED (2026-07-28, [ADR-0027](docs/decisions/ADR-0027-m4-opening-sequence.md) + [m4-plan.md](docs/design/m4-plan.md)): Grindstone — a different signal source.** Identity: split, Grindstone spine + one promoted infra deliverable (user: mix efficiency work in). Deliverables: **D1 adaptation probe `d6-run9`** (standing recipe verbatim from `run7b/iter-014/{train,critic}` on the rebased fork, ~10 guarded iterations, fresh seeds — resolves the re-baseline mechanism question under *training*, which no late-M3 run did: recovery ⇒ opponent-fit, adapted ckpt = new baseline; flat ⇒ heuristic stronger, 0.5121 stands; closing 2,000-game read doubles as D2's curation substrate) → **D2 Grindstone v0** (regenerate early-doom/curation on new-engine games — the old 531-loss list only replays on `pre-rebase-20260725`, so it's method-validation, not a usable seed list; drill machinery = re-aim the parked `-rollout K -points M` apparatus from sampled to *curated* crash windows; drill manifest w/ full provenance; re-price economics under micro-batching; drill eval suite; **LLM filter + ddmin deferred** per user) → **D3 signal integration, (a) first** (drill-mixed generation at a priced fraction, learner unchanged; escalation (b) K-rollout advantage baselines → (c) per-action contrastive/expert-iteration, entered only on resolved-negative; gate = paired 2,000-game corrected read vs the D1 baseline) → **D4 serving-path throughput, PROMOTED** (generation ~80% of run cost, w=8 saturates ~189 rps ≈ 5.3 ms/request Python overhead; noise floor → profile → one measured lever; bounded: measured gain or documented negative). Track C expressiveness stays deferred; upstream = watches only (#11285 nudge via queued #11360 comment). Key insight recorded: K rollouts from one forked position collapse advantage variance exactly at the near-tie windows ADR-0024 diagnosed as signal-free — drills are the mechanical answer to the absent-signal verdict, not just extra data. Done-when: (1) re-baseline ADR + M4 baseline number — **SATISFIED 2026-07-29 ([ADR-0028](docs/decisions/ADR-0028-d1-adaptation-probe.md)): D1 CLOSED same-night.** `d6-run9` ran 10/10 under guards (zero halts, zero tripwires; post-collate cadence proven in production: train ~3 min vs generation ~28 min/iter); iter-009 closing read 0.5233 ± 0.0110 corrected, **paired +1.15pp ± 0.90 vs run7b-i14 (t=1.28) = directionally-positive tie ⇒ verdict: the heuristic got stronger** (opponent-fit predicted fast recovery and didn't deliver; residual ≲1pp); **baseline stays 0.5121, ckpt of record unchanged**; veto re-fit real and strength-neutral (first-attempt 0.118→0.082, third confirmation of wasted-intent); `d6-run9/iter-009` = designated D3 init (adapted, veto-clean); closing-read stores (`run9-finalarm-s{0,1}`, obs on) = D2's curation substrate, traces via the probe's own new-engine critic; the probe also re-confirms ADR-0024's absent-signal diagnosis on the new engine (standing recipe: 10 healthy iterations ⇒ ~1pp inside noise); (2) Grindstone v0 online end-to-end; (3) a drill-signal run beats the baseline outside noise (or escalation exhausted, negatives to ADR standard); (4) serving path profiled + measured improvement or documented negative. **D2.1 DONE same morning (records `data/runs/early-doom-run9-{i009,d4crit}/`): the curation shape SURVIVED the rebase — 584 addressable losses (61.2% of 954; old 531/59.6%), ceiling 0.82–0.91, luck-locked 18–38%, 208 single-turn ≥30pp collapses across 82 decks; two-critic agreement within 0.3pp on every headline number. The new-engine drill seed list is live (`early-doom-run9-i009/curation.jsonl`); user context corroborates the D1 verdict (Discord posts show active bot-tuning work in the rebase window).** **D2.2+D2.3 BUILT + SMOKE-GATED same day (fork `46e2ec7c16`, Anvil `b1b6117`, suite 103 green): drill machinery end-to-end** — fork `-drillfile`/`-drillstop` (explicit fork turns; gameless skips w/ accounting rows; labels `tt` join field; early mainline stop), `anvil.grindstone plan|generate` (curation → manifest pinning the source `run.json` replay recipe; argmax server on the manifest ckpt; obs ON per the M1 replay rule). **Gate: 7/8 source games reproduce exactly (also = the rebuilt jar's empirical game-path-equivalence proof); the 1 = the known ~1–2% heuristic wall-clock replay-drift class ⇒ diverged replays yield no label — miss-rate is an accounted statistic.** Economics re-priced: ~47 s/drill (w=4, K=8) ⇒ full 584-drill sweep ≈ 2.5–4 h at w=8. **Design-shaping smoke finding: ground truth at crash windows ≈ 33% model winrate vs critic 70–90% (two 0/8 games = error upstream of the crash; one 8/8 = draw-order luck) — fork-at-crash is often late; `--turn-offset`/`peak_turn` = the first D3 sweep axis.** Pinned hazards: `-drillstop` mainlines record pseudo-winners ⇒ drill mainline stores NEVER enter training ingest; store-side `drill` provenance tag + fork-session ingest = the open D3 integration work. **THE FULL 584-DRILL GROUND-TRUTH MAP LANDED same day (`drill-map-r9i9-k8/`, `grindstone report` verb formalized w/ multi-run supersede): rollout winrate 23.7% vs mean v_before 0.584; bins lost-at-crash 58% / long-shot 22% / coin 9% / winnable 12%; replay-miss 1.0% ⇒ the turn-offset/peak-turn sweep is D3's FIRST experiment (most crash windows sit after the real error).** The map caught two engine crash classes: **monarchy-transfer copy bug FIXED same day** (fork `6d728677d1` — `Zone.remove` leaves stale zone pointers on cached monarch/initiative effect cards; membership guard, 2 validated-failing-first tests, suite 299 green, 35/44 positions recovered, upstream candidate queued w/ evidence) and **MayPlayPlayer `.get(0)`-on-empty DIAGNOSED w/ deterministic repro** (9 positions; upgrades the carried IndexOutOfBounds item; fix deferred to the copy-ordering question — likely folds into the consolidation PR). **D2.4 DONE same day ⇒ done-when #2 SATISFIED, D2 COMPLETE (opened and closed 07-29): dual-policy server (`--drill-ckpt`: fork wids → ckpt under eval, mainline replay pinned — also D3's serving substrate) + `grindstone evalset` (176 held-out drills, stratified over ground-truth bins, membership recorded — D3 training must subtract) + `grindstone eval` (per-bin paired vs baseline).** Validation gate decoded: 176/176 labeled, 85% bit-exact reproduction, corr 0.862 = plumbing proven; exact identity unachievable (bf16 argmax near-tie flips + heuristic wall-clock), and **the winnable bin's −7.2pp re-measure = regression to the mean from selecting on noisy K=8 estimates — protocol pinned: evals pair vs the pinned policy's own re-measurement (`baseline_eval`, auto-used), never selection-time labels** (winnable true mean 0.794 under eval conditions, not 0.865). Suite 105 green. Critic-sharpening noted as the (b)-adjacent D3 lever: the 578 K-rollout labels double as a critic calibration set (critic 0.58 vs ground truth 0.24). **D3 OPENED same evening — experiment 1 (turn-offset/peak-turn sweep) RESOLVED POSITIVE overnight (`drill-sweep-lost-20260729/`, Anvil `faf9d01`): the 330 lost-at-crash drills re-drilled at 4 anchors (`plan --anchor {crash,peak}` + `--tag` per-arm run-dir isolation — report's later-supersedes aggregation would otherwise cross-clobber arms; `scripts/drill_sweep.py` with o0 re-measure baseline per the D2.4 protocol). Winrate monotone in fork earliness: at-crash 0.048 (bin validated, not selection noise) → −2 0.248 → −4 0.331 → peak 0.356 (+30.2pp paired); 81% of games have a fork point in the trainable band [0.25,0.75], only 46 stay ≤0.2 everywhere (luck-locked profile: gap 3.7, peak_v 0.645 — out of the drill pool); error localization: 75 games within 2 turns of crash / 53 in (−4,−2] / 52 peak-only. KEY DESIGN CONSEQUENCE: no global offset wins (o4/peak recover different games, 65 vs 56 exclusive) ⇒ drill fork turns selected PER GAME from the sweep's own labels; sweep labels also 3× the critic calibration set (~1,900). **Per-game selection SHIPPED next morning (07-30, `grindstone select`, record `drill-selection-v1/`): latest in-band [0.25,0.75] fork per game (fallback latest above-band — converting won positions is still a drill; luck-locked excluded), label sources merge with later-supersedes (re-measures over selection-time labels per D2.4), evalset holdout subtracted AT THIS BOUNDARY (training lists are built here) — 349 training drill points (334 band + 15 above / 44 excluded / 176 held out; mean sel winrate 0.416 ≈ variance-maximal; offsets 93@0 / 128@−2 / 87@−4 / 41 earlier; accounting exact: 584 = 569 usable + 9 all-crash + 6 replay-miss); `plan --anchor selected` consumes selection.jsonl directly (349 drills planned clean, suite 111 green).** **Fork-session ingest SHIPPED same day (fork `66fa3fe45e`, Anvil `add7326`): `-forkobs` makes drill completions store frames of their own** (separate `obs-forks.zst`; synthetic unique ids; fork provenance headers; per-completion announced seeds — kills the μ (g,s)-collision and identical-noise hazards found latent in the wire-only design; per-dec STORED serve-time hist — fork first-windows carry parent-ring context no loader reconstruction could recover) + `anvil.store ingest --forks` (fork frames only, **drill mainlines never ingest — enforced by construction**; games.jsonl synthesized from driver-computed end winners; per-fork-point labels cross-check; manifest `source: drill-forks` + parent provenance) + loaders (rl.py, dataset.py) prefer stored hist. Smoke end-to-end clean (16 frames, cross-check 2/2, castplan OK, 8/8 parent-ring hist rows on first window); suites 113 + fork 294 green. **Sampled+μ backend SHIPPED same day (`7823144`): server `--drill-sample`/`--drill-mu-out` (fork sessions sampled w/ μ, mainline argmax pinned; sampling a wire-only g=−1 header raises → loud decline, no μ ⇒ misconfigured runs can't emit trainable data); `generate --sample-forks` = one server+μ file per arm (synthetic-id namespaces overlap); gate: 2,787 μ-joined windows, zero mu_mismatch, rewards exact — fork-frame serve/train parity proven end-to-end.** **Driver drill phase SHIPPED + d6-run10 LAUNCHED same day (`608140d`): per-iteration rotating selection slice (drill stores join the iteration's store GROUP ⇒ §6d fresh→replay aging free; f by volume: 15 pts × K=8 = 120 completions/iter = 20% of fresh games, ~14% of fresh trajs; full cycle ≈ 23 iters), mini-run green (tripwire 0, crash-skips correct, no flags) — d6-run10 = standing recipe verbatim + drill phase, init run9-i009 (train+critic), mainline pinned run9-i009, 20 iterations ≈ 14 h, guards+watcher armed. **RUN CLOSED 2026-08-01 ([ADR-0030](docs/decisions/ADR-0030-d3-experiment-a-resolution.md)): guard halt at iter 16 (KL 0.085 + veto 0.154 + entropy together — real multi-signal drift, halted by design; 16 accepted iterations). Closing reads: iter-004 0.5254 / iter-015 0.5273 ± 0.011 corrected — vs baseline +1.4pp at t≈1.4 (tie, GATE NOT CLEARED); vs own init +0.25pp and i15-vs-i04 +0.05pp (dose-response exactly flat) ⇒ experiment (a) at f=20% = strength-neutral. BUT the drill evalset decomposition found the mechanism: held-out lost/long_shot +6.6/+6.7pp (real generalization, grew with dose) cancelled by winnable −9.7pp — and the training list was ~100% behind-positions BY CONSTRUCTION (the D2.4 evalset held out ALL winnable+coin bins). Rej-split: drill trajs carry 1.65× the rejection density (the §6c pressure behind the shaped-vs-masked flag that led the halt by 4 iterations). Ckpt of record + 0.5121 baseline unchanged; drill-evalset-v1 superseded for training-adjacent use (re-split required). En route: iter-9 driver crash (wedged-completion → abandoned-thread → fork-fd death → ingest death cascade) fixed at all three layers (fork `7249a41b60` owner-bound frames, Anvil `eb6381f` quarantine ingest) and the same wedge recurring later cost exactly one quarantined frame; standing systemd watcher `anvil_watchd` shipped (registration-file + pid/starttime/btime identity — the pgrep self-match class claimed three babysitters before the pattern was fully excised); targeting-retry hang class queued for forensics. DECIDED 2026-08-01 (user): **the bounded rebalanced (a2) run is the current plan** — stratified evalset re-split (whole bins never again consumed by holdout) → `select` re-run w/ ahead-positions in training → (a2) vs the same 0.5121 baseline; escalation (b) stays armed behind an (a2) negative. Watcher self-registration SHIPPED same day (notify.py `watch_register/unregister`; selfplay driver + final_read report their OWN pids, deliberate exits unregister, crashes leave the registration so the watcher fires GONE — the pgrep-derived pid class fully retired; watchd env-overridable + 3 tests). **(a2) PREPPED + d6-run11 LAUNCHED same day (session 4, zero code changes — the verbs already took the knobs): evalset v2** (`drill-evalset-v2`: winnable 22 / coin 17 / long_shot 40 / lost 40 = 119 held out — ~⅓ of each small bin, whole bins never again consumed; baseline re-measure 118/119 w/ `baseline_eval` pinned; instrument cross-check: agrees with v1's re-measure within noise in every bin on the 99 shared games — v1's winnable regression 0.865→0.794 lived entirely in the 44 unpicked games, i.e. regression-to-mean is per-game and the D2.4 pairing protocol absorbs it), **selection v2** (`drill-selection-v2`: 409 training pts = 370 band + 39 above / 41 excluded, mean fork wr 0.470; **18.8% ahead-positions** = exactly the 77 winnable+coin games released from v1's holdout, mean sel_wr 0.714; rotation slices carry 2–4 ahead each, accounting exact 119+409+41=569), **d6-run11 = run10 recipe VERBATIM except fresh seeds 20261031 + selection-v2** (init + mainline pin run9-i009, 20 iters ≈ 14 h, guards + self-registered watcher) — one variable changed: mixture composition. Hypothesis under test: run10's winnable −9.7pp was the balancing term; ahead-weight converts the held-out +6.6/+6.7pp into net strength. Gate: standing 2,000-game paired read vs 0.5121 + evalset-v2 decomposition on candidates (v1 stays valid only for run10-era ckpts); (b) armed behind a negative. **RUN CLOSED 2026-08-02 ([ADR-0031](docs/decisions/ADR-0031-a2-resolution.md)): (a2) RESOLVED POSITIVE — done-when #3 SATISFIED.** Full 20 iterations, zero guard halts (the §6c drill pressure gone with ahead-weight); mechanism confirmed on held-out drills (lost +7.5pp t=2.4 / long_shot +7.4 t=2.0 persisted; winnable regression HALVED −9.7→−5.1 and shrank with dose — run10's inverse); strength: standing-seeds paired +2.00pp ± 1.00 (t=2.01, at the line) ⇒ fresh-seed paired confirmation of BOTH ckpts (seeds 20260711, matched load) replicated +1.95 ± 1.02 ⇒ **combined +1.98pp ± 0.71, t=2.77 over 3,994 paired games — first outside-noise drill-signal win**; vs own init +0.80 ± 1.01 (arithmetic-consistent). Fresh-seed-confirmation-on-marginal-t adopted as standing tiebreaker. **PROMOTED 2026-08-02 (user): `d6-run11/iter-019` = RL ckpt of record; new M4 baseline 0.5316 ± 0.0110 corrected (standing seeds 20260710; the 20260711 read = replication evidence). Winnable −5.1pp residual documented as known trade. All 0.5121-era comparisons are old-baseline. Drill curation must regenerate from iter-019's own losses before any future drill run (the run9-derived selection is stale vs the new policy).** Escalation (b) now opt-in, not ladder-required. **D4 CLOSED 2026-08-03 ([ADR-0032](docs/decisions/ADR-0032-d4-serving-path-resolution.md)) ⇒ done-when #4 SATISFIED — ALL FOUR M4 CLAUSES RESOLVED (closure = user decision).** Arc: profile = documented negative for serve-path code (0.05 ms Python-active/request vs 5.3 ms wall — the ceiling is waiting); the noise floor caught the chunk-tail artifact (single-round batches paced by the slowest worker, 11× tails — it manufactured a false "37% environment regression", retracted same day, and overturned the old "w=16 slower" read); the live defect was the §6d batch split (mirror 240 + heur 120/120 launch separately, so run11's flat chunk 30 was one-round/tail-bound). Landed: driver `batch_chunk()` per-batch ≥2-rounds clamp (`7e34eab`) + **standing recipe adopts w=16**; validated end-to-end in mini-run `d4-w16val` (3 iters of the run11 recipe off iter-019, zero guard trips): **1589/1183/1291 g/h vs run11 median 1025 = ~+30% in-loop, ≈2 h returned per 20-iter run.** Standing lesson: cross-era gen_s comparisons are valid only at identical chunking. En route: `ANVIL_NOTIFY_SILENT` test seam (the watchd fabricated-crash test was popping a real "anvil t2 GONE" desktop alert on every full-suite run). Next: milestone-close decision; before any next drill run, regenerate curation from iter-019's losses.**

## M3 (closed 2026-07-28, ADR-0026)

- **M3 CLOSED (2026-07-28, [ADR-0026](docs/decisions/ADR-0026-m3-closeout.md)): all four done-when clauses resolved (ADR-0022 / ADR-0023 / D3 surface shipped / ADR-0025). RL ckpt of record `d6-run7b/iter-014/train/last.pt` at 0.5121 ± 0.0110 corrected on the post-rebase engine (+6.69pp over BC, t=4.32; parity vs the current heuristic); BC `d5-combat` stays the engine-bump certification policy. The M4 planning baseline is 0.5121 — all pre-rebase numbers (incl. the early-doom ceiling) are old-scale. NEXT: dedicated Grindstone/M4 design session on fresh context, seeded by the 531-loss curation list + ADR-0024's signal-source argument + the re-baseline mechanism question + [m3-candidates.md](docs/design/m3-candidates.md) leftovers; the collate refactor (the pre-run prerequisite) turned out to be ALREADY BUILT (`c09a755`, 07-26, a parallel session the status never absorbed — see the corrected learner paragraph below); upstream watch: #11285 unreviewed (the queued #11360 complementarity comment doubles as the nudge).** Original plan: **[m3-plan.md](docs/design/m3-plan.md)** (seeded from [ADR-0021](docs/decisions/ADR-0021-m3-opening-sequence.md), M1/M2-plan pattern). **Identity: split milestone, RL spine dominant** (user decisions: split / rebase-closes / Grindstone-flex). Spine: **D1 commander-tax fix + first-attempt-veto metric** (the iter-013 forensics cause candidate; derivability check expected reader-side, no corpus regen; `load_compat` feature shim; user lean = introduce the feature early) → **D2 guarded scaling runs** (find the recipe's ceiling; reserve levers in order: counterspell-hold examination → §3d-pinned veto penalty → per-head entropy floors). Parallel: **D3 upstream track at community pace** — the #ai-plotting reply (posted 2026-07-16) drew khaliostr/manabrew collaboration enthusiasm and **maintainer Hanmac filed [forge#11260](https://github.com/Card-Forge/forge/issues/11260) (per-Game ID counters, self-assigned + tool4ever + MostCromulent) next morning**; our design-input comment posted 2026-07-17 (sibling statics, counter-must-travel-on-copy constraint from #11203, forkcheck offer); our authored surface = ThreadLocal MyRandom + pre-shuffle sort + tests (joint PR w/ manabrew), #445 protocol review vs the 109-callback census, endstep concurrency evaluation (failure path tested, not assumed). **manabrew's Fireball SVar claim probed and does NOT reproduce** (`FireballRaiseCostTest`, fork `023e8c5da9`: cost raise applies correctly; excluded from the joint PR — every upstream claim carries a test). Close: **D4 = the #11161 rebase as the dataset-boundary event** (fold #11203 fixes + `thisTurnCast` rework + determinism work; forkcheck BC-ckpt gate; fork pinned through all M3 RL runs — pre-shuffle sort lands fork-side only here). Flex: Grindstone seeding from the 34 never-cast blind spots. Infra riders: VRAM elasticity (task #12) before the next long run, guard-halt push notification. Done-when: (1) veto cause resolved on fresh-RL forensics + first-attempt veto ≤ D5 baseline — **SATISFIED 2026-07-19 ([ADR-0022](docs/decisions/ADR-0022-d1-closeout.md), D1 CLOSED)**; (2) a guarded run beats iter-012 outside noise at the 2,000-game corrected read; (3) upstream surface submitted w/ tests + #445 gap list delivered; (4) rebase complete, gate green, fork-local copies dropped. **D1 arc (2026-07-17→19): the "obs gap" was a featurization gap** (`cmdcast` in every dec record since M1 D1; transform v4 `cmd_tax` on command-zone commander rows, zero fork changes/regen) **+ first-attempt veto metric** (chain-independent; D5 bar 0.1358 argmax) **+ the §6c reframe: feature and penalty are ONE deliverable** — d6-run4 (feature-only control) falsified feature-alone (0.242→0.403 argmax over 10 iters; under re-ask every chain attempt shares the window outcome ⇒ no advantage differential to train the feature), d6-run5 (λ=0.02 rejected-intent penalty on vetoed casts + combat drops, census-reconciliation-gated derivation, casts/game anti-passivity guard, guard-kl 0.06 after a transition halt) collapsed first-attempt veto 0.34→~0.10 in 5 iters and completed the project's first full 20-iteration schedule. **Closeout numbers: 2,000-game read on iter-9 ckpt raw 0.5185/corrected 0.5171 ± 0.0110 — dead tie with iter-012 paired (−0.30pp ± 1.12); post-first-cast commander vetoes −72% paired (1.567→0.438/game); veto reduction cost zero strength and bought zero** (the D3 lesson's RL echo). En route: the silent MinMaxBlocker illegal-block-discard class discovered (§6c reconciliation dive; census-blind, derivation catches it; worklist has the realizer fix); §6 anomaly rule re-based to shaped return. **Ceiling datum for D2: late-run paired arms declined 0.540→0.475; this recipe (lr 2e-5, 480 g/iter, masked critic) does not push past iter-012 — D2 needs a different lever (scale at lower lr / full-vis critic in loop / rollout labels under micro-batching). RL ckpt of record UNCHANGED (d6-run3/iter-012); d6-run5/iter-009 = the veto-clean init candidate for D2 runs.** **D2 OPENED on §6d mixed-opponent generation (2026-07-19→21, user picks: this lever first, init run-5 iter-9): d6-run6 (heur_frac 0.5, seeded-personality opponents, iteration store groups) ran 20/20 — arms 0.530/0.5025/0.530/0.5400 with NO late decline where run-5 fell to 0.4750 on the same paired seeds: §6d = a STABILITY lever (mirror-overfit decay eliminated). Gains absent and diagnosed: the per-batch reward split read the heur-half (τ=1 sampled play vs the eval opponent) at 0.5064 ± 0.0081 — sampled play sits at PARITY with the heuristic (~2pp τ tax lands exactly on the signal), so the lever was mispriced by the temperature gap; τ is upstream and the two compound. Next: τ-strength curve on run-6's best ckpt → run-7 at generation τ≈0.5–0.8 with §6d kept (learner plumbing prerequisite: μ-recompute tripwire must use generation τ from mu meta); then full-vis critic. En route: desktop-logout killed the attached run mid-iter (~20 min lost, phase-idempotent resume) → detached-launch convention adopted (setsid nohup + pgrep watcher, memory note).** Upstream track same span: **draft PR [forge#11285](https://github.com/Card-Forge/forge/pull/11285) opened** (determinism hooks re-authored from manabrew's `d658cbc757` w/ co-author credit + 2 refinements: InheritableThreadLocal, sorted-iteration; 3 tests, suite 289 green, forkcheck twin 40/40 + 0 statics on the verification jar), **#11260 getCardForUi design input posted** (gameless UI cards → static negative-counter fallback), **#445 review DELIVERED** (6-item gap list posted, [manabrew-protocol-445-review.md](docs/design/manabrew-protocol-445-review.md)) — D3's committed surface is fully shipped; watches: #11285 review, khaliostr's SVar repro, #445 responses (#445 since MERGED with our review as closing comment). **D2 τ-lever FALSIFIED + §6f critic launched (2026-07-21): τ-strength curve on run-6 iter-19 (`scripts/tau_curve.py`, 400 paired games/point) is FLAT in [0.3, 1.0]** — 0.5200/0.5025/0.5200/0.5075 vs argmax 0.5400, CRN-paired τ0.3-vs-τ1.0 only +1.25pp ± 2.0; veto exposure moves monotonically with τ but outcomes don't ⇒ the tax lives in near-tie windows at any temperature = the price of exploration, and the defect is CREDIT (variance never attributed back), not temperature — §6e resolved negative, run-7 keeps τ=1.0. Tempered-μ plumbing landed anyway (tripwire reads generation τ from mu meta; wrong-τ negative-control test). **Run-6 iter-19 formal read: raw 0.5325 / corrected 0.5321 ± 0.0110, paired +1.00pp ± 1.13 vs iter-012 (t=0.89) — directionally positive tie; done-when #2 open, iter-012 stays ckpt of record; §6d stability formal at 2,000 games** (`scripts/final_read.py` = the closeout protocol formalized, `paired_arms.py` validated on the run5-vs-run3 record). **§6f full-vis critic in the loop built + merged (93 tests): asymmetric V-trace** (pass-A values from frozen full-vis critic — `Featurizer.example(full_vis=)` ~3 lines, zero fork changes; per-iteration critic phase via `finetune_value --full-vis --trainable all`, advances only with accepted ckpts; masked head keeps training = free per-iteration masked-vs-full-vis A/B as `v0_masked`; anomaly basis split critic=raw/masked=shaped; leak boundary test-pinned — fv never labeled, masked stream byte-identical). Smoke 2×40 clean (critic chained, ρ 0.997+, tripwire 0, A/B live). **D2 CLOSED 2026-07-23 ([ADR-0023](docs/decisions/ADR-0023-d2-closeout.md)): done-when #2 SATISFIED — RL ckpt of record → `d6-run7b/iter-014/train/last.pt` at 0.5530 ± 0.0109 corrected, paired +3.26pp ± 1.16 vs iter-012 (t=2.80), first outside-noise supersession.** Arc: run-7 (critic @ lr 2e-5) guard-halted iter 6 in ~10 min — veto OSCILLATING 0.151→0.099→0.306 with KL 3× run-6 at the same lr = step size vs the critic's sharper gradients (ADR-0017 extended: lr brackets are per-signal-regime; with-critic bracket now (1e-5 stable, 2e-5 unstable)); run-7b (lr 1e-5, all else identical) ran **20/20 with ZERO guard halts/tripwires** — arms 0.4950 (real consolidation dip, recovered) → 0.5525 → 0.5625 (i14) → 0.5475; the critic stayed calibrated all run and v0_masked never diverged (§6f info-gathering caveat = watch, unobserved). Standing recipe = §6d mix + §6c penalty + §6f critic @ lr 1e-5. En route: detached launch survived a desktop logout-era lesson's twin (guard halt at 01:15 push-notified by hand — driver push hook queued as QoL); sleep-inhibitor-during-runs convention adopted; `card-addition-pipeline-sketch.md` (upstream tiers, two-sided confidence gate on RemAIDeck/blind spots, cadence argument). M3 state: #1 ✓ · #2 ✓ · #3 shipped pending #11285 review · #4 rebase open. **Early-doom analysis DONE (2026-07-23 session 2, `scripts/early_doom.py`, records `data/runs/early-doom-run7b-{i14,d4crit}/`): the ceiling is NOT binding** — full-vis critic per-turn over the 2,000-game closing read, two independently-trained critics agree within ~1pp: luck-locked losses 39.2% strict (never v≥0.5) / 17.8% conservative (never v≥0.3) ⇒ eval ceiling 0.826/0.920 vs current 0.5530 (false-doom on wins ≤1.6%; reliability bins within ~2–5pp); **curation signal delivered: 531 addressable losses with ranked value-crash windows (185 single-turn ≥30pp collapses, 102 decks) = Grindstone drill seeds with full provenance**. **Sequencing decided (user, 2026-07-23): scale probe → rebase → Grindstone/M4. d6-run8 (960 g/iter, traj-per-step 8, critic-steps 4000, init run7b-i14, all else run-7b verbatim) RESOLVED NEGATIVE ([ADR-0024](docs/decisions/ADR-0024-run8-batch-lever.md)): guard halt iter 18 (veto 0.217, §6 anomaly concurrent), best-by-arms i14 read 0.5340 ± 0.0110 corrected at 2,000 games, paired −2.00pp ± 0.99 (t=−2.03) vs run7b-i14 — the 400-game 0.5675 was a flattering fluctuation the promotion protocol caught. Ckpt of record UNCHANGED. Diagnosis sharpened: the near-tie residual is absent SIGNAL, not gradient noise (variance halving didn't help) — reaching the measured headroom needs a different signal source (Grindstone drills / rollout labels / post-rebase); batch axis joins the falsified-lever list, lr brackets now per-batch-size too (2× @ 1e-5 = edge-adjacent).** En route (07-23→25): early-doom analysis (ceiling NOT binding: luck-locked 18–39% of losses, eval ceiling 0.83–0.92, 531-loss curation list = Grindstone seeds, `scripts/early_doom.py`); VRAM elasticity task #12 merged (OOM-halving segs, grad-accum critic split, driver seg autotune, 98 tests); driver notify hook + PDEATHSIG sleep inhibitor; detached-driver stdout was block-buffered 36h (zero narration in log — watchers starved silently; line-buffering fix merged, lesson: verify watchers on a real event); upstream determinism convergence 07-24 (#11360 = our D1 twin-residual class fixed upstream, #11358, #11365; #11285 MERGEABLE/awaiting review; queued: #11360 complementarity comment). **D4 CLOSED (2026-07-26, [ADR-0025](docs/decisions/ADR-0025-d4-rebase-closeout.md)): the rebase landed and M3 done-when #4 is SATISFIED — all four M3 clauses now resolved; M3 closeout is the next session-level decision.** Fork rebased onto upstream `3e3818f1ba` (last 07-24 commit — deliberately not tip: excludes a landed-and-reverted AI change #11379/#11391 and the hours-old #11388, since the pin becomes the teacher/opponent for all of M4); 28/28 replayed conflict-free, `42e15f4822` dropped because **#11203 came home byte-identical** (GameCopier/Game/Player left the fork diff entirely — the fork's whole engine delta is now **14 lines in two files**), determinism hooks (`f4fc37db3e`, the #11285 surface) cherry-picked in, old tip tagged `pre-rebase-20260725`. Gates: Java 292 / Python 98 green; **`forkcheck -twin` divergence 16.5%→7.0%, chain 2/200→1/200** (the #11360 dividend, exactly as the worklist predicted); **`-grpc` gate certified with `d5-combat` — twin determinism 40/40**, 0 statics/transport/fallbacks; 1,701/1,701 pool cards resolve; **census on 113 decks: 120/120 decisive, turns median 21 vs 21, callbacks/game 1,117 vs 1,136** (teacher decision *volume* unchanged). **Connive regression found and fixed:** upstream #11355 ("Recruit: add Effect") handed `discard()` an immutable `Map.of(...)` while `SpellAbilityEffect.discard():906` writes back into it — every connive resolution threw, `RecruitEffect` shipped with the same bug, **the rebase inherited it and it cost a full 4,000-game read** (28 UOE crashes in 2,000 games vs 4 in all pre-rebase history, concentrated in the 5 pool decks with connive cards); fixed fork-side both sites, pinned by `ConniveDiscardMapTest` (validated failing first), 0 UOE in 2,000 games after; **deliberately not upstreamed** (Hanmac owns the fix; no caller reads the map back so every plausible maintainer fix is behaviorally identical — expect a conflict on those lines next rebase and DROP OURS). **The re-baseline (four 2,000-game Ante-corrected reads, run sequentially at matched load): RL `run7b-i14` 0.5530→0.5121 ± 0.0110 (−4.09 ± 1.55, t=−2.64); BC `d5-combat` 0.4616→0.4452 ± 0.0109 (−1.64 ± 1.54, t=−1.06); difference-in-differences −2.45pp ± 2.19 (t=−1.12) = NOT significant** (the control was run to separate uniform-shift from RL-specific and does not resolve it — stacking two differences inherits SE 2.19pp; the power calculation belonged before the runs). **RL over BC survives robustly: +6.69pp ± 1.55 (t=+4.32)** — the core M2/M3 claim holds, ckpt of record UNCHANGED. **But against the heuristic the agent is now at PARITY: +1.21pp ± 1.10 (t=1.10), where the old engine read +5.30pp (t=4.86) — ADR-0019/0023's "first teacher-surpassing read" was measured against the OLD heuristic and does not hold on the current engine.** Leading explanation: the heuristic got stronger (26 forge-ai commits; maintainers actively tuning AI behavior), supported by BC's first-attempt veto *falling* 0.1395→0.1072 (actions land more easily; the opponent simply plays better); opponent-specific-fit loss (§6d family) is an unexcluded second mechanism. **All pre-rebase winrates — including the early-doom ceiling 0.826/0.920 — sit on the old scale; the M4 planning baseline is 0.5121, not 0.5530.** Fork-discipline convention amended (behavior-identical patches are not boundaries, proven empirically by matching forkcheck trace hashes — never by argument). Carried forward: `IndexOutOfBoundsException` class (2 crashes, both deck `dc-863943`, early + seed-pinned — replay with `-Danvil.crash.trace`, plausible upstream filing); ~~`final_read.py` riders~~ **DONE same day (`e012736`, the unabsorbed parallel session): shared notifier `anvil/training/notify.py` (nonzero-exit-only after a cried-wolf `--help` false alarm), `final_read` auto-derives + passes `pool_version`, stdout line-buffered, learner default workers 6→12**; pool-manifest mtime-selection hazard **still open and now HIGHER stakes — `final_read` provenance flows through `latest_pool_manifest()` (`pairs.py:22`, mtime-sorted), so a stale-mtime pick stamps wrong provenance instead of just launching the wrong pool**. **Learner speed diagnosed AND FIXED (`c09a755`, 07-26 — landed in a parallel session; the record here said "designed, not built" until the 07-28 closeout session went to build it and found it done): the train phase was 62% of a run's wall clock at 87% LOADER WAIT** (throughput flat in `--seg` 64→512, *worse* with more workers, GPU ~10% busy, main process 83% CPU deserializing while 6 workers idled — cause: `RlTrajectories` yielded per-window example dicts and `collate` ran main-process-side, shipping thousands of small tensors through the DataLoader shm+pickle path to one consumer). Fix = **collate inside the worker at exactly the learner's seg size** (segmentation + padding identical; OOM elasticity survives by slicing the pre-collated segment on dim 0, inheriting the parent's padded width — numerically identical where re-collating would change it). **Measured: 0.442→2.062 traj/s at 6 workers (4.7×), 2.979 at 12 (6.7×) — more workers now HELP; run-7b's 16.6h train phase → ~2.5h, so generation becomes ~80% of a run.** Equivalence gate done honestly: **GPU bf16 is not bit-reproducible** (new-vs-new ≈ old-vs-new ≈ 3e-5, so a GPU diff proves nothing — the earlier "strict gate available" claim hadn't checked that); on CPU the model IS deterministic and the gate is real: **old-vs-new BIT-IDENTICAL across all 195 tensors**, new-vs-new as control, suite 99 green (100 as of 07-28). The next long RL run is unblocked. **Same session also measured the NEW bottleneck (`f9f344f`, `scripts/bench_generation.py`, RL-loop-exact config): generation is now ~80% of run cost and 8 workers already saturate the server's ~189 rps micro-batching ceiling (w=8 1,150.5 g/h vs w=16 1,078.5 — doubling workers is 6% SLOWER; 189 rps for a 42M model ≈ 5.3 ms/request = per-request Python overhead (featurization/gRPC), not GPU compute) — serving-path profiling = the queued follow-on throughput lever (fresh-context task; establish the noise floor first); incidental: post-rebase generation cost unchanged (1,150.5 vs 1,146 g/h, 0.4%).** Also landed that session, previously unabsorbed: `47fea51` (forkcheck `FIXED_HASH` stress instrument = the #11360 `-XX:hashCode=3` harvest, adopted; `rl.py --max-traj`; `bench_learner.py`) and per-row `phase` shares in learner metrics (`4038d96` — loader-starvation regressions visible without a special run). **Closeout executed 2026-07-28 (ADR-0026 — see the head of this bullet for the M4 seed material).**

## M2 (closed 2026-07-17, ADR-0020)

- **M2 CLOSED (2026-07-17, [ADR-0020](docs/decisions/ADR-0020-m2-closeout.md)): all four done-when clauses resolved (ADR-0011/0012/0015/0019); RL ckpt of record `d6-run3/iter-012/train/last.pt` at 0.5213 ± 0.0110 corrected vs the teacher (+6.75pp over BC, t=2.92); closing hygiene — forkcheck micro-batch re-cert matrix (batcher exonerated bit-for-bit at the BC ckpt; twin determinism = a policy property: RL ckpt 36/40 solo / 32/40 polluted, 0 statics anywhere; gate protocol amended: engine bumps certify with the BC ckpt), fallback class closed (combat-declaration answer edge ~0.1%, was always present), commander-tax feature gap = the veto-drift cause candidate ([run3-veto-forensics.md](docs/design/run3-veto-forensics.md)). NEXT: dedicated M3 planning session on fresh context, seeded by [m3-candidates.md](docs/design/m3-candidates.md).** Original plan: **[m2-rl-plan.md](docs/design/m2-rl-plan.md)** (seeded from [ADR-0010](docs/decisions/ADR-0010-m2-opening-sequence.md), M1-plan pattern). Deliverables in order: **D1 fork-API hardening** (the rollout contract = state fidelity + determinism, NOT trajectory reproduction — ADR-0002's 50% divergence is not a rollout defect; fix the two static bug classes → upstream PR #1, the `clonePlayer` controller-swap landmine, deterministic construction ordering + RNG capture/restore; `forkcheck` gains a mid-game-fork → digest-identical → deterministic-completion-under-bridge gate; measure fork+rollout throughput — it prices D4) → **D2 SA-level schema + BC retrain** (pointer scores candidate rows, PASS stays candidate 0; labels = exact `oi` + sa-string join ~69% with masked mass reported; order rung deleted from executor, ladder shrinks to shape→pay; nested-window loader fix rides; 3-epoch recipe; agreement reported on both bases; fresh δ table) → **D3 arms reprice** (δ=0 + rate-matched, ~12 min each) → **D4 rollout-labeled asymmetric critic under the BC policy** (pilot prices label economics first; gate = beat the ~0.60 matchup-prior BCE floor, lift final-turn AUC off 0.68) **+ Ante mirror certification** (§13 places it in M2) → **D5 combat constructs** (flexible slot) → **D6 first V-trace self-play** (δ=0 init, cap-aware reward, concession disabled, anomaly monitor from day one; ~24K games/day — first runs are 10s–100s of K games, not §14's 1–3M). Yield macros stay parked (plan decision #10); fork stays pinned through hardening (upstream rebase = separate boundary event, anchor #11161). Done-when: fork contract gated + PR #1 submitted; SA checkpoint on both bases + repriced arms; critic beats the floor + Ante certified; V-trace improves the arms outside noise. **D1 day-one (2026-07-10 evening): statics 12%→0** (face-down exile leak + effect-card field wiring, 60/60 repro seeds + 500-game run clean); **divergence 50%→13.4%** (root cause = GameCopier card-id renumbering, ~10-line fix, 204 sim tests pass; residual = this-turn copy gaps, 67 repro seeds); **twin determinism 99%** at heuristic lengths (`-twin` mode = the rollout determinism gate; residual 1% = hash-ordered iteration in decision paths, amplified 40× under random-index policy — the `-bridge` stress instrument; doesn't bias rollout labels, bounds bit-exact replay). Fork commits `42e15f4822`/`2e3c7e0c65`/`dc5b46e06e`; fresh baselines in `data/forkcheck/run-20260710{,-twin}/`. **D1 night session: rollout contract CERTIFIED under the BC policy** (fork `3aa1995658`: Obs per-game wire sessions — forks produce bridge observations with the store off, history ring seeded from the parent at the fork point; store path verified byte-identical; forkcheck `-grpc` = the full gate vs the model server): smoke 10/10 clean; **twin 40/40 = 100% rollout determinism under the model** (main-vs-fork 39/40; the 1 = the residual copy-gap class, seed 20260955, twins agree); 0 statics / transport failures / server fallbacks across 50 games + 90 fork replays. **D4 pricing: copy 7ms, rollout median 4.4s / mean 6.3s** → ~70 positions/h/worker at K=8; GPU batch-1 measured 59 rps → server micro-batching is the first D4 lever. The BC policy is a fidelity amplifier in the good direction (divergence 13.4%→2.5%, twin tail 1%→0: the model reads neither this-turn fields nor hash-order paths at random-policy rates). Record: `data/forkcheck/run-20260710-grpc/`. **PR #1 SUBMITTED (2026-07-10 late): [Card-Forge/forge#11203](https://github.com/Card-Forge/forge/pull/11203)** — fixes re-authored on clean upstream (`1eec01434e`; cherry-pick conflict-free), 3 differential regression tests in `GameSimulationTest` (each validated failing pre-fix; the negative check exposed a bonus latent NPE in the unfixed copier with monarch/blessing cards in play), full forge-gui-desktop suite 285 green; branch `gamecopier-fidelity-fixes` @ `2437820aee`, worktree `../forge-pr1`. **PR #11203 MERGED (2026-07-12, tool4ever, merge `1922ce411a`)** — approval note frames Copier/Snapshot duplication as technical debt to remove ("should also help with your project"): the scoped-PR-plus-follow-up proposal was accepted verbatim, the consolidation follow-up is maintainer-blessed (queued in [upstream-worklist.md](docs/design/upstream-worklist.md), not on the M2 critical path), and the fixes return to us at the next fork rebase (anchor #11161, dataset-boundary event; drop the fork-local copies then). The worklist also holds the 57-commit drift sweep: `thisTurnCast` is now SpellAbility-typed — author residual-divergence copier work against the new representation. Same evening: **Anvil repo made public** (LICENSE GPL-3.0 + README refresh; secrets/history scan clean; CONTRIBUTING.md AI-agents policy satisfied — co-author trailer + body note, 157 AI-co-authored commits upstream in 2026). **D1 CLOSED ([ADR-0011](docs/decisions/ADR-0011-d1-closeout.md)): rollout contract certified, workarounds retired (fork-at-start, digest tripwire), `forkcheck -grpc` = the standing engine-bump gate.** **D2 BUILT (2026-07-11 overnight, Anvil `31664a1`): SA-level schema end-to-end, retrain in flight.** Groundwork sweep (113,592 games / 6.11M casts, record `data/training/d2-sa-label-measure-full.json`): multi-SA hosts 16.7% of casts; **label join resolves 99.92% exact + 0.08% prefix-fallback, masked = 228 casts (0.004%) — NOT the ~31% priced**: the ambiguous class is identical-string duplicates (permission routes, same-rendering cost variants); collapsing identical (host row, normalized sa) pairs into one candidate makes the label exact — the model can't distinguish identical descriptors anyway, the executor keeps first-fit. Pinned vocab `anvil/training/sa_vocab_v1.json` (33,691 strings, held-out OOV 0.03–0.05%; **never regenerate in place** — ckpt embedding rows key off string order); pointer key += learned SA descriptor (37.3M→39.5M params; n_sa=0 loads pre-D2 ckpts unchanged); store reader now joins `oi` (was silently dropped); **nested-window history fix landed with measured 0 training-distribution shift** on the heuristic corpus (pure serve-parity insurance — no D3 attribution confound); both-bases eval (`agree_honest` SA basis = new headline, `agree_honest_host` = M1 continuity, `n_masked`) in every row; **serve path needed zero Java changes** (server sends exact option index + `host_level=False` from ckpt `sa_vocab_size`; realizer ladder degenerates to shape→pay on the singleton; census rung telemetry reads "single" = D3's order-rung≈0 check; pre-D2 ckpts keep the full ladder); parity test green on the new basis. **Retrain DONE (2026-07-11 evening, 774K steps / 15.12h / exit 0): SA-level agreement at PARITY with the host-level record — order rung retired at zero agreement cost.** 600-batch finals (val, n=148.9K): **honest 0.9748 SA-basis / 0.9751 host-basis vs d7-ep3's 0.9758** (−0.07pp, run noise); nonpass 0.9247 on the strictly-harder basis (names the SA; d7 host-basis 0.9339); target 0.9769; mulligan 0.9969; value 0.573 (floor, as diagnosed); n_masked = 1 per 600 batches; valpair ≥ val on everything (honest 0.9777). X head 0.852 full-val basis vs d7's 0.874 (−2.2pp, ~1.7σ — watch, not act). **Fresh δ table: model under-passes 1.7pp raw, δ=+2.180 rate-matches → honest 0.9837** (`d2-sa/pass_calibration.json`; D7's structure reproduced almost exactly — the pass boundary is a corpus+recipe property, not an architecture one). **`d2-sa/last.pt` = the D3 candidate checkpoint.** Worktree removed; `data/training/d2-sa/` is the record. **PR #11203 got its first review same day (tool4ever, CHANGES_REQUESTED-but-substance-accepted): consolidate GameCopier onto the GameSnapshot path now that ids are preserved** — archaeology confirmed the direction (snapshot path carries sibling bugs of two of our three classes; gaps: advanceToPhase, PRUNE_HIDDEN_INFO); replied same day (scoped PR now, consolidation volunteered as follow-up PR gated by forkcheck, snapshot-path fixes offered as fold-in); follow-up queued in the worklist, awaiting their pick. **D3 DONE same evening + D2/D3 CLOSED ([ADR-0012](docs/decisions/ADR-0012-d2-d3-closeout.md)): arms are a statistical tie — δ=0 0.4625 ± 0.0249 (D8: 0.4675) / δ=+2.180 0.4400 (D8: 0.4550), paired seeds; 800/800 decisive, 0 crashes (D8 0.5%), 0 fallbacks (117K requests); order rung RETIRED in play** (rungs = single+modal only vs D8's 31% order). **The honest resolution: the 31–36% "largest known winrate leak" did not cash out as winrate** — kind-prior first-fit was a good proxy for model intent, and the exact-SA answer forfeits the ladder rescue (veto 7.8%→10.0%, casts/game 33.5→26.8). D2/D3's purchase = the RL prerequisite: model-owned action identity, exact labels, per-SA veto decomposition, zero agreement cost. New named leak candidate = the veto class (~10% of cast attempts, queued behind D4/D6; serve-time re-ask-on-veto is the candidate lever). X ">16" clamp fires 0.4% of casts in play (watch in D6). `scripts/arms_report.py` = formalized standing per-checkpoint eval (validated against the D8 report exactly); record `data/runs/d3-arms-report.json`. **M2 done-when #2 SATISFIED.** **Pre-RL scope rule adopted (ADR-0012 addendum + plan decisions #11/#12): the BC stage is finished as a strength program** — pre-RL work is justified only by RL expressiveness, label/obs correctness, or eval sharpness, never projected BC winrate; arms demote from goal to instrument. Consequences: **D5 combat = hard prerequisite for D6** (its BC arms will tie by construction — not a failure signal); **Ante certification moves to the FRONT of D4** (eval resolution ±2.5pp is the binding constraint; certify the variance-reduced arm before the critic pilot). **D4 OPENED (2026-07-11 night, Anvil `17b0bf8`/`d7857a4`): Ante v0 ledger built end-to-end** (`anvil/ante/ledger.py`+`certify.py`: AIVAT corrections via observation surgery over logged full-state records — openers as uniform k-subset deals, draws only when provably uniform [order-methods poison game-long, known-bottom tuck exclusion, seen-id check, exact library-multiset self-check per node], die roll via leave-one-out on-play winrate; zero Java changes; ~13 corrected nodes/game, 0.4 s/game on GPU, zero-mean t<1 in every shakedown; `(A,A)` mirror pairs files just work). **The shakedown found ADR-worthy corpus poisoning ([ADR-0013](docs/decisions/ADR-0013-winner-label-poisoning.md)): obs `end.winner` was a post-elimination live-list index — ~always 0, wrong ~50% of games — so every value head to date trained on SEAT NOISE** (d2-sa value head vs true outcomes: AUC 0.506 = chance even at ≤2 turns from end; the 0.573-"floor"/"final-turn-AUC-0.68" narrative was measured against broken labels; arms/winrates unaffected — games.jsonl always carried truth, verified 492/492 vs final life totals). Fixes: fork `06dd428313` (registered-players index, verified 24/24 on smoke), reader-side `TrajectoryStore.winner_seat()` join (whole 113K corpus recovered, no regeneration), validator two-record cross-check, 50 tests green. **Corrected labels + a 38-min value-head-only probe (trunk+policy frozen = RL init untouched): BCE 0.5495 / AUC 0.788 overall (600 val games, 354K windows); turns-from-end AUC 0.989/0.949/0.971/0.927/0.861/0.70/0.55 at 0/1/2/3–4/5–8/9–16/17+; calibration clean — `d4-valuefix/last.pt` = interim value head of record.** The trunk read boards fine all along; only the head's labels were garbage. Ante correction magnitudes rose ~30× under the fixed head (opener 0.083, draw 0.020), zero-mean held. **ANTE CERTIFICATION PASSES (2026-07-12, [ADR-0014](docs/decisions/ADR-0014-ante-certification.md); Anvil `9692bfe`, record `data/runs/ante-cert-20260712-v11.json`):** 12,800-game mirror landed clean (12,793 decisive, ingest 16.8M decisions verified, validator zero winner-mismatches = the ADR-0013 cross-check's first live pass); raw seat-0 winrate **0.4953 ± 0.0044** (mirror sanity ✓); **the ledger's own zero-mean test caught a v0 modeling error** — openers t=+2.72, localized entirely to mulligan re-deals (deal#0 t=+0.52 clean / deal#1 t=+5.46): **Forge's London mulligan tucks BEFORE the keep decision**, so re-deal keeps are choice-filtered best-k-of-7, not chance outcomes; v1.1 excludes them (6,572/31,840 nodes; correct anchor = the tuck dec's pre-choice 7, queued with the critic upgrade). Post-fix: **every class zero-mean (opener +0.52 / draw −0.33 at n=124K); β=1 AIVAT adds variance at current critic quality (1.074) but the fitted control-variate β̂≈0.21 gives var ratio 0.9936, CI90 [0.991, 0.996] — corrected converges to 50% strictly faster than raw.** Effect-size honesty: corr(raw,ledger)=0.08 caps today's reduction at ~0.6% — critic-bound exactly per §7; the certified apparatus + the D4 critic are one deliverable pair (`certify --from-ledger` re-aggregates free; re-eval = 96 min GPU). **PR #11203 MERGED upstream same morning.** **Model-mirror distribution check PASSED same day** (512 games all-seats-bridged, `ante-model-mirror-20260712-101309`: raw 0.4765 ± 0.0221, all ledger classes zero-mean on model games — the Ante half of D4 is DONE); en route it exposed and fixed the **headless-worker wedge class** (OOM escaped `catch(Exception|StackOverflowError)` → Forge's modal bug-report dialog; fork `67e55ba1c1` headless uncaught handler + `Throwable` catches, harness `-XX:+ExitOnOutOfMemoryError`, Anvil `0443a18`) — a D6 prerequisite found at 512-game scale (self-play = model-mirror at scale; per-game memory accumulation under both-seats-bridged mode flagged for pre-D6 profiling). **D4 CLOSED (2026-07-13, [ADR-0015](docs/decisions/ADR-0015-d4-closeout.md)): M2 done-when #3 SATISFIED.** **`d4-critic-fullvis` = critic of record** (§4 asymmetric critic: full-vis input via `transform(full_vis=)`, trained value-only from d4-valuefix in 110 min, policy untouched; ckpt policy heads = garbage by construction, never serve): **BCE 0.4541 / AUC 0.8658 overall; turns-from-end AUC 0.991/0.975/0.985/0.965/0.927/0.821/0.673 — +6.6/+12.0/+12.2pp over the masked head at 5–8/9–16/17+; clears the re-based gate by 12 points.** **Rollout-label machinery built, certified, priced** (fork `80bd8ded76`: `-rollout K -points M` forks live model games at sampled quiescent MAIN1 windows, K wire-session completions with per-rollout library re-randomization; Obs `mark` records key points to windows; outcome vectors seed-deterministic across runs; labeler mainlines = on-policy model games by design). **Pilot verdict (501 games / 1,951 points, `d4-pilot-signal-comparison.json`): rollout means out-rank the full-vis critic in every band (AUC 0.864 vs 0.842; +5.5pp at 5–8) — but at ~17 positions/h/worker (each labeled game ≈ 33 games through a batch-1 server), so scale-up DEFERRED behind server micro-batching or D6 telemetry demand; machinery parked, not abandoned.** **Ante re-certified at the omniscient tier:** corr(raw,ledger) 0.08→0.22, β̂→~0.70, **fitted var ratio 0.9516 (~4.8% cut, 7.5× the interim head); β=1 AIVAT now reduces variance too (0.9603)**; game-level zero-mean everywhere; new finding recorded: node-level draw bias (+5e-4, t=6.4) resolvable only by the sharp critic — likely unserialized tutor-to-hand entries misread as uniform draws; Ante's next correctness item (with the 69% draw-poison coverage lever + re-deal re-anchoring). En route D4 also fixed the headless OOM→dialog wedge class (fork `67e55ba1c1` + `-XX:+ExitOnOutOfMemoryError`) and flagged both-seats-bridged per-game memory accumulation for pre-D6 profiling. **D5 CLOSED (2026-07-14, [ADR-0016](docs/decisions/ADR-0016-d5-closeout.md)): combat constructs end-to-end — D6 unblocked.** Design: factorized per-creature heads (attack logit + dedup count + target pointer; block pointer over attackers∪none; AR decoder = documented D6 upgrade path); labels = **combat-bounded obs join** — all 145 corpus block violations classified as multi-combat overshoot; bounding at the next declareAttackers dec drives them to **0/113,591 games** and also fixes a violation-free same-ids variant caught only by loader-vs-measure exact reconciliation (loader raises on residuals; v2 record `data/training/d5-combat-label-measure-full-v2.json`). **`d5-combat/last.pt` = checkpoint of record** (795K steps/16.2h, 42.1M params; combat finals: attack row 0.954 / window-exact 0.923 / target 0.993 / count 0.94–0.98, block row 0.86, valpair ≥ val everywhere; **honest 0.9671 = −0.77pp vs d2-sa, real multi-task cost at combat_weight=1.0, accepted per the pre-RL scope rule and priced at zero by the arms; value BCE 0.4531** — first policy ckpt trained end-to-end on ADR-0013-corrected labels). Serve: entity-ref AttackMap/BlockMap (v0 index form was never implemented — reshaped, fields reserved); featurizer derives candidates with the loader's own `_eligible_rows` (skew-free by construction; obs `sick` is haste-aware); combat tags auto-advertise only with trained heads; `load_compat` = the standing D5 ckpt-boundary shim (task_emb 6→8 + fresh combat params). Realizers (fork `380ec69d71`): engine-legality gating + engine-authoritative repair — attacks pre-drop unpayable taxes and self-validate in tiers (`getLegalAttackers` union-merge → wholesale; the engine RE-ASKS invalid declarations), blocks forced-add via `mustBlockAnAttacker` fixed point + terminal AiBlockController fallback (**the engine never validates AI blocks** — `validateBlocks` is human-input-only; ignoring requirements = silently rules-illegal games). **Arms tripwire: TIE as ADR-0012 predicted — 0.4550 ± 0.0249 vs D3's 0.4625 ± 0.0249** (paired seeds, 400/400 decisive, 0 crashes; `data/runs/d5-arms-report.json`); telemetry over 2,754 combat windows: attack drop 0.18% / block drop 3.7% / **0 forced / 0 fallback / 0 transport failures** — model combat intent reaches the engine essentially unfiltered (contrast M0's 65% cast-veto). **D6 data loop verified closed:** model-game obs ingest zero-error and the bounded join extracts combat labels from model games with 0 violations. Watch: veto_rate read 13.9% this arm (D3 ~10%) — re-read at D6; block-drop class joins the veto class behind the serve-time re-ask lever. **D6 OPENED (2026-07-14 session 2): server GPU micro-batching built** (`_Batcher`: ≤16/3ms window, per-item views + per-item pass_delta; 73 rps at just 2 workers vs 59 batch-1) **+ both-seats memory verdict: BOUNDED** (heap fill to ~2.4 GB = Xmx2g+native, zero drift over 120-game chunks — the D4-flagged class is not a leak). **D6 LOOP BUILT + SMOKE-CLOSED (session 3, plan [d6-vtrace-loop.md](docs/design/d6-vtrace-loop.md)): the V-trace self-play machinery runs end-to-end.** Fleet number: **1,146 g/h at w=8 both-seats under micro-batching** (240/240 decisive, 591 req/game, ~188 rps, batch-1 only 20% of forwards; run `d6-fleet-throughput-20260714-184530`) — a 480-game iteration ≈ 25 min generation. Design pins: **synchronous iterations** (alternation <15% wall-clock; no GPU contention, no hot-swap, per-iteration provenance-clean stores); **action = the model's sampled composite pick, realizer+engine = environment** (IS ratios exact by construction; vetoes/drops = env stochasticity); **μ recorded server-side to `mu.jsonl` keyed (g,s)** — the seq the store already joins dec/ret on, zero Java changes; reward terminal-only, win=1 / draw-cap-crash≠win both seats (§3d); critic = the policy's own masked value head (full-vis critic = documented upgrade, stays the Ante/eval critic). Machinery: **seeded Gumbel-max sampling** (per-decision Philox noise from splitmix64(game_seed,s), generated at item shapes and scatter-padded into the GPU batch — samples independent of batch composition, twin-determinism survives batching; `act(noise=)` greedy path byte-identical); **`composite_logp` = one body** for the μ-recompute tripwire / π ratios / differentiable PG term (inclusion rules live in label construction; `mu_record`↔`apply_mu_labels` lockstep); V-trace targets unit-tested (**caught a terminal-bootstrap double-count before any training ran**); `anvil/training/rl.py` two-pass learner (no-grad targets → grad segments; replay via fractional store weights, fresh 1.0 + old 0.33 ≈ 50% fresh); `anvil/training/selfplay.py` driver (generate→ingest→train→swap, STOP-file + loop_state resume, §6 reward-vs-critic anomaly monitor, per-N-iteration arms). **2-iteration smoke: loop closed** — iteration 1 served the RL-trained ckpt with a 2-store mixture; 0 tripwire violations (tolerance 0.2, calibrated: bf16 serve-vs-recompute noise ≤0.075 on soft heads; real skew = pick mismatches / O(1) deviations), KL(π‖μ)~1e-3 at lr 1e-5, critic v0 ≈ realized reward, the monitor flagged exactly the known 1-in-142K block-realizer terminal fallback. **72 tests green** (+9: sampling determinism / batch-composition invariance / μ roundtrip priority+combat / V-trace units). Watch items: x factor included on every cast (untrained head on non-X casts = ratio variance + noise gradient — refine inclusion if ent_x drifts); ckpt step now monotonic across the BC→RL chain (μ-step tripwire gate needs unique step per generating ckpt); re-run `forkcheck -grpc` twin gate under micro-batching sometime (D1 certified at batch-1) — hygiene, not a blocker. **d6-run1 COMPLETE (2026-07-15 overnight): 12 iterations × 480 games = 5,760 self-play games, zero unhandled failures.** First contact surfaced and fixed 3 real bugs (commits `76341c6`/`7e96c2d`/`e502a91`): diverged re-issued games chimera the (g,s) mu join → conflicting-duplicate games dropped at ingest + `mu_matches` loader backstop; pass B held all segment autograd graphs → generator (one live graph); torn worker log lines + phase-idempotent resume (run/store reuse, train DONE marker). Loop health across 12 ckpts: entropy 0.131→0.165 (no collapse), per-iter KL 0.001–0.006, ρ 0.94–0.99, critic tracked reward within ~1.5pp every iteration (§6 anomaly rule quiet), turns flat, 480/480 decisive every iteration. **Arms: iter004 0.4525 ± 0.0249 → iter009 0.4925 ± 0.0250 → final ckpt 0.478 ± 0.011 on 2,000 games; paired vs the D5 arm +3.76pp ± 2.22 (t=1.69). Honest verdict: +2–4pp at ~85–95% confidence — improves, NOT yet outside noise; done-when #4 stays open.** Run-2 findings: cumulative KL from init only ~0.05 (lr 1e-5 barely moved the policy — scale iterations/lr next); **veto drift 14.1%→19.2% sampled / 16.6% argmax (D5: 13.9%) = a structural reward leak growing under RL pressure — the serve-time re-ask-on-veto lever promotes to the top of the run-2 list**; bf16 tripwire tail to 0.44 on soft heads (picks always match; switch to pick-match tripwire if it fattens). Records: `data/training/d6-run1/` (monitor.jsonl, arms at iter-004/009, final-arms-report.json). **Run-2 prep DONE (2026-07-15 session 2): re-ask-on-veto built end-to-end + Ante-ledger reads in the standing arms eval + arms re-baselined under the new environment.** Decisions (user): re-ask first, then lr/iteration scaling; Ante adopted now. Design pinned [d6-vtrace-loop.md](docs/design/d6-vtrace-loop.md) §6b — action pin unchanged, re-ask is an *environment* change: on M1 CastPlan veto the window re-issues with the vetoed candidate removed (whole host at host-level), fresh Obs seq + μ record + noise per attempt, `retry_of` wire mark (proto field 9, was reserved-unused), cap 8, M0 path never re-asks. Fork `64c6322f35` (`-reask` worker flag); Anvil `6c13c99`: harness manifest→worker plumbing, selfplay driver `--reask` (generation+arms), census `reask`/rescue telemetry, `arms_report --ante` (certify-report merge, seat-flipped fitted-β, pooled; reproduces the D5 record exactly on old fields), 2 new tests (74 green: same-window consecutive decs through ingest/ret/μ joins; μ-parity on reduced candidate lists). Smoke (40 sampled games): 644 re-asks, 44 rescues, max chain 3, **79% of re-asks turn silent forced passes into model-chosen passes with μ records** — the reward-visibility fix; ingest+validate zero-error; learner tripwire 0, ρ_init 0.998. En route fixed: rl.py first-log-row metrics dilution (absolute-step modulo vs actual window size — run-1 first rows were ~2× diluted, conclusions unaffected); post-D5 latent skew in Ante (`ledger.py` value examples lacked collate's combat fields — first certify since D5 tripped it); lesson: servers started as `&` children of non-interactive shells inherit SIGINT=ignored (Python leaves SIG_IGN in place) — SIGTERM them, or Popen directly like the selfplay driver. **Re-baseline (400 games/ckpt, paired seeds 20260710, argmax, `-reask`+obs, record `data/runs/reask-rebaseline-arms-report.json` + 4 ante-* certify reports): d5 raw 0.4775 ± 0.0250 / corrected 0.4789 ± 0.0244; rl1 (iter-011) raw 0.5075 ± 0.0250 / corrected 0.5083 ± 0.0246 — the RL ckpt's first arm read at parity with its teacher; paired rl1−d5 +3.00pp ± 1.93 (t=1.55, 36 up/24 down), consistent with run-1's +3.76 ± 2.22.** Re-ask alone ≈ +2.3pp on the BC arm (cross-env caveat); rescue ≈ 10% of vetoes at argmax; note veto_rate semantics changed under re-ask (re-veto chains count: rl1 23.6% vs d5 16.4% — the RL drift direction persists, but vetoed intent now resolves to a model-chosen outcome instead of leaking). Ante ledger zero-mean on all four arms; per-run var ratios 0.92–0.99 (n=400 — the cut matters more at the 2,000-game reads). **d6-run2 COLLAPSED overnight (2026-07-16, [ADR-0017](docs/decisions/ADR-0017-run2-entropy-collapse.md)): lr 3e-5 × 16 iters, caught mid-iter-6 at the morning check and stopped (~9.5h; entropy 0.18→4.4, per-iter KL →4.4, veto 24%→87%, iter-004 arms 0.085 ± 0.014).** Diagnosis conclusive from monitor/metrics/μ/census: **entropy-bonus runaway under near-zero advantages** — mirror self-play + calibrated critic ⇒ pg term ±0.002 all run, so the always-on ent bonus (3e-3) is the only persistent policy gradient and lr sets its compounding rate (run-1's 0.131→0.165 was the same force at ⅓ speed, not "healthy exploration" — **the v0 loss has no entropy equilibrium**); positive feedback via KL↑→ρ clipped→advantages shrink; priority *choice* head led (0.081→1.14), X head flat (pre-registered suspect exonerated), value head downstream (v-loss flat 0.32–0.43); **re-ask = volume amplifier, not cause** (iter 5: 1.06M priority decs = 4×, 66% veto events, 103K chains ≥4, cap held; turns flat through iter 4). The §4 "no anchor v0, contingency if KL runs away" bet resolved FALSE — every signal was in monitor.jsonl by iter 4, nothing acted. **ADR-0017 decisions: driver guards promote to v0 (halt/reject ckpt on per-iter KL > 0.05 / entropy > 2× iter-0 / veto > 1.5× iter-0); §6 anomaly rule two-sided (critic ≫ reward flags — iter 5's v0 0.65 vs 0.50 went unflagged); always-on entropy bonus retired → hinge floor at ~iter-0 entropy (exploration = τ=1 sampling's job); lr ceiling bracketed (1e-5, 3e-5) with-bonus, untested without; run-2 chain discarded (dir kept as negative-result record), run-3 init = d6-run1 final ckpt.** Priced honestly: the 3e-5 arm cost ~9.5h and falsified "run-1 left free speed on the table" — the drift rate WAS the stability margin. **Next: implement guards + hinge floor (small; signals already computed) → launch run-3 (guarded, lr probe 1–2e-5, `-reask`, fresh seeds, init d6-run1 final); done-when #4 read at 2,000 games corrected when a run survives its guards.** Watch: server fallbacks 3–9/iter flat across 4× load (run-1 had 0, unexplained); chain-depth ≥4 census count = cheap secondary collapse indicator. **Session 2 (2026-07-16, while run-3 trained): community archaeology + public-docs pass** — #ai-plotting survey + 3 repo dives ([discord-ai-plotting-survey.md](docs/design/discord-ai-plotting-survey.md) + survey-dive-*), [forge-ai-field-guide.md](docs/forge-ai-field-guide.md) published, project map reworked (hierarchical milestones, self-play-loop flow row); harvest: shared pre-shuffle CardPool hash-order determinism hole verified in our fork (dataset-boundary fix + joint determinism-hooks PR w/ manabrew queued in the worklist), corpus checks — aura/Attach labels CLEAN (only targetless residue = Plot, correctly so), never-cast audit 138/1,701 pool cards (95 RemAIDeck, 34 unflagged heuristic blind spots; record `data/runs/never-cast-audit-20260716.json`). **[ADR-0018](docs/decisions/ADR-0018-ruleset-scope-clarification.md): ruleset scope clarified — games are 1v1 Commander at 40 life; Duel Commander = pool provenance, not the format (no dataset invalidation); roadmap rule adopted: features before breadth, content in set-sized dataset-boundary chunks (precons queued first), modes via bridge-tag coverage, 4-player classified as a feature.** **d6-run3 (2026-07-16→17, guarded: lr 2e-5, hinge floor, `-reask`, init run-1 final): 13 healthy iterations, then the FIRST LIVE GUARD HALT — veto tripline fired at 41.3% (1.5× the 25.1% baseline), ckpt rejected, exit 3, ~10 min wasted vs run-2's ~5h.** Entropy pinned 0.17–0.21 all run (hinge held where run-2 tripled by iter 4), KL never near 0.05 — this was pure veto drift (run-1's structural leak at 2× lr), not entropy runaway; rescue rate declined 10.6→8.4% as it rose (drifting toward unrescuable casts). En route: two GPU-cotenancy OOMs beside resident ComfyUI (10.45 GiB) fixed durably (expandable-segments baked into the driver + `--rl-seg` peak-halving knob, commit `bbf8f1d`; VRAM-elasticity QoL item queued — per-phase seg autotune + OOM-catch-retry). Strength grew the whole run: arms init 50.75 → i4 50.0 → i9 52.5 → i12 (last accepted) 54.75 (400-game reads). **The 2,000-game corrected read on iter-012 ([ADR-0019](docs/decisions/ADR-0019-d6-donewhen4-resolution.md), record `data/runs/run3-final-arms-report.json`): raw 0.5220 ± 0.0112 / Ante-corrected 0.5213 ± 0.0110; paired vs the D5 BC arm +6.75pp ± 2.31 (t=2.92) — M2 done-when #4 SATISFIED; the agent is ~2σ better than the heuristic it imitated (first teacher-surpassing read).** `d6-run3/iter-012/train/last.pt` = RL checkpoint of record. Paired vs its own init +3.75 ± 2.16 (t=1.74) — the cumulative chain clears the gate, per the gate's wording. **All four M2 done-when clauses now resolved; M2 closeout + M3 seeding = the next session-level decision.** Named open front: veto drift (levers per ADR-0019: reward veto penalty [needs a §3d pin], first-attempt veto monitor metric, per-head entropy floors, X-inclusion refinement; iter-013's rejected ckpt kept for a what-does-it-try-to-cast forensics dive).

## M1 (closed 2026-07-10, ADR-0009)

- **M1 CLOSED (2026-07-10): D8 built and measured same-day — the BC agent plays real games at 46.8% ± 2.5pp vs the heuristic it imitates, on held-out matchups ([ADR-0009](docs/decisions/ADR-0009-m1-closeout.md)).** CastPlan executor = legality-only realizer (canPlaySa judgment never consulted; M0's 65% veto → **3.1–7.8%**, decomposed: unpayable/no-fit/timing) + host-level disambiguation ladder (shape→pay→kind→order; host-vs-SA ambiguity measured at 23.6% of expert actions — SA-level candidates queued for M2, exact labels via new `oi` field logged from now on); modal spells cast mode-less (modes heuristic per M1 scope), divided-as-you-choose even-split. Serve path: wire obs = dec record + last-K history ring; featurization **loader-parity-tested** (byte-identical tensors, act()==forward — `tests/test_serve_parity.py`); info-set rule stays in the leak-tested transform. Arms (200 valpair pairs, mirrored seats, 1,000 games): δ=0 46.8% / δ=+2.169 45.5% (**tie**; calibration halves vetoes at no winrate cost — RL init should start action-rich), 398/400 decisive, 0 draw clocks, 0.5% crashes; **927–1,068 g/h vs 959 heuristic = inference-in-the-loop operationally free at w=8** (M0 2.6% echo tax = protocol floor). Two smoke bugs fixed en route (protobuf fallback-oneof default misread → infinite London mulligan + insurance cap; divided-allocation NPE). Standing rule: never read a bridge answer field without checking `fallback` first; eval runs keep census+obs on. Report: `data/runs/d8-arms-report.json`; per-checkpoint winrate arms are ~12 min = the standing strength eval. **Next: M2, opening order per [ADR-0010](docs/decisions/ADR-0010-m2-opening-sequence.md)** — fork-API hardening → **SA-level action schema + BC retrain** (play-time order-rung measured at 31–36% of casts, ~2.5× the corpus estimate — the largest known winrate leak; ~69% of the current corpus's multi-SA labels recoverable via string join, exact `oi` labels accumulating; loader nested-window history fix rides along) → winrate arms reprice → rollout value labels **under the BC policy** (amended from heuristic rollouts) → RL fine-tune (δ=0 init). D9 combat constructs get an explicit slot after the schema work. First M2 deliverable: the M2 plan doc, seeded from ADR-0010.
- **M1 D7 CLOSED (2026-07-10): the gate resolved TRUE — held-out honest agreement 0.9758, ~9pp above §15's high-80s row ([ADR-0008](docs/decisions/ADR-0008-d7-gate-resolution.md)); 3-epoch recipe; X un-plateaued.** Epoch sweep (1/2/3 full epochs = 258K/516K/774K steps, from-scratch, merged 37.3M architecture, pw=0.1, full 113,592-game corpus, 600-batch finals): honest 0.9647/0.9719/**0.9758**, nonpass 0.9043/0.9229/**0.9339** — each epoch paid outside noise (SE ~0.33pp), returns halving, **recipe = 3 epochs** (~14h nice-19); repetition-vs-diversity resolved (reshuffled passes over the frozen corpus keep paying → ADR-0006 stop vindicated; loader now reseeds the shuffle per epoch, `f150afc`). Per-tag (ep3): mulligan 0.9969 (n=639), tuck 0.868, binary/number/trigger 1.0 on tiny n; value flat ~0.57 (as diagnosed); valpair +0.0–0.4pp above val (standing quirk). **X head: the ADR-0006 "plateau" was compute-starvation, not corpus limitation — fixed 672-window basis 0.647 (100K curve) → 0.795/0.839/0.874 across the sweep** (combined basis n=1,518 agrees; `eval_x --store` reproduces historical bases). **D8 started: PASS-logit calibration done** (`anvil/training/calibrate_pass.py`; model under-passes 1.7pp at pw=0.1; δ=+2.169 rate-matches and lifts honest to **0.9841** — D5's "recalibratable post-hoc" premise exact; both operating points in `d7-ep3/pass_calibration.json`). **`d7-ep3/last.pt` = checkpoint of record.** M1 done-when #1 satisfied. Next: **D8 executor arc** — CastPlan executor in the fork (one-shot AI-path casting; expect the 65% veto rate to collapse), real observations over the bridge + server-side inference, full games vs heuristic (winrate recorded, not gated; run δ=0 vs δ=+2.169 arms), bridge-tax re-measure with real payloads.
- **M1 D6 CLOSED + D4 CLOSED (2026-07-08): the stopping rule fired at the first checkpoint — corpus final at ~113.6K games ([ADR-0006 resolution](docs/decisions/ADR-0006-500k-default-on-stopping-rule.md)); one-field heads merged and shaken down.** Learning curve on fixed pilot-val basis (10K/25K/50K/100K games, 113K steps each): nonpass 0.8548/0.8695/0.8746/**0.8685** — 50K→100K doubling = −0.61pp vs the +0.7pp stop threshold; honest/value flat to the 4th digit. Rare-label clause (new `eval_x`, fixed 672-window basis, SE ~1.9pp): X 0.573/0.637/0.674/**0.647** — the climb stopped; doubled X diversity at matched compute bought nothing (X headroom = drills/architecture, not corpus scale — §6 premise showing up early). **Generation stopped at 63.6K extension games (~42 h wall, 23% of ceiling); run paused-not-deleted (deterministic stream resumable); repetition control (2-epoch) folds into D7 recipe tuning.** Same day: **d4-heads merged** (one-field heads + [PLAN] token, 37.3M params) — shakedown (20K steps, combined corpus): all six task types train end-to-end, **mulligan agreement 0.981 (n=639)** at 18% of an epoch; tails tiny-n as measured (tuck 0.76, binary 0.85/n=39, number 0.36/n=50, trigger n=4). Post-hoc-basis note: multi-store arms' in-training evals use the combined val split — curve comparisons need the pilot-val re-eval (scratchpad script → formalize if reused). Next: **final-store ingest + zero-error validate → D7 train-to-gate** (full head set, ~113.6K games, epoch sweep 1–3, per-tag report vs §15's high-80s row); then D8 eval path (CastPlan executor, pass-logit calibration, winrate vs heuristic).
- **M1 D5 matrix day (2026-07-07): embedding pin resolved ([ADR-0007](docs/decisions/ADR-0007-embedding-pin.md)), pass-weight knee found, text channel measured ≈0 at rung 1.** All arms full-epoch (113K steps) with 600-batch finals: **Qwen3-vs-BGE = statistical tie** (honest 0.9636 vs 0.9630; target identical to 4 decimals) → pin stays Qwen3 (better NN structure for the axes this eval can't see), BGE = tested escape hatch; **pass-weight 1.0/0.3/0.1 → knee at 0.3** (+7.3pp nonpass for −0.4pp honest), default now **0.1** (action-rich prior for M2; pass boundary recalibratable post-hoc via PASS-logit offset; targets/X/value flat across the sweep); **null-text arm: zeroing the text buffer costs −0.19pp honest, rest noise** → the tie means "text barely used," 8B-encoder questions closed for rung-1 in-pool, shortcut learning confirmed (ID crowds out text; **ID dropout** = the pre-Tutor lever). Also same day: multi-store loader (pilot+extension as one corpus, `--store a,b`, ADR-0005-clean on both stores), **#11161 triage** (6/7 pilot crashes = races, not solo-reproducible, likely fixed upstream — don't file; 38257 StackOverflow reproduces deterministically = the one real filing candidate; #11161 = anchor for next fork bump, branch `test-11161` parked), d6ext healthy (~1,370–1,555 g/h, failure census tracking pilot baselines). Next: **100K stopping-rule checkpoint** (ingest ckpt-100k, learning curves at fixed 113K steps, first ADR-0006 reading); D4 remainder (one-field heads, turn-plan latent); pass-logit calibration before eval-path play.
- **M1 D5 diagnosis arc + full-epoch run (2026-07-06): honest 0.964 / nonpass 0.837 at one epoch; value head diagnosed to the ground; [ADR-0006](docs/decisions/ADR-0006-500k-default-on-stopping-rule.md).** The at-chance value head was input conditioning, fixed twice: transform v2 scales state scalars to O(1) (lib ~90 drowned life diffs in `state_proj`; BCE 0.691→0.647), v3 scales entity scalars + card-fusion inputs (unit-L2 text dims ~0.016 beside raw CMC~15 = **~1,000× per-dim**; ID-emb init 60× louder than text = memorization favored over generalization — would have confounded the embedding ablation; BCE →0.578, calibrated commits on 17% of windows, valpair lockstep). **pilot-run4 (full epoch, 113K steps, ~90 min nice-19): policy/target/X were data-starved — honest 0.964, nonpass 0.708→0.837, X 0.41→0.67 (n=173), valpair 0.966 = zero held-out-matchup gap; value was NOT (0.575, binned shape unchanged).** Matchup-entropy measurement (pilot games.jsonl, 4,622 five-game pairs): **21.4% of pairs go 5–0 (coin-flip: 6.2%)**, matchup-prior BCE floor ~0.60; the model beats the floor early (reads own hand too), sits AT it late — board reading absent; final-turn AUC 0.68 on clean-label decided states ⇒ residual = hidden-info entropy + board/combat-math reading + grind label noise, NOT corpus size. **M2 handoff: rollout-labeled value targets = first workload for the fixed fork API.** ADR-0006: 500K generation **default-on behind fork hardening** with pre-registered stopping rule (doubling buys nonpass < 2×SE AND rare heads plateaued → stop; value excluded — measured not data-limited). Eval upgrade: per-metric ns in metrics.jsonl, 600-batch final eval (nonpass SE ~0.33%). Diagnostic tool: `anvil/training/diagnose_value.py` (binned BCE/AUC/calibration per checkpoint). **Fork hardened same day (fork `809db975a5`: RAW_CAP runaway guard + frame-accounting fixes, smoke-verified byte-identical healthy path) and D6 generation is LIVE: `d6ext-20260706-220552`, 450K games extending the pilot's seed stream via harness `--start-index` (pilot = games 0–50K of one deterministic corpus; pair-schedule prefix verified; game 50000 continuity checked). 100K schedule = 12,654 unique ordered pairs = full pair space — saturation confirmed.** Next: multi-store loader before the 100K stopping-rule checkpoint; D5 matrix remainder at full-epoch depth (pass-weight sweep, now-unconfounded Qwen3-vs-BGE ablation, learning curves per ADR-0006); D4 remainder (one-field heads, turn-plan latent); upstream worklist has the 7 crash seeds ([upstream-worklist.md](docs/design/upstream-worklist.md)).
- **M1 D4 core + D5 first run (2026-07-06): honest agreement 0.944 on the first training run.** D4: embedding caches (Qwen3-4B 2560d rev `5cf2132abc99` + BGE-M3, pinned to pool+revision+text-hash; NN sanity clean — Qwen3 clusters by mechanic), 25-dim structured card features, `CardEncoder` (frozen text+features, learned null for hidden rows), `StateAssembler` ([STATE]+entity+K=8 history tokens), d=512×10 trunk, priority pointer head, **autoregressive target decoder** (T=4 slots+STOP = 100% coverage measured; teacher-forced, entity/player/STOP), X head (18 classes), value head — 36.2M params. Loader: streaming worker-sharded IterableDataset, PASS=candidate 0, ADR-0005 candidate rows, chosen-outside-candidates raises. D5 scaffold: deterministic hash splits (val by game, valpair by pair), 105-method vocab pinned, bf16 train loop with honest metrics (batch 256 — 512 OOMs beside the desktop; entity sequences hit 150+). **pilot-run1: 20K steps / 5.12M windows / 21 min → val honest 0.944 / nonpass 0.708 (rising at cutoff) / target-slot 0.951 / valpair 0.944 = zero held-out-matchup gap.** §15's high-80s row is tracking to resolve true (formal resolution at D7, full corpus, per-tag). **Red flags: value head at chance (BCE ~0.69 flat — late-turn probe next), X data-starved (train memorized, val ~0.48 on tiny n).** Remaining D4: one-field heads, turn-plan latent. Next: value/X diagnosis, learning curves + pass-weight sweep + embedding ablation (runs are 21 min — the D5 matrix is an afternoon).
- **M1 D3 COMPLETE (2026-07-06): 50K pilot corpus generated and validated.** `d3pilot-20260704-175219`: 50,000/50,000 games, zero skips, 99.96% decisive, 33.2 h wall at **1,508 g/h effective** (16 workers nice-19 through desktop use). Store: **2.33 GB, 46.6 KB/game at 94.5×** (500K ≈ 23 GB). **Full validation: 49,999 games, 28.99M priority windows, 2.70M cast labels (588K targeted / 31.6K X / 25.3K optional-cost), zero label errors**; 1 frame quarantined (hard-capped game killed mid-write, 35 MB runaway — store gained `skip_undecodable` + loud quarantine, Anvil `0aebeaa`). Non-decisive census: 15 hard-cap grinders (deck-clustered: `dc-863788`×6, `dc-864556`×4 — note for D5 splits) + **7 seed-pinned engine crashes for the upstream worklist** (4× ConcurrentModification, StackOverflow, ArrayIndexOOB, NoSuchElement); 21 readable crash games still carry 40.9K policy labels (value-head exclusions via `status`). Slow-tail root cause: big-board superlinear heuristic eval + AI timeouts, capped, NOT convoke; structural fix is D8/M2 (model replaces heuristic), not engine patching. 500K decision: staged generation is byte-identical to burst (deterministic config) — D4/D5 proceed on the pilot now; D5 learning curves price the 500K; fork commit frozen across chunks when it runs. Before 500K: runaway-frame guard + hard-cap frame close (small fork hardening). **Next: D4 encoder/trunk/heads** (first artifact: Qwen3-Embedding-4B cache for the 1,701-card pool).
- **M1 D3 machinery + mask redesign (2026-07-04): [ADR-0005](docs/decisions/ADR-0005-d3-label-mask-semantics.md).** Corpus machinery landed: worker `-pairs`/`-gpp` (index→deck-pair schedule) + seeded per-seat AI personalities (pure function of game seed; logged in results JSONL + obs game record); harness `launch --pool` (pair schedule from the pool manifest, pool_version pinned in run.json — D1 residue discharged); summarize reports obs bytes + wall tail. **Label mask redesigned (ADR-0005):** D2's payable-options scan was wrong (payability needs target/X context the scan can't have — 11 expert picks masked out: Mystical Dispute/Dargo/X-spells) and expensive (+77% w=16 haircut; mana enumeration duplicated per window); scan-sharing was a mirage (the AI early-exits its value-sorted loop — no availability set exists). Now: **timing-legal candidates** (canPlay-only — superset of expert-castable by construction; gate metric's single-option exclusion on this basis), **library tops serialized under mayPlayerLook** (schema 5a; priority decs union unwalked-zone option hosts — label-host-in-obs by construction), stale-target guard (modal sub-chains retain dead refs from earlier AI passes). **Measured (320-game arms, w=16, same seeds): obs-off 1,248 g/h / old scan 703 (+77%) / new scan 1,055 g/h (+18%); validation 74 → 2 → 0 errors; ~48 KB/game (500K ≈ 24 GB).** Recalibrations: pool+personalities heuristic baseline = 1,248 g/h at w=16 (the precon-derived 2,300–2,900 band is retired); draw-clock/hard-cap tail ~2% and NOT convoke-driven (convoke = 1.8% of records, deck-clustered, zero overlap with the tail — stays look-before-M2); ref-dedup declined (zstd already is the storage dedup; an "unchanged-state" stamp is its own poisoning risk); 10-game wall probes are void (scan perturbs trajectories — 320 games is the throughput measurement unit). **Pilot: `d3pilot-20260704-175219` — 50K games, pool `cf2ca6ba`, 10K pairs × 5, fork `ca76c842a8`, w=16 nice-19, ~2 days at measured rate. Zero-error validation is the standing corpus-launch gate.** Next: shepherd the pilot (early-slice ingest+validate, watch the ~2% tail), then D4 encoder/trunk/heads.
- **M1 D2 landed (2026-07-04): CastPlan label extractor** (schema doc amended; corpus-blocking deliverable done). `Obs` serializes every SpellAbility answer as a CastPlan (host/targets/X/alt-cost/optional-costs/multikicker/bound-modes/sub-targets, observation-idiom refs joinable to ents+stack); priority decs log **structured engine-legal options** ({"e","sa","kind"} — the gate metric's single-legal-option exclusion basis + the training action mask, logged at generation time because replay drift forbids recomputing legality); Python contract `anvil/store/castplan.py` + `python -m anvil.store validate` (five checks incl. per-seat playChosen cross-match). **Sanity gate passed: 20 pool games, 9,547 windows / 860 cast labels, zero errors; 42 KB/game at 75× → 500K ≈ ~21 GB.** Validator's first batch caught a real corpus-poisoning bug: alt-cost-only spells (Snuff Out for 4 life) missing from options — scan now iterates the AI's own `getOriginalAndAltCostAbilities` set. **Determinism measured: with logging ON, same-seed outcomes reproduce 20/20** (only unlabeled `payManaCost` micro-order wobbles); **the option scan is NOT a pure observer** (6/20 diverge vs bare, same policy/different draw — harness `replay` now passes `-obs` for obs runs); **cost flag for D3: +52% single-worker haircut** (scan duplicates AI availability work; measure at w=16, scan-sharing is the lever). **Next: D3 pilot corpus (~50K games).**
- **M1 D1 landed (2026-07-04): [observation-schema-v1.md](docs/design/observation-schema-v1.md)** — observation schema v1 + trajectory store v0, end to end (fork `c9c0191b84`). JSONL-in-zstd-frames per game; full-state records with per-entity visibility, information set enforced by the Python transform (`anvil/encoder/transform.py`, leak-tested); `dec`/`ret` split records around all 109 callbacks; libraries derived, not serialized; store ingest/reader in `anvil/store` (`python -m anvil.store ingest`); harness `--obs` flag. **Smoke measured: ~2 MB raw/game compresses ~70× → 28 KB/game (500K games ≈ ~14 GB, 10× inside budget); serialization haircut ~4.3% single-worker; bit-identical outcomes with logging on.** Two corpus-poisoning bugs caught by the smoke + leak test: `getPlayers()` reindexes after elimination (fixed via registered players + `lost` flag); transform row order leaked hidden ordering (fixed: canonical sort). **Next: D2 CastPlan label extractor** (corpus-blocking).
- **M1 plan done (2026-07-04): [m1-bc-plan.md](docs/design/m1-bc-plan.md) + [ADR-0004](docs/decisions/ADR-0004-m1-scope.md) (scope decisions).** Headlines: Java logs versioned entity-level observations, Python owns tensor assembly (refines §9's featurize-in-Java line; forced by the replay-drift caveat — observations must be logged at generation time); **labels for all live callbacks, heads cut at rung 1** (CastPlan-composite priority + M0 one-field tags; combat constructs = stretch); CastPlan splits into label extractor (corpus-blocking — census proved targets/X never surface as callbacks) and executor (eval path, the veto-rate fix); embedding pin Qwen3-Embedding-4B with BGE-M3 pilot ablation (Tagger tags + gap-filler deferred); corpus staged (~50K pilot → 500K full, overnight chunks ~22–28 nights as the planning assumption, meta-pool `cf2ca6ba` only, ~100–200GB compressed); yield macros moved to M2 (amends ADR-0003). Gate: held-out agreement with forced/pass-trivial decisions excluded, per-tag. First implementation deliverable: observation schema v1 + trajectory store v0 (D1).

## M0 and pre-M0 foundations (2026-07-02 → 07-04, closed by ADR-0003)

- **M0 CLOSED (2026-07-04): [ADR-0003](docs/decisions/ADR-0003-m0-closeout.md).** All deliverables landed and measured; §14/§15 recalibrated (throughput row resolved true 6× pre-optimization; calendar risk shifts to M1 representation quality and M2 RL machinery). **Current milestone: M1 (BC)** — main line is encoder/featurization, real observations over the bridge, `CastPlan` one-shot (also fixes the 65% veto rate); parallel tracks: upstream PR #1 (static fork-bug fixes, per ADR-0002), DC card pool/decklist pipeline, census re-run on the pool. First M1 artifact: the M1 plan doc.
- **DC pool pipeline landed (2026-07-04): [dc-pool-pipeline.md](docs/design/dc-pool-pipeline.md).** `uv run python -m anvil.pool fetch|banlist|build|install|status`; acquisition (mtgtop8 `f=EDH` + duelcommander.com banlist, incremental, ≥2s politeness) is split from deterministic derivation (manifest content hash = pool version; `data/` stays outside git, flex/overrides force-tracked as config). **Current pool: version `cf2ca6ba` — 1,701 cards from 113/120 decks** (20 events since 2025-07, banlist 2026-07-04, 112 cards); all 7 exclusions legitimate (6 banned, 1 malformed source). **Upstream-gap worklist: empty — Forge scripts every card in 120 competitive DC decklists.** (The suspected "Greymond gap" was a pipeline bug: Universes Within twins live as `Variant:...FlavorName:` lines — a 182-card class — now indexed to their canonical names, e.g. Greymond → Rick, Steadfast Leader.) Partner commanders supported; smoke-load gate passed in the fork (single + partner + recovered-Greymond decks, seeded games). Fetcher lessons: mtgtop8 serves iso-8859-1 (decode by header, not assumption) and spells split cards `Fire/Ice`. The 2026-07-04 census ran on predecessor pool `f568b187` (111 decks; the 2 recovered decks don't change its conclusions). **Unblocked the census re-run** (done same day — see census bullet; the 161-turn smoke game turned out to be an outlier: pool median is 21 turns).
- Pre-M0 prior-work survey **done** (2026-07-02): [docs/design/prior-work-survey.md](docs/design/prior-work-survey.md). Key upshots: memory/allocation pressure is the recurring Java-engine killer (M0 must include an RSS/GC soak test); Forge's `GameCopier`/simulation layer is reportedly unstable, raising risk on state forking (§9); Forge design conversations go in PRs/Discord, never issues (stale bot, ~35 days).
- **[ADR-0001](docs/decisions/ADR-0001-prior-work-responses.md)** (2026-07-02) records the survey responses: engine stays Forge; M0 gains soak + fork-fidelity tests; bridge protocol invariants (legal-actions-only materialization; one-shot-or-micro-step decision answering); standing gated bet — stable single-step fork API as flagship upstream contribution, resolved after M0.
- Forge fork: **created** (2026-07-02). Lives at `../forge` (sibling repo, blobless clone); `origin` = Tyrathalis/forge, `upstream` = Card-Forge/forge. Cloned/pinned at upstream commit `0bfdaa572f30c03e105bd5573b5d851a7c5b7a44` (2026-07-02, "Refactor CountersPutAi #11141"). Build: user-local Maven at `~/.local/opt/maven/bin/mvn`, profile `-P windows-linux`, Java release 17 on system JDK 26. Formal per-run pinning machinery arrives with the M0 harness.
- **First measurements (2026-07-03 soak, 6K games/8.2h single JVM):** forward-play is leak-free (heap-after-GC flat; LearnForge's killer confirmed dead); ~2.3 GB allocations/game; ~26 MB/h native RSS drift (recycle workers routinely); 0.4% of games hit the 120s draw clock.
- **M0 measurement set complete (2026-07-03): [ADR-0002](docs/decisions/ADR-0002-fork-api-gate-resolution.md)** resolves the fork-API gate. Headlines: **64 games/min total at 16 workers** (process-per-worker scales monotonically; §15 target beaten 6× pre-optimization; 32-core purchase declined); **fork fidelity is broken-but-characterized** (50% deterministic trajectory divergence, 12% static corruption in two seed-reproducible bug classes, zero crashes in 2,000 forks; copying *canonicalizes* — 2.4% fresh drift per additional fork). Fork API promoted to flagship upstream contribution, sequenced before M2; static-bug fixes are upstream PR #1 post-M0; interim workarounds: fork-at-start + digest tripwire. The `forge forkcheck` harness (fork commit `8907112`) is the permanent fidelity regression gate for engine bumps.
- **`PlayerController` override plan done (2026-07-03): [playercontroller-override-plan.md](docs/design/playercontroller-override-plan.md).** Headlines: injection is zero-engine-change (`LobbyPlayerAnvil` via `IGameEntitiesFactory`); decision surface is 109 methods (~88 real decisions); `PlayerControllerAnvil extends PlayerControllerAi` with per-decision `answeredBy: bridge|heuristic-fallback` provenance tagging; one-shot casting proven feasible via the AI play path (pre-set targets + `AiCostDecision`), mid-resolution choices irreducibly micro-step — ADR-0001's one-shot-or-micro-step invariant maps 1:1 onto engine structure. Landmine logged: `GameCopier.clonePlayer` swaps non-AI controllers to heuristic AI on fork (fix before M2 forking). First implementation step: instrumented callback-frequency census.
- **Bridge protocol v0 drafted (2026-07-03): [bridge-protocol-v0.md](docs/design/bridge-protocol-v0.md).** Wire schema is six game-agnostic *answer shapes* (SELECT_ONE/K, INT_IN_RANGE, BOOL, ORDER_N, CONSTRUCT); Magic semantics are namespaced string tags + opaque context (data, not schema — §1 hygiene at the wire). One bidirectional gRPC stream per worker; server-side `bridged_tags` config drives coverage; one-shot cast = composite `CastPlan` answer behind a config flag; fallback is a first-class response with provenance telemetry; trajectories stay worker-side (bridge carries decisions only). M0 measurement plan: three-arm throughput delta vs the 3,818 g/h baseline.
- **Batch-harness spec done (2026-07-03): [m0-batch-harness-spec.md](docs/design/m0-batch-harness-spec.md).** Python orchestrator in `anvil/bridge/harness/`; the **chunk** (200-game worker invocation, exit when done) is the single mechanism behind recycling, resume, pause, and crash isolation; `run.json` manifest is the formal per-run pinning machinery (fork commit + jar sha256, worker refuses on mismatch); `replay <run> <index>` is a first-class verb; defaults table transcribed from ADR-0002 (w=16/12, 2g heaps, ActiveProcessorCount=2, nice 19).
- **M0 implementation and measurement COMPLETE (2026-07-04).** Calibrated three-arm run (w=16, 320 games/arm, orchestrator-managed): heuristic 3,016 g/h / local-random 1,772 g/h / gRPC-echo 1,727 g/h → **bridge tax 2.6% at w=16; zero transport failures; 314/320 games bit-identical across transport** (6 divergences = the known heuristic-fallback nondeterminism floor, same winners). **The M0 calibrating number: ~1,700+ games/h with the bridge in the loop** (~41K games/day). Measured table in [bridge-protocol-v0.md](docs/design/bridge-protocol-v0.md). Closed out by [ADR-0003](docs/decisions/ADR-0003-m0-closeout.md).
- **Batch orchestrator landed (2026-07-04, Anvil `8b5037c`, fork `80b66aa262`):** `uv run python -m anvil.bridge.harness launch|pause|resume|status|replay|summarize`. Chunk mechanism verified end-to-end (pause mid-run → resume → 24/24 unique games, no dups/gaps); seeds are a SplitMix64 keyed stream with a Java↔Python lockstep test (`tests/test_seeds.py` — `base^i` collided across nearby bases, caught by test, fixed both sides); jar-hash verified per launch; zero-progress abort guard distinguishes systemic failures from per-game skips. **Replay caveat measured:** solo replays are self-consistent but can drift from in-run instances (warm-JVM identity-hash order + AI wall-clock timeouts — ADR-0002's nondeterminism, now at harness level); spec amended.
- **gRPC bridge live end-to-end (2026-07-04, fork `4d6f929057`, Anvil `b9ea529`).** Canonical proto at `anvil/bridge/proto/anvil_bridge.proto` (synced copy compiled into forge-gui-desktop via protobuf-maven-plugin); Python decision server `uv run python -m anvil.bridge.server` (echo + random modes, server-driven `bridged_tags`); Java `GrpcBridge` (one bidi stream, blocking round-trips, deadline→local-answer fallback). **Echo instrument verified: gRPC-arm games bit-identical to local-arm across 20 seeds. First bridge-tax number: ~2.3% single-worker** (132s vs 129s / 20 games, ~full M0 tag set) — in the census-projected band; the calibrated multi-worker number comes with the orchestrator.
- **`PlayerControllerAnvil` landed (2026-07-04, fork commit `56c96c6c40`, `forge anvil` command).** Extends `CensusPlayerController` (census log doubles as the provenance log: `by=bridge` vs implicit heuristic-fallback); M0 tag set routed through the `AnvilBridge` seam; `LocalRandomBridge` answers from `MyRandom` (deterministic per seed, transport-free control arm). Priority materializes engine-legal options; picked spells go through `canPlaySa` (targets/X pre-set AI-path style) with veto→pass. Smoke (5 games): all decisive, 5.8 s/game, ~656 bridged priority windows/game, **65% veto rate** — `canPlaySa` applies heuristic judgment, not just legality, so M0 plays "random proposal, heuristic-gated"; acceptable for bridge-tax measurement (round-trips unaffected), revisit with M1 CastPlan targeting.
- **Callback census done (2026-07-03), DC-pool re-run done (2026-07-04): [callback-census-results.md](docs/design/callback-census-results.md)** (precon: 500 games/388K callbacks; pool re-run: 500 games/568K callbacks across 100 pairs from pool `f568b187`, driver `scripts/census/run_dc_census.py`). Precon headlines: `chooseSpellAbilityToPlay` 56% of traffic; 45/109 methods fire; cast path confirmed (targets/X pre-set on SA, optional costs fold into priority answer, modes at two interception points; mid-resolution 100% outside cast windows). **Pool re-run: 64/109 methods fire; callbacks/game +46% (mean 1,136) on turns only +22% (median 21) — plan M1 on ~⅔–¾ of precon-calibrated g/h; priority still 51%, top-5 = 80%, ranks held; cast-path structure survives; M0 tag coverage now measured-on-pool (~52%). Flag: `chooseCardsForConvokeOrImprovise` hit 2,788 calls in one game — apparent AI re-evaluation loop, look before M2.**

## M7 — verbose running record, snapshot 2026-08-12 (milestone OPEN; live compact summary in CLAUDE.md)

Snapshotted verbatim from CLAUDE.md during the 2026-08-12 documentation cleanup pause. ADRs 0050-0055 + m7-plan.md are authoritative for every decision below; this preserves the running narrative exactly as it stood.

- **M7 OPEN (2026-08-10, [m7-plan.md](docs/design/m7-plan.md) + [ADR-0050](docs/decisions/ADR-0050-m6-closeout.md)): the credit-assignment question — can dense per-decision signal reopen improvement?** Headline gate = standing combined paired read vs **0.5373 ± 0.0112 ([ADR-0055](docs/decisions/ADR-0055-d3-boundary-close.md) D3 boundary CLOSED 08-12: fork era `d798917ae5` = upstream rebase + stability pass + monarch NPE + forced-seq extension + the Talor-#9 grpc decision_seq/poisoned-stream fix; 1,999-game re-baseline 0 crashes / 0 poison events; iter-019 HELD through the rebase; fidelity 11.6% divergence w/ FIXED_HASH discriminator annexed; #9 label sweep ≤181 suspect rows, no conclusion threatened; λ re-tune licensed for the next mixture)**. Opened on ADR-0049’s verdict: the §6c penalty is the loop’s only dense per-decision signal; terminal outcomes at 20+ turns cannot shape timing. **Fork-index namespace fix LANDED same day (fork `a73ee9d4e4`, FORK_G_BASE = 1e12, int→long, smoke-proven: g=0 mainline + fork stores join cleanly; the run13-crash landmine closed; era-scoped — never mix pre/post-base stores in one MultiStore join).** **D1 P0 RESOLVED same day ([ADR-0051](docs/decisions/ADR-0051-p0-decision-delta-probe.md)): gate NOT MET — natural-variation dense labeling falsified as an instrument.** `scripts/decision_delta_probe.py` on run13’s in-loop fork stores (299 points / 2,392 completions; old-era support 797 points): within-fork-point action splits at K=8 are starved (split frac **4.0% / 6.5%** vs the 0.30 pin — the sampled policy is confident at ~95% of drilled decisions) and where splits exist the differential is sampling noise (**RMS true Δwr 0.082 / 0.000** vs the 0.10 pin; directional hold-then-act read null both eras). Sharper: per-cell sampling variance ~0.115 puts the pinned threshold below the estimator’s noise floor at ANY achievable split count — the method fails, not just the result. **Routing per the pre-registered pin: D2’s candidate is now forced-branch paired rollouts at drilled decisions** (force act vs hold as the first post-fork decision, K completions per branch, paired seeds cancel shared downstream luck, K = a resolution dial) — simultaneously tier-3’s first rung (1-ply search targets), design §6’s contrastive-pair generator, and the instrument P0 lacked. C2a (fork-point wr value targets) rides as a bundled aux loss only; C3 (§6c economy re-tune: chain-vs-independent telemetry read → λ decay or first-attempt-only) untouched by P0; D3 fork stability pass now rides with the forced-branch build (same fork-rollout-heavy justification). Standing lesson recorded: check a gate threshold against the estimator’s resolvability at design time. **Forced-branch harness BUILT + smoked (same day, session 2; design pins user-approved in m7-plan D2): act = on-policy with pass masked (`forbid_decline` protocol flag, server logit mask), hold = one forced pass, NO forced-branch obs in v1 (labels-only; `--fork-instrument` = sampled wire-fork serving with no mu), pairing = shared rollSeed per (fp,r), pairs with crashed/skipped members drop whole; fork `694e9c93f9` (`-forcebranch`), `scripts/forced_branch_read.py` is the analyzer. Census runaway cap also landed (fork `f9ec33ca8a`, per-game 16 MiB `anvil.census.rawcap` — the 6/6 stale-pass trim pattern retired; cause was a wedged 360s game with Obs capped but census unbounded). **K-sizing read RESOLVED (2026-08-11, [ADR-0052](docs/decisions/ADR-0052-ksizing-read-map-serving-mismatch.md)): NULL at every K ≤ 16 on a corrected population — C2b per-decision Δwr labels falsified; and the read found the standing drill-map serving mismatch:** `gs generate` replayed mainlines argmax over sampled sources since curation went sampled-source, so map winrates priced divergent states (cycle3: map 0.374 vs true 0.062, corr 0.23; true crash states 78% dead; the selection band never described the real states). Fix landed (`--sample-mainline` + manifest stamp); corrected three-anchor true maps in `data/runs/drill-map-cycle3-true/` (crash−2 contested at 0.491, 52% band mass); corrected read on 59 in-band forced points = pure 1/√K noise, RMS true Δwr ≤ ~0.08 vs the 0.10 pin — single-decision deltas are small in truth; the signal lives in sequences (composes with ADR-0049). Forced-branch instrument itself validated (hold ≈ natural corr 0.885, 0 crashes) and stands as tier-3's fork substrate. **Standing rules born here:** blast-radius sweep on instrument bugs (past nulls through the buggy path are suspect, positives survive — ADR-0052 table: run11 reread as distributional supplementation, case-drilling never actually tried; one-shot verdict applies to supplementation); sampled-source maps REQUIRE `--sample-mainline`; stale-data passes keep source `run.json`+`pairs.txt`. **D2 routing PINNED (user 08-11): C-probe → B-vehicle → A-parked. Sequence probe BUILT (fork `6d2f44c9d3`, `-forceseq` persistent directive: natural/hold-N/act-N × K paired arms) and RESOLVED across three rungs same day ([ADR-0053](docs/decisions/ADR-0053-sequence-probe-resolution.md)): the C bundle is FUNDED — deferral cost compounds (−2.6pp @ N=2 → −6.1pp @ N=4, t=−4.49, ≈ −1.5pp per held turn, 27/28 points negative), per-point RMS true Δwr grows with horizon to 0.090/0.085 ≈ the 0.10 label pin at N=4 (single decisions: zero — plan-granularity credit is the confirmed axis), timing ordering natural > greedy ≫ hold (pure aggression is NOT the target; act−nat −1.7pp at N=4). Trainable contrast = act−hold (2 forced arms), 64 completions/point at N=4/K=32. **C-bundle design PINNED (user-approved 08-11 session 3, [ADR-0054](docs/decisions/ADR-0054-c-bundle-design.md)):** C-seq = advantage × specific-cast contrast at the mainline fork window (L_seq = −Â·[logp(cast*) − logp(pass)], Â clipped ±0.25, cast* = act arm's modal first cast; labels-row extension records it), campaign P≈100/iter at K=16 fresh each iteration (~+1h/iter), w_seq ≈ 10% of PG mass; C2a = wr_K(fp) aux BCE in the PER-ITERATION CRITIC PHASE (discovered: pass-A values come from the iteration-retrained critic — the masked-head-only wording would never reach the advantage) + same aux on the masked head; C3 = **first-attempt-only + λ=0.01** — the pre-work chain read (new standing `scripts/rejected_chain_read.py`; 20 run13 stores, 49,902 events, 5.2/traj) found 57.8% singletons + short chains (first-attempt-only alone cuts only 23.5%), so the over-pricing is λ magnitude (0.02/event > 0.015 = one held turn's measured cost; 76% of veto windows end abandoned — penalty AND tempo both paid); per-window exposure now < one held turn, total ~38% of current; λ change rides the D3 boundary. Owed pre-run: ADR-0049 cast-suppression/holding reads productionized (the gate's attribution instruments). Next: D3 boundary (stability pass + harness extension + forkcheck + re-baseline) → the run vs the standing gate. Fork PR #1 (Talor, AiBlockController block-legality cache, claimed −15.6%): VERIFIED 08-11 — behavior-identical per the ADR-0025 empirical gate (499/500 cross-jar mainline trace identity, FIXED_HASH pair; the 1 mismatch exonerated: seed 20260844 is same-jar launch-nondeterministic, baseline disagrees with itself 1-in-3) but the speedup does NOT reproduce on our workloads (loop generation 1882.5 → 1867.1 g/h = −0.8% noise; heuristic fork-replay segments −1.4%; per-game distribution identical through p99/max — likely workload-dependent: his bench is stock-vs-stock full games with a hot `predictNextCombatsRemainingLife`, our DC pool mix isn't). Instrument note: cross-jar identity = FIXED_HASH=1 forkcheck pair + mainTraceHash join, single-seed same-jar reruns as the nondeterminism discriminator. **MERGED 08-11 (`c758be07a6`) + audit follow-ups: wrapper improvements committed (`e598d20bdd`, pair-cache persistence + pure-pair short-circuit, identity 499/500 same-outlier-seed); JFR profiling found the REAL whale — `AnvilOptions.priorityOptions` = 55.5% of in-game worker CPU on the loop config (cast enumeration, statics 48.6%), 64.3% of asks repeat the previous mask; mask cache built + equivalence-proven via the new obs-diff gate (v1 caught 3 staleness classes: MagicStack-vs-stack-zone, timestamp-invisible hand arrivals, tapped state; v2 clean on 3,346/3,346 windows) and committed DEFAULT OFF (`4c7d21cd6a`) because generation at w=16 is NOT engine-CPU-bound — wall-clock is ~1,000 serialized bridge round-trips/game (3 ms micro-batch floor), all engine-CPU deltas wash (genbench 1845–1882 g/h across all four jars). Standing instrument born: the obs-diff gate (same-seed A/B, window-by-window recorded-mask identity) = the mask-cache re-enable protocol after any rebase. PARKED (user 08-11, do not interrupt M7): w-scaling retest 16/24/32 under the new CPU profile; adaptive micro-batch window (close early on queue drain).**

## Playable-fork track — v12–v14 continuation, snapshot 2026-08-12 (compact summary in CLAUDE.md)

Continuation of the playable-fork section above (which covers through v11). Snapshotted verbatim from CLAUDE.md during the 2026-08-12 documentation cleanup pause.

- **Playable-fork track (2026-07-26→30): founding worklist items 1–8 ALL BUILT, PUBLISHED, and user-verified in play; current release v14 (2026-08-02, `881207ed27`, shipped live mid-game-night: **net-lobby fixes** — (1) upstream #11190 (`76eaaa010e`, arrived via v12's refresh) made `FTextField` fire CHANGE per keystroke for live search filtering, breaking every committed-value consumer: chat sent one message per letter, the lobby name field broadcast full slot state per keystroke, echo `setText` reset carets ⇒ reversed typing; now opt-in `setLiveChangeEvents`, no caller opts in. (2) STOCK team-selection echo, exposed on the first 2v2 attempt: `FComboBox` fires handlers on programmatic sets + the client wire listener drops the panel index + the hardened server applies client events to the sender's own slot ⇒ any team change cascades until the lobby converges onto one team; guards: `!mayEdit` + network-apply suppression + mid-edit `setPlayerName` skip. Both queued upstream — upstream mobile chat is per-keystroke broken on master today. Prior v13 2026-08-01: updater orphan-deletion pass (stale `Code=HOC` edition collision; jar-only-manifest floor + 500-file cap + case-insensitive skip; cleanup fires on the first update APPLIED BY a v13+ jar). Publish MUST pin `-R Tyrathalis/forge` — gh default repo is upstream, same release tag name. Friend-install pins: Temurin 21 JRE via the adoptium `installer/latest` permalinks; macOS 15+ Gatekeeper flow (Settings→Privacy & Security→Open Anyway) baked into the zip's `Forge.command`). Residue: T2 live-rescale only (workaround: launch at play size).** [playable-fork-worklist.md](docs/design/playable-fork-worklist.md) is the doc of record. A human-facing distribution build on fork branch `playable` (worktree `../forge-play`; the pinned research fork `master` has zero delta from any of this). Shipped: resizable window + live viewport (six frozen-size layers found and fixed: hints/bounds/projections/selector/backdrop/headers), deck-site URL import on mobile + bulk Archidekt sync (per-username flat folders under `decks/URL/`, never-move re-sync, Unknown-format sentinel + lazy `Unsorted/`, annotate-not-quarantine; Moxfield deferred — relationship problem, not engineering), configurable tap angle `UI_TAP_ANGLE` 90–15° on both clients (shared `RotatedRect` inverse-rotation hit-tests, identity-gated at 90°; en route caught and fixed the stock nested-transform bug — mobile `startRotateTransform` never composed), slot packing + single-column zones, in-place delta auto-updater repointed at the fork (per-file manifest delta, hash-verified, staged-jar swap; hardened by three field incidents: `..`-substring path check, percent-encoding for 30K+ space paths, bridge manifest + res self-heal; **v4→v5 and two-hop v7→v9 self-updates verified live**), Adventure horizontal hand. Release pipeline: `scripts/release-playable.sh` → `daily-snapshots` prerelease; post-publish routine parses the published manifest through the shipped jar (standing lesson: verify the production artifact through the production code path). Stock bugs found en route, fixed on `playable`, queued as upstream candidates: nested-folder storage stub, `RestartUtil` Runtime.exec-tokenization (desktop restart never worked), quest BALANCED all-colors empty pool, plus the tap-angle hit-test generalization. **Standing hazards:** the playable build shares Forge's user deck store with the research harness — `launch --pool` gates on installed-deck content hashes (Anvil `89178d7`); never check out `playable` in the research worktree. **Multiplayer hardening (ran hidden alongside, CLOSED 07-30): [multiplayer-hardening.md](docs/design/multiplayer-hardening.md) is the public record** — 9 findings, 4 private advisories, **3 merged to public Forge master byte-identical**, 2 declined-and-documented as trust-model properties (host is not an authoritative referee; seat reclaim by username is intended); chat rate limiting = public PR #11457; both declined patches live on `playable`; method notes (test-must-fail-first, measured allowlist, two deser sinks, size-as-review-currency) recorded there. `origin/playable` @ `2900701083` = QoL + upstream master + security, consolidated; fork repo tidied to exactly `master` + `playable`. Full blow-by-blow: [status archive](docs/status-archive.md).

## Chronicle side stream — running record through D3 v1, snapshot 2026-08-12 (track ACTIVE; live compact summary in CLAUDE.md)

Snapshotted verbatim from CLAUDE.md during the 2026-08-12 documentation cleanup pause. ADR-0029 + chronicle-mvp-plan.md are authoritative.

- **Chronicle SCHEDULED (2026-07-31, [ADR-0029](docs/decisions/ADR-0029-chronicle-scheduling.md) + [chronicle-mvp-plan.md](docs/design/chronicle-mvp-plan.md)): the collection/incremental mode graduates from parked concept to a playable-branch side stream** (sibling to the QoL track; M4 keeps the research hours; long-range goal = upstreaming via the fork-first/re-cut strategy). Scope = MVP slice v0 from [collection-mode-sketch.md](docs/design/collection-mode-sketch.md) (1993–94 Alpha→FEM collector loop; battler/market/foils out). Session pins (user): **MVP income = allowance stipend** (gap found: with the battler out and pack-cracking EV-negative by invariant, currency strictly drained — the LGS was unreachable); day tick fires on **ration collection** (≤1/real day, local + ~4am grace); **seed-integrity invariant** (all daily randomness from (run seed, day index, domain); sealed items commit contents at acquisition — no savescum rerolls); prestige-proof save schema (run-id + meta split) from day one; **hidden behind a pref until the author's 2-week dogfood gate passes**; Android packaging spike parallel-early (dogfood is phone-based; sideload, no Android updater yet). Architecture: headless services `forge-gui/.../gamemodes/chronicle/` (Quest precedent — headlessness enforced by module structure), screens `forge-gui-mobile`, data `forge-gui/res/chronicle/`, tests in the desktop TestNG tree, Adventure `SaveFileData` persistence. Staging D0 paperwork (done) → D1 headless core (gate: simulated-fortnight test) → D2 screens on `mobile-dev` → D3 reveal scene (bar DEFINED same session: period-wrapper identity evolving w/ the timeline — the reveal object is the set's real booster art, foil gloss debuts w/ the 1999 event; tier-2 player-paced mechanics; BOTH registers day one — ceremony + batch w/ auto-pause, because the day-one starter is 60 cards and the LGS sells boxes; harvest+tiny sound) → D4 Android spike (parallel) → D5 dogfood + numbers pass → visibility flip + Discord courtesy float. Stretch: kitchen table (ownership-legality only). **Same-session design rounds also closed: awards first pass** (avatars via scaled crop derivation on the mode's home page + Chronicle-clipping scrapbook trophies; standing principle: cosmetics derive at RUNTIME from card images Forge already downloads, no baked/generated assets; aesthetic north star = purposeful-retro lo-fi, decision deferred) **and famous-card allowlist v0** (path-shaping ONLY — endpoints backout-guaranteed; one entry per STORY, event-anchored legs w/ backout-consistent settle multipliers; ~12-entry seed incl. RL 3-leg + Juzám + Shivan-deflation; seed+calibrate authoring; **MVP buylist amended: + hand-authored 1994-desirability tier table = the allowlist file's first column** — rarity-flat would sell a pulled Lotus like bulk, all-prices.txt tiers are anachronistic and break the pack-EV invariant). **D1 HEADLESS CORE DONE same day (session 4, fork `playable` @ `574abedc08`): done-when gate GREEN** — simulated fortnight (14 scripted days: day-0 starter moment, Alpha shelf turnover day 7 + payday, mid-fortnight save/reload w/ digest-identical continuation, no-savescum reopen) + 27 service unit tests, 30 total in the desktop tree; full desktop suite 400 green. Archaeology dividends: **seed-integrity worked day one** (every RNG site on the 1993-94 booster path routes through the determinism-hooks thread-local `MyRandom` ⇒ committed contents-seed = `setRandom(new Random(seed))` per pack); `SaveFileData` is libGDX-bound ⇒ headless port w/ Quest-style version ladder + Adventure containsKey defensiveness; **no FModel entanglement** (Adventure own-singleton pattern; shared-file footprint = 3 additive `ForgeConstants` lines); notables priced by NAME only (ATQ prints split rarities per name); the 1994 tier table shipped grep-verified (Lotus ×30 = $12 buylist ≈ the period dozen packs) and **the exact sheet-computed pack-EV ledger verifies EV-negative for every product even at max LGS discount**. The gate suite caught a real hole: the tick guard needed *strict monotonicity* on the effective calendar day (equality would let a wound-back clock re-arm the tick — clock-scum, the sibling of savescum; plan pin 5 addendum). Carried: box-as-item (stage 2), `BoosterSlot.replaceSlot()` raw `Math.random()` landmine before any post-2018 window. **D2 SCREENS v1 BUILT same session (`bb7e670cff`, D1 pushed as `574abedc08`): full mobile surface** — pref-gated New Game entry (`CHRONICLE_MODE_ENABLED` default OFF, Settings→General toggle, applies without restart since buildMenu re-runs per open; never persists as preferred screen), home screen (session-script spine w/ collect-ration flow + day-summary), binder shelf (booster-art tiles + completion bars + NEW counts) → spread (full-set collector-order grid; **all four archaeology gaps closed via GENERIC upstreamable ImageView plumbs**: NEW/OWNED column overrides modeled on the live fnPrice pattern — the scouted fnIsFavorite is assigned-but-never-read — driving NEW badge/qty pips/unowned rendering, grayscale on-disk / dimmed placeholder otherwise w/ deliberately no network fetch for unowned; group-caption composer hook for completion; `CHRONICLE_BINDER` config w/ pileBy=null since pile-by renders fanned stacks not grids), LGS screen (MSRP shelf + seeded deals + sold-out), paper screen, tap-through placeholder opener (D3 owns the real reveal). Headless riders desktop-tested: ChroniclePaper (templated issue + 15 seeded era-flavor lines), ChronicleData.setUniverse (collector-order resolver; test pins ATQ split-rarity variants), markSeen batch. Suite 32 green; mobile-dev jar boots clean (must run from the module dir — repo root resolves the wrong ../forge-gui). **D2 gate PASSED 2026-08-01 (author's interactive pass: begin/paper/allowance/opening/binder all work) — D2 DONE.** Feedback round 1 landed same day (`59adfb8707`): **ChronicleAcquisitionLog** (provenance journal: every opening recorded w/ per-card first-pull ordinals, persisted in the run; `sourcesFor` = which products yield a printing — basics are starter-only under Forge's era sheets), binder Sort:New = TRUE acquisition order, deals above the shelf, reveal long-press → match-inspect CardZoom w/ oracle flip (binder spread already had it via ImageView), pack-wide art prefetch at open; suite 34 green. Backlog recorded in the plan: provenance UI, desktop scroll-bar indicator, stronger art preloading (D3), save-slot browser (today = one implicit autosave run, loaded automatically). Fidelity answers recorded in the plan (15-card ABU accurate; no-basics-in-boosters = engine-wide Forge sheet model, documented deviation; random 5-color starters period-accurate; collector order = canonical). **Nav trap found by the user's second pass and FIXED (`003a941818`): opening Chronicle made it the in-memory preferred New Game screen (the guard only sanitized the disk write) and its home screen had no NewGameMenu header — other modes unreachable until restart; Chronicle now excluded from preferred tracking entirely + carries the standard hamburger.** All Chronicle commits pushed. **D3 reveal scene v1 BUILT 2026-08-01 (`8c2134fdbf`, pushed): both registers** — ceremony (drag-to-tear wrapper on real booster art w/ shearing-strip fly-off, tap-to-flip w/ back-ease pop, best-last staging rarity→buylist-value, commons batch-flip on 3+ runs, final-card buildup pulse+shiver, first-pull glint+NEW badge from the acquisition log [opening-seq vs oldest-event; dupes glint once], revealed row, no-peek long-press zoom) and batch (starters/Open All: cascade w/ hard auto-pause on rare+/notable, soft hold on new uncommons, pack-art interstitials, tap pause/resume); D2-backlog art preload folded in (bounded 8s block + one-ahead pre-touch); stock-audio sound pass + haptics; summary spread w/ buylist total + Open another/all; leaving mid-reveal releases the animation driver (continuous-rendering leak caught in review). Pacing constants = one block atop `ChronicleRevealScene` (the dogfood tuning surface). Suite 34 green, mobile-dev jar boots clean. **Done-when stays open by design — experiential bar, author-judged, iterate through D5.** D4 scouted: `forge-gui-android` builds via `android-debug` profile, needs Android SDK build-tools 35 + platform 35 — not on the box; SDK install = user decision, then `mvn -P android-debug` + adb sideload. **First live test round CLOSED same evening (5 commits → `ba94fc4cfd`): dev-mode testing actions shipped** (home-screen New day + cash; timeline `devRewindOneDay` re-arms the tick w/o touching the clock-scum guard — day contents are per-day-index deterministic so seed integrity holds; 35 tests green) **+ six fixes from play**: booster art had NEVER downloaded anywhere (stock `imageKeyFileExists` returns true for all product prefixes ⇒ fetch never fired, + bare `b:CODE` keys mismatch saved filenames — `ChronicleHub.boosterArtKey` list-resolves; binder tiles were silently placeholders through all of D2), reveal-NEW cross-checked against collection counts (log-blind pre-log openings glinted re-pulls), cascade pacing slowed + soft-holds for all first pulls, pulsing tap-to-continue pill (the auto-pause was invisible), revealed-row long-press hit-test, EDT hop for fetches (fetchImage asserts EDT; bulk-open crashed from the overlay thread). Starter deck-box art: fetcher `o:` support added via generalized `fetchProductImage` (upstream candidate) but `downloads.cardforge.org` has NO DNS record — starters draw booster-art fallback, self-upgrade if rehosted. **CI flake diagnosed + fixed (`ba94fc4cfd`, upstream candidate): FCollectionTest.testCompletableFuture asserts pre-`b858b48fde` synchronized semantics — unsynchronized concurrent removes race (stress-proven: fail at 30/3000 stock, 3000/3000 clean synchronized); test mutation now synchronized; caveat recorded: live code doing concurrent FCollection removes shares the race.** Upstream-candidate queue: fetcher `o:`, `imageKeyFileExists` fallthrough, FCollection test fix, dead cardforge.org list entries, ItemManager context-menu embedded-screen offset (`10c22003df`: menu anchored on global screenPos but positioned in container-local space — off by the nav-sidebar width in mobile-dev's desktop shell, identity on phones; found via the Chronicle provenance menu, diagnosed by instrumented Xvfb-less xdotool repro on an isolated `-Duser.home` profile). NEXT: D3 iteration (200th-pack bar, Pocket side-by-side) + D4 SDK decision.


## Chronicle side stream — running record through D6 verified in play, snapshot 2026-08-24 (track ACTIVE; live compact summary in CLAUDE.md)

Snapshotted verbatim from CLAUDE.md when the bullet passed the ~2-3 KB compression rule (it had reached 11.5 KB). ADR-0029 / ADR-0070 / ADR-0071 + chronicle-mvp-plan.md are authoritative.

- **Chronicle side stream ACTIVE (scheduled 2026-07-31, [ADR-0029](docs/decisions/ADR-0029-chronicle-scheduling.md) + [chronicle-mvp-plan.md](docs/design/chronicle-mvp-plan.md)): the 1993–94 Alpha→FEM collector-loop MVP as a playable-branch side stream, hidden behind `CHRONICLE_MODE_ENABLED` (default OFF) until the author's 2-week dogfood gate passes.** Key invariants: seed-integrity (all daily randomness from (run seed, day index, domain); sealed items commit contents at acquisition — no savescum rerolls; the day tick is strictly monotonic on the effective calendar day — no clock-scum), pack-EV verified negative for every product, prestige-proof save schema, cosmetics derive at runtime from images Forge already downloads. Progress: **D1 headless core DONE** (simulated-fortnight gate green, services in `forge-gui/.../gamemodes/chronicle/`); **D2 mobile screens DONE** (author-verified: home/binder/spread/LGS/paper; acquisition-log provenance journal; nav-trap fixed — Chronicle excluded from preferred-screen tracking); **D3 reveal scene v1 BUILT** (ceremony + batch registers, pacing constants = the dogfood tuning surface; experiential bar stays open by design, iterate through D5); first live round shipped dev-mode testing actions (New day / cash / `devRewindOneDay`) + six fixes from play (headline: booster art had NEVER downloaded — stock `imageKeyFileExists` returns true for all product prefixes so the fetch never fired). Upstream-candidate queue: fetcher `o:` product-image support, `imageKeyFileExists` fallthrough, FCollection concurrent-remove test race fix (stress-proven), dead cardforge.org list entries, ItemManager context-menu embedded-screen offset. **D4 CLOSED 2026-08-22** (done-when met — the author runs the daily loop on the phone; the SDK question was already closed, and `scripts/build-android.sh` ships release APKs on the `android-debug` profile with a pinned toolchain: Maven 3.8.1, Temurin 17, `forge-gui-android/tools/uber-apk-signer.jar`, `~/.android/debug.keystore`). **D5 DOGFOOD OPEN — round 1 read 2026-08-22, three findings.** Two were reveal-scene defects, both fixed same day (`2177e66f6d`): (a) *the tear gesture was not a swipe* — `pan()` accumulated `Math.abs(deltaX)` so the return stroke opened the pack, nothing handled the finger lifting, and the threshold was 1.1 **screen widths** (uncoverable in one swipe ⇒ `fling` was the only real way in); now direction-committed one-way travel, proportional close on pull-back, spring-shut on short release, axis+direction-gated fling, anchor-against-absolute-x so lift-and-resume is correct; (b) *the animation was a tilt, not a tear* — one rigid quad slid right and rotated ≤7°; now 16 columns each whole until the tear reaches it, split on a stable per-pack zigzag, peeling with a lag from the gripped end so the split **propagates** as you pull, connected bright fresh edge, direction-following. Compiles clean; **on-device feel unverified** (held while `d6-run19` had the box). Pixel-dissolve register considered and not taken (reads digital against period booster art; the D3 bar pinned a real paper tear). **The third finding is the design result: [ADR-0070](docs/decisions/ADR-0070-chronicle-effort-reward-sink.md) — the loop has NO effort→reward channel.** Ration + stipend are grants on a timer and the buylist pays less than packs cost by construction, so nothing in the mode converts invested time into progress ("truly nothing to do with the cards but look at them"). New **pin 8, above the MVP/stage line**: the mode must contain at least one such channel; the pin outlives its first implementation. Consequences: the kitchen-table **stretch is PROMOTED to deliverable D6** (deck editor + ownership-legality + a purse — all existing Forge machinery, wiring plus an economy line), D6 is the highest-value remaining work though not required to be next, the **numbers pass is deferred behind it** (a purse re-derives ration size/stipend cadence/MSRP/buylist), and the two-week dogfood clock restarts when D6 lands. Sketch layer ordering unchanged — battler/venues/tournaments/market stay stage 2/3. Open at D6 design time: the purse's flavor (ante is period-authentic and namesake-appropriate but stakes the collection against the collection chase), how deck legality reads the inventory, the variable-income knock-on. **D6 DESIGN ROUND DONE same day ([ADR-0071](docs/decisions/ADR-0071-d6-design-round.md)): purse = cash by default + ante as an OPT-IN stake at better payout** (ante as the default inverts the mode's core loop — Chronicle is all accumulation, and ante makes the primary reward channel *subtractive from the collection*, permanently, since seed integrity forbids the reroll; as a choice it's period-accurate and adds a risk axis); **opponent = a small cast of named rival collectors with seeded collections that grow along the player's timeline**, decks generated from *that rival's* pool (difficulty rises with the era for free and in fiction; the paper gains its first narrative content; rival acquisitions derive from (run seed, rival id, day index) so seed integrity extends unchanged); **legality = reference-only, decks borrow rather than consume** (already what `DeckEditorConfig.usePlayerInventory()` means in Quest; reservation semantics rejected until the loop is proven); **generation = `DeckGeneratorBase` over a `DeckGenPool`** built from the relevant collection, with period `.thm` archetype files an optional later layer; **Anvil is the designed long-run replacement** (sketch layer 3) so rival-deck construction goes behind a narrow pool-in/deck-out seam. **D6 needs NO new machinery** — `DeckGeneratorBase` takes an arbitrary `PaperCard` pool (the collection already is one), `FDeckEditor.DeckEditorConfig` exposes `getCardPool()`/`usePlayerInventory()`, and `MatchController.hostMatch()`+`startMatch(...)` with `adventure.scene.DuelScene` as the harvest template. **Finding of record: there is NO corpus of period decklists and that is historically correct** — 1993–94 predates published deck tech, the earliest documented lists post-date FEM, and all three shipped Forge corpora are anachronistic (LDA models cover Modern/Legacy/Historic/Pauper w/ Sneak Show + Merfolk archetypes; the 505 quest precons are modern WotC precons; the 786 `geneticaidecks` are GA-evolved Legacy). **Still open — the live question, where pin 8 gets tested: the grind shape.** Unbounded paying rematches are an income exploit and clash with the no-engagement-traps stance; a hard daily cap partly defeats pin 8. Recommended and to be confirmed by dogfood, not pinned: one paying challenge per rival per in-game day, rematches free-but-unpaid, purse scaling with rival difficulty, cast growing with the timeline ⇒ time converts through deck quality and harder opponents, not repetition. **D6 BUILT same day** (`c647ba081e` headless + `92708b5452` screens; **shared-file footprint ZERO**, Chronicle suite 57 green, full desktop suite 506/0/11-skipped). Headless: `ChronicleRival`/`ChronicleRoster`/`rivals.txt` (5-kid seed cast), `ChronicleRivalPool` (**a rival's collection is derived, never stored** — (run seed, rival id, **pack index**), deliberately day-free, so pack *i* is the same pack forever, day D+1's pool contains day D's, and the save carries nothing; pack *i*'s acquisition day bounds it to products **released** by then = the entire difficulty model, `packsPerDay` is the kid's allowance and there is no power knob), `ChronicleDeckSource` (the ADR-0071 seam) + `ChronicleDeckBuilder` (fences forge-core's generator: **count-blind** DeckGenPool clamped to owned counts, colours picked as the collection's deepest two instead of random, and **basics supplied outside the collection** in a period edition since era sheets put none in 1993-94 boosters ⇒ a pack-only rival owns zero lands; determinism free — every forge-core generator routes through `MyRandom` like `BoosterGenerator`), `ChronicleDecks` (reference-only, shortfall reported never silently rewritten), `ChronicleKitchen` (purse once per rival per played day, a loss doesn't burn the day), `pursebaseCents` knob, run/controller wiring. Mobile: home-screen Kitchen Table entry showing uncollected purses, `ChronicleKitchenScreen`, `ChronicleDeckScreen` + an `IDeckController` routing the **stock** `FDeckEditor` into the run — **decks live in the Chronicle save, NOT Forge's shared deck store** (load-bearing: the playable build shares it with the research harness, whose `launch --pool` gates on installed-deck content hashes); editor's only hook is `setPlayerInventorySupplier`. **`ChronicleMatch` needed no shared-file change** — Adventure reports results via an `isMobileAdventureMode` branch inside `MatchController.finishGame()`, which is exactly what the isolation convention exists to avoid; `HostedMatch.setEndGameHook` + `getGame().getOutcome().isWinner(...)` replaces it. Two rules pinned rather than inherited: **one game, not a match**, and **ante forced OFF** (global `UI_ANTE` must never reach this table — ante is opt-in per ADR-0071 and the opt-in isn't built, so inheriting would silently gamble the collection). **ANTE BUILT same day (`111a441f74`) — the grind shape RESOLVED as TWO SPEEDS** ([ADR-0071 addendum](docs/decisions/ADR-0071-d6-design-round.md); user counter-proposal, better than the plan's recommendation): **cash bounded (once per rival per day), ante UNBOUNDED and priced in risk instead of time.** Verified against the implementation, not argued: by volume ante is a poor source (1 card/won game ≈ 4–10/hour flawless vs 30 free cards/day from the ration), and `Game.chooseCardsForAnte` draws rarity **uniformly over the classes both libraries share** ⇒ ~1-in-3 games stakes a rare (vs 1-in-15 in a pack) — gated behind winning, symmetric since the player stakes their own at the same rate. Self-limiting three ways not by a timer: stake comes from your own deck, the rival's pool really depletes, they decline below `anteRivalFloorCards`. **Architectural consequence + a mechanic that disappeared:** rival pools were seed-pure with zero save state and ante moves real cards ⇒ persist only a delta (`ChronicleRivalLedger`), `pool = derived(packs by day) − lostToPlayer + wonFromPlayer`. **The catch-up mechanic proved UNNECESSARY** — the derived term grows on schedule so a stripped rival recovers as their allowance rolls in; no rubber band, no engagement-trap spring — and it **bounds the world's card supply** (total extractable ≤ the derived curve), so an unbounded channel cannot outpace the release calendar and the scarcity engine + pack-EV invariant survive. Free: a stripped rival plays a worse deck (difficulty self-corrects in fiction); "run out of spares" is a legible state. Stakes = Forge's real ante (random card from the DECK, rarity-matched, basics excluded, `GameOutcome.AnteResult`) so your best cards are both win condition and collateral — period-authentic, permanent under seed integrity, confirm dialog says so, and **never inherited from the global `UI_ANTE` preference**. **The provenance journal now records departures** — `Source` enum (BOOSTER/STARTER/ANTE_WON/ANTE_LOST) keeping `SealedItem.Kind`'s names so pre-ante saves load unchanged; an ante win takes a first-pull ordinal. Suite 64 Chronicle / 513 desktop, footprint still zero. **NEXT: on-device verification of the whole loop (tear + kitchen table + ante); publish desktop+APK together.** Full running record: [status archive](docs/status-archive.md).

## M7 (archived verbatim at close, 2026-08-16)

- **M7 OPEN (2026-08-10, [m7-plan.md](docs/design/m7-plan.md) + [ADR-0050](docs/decisions/ADR-0050-m6-closeout.md)): the credit-assignment question — can dense per-decision signal reopen improvement?** Opened on ADR-0049's verdict: the §6c penalty is the loop's only dense per-decision signal; terminal outcomes at 20+ turns cannot shape timing. **Headline gate = standing combined paired read vs 0.5373 ± 0.0112 on fork era `d798917ae5`** ([ADR-0055](docs/decisions/ADR-0055-d3-boundary-close.md), D3 boundary closed 08-12: 1,999-game re-baseline, 0 crashes / 0 poison events; iter-019 HELD through the rebase — the first boundary with no re-scale down). Ckpt of record stays `d6-run11/iter-019`. Era fidelity: 11.6% divergence = a **deterministic copy-state class** (identical 58-seed divergent set across three runs / two jars / both identity-hash modes; repro list `data/forkcheck/run-20260812-fixedhash`; queued on the upstream-worklist, forensics ride the GameCopier→GameSnapshot consolidation; does NOT reopen the boundary).
  - **Resolved so far:** D1 P0 natural-variation dense labeling FALSIFIED ([ADR-0051](docs/decisions/ADR-0051-p0-decision-delta-probe.md): action splits starved at ~95% policy confidence, and the pinned threshold sits below the estimator's noise floor at any split count; standing lesson — check a gate threshold against the estimator's resolvability at design time). K-sizing read NULL at every K ≤ 16 on a corrected population ([ADR-0052](docs/decisions/ADR-0052-ksizing-read-map-serving-mismatch.md): single-decision Δwr is small in truth — the signal lives in sequences; the read also found + fixed the drill-map serving mismatch, so sampled-source maps REQUIRE `--sample-mainline`; standing rule — blast-radius sweep on instrument bugs: past nulls through the buggy path are suspect, positives survive). Sequence probe FUNDED the C bundle ([ADR-0053](docs/decisions/ADR-0053-sequence-probe-resolution.md): deferral cost compounds ≈ −1.5pp per held turn to −6.1pp @ N=4; timing ordering natural > greedy ≫ hold, so pure aggression is NOT the target; plan-granularity credit is the confirmed axis; trainable contrast = act−hold, 64 completions/point at N=4/K=32). **C-bundle design PINNED (user-approved, [ADR-0054](docs/decisions/ADR-0054-c-bundle-design.md)):** C-seq = advantage × specific-cast contrast at the mainline fork window (w_seq ≈ 10% of PG mass; campaign P≈100/iter at K=16, fresh each iteration); C2a = wr_K(fp) aux BCE in the PER-ITERATION CRITIC PHASE + the masked head; C3 = first-attempt-only + λ=0.01 (chain read `scripts/rejected_chain_read.py`: the over-pricing is λ magnitude, per-window exposure now < one held turn).
  - **Engine/infra this era:** Talor fork PR #1 (block-legality cache) verified behavior-identical per the ADR-0025 empirical gate (499/500 cross-jar trace identity) and MERGED (`c758be07a6`); the claimed speedup does not reproduce on our workloads — generation at w=16 is bridge-round-trip-bound (~1,000 serialized round-trips/game at the 3 ms micro-batch floor), not engine-CPU-bound, so the real CPU whale (`AnvilOptions.priorityOptions`, 55.5% of in-game worker CPU) got a mask cache that is equivalence-proven and committed DEFAULT OFF (`4c7d21cd6a`). Standing instrument born: the **obs-diff gate** (same-seed A/B, window-by-window recorded-mask identity) = the mask-cache re-enable protocol after any rebase. Talor #9 grpc off-by-one CONFIRMED + FIXED (decision_seq check + poisoned-stream on the disposable-worker machinery; label-span sweep ≤181 suspect rows, no conclusion threatened). Era-scoped store notes: FORK_G_BASE namespace fix (`a73ee9d4e4`) — **never mix pre/post-base stores in one MultiStore join**; forced-branch (`-forcebranch`) + forced-seq (`-forceseq`) harness forks are tier-3's fork substrate; census 16 MiB rawcap.
  - **PARKED (user 08-11, do not interrupt M7):** w-scaling retest 16/24/32 under the new CPU profile; adaptive micro-batch window.
  - **ADR-0049 instruments DONE (08-12) as the standing analysis battery** ([run-analysis-protocol.md](docs/design/run-analysis-protocol.md) pinned + `anvil/evals/battery.py`, auto-run by the driver (per-iteration + run-end) and `final_read` (eval read); anomaly lines ride the completion notifies — read-by-default): behavioral delta (null control exact zeros; known-positive reproduces run13's cast-suppression 17.4% changed / 34% cast→pass), holding read (candidacy-based v1; era baseline: model hold 0.869, same-turn hold-then-cast 0.185 vs heur 0.348), monitor curves (retroactively auto-flags run13's kl_mu 5.3× growth). Era baseline artifacts: `data/runs/d3-rebaseline-analysis/`. Exploratory flag on file (NOT a verdict): rebaseline seed-half gap 5.15pp (~2.3σ, single parity split; the 0.5373 gate stands).
  - **C-bundle BUILT (08-12 session 3, all four ADR-0054 components):** C3 `--penalty-grouping first` (chain continuations free; `event` kept for era reproduction); `anvil/training/seqlabels.py` = the label↔window join (Â/cast*/wr_nat from the 3-arm rows — the natural arm supplies C2a's wr_K(fp) free); C-seq `seq_pass` in rl.py (L_seq + masked-head aux, w_seq auto-calibrated to ~10% PG magnitude over the first 50 steps → `seq_calibration.json`); C2a critic-phase aux in finetune_value (`--seq-every` 10 ≈ 10% of batches — the channel that reaches pass-A values); campaign = `grindstone generate --force-seq` + driver `_seq_phase` riding the drill slice, fresh labels every iteration. Deviations recorded: 3-arm campaign on the era jar (2-arm trim waits for the next boundary; ~1.4–1.5h/iter at P=100/K=16), argmax mainline both phases (labels ≡ windows states). Validated: 4 unit tests + live join smoke on a run13 fork store (specific-cast, mass-fallback, clip paths all exercised); suite 159 green.
  - **Run-up COMPLETE (08-12 sessions 4–6): d6-run14 LAUNCH-READY** — curation regenerated from the d3-rebaseline stores (418 addressable / 45.2%, replicating cycle 3; migration read distribution-stable; c2-era isotonic drift flagged, fresh era remap owed from the new map labels); three-anchor K=16 maps (band 66–71%, argmax sources so no sampled-mainline requirement); recipe PINNED (user): windows-only mixture + drill K=2, crash−2 in-band anchor, evalset v4 rebuilt (baseline 0.4778), ADR-0054 constants; selection v5 → seat-activity-screened to **`drill-selection-v5-active` (99 points = P≈100 exactly)**; w-bench (peak w=24; run splits gen 8 ‖ campaign 16); driver gained `--overlap-campaign`/`--campaign-workers`/`--drill-windows-only` (+~25% wall back, ~2h05m/iter ≈ 1.75 days); smoke CLEAN (join 7/7, w_seq calibrated + carry-forward fix, aux-logging fix, battery end-to-end). **d6-run14 LAUNCHED 08-12 21:16 → GUARD HALT iteration 2 (08-13, [ADR-0056](docs/decisions/ADR-0056-run14-seq-divergence.md)): the unbounded-L_seq runaway — a BUG, not a falsification; no gate read.** seq_raw grew −0.22 → −8.5 across three iterations under the frozen w_seq (the contrast is unbounded log-prob; calibrate-once assumed |L_seq| stationary — false; by the halt the seq gradient was ~40× its 10% PG-mass target); downstream: veto 0.211→0.384, rej chains 5→13, hold-then-cast moving *away* from heur (0.241→0.194 vs 0.345). Guards worked (ADR-0017); iter-000/001 ckpts veto-damaged, discarded. **Fix BUILT + tested (suite 162 green): L_seq hinged (`--seq-margin` 6.0 — the M6 clips-at-birth rule applied to loss terms), live `seq_share` telemetry + `--guard-seq-share` 0.3 (guards the cause; kl is the symptom), battery seq curves + growth anomaly, in-phase kl abort at 5× guard.** Standing lesson: any auto-calibrated weight needs its invariant instrumented and guarded. Pre-registered secondary read (not gate-bearing): hold-then-cast toward heur ~0.345. Iteration wall re-anchored: ~2h45m/iter (campaign at P=99 is the pole). **Seqfix smoke PASSED → d6-run15 ran 3 clean iterations → user-authorized STOP ([ADR-0057](docs/decisions/ADR-0057-run15-share-drift.md)): seq_share drifted 0.206→0.286 vs the 0.1 target via DENOMINATOR drift** (PG mass falls ~3× within every epoch — run13 forensic: a standing loop property across eras, not a bundle effect; the share telemetry is just the first instrument to measure against PG mass), veto climbing 0.196→0.255 — 1–2 iterations from predictable guard death at uninterpretable share. The hinge itself worked (seq_raw plateaued, no runaway) and **the pre-registered hold-then-cast read ticked TOWARD heur for the first time (0.221→0.243 vs target ~0.345)** — encouraging but not interpretable at ~2.9× design share. **Fix: per-iteration w_seq recalibration = driver default** (safe under the hinge — the run14 missing precondition; `--seq-carry-w` = era reproduction; within-epoch share ramp 0.1→~0.3 documented as expected behavior, the mean guard tolerates it; per-iteration `seq_calibration.json` = free w_seq/PG-mass trajectory instrument). run15 stores veto-elevated — never in a mixture. Trackio queued on the map watch list (user: potential future milestone, do not start). **d6-run16 LAUNCHED (run15 verbatim + recalibration default, seed_base 20260817, ~55h): watch seq_share means ≈0.1 + hold-then-cast reappearing → the 0.5373 gate → M7 closeout ADR.** Full verbose running record: [status archive](docs/status-archive.md).

## M9 — verbose running record, snapshot 2026-08-23 (milestone OPEN; live compact summary in CLAUDE.md)

Supersedes the 2026-08-22 snapshot below (which stopped at the D4 launch);
this one carries the D4 read (ADR-0069) and the D4 control (ADR-0072).

- **M9 OPEN (2026-08-19, [m9-plan](docs/design/m9-plan.md)): the interface round — conscious mana payment (§3c) probe-gated, veto collapse as the falsifiable mechanism check; §3a turn-plan latent as the second act.** Ladder: **D1 RESOLVED** ([ADR-0063](docs/decisions/ADR-0063-m9-d1-veto-knowability.md), v2 sick-aware) — knowable 0.5392 sampled / 0.5097 argmax, premise STANDS, collapse baseline kvr 0.0588 sampled / 0.0435 argmax; **addendum 2 (2026-08-21) re-derived the argmax baseline IN-ERA from the m9-rebaselinearm stores at zero cost: knowable 0.5064, kvr 0.0432 [0.0417, 0.0447] — the boundary did NOT move the veto channel, the eras are comparable by construction.** → **D2a RESOLVED** ([ADR-0064](docs/decisions/ADR-0064-d2a-affordability-probe.md)) — `[STATE]⊕cand` AUC 0.8809, veto gap is behavioral/interface, D2b skipped → **D3 BUILT** (rung 1 [ADR-0065](docs/decisions/ADR-0065-d3-engine-capability-audit.md): the engine CAN execute directed chained payments; rung 2 [spec](docs/design/m9-payment-surface-spec.md): legality-derived goal enumeration, `SELECT_ONE` + `mtg.pay_mana_class`, float-then-apply executor; rung 3 [draft](docs/design/m9-rung3-draft.md): Option-A sub-head + zero-init `pay_kind`, +2.0 auto bias, certification harness. Two enumerator over-admission bugs found and fixed by the salvage rule — host exclusivity ([ADR-0066](docs/decisions/ADR-0066-certify-salvage-host-exclusivity.md)) and chain color feasibility ([ADR-0067](docs/decisions/ADR-0067-certify-salvage-chain-color-feasibility.md)); baseline 15.28/g consequential. **Evalset of record: 69 positive (bp 13 / ch 26 / phy 13 / wc 17) + 224 auto-correct**, forced family deferred — structurally unminable under heuristic play) → **BOUNDARY BUNDLE LANDED** ([ADR-0068](docs/decisions/ADR-0068-m9-boundary-bundle.md), Reads complete): rebase onto `23c3d2a85d` + §3c surface + store-namespace fix + obs schema v2 (choice-state) + multi-format enablement; forkcheck 10.0%, **THE M9 GATE RE-PINS AT 0.5279 ± 0.0110**, evalset revalidated 278/278 identical argmax across eras → **D4 RESOLVED, NEGATIVE ([ADR-0069](docs/decisions/ADR-0069-d4-read-adjudication.md), read session 2026-08-22, done-when 4): straight RL through the §3c interface did not teach conditional payment discrimination.** `d6-run18` 8×480, all accepted, no halts, 3h; landed in the discuss zone (FUND ≥7/64 never approached, max 4/64; CLEAN NEGATIVE's <2% deviation clause unmet, bottomed 0.0288) and the read adjudicated it. **Finding of record = a selectivity measurement, not the accuracy curves: P(deviate|positive) is never distinguishable from P(deviate|auto-correct)** (ratio 1.03–1.39, pooled z=+0.75; deviation sets NEST — pruned, never promoted; precision-on-deviation flat at chance 13/39). Training moved one global threshold and left the ordering alone. **42% of the gate denominator could never move** (phy 13 + wc 14 positives 0-correct at every point incl. day-zero; half the auto-correct climb 0.916→0.967 is phyrexian, the family whose positives are permanently zero), and **≥7/64 silently demanded ~2.8× selectivity against a measured max 1.39**. The head moved (`pay_kind_emb` rms 0→0.0337) ⇒ formulation/credit, not dose; indicated layer = signal density/credit (M6's, ADR-0049). **Pre-registered signal 2 (knowable-veto trajectory) had NEVER been read; the read took it and then killed it as evidence** — kvr 0.0635→0.0425 (−33%, CIs separated, steepest veto slope on record) BUT the ledger splits perfectly by campaign status (every drill-fed run flat-to-rising, every drill-free run flat-to-falling: run9 −0.0059 t=−4.20) and D4 pinned `drill_selection: None`; and the decline has no affordability signature (`knowable:timing`, untouchable by §3c, fell −51.6% vs colors_short −24.6% / generic_short −35.4%). **ADR-0062's collapse prediction is UNTESTED, not falsified — D4's recipe made it untestable.** `autopayer_xcost` +138% is the one category §3c made worse; ADR-0063's premise re-confirmed in-era sampled (0.519–0.606). Live telemetry healthy throughout (11.2 consequential windows/g; directed_fail and salvage both 0.0028 of deviations). **Standing rules (ADR-0069): gate a capability on a discrimination statistic, never an accuracy count; a sub-population 0-correct at day-zero is a gate defect, not a hard drill; a recipe pin that removes a condition must be re-checked against every pre-registered readout; read every pre-registered signal, including the ones the headline gate doesn't depend on.** Assets: `scripts/graft_pay_init.py`, `--pay-lr` param group, per-deviation failure telemetry, in-loop drill scoring, `data/runs/veto-knowability-d4read{,-mid}` (first in-era sampled kvr curve + the drill-free/drill-fed veto-slope control table). **→ D4 CONTROL RUN `d6-run19` (2026-08-22/23, [ADR-0072](docs/decisions/ADR-0072-d4-control-run-veto-collapse-falsified.md)): ADR-0062's VETO-COLLAPSE PREDICTION IS FALSIFIED on both halves.** run18 verbatim + a 320-entry in-era drill campaign; 10 accepted iterations, GUARD HALT i10 (veto 0.3074 > 1.5×iter-0). **No collapse:** kvr 0.0665→0.0640 (−3.7%, CIs overlap, validity 0.985–0.986) vs run18's 0.0635→0.0425 (−33%, separated); veto slope i0–i7 **−0.00008 t=−0.02**, inside the drill-fed band, vs run18's −0.00762 t=−7.55 ⇒ **run18's decline was the MISSING CAMPAIGN.** **Taxonomy repeats the inverted signature under the opposite condition:** `knowable:timing` (untouchable by §3c) −61.6%, hardest of any category, while `generic_short` ROSE +6.6% and `colors_short` −14.3%. **No stability dividend:** halted i10 at a HIGHER threshold (0.3044) than run17's (0.2925), ONE ITERATION EARLIER than the §3c-off run17 (run16 halted i16); runaway signature rej 5.115→7.888, ent 0.1508→0.2030, kl_mu 17×, reward 0.5131→0.4900 while v0 ROSE. **Capability negative REPLICATED at 2× head dose:** pooled selectivity 1.19 z=+1.13 (run18 1.14 z=+0.75), kind_rms 0→0.0629 vs 0→0.0337. Lead not finding: precision-on-deviation 49% (26/53) vs run18's 33% (13/39), z=1.52. **THE FINDING THAT OUTRANKS THE RUN: the payment evalset certifies a 2-TURN PROXY, not winrate.** `payment_certify.py` scores `HORIZON=2` board/tempo proxies (life+creatures kept, hand-down/board-up) — the 69 certified positives establish a better board two turns later; **whether that converts to WINNING has never been measured.** Executor + enumerator + sub-head + evalset + two training runs were spent against a ±1.1pp gate without establishing the taught behavior clears its noise floor. **Standing rules (ADR-0072): an evalset's certification horizon is part of its type — never let "certified" stand unqualified in a strength argument; measure the ceiling before building the learner; a control restores the CONDITION, not the asset; check whether candidate curation stock is gate-seeded** (`m9-rebaselinearm` shares `final_read.py`'s default seed base 20260710 — curating from it would have contaminated D5's own strength read). **NEXT: measure the ceiling — run the 69 certified drills to GAME END instead of the 2-turn horizon** (69×K on existing machinery; gates every remaining §3c decision). Three outcomes route differently: value survives ⇒ supervised conditional signal (ADR-0015 machinery, parked since M2); value evaporates ⇒ downstream squandering (influence-surface hypothesis, localized) or non-predictive proxy; value never there ⇒ payment is not a strength lever at gate resolution, surface is infrastructure permanently. **Routing recommended-pending:** hold the payment surface as infrastructure + take the §3a second act (D6) — ADR-0069 argued §3c's remaining payoff was run stability and run19 closed that off. Also due: pin-12 forced re-mine; evalset repair (phy/wc positives unreachable as constructed). Assets: `early-doom-m9control`, `drill-selection-m9control` (320 in-era entries), `veto-knowability-run19`, `scripts/launch_d6_run19.sh`, `data/training/m9-control-chain.sh`. **Standing rule (ADR-0066): an enumerator's unit of exclusivity must be the unit the executor consumes; deterministic per-arm salvage ⇒ suspect the enumerator, not the executor.** **Payment-completion queue** (m9-plan, no-silent-loss routing at M10 scoping + the closeout ADR): cost-composition cousins, effect payments, costmod per-spell refinement, pool-tie enumerator residual. Out of M9: tier-3 search, §3b stops (the M10 candidate), deterrence-family anything (ADR-0062), combo-enabler valuation (perception floor only). Verbose record through the boundary: [status archive](docs/status-archive.md).

## M9 — verbose running record, snapshot 2026-08-22 (milestone OPEN; live compact summary in CLAUDE.md)

Snapshot taken at the D4 read: the bullet had reached ~20KB, an order of magnitude past
the ~2-3KB compression threshold. Verbatim, nothing rewritten. D1 through the boundary
bundle are all here; the D4 result was appended to the compact summary, not to this text.

- **M9 OPEN (2026-08-19, [m9-plan](docs/design/m9-plan.md)): the interface round — conscious mana payment (§3c) probe-gated, with veto collapse as the falsifiable mechanism check; turn-plan latent (§3a tier-2) as the second act.** Settled at the design session: **training signal = straight RL over `{auto} ∪ payment-classes` with auto-biased init** (BC-from-heuristic dropped entirely — the head fires only in consequential windows, exactly where the auto-payer is least trustworthy: `ComputerUtilMana` can't construct chained-activation payments, and never weighs dork-as-blocker/attacker-vs-mana residuals); **payment-class enumeration must be legality-derived, never auto-payer-derived** (else the blind spot bakes into the interface); **ONE boundary event** — the §3c fork delta rides the queued bundle (rebase + multi-format + fork-index store-namespace fix + copy-state forensics; 2-arm trim flagged likely moot — land or retire explicitly), held until the engine changes are ready, so D1/D2 run on the current era. The ladder, each rung falsifiable before the next spends money: **D1 veto knowability decomposition RESOLVED (2026-08-19, [ADR-0063](docs/decisions/ADR-0063-m9-d1-veto-knowability.md)); baseline RE-PINNED same day under instrument v2 (sick-aware, ADR addendum): gate PASS all four populations — knowable 0.5392 sampled / 0.5097 argmax / 0.5336 stock / 0.6044 elevated (conservative lower bounds; validity bar 0.986–0.991 vs the 0.95 pin); premise STANDS; collapse baseline = knowable-veto rate 0.0588 sampled / 0.0435 argmax; the elevated population is MORE knowable — run17's veto climb happened in the knowable channel (i011 halt kvr 0.1049, near-double); not_knowable mass is purely auto-payer artifact (`ComputerUtilCost` refusing payable boards, X/phyrexian refusals, mana-ability routing); v1's "sickness absent from obs schema" claim was BACKWARDS — `sick` is emitted + fed to the model; the sickness windows are knowable (`sickness_short`) and a new `conditional_production` family (RestrictValid/tapXType) is a D3 enumeration input; instrument = `scripts/veto_knowability.py` (standing asset, 32 tests, suite 198 green); killed sampled-play census restored from kopia pre-kill snapshot (i000 trio + m8stock + user-directed i001–i011 climb dirs 2.1G)** → **D2a RESOLVED (2026-08-19, [ADR-0064](docs/decisions/ADR-0064-d2a-affordability-probe.md)): gate PASS high branch — `[STATE]⊕cand` AUC 0.8809 ≥ 0.75, +0.0378 over explicit arithmetic, transfer 0.8967/0.8917 no-refit; veto gap is behavioral/interface; D2b SKIPPED per pinned routing; the trunk partially predicts the auto-payer's own artifact refusals (0.8422 where arithmetic is chance 0.5385) — banked as D3 design input; instrument `scripts/affordability_probe.py` (suite 205 green)** → D3 build (engine capability audit first — can the engine *execute* a directed chained-activation payment; consequential-payment flag with own census telemetry; payment drill evalset built before any run) → D4 ~5-iteration learning-signal probe (deviation telemetry / veto trajectory / drill accuracy) → D5 full run vs the post-boundary re-pinned gate (pre-boundary reference 0.5373 ± 0.0112) with three attributions: strength, knowable-veto collapse (falsification first-class), instrumented throughput dividend → D6 plan latent, own design session + own read (inherits the promotion-attempt slot on a D4 negative). Gate thresholds are PIN-AT-DESIGN slots per deliverable. **Out of M9:** tier-3 search (critic-leaf 0.42-vs-0.94 disqualifying; unpark conditions named), §3b learnable stops (**named the M10 candidate**), deterrence-family anything (ADR-0062), pool growth beyond the bundle's multi-format enablement, combo-enabler valuation (drills post-M9; perception floor only). **D2a session (2026-08-19): pins landed in m9-plan** (label = engine verdict stratified by D1 verdict; substrate `[STATE] ⊕ cand-token`; 5-arm baseline ladder, ≥0.03-AUC margin over obs-arithmetic; gate 0.75/0.60; D2b routing inverted — high AUC ⇒ skip). **D3 OPENED (2026-08-19): scope pins landed in m9-plan pre-audit** — wire = follow-up micro-decision permanently (CastPlan payment slot retired at the boundary; conditioning answers the joint-decision worry); v1 window scope = casts + activations `effect=false` (~61/g; `payManaCost` re-measured **189/g** total on the census — the ~120/g figure was stale — with ~73/g never-consequential mana-ability windows and ~54/g `effect=true` resolution payments deferred to the §3d′ ledger); cost-composition cousins OUT-graftable (resource-commitment-set abstraction; rider on the next payment-family touch, not auto-M10); audit-fail routing pre-registered = scope to what the engine can execute, directed executor funded only by a confirmed D5 mechanism (blind spot recorded, not the interface trap — executability-scoped, enumeration stays legality-derived). **Payment-completion queue born (user direction, m9-plan): every M9-deferred payment item — 1. directed-executor completion (conditional), 2. cost-composition cousins, 3. effect payments — is routed BY NAME at M10 scoping + the M9 closeout ADR (scheduled or re-deferred with reason; silent loss is not an outcome); done-when 6 amended to enforce it.** **D3 rung 1 RESOLVED same day ([ADR-0065](docs/decisions/ADR-0065-d3-engine-capability-audit.md)): the engine CAN execute directed payments incl. chained activation — YES branch, boundary bundle NOT re-priced, queue item 1 moot.** The blind spot is one candidate-filter line (`getAIPlayableMana` skips mana-costed mana abilities — "the AI will miscalculate"); the game layer anticipates chains (`CostPartMana.payAsDecided` controller re-entry + `costPaymentStack`); color direction controller-reachable (express choice / `chooseColor` / `specifyManaCombo`). Empirical probe `DirectedPaymentAuditTest` 4/4 (fork, standing regression asset): heuristic refuses the payable Signet board, the directed chain executes and casts, express choice steers any-color. v1 executor = float-then-apply, zero new engine execution code. Rung-2 hard rule: enumerate from `Card.getManaAbilities()`+`canPlay()`, NEVER `getAIPlayableMana` (the trap, concretely). Bonus: `AI:RemoveDeck:All` cards revealed not stripped — Signet-class cards are in-deck auto-payer blanks the payment head can unlock. **Rung 2 design PINNED same day ([m9-payment-surface-spec.md](docs/design/m9-payment-surface-spec.md)):** hook/atoms/DFS enumeration with chained admissibility; flag = |classes|≥2; **wire = `SELECT_ONE` + new tag `mtg.pay_mana_class`, zero proto change** (recorded simplification); float-then-apply executor with reason-coded never-a-veto failures; census telemetry-only mode; K_MAX=8, logged truncation, 5% revisit gate. **Core BUILT on fork branch `m9-payment-surface` (`fd756cc321`, research master untouched): `PaymentEnumerator` + tests 6/6 — chained Signet class enumerated AND executed, yield/residual/phyrexian splits, truncation loud; payment suite 10/10.** **Wiring LANDED same day (branch `e857277117`, payment suite 13/13):** controller hook + `mtg.pay_mana_class` bridging (observation rides `selectOne` unchanged) + obs window emission (cost/fpool/forced + ents/pool/phy class labels) + census telemetry-only mode (`-paytelemetry`, generator `REC_OVERRIDES` hook). **Flag rule AMENDED at the wiring session — the wiring test caught the draft `≥2` rule failing the motivating I+I+Signet board (exactly ONE class): consequential now also fires on FORCED windows (`|classes|≥1 ∧ ¬auto-payable`; widens-only, trap-safe; day-zero bit-identity preserved).** **Python side LANDED same day: `scripts/payment_read.py`** (the §8 census read — conseq/forced/truncation vs the 61/g budget + 5% gate, loud gate banner), obs-reader round-trip proven (additive kv, no sv bump — bump rides the bundle), **fallback-semantics hole fixed (fork `bcd56878cf`): the payment tag's decline echo was RANDOM — now auto** (`TAG_TASK` reserves the tag; the `pay_class` task lands with the sub-head). Suites: Python 208, fork payment 13/13. **Census read DONE same day (`run-20260819-payflag`, 500 games, 74M): consequential 20.79/g (bridge-tax budget ✓); TRUNCATION GATE FIRED — 0.3911 vs the pinned 0.05** (monotone in turn 0.00→0.59, truncated windows 11.5 atoms vs 6.0, histogram bimodal with 4,233 windows pinned at the 8-cap); zero-class (284) + forced (32) windows = the **cost-modified family** (delve/affinity/alt-zone — the controller sees the raw mana part, `CostAdjustment` applies deeper); guard-caught CME ×144 fixed (`f98a555a95`). **Pre-D4 revisit session RESOLVED (2026-08-19/20, spec §12): the payment decision object is a preservation GOAL, not a composition.** Tail probe (`run-20260819-paytail`, K→64) measured cap-raising out (consequential class-count p90 55, tail past 64; explosion = assignment combinatorics over ≤11 source classes; DFS compute free) ⇒ options = `spare(k)` min-taps per source class + `min_life` on phyrexian, per-goal argmax (spread-then-lex tie-break), outcome dedupe, wire unchanged; chain-via-spare-goals TEST-PROVEN (no explicit chain goal); costmod out-of-scope v1 two-pronged; forced gated on ¬costmod. Fork `531dafdff4` (suite 14/14), Python 209. **FINAL PRE-D4 BASELINE PINNED (`run-20260819-paygoals2`, 500 games, ALL GATES PASS): consequential 15.99/g (bridge tax ~1.6%), goal truncation 0.0000 (≤9 options), nodecap 0.0073 (200k→2M re-pin cleared a 1.25% first-run fire), forced 6 CLEAN, costmod 25.48% leak-zero — presence-scoping inflation, refinement routed BY NAME as payment-completion queue item 4.** **Rung 3 BUILT (2026-08-20, session pins in [m9-rung3-draft.md](docs/design/m9-rung3-draft.md)): sub-head = Option A pointer + zero-init `pay_kind` 6-code vocab (day-zero argmax=auto measured on the D5-era ckpt; suite 219), pay_deviation telemetry live, certification harness (`PayDirective` + `CensusRun -certify` + `payment_certify.py` plan/lanes/read; 126 jobs planned), smoke fire rate 100%.** **Certify-salvage finding RESOLVED same day ([ADR-0066](docs/decisions/ADR-0066-certify-salvage-host-exclusivity.md)): the 52%/100% smoke salvage was enumerator over-admission — the DFS tracked availability per class, feasibility is per HOST CARD (a dual's two abilities = two classes both picking the lowest-id copy: same card committed twice, second `canPlay` fails).** Fixed fork-side (`37bde8051e`: `usedHosts` + reason-coded `exec_why` + dual-land regression; suite 15/15); re-smoke salvage **0.0000 on 64 directed rows** (blocker_pressure 64/64 directed_ok). **Fallout: the forced family was 100% phantom** (all six Black Panther windows enumerate zero options post-fix — "forced 6 CLEAN" falsified; the auto-payer was right); paygoals2 consequential 15.99/g one-sidedly inflated by phantom options. **TRUE pre-D4 baseline RE-PINNED same day (`run-20260820-paygoals3`, 500g verbatim recipe on the fixed jar, ALL GATES PASS): consequential 15.28/g (phantom inflation measured −0.71/g), forced 0.0000 (family empty as predicted), goal-trunc 0, nodecap 0.0059, costmod 25.47% leak-zero; re-mined 5,421 candidates → re-planned 120 jobs (40×3, k=8, no forced shape; phyrexian stays hand-built). Certification run DONE same day: **salvage 0.0000 on 4,392 directed rows (executor faithful at scale); certified 12/120** (wide 7 / blocker 3 / color 2, margins 2.0–24.2); margin distribution banked — over-threshold mass mostly NEGATIVE (directed deviations usually lose to auto; the D3 sparse-consequential premise, measured), consistency gate killed 27 arms. **Evalset-assembly decisions PINNED (2026-08-20 session 2, pins in m9-rung3-draft.md): gap closes by SCALING jobs (miner rank measured non-predictive of certifiability ⇒ deeper pool cuts yield flat; threshold sweep to 1.0 moves 12→16 only — lowering confirmed dead); auto-correct drills ADOPTED as a separate metric (reader emits `autocorrect-drills.jsonl`; batch 1 = 29, margins to −20.3; never blended — the +2.0 init would inflate it); composition yield-driven with a ~10/shape floor; reader best-arm masking fix (0 jobs affected, re-read bit-identical). **`certify2` RUN + READ (overnight → 2026-08-21): certified 26/600, auto-correct 113, salvage gate 0.0022 ok — merged batches 1+2 = 38 positive drills (bp 13 / ch 8 / wc 17) + 142 auto-correct (margins p50 5.25, max 24.2).** Yield DECAYED with pool depth (bp 4.2%/ch 2.5%/wc 8.3% vs the 7.5%/5%/17.5% top-40 projection — miner rank weakly predictive at scale; batch-1 "non-predictive" was small-N); absorbed by the yield-driven pin, no redesign. New non-blocking salvage family recorded: ALL 32 salvage rows = `costs:` on Arena of Glory (4 jobs, none certified) — enumerator-edge chip filed per the ADR-0066 suspect rule. Floor check: bp ✓ wc ✓ **color_hold 8 < 10 ✗ → pre-registered top-up taken: `certify3` LAUNCHED (240 ch-only jobs, next slice, 720 prior windows excluded; full detach+watchd+waiter coverage)**. `certify3` READ same night: 18/240 (7.5% — 3× batch 2's ch rate from a DEEPER slice ⇒ yield-decay revised: slice variance dominates, rank non-predictive both directions), +51 auto-correct, salvage again Arena-of-Glory-only. **MERGED batches 1+2+3: 56 positive (bp 13 / ch 26 / wc 17, all shapes ≥ floor) + 193 auto-correct.** **Evalset of record ASSEMBLED (2026-08-21, `data/runs/payment-evalset-v1/`): 56 positive + 192 auto-correct + 1 HELD; auto-correct cap = NONE (separate metric — N can't inflate the headline, larger N tightens the deviates-wrongly read); `payment_certify.py evalset` = standing merge subcommand (suite 224) that re-derives salvage-suspect drills from certouts — held b3 job 202 (arm 7 deterministic `costs:Arena of Glory#4@5`; "auto is best" rests on an unverified arm) routed by name to the in-flight Arena of Glory diagnosis: re-adjudicate + re-merge post-fix.** **Arena-of-Glory salvage family RESOLVED (2026-08-21, [ADR-0067](docs/decisions/ADR-0067-certify-salvage-chain-color-feasibility.md), fork `b49e95eea8`): enumerator over-admission again — `chainOrderFeasible` was count-based, color-blind, allowed self-payment (an atom's own units paying its own activation shard), and solved over masks instead of materialized unit colors (job 202's Shivan Reef U|R unit committed to U "covering" Arena's {R}); ADR-0066's "chainOrderFeasible NOT implicated" falsified. Fix = replay the executor's exact order (shared `EXEC_ORDER`), exact backtracking over materialized colors with temporal limits, admit-on-budget-exhaust; salvage rows now dump the plan (`describePlan` — gap 3 was found from ONE such row). Suite 20/20 (color-starved chain rejected + exert chain still executes); 5-job re-adjudication salvage 0.0000/176 rows — certify2 phantom options gone (358 opts 2→1, 382 3→2, 470 2→1; 227 re-argmaxed feasible), certify3 job 202 ALL 7 arms directed_ok (the held drill's arm 7 is REAL → evalset re-merge). paygoals3 consequential 15.28/g now an UPPER bound (fix only removes plans; salvage was 0.22% of directed rows) — paygoals4-vs-accept-the-bound routed BY NAME to the D4 gate session.** **Job-202 re-merge DONE same day: re-adjudication on the ADR-0067 jar = failed_predicate (all 7 arms directed_ok; the old −5.125 auto-correct margin did not reproduce, now −0.625@0.88; the real arm 7 is noise +0.125@0.50) — a noise window, RETIRED via the new `evalset --retire BATCH:JOB=REASON` path (recorded in meta, never silent; suite 225); evalset of record stands at 56 positive + 192 auto-correct, held empty.** **Hand-built drills DONE same day (mechanism = drill decks, design-§6.6 hand-constructed seeds + standing certification; pins in m9-rung3-draft): hb-phy Najeela self-pair census (150g, 1.00 phy conseq windows/g = 50× DC) → 113 candidates → certify4 13 certified + 32 auto-correct (salvage 0.0000/2,944 — ADR-0067 jar clean on a new family); FORCED family mining premise falsified STRUCTURALLY (forced windows unreachable under heuristic cast decisions — the ADR-0065 blind spot upstream of the window; 0/30 on a signet-stuffed deck) → user pin: defer BY NAME to the D4 midpoint re-mine, hb-signet banked for D4-era; §12a pool-tie board re-typed enumerator residual (lex-hidden plan has no arm to certify) → D4 gate session. EVALSET OF RECORD COMPLETE: 69 positive (bp 13 / ch 26 / phy 13 / wc 17, ALL floors met) + 224 auto-correct.** **D4 accuracy scorer BUILT + day-zero read DONE same day: fork certify OBSERVE mode (`116476eebb` — replay to window, emit the schema-v1 obs frame with the serve path's OWN labels, direct nothing; suite 21/21) + `payment_drill_score.py` plan/lanes/score (per-(shape×kind), positive/auto-correct never blended, loud miss/option-mismatch exclusions; suite 230). Observe run 293 jobs → 290 scored (3 option_mismatch = cross-era enumeration drift b1/b2, the guard's target class). FINDING: the "+2.0 ⇒ day-zero argmax=auto" pin is APPROXIMATE on real windows — argmax deviates 8.3% on iter-019 (27.6% d5-combat; bias-application verified, mechanism = PASS anchor raw score strongly negative on payment windows); banked day-zero baselines: positive 2.9%, auto-correct 91.9%.** **D4 GATE PINNED (2026-08-21 gate session, pins in m9-plan D4 + devlog session 4): re-posed as movement-from-baseline (the 8.3% day-zero argmax deviation made the leaves-auto floor vacuous — demoted to diagnostic). FUND ⇒ D5: positive-drill argmax accuracy ≥7/68 (~10.3%; ≥+5 net drills, ~2.5× binomial SE) at any accepted iteration WITH auto-correct ≥85% (≥189/222) same-iteration (guardrail, never target — starts 91.9% from the +2.0 init). CLEAN NEGATIVE: argmax deviation <2% (always-auto collapse) + positive ≤4/68. Between = discuss-zone. Drill baselines = banked day-zero scores; live pay_deviation baselines at run iter-0 (argmax/sampled separate); observe frames ckpt-independent ⇒ every accepted iteration scored. Decisions: bias STAYS +2.0 (movement framing absorbs the 8.3%; raising starves the ~10–12% sampled exploration; boundary bit-identity is a wire property); paygoals4 NOT run — 15.28/g accepted as upper bound (superseded by the post-boundary re-baseline); pool-tie residual = payment-completion queue item 5 (fix never mid-era — enumeration changes de-sync certification jar from training jar); post-boundary evalset revalidation pass joins done-when 3 (re-observe on bundle jar, floors re-checked, day-zero re-banked); forced family = D4 midpoint re-mine restated (hb-signet self-pair under the D4 ckpt as accelerant; addendum set scored separately, never retroactively in the gate).** **BOUNDARY BUNDLE LANDED (2026-08-21 session 5, [ADR-0068](docs/decisions/ADR-0068-m9-boundary-bundle.md) — Reads section PENDING until the chain completes): upstream rebase onto `23c3d2a85d` (64/64 replayed, zero conflicts, Player.java fixes verified; `pre-rebase-20260821` pinned) + §3c surface ff-merged (`116476eebb`) + store-namespace fix (`-forkns`, planner-assigned per source store, manifest-recorded) + obs schema v2 (choice-state `cho` kv VIEW-sourced so secret choices never leak, `ObsChoiceStateTest` 4/4; ENTITY_FEATURES +7 zero-pad-compat; strict sv check = the era gate; smoke: cho live on 3,422 windows/60g) + multi-format enablement (fmt one-hot + vocab formats registry + state_proj INSERT zero-pad, d5-ckpt-verified; no content growth) + 2-arm trim RETIRED explicitly (campaign opt-in, act−hold dead, `-seqarms nat` covers the chartered future) + ADR-0059-genre instrument fixes (harness `_find_jar` newest-mtime; run.json obs_schema constant). Era-scoped fixture skips (17) pending `pilotv2` repoint; era rule: never mix sv=1/sv=2 stores. ALL THREE BOUNDARY READS DONE same day (ADR-0068 Reads complete): **forkcheck 450/50 = 10.0% divergence (statics 0, crashes 0 — at/below the pre-boundary 11.2–11.4%, fidelity not degraded; the 50 rows = the forensics substrate)**; **`m9-rebaseline` 1,999 games 0 crashes → THE M9 GATE RE-PINS AT 0.5279 ± 0.0110 corrected** (raw 0.5283 ± 0.0112; pre-boundary 0.5373 reference-only, −0.94pp = re-scale not verdict; battery anomaly recorded: seed-half gap 7.15pp > 2×SE, run12 class — exploratory per protocol, but binds forward: D4/D5 reads within ~1pp of gate get fresh-seed paired confirmation as a matter of course); **evalset revalidation: same 3 option_mismatch as day-zero (zero new enumeration drift), 12 new window misses (4.1%, floors survive bp12/ch25/phy13/wc14), 278/278 IDENTICAL argmax picks across eras (zero-pad compat proven in production); re-banked day-zero baselines positive 2/64 (3.1%) / auto-correct 196/214 (91.6%) / deviation 8.6% — gate arithmetic carries: FUND ≥7/64 + auto-correct ≥182/214.**** NEXT: D4 launch (recipe session: ~5 iterations, standing loop, iter-019 init + payment sub-head, live pay_deviation baselines at iter-0); side items: copy-state forensics FIXED_HASH triplet (daytime), m8d1-proof prune, critic_loop fixture repoint at the first D4 sampled store.** Standing rule (ADR-0066): an enumerator's unit of exclusivity must be the unit the executor consumes; deterministic per-arm salvage = suspect the enumerator, not the executor. Riders queued at the session: obs choice-state emission joins the boundary bundle ("as enters, choose" is public, never emitted — Utopia-Sprawl-class imperceivable without it); yield-differing taps are consequential by definition (D3 flag spec); D3 must record its stance on cost-composition cousins (convoke/improvise/delve). **Decision-surface coverage ledger added to anvil-design-v2.md §3** (every family the model can't answer, traffic-ranked: mid-resolution object choices = the tutor-target sleeper, trigger ordering, modal re-entry, naming, bottoming; one surface per milestone, §3c template).

## M9 (archived verbatim at close, 2026-08-25, ADR-0077)

- **M9 OPEN (2026-08-19, [m9-plan](docs/design/m9-plan.md)): the interface round — conscious mana payment (§3c), with veto collapse as the falsifiable mechanism check. THE MECHANISM QUESTION IS NOW CLOSED, FALSIFIED; the strength question is untouched (no full run closed in M9).** Ladder: **D1 RESOLVED** ([ADR-0063](docs/decisions/ADR-0063-m9-d1-veto-knowability.md)) — knowable ≥0.50 all populations, premise STANDS; collapse baseline kvr 0.0588 sampled / 0.0432 argmax in-era → **D2a RESOLVED** ([ADR-0064](docs/decisions/ADR-0064-d2a-affordability-probe.md)) — `[STATE]⊕cand` AUC 0.8809, the veto gap is behavioral/interface, D2b skipped → **D3 BUILT** ([ADR-0065](docs/decisions/ADR-0065-d3-engine-capability-audit.md) the engine CAN execute directed chained payments; [spec](docs/design/m9-payment-surface-spec.md) legality-derived enumeration + float-then-apply executor; two enumerator over-admission bugs found by the salvage rule, [ADR-0066](docs/decisions/ADR-0066-certify-salvage-host-exclusivity.md) / [ADR-0067](docs/decisions/ADR-0067-certify-salvage-chain-color-feasibility.md)). **Evalset of record: 69 positive (bp 13 / ch 26 / phy 13 / wc 17) + 224 auto-correct — RE-TYPED at ADR-0072 as a 2-TURN PROXY instrument, not a winrate one** → **BOUNDARY BUNDLE LANDED** ([ADR-0068](docs/decisions/ADR-0068-m9-boundary-bundle.md)): rebase onto `23c3d2a85d` + §3c surface + obs schema v2 + multi-format; forkcheck 10.0%, **THE M9 GATE RE-PINS AT 0.5279 ± 0.0110** → **D4 RESOLVED NEGATIVE** ([ADR-0069](docs/decisions/ADR-0069-d4-read-adjudication.md), `d6-run18`): the discuss zone adjudicated — **P(deviate|positive) never distinguishable from P(deviate|auto-correct)** (pooled z=+0.75; deviation sets NEST, pruned never promoted; precision-on-deviation at chance). 42% of the gate denominator (phy+wc) was 0-correct at every point incl. day-zero, and ≥7/64 silently demanded ~2.8× selectivity vs a measured max 1.39. Pre-registered signal 2 had NEVER been read; the read took it and killed it as evidence → **D4 CONTROL `d6-run19` (2026-08-23, [ADR-0072](docs/decisions/ADR-0072-d4-control-run-veto-collapse-falsified.md)): ADR-0062's VETO-COLLAPSE PREDICTION FALSIFIED ON BOTH HALVES.** run18 verbatim + a 320-entry in-era campaign; GUARD HALT i10. **No collapse** — kvr −3.7% (CIs overlap) vs run18's −33%; slope −0.00008 t=−0.02, inside the drill-fed band ⇒ **run18's decline was the MISSING CAMPAIGN**; taxonomy repeats the inverted signature (`knowable:timing`, untouchable by §3c, −61.6% — hardest of any category — while `generic_short` ROSE). **No stability dividend** — halted i10 at a HIGHER threshold than run17's, ONE ITERATION EARLIER than the §3c-off run17. **Capability negative REPLICATED at 2× head dose** (selectivity 1.19 z=+1.13; kind_rms 0→0.0629). **THE FINDING THAT OUTRANKS THE RUN: `payment_certify.py` scores a `HORIZON=2` board/tempo proxy — whether that converts to WINNING had never been measured; two runs were spent against a ±1.1pp gate without it** → **CEILING MEASURED (2026-08-24, [ADR-0073](docs/decisions/ADR-0073-m9-ceiling-measurement.md), `run-20260824-ceiling`, both-horizon paired replay, pins committed pre-read): the proxy CONVERTS where it holds — +12.5pp/window on the in-era-recertified subset (z=+3.74), Spearman +0.465, "non-predictive proxy" KILLED — and the AGGREGATE CEILING IS SUB-GATE on every measured bound (+0.52–0.73pp/game vs the ±1.1pp floor; mined window rate 0.112/g/seat is a lower bound, plausibly 2–4× low).** Recert guard fired (44.6% < 70%), adjudicated winner's-curse mis-calibration, not era drift (12/65 sign-flips; margin>0 set converts +6.6pp). **Routing ADJUDICATED (user, 2026-08-24): payment surface = INFRASTRUCTURE; D6 (§3a turn-plan latent) takes the promotion slot** — the D6 design session must pin why the latent escapes the marginal-vs-conditional failure mode + a pre-registered kill signal. Supervised-conditional-signal = named M10 candidate CONTINGENT on the window-rate bound — **sweep RESOLVED same night (`run-20260824-ratesweep`, uniform 600 of the 5,076-window tagged universe): certifiable rate 3.17% [2.04, 4.89] ⇒ 0.321 windows/game — ~3× the mined bound; perfect-play +1.49pp/game central [+0.96, +2.29] / +4.02 upper [+2.58, +6.21] ⇒ upper CI entirely above the 1.1pp floor ⇒ M10 CANDIDACY STANDS per the pinned rule; **stage-2 direct conversion ([ADR-0075](docs/decisions/ADR-0075-window-rate-sweep.md)): +9.21pp/window ± 4.26 on the uniform 19, recert 100% ⇒ completed arithmetic ≈ +2.96pp/game, ~2.7× the floor — perfect payment play is REAL at gate resolution; the closeout routes the queue against a live M10 candidate.** **D6 DESIGN PINNED same day ([m9-d6-plan-latent-spec.md](docs/design/m9-d6-plan-latent-spec.md), four forks user-approved): detached carry + dense aux supervision (emit `[PLAN]` once per turn, carry per (seat,turn) in Session locals, no BPTT; escape argument = both signal paths dense by construction; kill signal = flat plan-reliance at plateaued aux over 4 iterations). R1 aux-target probe RESOLVED same day ([ADR-0074](docs/decisions/ADR-0074-d6-r1-aux-target-probe.md), pins pre-data, 20,191 turn-groups): both targets clear WIDE — action-summary macro-AUC 0.9235 vs arith 0.7528, delta Spearman 0.5665 vs 0.4462 — selection JOINT; the static [PLAN] readout already adds +0.016 AUC over [STATE].** → **D6 BUILT + PROBED in under two days ([ADR-0076](docs/decisions/ADR-0076-d6-probe-read.md), `d6-run20` closed at i2, 2026-08-25): the MECHANISM VALIDATES — the latent is consumed within ONE iteration (argmax-flip 3.37% at accepted i1, FUND's letter met; aux BCE 0.7105→0.002; day-zero bit-identity held exactly) — and v1's order-free action-bag target AMPLIFIES INTERFACE PROBING at compounding speed (veto 0.18→0.28→0.38 in three iterations, knowable fraction RISING, `generic_short` +165% — the ADR-0062 account through a THIRD independent channel at 10× drill-fed speed; three guard halts each adjudicated same-day: proj-lr 100× overdose → split groups 1e-4/1e-3; veto 2.5× probe amendment; the i2 close). NO v1 promotion run — the target's shape is a behavioral prior and this one trains probing. v2 ROUTED: sequencing + resources in the plan target (pull D2a's measured cost knowledge into the conditioning channel). New standing rules: price a conditioning channel's lr by gradient DENSITY not init; a recipe-change relaunch must clear the rejected phase's artifacts; an aux-target's shape is a behavioral prior; falling reliance + compounding behavior = absorption, read jointly. run20 stores veto-elevated, never in a mixture. THE CLOSEOUT'S TWO OPEN CALLS: done-when 5 (v2-within-M9 vs close-and-carry to M10's unified resource-scheduling competency — planning×payment as ONE competency, user framing on the spec ledger) + the payment-queue routing vs ADR-0075's live M10 candidate.** New standing rules (ADR-0073): a re-cert threshold on a selected population must price winner's-curse regression, not only drift; per-window value and gate-scale value are distinct claims — a ceiling statement carries both. Also due: pin-12 forced re-mine (re-deferral with recorded reason now the likely routing); evalset repair — now empirical: phyrexian value-free at game end (Δ=0.0 exactly), wide_choice +7.5pp (repair = reachability, not existence). **Standing rules born M9: an evalset's certification horizon is part of its type — never let "certified" stand unqualified in a strength argument; measure the ceiling before building the learner; gate a capability on a discrimination statistic, never an accuracy count; a day-zero-0-correct sub-population is a gate defect; a recipe pin that removes a condition must be re-checked against every pre-registered readout; read every pre-registered signal; a control restores the CONDITION, not the asset; check whether candidate curation stock is gate-seeded (`m9-rebaselinearm` shares `final_read.py`'s seed base 20260710); an enumerator's unit of exclusivity must be the unit the executor consumes (ADR-0066).** **Payment-completion queue** (m9-plan, no-silent-loss routing at M10 scoping + the closeout ADR): cost-composition cousins, effect payments, costmod per-spell refinement, pool-tie enumerator residual. Out of M9: tier-3 search, §3b stops (the M10 candidate), deterrence-family anything (ADR-0062), combo-enabler valuation. Verbose record: [status archive](docs/status-archive.md).

## Playable-fork track — running record through v16, snapshot 2026-08-25 at the CLAUDE.md compression pass (track live; compact summary in CLAUDE.md)

- **Playable-fork track (2026-07-26→08-21; current release v16 `291a866fa3`, `2.0.15-SNAPSHOT-08.22` — upstream sync onto `23c3d2a85d`, the M9-boundary engine commit; v15 08-07 = whose-action tracking + Android coexistence build): founding worklist items 1–8 ALL BUILT, PUBLISHED, user-verified in play.** [playable-fork-worklist.md](docs/design/playable-fork-worklist.md) is the doc of record; a human-facing distribution build on fork branch `playable` (worktree `../forge-play`; the pinned research fork `master` has zero delta from any of this). Shipped: resizable window + live viewport (six frozen-size layers found and fixed), deck-site URL import on mobile + bulk Archidekt sync (never-move re-sync, annotate-not-quarantine; Moxfield deferred — relationship problem, not engineering), configurable tap angle `UI_TAP_ANGLE` 90–15° both clients (shared `RotatedRect` inverse-rotation hit-tests; caught + fixed the stock nested-transform bug en route), slot packing + single-column zones, in-place delta auto-updater repointed at the fork (hash-verified; self-updates live-verified incl. two-hop v7→v9), Adventure horizontal hand; v13 = updater orphan-deletion pass; v14 = net-lobby fixes (upstream #11190 made `FTextField` fire CHANGE per keystroke, breaking chat/lobby/carets — now opt-in `setLiveChangeEvents`; stock team-selection echo cascade guarded — both queued upstream). Release pipeline: `scripts/release-playable.sh` → `daily-snapshots` prerelease; **publish MUST pin `-R Tyrathalis/forge`** (gh default repo is upstream, same tag name); post-publish routine parses the published manifest through the shipped jar (standing lesson: verify the production artifact through the production code path). Friend-install pins: Temurin 21 JRE via adoptium `installer/latest` permalinks; macOS 15+ Gatekeeper flow baked into `Forge.command`. Stock-bug upstream candidates queued: nested-folder storage stub, `RestartUtil` Runtime.exec-tokenization (desktop restart never worked), quest BALANCED all-colors empty pool, tap-angle hit-test generalization. **Standing hazards:** the playable build shares Forge's user deck store with the research harness — `launch --pool` gates on installed-deck content hashes (Anvil `89178d7`); **never check out `playable` in the research worktree**. **Multiplayer hardening (ran hidden alongside, CLOSED 07-30, [multiplayer-hardening.md](docs/design/multiplayer-hardening.md) is the public record):** 9 findings, 4 private advisories, 3 merged to public Forge master byte-identical, 2 declined-and-documented as trust-model properties; chat rate limiting = public PR #11457; both declined patches live on `playable`. Residue: T2 live-rescale only (workaround: launch at play size). Full blow-by-blow: [status archive](docs/status-archive.md).

## Chronicle side stream — running record snapshot 2026-08-25 at the CLAUDE.md compression pass (track ACTIVE; compact summary in CLAUDE.md)

- **Chronicle side stream ACTIVE (scheduled 2026-07-31, [ADR-0029](docs/decisions/ADR-0029-chronicle-scheduling.md) + [chronicle-mvp-plan.md](docs/design/chronicle-mvp-plan.md)): the 1993–94 Alpha→FEM collector-loop MVP as a playable-branch side stream, hidden behind `CHRONICLE_MODE_ENABLED` (default OFF) until the author's 2-week dogfood gate passes.** Invariants: seed integrity (all daily randomness from (run seed, day index, domain); sealed items commit contents at acquisition), strictly-monotonic day tick (no clock-scum), prestige-proof save schema, pack EV negative by construction, cosmetics derived at runtime. **D1 headless core / D2 screens / D3 reveal scene / D4 Android — all DONE.** **D5 dogfood OPEN**; **D6 (kitchen table) BUILT AND VERIFIED IN PLAY 2026-08-24** — the author has built decks and played rival games on device.
  - **The D5 round-1 finding was a design result, not a bug: the loop had no effort→reward channel** ([ADR-0070](docs/decisions/ADR-0070-chronicle-effort-reward-sink.md)). Ration + stipend are timer grants and the buylist pays less than packs cost by construction, so nothing converted invested time into progress. **Pin 8, above the MVP/stage line**, phrased as the property so it outlives its first implementation.
  - **D6 design** ([ADR-0071](docs/decisions/ADR-0071-d6-design-round.md) + same-day addendum): **two speeds — cash bounded** (one paying game per rival per played day), **ante UNBOUNDED and priced in risk not time**. Opponent = **named rival collectors whose pools are DERIVED, never stored**, from (run seed, rival id, **pack index**) — day-free, so a collection only ever grows and the save carries nothing; each pack's acquisition day bounds it to products *released* by then, which IS the difficulty model (no power knob). Ante persists only a delta (`derived − lostToPlayer + wonFromPlayer`), which made the catch-up mechanic **unnecessary** and **bounds the world's card supply** to the derived curve, so an unlimited channel cannot outpace the release calendar. Reference-only deck legality; Forge's real ante (rarity-matched, from the deck, permanent) **never inherited from global `UI_ANTE`**; provenance journal now records departures. Finding: **no corpus of period decklists exists, and that is historically correct.**
  - **D6 shipped with four rounds of UI defects found only in play (v18–v22, devlog [2026-08-23-session2](docs/devlog/2026-08-23-session2.md)):** tear direction inverted; deck editor crashed on open (`ColumnDef.NEW` ships null functions — every consumer must supply them via `getColOverrides`); `ImageView` pile-by dereferenced the raw static `fnSort` (**upstream candidate** — Quest can hit it too); Chronicle was sharing Quest's *persisted* editor view state (now `CHRONICLE_DECK_POOL`); and **the root cause of the empty catalog was mine — `IDeckController.setEditor` must also call `editor.notifyNewControllerModel()`**, without which `getDeck()` stays null, `onDeckChanged` never fires and the catalog never refreshes. Finally, a pre-D6 save showed no rivals because `rivals.txt` was the only Chronicle data file introduced *after* that install's `assets.zip` — **small curated data that ships with a feature now also ships inside the jar/APK**, res/ winning when present, two copies pinned byte-identical by test.
  - **Standing lessons (this track):** a libGDX screen defect cannot be found by reading or tested from the desktop tree — **drive the build** (isolated `-Duser.home` + `xdotool` + `import -window`; the Wayland compositor screenshot cannot see the XWayland surface); **probe by writing to a file**, since Forge's logging stops at boot and console silence is misleading; **a confirmed mechanism is not a complete explanation**; implementing an interface means implementing its *contract*, not its signature. Hazard: use the window title, never the app name, to focus a build under test — `open_application "Forge Desktop"` launches the *installed* Forge against the real profile.
  - **NEXT: D5 dogfood proper** (the two-week clock can now actually run), then the numbers pass — deferred behind D6 and now three-axis (purse-per-difficulty, ante multiplier, `anteRivalFloorCards`). Watch item: the author's Android res delta looks stale — `rivals.txt` was just where it became visible. Verbose record: [status archive](docs/status-archive.md).


## M10 — verbose running record, snapshot 2026-08-26 at the build-design-session compression pass (milestone OPEN; live compact summary in CLAUDE.md)

- **M10 SCOPING (opened 2026-08-25): the unified resource-scheduling competency** — turn planning × payment as ONE competency ([m9-d6-plan-latent-spec.md](docs/design/m9-d6-plan-latent-spec.md) ledger, user framing). Candidate shape: v2 schedule-bearing plan target (sequencing + resources, D2a cost knowledge into the conditioning channel) + re-advertised payment actuation + ADR-0075 supervised conditional labels, trained and read as one competency. Design-round obligations before any build: charter adjudication vs §3b stops (the named alternative), the planning ceiling measurement (ADR-0073 rule), the v2 offline target probe (ADR-0074 pattern, co-designed with actuation), plus two funded ceiling probes routing M11 — tutor/fetch targets (§3d′ family 2, user-raised 2026-08-25) and resolution-effect payments (the measured argument ADR-0077 demands). Charter held on the substrate corollary (user): the plan latent is plausibly the prerequisite for downstream capability expansions (tutoring included) to show full potential. Design round progress (2026-08-25): schedule terrain census run (31.6% of own turns resource-bound, `data/runs/schedule-census-m10`); latent-vs-emitted fork ADJUDICATED to soft emission (interaction-robustness rationale; hard execution staged behind follow/validity evidence); **planning-ceiling measurement PRE-REGISTERED and adjudicated ([m10-ceiling-spec.md](docs/design/m10-ceiling-spec.md)**: two-stage h2-certify → game-end conversion + h4 side-sample, joint schedule-consistent payment, 2×-floor funding thresholds); **`-forceschedule` BUILT + mechanical smoke PASSED (session 4)** — `ScheduleDirective` soft-emission executor (land-first, degrade-and-count) + knob-c payment scorer + AnvilRun sched rollouts (rollSeed keyed on target turn for the stage-2 identity) + `local-oneshot` smoke bridge (`scripts/schedule_smoke.py`; forced-draw winner nondeterminism found+fixed, spec addendum records executor semantics). **Launch commit LANDED (session 4): θ=2.0 + pinned composite, h4 flag 1.25×/+3, seed base 20520825, sampling rng — `scripts/sched_pins.py` executable source.** Same night: fresh census RUN + ingested (500 games, jar-parity vs the bundle commit proven empirically first), pinned sample drawn (4,084 eligible, 8.17/game; 600+200+100), read pre-registered (`schedule_read.py`), model-serve smoke PASSED (margins byte-identical across re-run; en route: sched-scale watchdog-clock OOM found+fixed trace-invisibly, fork `07900c3db1`), **CEILING MEASURED AND FUNDED ([ADR-0078](docs/decisions/ADR-0078-m10-ceiling-measurement.md), 2026-08-26): +13.5pp/game central [4.3, 25.8], ~6× the 2.2pp funded threshold** — rate 29.16% of eligible turns certify (2.38/game, 7.4× payment's) × conversion +5.69pp/turn [+2.05, +9.56]; 1b h4 flag did NOT fire; caveats recorded (h2 margin is a detector not a ranker, Spearman 0.109; ~50% selected-arm divergence = schedule×payment entanglement measured, included in the ceiling). En route: the AiCache bridged-JVM leak found+fixed (standing rule; chunk recycling was load-bearing all era). **The M10 build proceeds on the funded candidate** — v2 schedule-bearing plan target + payment actuation + supervised conditional labels as ONE competency; §3b stays shelved on ceiling grounds. Next: the build design session (v2 target probe rides the sweep vocabulary; 170 positives + sweep rows = seed supervision material). Scheduled in: cost-composition cousins + costmod per-spell + pool-tie residual (ADR-0077 routing).

## M10 — verbose running record, snapshot 2026-08-28 at the probe-read compression pass (milestone OPEN; live compact summary in CLAUDE.md)

- **M10 OPEN — the unified resource-scheduling competency (charter 2026-08-25; ceiling FUNDED + design round nearly closed 2026-08-26).** One competency across two surfaces: v2 schedule-bearing plan target + re-advertised payment actuation + ADR-0075 supervised conditional labels; **[m10-plan.md](docs/design/m10-plan.md) is the doc of record** (every fork recorded there with reasoning). **Ceiling MEASURED AND FUNDED ([ADR-0078](docs/decisions/ADR-0078-m10-ceiling-measurement.md)): +13.5pp/game central [4.3, 25.8], ~6× the pinned threshold** = rate 29.16% (2.38 certified turns/game, 7.4× payment's) × conversion +5.69pp/turn; 1b flag no-fire; §3b stays shelved on ceiling grounds. **Exploratory reads all in ([m10-ceiling-exploratory-reads.md](docs/design/m10-ceiling-exploratory-reads.md) §1–6):** certified best arms 56% bespoke ordered subsets / 43% partial-set / 11 pure holds (pointer decoder justified; selection ≈ ordering); rate concentrates where scheduling binds; divergence veto-dominated (realization fitting = the binding surface); payment executor PERFECT at sweep scale; **the schedule leg carries the joint ceiling** (joint 26.4% vs auto 23.9%; 83% of certified schedules survive auto-pay; payment-given-schedule = sparse one-sided tail); **conversion lives in behind/contested states** (v<0.45: +14.1pp vs +0.5pp above — the whole +5.69; binned competency-instrument prototype WORKS, both critics ρ=0.98). **Build design session 2026-08-26 — all forks adjudicated but the read-protocol pin:** resource component BOTH + a feasibility probe target minted from the 96k sweep rows (contingent lines stay emergent, three fingerprints recorded); cadence = revise-on-trigger (4 engine triggers, no-op revisions first-class; model-placed triggers → canonical register); ingestion = schedule tokens + [PLAN] readout (discrete carry ⇒ bit-exact loader reconstruction); payment = slot-conditions marked candidate, scope pinned, **PG staged mask** (≥4 accepted iterations; the marginal-collapse path must not re-enter via on-policy PG), ADR-0073 attribution stripping retired loudly; wiring = direct aux at birth (labels are era-assets; the sweep machinery = a re-runnable label mint), drill families routed behind birth telemetry. Kill-signal/telemetry package structure adjudicated (5-family roster; FUND degeneracy veto; probing-dissolution pre-registered secondary read; penalty rule knowability-gated + ADR-0053-calibrated). **[canonical-register.md](docs/design/canonical-register.md) born**: 8 philosophically-correct future forms, each with its named promotion instrument. Engine assets: `-forceschedule` soft-emission executor + knob-c scorer + sched rollouts (target-turn-keyed rollSeeds); AiCache leak fix (standing rule). **v2 offline target probe RESOLVED ([ADR-0079](docs/decisions/ADR-0079-v2-target-probe.md), spec + instrument pre-data at `1a56dee`): E (EOT resource summary) PASS +0.206 (4.1× margin), R (running ledger, 38,948 slots) PASS +0.066, F (schedule feasibility, 96k-row label mint → 7,950 arm-rows) FAIL (−0.003 trunk increment over a 0.807 cheap-feature baseline) ⇒ birth aux roster = E+R, validity predicate = R + veto-knowability splitter, nonlinear F re-probe = named future instrument; the [PLAN]-slot dividend replicates (E +0.036).** M11-routing probes spec ADJUDICATED ([m11-routing-probes-spec.md](docs/design/m11-routing-probes-spec.md)): both SELECT_ONE tutor classes (~11.7/g pre-filter), decline-legal effect payments only (50.12/g raw confirmed), routing bar = ADR-0078 threshold scale, engine deltas (`-forcechoice` + pay/decline override) in one Java session after this probe, mining rungs first. Mining rungs RUN (`m11_mining.py` pre-run at `9a1e2de`): probe T live at 1.08/seat-game in-family forceable (3.4× payment rate; routing bar needs ≈+2pp/window); probe P's decline-legal universe found at its OWN callback (`payCostToPreventEffect`, 1.66/seat-game, decline-legal by construction — the payManaCost "telemetry-blocked" verdict shrank to an attribution gap). **Engine deltas BUILT + PROVEN same day (fork `07c28fcf8a`, one touch): `-forcechoice` (ChoiceDirective + AnvilRun choice-rollout mode; family regex = the mining classifier verbatim), pay/decline override (willPayUnlessCost bypassed via the PlayerControllerAi pay path), census src/api enrichment; ADR-0025 proof = banked era-jar smoke 72/72 labels rows + census clean modulo declared classes; `choice_smoke.py` PASSED (distinct candidates forced by index, idx_oob, pay/decline, determinism).** **Forcing rungs RAN EXHAUSTIVE + RESOLVED (pins/planner/read pre-data at `5c0b307`; 67,880 game-end completions, 19h, 12 lanes, health green): BOTH RE-DEFER ([ADR-0080](docs/decisions/ADR-0080-m11-routing-probes.md)) — tutor/fetch 1.41pp/g [lo −0.63] (per-arm flat: no fixed-candidate policy beats the heuristic; value if any = state-dependent selection, priced under the bar), effect payments 0.69pp/g (decline costs 2.5pp/window, heuristic already always-pays — real stakes, no headroom; ADR-0077's re-deferral condition satisfied; natural≈always-pay = measured reference for the conditional labels).** **DESIGN ROUND CLOSED ([ADR-0081](docs/decisions/ADR-0081-m10-design-round-closeout.md)): all six obligations resolved, queue routed by name; the build inherits aux roster E+R + schedule tokens/[PLAN] + revise-on-trigger + slot-conditions/PG-mask + direct-aux labels + telemetry-from-birth, numerics pinned pre-data at the training-probe launch.** Watch: the copy+resume crash class (0.76% of completions, point-shaped, early-resume; hunt verdict: VARIANT-RANDOM not position-intrinsic — 39/39 poisoned points clean under a fresh JVM draw ⇒ an ordering-sensitive GameCopier defect; `crash_why` now standard on crash rows at `7c4af49fa4`, the next big run names the class free; FIXED_HASH full-rerun = the named escalation) — own record when named. **BUILD SESSION 1 (2026-08-27): graft R1–R4 LANDED end-to-end ([m10-build-spec.md](docs/design/m10-build-spec.md) = the as-built mechanics of record; sub-pins user-adjudicated: trigger-2 ANY opponent action / trigger-3 END STEP / slot cap 6+mask / failed-slot consumed; build-era jar `7c4af49fa4` ADR-0025-gate-PASSED zero diff classes).** Landed: slot tokens + decode/E/R heads (v2 identity contract: absent/closed exact, content-invariant at zero init, PRESENCE measured — floor 0.0125 argmax flip BANKED, content_flip 0.0 exact on 4,715 windows), revise-on-trigger serve carry proven on the real bridge (**tripwire 0 violations on a live-conditioned store** = the discrete carry's bit-exactness claim), PG staged pay mask (advantage-masked, tripwire-safe), pay tag re-advertised from the graft (286 windows/24g at the +2.0 auto init, conditioning present), `m10-sched-init` ckpt + pinned reliance population `m10-reliance-pop-20260827` + telemetry from birth (sched_reliance instrument, counts dump, battery rows). Found+fixed: run20's plan-share guard was DEAD (`_rl_summary` key gap); `d6-run18-i000` restored from kopia. **R5 (session 3, same day) LANDED — the label round:** evalset repair resolved as an INSTRUMENT defect ([ADR-0082](docs/decisions/ADR-0082-payment-evalset-repair.md): the "unreachable" positives were exact-index scoring on margin-TIED multi-arm outcome classes — 11/17 wide_choice windows have ≥2 independently-certified arms; class scoring adopted end-to-end, phyrexian's 13 retired, ADR-0066 corollary → standing rules); `payment-evalset-v2` (56/224/13) ingests as class-CE direct aux (`paylabels.py`, 265/280 joined at the post-boundary revalidation frames — the pay head's only signal under the PG mask, `dataset.py` pin-scope stated loudly); **`payment-holdout-v1`** (uniform ratesweep set, 19+123) = the pre-registered conditional holdout with FRESH build-era observe frames (142/142, zero misses; day-zero banked 0/19 pos + 96.7% auto); `cand_paymark` marked candidate live (schedule-consistent option, mu-serialized, follow/deviate counted; live-but-untrained at birth — recorded); best-arm seed supervision minted 170/170 + wired at frac 0.05. Full stack calibrates together, tripwire 0, suite 253 green. **R6 (2026-08-28) LANDED — the COUSINS TOUCH ([ADR-0083](docs/decisions/ADR-0083-cousins-touch.md); four sub-pins user-adjudicated: delve IN type-grouped / payCombatCost bridged now / costmod applicability-only / GOAL_MAX 24):** convoke/improvise/delve enumerate as restricted cousin atoms in the same DFS (main-cost-only, `cousinCanPay` mirrors the engine arithmetic; kept out of `allAtoms` = the sched scorer's mana universe), directed execution via `CousinDirective` + generated FORCE_OVERRIDES hooks on the two callbacks (unarmed = natural; empty maps = deliberate — heuristic convoke suppressed on directed plans), costmod presence-scan → per-spell `CostAdjustment.staticAppliesTo` (ReduceCost + SetCost; the ~25.48% recovery measured at the next census), pool-tie residual FIXED (`spare_pool` kind-7 goal surfaces the lex-hidden pay-life plan, regression-tested), combat costs bridged (`payCombatCost` marker relaxes the nested window's effect gate; auto=0 bit-identical; pay/decline stays heuristic = ADR-0080 untouched), `PAY_KINDS` 8 kinds + graft regen + **day-zero reliance re-banked IDENTICAL** (flip 0.012513 / content 0.0 exact). Proofs: payment suites 16+3+4+4 green, Anvil 253 green, **ADR-0025 jar gate PASSED (sched 9,969 + choice 17,409 census rows, 72/72 ×2 + 84/84 ×2 labels, sched/choice files byte-identical — zero diff classes)**. **PRE-FLIGHT RESOLVED + NUMERICS PINNED PRE-DATA (2026-08-28 session 2, [ADR-0084](docs/decisions/ADR-0084-m10-probe-preflight.md)): remints on the cousins jar clean — holdout ZERO-drift (142/142, day-zero identical 0/19 + 96.7%), evalset 263/280 joined (option_mismatch 3→5 = exactly the predicted spare_pool/costmod class, all +options, floors survive bp12/ch25/wc14; probe recipe repoints `--pay-observe`/`--pay-drill-dir` to the 20260828 cousins dirs); census (paygoals4, verbatim recipe, 500g): costmod 25.47%→4.35% (the predicted recovery), conseq 19.79/g ✓, goal-trunc 0 (GOAL_MAX 24 never binds), nodecap 0.0068 ✓, forced REBORN as the delve/improvise family (129 windows — heuristic casts on the discount, raw auto-pay can't = the capability-unlock universe measured), combat 0/500g natural (blind-spot upstream; rides the probe census). Numerics (user-adjudicated 4/4 on the drafted leans; [m10-probe-numerics-draft.md](docs/design/m10-probe-numerics-draft.md) = statement of record): KILL/FUND CONTENT-PRIMARY (content_flip carries v1 thresholds verbatim <0.005/≥0.02 on its true-zero floor; presence flip floor-relative joins KILL; presence-only movement can't FUND — standing rule born), degeneracy veto (pure-hold >25% / mean len <1.0 / utilization <25%, 2-iter sustained), PG-unmask (≥4 masked + pay-CE plateau + follow ≥max(2×iter-0, 0.05) + auto ≥85% + realization-failure share <0.5), competency read = SCALAR gate (mean Δwr on v<0.45 vs banked +14.1pp; sweep re-run population; curve never gates), invalid-schedule penalty ZERO by derivation (`sched_penalty_derive.py`: void arms −0.19 composite ≈ free ⇒ ADR-0053 cap binds at ≈0; term not built, contingency pinned). Next: THE TRAINING PROBE LAUNCHES — D4-shape short run (~5 accepted iterations) on the standing recipe + R5 label stack + repointed dirs; every pre-registered number now exists before iteration 0.** Verbose record: [status archive](docs/status-archive.md).

## M10 — verbose running record, snapshot 2026-09-03 at the reset-adoption compression pass (milestone OPEN; live compact summary in CLAUDE.md)

- **M10 OPEN — the unified resource-scheduling competency (charter 2026-08-25); ceiling FUNDED ([ADR-0078](docs/decisions/ADR-0078-m10-ceiling-measurement.md)): +13.5pp/game central [4.3, 25.8] ~6× threshold, conversion concentrated in behind/contested states (v<0.45: +14.1pp vs +0.5 above; the schedule leg carries the joint ceiling). [m10-plan.md](docs/design/m10-plan.md) = doc of record.** Design round CLOSED ([ADR-0081](docs/decisions/ADR-0081-m10-design-round-closeout.md)); v2 target probe → birth aux roster E+R ([ADR-0079](docs/decisions/ADR-0079-v2-target-probe.md)); M11 routing probes both RE-DEFERRED ([ADR-0080](docs/decisions/ADR-0080-m11-routing-probes.md)). **Build R1–R6 LANDED ([m10-build-spec.md](docs/design/m10-build-spec.md) = as-built record; [ADR-0082](docs/decisions/ADR-0082-payment-evalset-repair.md) evalset repair, [ADR-0083](docs/decisions/ADR-0083-cousins-touch.md) cousins touch):** slot tokens + decode/E/R heads, revise-on-trigger serve carry (tripwire 0 on the live bridge), PG staged pay mask, telemetry from birth (presence floor 0.012513 / content_flip 0.0 exact banked on `m10-reliance-pop-20260827`), R5 label stack (`payment-evalset-v2` class-CE aux + `payment-holdout-v1` pre-registered holdout + `cand_paymark` + 170/170 seed supervision), cousins jar `5bbc397412` (ADR-0025 gate PASSED zero diff classes). **Pre-flight + numerics PINNED PRE-DATA ([ADR-0084](docs/decisions/ADR-0084-m10-probe-preflight.md) + [m10-probe-numerics-draft.md](docs/design/m10-probe-numerics-draft.md)):** remints clean (holdout zero-drift 142/142; evalset 263/280, option_mismatch 5 = the predicted class), census costmod 25.47%→4.35%, forced family reborn as delve/improvise (129 windows); KILL/FUND CONTENT-PRIMARY (content_flip <0.005 / ≥0.02; presence floor-relative joins KILL; presence-only can't FUND), degeneracy veto (pure-hold>25% / len<1.0 / util<25%, 2-iter), PG-unmask conditions, scalar competency gate, invalid-schedule penalty DERIVED TO ZERO (void arms ≈free — term not built). **TRAINING PROBE m10-probe1 RAN + READ same day ([ADR-0085](docs/decisions/ADR-0085-m10-probe1-read.md); launch `20028fd`, D4-shape 6×480, port 50070/seed 20280828): GUARD HALT at iteration 2 — verdict NO FUND, explicitly NOT a channel-KILL.** The dense decode-on-own-emissions aux is self-referential with a degenerate fixed point at empty: emissions collapsed day-zero-garbage-full → 62.3% pure-hold in ONE RL iteration (min-CE hedge on unpredictable targets), then held (60.5%, marginal recovery); decode-confidence blowup made off-mode sampled targets exponentially surprising (max step CE 7.7→46.8→543.5 at a stable ~3.2 median; the share guard's mean 1.504 was spike-dominated, median 0.178 under the bar); content_flip demonstrated D6-speed consumption at iteration 0 (0.0138 = 2.8× the KILL bar) then unwound as targets emptied; degeneracy-veto conditions met on BOTH accepted ckpts; the void-arms-are-free derivation confirmed in vivo (seed anchor at frac 0.05 ~20× too weak vs the dense term). Aux heads on the pinned population stayed healthy — the failure localizes to the emission loop, not the surface/carry/conditioning. **Surgery LANDED ([ADR-0086](docs/decisions/ADR-0086-emission-grounding.md), 2026-08-29): the two routed items collapse into one move (the decode head IS the emitter ⇒ supervised decode on certified arms = supervised emission) — own-emission decode CE RETIRED (`sched_term` = E/R only, `--sched-frac` 0.05 mass-preserving), seed supervision PROMOTED to the primary decode signal (`--seedlab-frac` 0.1, share guard 0.3, spike tripline ported), empty-emission cost NOT built (fights the 11 certified pure holds; contingency stays pinned). FUND decode leg restated on seedlab CE ≤ 0.8× day-zero 2.730022 (banked `m10-sched-init/seedlab-dayzero.json`; weakly discriminating — emission health rides the degeneracy veto). No graft change ⇒ floors stand; no fresh mint pre-relaunch (per-era re-mint routed by name if FUND). Suite 255 green.** Housekeeping landed with the read: `_stop_server` SIGTERM (the SIGINT/SIG_IGN counts-dump defect; iter-0 serve counts lost, external rescue recovered iters 1–2), share guards read the step MEDIAN + `--guard-sched-spike` 100×-median tripline (regression-tested on the real iteration-2 numbers); standing rule born → [standing-rules.md](docs/standing-rules.md). Assets banked: 2 accepted iterations' full telemetry, paymark iter-0 baseline 3.43% (unmask bar 0.0685; follow rose to 9.2% under labels — the healthy line), pay-label positive CE 0.77→0.43, probe stores/ckpts kept (iter-2 REJECTED marker). Suite 254 green. **PROBE2 RAN + READ same day ([ADR-0087](docs/decisions/ADR-0087-m10-probe2-read.md)): GUARD HALT at iteration 0 (`kl_mu 0.06128 > 0.06`) — read, not amended.** Two findings. (1) **A named calibration defect, M10-independent:** the fixed 170-window seed batch is memorized in ~10 optimizer steps (CE 2.728→0.046) and ADR-0057 calibrate-then-freeze front-loads its mass onto the shared trunk — KL rose to 0.103 within the iteration (vs probe1's 0.051); `seedlab_share` DECAYS as it fits (0.070→0.017), so the share guard is **structurally blind** to the impulse. (2) **Certified seed labels alone do not carry the conditioning channel:** content_flip 0.0138→**0.0019** (below the KILL bar) with presence flip UP 0.0305→0.0448 and live-row decode CE 2.609→6.404 — presence-heavy, content-quiet = the unfundable pattern. ⇒ **the retired own-emission term WAS the content-consumption driver**; ADR-0086's "170 is enough" lean FALSIFIED and the per-era re-mint promoted from follow-on to **prerequisite**. Emission-collapse question stays OPEN (iter-0 emissions are the init ckpt's = day zero reproduced, 6.01% pure-hold / mean len 4.85; iteration 1 never ran). Healthy: tripwire 0, E 0.361 / R 0.371 under FUND bars, paylab pos CE 0.766, holdout auto 96.7%. Two standing rules born. **R8 GROUNDED DRIVER LANDED ([ADR-0088](docs/decisions/ADR-0088-grounded-driver.md), 2026-08-30; [grounded-driver design](docs/design/m10-grounded-driver-draft.md) = statement of record, 5 forks adjudicated on the drafted leans): the probe-pair triangulation (grounded × dense × on-distribution) is the driver spec.** Fixed-batch mechanics loop-wide (`labbatch.py`: `--lab-k` per-step chunk subsampling + `--lab-warmup` ramp + `--guard-lab-memorize` 0.5 first-window tripline, regression-tested from the real probe2 numbers; paylab same genre 0.994→0.230 in ~10 steps ⇒ uniform; `--seedlab-carry-w` built — per-iteration recalibration REJECTED, probe1 grew w 12× on the fitting batch; `--seedlab-frac` default → 0.05 = the retired term's effective decode mass). The driver rebuilt as a **per-era certified mint** (`scripts/sched_mint.py`): sweep pins verbatim, per-store replay params + GENERATION flag set from run.json, serve = generating ckpt sampled `--fork-instrument`, **replay parity witnessed by obs-stream comparison** (standing rule); substrate = the two init-ckpt stores (~4,000 eligible, ~3,600 sampled → ~1,000 labels at the banked 28% yield, `--lab-k 8` ⇒ ~1.4 visits/window/iteration); cost bench resolved from banked artifacts (≈44 s/window at 8 lanes ⇒ per-era cadence; online per-iteration labeler NOT priced in, routed by name on the staleness trigger). Staleness instrument: `sched_live_ce` grad-free on the retired term's target pipeline; live-gap > 3× over two accepted iterations = staled mint (read, never guard). Multi-store seed labels (comma-lists). Suite 259 green. **MINT REPLAY INTEGRITY ([ADR-0089](docs/decisions/ADR-0089-mint-replay-integrity.md), 2026-08-31): the parity witness killed two mint runs and found two stacked mechanisms.** (1) serve carry keyed (g,seat) collides across the stores' overlapping 0–239 ranges (fixed: phased-by-store lanes, fresh server per phase, driver-owned; channel-keyed carry routed as serve hardening). (2) **The deep one: `Obs.mark` shares the decision seq counter (M2 D4) and serve noise is keyed (game_seed, s) (M2 D6) — fork-bearing replay of a fork-free generation shifted every post-fork s by +1/fork and RE-ROLLED every post-fork decision.** Fixed at fork `f9eadfa8d4` (mark stops consuming ids; game-path-identical, `--allow-jar-drift` recorded, parity witness = the ADR-0025 proof — fix smoke STRICT EXACT through six consecutive forks). ADR-0078 VERIFIED IMMUNE (argmax serving never reads the noise key; the mint is the first SAMPLED replay in project history — the latency explained). Two standing rules born. **MINT COMPLETE + READ CLEAN (2026-09-02): parity witness 79.0% valid-turn survival (288 games byte-exact incl. s — the ADR-0089 fixes verified at fleet scale; 153 sparse-flipped, salvage-by-prefix priced; 50% floor never approached) → 653 certified positives, 110 witness-dropped → 543/543 labels minted (cert rate 19.2% on probe states vs the census 28% prior — population-specific, recorded). 543 < 750 ⇒ the pre-adjudicated `--lab-k 4` path (~1.3 visits/window/iter). Seedlab day-zero RE-BANKED: CE 2.676759 on 543/543 (`seedlab-dayzero-mint20260830.json`; cross-validates the 170-batch's 2.730) ⇒ FUND decode leg ≤ 2.141. En route: lane OOM (heap 3g→4g, the AiCache mainline-accumulation class), two degenerate-board lane cuts (witness-priced), a wedged-roll crash-family specimen (g171). PROBES 3–4 RAN + READ (2026-09-02, [ADR-0090](docs/decisions/ADR-0090-m10-probe4-read.md)): probe3 false-halted at iteration 0 on a units bug in the new memorize guard (per-trajectory row values vs per-step calibration; re-based on per-step keys + iteration-min 0.25×); probe4 ran the cleanest loop of the four probes (KL 0.011–0.029, seedlab 2.68→1.35, no impulse — the ADR-0088 mechanics work) and the **degeneracy veto FIRED** on two consecutive generations (pure-hold 42%→52%, mean length 1.0, utilization 5.5%) — **a serve decode-rule defect, not the supervision**: labels are 8% pure-hold / mean 2.45, but whole-row argmax let the plurality STOP class (17%/25%/72% at slots 1–3) beat every candidate individually and collapsed length to ~1; invisible at day zero, unmasked by the first real supervision. Fixed: `sched_slot_pick` (STOP iff p_stop > 0.5, else argmax over candidates); standing rule. Also: the '8 BridgePoisonedException skips' in every probe's iteration 0 are a cold-server launch artifact (8 workers × 1 first request), not the crash family — ADR-0085/0087 re-attributed. **PROBE5 READ ([ADR-0091](docs/decisions/ADR-0091-m10-probe5-read.md), 2026-09-02): decode fix VALIDATED (iteration-1 emissions 6.9% hold / 2.13 mean vs labels 8.1% / 2.45); loop mechanics clean over four accepted iterations (the ADR-0088 fixes hold); **the consumer is UNCOUPLED — utilization ≤4% for four generations (degeneracy veto FIRED on that axis), content_flip ≤0.002, presence at floor: sensible schedules emitted, the cast policy ignored them — NO FUND.** The emitter also drifts off-support (hold 6.9→~28%) because mint labels cover only certified-positive windows (~19% of states). ADR-0087's 'the retired term was the content driver' confirmed from the other side: emission and consumption are two competencies built with one supervision. **CONSUMER COUPLING ADJUDICATED ([ADR-0092](docs/decisions/ADR-0092-consumer-coupling.md), 2026-09-02; [design](docs/design/m10-consumer-coupling-draft.md) = statement of record): two supervisions for two competencies — (1) feed-and-follow CE on the priority pointer at certified mint windows with the certified arm FED (frac 0.05, ADR-0088 mechanics verbatim); (2) **the emitter schedules EVERY turn** — full-support labels: certified arm + the natural line's witnessed realized casts (from the frozen mint store, non-self-referential) + hold where natural held (~3,250 windows, zero rollouts); veto axes absolute; (3) FUND reads content_flip ≥ 0.02 primary, utilization ≥ 25% a floor; (4) probe6 + guard windowed-min + teardown cascade. Standing rule: a supervised emitter needs full-support labels. **PROBE6 COMPLETE + READ ([ADR-0093](docs/decisions/ADR-0093-m10-probe6-read.md), 2026-09-02): six iterations, all accepted, zero guard trips — the first probe to finish; clean exit, zero orphans. DISCUSS-ZONE: KILL does not fire (content_flip ≥ 0.005 at iters 2–5, presence 3–5× floor), FUND not met (content peak 0.0064 vs 0.02; aux legs clear; utilization 0.2% → 37.8% peak; veto never bound). **The coupling is consumed as PRESENCE and FOLLOWING** (reliance L1 0.56 → 1.80, follow CE 10.5 → 5.2, 6.7% behavioral delta from init) while **content sensitivity moved 10× from day zero and plateaued** just above the KILL bar; **the on-policy loop drifts apart** (batch follow CE halves while live follow rate falls 52% → 32%, sched_live_ce rises 4.7 → 7.1) — nothing supervises following the emitter's OWN live plans (the retired self-referential term's job). Emitter DONE (grounded, full-support, decode-correct, stable — first-window hold 5–13%, length oscillating around the label mean 1.95); consumer PARTIAL; on-policy closure OPEN. Record corrections landed: ADR-0091's hold-drift retracted (the counters lump first-window and revision emissions; first-window hold stayed ≤ 9%); the veto's emission axes read first-window emissions. **Probe6 ADJUDICATED (user, 2026-09-02 late): NO-FUND, no KILL — the label-shaped content probe (`scripts/sched_content_probe.py`, ADR-0093 addendum) shows schedule-conditioned following ≈5pp on the consumer's own training windows and content sensitivity ≈3% on label-shaped inputs (0.6% on the pinned day-zero population); the live follow/utilization headlines are natural-line inflation (fed = closed on natural windows); PG never reaches the emitter and the live-gap ratio 3.3→5.7× fires the ADR-0088 staleness tell — 'drifting apart' and 'staled mint' are one fact. **STEP-BACK ROUND OPEN ([m10-reset-draft.md](docs/design/m10-reset-draft.md), six forks for adjudication):** the ckpt of record has not moved since 2026-08-03 across M5–M10; the funded ceiling was measured under binding `-forceschedule` execution while the live surface is advisory. Draft: the planner hierarchy (turn planner = schedule + hold-set as ONE log-prob'd action; window executor-reactor owns the rest), binding execution at serve, planner PG anchored by mint distillation, the certifier as an asynchronous era-weighted background labeler, a stratified paired strength read (v<0.45, the ceiling's own instrument) as the PRIMARY probe read, a retirement list (follow term, content/utilization gates, v1 plan), payment under the same principle named as Fork 6; the learned-fidelity follower recorded by name as the deferred alternative. Budget: two build sessions + one probe; nothing on the stratum = a legitimate negative answer at the hierarchy level. **Fork 1 ADJUDICATED (user, 2026-09-02 late): binding execution, all sub-pins (post-land binding, hold binding on spells, forced-veto attribution to the planner) — on the stated principle: prefer the simpler, more elegant architecture that lets the model act correctly and coherently, even at more data and training time; the two costs (missed-trigger residual, revision-window supervision) promoted to pre-flight items. Fork 2 ADJUDICATED (same session): reward-trained planner (PG on the schedule action, revisions included, reward-only in probe7) with the mint CE anchor at frac 0.05 — on density grounds; planner KL guard at 0.06, entropy a read; E/R stay; lrs unchanged. Fork 3 ADJUDICATED (2026-09-03): INLINE certification — the generation worker certifies a sampled 2% of live MAIN1 windows (rollout-label mode + a per-fork schedule directive over the wire; label = search-adjudicated best of {own plan, arms}, rolled-out windows only — under binding the natural line is no longer an independent witness), the label + arm spread ride the store (the replay window is the era weighting; the 08-30 mint stays an anchor batch until the in-store pool exceeds it), the mint term moves into the RL pass on labeled rows (grounded × dense × on-distribution by construction; replay parity + labbatch mechanics leave the critical path); pivotal-moment head (trained on the arm spread; certification yield / drills / deployment live search) = the named extension. Verified: live forking exists since M2 D4; workers 2g→4g fits (32 of 62 GB). Fork 4 ADJUDICATED (2026-09-03): PRIMARY read = stratified paired strength (candidate vs the same init ckpt under advisory serve, K=8 paired completions from a FIXED baseline-generated v<0.45 population of 600 windows, ~1 h; bar at the ADR-0078 threshold scale, pinned pre-flight) with a DAY-ZERO read (binding execution of the distilled mint alone, before training; below minus the bar = halt for adjudication, not auto-kill); headroom shrinkage (the ADR-0084 sweep read) demoted to promotion-only; KILL = veto first-window axes or forced-veto rate, 2 iterations from the 4th; content/utilization/follow gates, the live-gap ratio and the content probe retired as reads. Fork 5 ADJUDICATED (2026-09-03): the budget STAGED around the day-zero read — session one = binding serve rule + paired-read script + fixed population + the day-zero read (probe6 iter-5 played binding vs advisory; it is also probe7's init); mid-point rule: below minus the bar halts for adjudication (better labels, not a training loop), flat or positive proceeds; session two = loader action + planner PG/KL twin + inline certifier + smoke + pre-flight; then the 6×480 probe reads what training adds. Next: Fork 6 (payment, lean = defer), then stamp ADOPTED + ADR + compress this bullet to the archive.** Five layers peeled in five probes, each root-caused and rule-closed; the exposed layer is now the design. Watch: the copy+resume crash class (0.76% of completions, point-shaped, early-resume; hunt verdict: VARIANT-RANDOM not position-intrinsic — 39/39 poisoned points clean under a fresh JVM draw ⇒ an ordering-sensitive GameCopier defect; `crash_why` standard on crash rows at `7c4af49fa4`; 8 BridgePoisonedException skips in m10-probe1 iteration 0 = the same family, rate unchanged) — own record when named. Verbose record: [status archive](docs/status-archive.md).
