# Anvil

A neural agent for Magic: The Gathering built on the Forge rules engine. Non-commercial, GPL-aligned, designed to be contributed back to Forge. Solo nights-and-weekends project.

**Canonical design doc: [docs/design/anvil-design-v2.md](docs/design/anvil-design-v2.md).** Read it before substantive work. Design changes go into that doc or into an ADR in `docs/decisions/` — never live only in chat history.

## Module naming (Magic vocabulary under the smithy umbrella)

- **Anvil** — the project and the pilot agent (plays the game)
- **Tutor** — the deckbuilder (searches the pool for what the deck needs)
- **Mentor** — the coaching product
- **Grindstone** — the drill economy (grinds scenarios, sharpens the model)
- **Ante** — the luck ledger (accounts what chance took and gave)

Initial scope: 1v1 Commander (40-life Commander ruleset), curated ~1–2K card pool derived from Duel Commander meta decklists + flex slots (ADR-0018: DC is the pool's provenance, not the ruleset; content breadth scales in set-sized dataset-boundary chunks after core features).

## Status

- **M10 SCOPING (opened 2026-08-25): the unified resource-scheduling competency** — turn planning × payment as ONE competency ([m9-d6-plan-latent-spec.md](docs/design/m9-d6-plan-latent-spec.md) ledger, user framing). Candidate shape: v2 schedule-bearing plan target (sequencing + resources, D2a cost knowledge into the conditioning channel) + re-advertised payment actuation + ADR-0075 supervised conditional labels, trained and read as one competency. Design-round obligations before any build: charter adjudication vs §3b stops (the named alternative), the planning ceiling measurement (ADR-0073 rule), the v2 offline target probe (ADR-0074 pattern, co-designed with actuation), plus two funded ceiling probes routing M11 — tutor/fetch targets (§3d′ family 2, user-raised 2026-08-25) and resolution-effect payments (the measured argument ADR-0077 demands). Charter held on the substrate corollary (user): the plan latent is plausibly the prerequisite for downstream capability expansions (tutoring included) to show full potential. Design round progress (2026-08-25): schedule terrain census run (31.6% of own turns resource-bound, `data/runs/schedule-census-m10`); latent-vs-emitted fork ADJUDICATED to soft emission (interaction-robustness rationale; hard execution staged behind follow/validity evidence); **planning-ceiling measurement PRE-REGISTERED and adjudicated ([m10-ceiling-spec.md](docs/design/m10-ceiling-spec.md)**: two-stage h2-certify → game-end conversion + h4 side-sample, joint schedule-consistent payment, 2×-floor funding thresholds); **`-forceschedule` BUILT + mechanical smoke PASSED (session 4)** — `ScheduleDirective` soft-emission executor (land-first, degrade-and-count) + knob-c payment scorer + AnvilRun sched rollouts (rollSeed keyed on target turn for the stage-2 identity) + `local-oneshot` smoke bridge (`scripts/schedule_smoke.py`; forced-draw winner nondeterminism found+fixed, spec addendum records executor semantics). **Launch commit LANDED (session 4): θ=2.0 + pinned composite, h4 flag 1.25×/+3, seed base 20520825, sampling rng — `scripts/sched_pins.py` executable source.** Same night: fresh census RUN + ingested (500 games, jar-parity vs the bundle commit proven empirically first), pinned sample drawn (4,084 eligible, 8.17/game; 600+200+100), read pre-registered (`schedule_read.py`), model-serve smoke PASSED (margins byte-identical across re-run; en route: sched-scale watchdog-clock OOM found+fixed trace-invisibly, fork `07900c3db1`), **stage-1 sweep LAUNCHED overnight** (`data/runs/sched-sweep-m10`, ~111k rollouts, watchd+notify covered). Next: stage-1 read + h4flag → stage-2 quiet-box conversion → funding verdict (ADR owed). Scheduled in: cost-composition cousins + costmod per-spell + pool-tie residual (ADR-0077 routing).
- **M9 CLOSED (2026-08-25, [ADR-0077](docs/decisions/ADR-0077-m9-closeout.md)): the interface round — mechanism verdict FALSIFIED (no veto collapse under the restored campaign, [ADR-0072](docs/decisions/ADR-0072-d4-control-run-veto-collapse-falsified.md); the probing-equilibrium account instead confirmed through THREE channels, the plan-latent conditioning channel at 10× drill-fed speed, [ADR-0076](docs/decisions/ADR-0076-d6-probe-read.md)); strength verdict NO PROMOTION (done-when 5 superseded with reason — ckpt of record stays `d6-run11/iter-019`, post-boundary baseline 0.5279 ± 0.0110, [ADR-0068](docs/decisions/ADR-0068-m9-boundary-bundle.md) era `2f87180cdf`).** The funded facts: perfect payment play is REAL at gate resolution (≈+2.96pp/game, ~2.7× the floor, as a supervised conditional competency — [ADR-0075](docs/decisions/ADR-0075-window-rate-sweep.md), 5,076-window labeled universe) and the D6 plan-latent mechanism VALIDATES (consumed in one iteration). Assets carried: payment surface as infrastructure (legality-derived enumeration + float-then-apply executor + `payment_certify.py` 2-turn proxy), full D6 plan machinery + `plan_reliance.py` + `d6-plan-init`, veto-knowability instrument v2 across eras, boundary bundle (engine `23c3d2a85d`, obs sv=2, multi-format). Queue routing all by name in the ADR (cousins/costmod/pool-tie → M10 actuation build; effect payments re-deferred; pin-12 re-mine retired; evalset repair rides with M10 label work). run18/19/20 stores veto-elevated, mixture-banned permanently. Ten standing rules born here → [standing-rules.md](docs/standing-rules.md). Verbose record: [status archive](docs/status-archive.md).
- **M8 CLOSED (2026-08-19, [ADR-0062](docs/decisions/ADR-0062-m8-closeout.md)): the curriculum × curation family measured out — critic-ordered curation, the family's last never-run method, TIED at the gate (`d6-run17` iter-009 +0.75pp ± 0.99; no promotion).** D1 natural-timing probe FAILED its pin ([ADR-0060](docs/decisions/ADR-0060-d1-natural-timing-probe.md)) ⇒ pre-registered pivot; rank-critic ordering SURVIVED the era transfer ([ADR-0061](docs/decisions/ADR-0061-d2prime-audit-resolution.md), Spearman 0.4205 on 492 K=8 anchors). Born here: the standing veto account (vetoes = interface artifact; deterrence axis CLOSED) that M9 then falsification-tested. Assets: `rankcrit_audit.py`, `critic_select.py`, parameterized `cycle_stock.py`, 492 banked K=8 labels, VRAM scale-to-zero. [Archive](docs/status-archive.md).
- **M7 CLOSED (2026-08-16, [ADR-0058](docs/decisions/ADR-0058-m7-closeout.md)): credit assignment split verdict — dense per-decision signal is TRAINABLE and BEHAVIOR-MOVING (`d6-run16`, 16 clean iterations, the term taught its target into the greedy-aggression trade); the act−hold formulation is STRENGTH-NEUTRAL (both gate candidates TIE; no promotion).** Two instructive halts en route ([ADR-0056](docs/decisions/ADR-0056-run14-seq-divergence.md) unbounded-L_seq runaway, [ADR-0057](docs/decisions/ADR-0057-run15-share-drift.md) PG-mass fade = standing loop property). Machinery carried: seqlabels join, hinged seq_pass, share guard, battery seq curves, evalset v4. The chartered natural-timing follow-up resolved at M8 D1 (negative). [Archive](docs/status-archive.md).
- **M6 CLOSED (2026-08-10, [ADR-0050](docs/decisions/ADR-0050-m6-closeout.md)): representation is partially gradient-reachable and NOT the binding constraint — the bottleneck is learning-signal density ([ADR-0049](docs/decisions/ADR-0049-flat-cycle-audit.md): §6c was the loop's only dense per-decision signal).** Elimination chain 0043–0048; graduated build + best-ever curation TIED at the gate (no promotion). Assets: `rank-critic-c2v3` (holdout 0.4833) + era-scoped isotonic map, `labelset-c2-v3` (8,683/1,197), frozen-probe benchmark + holdout-freeze protocol. [Archive](docs/status-archive.md).
- **M5 CLOSED (2026-08-05, [ADR-0037](docs/decisions/ADR-0037-m5-closeout.md)): the drill loop is ONE-SHOT per curation method — Δ2 = −0.58pp ± 0.73 vs Δ1 = +1.98 ± 0.71, predicted pre-launch by the migration read (promoted to standing tool, `scripts/migration_read.py`).** D3 critic calibration split verdict ([ADR-0036](docs/decisions/ADR-0036-d3-critic-calibration.md)): isotonic remap fixes absolute scale (ECE 0.33→0.03, adopted era-scoped), ranking is representation-blind (Spearman 0.27 vs 0.94 achievable). No promotion. [Archive](docs/status-archive.md).
- **M4 CLOSED (2026-08-03, [ADR-0033](docs/decisions/ADR-0033-m4-closeout.md)): Grindstone online end-to-end — first outside-noise drill-signal win (+1.98pp ± 0.71 t=2.77, replicated fresh-seed, [ADR-0031](docs/decisions/ADR-0031-a2-resolution.md)); `d6-run11/iter-019` PROMOTED (the still-standing ckpt of record).** Serve path = documented negative; the real lever was generation shape (w=16 + chunk clamp, ~+30%, [ADR-0032](docs/decisions/ADR-0032-d4-serving-path-resolution.md)). Infra born: `anvil_watchd` + self-registration, mid-run drill-eval, fork-session ingest, pool `CURRENT` pin, D2.4 pairing protocol. [Archive](docs/status-archive.md).
- **Playable-fork track (2026-07-26→08-21; current release v16 `291a866fa3`, `2.0.15-SNAPSHOT-08.22` — upstream sync onto `23c3d2a85d`, the M9-boundary engine commit): founding worklist items 1–8 ALL BUILT, PUBLISHED, user-verified; [worklist](docs/design/playable-fork-worklist.md) is the doc of record** (fork branch `playable`, worktree `../forge-play`; the research fork has zero delta from any of this). Shipped highlights: resizable window/viewport, deck-site import + Archidekt sync, configurable tap angle, in-place delta auto-updater (hash-verified, two-hop-verified), net-lobby fixes queued upstream. Release pipeline: `scripts/release-playable.sh` → `daily-snapshots` prerelease; **publish MUST pin `-R Tyrathalis/forge`**; post-publish parses the manifest through the shipped jar. Friend-install pins: Temurin 21 via adoptium permalinks; macOS 15+ Gatekeeper flow in `Forge.command`. **Multiplayer hardening CLOSED 07-30** ([record](docs/design/multiplayer-hardening.md)): 9 findings, 4 advisories, 3 merged upstream, chat rate limiting = PR #11457. Residue: T2 live-rescale only. Upstream-candidate queue: see the project map watch list. **Hazards: shared user deck store (`launch --pool` hash-gates it); never check out `playable` in the research worktree.** [Archive](docs/status-archive.md).
- **Chronicle side stream ACTIVE ([ADR-0029](docs/decisions/ADR-0029-chronicle-scheduling.md) + [mvp-plan](docs/design/chronicle-mvp-plan.md)): the 1993–94 collector-loop MVP on the playable branch, behind `CHRONICLE_MODE_ENABLED` (default OFF) until the 2-week dogfood gate.** Invariants: seed integrity, monotone day tick, prestige-proof saves, pack EV negative by construction, runtime cosmetics. **D1–D4 + D6 DONE (D6 kitchen-table verified in play on device 08-24); D5 dogfood OPEN — the two-week clock is running.**
  - Pin 8, above the MVP/stage line ([ADR-0070](docs/decisions/ADR-0070-chronicle-effort-reward-sink.md)): **the loop must have an effort→reward channel** (round-1 finding: timer grants + negative-EV buylist meant nothing converted invested time into progress).
  - D6 essentials ([ADR-0071](docs/decisions/ADR-0071-d6-design-round.md) + addendum): cash bounded (one paying game per rival per played day), ante UNBOUNDED priced in risk; rival pools DERIVED never stored from (run seed, rival id, pack index) — the release calendar IS the difficulty model and bounds world card supply; ante persists only a delta. Forge's real ante never inherits `UI_ANTE`. Finding: no corpus of period decklists exists, and that is historically correct.
  - Standing lessons (verbatim in the [archive](docs/status-archive.md) + devlogs): drive the build to find libGDX screen defects (isolated `-Duser.home` + `xdotool` + `import -window`; probe to a FILE — logging stops at boot); implementing an interface means its CONTRACT (`setEditor` must call `notifyNewControllerModel()`); curated feature data ships inside the jar/APK too; focus windows by title, never app name.
  - **NEXT: D5 dogfood proper**, then the three-axis numbers pass (purse-per-difficulty, ante multiplier, `anteRivalFloorCards`). Watch: the author's Android res delta looks stale.
- **M3 CLOSED (2026-07-28, [ADR-0026](docs/decisions/ADR-0026-m3-closeout.md)): RL ckpt at 0.5121 ± 0.0110 corrected post-rebase (+6.69pp over BC, t=4.32; parity with the strengthened heuristic).** D1 veto fix (cmd-tax featurization + §6c penalty, [ADR-0022](docs/decisions/ADR-0022-d1-closeout.md)); D2 first outside-noise supersession via the full-vis critic ([ADR-0023](docs/decisions/ADR-0023-d2-closeout.md)); batch lever falsified, near-tie residual = absent signal ([ADR-0024](docs/decisions/ADR-0024-run8-batch-lever.md), the argument that seeded Grindstone); D4 rebase = the boundary-event template ([ADR-0025](docs/decisions/ADR-0025-d4-rebase-closeout.md)). Standing tools: `scripts/final_read.py` (2,000-game corrected closing protocol), collate-in-worker learner, `bench_generation.py`, `notify.py`, `scripts/early_doom.py`. Upstream: [#11285](https://github.com/Card-Forge/forge/pull/11285) determinism hooks (awaiting review). [Archive](docs/status-archive.md).
- **M2 CLOSED (2026-07-17, [ADR-0020](docs/decisions/ADR-0020-m2-closeout.md)): the V-trace self-play loop built end-to-end; first RL ckpt of record superseded BC (`d6-run3/iter-012`, 0.5213 old-scale).** Fork rollout contract certified (statics 0, twin 40/40, fixes upstream as [#11203](https://github.com/Card-Forge/forge/pull/11203)); SA-level action schema at zero agreement cost; winner-label poisoning found+fixed ([ADR-0013](docs/decisions/ADR-0013-winner-label-poisoning.md)); Ante AIVAT certified ([ADR-0014](docs/decisions/ADR-0014-ante-certification.md)); `d4-critic-fullvis` = eval/Ante critic of record; entropy-collapse guards + hinge floor born ([ADR-0017](docs/decisions/ADR-0017-run2-entropy-collapse.md)). [Archive](docs/status-archive.md).
- **M1 CLOSED (2026-07-10, [ADR-0009](docs/decisions/ADR-0009-m1-closeout.md)): the BC agent plays real games at 46.8% ± 2.5pp vs its teacher (held-out honest agreement 0.9758).** Obs schema v1 + zstd store, CastPlan extractor/executor (65%→~5% veto), 113,592-game zero-error corpus, frozen Qwen3 embeddings pinned ([ADR-0007](docs/decisions/ADR-0007-embedding-pin.md)), loader-parity serve path free at w=8. [Archive](docs/status-archive.md).
- **M0 CLOSED (2026-07-04, [ADR-0003](docs/decisions/ADR-0003-m0-closeout.md)) + foundations.** Fork at `../forge` (blobless; `origin` Tyrathalis/forge, `upstream` Card-Forge/forge; `~/.local/opt/maven/bin/mvn -P windows-linux`, Java 17 on JDK 26). Bridge protocol v0 (six game-agnostic shapes, [spec](docs/design/bridge-protocol-v0.md)), `PlayerControllerAnvil` injection, harness `launch|pause|resume|status|replay|summarize` (`run.json` pinning, chunk recycle/resume). Measured: bridge tax 2.6% at w=16; callback census 64/109. Pool pipeline `anvil.pool` — **current pool `cf2ca6ba`, 1,701 cards from 113/120 DC decks**. [Archive](docs/status-archive.md).

Update this section as milestones land. When a milestone or track CLOSES, move its full bullet(s) verbatim to [docs/status-archive.md](docs/status-archive.md) and leave a compact summary here (headline numbers, ADR links, operative assets/hazards, archive pointer). The same move applies mid-flight: when an OPEN bullet grows past ~2–3 KB, snapshot it verbatim to the archive and compress here — ADRs/devlogs carry the detail; this section carries the state. **Standing rules do NOT accumulate in these bullets: they live in [docs/standing-rules.md](docs/standing-rules.md)** (one line per rule, ADR-linked; added there in the same commit batch as the ADR that births them).

## Design invariants (re-read these every session)

- The engine adjudicates every claim any learned component makes.
- Every LLM judgment is downstream-verified (LLMs filter and narrate; they never generate training truth).
- Every drill is provenance-traced to a real game.
- The value function is continuously audited against rollouts.
- Detection is the engine's job; response is the model's.
- The error-accounting queue is the spine everything reports to.
- The model never sees the engine version — formats are rules to play to; versions are bugs not to learn.

These seven are the constitution; the operational layer under them is **[docs/standing-rules.md](docs/standing-rules.md)** — every measured rule the milestones have birthed (gating/reads, training-loop design, curation, engine/data hygiene, routing), one line each with its ADR. Read the relevant section before designing any run, gate, instrument, or curation cycle.

## Hard conventions

- **Game-agnostic schema:** keep Magic-specific assumptions (zone lists, feature names) out of the Python-side schema (design §1, cross-game hygiene). Costs a naming convention now, preserves multi-game optionality forever.
- **Seed everything; deterministic replay.** State forking with seed control is load-bearing for four systems.
- **Provenance on all trajectories:** source, engine hash, checkpoint, drill-template ID.
- **Fork discipline:** pinned Forge versions per run; engine upgrades are dataset boundary events. **Exemption ([ADR-0025](docs/decisions/ADR-0025-d4-rebase-closeout.md)):** a change that is behavior-identical on the game path is not a boundary — but the proof is *empirical, not argued* (same seed set on both jars → identical `forkcheck` trace hashes; any mismatch ⇒ boundary). A crash fix never qualifies; swapping our fork-local fix for an upstream fix of the same defect does. Upstream PRs stay small, tested, human-reviewed.
- **Long-running jobs launch at low priority** (`nice -n 19`) so the desktop preempts them; harnesses are designed for graceful stop + seeded game-granularity resume (design §9, "disposable workers"). Calibrated measurements are the exception: schedule them on a quiet box instead.
- Python ~60% / Java ~40% overall; Java work is archaeology in the Forge fork, not here.

## Repo layout

- `anvil/` — Python package; subpackages map to design-doc sections (`encoder` §1, `state` §2, `policy` §3, `heads` §4, `tutor` §5, `training`+`grindstone` §6, `ante` §7, `bridge`+`store` §9, `pool` §8, `evals` §7, `mentor` §11). Skeleton only until M1; M0 work concentrated in `bridge/`.
- `docs/design/` — canonical design docs.
- `docs/devlog/` — one dated file per working session (see workflow below).
- `docs/decisions/` — ADRs for anything that changes or resolves a design-doc question.
- `tests/` — pytest.

## Working session workflow

- **End of every session:** write `docs/devlog/YYYY-MM-DD.md` (copy `docs/devlog/TEMPLATE.md`): what was done, what broke, what the next session picks up. Claude writes this as part of wrapping up.
- **When a design question is resolved or a design-doc claim changes** (e.g., a probability from §15 gets measured, a bet flips): write an ADR in `docs/decisions/` (copy the template, next sequence number) and update the Status section above. If the ADR births a standing rule, add it to [docs/standing-rules.md](docs/standing-rules.md) in the same commit batch.
- **Keep `docs/project-map.html` current alongside the devlog/Status:** whenever a milestone opens/closes, a deliverable lands, or a headline number changes, update the map in the same commit batch. It's the living overview — a stale map is worse than none. Since the 2026-08-01 dashboard restructure, the parts that go stale FASTEST are at the top: the **Now panel** (current focus per track), the **state-of-record table** (ckpts/baseline/pool/fork pin/release), the **watch list**, the **run ledger** (one row per self-play run, verdict-colored — every closed run gets a row), and the footer stamp. On milestone/deliverable close, also sweep the embedded labels: flow-diagram box text, pill states, open-PR statuses, ADR index rows. Milestone entries are `<details>` (closed milestones stay default-collapsed with a one-line summary; the open milestone stays `open` with a done-when checklist + per-deliverable bullets — never a single prose wall).
- **Milestone close includes a stale-data deletion pass** (standing habit born in the M6 opening chore, 2026-08-05): inventory the closing milestone's forensic dumps / scratch dirs under `data/`, verify nothing operational references them (grep `docs/decisions/`, `scripts/`, `anvil/`; confirm calibration/drill tooling and ckpts of record live outside), present a kill list with per-dir size + justification, delete on user sign-off. Kept unconditionally: ckpts of record, drills.jsonl dirs, selection/evalset assets, baseline-era finalarm/confarm stores, Ante certs. Cheap-prune note: a forensic dump's bulk is usually one worker's `census.jsonl` — trimming that file is often enough.
- **Every run emits and we read its analysis battery by default** ([run-analysis-protocol.md](docs/design/run-analysis-protocol.md), pinned 2026-08-12): the driver/final_read auto-run `anvil/evals/battery.py`, `analysis.md` leads with ANOMALIES and the notify line carries them; session pickup after any run starts by opening its analysis report. Battery findings are exploratory only — verdicts stay pre-registered.
- Commit at natural checkpoints with plain descriptive messages; the devlog carries the narrative, not the commit log.
