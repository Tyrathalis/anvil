# Anvil

A neural agent for Magic: The Gathering built on the Forge rules engine. Non-commercial, GPL-aligned, designed to be contributed back to Forge. Solo nights-and-weekends project.

**Canonical design doc: [docs/design/anvil-design-v2.md](docs/design/anvil-design-v2.md).** Read it before substantive work. Design changes go into that doc or into an ADR in `docs/decisions/` — never live only in chat history.

## Module naming (Magic vocabulary under the smithy umbrella)

- **Anvil** — the project and the pilot agent (plays the game)
- **Tutor** — the deckbuilder (searches the pool for what the deck needs)
- **Mentor** — the coaching product
- **Grindstone** — the drill economy (grinds scenarios, sharpens the model)
- **Ante** — the luck ledger (accounts what chance took and gave)

Initial scope: 1v1 Commander (40-life Commander ruleset), curated ~1–2K card pool derived from Duel Commander meta decklists + flex slots (ADR-0018: DC is the pool's provenance, not the ruleset; content breadth scales in set-sized dataset-boundary chunks after core features).

## Status

- **M4 OPENED (2026-07-28, [ADR-0027](docs/decisions/ADR-0027-m4-opening-sequence.md) + [m4-plan.md](docs/design/m4-plan.md)): Grindstone — a different signal source.** Identity: split, Grindstone spine + one promoted infra deliverable (user: mix efficiency work in). Deliverables: **D1 adaptation probe `d6-run9`** (standing recipe verbatim from `run7b/iter-014/{train,critic}` on the rebased fork, ~10 guarded iterations, fresh seeds — resolves the re-baseline mechanism question under *training*, which no late-M3 run did: recovery ⇒ opponent-fit, adapted ckpt = new baseline; flat ⇒ heuristic stronger, 0.5121 stands; closing 2,000-game read doubles as D2's curation substrate) → **D2 Grindstone v0** (regenerate early-doom/curation on new-engine games — the old 531-loss list only replays on `pre-rebase-20260725`, so it's method-validation, not a usable seed list; drill machinery = re-aim the parked `-rollout K -points M` apparatus from sampled to *curated* crash windows; drill manifest w/ full provenance; re-price economics under micro-batching; drill eval suite; **LLM filter + ddmin deferred** per user) → **D3 signal integration, (a) first** (drill-mixed generation at a priced fraction, learner unchanged; escalation (b) K-rollout advantage baselines → (c) per-action contrastive/expert-iteration, entered only on resolved-negative; gate = paired 2,000-game corrected read vs the D1 baseline) → **D4 serving-path throughput, PROMOTED** (generation ~80% of run cost, w=8 saturates ~189 rps ≈ 5.3 ms/request Python overhead; noise floor → profile → one measured lever; bounded: measured gain or documented negative). Track C expressiveness stays deferred; upstream = watches only (#11285 nudge via queued #11360 comment). Key insight recorded: K rollouts from one forked position collapse advantage variance exactly at the near-tie windows ADR-0024 diagnosed as signal-free — drills are the mechanical answer to the absent-signal verdict, not just extra data. Done-when: (1) re-baseline ADR + M4 baseline number — **SATISFIED 2026-07-29 ([ADR-0028](docs/decisions/ADR-0028-d1-adaptation-probe.md)): D1 CLOSED same-night.** `d6-run9` ran 10/10 under guards (zero halts, zero tripwires; post-collate cadence proven in production: train ~3 min vs generation ~28 min/iter); iter-009 closing read 0.5233 ± 0.0110 corrected, **paired +1.15pp ± 0.90 vs run7b-i14 (t=1.28) = directionally-positive tie ⇒ verdict: the heuristic got stronger** (opponent-fit predicted fast recovery and didn't deliver; residual ≲1pp); **baseline stays 0.5121, ckpt of record unchanged**; veto re-fit real and strength-neutral (first-attempt 0.118→0.082, third confirmation of wasted-intent); `d6-run9/iter-009` = designated D3 init (adapted, veto-clean); closing-read stores (`run9-finalarm-s{0,1}`, obs on) = D2's curation substrate, traces via the probe's own new-engine critic; the probe also re-confirms ADR-0024's absent-signal diagnosis on the new engine (standing recipe: 10 healthy iterations ⇒ ~1pp inside noise); (2) Grindstone v0 online end-to-end; (3) a drill-signal run beats the baseline outside noise (or escalation exhausted, negatives to ADR standard); (4) serving path profiled + measured improvement or documented negative. **D2.1 DONE same morning (records `data/runs/early-doom-run9-{i009,d4crit}/`): the curation shape SURVIVED the rebase — 584 addressable losses (61.2% of 954; old 531/59.6%), ceiling 0.82–0.91, luck-locked 18–38%, 208 single-turn ≥30pp collapses across 82 decks; two-critic agreement within 0.3pp on every headline number. The new-engine drill seed list is live (`early-doom-run9-i009/curation.jsonl`); user context corroborates the D1 verdict (Discord posts show active bot-tuning work in the rebase window).** **D2.2+D2.3 BUILT + SMOKE-GATED same day (fork `46e2ec7c16`, Anvil `b1b6117`, suite 103 green): drill machinery end-to-end** — fork `-drillfile`/`-drillstop` (explicit fork turns; gameless skips w/ accounting rows; labels `tt` join field; early mainline stop), `anvil.grindstone plan|generate` (curation → manifest pinning the source `run.json` replay recipe; argmax server on the manifest ckpt; obs ON per the M1 replay rule). **Gate: 7/8 source games reproduce exactly (also = the rebuilt jar's empirical game-path-equivalence proof); the 1 = the known ~1–2% heuristic wall-clock replay-drift class ⇒ diverged replays yield no label — miss-rate is an accounted statistic.** Economics re-priced: ~47 s/drill (w=4, K=8) ⇒ full 584-drill sweep ≈ 2.5–4 h at w=8. **Design-shaping smoke finding: ground truth at crash windows ≈ 33% model winrate vs critic 70–90% (two 0/8 games = error upstream of the crash; one 8/8 = draw-order luck) — fork-at-crash is often late; `--turn-offset`/`peak_turn` = the first D3 sweep axis.** Pinned hazards: `-drillstop` mainlines record pseudo-winners ⇒ drill mainline stores NEVER enter training ingest; store-side `drill` provenance tag + fork-session ingest = the open D3 integration work. **THE FULL 584-DRILL GROUND-TRUTH MAP LANDED same day (`drill-map-r9i9-k8/`, `grindstone report` verb formalized w/ multi-run supersede): rollout winrate 23.7% vs mean v_before 0.584; bins lost-at-crash 58% / long-shot 22% / coin 9% / winnable 12%; replay-miss 1.0% ⇒ the turn-offset/peak-turn sweep is D3's FIRST experiment (most crash windows sit after the real error).** The map caught two engine crash classes: **monarchy-transfer copy bug FIXED same day** (fork `6d728677d1` — `Zone.remove` leaves stale zone pointers on cached monarch/initiative effect cards; membership guard, 2 validated-failing-first tests, suite 299 green, 35/44 positions recovered, upstream candidate queued w/ evidence) and **MayPlayPlayer `.get(0)`-on-empty DIAGNOSED w/ deterministic repro** (9 positions; upgrades the carried IndexOutOfBounds item; fix deferred to the copy-ordering question — likely folds into the consolidation PR). **D2.4 DONE same day ⇒ done-when #2 SATISFIED, D2 COMPLETE (opened and closed 07-29): dual-policy server (`--drill-ckpt`: fork wids → ckpt under eval, mainline replay pinned — also D3's serving substrate) + `grindstone evalset` (176 held-out drills, stratified over ground-truth bins, membership recorded — D3 training must subtract) + `grindstone eval` (per-bin paired vs baseline).** Validation gate decoded: 176/176 labeled, 85% bit-exact reproduction, corr 0.862 = plumbing proven; exact identity unachievable (bf16 argmax near-tie flips + heuristic wall-clock), and **the winnable bin's −7.2pp re-measure = regression to the mean from selecting on noisy K=8 estimates — protocol pinned: evals pair vs the pinned policy's own re-measurement (`baseline_eval`, auto-used), never selection-time labels** (winnable true mean 0.794 under eval conditions, not 0.865). Suite 105 green. Critic-sharpening noted as the (b)-adjacent D3 lever: the 578 K-rollout labels double as a critic calibration set (critic 0.58 vs ground truth 0.24). **D3 OPENED same evening — experiment 1 (turn-offset/peak-turn sweep) RESOLVED POSITIVE overnight (`drill-sweep-lost-20260729/`, Anvil `faf9d01`): the 330 lost-at-crash drills re-drilled at 4 anchors (`plan --anchor {crash,peak}` + `--tag` per-arm run-dir isolation — report's later-supersedes aggregation would otherwise cross-clobber arms; `scripts/drill_sweep.py` with o0 re-measure baseline per the D2.4 protocol). Winrate monotone in fork earliness: at-crash 0.048 (bin validated, not selection noise) → −2 0.248 → −4 0.331 → peak 0.356 (+30.2pp paired); 81% of games have a fork point in the trainable band [0.25,0.75], only 46 stay ≤0.2 everywhere (luck-locked profile: gap 3.7, peak_v 0.645 — out of the drill pool); error localization: 75 games within 2 turns of crash / 53 in (−4,−2] / 52 peak-only. KEY DESIGN CONSEQUENCE: no global offset wins (o4/peak recover different games, 65 vs 56 exclusive) ⇒ drill fork turns selected PER GAME from the sweep's own labels; sweep labels also 3× the critic calibration set (~1,900). **Per-game selection SHIPPED next morning (07-30, `grindstone select`, record `drill-selection-v1/`): latest in-band [0.25,0.75] fork per game (fallback latest above-band — converting won positions is still a drill; luck-locked excluded), label sources merge with later-supersedes (re-measures over selection-time labels per D2.4), evalset holdout subtracted AT THIS BOUNDARY (training lists are built here) — 349 training drill points (334 band + 15 above / 44 excluded / 176 held out; mean sel winrate 0.416 ≈ variance-maximal; offsets 93@0 / 128@−2 / 87@−4 / 41 earlier; accounting exact: 584 = 569 usable + 9 all-crash + 6 replay-miss); `plan --anchor selected` consumes selection.jsonl directly (349 drills planned clean, suite 111 green).** **Fork-session ingest SHIPPED same day (fork `66fa3fe45e`, Anvil `add7326`): `-forkobs` makes drill completions store frames of their own** (separate `obs-forks.zst`; synthetic unique ids; fork provenance headers; per-completion announced seeds — kills the μ (g,s)-collision and identical-noise hazards found latent in the wire-only design; per-dec STORED serve-time hist — fork first-windows carry parent-ring context no loader reconstruction could recover) + `anvil.store ingest --forks` (fork frames only, **drill mainlines never ingest — enforced by construction**; games.jsonl synthesized from driver-computed end winners; per-fork-point labels cross-check; manifest `source: drill-forks` + parent provenance) + loaders (rl.py, dataset.py) prefer stored hist. Smoke end-to-end clean (16 frames, cross-check 2/2, castplan OK, 8/8 parent-ring hist rows on first window); suites 113 + fork 294 green. **Sampled+μ backend SHIPPED same day (`7823144`): server `--drill-sample`/`--drill-mu-out` (fork sessions sampled w/ μ, mainline argmax pinned; sampling a wire-only g=−1 header raises → loud decline, no μ ⇒ misconfigured runs can't emit trainable data); `generate --sample-forks` = one server+μ file per arm (synthetic-id namespaces overlap); gate: 2,787 μ-joined windows, zero mu_mismatch, rewards exact — fork-frame serve/train parity proven end-to-end.** **Driver drill phase SHIPPED + d6-run10 LAUNCHED same day (`608140d`): per-iteration rotating selection slice (drill stores join the iteration's store GROUP ⇒ §6d fresh→replay aging free; f by volume: 15 pts × K=8 = 120 completions/iter = 20% of fresh games, ~14% of fresh trajs; full cycle ≈ 23 iters), mini-run green (tripwire 0, crash-skips correct, no flags) — d6-run10 = standing recipe verbatim + drill phase, init run9-i009 (train+critic), mainline pinned run9-i009, 20 iterations ≈ 14 h, guards+watcher armed. **RUN CLOSED 2026-08-01 ([ADR-0030](docs/decisions/ADR-0030-d3-experiment-a-resolution.md)): guard halt at iter 16 (KL 0.085 + veto 0.154 + entropy together — real multi-signal drift, halted by design; 16 accepted iterations). Closing reads: iter-004 0.5254 / iter-015 0.5273 ± 0.011 corrected — vs baseline +1.4pp at t≈1.4 (tie, GATE NOT CLEARED); vs own init +0.25pp and i15-vs-i04 +0.05pp (dose-response exactly flat) ⇒ experiment (a) at f=20% = strength-neutral. BUT the drill evalset decomposition found the mechanism: held-out lost/long_shot +6.6/+6.7pp (real generalization, grew with dose) cancelled by winnable −9.7pp — and the training list was ~100% behind-positions BY CONSTRUCTION (the D2.4 evalset held out ALL winnable+coin bins). Rej-split: drill trajs carry 1.65× the rejection density (the §6c pressure behind the shaped-vs-masked flag that led the halt by 4 iterations). Ckpt of record + 0.5121 baseline unchanged; drill-evalset-v1 superseded for training-adjacent use (re-split required). En route: iter-9 driver crash (wedged-completion → abandoned-thread → fork-fd death → ingest death cascade) fixed at all three layers (fork `7249a41b60` owner-bound frames, Anvil `eb6381f` quarantine ingest) and the same wedge recurring later cost exactly one quarantined frame; standing systemd watcher `anvil_watchd` shipped (registration-file + pid/starttime/btime identity — the pgrep self-match class claimed three babysitters before the pattern was fully excised); targeting-retry hang class queued for forensics. DECIDED 2026-08-01 (user): **the bounded rebalanced (a2) run is the current plan** — stratified evalset re-split (whole bins never again consumed by holdout) → `select` re-run w/ ahead-positions in training → (a2) vs the same 0.5121 baseline; escalation (b) stays armed behind an (a2) negative. Watcher self-registration SHIPPED same day (notify.py `watch_register/unregister`; selfplay driver + final_read report their OWN pids, deliberate exits unregister, crashes leave the registration so the watcher fires GONE — the pgrep-derived pid class fully retired; watchd env-overridable + 3 tests). **(a2) PREPPED + d6-run11 LAUNCHED same day (session 4, zero code changes — the verbs already took the knobs): evalset v2** (`drill-evalset-v2`: winnable 22 / coin 17 / long_shot 40 / lost 40 = 119 held out — ~⅓ of each small bin, whole bins never again consumed; baseline re-measure 118/119 w/ `baseline_eval` pinned; instrument cross-check: agrees with v1's re-measure within noise in every bin on the 99 shared games — v1's winnable regression 0.865→0.794 lived entirely in the 44 unpicked games, i.e. regression-to-mean is per-game and the D2.4 pairing protocol absorbs it), **selection v2** (`drill-selection-v2`: 409 training pts = 370 band + 39 above / 41 excluded, mean fork wr 0.470; **18.8% ahead-positions** = exactly the 77 winnable+coin games released from v1's holdout, mean sel_wr 0.714; rotation slices carry 2–4 ahead each, accounting exact 119+409+41=569), **d6-run11 = run10 recipe VERBATIM except fresh seeds 20261031 + selection-v2** (init + mainline pin run9-i009, 20 iters ≈ 14 h, guards + self-registered watcher) — one variable changed: mixture composition. Hypothesis under test: run10's winnable −9.7pp was the balancing term; ahead-weight converts the held-out +6.6/+6.7pp into net strength. Gate: standing 2,000-game paired read vs 0.5121 + evalset-v2 decomposition on candidates (v1 stays valid only for run10-era ckpts); (b) armed behind a negative.**
- **Playable-fork track (2026-07-26→30): founding worklist items 1–8 ALL BUILT, PUBLISHED, and user-verified in play; current release v12 (2026-08-01: upstream refresh + Chronicle provenance UI + context-menu offset fix; publish MUST pin `-R Tyrathalis/forge` — gh default repo is upstream, same release tag name). Residue: T2 live-rescale only (workaround: launch at play size).** [playable-fork-worklist.md](docs/design/playable-fork-worklist.md) is the doc of record. A human-facing distribution build on fork branch `playable` (worktree `../forge-play`; the pinned research fork `master` has zero delta from any of this). Shipped: resizable window + live viewport (six frozen-size layers found and fixed: hints/bounds/projections/selector/backdrop/headers), deck-site URL import on mobile + bulk Archidekt sync (per-username flat folders under `decks/URL/`, never-move re-sync, Unknown-format sentinel + lazy `Unsorted/`, annotate-not-quarantine; Moxfield deferred — relationship problem, not engineering), configurable tap angle `UI_TAP_ANGLE` 90–15° on both clients (shared `RotatedRect` inverse-rotation hit-tests, identity-gated at 90°; en route caught and fixed the stock nested-transform bug — mobile `startRotateTransform` never composed), slot packing + single-column zones, in-place delta auto-updater repointed at the fork (per-file manifest delta, hash-verified, staged-jar swap; hardened by three field incidents: `..`-substring path check, percent-encoding for 30K+ space paths, bridge manifest + res self-heal; **v4→v5 and two-hop v7→v9 self-updates verified live**), Adventure horizontal hand. Release pipeline: `scripts/release-playable.sh` → `daily-snapshots` prerelease; post-publish routine parses the published manifest through the shipped jar (standing lesson: verify the production artifact through the production code path). Stock bugs found en route, fixed on `playable`, queued as upstream candidates: nested-folder storage stub, `RestartUtil` Runtime.exec-tokenization (desktop restart never worked), quest BALANCED all-colors empty pool, plus the tap-angle hit-test generalization. **Standing hazards:** the playable build shares Forge's user deck store with the research harness — `launch --pool` gates on installed-deck content hashes (Anvil `89178d7`); never check out `playable` in the research worktree. **Multiplayer hardening (ran hidden alongside, CLOSED 07-30): [multiplayer-hardening.md](docs/design/multiplayer-hardening.md) is the public record** — 9 findings, 4 private advisories, **3 merged to public Forge master byte-identical**, 2 declined-and-documented as trust-model properties (host is not an authoritative referee; seat reclaim by username is intended); chat rate limiting = public PR #11457; both declined patches live on `playable`; method notes (test-must-fail-first, measured allowlist, two deser sinks, size-as-review-currency) recorded there. `origin/playable` @ `2900701083` = QoL + upstream master + security, consolidated; fork repo tidied to exactly `master` + `playable`. Full blow-by-blow: [status archive](docs/status-archive.md).
- **Chronicle SCHEDULED (2026-07-31, [ADR-0029](docs/decisions/ADR-0029-chronicle-scheduling.md) + [chronicle-mvp-plan.md](docs/design/chronicle-mvp-plan.md)): the collection/incremental mode graduates from parked concept to a playable-branch side stream** (sibling to the QoL track; M4 keeps the research hours; long-range goal = upstreaming via the fork-first/re-cut strategy). Scope = MVP slice v0 from [collection-mode-sketch.md](docs/design/collection-mode-sketch.md) (1993–94 Alpha→FEM collector loop; battler/market/foils out). Session pins (user): **MVP income = allowance stipend** (gap found: with the battler out and pack-cracking EV-negative by invariant, currency strictly drained — the LGS was unreachable); day tick fires on **ration collection** (≤1/real day, local + ~4am grace); **seed-integrity invariant** (all daily randomness from (run seed, day index, domain); sealed items commit contents at acquisition — no savescum rerolls); prestige-proof save schema (run-id + meta split) from day one; **hidden behind a pref until the author's 2-week dogfood gate passes**; Android packaging spike parallel-early (dogfood is phone-based; sideload, no Android updater yet). Architecture: headless services `forge-gui/.../gamemodes/chronicle/` (Quest precedent — headlessness enforced by module structure), screens `forge-gui-mobile`, data `forge-gui/res/chronicle/`, tests in the desktop TestNG tree, Adventure `SaveFileData` persistence. Staging D0 paperwork (done) → D1 headless core (gate: simulated-fortnight test) → D2 screens on `mobile-dev` → D3 reveal scene (bar DEFINED same session: period-wrapper identity evolving w/ the timeline — the reveal object is the set's real booster art, foil gloss debuts w/ the 1999 event; tier-2 player-paced mechanics; BOTH registers day one — ceremony + batch w/ auto-pause, because the day-one starter is 60 cards and the LGS sells boxes; harvest+tiny sound) → D4 Android spike (parallel) → D5 dogfood + numbers pass → visibility flip + Discord courtesy float. Stretch: kitchen table (ownership-legality only). **Same-session design rounds also closed: awards first pass** (avatars via scaled crop derivation on the mode's home page + Chronicle-clipping scrapbook trophies; standing principle: cosmetics derive at RUNTIME from card images Forge already downloads, no baked/generated assets; aesthetic north star = purposeful-retro lo-fi, decision deferred) **and famous-card allowlist v0** (path-shaping ONLY — endpoints backout-guaranteed; one entry per STORY, event-anchored legs w/ backout-consistent settle multipliers; ~12-entry seed incl. RL 3-leg + Juzám + Shivan-deflation; seed+calibrate authoring; **MVP buylist amended: + hand-authored 1994-desirability tier table = the allowlist file's first column** — rarity-flat would sell a pulled Lotus like bulk, all-prices.txt tiers are anachronistic and break the pack-EV invariant). **D1 HEADLESS CORE DONE same day (session 4, fork `playable` @ `574abedc08`): done-when gate GREEN** — simulated fortnight (14 scripted days: day-0 starter moment, Alpha shelf turnover day 7 + payday, mid-fortnight save/reload w/ digest-identical continuation, no-savescum reopen) + 27 service unit tests, 30 total in the desktop tree; full desktop suite 400 green. Archaeology dividends: **seed-integrity worked day one** (every RNG site on the 1993-94 booster path routes through the determinism-hooks thread-local `MyRandom` ⇒ committed contents-seed = `setRandom(new Random(seed))` per pack); `SaveFileData` is libGDX-bound ⇒ headless port w/ Quest-style version ladder + Adventure containsKey defensiveness; **no FModel entanglement** (Adventure own-singleton pattern; shared-file footprint = 3 additive `ForgeConstants` lines); notables priced by NAME only (ATQ prints split rarities per name); the 1994 tier table shipped grep-verified (Lotus ×30 = $12 buylist ≈ the period dozen packs) and **the exact sheet-computed pack-EV ledger verifies EV-negative for every product even at max LGS discount**. The gate suite caught a real hole: the tick guard needed *strict monotonicity* on the effective calendar day (equality would let a wound-back clock re-arm the tick — clock-scum, the sibling of savescum; plan pin 5 addendum). Carried: box-as-item (stage 2), `BoosterSlot.replaceSlot()` raw `Math.random()` landmine before any post-2018 window. **D2 SCREENS v1 BUILT same session (`bb7e670cff`, D1 pushed as `574abedc08`): full mobile surface** — pref-gated New Game entry (`CHRONICLE_MODE_ENABLED` default OFF, Settings→General toggle, applies without restart since buildMenu re-runs per open; never persists as preferred screen), home screen (session-script spine w/ collect-ration flow + day-summary), binder shelf (booster-art tiles + completion bars + NEW counts) → spread (full-set collector-order grid; **all four archaeology gaps closed via GENERIC upstreamable ImageView plumbs**: NEW/OWNED column overrides modeled on the live fnPrice pattern — the scouted fnIsFavorite is assigned-but-never-read — driving NEW badge/qty pips/unowned rendering, grayscale on-disk / dimmed placeholder otherwise w/ deliberately no network fetch for unowned; group-caption composer hook for completion; `CHRONICLE_BINDER` config w/ pileBy=null since pile-by renders fanned stacks not grids), LGS screen (MSRP shelf + seeded deals + sold-out), paper screen, tap-through placeholder opener (D3 owns the real reveal). Headless riders desktop-tested: ChroniclePaper (templated issue + 15 seeded era-flavor lines), ChronicleData.setUniverse (collector-order resolver; test pins ATQ split-rarity variants), markSeen batch. Suite 32 green; mobile-dev jar boots clean (must run from the module dir — repo root resolves the wrong ../forge-gui). **D2 gate PASSED 2026-08-01 (author's interactive pass: begin/paper/allowance/opening/binder all work) — D2 DONE.** Feedback round 1 landed same day (`59adfb8707`): **ChronicleAcquisitionLog** (provenance journal: every opening recorded w/ per-card first-pull ordinals, persisted in the run; `sourcesFor` = which products yield a printing — basics are starter-only under Forge's era sheets), binder Sort:New = TRUE acquisition order, deals above the shelf, reveal long-press → match-inspect CardZoom w/ oracle flip (binder spread already had it via ImageView), pack-wide art prefetch at open; suite 34 green. Backlog recorded in the plan: provenance UI, desktop scroll-bar indicator, stronger art preloading (D3), save-slot browser (today = one implicit autosave run, loaded automatically). Fidelity answers recorded in the plan (15-card ABU accurate; no-basics-in-boosters = engine-wide Forge sheet model, documented deviation; random 5-color starters period-accurate; collector order = canonical). **Nav trap found by the user's second pass and FIXED (`003a941818`): opening Chronicle made it the in-memory preferred New Game screen (the guard only sanitized the disk write) and its home screen had no NewGameMenu header — other modes unreachable until restart; Chronicle now excluded from preferred tracking entirely + carries the standard hamburger.** All Chronicle commits pushed. **D3 reveal scene v1 BUILT 2026-08-01 (`8c2134fdbf`, pushed): both registers** — ceremony (drag-to-tear wrapper on real booster art w/ shearing-strip fly-off, tap-to-flip w/ back-ease pop, best-last staging rarity→buylist-value, commons batch-flip on 3+ runs, final-card buildup pulse+shiver, first-pull glint+NEW badge from the acquisition log [opening-seq vs oldest-event; dupes glint once], revealed row, no-peek long-press zoom) and batch (starters/Open All: cascade w/ hard auto-pause on rare+/notable, soft hold on new uncommons, pack-art interstitials, tap pause/resume); D2-backlog art preload folded in (bounded 8s block + one-ahead pre-touch); stock-audio sound pass + haptics; summary spread w/ buylist total + Open another/all; leaving mid-reveal releases the animation driver (continuous-rendering leak caught in review). Pacing constants = one block atop `ChronicleRevealScene` (the dogfood tuning surface). Suite 34 green, mobile-dev jar boots clean. **Done-when stays open by design — experiential bar, author-judged, iterate through D5.** D4 scouted: `forge-gui-android` builds via `android-debug` profile, needs Android SDK build-tools 35 + platform 35 — not on the box; SDK install = user decision, then `mvn -P android-debug` + adb sideload. **First live test round CLOSED same evening (5 commits → `ba94fc4cfd`): dev-mode testing actions shipped** (home-screen New day + cash; timeline `devRewindOneDay` re-arms the tick w/o touching the clock-scum guard — day contents are per-day-index deterministic so seed integrity holds; 35 tests green) **+ six fixes from play**: booster art had NEVER downloaded anywhere (stock `imageKeyFileExists` returns true for all product prefixes ⇒ fetch never fired, + bare `b:CODE` keys mismatch saved filenames — `ChronicleHub.boosterArtKey` list-resolves; binder tiles were silently placeholders through all of D2), reveal-NEW cross-checked against collection counts (log-blind pre-log openings glinted re-pulls), cascade pacing slowed + soft-holds for all first pulls, pulsing tap-to-continue pill (the auto-pause was invisible), revealed-row long-press hit-test, EDT hop for fetches (fetchImage asserts EDT; bulk-open crashed from the overlay thread). Starter deck-box art: fetcher `o:` support added via generalized `fetchProductImage` (upstream candidate) but `downloads.cardforge.org` has NO DNS record — starters draw booster-art fallback, self-upgrade if rehosted. **CI flake diagnosed + fixed (`ba94fc4cfd`, upstream candidate): FCollectionTest.testCompletableFuture asserts pre-`b858b48fde` synchronized semantics — unsynchronized concurrent removes race (stress-proven: fail at 30/3000 stock, 3000/3000 clean synchronized); test mutation now synchronized; caveat recorded: live code doing concurrent FCollection removes shares the race.** Upstream-candidate queue: fetcher `o:`, `imageKeyFileExists` fallthrough, FCollection test fix, dead cardforge.org list entries, ItemManager context-menu embedded-screen offset (`10c22003df`: menu anchored on global screenPos but positioned in container-local space — off by the nav-sidebar width in mobile-dev's desktop shell, identity on phones; found via the Chronicle provenance menu, diagnosed by instrumented Xvfb-less xdotool repro on an isolated `-Duser.home` profile). NEXT: D3 iteration (200th-pack bar, Pocket side-by-side) + D4 SDK decision.
- **M3 CLOSED (2026-07-28, [ADR-0026](docs/decisions/ADR-0026-m3-closeout.md)): all four done-when clauses resolved.** RL ckpt of record `d6-run7b/iter-014/train/last.pt` at **0.5121 ± 0.0110 corrected on the post-rebase engine** (+6.69pp over BC t=4.32; parity vs the current heuristic — ADR-0019/0023's teacher-surpassing reads were old-engine); BC `d5-combat` = the engine-bump certification policy; **all pre-rebase numbers are old-scale**. Arc: D1 veto cause = commander-tax featurization gap + §6c rejected-intent penalty, one deliverable ([ADR-0022](docs/decisions/ADR-0022-d1-closeout.md)); D2 ceiling hunt — §6d mixed opponents = a stability lever, τ falsified, §6f full-vis critic in the loop delivered the first outside-noise supersession ([ADR-0023](docs/decisions/ADR-0023-d2-closeout.md)), batch lever falsified with the residual diagnosed as **absent signal in near-tie windows** ([ADR-0024](docs/decisions/ADR-0024-run8-batch-lever.md)) — the argument that seeded Grindstone; early-doom analysis: ceiling NOT binding (luck-locked 18–39%, `scripts/early_doom.py`); D3 upstream surface shipped (draft PR [forge#11285](https://github.com/Card-Forge/forge/pull/11285) determinism hooks — still awaiting review, #11260 design input, #445 protocol review delivered); D4 = the #11161 rebase as dataset-boundary event ([ADR-0025](docs/decisions/ADR-0025-d4-rebase-closeout.md)): fork engine delta now **14 lines in two files**, twin divergence 16.5%→7.0%, connive regression (upstream #11355) fixed fork-side + test-pinned, re-baseline verdict = the heuristic got stronger. Standing tools born here: `scripts/final_read.py` (the 2,000-game corrected closing protocol), collate-in-worker learner (4.7×, train ~2.5h), `bench_generation.py` (w=8 saturates ~189 rps), VRAM elasticity, `notify.py`. **Still open from M3: pool-manifest mtime-selection hazard** (`pairs.py:22` mtime-sorted; a stale pick stamps wrong provenance in `final_read`). Full history: [status archive](docs/status-archive.md).
- **M2 CLOSED (2026-07-17, [ADR-0020](docs/decisions/ADR-0020-m2-closeout.md)): all four done-when clauses resolved — the V-trace self-play loop built end-to-end and its first RL ckpt of record superseded BC.** Arc: D1 fork rollout contract certified (statics 12%→0, divergence 50%→2.5% under the BC policy, twin determinism 40/40; [ADR-0011](docs/decisions/ADR-0011-d1-closeout.md); fidelity fixes merged upstream as [forge#11203](https://github.com/Card-Forge/forge/pull/11203)); D2/D3 SA-level action schema at zero agreement cost, order rung retired ([ADR-0012](docs/decisions/ADR-0012-d2-d3-closeout.md) + the **pre-RL scope rule**: BC is finished as a strength program; sa_vocab pinned — **never regenerate in place**); [ADR-0013](docs/decisions/ADR-0013-winner-label-poisoning.md) winner-label poisoning found and fixed reader-side; Ante AIVAT ledger certified ([ADR-0014](docs/decisions/ADR-0014-ante-certification.md)); asymmetric full-vis critic `d4-critic-fullvis` = eval/Ante critic of record ([ADR-0015](docs/decisions/ADR-0015-d4-closeout.md); rollout-label machinery built and parked); D5 combat constructs, combat intent reaches the engine unfiltered ([ADR-0016](docs/decisions/ADR-0016-d5-closeout.md)); D6 loop (GPU micro-batching, seeded Gumbel-max sampling batch-composition-invariant, server-side μ + recompute tripwire, synchronous iterations, re-ask-on-veto §6b): run-1 healthy, run-2 entropy-bonus collapse → **v0 guards + hinge entropy floor** ([ADR-0017](docs/decisions/ADR-0017-run2-entropy-collapse.md)), run-3 first live guard halt + `d6-run3/iter-012` at 0.5213 corrected ([ADR-0019](docs/decisions/ADR-0019-d6-donewhen4-resolution.md), old scale). Headless-worker OOM wedge class fixed (fork uncaught handler + `-XX:+ExitOnOutOfMemoryError`). Full history: [status archive](docs/status-archive.md).
- **M1 CLOSED (2026-07-10, [ADR-0009](docs/decisions/ADR-0009-m1-closeout.md)): the BC agent plays real games at 46.8% ± 2.5pp vs the heuristic it imitates** (D7 gate TRUE at held-out honest agreement **0.9758**, [ADR-0008](docs/decisions/ADR-0008-d7-gate-resolution.md); 3-epoch recipe). Built across D1–D8: observation schema v1 + zstd trajectory store ([observation-schema-v1.md](docs/design/observation-schema-v1.md)), CastPlan label extractor + executor (M0's 65% veto → 3.1–7.8%; legality-only realizer + host-level disambiguation ladder), 113,592-game corpus (labels zero-error; [ADR-0005](docs/decisions/ADR-0005-d3-label-mask-semantics.md) timing-legal candidate masks; [ADR-0006](docs/decisions/ADR-0006-500k-default-on-stopping-rule.md) stopping rule fired at the first checkpoint), encoder/trunk/heads (frozen Qwen3 text embeddings pinned by [ADR-0007](docs/decisions/ADR-0007-embedding-pin.md); pass-weight knee 0.1; value-head conditioning diagnosed and fixed), loader-parity-tested serve path, inference-in-the-loop operationally free at w=8. **Standing rules born here:** never read a bridge answer field without checking `fallback` first; eval runs keep census+obs on; zero-error validation is the corpus-launch gate. Full history: [status archive](docs/status-archive.md).
- **M0 CLOSED (2026-07-04, [ADR-0003](docs/decisions/ADR-0003-m0-closeout.md)) + foundations (2026-07-02→04).** Fork lives at `../forge` (blobless clone; `origin` Tyrathalis/forge, `upstream` Card-Forge/forge; build `~/.local/opt/maven/bin/mvn -P windows-linux`, Java release 17 on system JDK 26). Landed: bridge protocol v0 (six game-agnostic answer shapes, [bridge-protocol-v0.md](docs/design/bridge-protocol-v0.md)), `PlayerControllerAnvil` injection ([playercontroller-override-plan.md](docs/design/playercontroller-override-plan.md), 109-method decision surface), gRPC bridge, batch orchestrator (`anvil.bridge.harness launch|pause|resume|status|replay|summarize`; `run.json` = per-run pinning; chunk mechanism = recycle/resume/crash isolation). Measured: **bridge tax 2.6% at w=16 (~1,700+ g/h)**, 314/320 bit-identical across transport; forward play leak-free; fork fidelity characterized broken ([ADR-0002](docs/decisions/ADR-0002-fork-api-gate-resolution.md) → hardened in M2 D1; `forge forkcheck` = the standing engine-bump gate). Callback census on the pool: 64/109 methods fire, priority ~51% of traffic ([callback-census-results.md](docs/design/callback-census-results.md)). DC pool pipeline (`anvil.pool fetch|banlist|build|install|status`, [dc-pool-pipeline.md](docs/design/dc-pool-pipeline.md)): **current pool `cf2ca6ba` — 1,701 cards from 113/120 decks**; upstream-gap worklist empty. Prior-work survey + responses ([ADR-0001](docs/decisions/ADR-0001-prior-work-responses.md)). Full history: [status archive](docs/status-archive.md).

Update this section as milestones land. When a milestone or track CLOSES, move its full bullet(s) verbatim to [docs/status-archive.md](docs/status-archive.md) and leave a compact summary here (headline numbers, ADR links, standing rules/hazards born there, archive pointer).

## Design invariants (re-read these every session)

- The engine adjudicates every claim any learned component makes.
- Every LLM judgment is downstream-verified (LLMs filter and narrate; they never generate training truth).
- Every drill is provenance-traced to a real game.
- The value function is continuously audited against rollouts.
- Detection is the engine's job; response is the model's.
- The error-accounting queue is the spine everything reports to.
- The model never sees the engine version — formats are rules to play to; versions are bugs not to learn.

## Hard conventions

- **Game-agnostic schema:** keep Magic-specific assumptions (zone lists, feature names) out of the Python-side schema (design §1, cross-game hygiene). Costs a naming convention now, preserves multi-game optionality forever.
- **Seed everything; deterministic replay.** State forking with seed control is load-bearing for four systems.
- **Provenance on all trajectories:** source, engine hash, checkpoint, drill-template ID.
- **Fork discipline:** pinned Forge versions per run; engine upgrades are dataset boundary events. **Exemption ([ADR-0025](docs/decisions/ADR-0025-d4-rebase-closeout.md)):** a change that is behavior-identical on the game path is not a boundary — but the proof is *empirical, not argued* (same seed set on both jars → identical `forkcheck` trace hashes; any mismatch ⇒ boundary). A crash fix never qualifies; swapping our fork-local fix for an upstream fix of the same defect does. Upstream PRs stay small, tested, human-reviewed.
- **Long-running jobs launch at low priority** (`nice -n 19`) so the desktop preempts them; harnesses are designed for graceful stop + seeded game-granularity resume (design §9, "disposable workers"). Calibrated measurements are the exception: schedule them on a quiet box instead.
- Python ~60% / Java ~40% overall; Java work is archaeology in the Forge fork, not here.

## Repo layout

- `anvil/` — Python package; subpackages map to design-doc sections (`encoder` §1, `state` §2, `policy` §3, `heads` §4, `tutor` §5, `training`+`grindstone` §6, `ante` §7, `bridge`+`store` §9, `pool` §8, `evals` §7, `mentor` §11). Skeleton only until M1; M0 work concentrated in `bridge/`.
- `docs/design/` — canonical design docs.
- `docs/devlog/` — one dated file per working session (see workflow below).
- `docs/decisions/` — ADRs for anything that changes or resolves a design-doc question.
- `tests/` — pytest.

## Working session workflow

- **End of every session:** write `docs/devlog/YYYY-MM-DD.md` (copy `docs/devlog/TEMPLATE.md`): what was done, what broke, what the next session picks up. Claude writes this as part of wrapping up.
- **When a design question is resolved or a design-doc claim changes** (e.g., a probability from §15 gets measured, a bet flips): write an ADR in `docs/decisions/` (copy the template, next sequence number) and update the Status section above.
- **Keep `docs/project-map.html` current alongside the devlog/Status:** whenever a milestone opens/closes, a deliverable lands, or a headline number changes, update the map in the same commit batch. It's the living overview — a stale map is worse than none. Since the 2026-08-01 dashboard restructure, the parts that go stale FASTEST are at the top: the **Now panel** (current focus per track), the **state-of-record table** (ckpts/baseline/pool/fork pin/release), the **watch list**, the **run ledger** (one row per self-play run, verdict-colored — every closed run gets a row), and the footer stamp. On milestone/deliverable close, also sweep the embedded labels: flow-diagram box text, pill states, open-PR statuses, ADR index rows. Milestone entries are `<details>` (closed milestones stay default-collapsed with a one-line summary; the open milestone stays `open` with a done-when checklist + per-deliverable bullets — never a single prose wall).
- Commit at natural checkpoints with plain descriptive messages; the devlog carries the narrative, not the commit log.
