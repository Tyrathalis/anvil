# Anvil

A neural agent for Magic: The Gathering built on the Forge rules engine. Non-commercial, GPL-aligned, designed to be contributed back to Forge. Solo nights-and-weekends project.

**Canonical design doc: [docs/design/anvil-design-v2.md](docs/design/anvil-design-v2.md).** Read it before substantive work. Design changes go into that doc or into an ADR in `docs/decisions/` — never live only in chat history.

## Module naming (Magic vocabulary under the smithy umbrella)

- **Anvil** — the project and the pilot agent (plays the game)
- **Tutor** — the deckbuilder (searches the pool for what the deck needs)
- **Mentor** — the coaching product
- **Grindstone** — the drill economy (grinds scenarios, sharpens the model)
- **Ante** — the luck ledger (accounts what chance took and gave)

Initial scope: 1v1 Commander (40-life Commander ruleset), curated ~1–2K card pool derived from Duel Commander meta decklists + flex slots (ADR-0018: DC is the pool's provenance, not the ruleset; content breadth scales in set-sized dataset-boundary chunks after core features).

## Status

- **M8 OPEN (2026-08-17, [m8-plan](docs/design/m8-plan.md)): one lever to a promotion attempt — natural-timing credit, probe-gated, with a pre-registered pivot.** **D1 RESOLVED same-day ([ADR-0060](docs/decisions/ADR-0060-d1-natural-timing-probe.md)): gate FAILED, decisively — the pivot is taken.** The probe (single NATURAL arm under the new OBSERVE directive, K=64 on `drill-selection-v5-active`, gate PINNED at design before generation: two-bin abundance ≥30% at minority ≥12.5% · RMS true Δwr ≥0.10 · headroom ≥1pp at play-weight 0.0495) ran pristine — 99/99 points, 6,335/6,336 completions — and measured **split abundance 3.0% vs the 30% pin** (71.8% of completions cast in-window; modal first casts often identical across all 64 samples; headroom clause also fails at 0.55pp). Within-natural timing variation does not exist at plan granularity: ADR-0051's natural-variation falsification extends from single decisions to plans; a natural-timing target has nothing to grade. **M8 proceeds on D2′: curriculum × rank-critic curation** (the only lever family that ever promoted, +1.98pp ADR-0031, one-shot per method ADR-0035 — critic-ordered is a never-run method; **entry gate next: rollout audit of `rank-critic-c2v3` ordering, threshold pinned at its own design session** per ADR-0036) → one run vs **0.5373 ± 0.0112**. En route ([ADR-0059](docs/decisions/ADR-0059-instrument-corrections.md)): the boundary-exemption proof for the labels extension passed at the instrument's own noise floor, and two instrument corrections landed — run_forkcheck.sh had run ALL THREE ADR-0055 fidelity checks on the stale pre-rebase jar (annex determinism claim retracted; true era fidelity 11.2–11.4% + a ~0.5% wall-clock-nondeterministic class), and AnvilRun.jstr never escaped control chars (modal-spell newlines split labels rows; M7 campaign labels silently dropped rows — verdict stands, dose smaller than designed; drops are loud now). Probe instrument = standing asset (the cheap "does the policy vary here at all" screen). **Pre-commitment: a timing-branch TIE closes the credit-assignment family at this scale.** Rebase assessed and declined at open (6-day delta, zero pool intersection, no engine-path relevance); **next-boundary bundle** on the map watch list: rebase + multi-format model-side enablement (pool scaffold #8 landed at ADR-0055) + 2-arm campaign trim + copy-state divergence forensics. Carried-items audit: the M4 fork items ALL resolved at the ADR-0055 boundary (MayPlay `b361dfcb8f`, IndexOOB `9f0a2c0886`, MinMaxBlocker 07-25, targeting-retry closed-as-bounded) — nothing carries. M7 stale-data pass: training dirs done earlier; the runs-store deletion (~4.5G, kill list in devlog 2026-08-17) pending user execution.
- **M7 CLOSED (2026-08-16, [ADR-0058](docs/decisions/ADR-0058-m7-closeout.md)): the credit-assignment question answered in a split verdict — dense per-decision signal is TRAINABLE and BEHAVIOR-MOVING; the act−hold formulation is STRENGTH-NEUTRAL.** The C bundle (ADR-0054) ran three times: d6-run14 halted iter 2 on the unbounded-L_seq runaway ([ADR-0056](docs/decisions/ADR-0056-run14-seq-divergence.md): hinge at ±6 + seq_share telemetry/guard + kl abort born); d6-run15 stopped at iter 3 on share drift via the within-epoch PG-mass fade — measured a standing loop property ([ADR-0057](docs/decisions/ADR-0057-run15-share-drift.md): per-iteration w_seq recalibration born); **d6-run16 = the clean experiment: 16 accepted iterations at honest pricing, the term visibly taught its target (|l_seq| 0.43→0.68 on fresh labels) and generalized — into the greedy-aggression trade** (lost-bin +6.9pp saturating vs winnable −5.5/coin −4.9 decay, hold-then-cast 0.233→0.182 AWAY from heur 0.345, veto compounding to the iter-16 guard halt) — exactly what ADR-0053's natural > greedy ≫ hold ordering predicted for an act-push target. **Gate sweep (two-candidate, arms-selected, disclosed): iter-009 0.5364 ± 0.0110, iter-014 0.5340 ± 0.0110 vs 0.5373 ± 0.0112 — both TIE; no promotion; ckpt of record stays `d6-run11/iter-019`.** Standing rules born: clips at birth applies to loss terms; auto-calibrated weights get their invariant instrumented + guarded + recalibrated at the cadence it varies; **fixed-subset arms reads are ONE observation, not N** (the mid-run 0.575/0.570 arms elevation was one ~1.5σ fluctuation on a shared seed subset counted twice). Machinery carried: seqlabels join, hinged seq_pass, share guard, forced-seq campaign, battery seq curves, screened selection + evalset v4. run14/15/16 stores veto-elevated — never in a mixture. **Chartered follow-up (new design round, NOT an M7 extension): the natural-timing-target formulation (price the natural arm's timing, ADR-0053's measured optimum); act−hold dose escalation explicitly not recommended.** Full verbose history: [status archive](docs/status-archive.md).
- **M6 CLOSED (2026-08-10, [ADR-0050](docs/decisions/ADR-0050-m6-closeout.md)): the representation question answered — partially gradient-reachable, NOT the binding constraint; the real bottleneck is learning-signal density ([ADR-0049](docs/decisions/ADR-0049-flat-cycle-audit.md)).** The elimination chain: not arithmetic (0043), not card function (0045), not hidden info (0047), not label count at the measured slope (0046); partial unfreeze IS reachable (0044/0046: 0.4528→0.4829, the only lever past the frozen ceiling) — yet the graduated build + the best curation ever measured produced a TIE at the strength gate (0048: 0.5199 ± 0.0110 vs 0.5316, no promotion; **ckpt of record stays `d6-run11/iter-019` at 0.5316 ± 0.0110**). The flat-cycle audit named the layer in a day (0049): §6c = the loop’s only dense per-decision signal (15–30% of win magnitude per trajectory, continuously), twenty iterations trained measurable cast-suppression (16.9% of iter-019’s casts changed, 46% cast→pass), hold-then-cast abundant in exploration and flat across training. Assets carried: `rank-critic-c2v3` (holdout 0.4833) + era-scoped isotonic map; `labelset-c2-v3` (8,683 train / 1,197 frozen holdout); the frozen `frozen-probe-ext2-c2` benchmark + holdout-freeze protocol. Standing rules born in M6: probe on `[STATE]`, never the trained head; inner-val pool pinning; mid-campaign checkpoints sweep the two leading Ns; engineered aggregates get clips at birth; drill-mode labeling is the cheap population (27.3 pos/h/worker); audit the signal path before the next lever when a gate goes flat with improving instruments. Full history: [status archive](docs/status-archive.md).
- **M5 CLOSED (2026-08-05, [ADR-0037](docs/decisions/ADR-0037-m5-closeout.md)): the compounding question answered — ONE-SHOT.** Δ2 = −0.58pp ± 0.73 (3,995 paired games) vs Δ1 = +1.98pp ± 0.71 ([ADR-0035](docs/decisions/ADR-0035-d2-compounding-read-resolution.md)); the pre-registered migration read predicted it from curation-stage data before the run launched (m5-plan D1 section; promoted to standing tool — run before pricing any cycle). No promotion: ckpt of record stays `d6-run11/iter-019`, baseline **0.5316 ± 0.0110**. **Honest headline: the drill loop is a one-shot corrector per curation method, and the measured reason is representation** — D3 ([ADR-0036](docs/decisions/ADR-0036-d3-critic-calibration.md)) split verdict on 3,750 banked K=8 labels: isotonic remap fixes absolute calibration (ECE 0.33→0.03, ADOPTED era-scoped for future curation/doom labels) but ranking is blind (Spearman 0.27 vs 0.94 achievable; post-remap winnable −56pp, global) — no calibration fixes it. **Standing rules/tools born here:** migration read gatekeeps cycle pricing (`scripts/migration_read.py`); ranking-from-rollouts (critic ordering is not evidence in loss-adjacent populations); value-audit invariant mechanized (`scripts/critic_calibration.py`, label set grows with every map/sweep); isotonic maps are era-scoped assets; critic-classified fractions are instrument-limited (rollout maps authoritative); single-seed-set reads at ~1pp are inconclusive (run12's halves disagreed 3.5pp — combined paired read is the standard). **M6 scoping inputs on file (ADR-0037):** deciding probe = can a fresh head rank from frozen trunk features on the 3,750 labels (yes ⇒ rollout-label targets/ADR-0015 path; no ⇒ encoder work); curriculum composition = proven cheap lever; Ante riders + isotonic wiring ride with the critic work. Full history: [status archive](docs/status-archive.md).
- **M4 CLOSED (2026-08-03, [ADR-0033](docs/decisions/ADR-0033-m4-closeout.md)): Grindstone online — all four done-when clauses resolved, first outside-noise drill-signal win.** Arc: D1 adaptation probe `d6-run9` = directionally-positive tie ⇒ the heuristic got stronger, baseline stayed 0.5121, closing stores seeded curation ([ADR-0028](docs/decisions/ADR-0028-d1-adaptation-probe.md)); D2 Grindstone v0 in one day — 584-loss curation, drill machinery (7/8 exact replay), the 584-drill ground-truth map (rollout 23.7% vs critic 0.584 = the calibration gap in one number; monarchy-transfer crash fixed, MayPlay diagnosed), evalset/eval with the **D2.4 pairing protocol** (evals pair vs the pinned policy's own re-measurement, never selection-time labels); D3 through a falsified intermediate — (a) all-behind mixture strength-neutral with the mechanism visible in held-out decomposition ([ADR-0030](docs/decisions/ADR-0030-d3-experiment-a-resolution.md)) → (a2) rebalanced at 18.8% ahead-positions = **combined paired +1.98pp ± 0.71 (t=2.77, 3,994 games), replicated on fresh seeds ([ADR-0031](docs/decisions/ADR-0031-a2-resolution.md)); `d6-run11/iter-019` promoted, M4 closed at baseline 0.5316 ± 0.0110** (winnable −5.1pp residual = the open edge of the curriculum-composition lever); D4 = serve path documented-negative (0.05 ms Python-active/request — the ceiling is waiting) + the real lever generation shape: **w=16 + per-batch ≥2-round chunk clamp, ~+30% in-loop ([ADR-0032](docs/decisions/ADR-0032-d4-serving-path-resolution.md))**. **Standing rules born here:** drill mainlines never enter training ingest; curation regenerates from the current ckpt-of-record's own losses every cycle (selection/evalset versions are ckpt-era-scoped); cross-era gen_s comparisons only at identical chunking; fresh-seed paired confirmation = the marginal-t tiebreaker; curriculum composition (behind/ahead balance) is a first-class lever. **Standing infra born here:** `anvil_watchd` + self-registration (+ `ANVIL_NOTIFY_SILENT` seam), mid-run drill-eval phase, fork-session ingest (`-forkobs` + sampled/μ parity), the w=16 recipe, pool `CURRENT` pin (M3 mtime hazard retired at close). Levers on file for M5: critic calibration (~1,900 K-rollout labels), winnable residual, escalation (b) opt-in. Carried fork items: all resolved by the ADR-0055 D3 boundary stability pass (2026-08-11). Full history: [status archive](docs/status-archive.md).
- **Playable-fork track (2026-07-26→08-02; current release v14 `881207ed27`): founding worklist items 1–8 ALL BUILT, PUBLISHED, user-verified in play.** [playable-fork-worklist.md](docs/design/playable-fork-worklist.md) is the doc of record; a human-facing distribution build on fork branch `playable` (worktree `../forge-play`; the pinned research fork `master` has zero delta from any of this). Shipped: resizable window + live viewport (six frozen-size layers found and fixed), deck-site URL import on mobile + bulk Archidekt sync (never-move re-sync, annotate-not-quarantine; Moxfield deferred — relationship problem, not engineering), configurable tap angle `UI_TAP_ANGLE` 90–15° both clients (shared `RotatedRect` inverse-rotation hit-tests; caught + fixed the stock nested-transform bug en route), slot packing + single-column zones, in-place delta auto-updater repointed at the fork (hash-verified; self-updates live-verified incl. two-hop v7→v9), Adventure horizontal hand; v13 = updater orphan-deletion pass; v14 = net-lobby fixes (upstream #11190 made `FTextField` fire CHANGE per keystroke, breaking chat/lobby/carets — now opt-in `setLiveChangeEvents`; stock team-selection echo cascade guarded — both queued upstream). Release pipeline: `scripts/release-playable.sh` → `daily-snapshots` prerelease; **publish MUST pin `-R Tyrathalis/forge`** (gh default repo is upstream, same tag name); post-publish routine parses the published manifest through the shipped jar (standing lesson: verify the production artifact through the production code path). Friend-install pins: Temurin 21 JRE via adoptium `installer/latest` permalinks; macOS 15+ Gatekeeper flow baked into `Forge.command`. Stock-bug upstream candidates queued: nested-folder storage stub, `RestartUtil` Runtime.exec-tokenization (desktop restart never worked), quest BALANCED all-colors empty pool, tap-angle hit-test generalization. **Standing hazards:** the playable build shares Forge's user deck store with the research harness — `launch --pool` gates on installed-deck content hashes (Anvil `89178d7`); **never check out `playable` in the research worktree**. **Multiplayer hardening (ran hidden alongside, CLOSED 07-30, [multiplayer-hardening.md](docs/design/multiplayer-hardening.md) is the public record):** 9 findings, 4 private advisories, 3 merged to public Forge master byte-identical, 2 declined-and-documented as trust-model properties; chat rate limiting = public PR #11457; both declined patches live on `playable`. Residue: T2 live-rescale only (workaround: launch at play size). Full blow-by-blow: [status archive](docs/status-archive.md).
- **Chronicle side stream ACTIVE (scheduled 2026-07-31, [ADR-0029](docs/decisions/ADR-0029-chronicle-scheduling.md) + [chronicle-mvp-plan.md](docs/design/chronicle-mvp-plan.md)): the 1993–94 Alpha→FEM collector-loop MVP as a playable-branch side stream, hidden behind `CHRONICLE_MODE_ENABLED` (default OFF) until the author's 2-week dogfood gate passes.** Key invariants: seed-integrity (all daily randomness from (run seed, day index, domain); sealed items commit contents at acquisition — no savescum rerolls; the day tick is strictly monotonic on the effective calendar day — no clock-scum), pack-EV verified negative for every product, prestige-proof save schema, cosmetics derive at runtime from images Forge already downloads. Progress: **D1 headless core DONE** (simulated-fortnight gate green, services in `forge-gui/.../gamemodes/chronicle/`); **D2 mobile screens DONE** (author-verified: home/binder/spread/LGS/paper; acquisition-log provenance journal; nav-trap fixed — Chronicle excluded from preferred-screen tracking); **D3 reveal scene v1 BUILT** (ceremony + batch registers, pacing constants = the dogfood tuning surface; experiential bar stays open by design, iterate through D5); first live round shipped dev-mode testing actions (New day / cash / `devRewindOneDay`) + six fixes from play (headline: booster art had NEVER downloaded — stock `imageKeyFileExists` returns true for all product prefixes so the fetch never fired). Upstream-candidate queue: fetcher `o:` product-image support, `imageKeyFileExists` fallthrough, FCollection concurrent-remove test race fix (stress-proven), dead cardforge.org list entries, ItemManager context-menu embedded-screen offset. **NEXT: D3 iteration (200th-pack bar, Pocket side-by-side) + D4 Android SDK decision** (`android-debug` profile needs SDK build-tools 35 + platform 35, not on the box — user decision). Full running record: [status archive](docs/status-archive.md).
- **M3 CLOSED (2026-07-28, [ADR-0026](docs/decisions/ADR-0026-m3-closeout.md)): all four done-when clauses resolved.** RL ckpt of record `d6-run7b/iter-014/train/last.pt` at **0.5121 ± 0.0110 corrected on the post-rebase engine** (+6.69pp over BC t=4.32; parity vs the current heuristic — ADR-0019/0023's teacher-surpassing reads were old-engine); BC `d5-combat` = the engine-bump certification policy; **all pre-rebase numbers are old-scale**. Arc: D1 veto cause = commander-tax featurization gap + §6c rejected-intent penalty, one deliverable ([ADR-0022](docs/decisions/ADR-0022-d1-closeout.md)); D2 ceiling hunt — §6d mixed opponents = a stability lever, τ falsified, §6f full-vis critic in the loop delivered the first outside-noise supersession ([ADR-0023](docs/decisions/ADR-0023-d2-closeout.md)), batch lever falsified with the residual diagnosed as **absent signal in near-tie windows** ([ADR-0024](docs/decisions/ADR-0024-run8-batch-lever.md)) — the argument that seeded Grindstone; early-doom analysis: ceiling NOT binding (luck-locked 18–39%, `scripts/early_doom.py`); D3 upstream surface shipped (draft PR [forge#11285](https://github.com/Card-Forge/forge/pull/11285) determinism hooks — still awaiting review, #11260 design input, #445 protocol review delivered); D4 = the #11161 rebase as dataset-boundary event ([ADR-0025](docs/decisions/ADR-0025-d4-rebase-closeout.md)): fork engine delta now **14 lines in two files**, twin divergence 16.5%→7.0%, connive regression (upstream #11355) fixed fork-side + test-pinned, re-baseline verdict = the heuristic got stronger. Standing tools born here: `scripts/final_read.py` (the 2,000-game corrected closing protocol), collate-in-worker learner (4.7×, train ~2.5h), `bench_generation.py` (w=8 saturates ~189 rps), VRAM elasticity, `notify.py`. The M3 pool-manifest mtime-selection hazard was retired at M4 close (pool `CURRENT` pin). Full history: [status archive](docs/status-archive.md).
- **M2 CLOSED (2026-07-17, [ADR-0020](docs/decisions/ADR-0020-m2-closeout.md)): all four done-when clauses resolved — the V-trace self-play loop built end-to-end and its first RL ckpt of record superseded BC.** Arc: D1 fork rollout contract certified (statics 12%→0, divergence 50%→2.5% under the BC policy, twin determinism 40/40; [ADR-0011](docs/decisions/ADR-0011-d1-closeout.md); fidelity fixes merged upstream as [forge#11203](https://github.com/Card-Forge/forge/pull/11203)); D2/D3 SA-level action schema at zero agreement cost, order rung retired ([ADR-0012](docs/decisions/ADR-0012-d2-d3-closeout.md) + the **pre-RL scope rule**: BC is finished as a strength program; sa_vocab pinned — **never regenerate in place**); [ADR-0013](docs/decisions/ADR-0013-winner-label-poisoning.md) winner-label poisoning found and fixed reader-side; Ante AIVAT ledger certified ([ADR-0014](docs/decisions/ADR-0014-ante-certification.md)); asymmetric full-vis critic `d4-critic-fullvis` = eval/Ante critic of record ([ADR-0015](docs/decisions/ADR-0015-d4-closeout.md); rollout-label machinery built and parked); D5 combat constructs, combat intent reaches the engine unfiltered ([ADR-0016](docs/decisions/ADR-0016-d5-closeout.md)); D6 loop (GPU micro-batching, seeded Gumbel-max sampling batch-composition-invariant, server-side μ + recompute tripwire, synchronous iterations, re-ask-on-veto §6b): run-1 healthy, run-2 entropy-bonus collapse → **v0 guards + hinge entropy floor** ([ADR-0017](docs/decisions/ADR-0017-run2-entropy-collapse.md)), run-3 first live guard halt + `d6-run3/iter-012` at 0.5213 corrected ([ADR-0019](docs/decisions/ADR-0019-d6-donewhen4-resolution.md), old scale). Headless-worker OOM wedge class fixed (fork uncaught handler + `-XX:+ExitOnOutOfMemoryError`). Full history: [status archive](docs/status-archive.md).
- **M1 CLOSED (2026-07-10, [ADR-0009](docs/decisions/ADR-0009-m1-closeout.md)): the BC agent plays real games at 46.8% ± 2.5pp vs the heuristic it imitates** (D7 gate TRUE at held-out honest agreement **0.9758**, [ADR-0008](docs/decisions/ADR-0008-d7-gate-resolution.md); 3-epoch recipe). Built across D1–D8: observation schema v1 + zstd trajectory store ([observation-schema-v1.md](docs/design/observation-schema-v1.md)), CastPlan label extractor + executor (M0's 65% veto → 3.1–7.8%; legality-only realizer + host-level disambiguation ladder), 113,592-game corpus (labels zero-error; [ADR-0005](docs/decisions/ADR-0005-d3-label-mask-semantics.md) timing-legal candidate masks; [ADR-0006](docs/decisions/ADR-0006-500k-default-on-stopping-rule.md) stopping rule fired at the first checkpoint), encoder/trunk/heads (frozen Qwen3 text embeddings pinned by [ADR-0007](docs/decisions/ADR-0007-embedding-pin.md); pass-weight knee 0.1; value-head conditioning diagnosed and fixed), loader-parity-tested serve path, inference-in-the-loop operationally free at w=8. **Standing rules born here:** never read a bridge answer field without checking `fallback` first; eval runs keep census+obs on; zero-error validation is the corpus-launch gate. Full history: [status archive](docs/status-archive.md).
- **M0 CLOSED (2026-07-04, [ADR-0003](docs/decisions/ADR-0003-m0-closeout.md)) + foundations (2026-07-02→04).** Fork lives at `../forge` (blobless clone; `origin` Tyrathalis/forge, `upstream` Card-Forge/forge; build `~/.local/opt/maven/bin/mvn -P windows-linux`, Java release 17 on system JDK 26). Landed: bridge protocol v0 (six game-agnostic answer shapes, [bridge-protocol-v0.md](docs/design/bridge-protocol-v0.md)), `PlayerControllerAnvil` injection ([playercontroller-override-plan.md](docs/design/playercontroller-override-plan.md), 109-method decision surface), gRPC bridge, batch orchestrator (`anvil.bridge.harness launch|pause|resume|status|replay|summarize`; `run.json` = per-run pinning; chunk mechanism = recycle/resume/crash isolation). Measured: **bridge tax 2.6% at w=16 (~1,700+ g/h)**, 314/320 bit-identical across transport; forward play leak-free; fork fidelity characterized broken ([ADR-0002](docs/decisions/ADR-0002-fork-api-gate-resolution.md) → hardened in M2 D1; `forge forkcheck` = the standing engine-bump gate). Callback census on the pool: 64/109 methods fire, priority ~51% of traffic ([callback-census-results.md](docs/design/callback-census-results.md)). DC pool pipeline (`anvil.pool fetch|banlist|build|install|status`, [dc-pool-pipeline.md](docs/design/dc-pool-pipeline.md)): **current pool `cf2ca6ba` — 1,701 cards from 113/120 decks**; upstream-gap worklist empty. Prior-work survey + responses ([ADR-0001](docs/decisions/ADR-0001-prior-work-responses.md)). Full history: [status archive](docs/status-archive.md).

Update this section as milestones land. When a milestone or track CLOSES, move its full bullet(s) verbatim to [docs/status-archive.md](docs/status-archive.md) and leave a compact summary here (headline numbers, ADR links, standing rules/hazards born there, archive pointer). The same move applies mid-flight: when an OPEN bullet grows past ~2–3 KB, snapshot it verbatim to the archive and compress here — ADRs/devlogs carry the detail; this section carries the state.

## Design invariants (re-read these every session)

- The engine adjudicates every claim any learned component makes.
- Every LLM judgment is downstream-verified (LLMs filter and narrate; they never generate training truth).
- Every drill is provenance-traced to a real game.
- The value function is continuously audited against rollouts.
- Detection is the engine's job; response is the model's.
- The error-accounting queue is the spine everything reports to.
- The model never sees the engine version — formats are rules to play to; versions are bugs not to learn.

## Hard conventions

- **Game-agnostic schema:** keep Magic-specific assumptions (zone lists, feature names) out of the Python-side schema (design §1, cross-game hygiene). Costs a naming convention now, preserves multi-game optionality forever.
- **Seed everything; deterministic replay.** State forking with seed control is load-bearing for four systems.
- **Provenance on all trajectories:** source, engine hash, checkpoint, drill-template ID.
- **Fork discipline:** pinned Forge versions per run; engine upgrades are dataset boundary events. **Exemption ([ADR-0025](docs/decisions/ADR-0025-d4-rebase-closeout.md)):** a change that is behavior-identical on the game path is not a boundary — but the proof is *empirical, not argued* (same seed set on both jars → identical `forkcheck` trace hashes; any mismatch ⇒ boundary). A crash fix never qualifies; swapping our fork-local fix for an upstream fix of the same defect does. Upstream PRs stay small, tested, human-reviewed.
- **Long-running jobs launch at low priority** (`nice -n 19`) so the desktop preempts them; harnesses are designed for graceful stop + seeded game-granularity resume (design §9, "disposable workers"). Calibrated measurements are the exception: schedule them on a quiet box instead.
- Python ~60% / Java ~40% overall; Java work is archaeology in the Forge fork, not here.

## Repo layout

- `anvil/` — Python package; subpackages map to design-doc sections (`encoder` §1, `state` §2, `policy` §3, `heads` §4, `tutor` §5, `training`+`grindstone` §6, `ante` §7, `bridge`+`store` §9, `pool` §8, `evals` §7, `mentor` §11). Skeleton only until M1; M0 work concentrated in `bridge/`.
- `docs/design/` — canonical design docs.
- `docs/devlog/` — one dated file per working session (see workflow below).
- `docs/decisions/` — ADRs for anything that changes or resolves a design-doc question.
- `tests/` — pytest.

## Working session workflow

- **End of every session:** write `docs/devlog/YYYY-MM-DD.md` (copy `docs/devlog/TEMPLATE.md`): what was done, what broke, what the next session picks up. Claude writes this as part of wrapping up.
- **When a design question is resolved or a design-doc claim changes** (e.g., a probability from §15 gets measured, a bet flips): write an ADR in `docs/decisions/` (copy the template, next sequence number) and update the Status section above.
- **Keep `docs/project-map.html` current alongside the devlog/Status:** whenever a milestone opens/closes, a deliverable lands, or a headline number changes, update the map in the same commit batch. It's the living overview — a stale map is worse than none. Since the 2026-08-01 dashboard restructure, the parts that go stale FASTEST are at the top: the **Now panel** (current focus per track), the **state-of-record table** (ckpts/baseline/pool/fork pin/release), the **watch list**, the **run ledger** (one row per self-play run, verdict-colored — every closed run gets a row), and the footer stamp. On milestone/deliverable close, also sweep the embedded labels: flow-diagram box text, pill states, open-PR statuses, ADR index rows. Milestone entries are `<details>` (closed milestones stay default-collapsed with a one-line summary; the open milestone stays `open` with a done-when checklist + per-deliverable bullets — never a single prose wall).
- **Milestone close includes a stale-data deletion pass** (standing habit born in the M6 opening chore, 2026-08-05): inventory the closing milestone's forensic dumps / scratch dirs under `data/`, verify nothing operational references them (grep `docs/decisions/`, `scripts/`, `anvil/`; confirm calibration/drill tooling and ckpts of record live outside), present a kill list with per-dir size + justification, delete on user sign-off. Kept unconditionally: ckpts of record, drills.jsonl dirs, selection/evalset assets, baseline-era finalarm/confarm stores, Ante certs. Cheap-prune note: a forensic dump's bulk is usually one worker's `census.jsonl` — trimming that file is often enough.
- **Every run emits and we read its analysis battery by default** ([run-analysis-protocol.md](docs/design/run-analysis-protocol.md), pinned 2026-08-12): the driver/final_read auto-run `anvil/evals/battery.py`, `analysis.md` leads with ANOMALIES and the notify line carries them; session pickup after any run starts by opening its analysis report. Battery findings are exploratory only — verdicts stay pre-registered.
- Commit at natural checkpoints with plain descriptive messages; the devlog carries the narrative, not the commit log.
